//! Auto-generated API clients for gcp/compute.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET projects/{project}/aggregated/acceleratorTypes
/// Retrieves an aggregated list of accelerator types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_accelerator_types_aggregated_list_execute()` to send, or `compute_accelerator_types_aggregated_list` for simplest API.

pub fn compute_accelerator_types_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/acceleratorTypes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/acceleratorTypes
/// Retrieves an aggregated list of accelerator types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_accelerator_types_aggregated_list_execute()` or `compute_accelerator_types_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_accelerator_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AcceleratorTypeAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AcceleratorTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/acceleratorTypes
/// Retrieves an aggregated list of accelerator types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_accelerator_types_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_accelerator_types_aggregated_list_task()`.
/// For the simplest API, use `compute_accelerator_types_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_accelerator_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_accelerator_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AcceleratorTypeAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_accelerator_types_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_accelerator_types_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAcceleratorTypesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/acceleratorTypes
/// Retrieves an aggregated list of accelerator types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_accelerator_types_aggregated_list_builder()` + `compute_accelerator_types_aggregated_list_execute()`.
/// For task-level control, use `compute_accelerator_types_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeAcceleratorTypesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AcceleratorTypeAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_accelerator_types_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_accelerator_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}
/// Returns the specified accelerator type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_accelerator_types_get_execute()` to send, or `compute_accelerator_types_get` for simplest API.

pub fn compute_accelerator_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    acceleratorType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/acceleratorTypes/{}",
        project, zone, acceleratorType,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}
/// Returns the specified accelerator type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_accelerator_types_get_execute()` or `compute_accelerator_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_accelerator_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AcceleratorType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AcceleratorType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}
/// Returns the specified accelerator type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_accelerator_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_accelerator_types_get_task()`.
/// For the simplest API, use `compute_accelerator_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_accelerator_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_accelerator_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_accelerator_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_accelerator_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAcceleratorTypesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: acceleratorType
    pub acceleratorType: String,
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}
/// Returns the specified accelerator type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_accelerator_types_get_builder()` + `compute_accelerator_types_get_execute()`.
/// For task-level control, use `compute_accelerator_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_get(
    client: &SimpleHttpClient,
    args: &ComputeAcceleratorTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_accelerator_types_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.acceleratorType,
    )?;
    compute_accelerator_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes
/// Retrieves a list of accelerator types that are available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_accelerator_types_list_execute()` to send, or `compute_accelerator_types_list` for simplest API.

pub fn compute_accelerator_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/acceleratorTypes",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes
/// Retrieves a list of accelerator types that are available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_accelerator_types_list_execute()` or `compute_accelerator_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_accelerator_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AcceleratorTypeList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AcceleratorTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes
/// Retrieves a list of accelerator types that are available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_accelerator_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_accelerator_types_list_task()`.
/// For the simplest API, use `compute_accelerator_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_accelerator_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_accelerator_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_accelerator_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_accelerator_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAcceleratorTypesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes
/// Retrieves a list of accelerator types that are available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_accelerator_types_list_builder()` + `compute_accelerator_types_list_execute()`.
/// For task-level control, use `compute_accelerator_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_list(
    client: &SimpleHttpClient,
    args: &ComputeAcceleratorTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_accelerator_types_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_accelerator_types_list_execute(builder)
}

/// GET projects/{project}/aggregated/addresses
/// Retrieves an aggregated list of addresses.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_aggregated_list_execute()` to send, or `compute_addresses_aggregated_list` for simplest API.

pub fn compute_addresses_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/addresses",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/addresses
/// Retrieves an aggregated list of addresses.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_addresses_aggregated_list_execute()` or `compute_addresses_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddressAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/addresses
/// Retrieves an aggregated list of addresses.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_addresses_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_addresses_aggregated_list_task()`.
/// For the simplest API, use `compute_addresses_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_addresses_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_addresses_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_addresses_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAddressesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/addresses
/// Retrieves an aggregated list of addresses.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_aggregated_list_builder()` + `compute_addresses_aggregated_list_execute()`.
/// For task-level control, use `compute_addresses_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeAddressesAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_addresses_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_addresses_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/addresses/{address}
/// Deletes the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_delete_execute()` to send, or `compute_addresses_delete` for simplest API.

pub fn compute_addresses_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    address: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}",
        project, region, address,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/addresses/{address}
/// Deletes the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_addresses_delete_execute()` or `compute_addresses_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/addresses/{address}
/// Deletes the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_addresses_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_addresses_delete_task()`.
/// For the simplest API, use `compute_addresses_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_addresses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_addresses_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_addresses_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAddressesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: address
    pub address: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/addresses/{address}
/// Deletes the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_delete_builder()` + `compute_addresses_delete_execute()`.
/// For task-level control, use `compute_addresses_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_delete(
    client: &SimpleHttpClient,
    args: &ComputeAddressesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_addresses_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.address,
        &args.requestId,
    )?;
    compute_addresses_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Returns the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_get_execute()` to send, or `compute_addresses_get` for simplest API.

pub fn compute_addresses_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    address: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}",
        project, region, address,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Returns the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_addresses_get_execute()` or `compute_addresses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Address>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Address = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Returns the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_addresses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_addresses_get_task()`.
/// For the simplest API, use `compute_addresses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_addresses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Address>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_addresses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_addresses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAddressesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: address
    pub address: String,
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Returns the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_get_builder()` + `compute_addresses_get_execute()`.
/// For task-level control, use `compute_addresses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_get(
    client: &SimpleHttpClient,
    args: &ComputeAddressesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Address>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_addresses_get_builder(client, &args.project, &args.region, &args.address)?;
    compute_addresses_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_insert_execute()` to send, or `compute_addresses_insert` for simplest API.

pub fn compute_addresses_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_addresses_insert_execute()` or `compute_addresses_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_addresses_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_addresses_insert_task()`.
/// For the simplest API, use `compute_addresses_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_addresses_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_addresses_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_addresses_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAddressesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_insert_builder()` + `compute_addresses_insert_execute()`.
/// For task-level control, use `compute_addresses_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_insert(
    client: &SimpleHttpClient,
    args: &ComputeAddressesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_addresses_insert_builder(client, &args.project, &args.region, &args.requestId)?;
    compute_addresses_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses
/// Retrieves a list of addresses contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_list_execute()` to send, or `compute_addresses_list` for simplest API.

pub fn compute_addresses_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/addresses
/// Retrieves a list of addresses contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_addresses_list_execute()` or `compute_addresses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddressList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/addresses
/// Retrieves a list of addresses contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_addresses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_addresses_list_task()`.
/// For the simplest API, use `compute_addresses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_addresses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_addresses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_addresses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAddressesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/addresses
/// Retrieves a list of addresses contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_list_builder()` + `compute_addresses_list_execute()`.
/// For task-level control, use `compute_addresses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_list(
    client: &SimpleHttpClient,
    args: &ComputeAddressesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_addresses_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_addresses_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/addresses/{address}/move
/// Moves the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_move_execute()` to send, or `compute_addresses_move` for simplest API.

pub fn compute_addresses_move_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    address: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}/move",
        project, region, address,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/addresses/{address}/move
/// Moves the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_addresses_move_execute()` or `compute_addresses_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/addresses/{address}/move
/// Moves the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_addresses_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_addresses_move_task()`.
/// For the simplest API, use `compute_addresses_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_addresses_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_addresses_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_addresses_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAddressesMoveArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: address
    pub address: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/addresses/{address}/move
/// Moves the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_move_builder()` + `compute_addresses_move_execute()`.
/// For task-level control, use `compute_addresses_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_move(
    client: &SimpleHttpClient,
    args: &ComputeAddressesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_addresses_move_builder(
        client,
        &args.project,
        &args.region,
        &args.address,
        &args.requestId,
    )?;
    compute_addresses_move_execute(builder)
}

/// POST projects/{project}/regions/{region}/addresses/{resource}/setLabels
/// Sets the labels on an Address. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_set_labels_execute()` to send, or `compute_addresses_set_labels` for simplest API.

pub fn compute_addresses_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}/setLabels",
        project, region, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/addresses/{resource}/setLabels
/// Sets the labels on an Address. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_addresses_set_labels_execute()` or `compute_addresses_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/addresses/{resource}/setLabels
/// Sets the labels on an Address. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_addresses_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_addresses_set_labels_task()`.
/// For the simplest API, use `compute_addresses_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_addresses_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_addresses_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_addresses_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAddressesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/addresses/{resource}/setLabels
/// Sets the labels on an Address. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_set_labels_builder()` + `compute_addresses_set_labels_execute()`.
/// For task-level control, use `compute_addresses_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeAddressesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_addresses_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_addresses_set_labels_execute(builder)
}

/// POST projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_test_iam_permissions_execute()` to send, or `compute_addresses_test_iam_permissions` for simplest API.

pub fn compute_addresses_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_addresses_test_iam_permissions_execute()` or `compute_addresses_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_addresses_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_addresses_test_iam_permissions_task()`.
/// For the simplest API, use `compute_addresses_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_addresses_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_addresses_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_addresses_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_addresses_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAddressesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_test_iam_permissions_builder()` + `compute_addresses_test_iam_permissions_execute()`.
/// For task-level control, use `compute_addresses_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeAddressesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_addresses_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_addresses_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/regions/{region}/advice/calendarMode
/// Advise how, where and when to create the requested amount of instances with specified accelerators, within the specified time and location limits. The method recommends creating future reservations for the requested resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_advice_calendar_mode_execute()` to send, or `compute_advice_calendar_mode` for simplest API.

pub fn compute_advice_calendar_mode_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/advice/calendarMode",
        project, region,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/advice/calendarMode
/// Advise how, where and when to create the requested amount of instances with specified accelerators, within the specified time and location limits. The method recommends creating future reservations for the requested resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_advice_calendar_mode_execute()` or `compute_advice_calendar_mode`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_advice_calendar_mode_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_advice_calendar_mode_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CalendarModeAdviceResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CalendarModeAdviceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/advice/calendarMode
/// Advise how, where and when to create the requested amount of instances with specified accelerators, within the specified time and location limits. The method recommends creating future reservations for the requested resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_advice_calendar_mode_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_advice_calendar_mode_task()`.
/// For the simplest API, use `compute_advice_calendar_mode()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_advice_calendar_mode_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_advice_calendar_mode_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CalendarModeAdviceResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_advice_calendar_mode_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_advice_calendar_mode`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAdviceCalendarModeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
}

/// POST projects/{project}/regions/{region}/advice/calendarMode
/// Advise how, where and when to create the requested amount of instances with specified accelerators, within the specified time and location limits. The method recommends creating future reservations for the requested resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_advice_calendar_mode_builder()` + `compute_advice_calendar_mode_execute()`.
/// For task-level control, use `compute_advice_calendar_mode_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_advice_calendar_mode(
    client: &SimpleHttpClient,
    args: &ComputeAdviceCalendarModeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CalendarModeAdviceResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_advice_calendar_mode_builder(client, &args.project, &args.region)?;
    compute_advice_calendar_mode_execute(builder)
}

/// GET projects/{project}/aggregated/autoscalers
/// Retrieves an aggregated list of autoscalers.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_aggregated_list_execute()` to send, or `compute_autoscalers_aggregated_list` for simplest API.

pub fn compute_autoscalers_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/autoscalers",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/autoscalers
/// Retrieves an aggregated list of autoscalers.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_autoscalers_aggregated_list_execute()` or `compute_autoscalers_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalerAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalerAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/autoscalers
/// Retrieves an aggregated list of autoscalers.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_autoscalers_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_autoscalers_aggregated_list_task()`.
/// For the simplest API, use `compute_autoscalers_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_autoscalers_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalerAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_autoscalers_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_autoscalers_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAutoscalersAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/autoscalers
/// Retrieves an aggregated list of autoscalers.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_aggregated_list_builder()` + `compute_autoscalers_aggregated_list_execute()`.
/// For task-level control, use `compute_autoscalers_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeAutoscalersAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalerAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_autoscalers_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_delete_execute()` to send, or `compute_autoscalers_delete` for simplest API.

pub fn compute_autoscalers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    autoscaler: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers/{}",
        project, zone, autoscaler,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_autoscalers_delete_execute()` or `compute_autoscalers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_autoscalers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_autoscalers_delete_task()`.
/// For the simplest API, use `compute_autoscalers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_autoscalers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_autoscalers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_autoscalers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAutoscalersDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: autoscaler
    pub autoscaler: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_delete_builder()` + `compute_autoscalers_delete_execute()`.
/// For task-level control, use `compute_autoscalers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_delete(
    client: &SimpleHttpClient,
    args: &ComputeAutoscalersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.autoscaler,
        &args.requestId,
    )?;
    compute_autoscalers_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Returns the specified autoscaler resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_get_execute()` to send, or `compute_autoscalers_get` for simplest API.

pub fn compute_autoscalers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    autoscaler: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers/{}",
        project, zone, autoscaler,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Returns the specified autoscaler resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_autoscalers_get_execute()` or `compute_autoscalers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Autoscaler>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Autoscaler = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Returns the specified autoscaler resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_autoscalers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_autoscalers_get_task()`.
/// For the simplest API, use `compute_autoscalers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_autoscalers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Autoscaler>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_autoscalers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_autoscalers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAutoscalersGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: autoscaler
    pub autoscaler: String,
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Returns the specified autoscaler resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_get_builder()` + `compute_autoscalers_get_execute()`.
/// For task-level control, use `compute_autoscalers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_get(
    client: &SimpleHttpClient,
    args: &ComputeAutoscalersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Autoscaler>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_autoscalers_get_builder(client, &args.project, &args.zone, &args.autoscaler)?;
    compute_autoscalers_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_insert_execute()` to send, or `compute_autoscalers_insert` for simplest API.

pub fn compute_autoscalers_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_autoscalers_insert_execute()` or `compute_autoscalers_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_autoscalers_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_autoscalers_insert_task()`.
/// For the simplest API, use `compute_autoscalers_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_autoscalers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_autoscalers_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_autoscalers_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAutoscalersInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_insert_builder()` + `compute_autoscalers_insert_execute()`.
/// For task-level control, use `compute_autoscalers_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_insert(
    client: &SimpleHttpClient,
    args: &ComputeAutoscalersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_autoscalers_insert_builder(client, &args.project, &args.zone, &args.requestId)?;
    compute_autoscalers_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Retrieves a list of autoscalers contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_list_execute()` to send, or `compute_autoscalers_list` for simplest API.

pub fn compute_autoscalers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Retrieves a list of autoscalers contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_autoscalers_list_execute()` or `compute_autoscalers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalerList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Retrieves a list of autoscalers contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_autoscalers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_autoscalers_list_task()`.
/// For the simplest API, use `compute_autoscalers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_autoscalers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_autoscalers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_autoscalers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAutoscalersListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Retrieves a list of autoscalers contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_list_builder()` + `compute_autoscalers_list_execute()`.
/// For task-level control, use `compute_autoscalers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_list(
    client: &SimpleHttpClient,
    args: &ComputeAutoscalersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_autoscalers_list_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_patch_execute()` to send, or `compute_autoscalers_patch` for simplest API.

pub fn compute_autoscalers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    autoscaler: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = autoscaler.as_ref() {
        query_parts.push(format!("autoscaler={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_autoscalers_patch_execute()` or `compute_autoscalers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_autoscalers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_autoscalers_patch_task()`.
/// For the simplest API, use `compute_autoscalers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_autoscalers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_autoscalers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_autoscalers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAutoscalersPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: autoscaler
    pub autoscaler: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_patch_builder()` + `compute_autoscalers_patch_execute()`.
/// For task-level control, use `compute_autoscalers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_patch(
    client: &SimpleHttpClient,
    args: &ComputeAutoscalersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_patch_builder(
        client,
        &args.project,
        &args.zone,
        &args.autoscaler,
        &args.requestId,
    )?;
    compute_autoscalers_patch_execute(builder)
}

/// POST projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_test_iam_permissions_execute()` to send, or `compute_autoscalers_test_iam_permissions` for simplest API.

pub fn compute_autoscalers_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_autoscalers_test_iam_permissions_execute()` or `compute_autoscalers_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_autoscalers_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_autoscalers_test_iam_permissions_task()`.
/// For the simplest API, use `compute_autoscalers_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_autoscalers_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_autoscalers_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_autoscalers_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAutoscalersTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_test_iam_permissions_builder()` + `compute_autoscalers_test_iam_permissions_execute()`.
/// For task-level control, use `compute_autoscalers_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeAutoscalersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_autoscalers_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_update_execute()` to send, or `compute_autoscalers_update` for simplest API.

pub fn compute_autoscalers_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    autoscaler: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = autoscaler.as_ref() {
        query_parts.push(format!("autoscaler={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_autoscalers_update_execute()` or `compute_autoscalers_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_autoscalers_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_autoscalers_update_task()`.
/// For the simplest API, use `compute_autoscalers_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_autoscalers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_autoscalers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_autoscalers_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_autoscalers_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeAutoscalersUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: autoscaler
    pub autoscaler: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_update_builder()` + `compute_autoscalers_update_execute()`.
/// For task-level control, use `compute_autoscalers_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_update(
    client: &SimpleHttpClient,
    args: &ComputeAutoscalersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_update_builder(
        client,
        &args.project,
        &args.zone,
        &args.autoscaler,
        &args.requestId,
    )?;
    compute_autoscalers_update_execute(builder)
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_add_signed_url_key_execute()` to send, or `compute_backend_buckets_add_signed_url_key` for simplest API.

pub fn compute_backend_buckets_add_signed_url_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendBucket: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/addSignedUrlKey",
        project,
        backendBucket,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_add_signed_url_key_execute()` or `compute_backend_buckets_add_signed_url_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_add_signed_url_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_add_signed_url_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_add_signed_url_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_add_signed_url_key_task()`.
/// For the simplest API, use `compute_backend_buckets_add_signed_url_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_add_signed_url_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_add_signed_url_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_add_signed_url_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_add_signed_url_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsAddSignedUrlKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_add_signed_url_key_builder()` + `compute_backend_buckets_add_signed_url_key_execute()`.
/// For task-level control, use `compute_backend_buckets_add_signed_url_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_add_signed_url_key(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsAddSignedUrlKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_add_signed_url_key_builder(
        client,
        &args.project,
        &args.backendBucket,
        &args.requestId,
    )?;
    compute_backend_buckets_add_signed_url_key_execute(builder)
}

/// GET projects/{project}/aggregated/backendBuckets
/// Retrieves the list of all BackendBucket resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_aggregated_list_execute()` to send, or `compute_backend_buckets_aggregated_list` for simplest API.

pub fn compute_backend_buckets_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/backendBuckets",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/backendBuckets
/// Retrieves the list of all BackendBucket resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_aggregated_list_execute()` or `compute_backend_buckets_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucketAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/backendBuckets
/// Retrieves the list of all BackendBucket resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_aggregated_list_task()`.
/// For the simplest API, use `compute_backend_buckets_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BackendBucketAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/backendBuckets
/// Retrieves the list of all BackendBucket resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_aggregated_list_builder()` + `compute_backend_buckets_aggregated_list_execute()`.
/// For task-level control, use `compute_backend_buckets_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BackendBucketAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_backend_buckets_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/backendBuckets/{backendBucket}
/// Deletes the specified BackendBucket resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_delete_execute()` to send, or `compute_backend_buckets_delete` for simplest API.

pub fn compute_backend_buckets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendBucket: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}",
        project, backendBucket,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/backendBuckets/{backendBucket}
/// Deletes the specified BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_delete_execute()` or `compute_backend_buckets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/backendBuckets/{backendBucket}
/// Deletes the specified BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_delete_task()`.
/// For the simplest API, use `compute_backend_buckets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/backendBuckets/{backendBucket}
/// Deletes the specified BackendBucket resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_delete_builder()` + `compute_backend_buckets_delete_execute()`.
/// For task-level control, use `compute_backend_buckets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_delete(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_delete_builder(
        client,
        &args.project,
        &args.backendBucket,
        &args.requestId,
    )?;
    compute_backend_buckets_delete_execute(builder)
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_delete_signed_url_key_execute()` to send, or `compute_backend_buckets_delete_signed_url_key` for simplest API.

pub fn compute_backend_buckets_delete_signed_url_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendBucket: &String,
    keyName: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/deleteSignedUrlKey",
        project,
        backendBucket,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyName.as_ref() {
        query_parts.push(format!("keyName={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_delete_signed_url_key_execute()` or `compute_backend_buckets_delete_signed_url_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_delete_signed_url_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_delete_signed_url_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_delete_signed_url_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_delete_signed_url_key_task()`.
/// For the simplest API, use `compute_backend_buckets_delete_signed_url_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_delete_signed_url_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_delete_signed_url_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_delete_signed_url_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_delete_signed_url_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsDeleteSignedUrlKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
    /// Query parameter: keyName
    pub keyName: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_delete_signed_url_key_builder()` + `compute_backend_buckets_delete_signed_url_key_execute()`.
/// For task-level control, use `compute_backend_buckets_delete_signed_url_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_delete_signed_url_key(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsDeleteSignedUrlKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_delete_signed_url_key_builder(
        client,
        &args.project,
        &args.backendBucket,
        &args.keyName,
        &args.requestId,
    )?;
    compute_backend_buckets_delete_signed_url_key_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Returns the specified BackendBucket resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_get_execute()` to send, or `compute_backend_buckets_get` for simplest API.

pub fn compute_backend_buckets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendBucket: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}",
        project, backendBucket,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Returns the specified BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_get_execute()` or `compute_backend_buckets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Returns the specified BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_get_task()`.
/// For the simplest API, use `compute_backend_buckets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucket>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Returns the specified BackendBucket resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_get_builder()` + `compute_backend_buckets_get_execute()`.
/// For task-level control, use `compute_backend_buckets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_get(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucket>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_get_builder(client, &args.project, &args.backendBucket)?;
    compute_backend_buckets_get_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_get_iam_policy_execute()` to send, or `compute_backend_buckets_get_iam_policy` for simplest API.

pub fn compute_backend_buckets_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/getIamPolicy",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_get_iam_policy_execute()` or `compute_backend_buckets_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_get_iam_policy_task()`.
/// For the simplest API, use `compute_backend_buckets_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_get_iam_policy_builder()` + `compute_backend_buckets_get_iam_policy_execute()`.
/// For task-level control, use `compute_backend_buckets_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_backend_buckets_get_iam_policy_execute(builder)
}

/// POST projects/{project}/global/backendBuckets
/// Creates a BackendBucket resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_insert_execute()` to send, or `compute_backend_buckets_insert` for simplest API.

pub fn compute_backend_buckets_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendBuckets
/// Creates a BackendBucket resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_insert_execute()` or `compute_backend_buckets_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendBuckets
/// Creates a BackendBucket resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_insert_task()`.
/// For the simplest API, use `compute_backend_buckets_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendBuckets
/// Creates a BackendBucket resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_insert_builder()` + `compute_backend_buckets_insert_execute()`.
/// For task-level control, use `compute_backend_buckets_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_insert(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_insert_builder(client, &args.project, &args.requestId)?;
    compute_backend_buckets_insert_execute(builder)
}

/// GET projects/{project}/global/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_list_execute()` to send, or `compute_backend_buckets_list` for simplest API.

pub fn compute_backend_buckets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_list_execute()` or `compute_backend_buckets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucketList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_list_task()`.
/// For the simplest API, use `compute_backend_buckets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_list_builder()` + `compute_backend_buckets_list_execute()`.
/// For task-level control, use `compute_backend_buckets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_list(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_backend_buckets_list_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/listUsable
/// Retrieves a list of all usable backend buckets in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_list_usable_execute()` to send, or `compute_backend_buckets_list_usable` for simplest API.

pub fn compute_backend_buckets_list_usable_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/listUsable",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendBuckets/listUsable
/// Retrieves a list of all usable backend buckets in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_list_usable_execute()` or `compute_backend_buckets_list_usable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_list_usable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketListUsable>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucketListUsable = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendBuckets/listUsable
/// Retrieves a list of all usable backend buckets in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_list_usable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_list_usable_task()`.
/// For the simplest API, use `compute_backend_buckets_list_usable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_list_usable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_list_usable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_list_usable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsListUsableArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/backendBuckets/listUsable
/// Retrieves a list of all usable backend buckets in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_list_usable_builder()` + `compute_backend_buckets_list_usable_execute()`.
/// For task-level control, use `compute_backend_buckets_list_usable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_list_usable(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsListUsableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_list_usable_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_backend_buckets_list_usable_execute(builder)
}

/// PATCH projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_patch_execute()` to send, or `compute_backend_buckets_patch` for simplest API.

pub fn compute_backend_buckets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendBucket: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}",
        project, backendBucket,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_patch_execute()` or `compute_backend_buckets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_patch_task()`.
/// For the simplest API, use `compute_backend_buckets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_patch_builder()` + `compute_backend_buckets_patch_execute()`.
/// For task-level control, use `compute_backend_buckets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_patch(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_patch_builder(
        client,
        &args.project,
        &args.backendBucket,
        &args.requestId,
    )?;
    compute_backend_buckets_patch_execute(builder)
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_set_edge_security_policy_execute()` to send, or `compute_backend_buckets_set_edge_security_policy` for simplest API.

pub fn compute_backend_buckets_set_edge_security_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendBucket: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/setEdgeSecurityPolicy",
        project,
        backendBucket,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_set_edge_security_policy_execute()` or `compute_backend_buckets_set_edge_security_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_set_edge_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_set_edge_security_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_set_edge_security_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_set_edge_security_policy_task()`.
/// For the simplest API, use `compute_backend_buckets_set_edge_security_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_set_edge_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_set_edge_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_set_edge_security_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_set_edge_security_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsSetEdgeSecurityPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_set_edge_security_policy_builder()` + `compute_backend_buckets_set_edge_security_policy_execute()`.
/// For task-level control, use `compute_backend_buckets_set_edge_security_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_set_edge_security_policy(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsSetEdgeSecurityPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_set_edge_security_policy_builder(
        client,
        &args.project,
        &args.backendBucket,
        &args.requestId,
    )?;
    compute_backend_buckets_set_edge_security_policy_execute(builder)
}

/// POST projects/{project}/global/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_set_iam_policy_execute()` to send, or `compute_backend_buckets_set_iam_policy` for simplest API.

pub fn compute_backend_buckets_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/setIamPolicy",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_set_iam_policy_execute()` or `compute_backend_buckets_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_set_iam_policy_task()`.
/// For the simplest API, use `compute_backend_buckets_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_set_iam_policy_builder()` + `compute_backend_buckets_set_iam_policy_execute()`.
/// For task-level control, use `compute_backend_buckets_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_backend_buckets_set_iam_policy_builder(client, &args.project, &args.resource)?;
    compute_backend_buckets_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_test_iam_permissions_execute()` to send, or `compute_backend_buckets_test_iam_permissions` for simplest API.

pub fn compute_backend_buckets_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_test_iam_permissions_execute()` or `compute_backend_buckets_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_test_iam_permissions_task()`.
/// For the simplest API, use `compute_backend_buckets_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_test_iam_permissions_builder()` + `compute_backend_buckets_test_iam_permissions_execute()`.
/// For task-level control, use `compute_backend_buckets_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_backend_buckets_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_update_execute()` to send, or `compute_backend_buckets_update` for simplest API.

pub fn compute_backend_buckets_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendBucket: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}",
        project, backendBucket,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_buckets_update_execute()` or `compute_backend_buckets_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_buckets_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_buckets_update_task()`.
/// For the simplest API, use `compute_backend_buckets_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_buckets_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_buckets_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_buckets_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_buckets_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendBucketsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_update_builder()` + `compute_backend_buckets_update_execute()`.
/// For task-level control, use `compute_backend_buckets_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_update(
    client: &SimpleHttpClient,
    args: &ComputeBackendBucketsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_update_builder(
        client,
        &args.project,
        &args.backendBucket,
        &args.requestId,
    )?;
    compute_backend_buckets_update_execute(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_add_signed_url_key_execute()` to send, or `compute_backend_services_add_signed_url_key` for simplest API.

pub fn compute_backend_services_add_signed_url_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/addSignedUrlKey",
        project,
        backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_add_signed_url_key_execute()` or `compute_backend_services_add_signed_url_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_add_signed_url_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_add_signed_url_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendServices/{backendService}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_add_signed_url_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_add_signed_url_key_task()`.
/// For the simplest API, use `compute_backend_services_add_signed_url_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_add_signed_url_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_add_signed_url_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_add_signed_url_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_add_signed_url_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesAddSignedUrlKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendServices/{backendService}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_add_signed_url_key_builder()` + `compute_backend_services_add_signed_url_key_execute()`.
/// For task-level control, use `compute_backend_services_add_signed_url_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_add_signed_url_key(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesAddSignedUrlKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_add_signed_url_key_builder(
        client,
        &args.project,
        &args.backendService,
        &args.requestId,
    )?;
    compute_backend_services_add_signed_url_key_execute(builder)
}

/// GET projects/{project}/aggregated/backendServices
/// Retrieves the list of all BackendService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_aggregated_list_execute()` to send, or `compute_backend_services_aggregated_list` for simplest API.

pub fn compute_backend_services_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/backendServices",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/backendServices
/// Retrieves the list of all BackendService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_aggregated_list_execute()` or `compute_backend_services_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/backendServices
/// Retrieves the list of all BackendService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_aggregated_list_task()`.
/// For the simplest API, use `compute_backend_services_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BackendServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_services_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/backendServices
/// Retrieves the list of all BackendService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_aggregated_list_builder()` + `compute_backend_services_aggregated_list_execute()`.
/// For task-level control, use `compute_backend_services_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BackendServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_services_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_backend_services_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/backendServices/{backendService}
/// Deletes the specified BackendService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_delete_execute()` to send, or `compute_backend_services_delete` for simplest API.

pub fn compute_backend_services_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}",
        project, backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/backendServices/{backendService}
/// Deletes the specified BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_delete_execute()` or `compute_backend_services_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/backendServices/{backendService}
/// Deletes the specified BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_delete_task()`.
/// For the simplest API, use `compute_backend_services_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/backendServices/{backendService}
/// Deletes the specified BackendService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_delete_builder()` + `compute_backend_services_delete_execute()`.
/// For task-level control, use `compute_backend_services_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_delete(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_delete_builder(
        client,
        &args.project,
        &args.backendService,
        &args.requestId,
    )?;
    compute_backend_services_delete_execute(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_delete_signed_url_key_execute()` to send, or `compute_backend_services_delete_signed_url_key` for simplest API.

pub fn compute_backend_services_delete_signed_url_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
    keyName: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/deleteSignedUrlKey",
        project,
        backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyName.as_ref() {
        query_parts.push(format!("keyName={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_delete_signed_url_key_execute()` or `compute_backend_services_delete_signed_url_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_delete_signed_url_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_delete_signed_url_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_delete_signed_url_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_delete_signed_url_key_task()`.
/// For the simplest API, use `compute_backend_services_delete_signed_url_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_delete_signed_url_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_delete_signed_url_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_delete_signed_url_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_delete_signed_url_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesDeleteSignedUrlKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: keyName
    pub keyName: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_delete_signed_url_key_builder()` + `compute_backend_services_delete_signed_url_key_execute()`.
/// For task-level control, use `compute_backend_services_delete_signed_url_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_delete_signed_url_key(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesDeleteSignedUrlKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_delete_signed_url_key_builder(
        client,
        &args.project,
        &args.backendService,
        &args.keyName,
        &args.requestId,
    )?;
    compute_backend_services_delete_signed_url_key_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Returns the specified BackendService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_get_execute()` to send, or `compute_backend_services_get` for simplest API.

pub fn compute_backend_services_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}",
        project, backendService,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Returns the specified BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_get_execute()` or `compute_backend_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendService>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Returns the specified BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_get_task()`.
/// For the simplest API, use `compute_backend_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Returns the specified BackendService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_get_builder()` + `compute_backend_services_get_execute()`.
/// For task-level control, use `compute_backend_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_backend_services_get_builder(client, &args.project, &args.backendService)?;
    compute_backend_services_get_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies
/// Returns effective security policies applied to this backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_get_effective_security_policies_execute()` to send, or `compute_backend_services_get_effective_security_policies` for simplest API.

pub fn compute_backend_services_get_effective_security_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/getEffectiveSecurityPolicies",
        project,
        backendService,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies
/// Returns effective security policies applied to this backend service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_get_effective_security_policies_execute()` or `compute_backend_services_get_effective_security_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_get_effective_security_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_effective_security_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies
/// Returns effective security policies applied to this backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_get_effective_security_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_get_effective_security_policies_task()`.
/// For the simplest API, use `compute_backend_services_get_effective_security_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_get_effective_security_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_get_effective_security_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_get_effective_security_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_get_effective_security_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesGetEffectiveSecurityPoliciesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
}

/// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies
/// Returns effective security policies applied to this backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_get_effective_security_policies_builder()` + `compute_backend_services_get_effective_security_policies_execute()`.
/// For task-level control, use `compute_backend_services_get_effective_security_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_effective_security_policies(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesGetEffectiveSecurityPoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_get_effective_security_policies_builder(
        client,
        &args.project,
        &args.backendService,
    )?;
    compute_backend_services_get_effective_security_policies_execute(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this BackendService.  Example request body:  { "group": "/`zones/us-east1-b/`instanceGroups`/lb-backend-example`" }
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_get_health_execute()` to send, or `compute_backend_services_get_health` for simplest API.

pub fn compute_backend_services_get_health_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/getHealth",
        project, backendService,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this BackendService.  Example request body:  { "group": "/`zones/us-east1-b/`instanceGroups`/lb-backend-example`" }
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_get_health_execute()` or `compute_backend_services_get_health`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_health_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceGroupHealth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this BackendService.  Example request body:  { "group": "/`zones/us-east1-b/`instanceGroups`/lb-backend-example`" }
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_get_health_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_get_health_task()`.
/// For the simplest API, use `compute_backend_services_get_health()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_get_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_services_get_health_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_get_health`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesGetHealthArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
}

/// POST projects/{project}/global/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this BackendService.  Example request body:  { "group": "/`zones/us-east1-b/`instanceGroups`/lb-backend-example`" }
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_get_health_builder()` + `compute_backend_services_get_health_execute()`.
/// For task-level control, use `compute_backend_services_get_health_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_health(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesGetHealthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_backend_services_get_health_builder(client, &args.project, &args.backendService)?;
    compute_backend_services_get_health_execute(builder)
}

/// GET projects/{project}/global/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_get_iam_policy_execute()` to send, or `compute_backend_services_get_iam_policy` for simplest API.

pub fn compute_backend_services_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/getIamPolicy",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_get_iam_policy_execute()` or `compute_backend_services_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_get_iam_policy_task()`.
/// For the simplest API, use `compute_backend_services_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_get_iam_policy_builder()` + `compute_backend_services_get_iam_policy_execute()`.
/// For task-level control, use `compute_backend_services_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_backend_services_get_iam_policy_execute(builder)
}

/// POST projects/{project}/global/backendServices
/// Creates a BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_insert_execute()` to send, or `compute_backend_services_insert` for simplest API.

pub fn compute_backend_services_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendServices
/// Creates a BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_insert_execute()` or `compute_backend_services_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendServices
/// Creates a BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_insert_task()`.
/// For the simplest API, use `compute_backend_services_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendServices
/// Creates a BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_insert_builder()` + `compute_backend_services_insert_execute()`.
/// For task-level control, use `compute_backend_services_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_insert(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_insert_builder(client, &args.project, &args.requestId)?;
    compute_backend_services_insert_execute(builder)
}

/// GET projects/{project}/global/backendServices
/// Retrieves the list of BackendService resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_list_execute()` to send, or `compute_backend_services_list` for simplest API.

pub fn compute_backend_services_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendServices
/// Retrieves the list of BackendService resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_list_execute()` or `compute_backend_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendServices
/// Retrieves the list of BackendService resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_list_task()`.
/// For the simplest API, use `compute_backend_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/backendServices
/// Retrieves the list of BackendService resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_list_builder()` + `compute_backend_services_list_execute()`.
/// For task-level control, use `compute_backend_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_list(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_services_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_backend_services_list_execute(builder)
}

/// GET projects/{project}/global/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_list_usable_execute()` to send, or `compute_backend_services_list_usable` for simplest API.

pub fn compute_backend_services_list_usable_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/listUsable",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_list_usable_execute()` or `compute_backend_services_list_usable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_list_usable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceListUsable>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceListUsable = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_list_usable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_list_usable_task()`.
/// For the simplest API, use `compute_backend_services_list_usable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_list_usable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_services_list_usable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_list_usable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesListUsableArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_list_usable_builder()` + `compute_backend_services_list_usable_execute()`.
/// For task-level control, use `compute_backend_services_list_usable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_list_usable(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesListUsableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_services_list_usable_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_backend_services_list_usable_execute(builder)
}

/// PATCH projects/{project}/global/backendServices/{backendService}
/// Patches the specified BackendService resource with the data included in the request. For more information, see Backend services overview. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_patch_execute()` to send, or `compute_backend_services_patch` for simplest API.

pub fn compute_backend_services_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}",
        project, backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/backendServices/{backendService}
/// Patches the specified BackendService resource with the data included in the request. For more information, see Backend services overview. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_patch_execute()` or `compute_backend_services_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/backendServices/{backendService}
/// Patches the specified BackendService resource with the data included in the request. For more information, see Backend services overview. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_patch_task()`.
/// For the simplest API, use `compute_backend_services_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/backendServices/{backendService}
/// Patches the specified BackendService resource with the data included in the request. For more information, see Backend services overview. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_patch_builder()` + `compute_backend_services_patch_execute()`.
/// For task-level control, use `compute_backend_services_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_patch(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_patch_builder(
        client,
        &args.project,
        &args.backendService,
        &args.requestId,
    )?;
    compute_backend_services_patch_execute(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_set_edge_security_policy_execute()` to send, or `compute_backend_services_set_edge_security_policy` for simplest API.

pub fn compute_backend_services_set_edge_security_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/setEdgeSecurityPolicy",
        project,
        backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_set_edge_security_policy_execute()` or `compute_backend_services_set_edge_security_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_set_edge_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_edge_security_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_set_edge_security_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_set_edge_security_policy_task()`.
/// For the simplest API, use `compute_backend_services_set_edge_security_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_set_edge_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_set_edge_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_set_edge_security_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_set_edge_security_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesSetEdgeSecurityPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_set_edge_security_policy_builder()` + `compute_backend_services_set_edge_security_policy_execute()`.
/// For task-level control, use `compute_backend_services_set_edge_security_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_edge_security_policy(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesSetEdgeSecurityPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_set_edge_security_policy_builder(
        client,
        &args.project,
        &args.backendService,
        &args.requestId,
    )?;
    compute_backend_services_set_edge_security_policy_execute(builder)
}

/// POST projects/{project}/global/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_set_iam_policy_execute()` to send, or `compute_backend_services_set_iam_policy` for simplest API.

pub fn compute_backend_services_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/setIamPolicy",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_set_iam_policy_execute()` or `compute_backend_services_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_set_iam_policy_task()`.
/// For the simplest API, use `compute_backend_services_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_set_iam_policy_builder()` + `compute_backend_services_set_iam_policy_execute()`.
/// For task-level control, use `compute_backend_services_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_backend_services_set_iam_policy_builder(client, &args.project, &args.resource)?;
    compute_backend_services_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_set_security_policy_execute()` to send, or `compute_backend_services_set_security_policy` for simplest API.

pub fn compute_backend_services_set_security_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/setSecurityPolicy",
        project,
        backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_set_security_policy_execute()` or `compute_backend_services_set_security_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_security_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_set_security_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_set_security_policy_task()`.
/// For the simplest API, use `compute_backend_services_set_security_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_set_security_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_set_security_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesSetSecurityPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_set_security_policy_builder()` + `compute_backend_services_set_security_policy_execute()`.
/// For task-level control, use `compute_backend_services_set_security_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_security_policy(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesSetSecurityPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_set_security_policy_builder(
        client,
        &args.project,
        &args.backendService,
        &args.requestId,
    )?;
    compute_backend_services_set_security_policy_execute(builder)
}

/// POST projects/{project}/global/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_test_iam_permissions_execute()` to send, or `compute_backend_services_test_iam_permissions` for simplest API.

pub fn compute_backend_services_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_test_iam_permissions_execute()` or `compute_backend_services_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_test_iam_permissions_task()`.
/// For the simplest API, use `compute_backend_services_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_backend_services_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_test_iam_permissions_builder()` + `compute_backend_services_test_iam_permissions_execute()`.
/// For task-level control, use `compute_backend_services_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_services_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_backend_services_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/global/backendServices/{backendService}
/// Updates the specified BackendService resource with the data included in the request. For more information, `seeBackend` services overview.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_update_execute()` to send, or `compute_backend_services_update` for simplest API.

pub fn compute_backend_services_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}",
        project, backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/global/backendServices/{backendService}
/// Updates the specified BackendService resource with the data included in the request. For more information, `seeBackend` services overview.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_backend_services_update_execute()` or `compute_backend_services_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/global/backendServices/{backendService}
/// Updates the specified BackendService resource with the data included in the request. For more information, `seeBackend` services overview.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_backend_services_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_backend_services_update_task()`.
/// For the simplest API, use `compute_backend_services_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_backend_services_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_backend_services_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_backend_services_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_backend_services_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeBackendServicesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/global/backendServices/{backendService}
/// Updates the specified BackendService resource with the data included in the request. For more information, `seeBackend` services overview.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_update_builder()` + `compute_backend_services_update_execute()`.
/// For task-level control, use `compute_backend_services_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_update(
    client: &SimpleHttpClient,
    args: &ComputeBackendServicesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_update_builder(
        client,
        &args.project,
        &args.backendService,
        &args.requestId,
    )?;
    compute_backend_services_update_execute(builder)
}

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Deletes the specified cross-site network in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_delete_execute()` to send, or `compute_cross_site_networks_delete` for simplest API.

pub fn compute_cross_site_networks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    crossSiteNetwork: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}",
        project, crossSiteNetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Deletes the specified cross-site network in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_cross_site_networks_delete_execute()` or `compute_cross_site_networks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Deletes the specified cross-site network in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_cross_site_networks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_cross_site_networks_delete_task()`.
/// For the simplest API, use `compute_cross_site_networks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_cross_site_networks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_cross_site_networks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_cross_site_networks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: crossSiteNetwork
    pub crossSiteNetwork: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Deletes the specified cross-site network in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_delete_builder()` + `compute_cross_site_networks_delete_execute()`.
/// For task-level control, use `compute_cross_site_networks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_delete(
    client: &SimpleHttpClient,
    args: &ComputeCrossSiteNetworksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_cross_site_networks_delete_builder(
        client,
        &args.project,
        &args.crossSiteNetwork,
        &args.requestId,
    )?;
    compute_cross_site_networks_delete_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Returns the specified cross-site network in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_get_execute()` to send, or `compute_cross_site_networks_get` for simplest API.

pub fn compute_cross_site_networks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    crossSiteNetwork: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}",
        project, crossSiteNetwork,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Returns the specified cross-site network in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_cross_site_networks_get_execute()` or `compute_cross_site_networks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CrossSiteNetwork>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CrossSiteNetwork = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Returns the specified cross-site network in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_cross_site_networks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_cross_site_networks_get_task()`.
/// For the simplest API, use `compute_cross_site_networks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_cross_site_networks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CrossSiteNetwork>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_cross_site_networks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_cross_site_networks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: crossSiteNetwork
    pub crossSiteNetwork: String,
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Returns the specified cross-site network in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_get_builder()` + `compute_cross_site_networks_get_execute()`.
/// For task-level control, use `compute_cross_site_networks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_get(
    client: &SimpleHttpClient,
    args: &ComputeCrossSiteNetworksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CrossSiteNetwork>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_cross_site_networks_get_builder(client, &args.project, &args.crossSiteNetwork)?;
    compute_cross_site_networks_get_execute(builder)
}

/// POST projects/{project}/global/crossSiteNetworks
/// Creates a cross-site network in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_insert_execute()` to send, or `compute_cross_site_networks_insert` for simplest API.

pub fn compute_cross_site_networks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/crossSiteNetworks
/// Creates a cross-site network in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_cross_site_networks_insert_execute()` or `compute_cross_site_networks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/crossSiteNetworks
/// Creates a cross-site network in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_cross_site_networks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_cross_site_networks_insert_task()`.
/// For the simplest API, use `compute_cross_site_networks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_cross_site_networks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_cross_site_networks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_cross_site_networks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/global/crossSiteNetworks
/// Creates a cross-site network in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_insert_builder()` + `compute_cross_site_networks_insert_execute()`.
/// For task-level control, use `compute_cross_site_networks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_insert(
    client: &SimpleHttpClient,
    args: &ComputeCrossSiteNetworksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_cross_site_networks_insert_builder(
        client,
        &args.project,
        &args.requestId,
        &args.validateOnly,
    )?;
    compute_cross_site_networks_insert_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks
/// Lists the cross-site networks for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_list_execute()` to send, or `compute_cross_site_networks_list` for simplest API.

pub fn compute_cross_site_networks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/crossSiteNetworks
/// Lists the cross-site networks for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_cross_site_networks_list_execute()` or `compute_cross_site_networks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CrossSiteNetworkList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CrossSiteNetworkList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/crossSiteNetworks
/// Lists the cross-site networks for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_cross_site_networks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_cross_site_networks_list_task()`.
/// For the simplest API, use `compute_cross_site_networks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_cross_site_networks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CrossSiteNetworkList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_cross_site_networks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_cross_site_networks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/crossSiteNetworks
/// Lists the cross-site networks for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_list_builder()` + `compute_cross_site_networks_list_execute()`.
/// For task-level control, use `compute_cross_site_networks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_list(
    client: &SimpleHttpClient,
    args: &ComputeCrossSiteNetworksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CrossSiteNetworkList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_cross_site_networks_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_cross_site_networks_list_execute(builder)
}

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Updates the specified cross-site network with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_patch_execute()` to send, or `compute_cross_site_networks_patch` for simplest API.

pub fn compute_cross_site_networks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    crossSiteNetwork: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}",
        project, crossSiteNetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Updates the specified cross-site network with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_cross_site_networks_patch_execute()` or `compute_cross_site_networks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Updates the specified cross-site network with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_cross_site_networks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_cross_site_networks_patch_task()`.
/// For the simplest API, use `compute_cross_site_networks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_cross_site_networks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_cross_site_networks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_cross_site_networks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_cross_site_networks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: crossSiteNetwork
    pub crossSiteNetwork: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Updates the specified cross-site network with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_patch_builder()` + `compute_cross_site_networks_patch_execute()`.
/// For task-level control, use `compute_cross_site_networks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_patch(
    client: &SimpleHttpClient,
    args: &ComputeCrossSiteNetworksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_cross_site_networks_patch_builder(
        client,
        &args.project,
        &args.crossSiteNetwork,
        &args.requestId,
        &args.updateMask,
        &args.validateOnly,
    )?;
    compute_cross_site_networks_patch_execute(builder)
}

/// GET projects/{project}/aggregated/diskTypes
/// Retrieves an aggregated list of disk types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disk_types_aggregated_list_execute()` to send, or `compute_disk_types_aggregated_list` for simplest API.

pub fn compute_disk_types_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/diskTypes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/diskTypes
/// Retrieves an aggregated list of disk types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disk_types_aggregated_list_execute()` or `compute_disk_types_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disk_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskTypeAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/diskTypes
/// Retrieves an aggregated list of disk types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disk_types_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disk_types_aggregated_list_task()`.
/// For the simplest API, use `compute_disk_types_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disk_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disk_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_disk_types_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disk_types_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDiskTypesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/diskTypes
/// Retrieves an aggregated list of disk types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disk_types_aggregated_list_builder()` + `compute_disk_types_aggregated_list_execute()`.
/// For task-level control, use `compute_disk_types_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeDiskTypesAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_disk_types_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_disk_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/diskTypes/{diskType}
/// Returns the specified disk type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disk_types_get_execute()` to send, or `compute_disk_types_get` for simplest API.

pub fn compute_disk_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    diskType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/diskTypes/{}",
        project, zone, diskType,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/diskTypes/{diskType}
/// Returns the specified disk type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disk_types_get_execute()` or `compute_disk_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disk_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/diskTypes/{diskType}
/// Returns the specified disk type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disk_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disk_types_get_task()`.
/// For the simplest API, use `compute_disk_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disk_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disk_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disk_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disk_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDiskTypesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: diskType
    pub diskType: String,
}

/// GET projects/{project}/zones/{zone}/diskTypes/{diskType}
/// Returns the specified disk type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disk_types_get_builder()` + `compute_disk_types_get_execute()`.
/// For task-level control, use `compute_disk_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_get(
    client: &SimpleHttpClient,
    args: &ComputeDiskTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disk_types_get_builder(client, &args.project, &args.zone, &args.diskType)?;
    compute_disk_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/diskTypes
/// Retrieves a list of disk types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disk_types_list_execute()` to send, or `compute_disk_types_list` for simplest API.

pub fn compute_disk_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/diskTypes",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/diskTypes
/// Retrieves a list of disk types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disk_types_list_execute()` or `compute_disk_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disk_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskTypeList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/diskTypes
/// Retrieves a list of disk types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disk_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disk_types_list_task()`.
/// For the simplest API, use `compute_disk_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disk_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disk_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_disk_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disk_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDiskTypesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/diskTypes
/// Retrieves a list of disk types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disk_types_list_builder()` + `compute_disk_types_list_execute()`.
/// For task-level control, use `compute_disk_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_list(
    client: &SimpleHttpClient,
    args: &ComputeDiskTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_disk_types_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_disk_types_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_add_resource_policies_execute()` to send, or `compute_disks_add_resource_policies` for simplest API.

pub fn compute_disks_add_resource_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/addResourcePolicies",
        project,
        zone,
        disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_add_resource_policies_execute()` or `compute_disks_add_resource_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_add_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_add_resource_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_add_resource_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_add_resource_policies_task()`.
/// For the simplest API, use `compute_disks_add_resource_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_add_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_add_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_add_resource_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_add_resource_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksAddResourcePoliciesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_add_resource_policies_builder()` + `compute_disks_add_resource_policies_execute()`.
/// For task-level control, use `compute_disks_add_resource_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_add_resource_policies(
    client: &SimpleHttpClient,
    args: &ComputeDisksAddResourcePoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_add_resource_policies_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.requestId,
    )?;
    compute_disks_add_resource_policies_execute(builder)
}

/// GET projects/{project}/aggregated/disks
/// Retrieves an aggregated list of persistent disks.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_aggregated_list_execute()` to send, or `compute_disks_aggregated_list` for simplest API.

pub fn compute_disks_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/disks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/disks
/// Retrieves an aggregated list of persistent disks.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_aggregated_list_execute()` or `compute_disks_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/disks
/// Retrieves an aggregated list of persistent disks.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_aggregated_list_task()`.
/// For the simplest API, use `compute_disks_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_disks_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/disks
/// Retrieves an aggregated list of persistent disks.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_aggregated_list_builder()` + `compute_disks_aggregated_list_execute()`.
/// For task-level control, use `compute_disks_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeDisksAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_disks_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_disks_aggregated_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_bulk_insert_execute()` to send, or `compute_disks_bulk_insert` for simplest API.

pub fn compute_disks_bulk_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/bulkInsert",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_bulk_insert_execute()` or `compute_disks_bulk_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_bulk_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_bulk_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_bulk_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_bulk_insert_task()`.
/// For the simplest API, use `compute_disks_bulk_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_bulk_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_bulk_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_bulk_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_bulk_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksBulkInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_bulk_insert_builder()` + `compute_disks_bulk_insert_execute()`.
/// For task-level control, use `compute_disks_bulk_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_bulk_insert(
    client: &SimpleHttpClient,
    args: &ComputeDisksBulkInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disks_bulk_insert_builder(client, &args.project, &args.zone, &args.requestId)?;
    compute_disks_bulk_insert_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/bulkSetLabels
/// Sets the labels on many disks at once. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_bulk_set_labels_execute()` to send, or `compute_disks_bulk_set_labels` for simplest API.

pub fn compute_disks_bulk_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
    resource: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/bulkSetLabels",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = resource.as_ref() {
        query_parts.push(format!("resource={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/bulkSetLabels
/// Sets the labels on many disks at once. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_bulk_set_labels_execute()` or `compute_disks_bulk_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_bulk_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_bulk_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/bulkSetLabels
/// Sets the labels on many disks at once. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_bulk_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_bulk_set_labels_task()`.
/// For the simplest API, use `compute_disks_bulk_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_bulk_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_bulk_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_bulk_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_bulk_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksBulkSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: resource
    pub resource: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/bulkSetLabels
/// Sets the labels on many disks at once. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_bulk_set_labels_builder()` + `compute_disks_bulk_set_labels_execute()`.
/// For task-level control, use `compute_disks_bulk_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_bulk_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeDisksBulkSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_bulk_set_labels_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
        &args.resource,
    )?;
    compute_disks_bulk_set_labels_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_create_snapshot_execute()` to send, or `compute_disks_create_snapshot` for simplest API.

pub fn compute_disks_create_snapshot_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    guestFlush: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/createSnapshot",
        project, zone, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = guestFlush.as_ref() {
        query_parts.push(format!("guestFlush={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_create_snapshot_execute()` or `compute_disks_create_snapshot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_create_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_create_snapshot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_create_snapshot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_create_snapshot_task()`.
/// For the simplest API, use `compute_disks_create_snapshot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_create_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_create_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_create_snapshot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_create_snapshot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksCreateSnapshotArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: guestFlush
    pub guestFlush: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_create_snapshot_builder()` + `compute_disks_create_snapshot_execute()`.
/// For task-level control, use `compute_disks_create_snapshot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_create_snapshot(
    client: &SimpleHttpClient,
    args: &ComputeDisksCreateSnapshotArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_create_snapshot_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.guestFlush,
        &args.requestId,
    )?;
    compute_disks_create_snapshot_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/disks/{disk}
/// Deletes the specified persistent disk. Deleting a disk removes its data permanently and is irreversible. However, deleting a disk does not delete any snapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_delete_execute()` to send, or `compute_disks_delete` for simplest API.

pub fn compute_disks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}",
        project, zone, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/disks/{disk}
/// Deletes the specified persistent disk. Deleting a disk removes its data permanently and is irreversible. However, deleting a disk does not delete any snapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_delete_execute()` or `compute_disks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/disks/{disk}
/// Deletes the specified persistent disk. Deleting a disk removes its data permanently and is irreversible. However, deleting a disk does not delete any snapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_delete_task()`.
/// For the simplest API, use `compute_disks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/disks/{disk}
/// Deletes the specified persistent disk. Deleting a disk removes its data permanently and is irreversible. However, deleting a disk does not delete any snapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_delete_builder()` + `compute_disks_delete_execute()`.
/// For task-level control, use `compute_disks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_delete(
    client: &SimpleHttpClient,
    args: &ComputeDisksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.requestId,
    )?;
    compute_disks_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Returns the specified persistent disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_get_execute()` to send, or `compute_disks_get` for simplest API.

pub fn compute_disks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}",
        project, zone, disk,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Returns the specified persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_get_execute()` or `compute_disks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Disk>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Disk = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Returns the specified persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_get_task()`.
/// For the simplest API, use `compute_disks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Disk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Returns the specified persistent disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_get_builder()` + `compute_disks_get_execute()`.
/// For task-level control, use `compute_disks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_get(
    client: &SimpleHttpClient,
    args: &ComputeDisksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Disk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_get_builder(client, &args.project, &args.zone, &args.disk)?;
    compute_disks_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_get_iam_policy_execute()` to send, or `compute_disks_get_iam_policy` for simplest API.

pub fn compute_disks_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/getIamPolicy",
        project, zone, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_get_iam_policy_execute()` or `compute_disks_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_get_iam_policy_task()`.
/// For the simplest API, use `compute_disks_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_get_iam_policy_builder()` + `compute_disks_get_iam_policy_execute()`.
/// For task-level control, use `compute_disks_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeDisksGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_disks_get_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks
/// Creates a persistent disk in the specified project using the data in the request. You can create a disk from a source (`sourceImage`, `sourceSnapshot`, `orsourceDisk`) or create an empty 500 GB data disk by omitting all properties. You can also create a disk that is larger than the default size by specifying the `sizeGb` property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_insert_execute()` to send, or `compute_disks_insert` for simplest API.

pub fn compute_disks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
    sourceImage: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceImage.as_ref() {
        query_parts.push(format!("sourceImage={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks
/// Creates a persistent disk in the specified project using the data in the request. You can create a disk from a source (`sourceImage`, `sourceSnapshot`, `orsourceDisk`) or create an empty 500 GB data disk by omitting all properties. You can also create a disk that is larger than the default size by specifying the `sizeGb` property.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_insert_execute()` or `compute_disks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks
/// Creates a persistent disk in the specified project using the data in the request. You can create a disk from a source (`sourceImage`, `sourceSnapshot`, `orsourceDisk`) or create an empty 500 GB data disk by omitting all properties. You can also create a disk that is larger than the default size by specifying the `sizeGb` property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_insert_task()`.
/// For the simplest API, use `compute_disks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceImage
    pub sourceImage: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks
/// Creates a persistent disk in the specified project using the data in the request. You can create a disk from a source (`sourceImage`, `sourceSnapshot`, `orsourceDisk`) or create an empty 500 GB data disk by omitting all properties. You can also create a disk that is larger than the default size by specifying the `sizeGb` property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_insert_builder()` + `compute_disks_insert_execute()`.
/// For task-level control, use `compute_disks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_insert(
    client: &SimpleHttpClient,
    args: &ComputeDisksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
        &args.sourceImage,
    )?;
    compute_disks_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks
/// Retrieves a list of persistent disks contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_list_execute()` to send, or `compute_disks_list` for simplest API.

pub fn compute_disks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/disks
/// Retrieves a list of persistent disks contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_list_execute()` or `compute_disks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/disks
/// Retrieves a list of persistent disks contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_list_task()`.
/// For the simplest API, use `compute_disks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/disks
/// Retrieves a list of persistent disks contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_list_builder()` + `compute_disks_list_execute()`.
/// For task-level control, use `compute_disks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_list(
    client: &SimpleHttpClient,
    args: &ComputeDisksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_disks_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_remove_resource_policies_execute()` to send, or `compute_disks_remove_resource_policies` for simplest API.

pub fn compute_disks_remove_resource_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/removeResourcePolicies",
        project,
        zone,
        disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_remove_resource_policies_execute()` or `compute_disks_remove_resource_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_remove_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_remove_resource_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_remove_resource_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_remove_resource_policies_task()`.
/// For the simplest API, use `compute_disks_remove_resource_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_remove_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_remove_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_remove_resource_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_remove_resource_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksRemoveResourcePoliciesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_remove_resource_policies_builder()` + `compute_disks_remove_resource_policies_execute()`.
/// For task-level control, use `compute_disks_remove_resource_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_remove_resource_policies(
    client: &SimpleHttpClient,
    args: &ComputeDisksRemoveResourcePoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_remove_resource_policies_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.requestId,
    )?;
    compute_disks_remove_resource_policies_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/resize
/// Resizes the specified persistent disk. You can only increase the size of the disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_resize_execute()` to send, or `compute_disks_resize` for simplest API.

pub fn compute_disks_resize_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/resize",
        project, zone, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/resize
/// Resizes the specified persistent disk. You can only increase the size of the disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_resize_execute()` or `compute_disks_resize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_resize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/resize
/// Resizes the specified persistent disk. You can only increase the size of the disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_resize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_resize_task()`.
/// For the simplest API, use `compute_disks_resize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_resize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_resize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksResizeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/resize
/// Resizes the specified persistent disk. You can only increase the size of the disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_resize_builder()` + `compute_disks_resize_execute()`.
/// For task-level control, use `compute_disks_resize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_resize(
    client: &SimpleHttpClient,
    args: &ComputeDisksResizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_resize_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.requestId,
    )?;
    compute_disks_resize_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_set_iam_policy_execute()` to send, or `compute_disks_set_iam_policy` for simplest API.

pub fn compute_disks_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/setIamPolicy",
        project, zone, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_set_iam_policy_execute()` or `compute_disks_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_set_iam_policy_task()`.
/// For the simplest API, use `compute_disks_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_set_iam_policy_builder()` + `compute_disks_set_iam_policy_execute()`.
/// For task-level control, use `compute_disks_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeDisksSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disks_set_iam_policy_builder(client, &args.project, &args.zone, &args.resource)?;
    compute_disks_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/setLabels
/// Sets the labels on a disk. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_set_labels_execute()` to send, or `compute_disks_set_labels` for simplest API.

pub fn compute_disks_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/setLabels",
        project, zone, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/setLabels
/// Sets the labels on a disk. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_set_labels_execute()` or `compute_disks_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/setLabels
/// Sets the labels on a disk. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_set_labels_task()`.
/// For the simplest API, use `compute_disks_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/setLabels
/// Sets the labels on a disk. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_set_labels_builder()` + `compute_disks_set_labels_execute()`.
/// For task-level control, use `compute_disks_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeDisksSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_set_labels_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.requestId,
    )?;
    compute_disks_set_labels_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_start_async_replication_execute()` to send, or `compute_disks_start_async_replication` for simplest API.

pub fn compute_disks_start_async_replication_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/startAsyncReplication",
        project,
        zone,
        disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_start_async_replication_execute()` or `compute_disks_start_async_replication`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_start_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_start_async_replication_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_start_async_replication_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_start_async_replication_task()`.
/// For the simplest API, use `compute_disks_start_async_replication()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_start_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_start_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_start_async_replication_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_start_async_replication`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksStartAsyncReplicationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_start_async_replication_builder()` + `compute_disks_start_async_replication_execute()`.
/// For task-level control, use `compute_disks_start_async_replication_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_start_async_replication(
    client: &SimpleHttpClient,
    args: &ComputeDisksStartAsyncReplicationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_start_async_replication_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.requestId,
    )?;
    compute_disks_start_async_replication_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_stop_async_replication_execute()` to send, or `compute_disks_stop_async_replication` for simplest API.

pub fn compute_disks_stop_async_replication_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/stopAsyncReplication",
        project,
        zone,
        disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_stop_async_replication_execute()` or `compute_disks_stop_async_replication`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_stop_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_stop_async_replication_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_stop_async_replication_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_stop_async_replication_task()`.
/// For the simplest API, use `compute_disks_stop_async_replication()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_stop_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_stop_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_stop_async_replication_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_stop_async_replication`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksStopAsyncReplicationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_stop_async_replication_builder()` + `compute_disks_stop_async_replication_execute()`.
/// For task-level control, use `compute_disks_stop_async_replication_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_stop_async_replication(
    client: &SimpleHttpClient,
    args: &ComputeDisksStopAsyncReplicationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_stop_async_replication_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.requestId,
    )?;
    compute_disks_stop_async_replication_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_stop_group_async_replication_execute()` to send, or `compute_disks_stop_group_async_replication` for simplest API.

pub fn compute_disks_stop_group_async_replication_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/stopGroupAsyncReplication",
        project,
        zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_stop_group_async_replication_execute()` or `compute_disks_stop_group_async_replication`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_stop_group_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_stop_group_async_replication_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_stop_group_async_replication_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_stop_group_async_replication_task()`.
/// For the simplest API, use `compute_disks_stop_group_async_replication()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_stop_group_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_stop_group_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_stop_group_async_replication_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_stop_group_async_replication`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksStopGroupAsyncReplicationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_stop_group_async_replication_builder()` + `compute_disks_stop_group_async_replication_execute()`.
/// For task-level control, use `compute_disks_stop_group_async_replication_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_stop_group_async_replication(
    client: &SimpleHttpClient,
    args: &ComputeDisksStopGroupAsyncReplicationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_stop_group_async_replication_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
    )?;
    compute_disks_stop_group_async_replication_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_test_iam_permissions_execute()` to send, or `compute_disks_test_iam_permissions` for simplest API.

pub fn compute_disks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_test_iam_permissions_execute()` or `compute_disks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_disks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_disks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_test_iam_permissions_builder()` + `compute_disks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_disks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeDisksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_disks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_disks_test_iam_permissions_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/disks/{disk}
/// Updates the specified disk with the data included in the request. The update is performed only on selected fields included as part of update-mask.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_update_execute()` to send, or `compute_disks_update` for simplest API.

pub fn compute_disks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    paths: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}",
        project, zone, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = paths.as_ref() {
        query_parts.push(format!("paths={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/disks/{disk}
/// Updates the specified disk with the data included in the request. The update is performed only on selected fields included as part of update-mask.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_update_execute()` or `compute_disks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/disks/{disk}
/// Updates the specified disk with the data included in the request. The update is performed only on selected fields included as part of update-mask.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_update_task()`.
/// For the simplest API, use `compute_disks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: paths
    pub paths: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/disks/{disk}
/// Updates the specified disk with the data included in the request. The update is performed only on selected fields included as part of update-mask.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_update_builder()` + `compute_disks_update_execute()`.
/// For task-level control, use `compute_disks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_update(
    client: &SimpleHttpClient,
    args: &ComputeDisksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_update_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.paths,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_disks_update_execute(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified persistent disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_update_kms_key_execute()` to send, or `compute_disks_update_kms_key` for simplest API.

pub fn compute_disks_update_kms_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/updateKmsKey",
        project, zone, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_disks_update_kms_key_execute()` or `compute_disks_update_kms_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_update_kms_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_update_kms_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_disks_update_kms_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_disks_update_kms_key_task()`.
/// For the simplest API, use `compute_disks_update_kms_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_disks_update_kms_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_disks_update_kms_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_disks_update_kms_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_disks_update_kms_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeDisksUpdateKmsKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/disks/{disk}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified persistent disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_update_kms_key_builder()` + `compute_disks_update_kms_key_execute()`.
/// For task-level control, use `compute_disks_update_kms_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_update_kms_key(
    client: &SimpleHttpClient,
    args: &ComputeDisksUpdateKmsKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_update_kms_key_builder(
        client,
        &args.project,
        &args.zone,
        &args.disk,
        &args.requestId,
    )?;
    compute_disks_update_kms_key_execute(builder)
}

/// DELETE projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Deletes the specified `externalVpnGateway`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_delete_execute()` to send, or `compute_external_vpn_gateways_delete` for simplest API.

pub fn compute_external_vpn_gateways_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    externalVpnGateway: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways/{}",
        project, externalVpnGateway,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Deletes the specified `externalVpnGateway`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_external_vpn_gateways_delete_execute()` or `compute_external_vpn_gateways_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Deletes the specified `externalVpnGateway`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_external_vpn_gateways_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_external_vpn_gateways_delete_task()`.
/// For the simplest API, use `compute_external_vpn_gateways_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_external_vpn_gateways_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_external_vpn_gateways_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_external_vpn_gateways_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: externalVpnGateway
    pub externalVpnGateway: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Deletes the specified `externalVpnGateway`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_delete_builder()` + `compute_external_vpn_gateways_delete_execute()`.
/// For task-level control, use `compute_external_vpn_gateways_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_delete(
    client: &SimpleHttpClient,
    args: &ComputeExternalVpnGatewaysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_external_vpn_gateways_delete_builder(
        client,
        &args.project,
        &args.externalVpnGateway,
        &args.requestId,
    )?;
    compute_external_vpn_gateways_delete_execute(builder)
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Returns the specified `externalVpnGateway`. Get a list of available `externalVpnGateways` by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_get_execute()` to send, or `compute_external_vpn_gateways_get` for simplest API.

pub fn compute_external_vpn_gateways_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    externalVpnGateway: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways/{}",
        project, externalVpnGateway,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Returns the specified `externalVpnGateway`. Get a list of available `externalVpnGateways` by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_external_vpn_gateways_get_execute()` or `compute_external_vpn_gateways_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExternalVpnGateway>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalVpnGateway = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Returns the specified `externalVpnGateway`. Get a list of available `externalVpnGateways` by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_external_vpn_gateways_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_external_vpn_gateways_get_task()`.
/// For the simplest API, use `compute_external_vpn_gateways_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_external_vpn_gateways_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalVpnGateway>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_external_vpn_gateways_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_external_vpn_gateways_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: externalVpnGateway
    pub externalVpnGateway: String,
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Returns the specified `externalVpnGateway`. Get a list of available `externalVpnGateways` by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_get_builder()` + `compute_external_vpn_gateways_get_execute()`.
/// For task-level control, use `compute_external_vpn_gateways_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_get(
    client: &SimpleHttpClient,
    args: &ComputeExternalVpnGatewaysGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalVpnGateway>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_external_vpn_gateways_get_builder(client, &args.project, &args.externalVpnGateway)?;
    compute_external_vpn_gateways_get_execute(builder)
}

/// POST projects/{project}/global/externalVpnGateways
/// Creates a ExternalVpnGateway in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_insert_execute()` to send, or `compute_external_vpn_gateways_insert` for simplest API.

pub fn compute_external_vpn_gateways_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/externalVpnGateways
/// Creates a ExternalVpnGateway in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_external_vpn_gateways_insert_execute()` or `compute_external_vpn_gateways_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/externalVpnGateways
/// Creates a ExternalVpnGateway in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_external_vpn_gateways_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_external_vpn_gateways_insert_task()`.
/// For the simplest API, use `compute_external_vpn_gateways_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_external_vpn_gateways_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_external_vpn_gateways_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_external_vpn_gateways_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/externalVpnGateways
/// Creates a ExternalVpnGateway in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_insert_builder()` + `compute_external_vpn_gateways_insert_execute()`.
/// For task-level control, use `compute_external_vpn_gateways_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_insert(
    client: &SimpleHttpClient,
    args: &ComputeExternalVpnGatewaysInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_external_vpn_gateways_insert_builder(client, &args.project, &args.requestId)?;
    compute_external_vpn_gateways_insert_execute(builder)
}

/// GET projects/{project}/global/externalVpnGateways
/// Retrieves the list of ExternalVpnGateway available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_list_execute()` to send, or `compute_external_vpn_gateways_list` for simplest API.

pub fn compute_external_vpn_gateways_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/externalVpnGateways
/// Retrieves the list of ExternalVpnGateway available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_external_vpn_gateways_list_execute()` or `compute_external_vpn_gateways_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExternalVpnGatewayList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalVpnGatewayList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/externalVpnGateways
/// Retrieves the list of ExternalVpnGateway available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_external_vpn_gateways_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_external_vpn_gateways_list_task()`.
/// For the simplest API, use `compute_external_vpn_gateways_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_external_vpn_gateways_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalVpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_external_vpn_gateways_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_external_vpn_gateways_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/externalVpnGateways
/// Retrieves the list of ExternalVpnGateway available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_list_builder()` + `compute_external_vpn_gateways_list_execute()`.
/// For task-level control, use `compute_external_vpn_gateways_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_list(
    client: &SimpleHttpClient,
    args: &ComputeExternalVpnGatewaysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalVpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_external_vpn_gateways_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_external_vpn_gateways_list_execute(builder)
}

/// POST projects/{project}/global/externalVpnGateways/{resource}/setLabels
/// Sets the labels on an ExternalVpnGateway. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_set_labels_execute()` to send, or `compute_external_vpn_gateways_set_labels` for simplest API.

pub fn compute_external_vpn_gateways_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways/{}/setLabels",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/externalVpnGateways/{resource}/setLabels
/// Sets the labels on an ExternalVpnGateway. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_external_vpn_gateways_set_labels_execute()` or `compute_external_vpn_gateways_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/externalVpnGateways/{resource}/setLabels
/// Sets the labels on an ExternalVpnGateway. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_external_vpn_gateways_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_external_vpn_gateways_set_labels_task()`.
/// For the simplest API, use `compute_external_vpn_gateways_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_external_vpn_gateways_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_external_vpn_gateways_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_external_vpn_gateways_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/externalVpnGateways/{resource}/setLabels
/// Sets the labels on an ExternalVpnGateway. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_set_labels_builder()` + `compute_external_vpn_gateways_set_labels_execute()`.
/// For task-level control, use `compute_external_vpn_gateways_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeExternalVpnGatewaysSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_external_vpn_gateways_set_labels_builder(client, &args.project, &args.resource)?;
    compute_external_vpn_gateways_set_labels_execute(builder)
}

/// POST projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_test_iam_permissions_execute()` to send, or `compute_external_vpn_gateways_test_iam_permissions` for simplest API.

pub fn compute_external_vpn_gateways_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_external_vpn_gateways_test_iam_permissions_execute()` or `compute_external_vpn_gateways_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_external_vpn_gateways_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_external_vpn_gateways_test_iam_permissions_task()`.
/// For the simplest API, use `compute_external_vpn_gateways_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_external_vpn_gateways_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_external_vpn_gateways_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_external_vpn_gateways_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_external_vpn_gateways_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_test_iam_permissions_builder()` + `compute_external_vpn_gateways_test_iam_permissions_execute()`.
/// For task-level control, use `compute_external_vpn_gateways_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeExternalVpnGatewaysTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_external_vpn_gateways_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_external_vpn_gateways_test_iam_permissions_execute(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_add_association_execute()` to send, or `compute_firewall_policies_add_association` for simplest API.

pub fn compute_firewall_policies_add_association_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    replaceExistingAssociation: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/addAssociation",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = replaceExistingAssociation.as_ref() {
        query_parts.push(format!("replaceExistingAssociation={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_add_association_execute()` or `compute_firewall_policies_add_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_add_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_add_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_add_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_add_association_task()`.
/// For the simplest API, use `compute_firewall_policies_add_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_add_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_add_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_add_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_add_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesAddAssociationArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: replaceExistingAssociation
    pub replaceExistingAssociation: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_add_association_builder()` + `compute_firewall_policies_add_association_execute()`.
/// For task-level control, use `compute_firewall_policies_add_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_add_association(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesAddAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_add_association_builder(
        client,
        &args.firewallPolicy,
        &args.replaceExistingAssociation,
        &args.requestId,
    )?;
    compute_firewall_policies_add_association_execute(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_add_rule_execute()` to send, or `compute_firewall_policies_add_rule` for simplest API.

pub fn compute_firewall_policies_add_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/addRule",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_add_rule_execute()` or `compute_firewall_policies_add_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_add_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_add_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_add_rule_task()`.
/// For the simplest API, use `compute_firewall_policies_add_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_add_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_add_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesAddRuleArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_add_rule_builder()` + `compute_firewall_policies_add_rule_execute()`.
/// For task-level control, use `compute_firewall_policies_add_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_add_rule(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesAddRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewall_policies_add_rule_builder(client, &args.firewallPolicy, &args.requestId)?;
    compute_firewall_policies_add_rule_execute(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_clone_rules_execute()` to send, or `compute_firewall_policies_clone_rules` for simplest API.

pub fn compute_firewall_policies_clone_rules_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
    sourceFirewallPolicy: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/cloneRules",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceFirewallPolicy.as_ref() {
        query_parts.push(format!("sourceFirewallPolicy={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_clone_rules_execute()` or `compute_firewall_policies_clone_rules`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_clone_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_clone_rules_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_clone_rules_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_clone_rules_task()`.
/// For the simplest API, use `compute_firewall_policies_clone_rules()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_clone_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_clone_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_clone_rules_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_clone_rules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesCloneRulesArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceFirewallPolicy
    pub sourceFirewallPolicy: Option<Option<String>>,
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_clone_rules_builder()` + `compute_firewall_policies_clone_rules_execute()`.
/// For task-level control, use `compute_firewall_policies_clone_rules_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_clone_rules(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesCloneRulesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_clone_rules_builder(
        client,
        &args.firewallPolicy,
        &args.requestId,
        &args.sourceFirewallPolicy,
    )?;
    compute_firewall_policies_clone_rules_execute(builder)
}

/// DELETE locations/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_delete_execute()` to send, or `compute_firewall_policies_delete` for simplest API.

pub fn compute_firewall_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE locations/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_delete_execute()` or `compute_firewall_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE locations/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_delete_task()`.
/// For the simplest API, use `compute_firewall_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesDeleteArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE locations/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_delete_builder()` + `compute_firewall_policies_delete_execute()`.
/// For task-level control, use `compute_firewall_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewall_policies_delete_builder(client, &args.firewallPolicy, &args.requestId)?;
    compute_firewall_policies_delete_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Returns the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_get_execute()` to send, or `compute_firewall_policies_get` for simplest API.

pub fn compute_firewall_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}",
        firewallPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Returns the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_get_execute()` or `compute_firewall_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Returns the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_get_task()`.
/// For the simplest API, use `compute_firewall_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesGetArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Returns the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_get_builder()` + `compute_firewall_policies_get_execute()`.
/// For task-level control, use `compute_firewall_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_get_builder(client, &args.firewallPolicy)?;
    compute_firewall_policies_get_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_get_association_execute()` to send, or `compute_firewall_policies_get_association` for simplest API.

pub fn compute_firewall_policies_get_association_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    name: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/getAssociation",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_get_association_execute()` or `compute_firewall_policies_get_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_get_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_get_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_get_association_task()`.
/// For the simplest API, use `compute_firewall_policies_get_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_get_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_get_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_get_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_get_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesGetAssociationArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_get_association_builder()` + `compute_firewall_policies_get_association_execute()`.
/// For task-level control, use `compute_firewall_policies_get_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_association(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesGetAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_get_association_builder(
        client,
        &args.firewallPolicy,
        &args.name,
    )?;
    compute_firewall_policies_get_association_execute(builder)
}

/// GET locations/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_get_iam_policy_execute()` to send, or `compute_firewall_policies_get_iam_policy` for simplest API.

pub fn compute_firewall_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_get_iam_policy_execute()` or `compute_firewall_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_get_iam_policy_task()`.
/// For the simplest API, use `compute_firewall_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET locations/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_get_iam_policy_builder()` + `compute_firewall_policies_get_iam_policy_execute()`.
/// For task-level control, use `compute_firewall_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_get_iam_policy_builder(
        client,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_firewall_policies_get_iam_policy_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_get_rule_execute()` to send, or `compute_firewall_policies_get_rule` for simplest API.

pub fn compute_firewall_policies_get_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/getRule",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_get_rule_execute()` or `compute_firewall_policies_get_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_get_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_get_rule_task()`.
/// For the simplest API, use `compute_firewall_policies_get_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_get_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_get_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesGetRuleArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_get_rule_builder()` + `compute_firewall_policies_get_rule_execute()`.
/// For task-level control, use `compute_firewall_policies_get_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_rule(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesGetRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_firewall_policies_get_rule_builder(client, &args.firewallPolicy, &args.priority)?;
    compute_firewall_policies_get_rule_execute(builder)
}

/// POST locations/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_insert_execute()` to send, or `compute_firewall_policies_insert` for simplest API.

pub fn compute_firewall_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    parentId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_insert_execute()` or `compute_firewall_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_insert_task()`.
/// For the simplest API, use `compute_firewall_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesInsertArgs {
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_insert_builder()` + `compute_firewall_policies_insert_execute()`.
/// For task-level control, use `compute_firewall_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewall_policies_insert_builder(client, &args.parentId, &args.requestId)?;
    compute_firewall_policies_insert_execute(builder)
}

/// GET locations/global/firewallPolicies
/// Lists all the policies that have been configured for the specified folder or organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_list_execute()` to send, or `compute_firewall_policies_list` for simplest API.

pub fn compute_firewall_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    parentId: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/firewallPolicies
/// Lists all the policies that have been configured for the specified folder or organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_list_execute()` or `compute_firewall_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/firewallPolicies
/// Lists all the policies that have been configured for the specified folder or organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_list_task()`.
/// For the simplest API, use `compute_firewall_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesListArgs {
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET locations/global/firewallPolicies
/// Lists all the policies that have been configured for the specified folder or organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_list_builder()` + `compute_firewall_policies_list_execute()`.
/// For task-level control, use `compute_firewall_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_list_builder(
        client,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.parentId,
        &args.returnPartialSuccess,
    )?;
    compute_firewall_policies_list_execute(builder)
}

/// GET locations/global/firewallPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_list_associations_execute()` to send, or `compute_firewall_policies_list_associations` for simplest API.

pub fn compute_firewall_policies_list_associations_builder<R>(
    client: &SimpleHttpClient<R>,
    includeInheritedPolicies: &Option<Option<String>>,
    targetResource: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/listAssociations",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeInheritedPolicies.as_ref() {
        query_parts.push(format!("includeInheritedPolicies={}", val));
    }
    if let Some(val) = targetResource.as_ref() {
        query_parts.push(format!("targetResource={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/firewallPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_list_associations_execute()` or `compute_firewall_policies_list_associations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_list_associations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_list_associations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPoliciesListAssociationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPoliciesListAssociationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/firewallPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_list_associations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_list_associations_task()`.
/// For the simplest API, use `compute_firewall_policies_list_associations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_list_associations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_list_associations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirewallPoliciesListAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_list_associations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_list_associations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesListAssociationsArgs {
    /// Query parameter: includeInheritedPolicies
    pub includeInheritedPolicies: Option<Option<String>>,
    /// Query parameter: targetResource
    pub targetResource: Option<Option<String>>,
}

/// GET locations/global/firewallPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_list_associations_builder()` + `compute_firewall_policies_list_associations_execute()`.
/// For task-level control, use `compute_firewall_policies_list_associations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_list_associations(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesListAssociationsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirewallPoliciesListAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_list_associations_builder(
        client,
        &args.includeInheritedPolicies,
        &args.targetResource,
    )?;
    compute_firewall_policies_list_associations_execute(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/move
/// Moves the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_move_execute()` to send, or `compute_firewall_policies_move` for simplest API.

pub fn compute_firewall_policies_move_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    parentId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/move",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/move
/// Moves the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_move_execute()` or `compute_firewall_policies_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/move
/// Moves the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_move_task()`.
/// For the simplest API, use `compute_firewall_policies_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesMoveArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/move
/// Moves the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_move_builder()` + `compute_firewall_policies_move_execute()`.
/// For task-level control, use `compute_firewall_policies_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_move(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_move_builder(
        client,
        &args.firewallPolicy,
        &args.parentId,
        &args.requestId,
    )?;
    compute_firewall_policies_move_execute(builder)
}

/// PATCH locations/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_patch_execute()` to send, or `compute_firewall_policies_patch` for simplest API.

pub fn compute_firewall_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH locations/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_patch_execute()` or `compute_firewall_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH locations/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_patch_task()`.
/// For the simplest API, use `compute_firewall_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesPatchArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH locations/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_patch_builder()` + `compute_firewall_policies_patch_execute()`.
/// For task-level control, use `compute_firewall_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewall_policies_patch_builder(client, &args.firewallPolicy, &args.requestId)?;
    compute_firewall_policies_patch_execute(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_patch_rule_execute()` to send, or `compute_firewall_policies_patch_rule` for simplest API.

pub fn compute_firewall_policies_patch_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/patchRule",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_patch_rule_execute()` or `compute_firewall_policies_patch_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_patch_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_patch_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_patch_rule_task()`.
/// For the simplest API, use `compute_firewall_policies_patch_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_patch_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_patch_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesPatchRuleArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_patch_rule_builder()` + `compute_firewall_policies_patch_rule_execute()`.
/// For task-level control, use `compute_firewall_policies_patch_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_patch_rule(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesPatchRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_patch_rule_builder(
        client,
        &args.firewallPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_firewall_policies_patch_rule_execute(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_remove_association_execute()` to send, or `compute_firewall_policies_remove_association` for simplest API.

pub fn compute_firewall_policies_remove_association_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    name: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/removeAssociation",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_remove_association_execute()` or `compute_firewall_policies_remove_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_remove_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_remove_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_remove_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_remove_association_task()`.
/// For the simplest API, use `compute_firewall_policies_remove_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_remove_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_remove_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_remove_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_remove_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesRemoveAssociationArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_remove_association_builder()` + `compute_firewall_policies_remove_association_execute()`.
/// For task-level control, use `compute_firewall_policies_remove_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_remove_association(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesRemoveAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_remove_association_builder(
        client,
        &args.firewallPolicy,
        &args.name,
        &args.requestId,
    )?;
    compute_firewall_policies_remove_association_execute(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_remove_rule_execute()` to send, or `compute_firewall_policies_remove_rule` for simplest API.

pub fn compute_firewall_policies_remove_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/removeRule",
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_remove_rule_execute()` or `compute_firewall_policies_remove_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_remove_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_remove_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_remove_rule_task()`.
/// For the simplest API, use `compute_firewall_policies_remove_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_remove_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_remove_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesRemoveRuleArgs {
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_remove_rule_builder()` + `compute_firewall_policies_remove_rule_execute()`.
/// For task-level control, use `compute_firewall_policies_remove_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_remove_rule(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesRemoveRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_remove_rule_builder(
        client,
        &args.firewallPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_firewall_policies_remove_rule_execute(builder)
}

/// POST locations/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_set_iam_policy_execute()` to send, or `compute_firewall_policies_set_iam_policy` for simplest API.

pub fn compute_firewall_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_set_iam_policy_execute()` or `compute_firewall_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_set_iam_policy_task()`.
/// For the simplest API, use `compute_firewall_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST locations/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_set_iam_policy_builder()` + `compute_firewall_policies_set_iam_policy_execute()`.
/// For task-level control, use `compute_firewall_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_set_iam_policy_builder(client, &args.resource)?;
    compute_firewall_policies_set_iam_policy_execute(builder)
}

/// POST locations/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_test_iam_permissions_execute()` to send, or `compute_firewall_policies_test_iam_permissions` for simplest API.

pub fn compute_firewall_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewall_policies_test_iam_permissions_execute()` or `compute_firewall_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewall_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewall_policies_test_iam_permissions_task()`.
/// For the simplest API, use `compute_firewall_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewall_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewall_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_firewall_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewall_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST locations/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_test_iam_permissions_builder()` + `compute_firewall_policies_test_iam_permissions_execute()`.
/// For task-level control, use `compute_firewall_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeFirewallPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_test_iam_permissions_builder(client, &args.resource)?;
    compute_firewall_policies_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/global/firewalls/{firewall}
/// Deletes the specified firewall.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_delete_execute()` to send, or `compute_firewalls_delete` for simplest API.

pub fn compute_firewalls_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewall: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}",
        project, firewall,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/firewalls/{firewall}
/// Deletes the specified firewall.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewalls_delete_execute()` or `compute_firewalls_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/firewalls/{firewall}
/// Deletes the specified firewall.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewalls_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewalls_delete_task()`.
/// For the simplest API, use `compute_firewalls_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewalls_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewalls_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewalls_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewall
    pub firewall: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/firewalls/{firewall}
/// Deletes the specified firewall.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_delete_builder()` + `compute_firewalls_delete_execute()`.
/// For task-level control, use `compute_firewalls_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_delete(
    client: &SimpleHttpClient,
    args: &ComputeFirewallsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewalls_delete_builder(client, &args.project, &args.firewall, &args.requestId)?;
    compute_firewalls_delete_execute(builder)
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Returns the specified firewall.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_get_execute()` to send, or `compute_firewalls_get` for simplest API.

pub fn compute_firewalls_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewall: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}",
        project, firewall,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Returns the specified firewall.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewalls_get_execute()` or `compute_firewalls_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Firewall>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Firewall = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Returns the specified firewall.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewalls_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewalls_get_task()`.
/// For the simplest API, use `compute_firewalls_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewalls_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Firewall>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewalls_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewalls_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewall
    pub firewall: String,
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Returns the specified firewall.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_get_builder()` + `compute_firewalls_get_execute()`.
/// For task-level control, use `compute_firewalls_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_get(
    client: &SimpleHttpClient,
    args: &ComputeFirewallsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Firewall>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewalls_get_builder(client, &args.project, &args.firewall)?;
    compute_firewalls_get_execute(builder)
}

/// POST projects/{project}/global/firewalls
/// Creates a firewall rule in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_insert_execute()` to send, or `compute_firewalls_insert` for simplest API.

pub fn compute_firewalls_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewalls
/// Creates a firewall rule in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewalls_insert_execute()` or `compute_firewalls_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewalls
/// Creates a firewall rule in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewalls_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewalls_insert_task()`.
/// For the simplest API, use `compute_firewalls_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewalls_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewalls_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewalls_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewalls
/// Creates a firewall rule in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_insert_builder()` + `compute_firewalls_insert_execute()`.
/// For task-level control, use `compute_firewalls_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_insert(
    client: &SimpleHttpClient,
    args: &ComputeFirewallsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewalls_insert_builder(client, &args.project, &args.requestId)?;
    compute_firewalls_insert_execute(builder)
}

/// GET projects/{project}/global/firewalls
/// Retrieves the list of firewall rules available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_list_execute()` to send, or `compute_firewalls_list` for simplest API.

pub fn compute_firewalls_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/firewalls
/// Retrieves the list of firewall rules available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewalls_list_execute()` or `compute_firewalls_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/firewalls
/// Retrieves the list of firewall rules available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewalls_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewalls_list_task()`.
/// For the simplest API, use `compute_firewalls_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewalls_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_firewalls_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewalls_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/firewalls
/// Retrieves the list of firewall rules available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_list_builder()` + `compute_firewalls_list_execute()`.
/// For task-level control, use `compute_firewalls_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_list(
    client: &SimpleHttpClient,
    args: &ComputeFirewallsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewalls_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_firewalls_list_execute(builder)
}

/// PATCH projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_patch_execute()` to send, or `compute_firewalls_patch` for simplest API.

pub fn compute_firewalls_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewall: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}",
        project, firewall,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewalls_patch_execute()` or `compute_firewalls_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewalls_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewalls_patch_task()`.
/// For the simplest API, use `compute_firewalls_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewalls_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewalls_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewalls_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewall
    pub firewall: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_patch_builder()` + `compute_firewalls_patch_execute()`.
/// For task-level control, use `compute_firewalls_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_patch(
    client: &SimpleHttpClient,
    args: &ComputeFirewallsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewalls_patch_builder(client, &args.project, &args.firewall, &args.requestId)?;
    compute_firewalls_patch_execute(builder)
}

/// POST projects/{project}/global/firewalls/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_test_iam_permissions_execute()` to send, or `compute_firewalls_test_iam_permissions` for simplest API.

pub fn compute_firewalls_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewalls/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewalls_test_iam_permissions_execute()` or `compute_firewalls_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewalls/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewalls_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewalls_test_iam_permissions_task()`.
/// For the simplest API, use `compute_firewalls_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewalls_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_firewalls_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewalls_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/firewalls/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_test_iam_permissions_builder()` + `compute_firewalls_test_iam_permissions_execute()`.
/// For task-level control, use `compute_firewalls_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeFirewallsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_firewalls_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_firewalls_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. Note that all fields will be updated if using PUT, even fields that are not specified. To update individual fields, please use PATCH instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_update_execute()` to send, or `compute_firewalls_update` for simplest API.

pub fn compute_firewalls_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewall: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}",
        project, firewall,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. Note that all fields will be updated if using PUT, even fields that are not specified. To update individual fields, please use PATCH instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_firewalls_update_execute()` or `compute_firewalls_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. Note that all fields will be updated if using PUT, even fields that are not specified. To update individual fields, please use PATCH instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_firewalls_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_firewalls_update_task()`.
/// For the simplest API, use `compute_firewalls_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_firewalls_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_firewalls_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_firewalls_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_firewalls_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFirewallsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewall
    pub firewall: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. Note that all fields will be updated if using PUT, even fields that are not specified. To update individual fields, please use PATCH instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_update_builder()` + `compute_firewalls_update_execute()`.
/// For task-level control, use `compute_firewalls_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_update(
    client: &SimpleHttpClient,
    args: &ComputeFirewallsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewalls_update_builder(client, &args.project, &args.firewall, &args.requestId)?;
    compute_firewalls_update_execute(builder)
}

/// GET projects/{project}/aggregated/forwardingRules
/// Retrieves an aggregated list of forwarding rules.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_aggregated_list_execute()` to send, or `compute_forwarding_rules_aggregated_list` for simplest API.

pub fn compute_forwarding_rules_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/forwardingRules",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/forwardingRules
/// Retrieves an aggregated list of forwarding rules.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_forwarding_rules_aggregated_list_execute()` or `compute_forwarding_rules_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRuleAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRuleAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/forwardingRules
/// Retrieves an aggregated list of forwarding rules.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_forwarding_rules_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_forwarding_rules_aggregated_list_task()`.
/// For the simplest API, use `compute_forwarding_rules_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_forwarding_rules_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ForwardingRuleAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_forwarding_rules_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_forwarding_rules_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeForwardingRulesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/forwardingRules
/// Retrieves an aggregated list of forwarding rules.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_aggregated_list_builder()` + `compute_forwarding_rules_aggregated_list_execute()`.
/// For task-level control, use `compute_forwarding_rules_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeForwardingRulesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ForwardingRuleAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_forwarding_rules_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Deletes the specified ForwardingRule resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_delete_execute()` to send, or `compute_forwarding_rules_delete` for simplest API.

pub fn compute_forwarding_rules_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    forwardingRule: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}",
        project, region, forwardingRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Deletes the specified ForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_forwarding_rules_delete_execute()` or `compute_forwarding_rules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Deletes the specified ForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_forwarding_rules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_forwarding_rules_delete_task()`.
/// For the simplest API, use `compute_forwarding_rules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_forwarding_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_forwarding_rules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_forwarding_rules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeForwardingRulesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: forwardingRule
    pub forwardingRule: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Deletes the specified ForwardingRule resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_delete_builder()` + `compute_forwarding_rules_delete_execute()`.
/// For task-level control, use `compute_forwarding_rules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_delete(
    client: &SimpleHttpClient,
    args: &ComputeForwardingRulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.forwardingRule,
        &args.requestId,
    )?;
    compute_forwarding_rules_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Returns the specified ForwardingRule resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_get_execute()` to send, or `compute_forwarding_rules_get` for simplest API.

pub fn compute_forwarding_rules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    forwardingRule: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}",
        project, region, forwardingRule,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Returns the specified ForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_forwarding_rules_get_execute()` or `compute_forwarding_rules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Returns the specified ForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_forwarding_rules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_forwarding_rules_get_task()`.
/// For the simplest API, use `compute_forwarding_rules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_forwarding_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_forwarding_rules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_forwarding_rules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeForwardingRulesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: forwardingRule
    pub forwardingRule: String,
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Returns the specified ForwardingRule resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_get_builder()` + `compute_forwarding_rules_get_execute()`.
/// For task-level control, use `compute_forwarding_rules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_get(
    client: &SimpleHttpClient,
    args: &ComputeForwardingRulesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_get_builder(
        client,
        &args.project,
        &args.region,
        &args.forwardingRule,
    )?;
    compute_forwarding_rules_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/forwardingRules
/// Creates a ForwardingRule resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_insert_execute()` to send, or `compute_forwarding_rules_insert` for simplest API.

pub fn compute_forwarding_rules_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/forwardingRules
/// Creates a ForwardingRule resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_forwarding_rules_insert_execute()` or `compute_forwarding_rules_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/forwardingRules
/// Creates a ForwardingRule resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_forwarding_rules_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_forwarding_rules_insert_task()`.
/// For the simplest API, use `compute_forwarding_rules_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_forwarding_rules_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_forwarding_rules_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_forwarding_rules_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeForwardingRulesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/forwardingRules
/// Creates a ForwardingRule resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_insert_builder()` + `compute_forwarding_rules_insert_execute()`.
/// For task-level control, use `compute_forwarding_rules_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_insert(
    client: &SimpleHttpClient,
    args: &ComputeForwardingRulesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_forwarding_rules_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Retrieves a list of ForwardingRule resources available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_list_execute()` to send, or `compute_forwarding_rules_list` for simplest API.

pub fn compute_forwarding_rules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Retrieves a list of ForwardingRule resources available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_forwarding_rules_list_execute()` or `compute_forwarding_rules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRuleList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRuleList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Retrieves a list of ForwardingRule resources available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_forwarding_rules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_forwarding_rules_list_task()`.
/// For the simplest API, use `compute_forwarding_rules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_forwarding_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRuleList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_forwarding_rules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_forwarding_rules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeForwardingRulesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Retrieves a list of ForwardingRule resources available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_list_builder()` + `compute_forwarding_rules_list_execute()`.
/// For task-level control, use `compute_forwarding_rules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_list(
    client: &SimpleHttpClient,
    args: &ComputeForwardingRulesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRuleList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_forwarding_rules_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_patch_execute()` to send, or `compute_forwarding_rules_patch` for simplest API.

pub fn compute_forwarding_rules_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    forwardingRule: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}",
        project, region, forwardingRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_forwarding_rules_patch_execute()` or `compute_forwarding_rules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_forwarding_rules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_forwarding_rules_patch_task()`.
/// For the simplest API, use `compute_forwarding_rules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_forwarding_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_forwarding_rules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_forwarding_rules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeForwardingRulesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: forwardingRule
    pub forwardingRule: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_patch_builder()` + `compute_forwarding_rules_patch_execute()`.
/// For task-level control, use `compute_forwarding_rules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_patch(
    client: &SimpleHttpClient,
    args: &ComputeForwardingRulesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.forwardingRule,
        &args.requestId,
    )?;
    compute_forwarding_rules_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_set_labels_execute()` to send, or `compute_forwarding_rules_set_labels` for simplest API.

pub fn compute_forwarding_rules_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}/setLabels",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_forwarding_rules_set_labels_execute()` or `compute_forwarding_rules_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_forwarding_rules_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_forwarding_rules_set_labels_task()`.
/// For the simplest API, use `compute_forwarding_rules_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_forwarding_rules_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_forwarding_rules_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_forwarding_rules_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeForwardingRulesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_set_labels_builder()` + `compute_forwarding_rules_set_labels_execute()`.
/// For task-level control, use `compute_forwarding_rules_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeForwardingRulesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_forwarding_rules_set_labels_execute(builder)
}

/// POST projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for forwarding rule. The new target should be of the same type as the old target.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_set_target_execute()` to send, or `compute_forwarding_rules_set_target` for simplest API.

pub fn compute_forwarding_rules_set_target_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    forwardingRule: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}/setTarget",
        project,
        region,
        forwardingRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for forwarding rule. The new target should be of the same type as the old target.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_forwarding_rules_set_target_execute()` or `compute_forwarding_rules_set_target`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_set_target_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_set_target_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for forwarding rule. The new target should be of the same type as the old target.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_forwarding_rules_set_target_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_forwarding_rules_set_target_task()`.
/// For the simplest API, use `compute_forwarding_rules_set_target()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_forwarding_rules_set_target_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_forwarding_rules_set_target_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_forwarding_rules_set_target_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_forwarding_rules_set_target`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeForwardingRulesSetTargetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: forwardingRule
    pub forwardingRule: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for forwarding rule. The new target should be of the same type as the old target.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_set_target_builder()` + `compute_forwarding_rules_set_target_execute()`.
/// For task-level control, use `compute_forwarding_rules_set_target_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_set_target(
    client: &SimpleHttpClient,
    args: &ComputeForwardingRulesSetTargetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_set_target_builder(
        client,
        &args.project,
        &args.region,
        &args.forwardingRule,
        &args.requestId,
    )?;
    compute_forwarding_rules_set_target_execute(builder)
}

/// GET projects/{project}/aggregated/futureReservations
/// Retrieves an aggregated list of future reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_aggregated_list_execute()` to send, or `compute_future_reservations_aggregated_list` for simplest API.

pub fn compute_future_reservations_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/futureReservations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/futureReservations
/// Retrieves an aggregated list of future reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_future_reservations_aggregated_list_execute()` or `compute_future_reservations_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FutureReservationsAggregatedListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FutureReservationsAggregatedListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/futureReservations
/// Retrieves an aggregated list of future reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_future_reservations_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_future_reservations_aggregated_list_task()`.
/// For the simplest API, use `compute_future_reservations_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_future_reservations_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FutureReservationsAggregatedListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_future_reservations_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_future_reservations_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFutureReservationsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/futureReservations
/// Retrieves an aggregated list of future reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_aggregated_list_builder()` + `compute_future_reservations_aggregated_list_execute()`.
/// For task-level control, use `compute_future_reservations_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeFutureReservationsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FutureReservationsAggregatedListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_future_reservations_aggregated_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel
/// Cancel the specified future reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_cancel_execute()` to send, or `compute_future_reservations_cancel` for simplest API.

pub fn compute_future_reservations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    futureReservation: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations/{}/cancel",
        project,
        zone,
        futureReservation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel
/// Cancel the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_future_reservations_cancel_execute()` or `compute_future_reservations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel
/// Cancel the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_future_reservations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_future_reservations_cancel_task()`.
/// For the simplest API, use `compute_future_reservations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_future_reservations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_future_reservations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_future_reservations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFutureReservationsCancelArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: futureReservation
    pub futureReservation: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel
/// Cancel the specified future reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_cancel_builder()` + `compute_future_reservations_cancel_execute()`.
/// For task-level control, use `compute_future_reservations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_cancel(
    client: &SimpleHttpClient,
    args: &ComputeFutureReservationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_cancel_builder(
        client,
        &args.project,
        &args.zone,
        &args.futureReservation,
        &args.requestId,
    )?;
    compute_future_reservations_cancel_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Deletes the specified future reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_delete_execute()` to send, or `compute_future_reservations_delete` for simplest API.

pub fn compute_future_reservations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    futureReservation: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations/{}",
        project, zone, futureReservation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Deletes the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_future_reservations_delete_execute()` or `compute_future_reservations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Deletes the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_future_reservations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_future_reservations_delete_task()`.
/// For the simplest API, use `compute_future_reservations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_future_reservations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_future_reservations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_future_reservations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFutureReservationsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: futureReservation
    pub futureReservation: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Deletes the specified future reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_delete_builder()` + `compute_future_reservations_delete_execute()`.
/// For task-level control, use `compute_future_reservations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_delete(
    client: &SimpleHttpClient,
    args: &ComputeFutureReservationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.futureReservation,
        &args.requestId,
    )?;
    compute_future_reservations_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Retrieves information about the specified future reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_get_execute()` to send, or `compute_future_reservations_get` for simplest API.

pub fn compute_future_reservations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    futureReservation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations/{}",
        project, zone, futureReservation,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Retrieves information about the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_future_reservations_get_execute()` or `compute_future_reservations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FutureReservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FutureReservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Retrieves information about the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_future_reservations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_future_reservations_get_task()`.
/// For the simplest API, use `compute_future_reservations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_future_reservations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FutureReservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_future_reservations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_future_reservations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFutureReservationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: futureReservation
    pub futureReservation: String,
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Retrieves information about the specified future reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_get_builder()` + `compute_future_reservations_get_execute()`.
/// For task-level control, use `compute_future_reservations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_get(
    client: &SimpleHttpClient,
    args: &ComputeFutureReservationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FutureReservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.futureReservation,
    )?;
    compute_future_reservations_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/futureReservations
/// Creates a new Future Reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_insert_execute()` to send, or `compute_future_reservations_insert` for simplest API.

pub fn compute_future_reservations_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/futureReservations
/// Creates a new Future Reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_future_reservations_insert_execute()` or `compute_future_reservations_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/futureReservations
/// Creates a new Future Reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_future_reservations_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_future_reservations_insert_task()`.
/// For the simplest API, use `compute_future_reservations_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_future_reservations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_future_reservations_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_future_reservations_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFutureReservationsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/futureReservations
/// Creates a new Future Reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_insert_builder()` + `compute_future_reservations_insert_execute()`.
/// For task-level control, use `compute_future_reservations_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_insert(
    client: &SimpleHttpClient,
    args: &ComputeFutureReservationsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
    )?;
    compute_future_reservations_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// A list of all the future reservations that have been configured for the specified project in specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_list_execute()` to send, or `compute_future_reservations_list` for simplest API.

pub fn compute_future_reservations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// A list of all the future reservations that have been configured for the specified project in specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_future_reservations_list_execute()` or `compute_future_reservations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FutureReservationsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FutureReservationsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// A list of all the future reservations that have been configured for the specified project in specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_future_reservations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_future_reservations_list_task()`.
/// For the simplest API, use `compute_future_reservations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_future_reservations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FutureReservationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_future_reservations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_future_reservations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFutureReservationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// A list of all the future reservations that have been configured for the specified project in specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_list_builder()` + `compute_future_reservations_list_execute()`.
/// For task-level control, use `compute_future_reservations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_list(
    client: &SimpleHttpClient,
    args: &ComputeFutureReservationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FutureReservationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_future_reservations_list_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Updates the specified future reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_update_execute()` to send, or `compute_future_reservations_update` for simplest API.

pub fn compute_future_reservations_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    futureReservation: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations/{}",
        project, zone, futureReservation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Updates the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_future_reservations_update_execute()` or `compute_future_reservations_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Updates the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_future_reservations_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_future_reservations_update_task()`.
/// For the simplest API, use `compute_future_reservations_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_future_reservations_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_future_reservations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_future_reservations_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_future_reservations_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeFutureReservationsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: futureReservation
    pub futureReservation: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Updates the specified future reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_update_builder()` + `compute_future_reservations_update_execute()`.
/// For task-level control, use `compute_future_reservations_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_update(
    client: &SimpleHttpClient,
    args: &ComputeFutureReservationsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_update_builder(
        client,
        &args.project,
        &args.zone,
        &args.futureReservation,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_future_reservations_update_execute(builder)
}

/// DELETE projects/{project}/global/addresses/{address}
/// Deletes the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_delete_execute()` to send, or `compute_global_addresses_delete` for simplest API.

pub fn compute_global_addresses_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    address: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}",
        project, address,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/addresses/{address}
/// Deletes the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_addresses_delete_execute()` or `compute_global_addresses_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/addresses/{address}
/// Deletes the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_addresses_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_addresses_delete_task()`.
/// For the simplest API, use `compute_global_addresses_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_addresses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_addresses_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_addresses_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: address
    pub address: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/addresses/{address}
/// Deletes the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_delete_builder()` + `compute_global_addresses_delete_execute()`.
/// For task-level control, use `compute_global_addresses_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_delete(
    client: &SimpleHttpClient,
    args: &ComputeGlobalAddressesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_delete_builder(
        client,
        &args.project,
        &args.address,
        &args.requestId,
    )?;
    compute_global_addresses_delete_execute(builder)
}

/// GET projects/{project}/global/addresses/{address}
/// Returns the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_get_execute()` to send, or `compute_global_addresses_get` for simplest API.

pub fn compute_global_addresses_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    address: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}",
        project, address,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/addresses/{address}
/// Returns the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_addresses_get_execute()` or `compute_global_addresses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Address>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Address = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/addresses/{address}
/// Returns the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_addresses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_addresses_get_task()`.
/// For the simplest API, use `compute_global_addresses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_addresses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Address>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_addresses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_addresses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: address
    pub address: String,
}

/// GET projects/{project}/global/addresses/{address}
/// Returns the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_get_builder()` + `compute_global_addresses_get_execute()`.
/// For task-level control, use `compute_global_addresses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_get(
    client: &SimpleHttpClient,
    args: &ComputeGlobalAddressesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Address>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_get_builder(client, &args.project, &args.address)?;
    compute_global_addresses_get_execute(builder)
}

/// POST projects/{project}/global/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_insert_execute()` to send, or `compute_global_addresses_insert` for simplest API.

pub fn compute_global_addresses_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_addresses_insert_execute()` or `compute_global_addresses_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_addresses_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_addresses_insert_task()`.
/// For the simplest API, use `compute_global_addresses_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_addresses_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_addresses_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_addresses_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_insert_builder()` + `compute_global_addresses_insert_execute()`.
/// For task-level control, use `compute_global_addresses_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_insert(
    client: &SimpleHttpClient,
    args: &ComputeGlobalAddressesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_insert_builder(client, &args.project, &args.requestId)?;
    compute_global_addresses_insert_execute(builder)
}

/// GET projects/{project}/global/addresses
/// Retrieves a list of global addresses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_list_execute()` to send, or `compute_global_addresses_list` for simplest API.

pub fn compute_global_addresses_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/addresses
/// Retrieves a list of global addresses.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_addresses_list_execute()` or `compute_global_addresses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddressList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/addresses
/// Retrieves a list of global addresses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_addresses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_addresses_list_task()`.
/// For the simplest API, use `compute_global_addresses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_addresses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_addresses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_addresses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/addresses
/// Retrieves a list of global addresses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_list_builder()` + `compute_global_addresses_list_execute()`.
/// For task-level control, use `compute_global_addresses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_list(
    client: &SimpleHttpClient,
    args: &ComputeGlobalAddressesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_global_addresses_list_execute(builder)
}

/// POST projects/{project}/global/addresses/{address}/move
/// Moves the specified address resource from one project to another project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_move_execute()` to send, or `compute_global_addresses_move` for simplest API.

pub fn compute_global_addresses_move_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    address: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}/move",
        project, address,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/addresses/{address}/move
/// Moves the specified address resource from one project to another project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_addresses_move_execute()` or `compute_global_addresses_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/addresses/{address}/move
/// Moves the specified address resource from one project to another project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_addresses_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_addresses_move_task()`.
/// For the simplest API, use `compute_global_addresses_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_addresses_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_addresses_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_addresses_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesMoveArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: address
    pub address: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/addresses/{address}/move
/// Moves the specified address resource from one project to another project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_move_builder()` + `compute_global_addresses_move_execute()`.
/// For task-level control, use `compute_global_addresses_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_move(
    client: &SimpleHttpClient,
    args: &ComputeGlobalAddressesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_move_builder(
        client,
        &args.project,
        &args.address,
        &args.requestId,
    )?;
    compute_global_addresses_move_execute(builder)
}

/// POST projects/{project}/global/addresses/{resource}/setLabels
/// Sets the labels on a GlobalAddress. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_set_labels_execute()` to send, or `compute_global_addresses_set_labels` for simplest API.

pub fn compute_global_addresses_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}/setLabels",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/addresses/{resource}/setLabels
/// Sets the labels on a GlobalAddress. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_addresses_set_labels_execute()` or `compute_global_addresses_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/addresses/{resource}/setLabels
/// Sets the labels on a GlobalAddress. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_addresses_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_addresses_set_labels_task()`.
/// For the simplest API, use `compute_global_addresses_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_addresses_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_addresses_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_addresses_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/addresses/{resource}/setLabels
/// Sets the labels on a GlobalAddress. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_set_labels_builder()` + `compute_global_addresses_set_labels_execute()`.
/// For task-level control, use `compute_global_addresses_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeGlobalAddressesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_global_addresses_set_labels_builder(client, &args.project, &args.resource)?;
    compute_global_addresses_set_labels_execute(builder)
}

/// POST projects/{project}/global/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_test_iam_permissions_execute()` to send, or `compute_global_addresses_test_iam_permissions` for simplest API.

pub fn compute_global_addresses_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_addresses_test_iam_permissions_execute()` or `compute_global_addresses_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_addresses_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_addresses_test_iam_permissions_task()`.
/// For the simplest API, use `compute_global_addresses_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_addresses_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_addresses_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_addresses_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_addresses_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_test_iam_permissions_builder()` + `compute_global_addresses_test_iam_permissions_execute()`.
/// For task-level control, use `compute_global_addresses_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeGlobalAddressesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_global_addresses_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/global/forwardingRules/{forwardingRule}
/// Deletes the specified GlobalForwardingRule resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_delete_execute()` to send, or `compute_global_forwarding_rules_delete` for simplest API.

pub fn compute_global_forwarding_rules_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    forwardingRule: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}",
        project, forwardingRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/forwardingRules/{forwardingRule}
/// Deletes the specified GlobalForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_forwarding_rules_delete_execute()` or `compute_global_forwarding_rules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/forwardingRules/{forwardingRule}
/// Deletes the specified GlobalForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_forwarding_rules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_forwarding_rules_delete_task()`.
/// For the simplest API, use `compute_global_forwarding_rules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_forwarding_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_forwarding_rules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_forwarding_rules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: forwardingRule
    pub forwardingRule: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/forwardingRules/{forwardingRule}
/// Deletes the specified GlobalForwardingRule resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_delete_builder()` + `compute_global_forwarding_rules_delete_execute()`.
/// For task-level control, use `compute_global_forwarding_rules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_delete(
    client: &SimpleHttpClient,
    args: &ComputeGlobalForwardingRulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_delete_builder(
        client,
        &args.project,
        &args.forwardingRule,
        &args.requestId,
    )?;
    compute_global_forwarding_rules_delete_execute(builder)
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Returns the specified GlobalForwardingRule resource. Gets a list of available forwarding rules by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_get_execute()` to send, or `compute_global_forwarding_rules_get` for simplest API.

pub fn compute_global_forwarding_rules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    forwardingRule: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}",
        project, forwardingRule,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Returns the specified GlobalForwardingRule resource. Gets a list of available forwarding rules by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_forwarding_rules_get_execute()` or `compute_global_forwarding_rules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Returns the specified GlobalForwardingRule resource. Gets a list of available forwarding rules by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_forwarding_rules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_forwarding_rules_get_task()`.
/// For the simplest API, use `compute_global_forwarding_rules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_forwarding_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_forwarding_rules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_forwarding_rules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: forwardingRule
    pub forwardingRule: String,
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Returns the specified GlobalForwardingRule resource. Gets a list of available forwarding rules by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_get_builder()` + `compute_global_forwarding_rules_get_execute()`.
/// For task-level control, use `compute_global_forwarding_rules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_get(
    client: &SimpleHttpClient,
    args: &ComputeGlobalForwardingRulesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_global_forwarding_rules_get_builder(client, &args.project, &args.forwardingRule)?;
    compute_global_forwarding_rules_get_execute(builder)
}

/// POST projects/{project}/global/forwardingRules
/// Creates a GlobalForwardingRule resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_insert_execute()` to send, or `compute_global_forwarding_rules_insert` for simplest API.

pub fn compute_global_forwarding_rules_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/forwardingRules
/// Creates a GlobalForwardingRule resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_forwarding_rules_insert_execute()` or `compute_global_forwarding_rules_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/forwardingRules
/// Creates a GlobalForwardingRule resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_forwarding_rules_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_forwarding_rules_insert_task()`.
/// For the simplest API, use `compute_global_forwarding_rules_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_forwarding_rules_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_forwarding_rules_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_forwarding_rules_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/forwardingRules
/// Creates a GlobalForwardingRule resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_insert_builder()` + `compute_global_forwarding_rules_insert_execute()`.
/// For task-level control, use `compute_global_forwarding_rules_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_insert(
    client: &SimpleHttpClient,
    args: &ComputeGlobalForwardingRulesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_global_forwarding_rules_insert_builder(client, &args.project, &args.requestId)?;
    compute_global_forwarding_rules_insert_execute(builder)
}

/// GET projects/{project}/global/forwardingRules
/// Retrieves a list of GlobalForwardingRule resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_list_execute()` to send, or `compute_global_forwarding_rules_list` for simplest API.

pub fn compute_global_forwarding_rules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/forwardingRules
/// Retrieves a list of GlobalForwardingRule resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_forwarding_rules_list_execute()` or `compute_global_forwarding_rules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRuleList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRuleList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/forwardingRules
/// Retrieves a list of GlobalForwardingRule resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_forwarding_rules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_forwarding_rules_list_task()`.
/// For the simplest API, use `compute_global_forwarding_rules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_forwarding_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRuleList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_forwarding_rules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_forwarding_rules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/forwardingRules
/// Retrieves a list of GlobalForwardingRule resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_list_builder()` + `compute_global_forwarding_rules_list_execute()`.
/// For task-level control, use `compute_global_forwarding_rules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_list(
    client: &SimpleHttpClient,
    args: &ComputeGlobalForwardingRulesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRuleList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_global_forwarding_rules_list_execute(builder)
}

/// PATCH projects/{project}/global/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_patch_execute()` to send, or `compute_global_forwarding_rules_patch` for simplest API.

pub fn compute_global_forwarding_rules_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    forwardingRule: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}",
        project, forwardingRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_forwarding_rules_patch_execute()` or `compute_global_forwarding_rules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_forwarding_rules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_forwarding_rules_patch_task()`.
/// For the simplest API, use `compute_global_forwarding_rules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_forwarding_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_forwarding_rules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_forwarding_rules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: forwardingRule
    pub forwardingRule: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_patch_builder()` + `compute_global_forwarding_rules_patch_execute()`.
/// For task-level control, use `compute_global_forwarding_rules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_patch(
    client: &SimpleHttpClient,
    args: &ComputeGlobalForwardingRulesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_patch_builder(
        client,
        &args.project,
        &args.forwardingRule,
        &args.requestId,
    )?;
    compute_global_forwarding_rules_patch_execute(builder)
}

/// POST projects/{project}/global/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_set_labels_execute()` to send, or `compute_global_forwarding_rules_set_labels` for simplest API.

pub fn compute_global_forwarding_rules_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}/setLabels",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_forwarding_rules_set_labels_execute()` or `compute_global_forwarding_rules_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_forwarding_rules_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_forwarding_rules_set_labels_task()`.
/// For the simplest API, use `compute_global_forwarding_rules_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_forwarding_rules_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_forwarding_rules_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_forwarding_rules_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_set_labels_builder()` + `compute_global_forwarding_rules_set_labels_execute()`.
/// For task-level control, use `compute_global_forwarding_rules_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeGlobalForwardingRulesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_global_forwarding_rules_set_labels_builder(client, &args.project, &args.resource)?;
    compute_global_forwarding_rules_set_labels_execute(builder)
}

/// POST projects/{project}/global/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for the GlobalForwardingRule resource. The new target should be of the same type as the old target.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_set_target_execute()` to send, or `compute_global_forwarding_rules_set_target` for simplest API.

pub fn compute_global_forwarding_rules_set_target_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    forwardingRule: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}/setTarget",
        project, forwardingRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for the GlobalForwardingRule resource. The new target should be of the same type as the old target.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_forwarding_rules_set_target_execute()` or `compute_global_forwarding_rules_set_target`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_set_target_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_set_target_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for the GlobalForwardingRule resource. The new target should be of the same type as the old target.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_forwarding_rules_set_target_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_forwarding_rules_set_target_task()`.
/// For the simplest API, use `compute_global_forwarding_rules_set_target()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_forwarding_rules_set_target_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_forwarding_rules_set_target_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_forwarding_rules_set_target_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_forwarding_rules_set_target`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesSetTargetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: forwardingRule
    pub forwardingRule: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for the GlobalForwardingRule resource. The new target should be of the same type as the old target.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_set_target_builder()` + `compute_global_forwarding_rules_set_target_execute()`.
/// For task-level control, use `compute_global_forwarding_rules_set_target_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_set_target(
    client: &SimpleHttpClient,
    args: &ComputeGlobalForwardingRulesSetTargetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_set_target_builder(
        client,
        &args.project,
        &args.forwardingRule,
        &args.requestId,
    )?;
    compute_global_forwarding_rules_set_target_execute(builder)
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a network endpoint to the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_attach_network_endpoints_execute()` to send, or `compute_global_network_endpoint_groups_attach_network_endpoints` for simplest API.

pub fn compute_global_network_endpoint_groups_attach_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}/attachNetworkEndpoints",
        project,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a network endpoint to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_network_endpoint_groups_attach_network_endpoints_execute()` or `compute_global_network_endpoint_groups_attach_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_attach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_attach_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a network endpoint to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_network_endpoint_groups_attach_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_network_endpoint_groups_attach_network_endpoints_task()`.
/// For the simplest API, use `compute_global_network_endpoint_groups_attach_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_attach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_network_endpoint_groups_attach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_network_endpoint_groups_attach_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_network_endpoint_groups_attach_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a network endpoint to the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_attach_network_endpoints_builder()` + `compute_global_network_endpoint_groups_attach_network_endpoints_execute()`.
/// For task-level control, use `compute_global_network_endpoint_groups_attach_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_attach_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_attach_network_endpoints_builder(
        client,
        &args.project,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_global_network_endpoint_groups_attach_network_endpoints_execute(builder)
}

/// DELETE projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group.Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_delete_execute()` to send, or `compute_global_network_endpoint_groups_delete` for simplest API.

pub fn compute_global_network_endpoint_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}",
        project, networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group.Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_network_endpoint_groups_delete_execute()` or `compute_global_network_endpoint_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group.Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_network_endpoint_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_network_endpoint_groups_delete_task()`.
/// For the simplest API, use `compute_global_network_endpoint_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_network_endpoint_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_network_endpoint_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_network_endpoint_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group.Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_delete_builder()` + `compute_global_network_endpoint_groups_delete_execute()`.
/// For task-level control, use `compute_global_network_endpoint_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeGlobalNetworkEndpointGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_delete_builder(
        client,
        &args.project,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_global_network_endpoint_groups_delete_execute(builder)
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_detach_network_endpoints_execute()` to send, or `compute_global_network_endpoint_groups_detach_network_endpoints` for simplest API.

pub fn compute_global_network_endpoint_groups_detach_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}/detachNetworkEndpoints",
        project,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_network_endpoint_groups_detach_network_endpoints_execute()` or `compute_global_network_endpoint_groups_detach_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_detach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_detach_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_network_endpoint_groups_detach_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_network_endpoint_groups_detach_network_endpoints_task()`.
/// For the simplest API, use `compute_global_network_endpoint_groups_detach_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_detach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_network_endpoint_groups_detach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_network_endpoint_groups_detach_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_network_endpoint_groups_detach_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_detach_network_endpoints_builder()` + `compute_global_network_endpoint_groups_detach_network_endpoints_execute()`.
/// For task-level control, use `compute_global_network_endpoint_groups_detach_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_detach_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_detach_network_endpoints_builder(
        client,
        &args.project,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_global_network_endpoint_groups_detach_network_endpoints_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_get_execute()` to send, or `compute_global_network_endpoint_groups_get` for simplest API.

pub fn compute_global_network_endpoint_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    networkEndpointGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}",
        project, networkEndpointGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_network_endpoint_groups_get_execute()` or `compute_global_network_endpoint_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_network_endpoint_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_network_endpoint_groups_get_task()`.
/// For the simplest API, use `compute_global_network_endpoint_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_network_endpoint_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_network_endpoint_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_network_endpoint_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_get_builder()` + `compute_global_network_endpoint_groups_get_execute()`.
/// For task-level control, use `compute_global_network_endpoint_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeGlobalNetworkEndpointGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_get_builder(
        client,
        &args.project,
        &args.networkEndpointGroup,
    )?;
    compute_global_network_endpoint_groups_get_execute(builder)
}

/// POST projects/{project}/global/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_insert_execute()` to send, or `compute_global_network_endpoint_groups_insert` for simplest API.

pub fn compute_global_network_endpoint_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_network_endpoint_groups_insert_execute()` or `compute_global_network_endpoint_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_network_endpoint_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_network_endpoint_groups_insert_task()`.
/// For the simplest API, use `compute_global_network_endpoint_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_network_endpoint_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_network_endpoint_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_network_endpoint_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_insert_builder()` + `compute_global_network_endpoint_groups_insert_execute()`.
/// For task-level control, use `compute_global_network_endpoint_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeGlobalNetworkEndpointGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_insert_builder(
        client,
        &args.project,
        &args.requestId,
    )?;
    compute_global_network_endpoint_groups_insert_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_list_execute()` to send, or `compute_global_network_endpoint_groups_list` for simplest API.

pub fn compute_global_network_endpoint_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_network_endpoint_groups_list_execute()` or `compute_global_network_endpoint_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_network_endpoint_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_network_endpoint_groups_list_task()`.
/// For the simplest API, use `compute_global_network_endpoint_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_network_endpoint_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_network_endpoint_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_network_endpoint_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_list_builder()` + `compute_global_network_endpoint_groups_list_execute()`.
/// For task-level control, use `compute_global_network_endpoint_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeGlobalNetworkEndpointGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_global_network_endpoint_groups_list_execute(builder)
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_list_network_endpoints_execute()` to send, or `compute_global_network_endpoint_groups_list_network_endpoints` for simplest API.

pub fn compute_global_network_endpoint_groups_list_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    networkEndpointGroup: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}/listNetworkEndpoints",
        project,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_network_endpoint_groups_list_network_endpoints_execute()` or `compute_global_network_endpoint_groups_list_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_list_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_list_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_network_endpoint_groups_list_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_network_endpoint_groups_list_network_endpoints_task()`.
/// For the simplest API, use `compute_global_network_endpoint_groups_list_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_network_endpoint_groups_list_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_network_endpoint_groups_list_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_network_endpoint_groups_list_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_network_endpoint_groups_list_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_list_network_endpoints_builder()` + `compute_global_network_endpoint_groups_list_network_endpoints_execute()`.
/// For task-level control, use `compute_global_network_endpoint_groups_list_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_list_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_list_network_endpoints_builder(
        client,
        &args.project,
        &args.networkEndpointGroup,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_global_network_endpoint_groups_list_network_endpoints_execute(builder)
}

/// GET projects/{project}/aggregated/operations
/// Retrieves an aggregated list of all operations.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_aggregated_list_execute()` to send, or `compute_global_operations_aggregated_list` for simplest API.

pub fn compute_global_operations_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/operations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/operations
/// Retrieves an aggregated list of all operations.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_operations_aggregated_list_execute()` or `compute_global_operations_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/operations
/// Retrieves an aggregated list of all operations.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_operations_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_operations_aggregated_list_task()`.
/// For the simplest API, use `compute_global_operations_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_operations_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_operations_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_operations_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/operations
/// Retrieves an aggregated list of all operations.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_aggregated_list_builder()` + `compute_global_operations_aggregated_list_execute()`.
/// For task-level control, use `compute_global_operations_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeGlobalOperationsAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_operations_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_global_operations_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_delete_execute()` to send, or `compute_global_operations_delete` for simplest API.

pub fn compute_global_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/operations/{}",
        project, operation,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_operations_delete_execute()` or `compute_global_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_operations_delete_task()`.
/// For the simplest API, use `compute_global_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: operation
    pub operation: String,
}

/// DELETE projects/{project}/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_delete_builder()` + `compute_global_operations_delete_execute()`.
/// For task-level control, use `compute_global_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_delete(
    client: &SimpleHttpClient,
    args: &ComputeGlobalOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_operations_delete_builder(client, &args.project, &args.operation)?;
    compute_global_operations_delete_execute(builder)
}

/// GET projects/{project}/global/operations/{operation}
/// Retrieves the specified Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_get_execute()` to send, or `compute_global_operations_get` for simplest API.

pub fn compute_global_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/operations/{}",
        project, operation,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/operations/{operation}
/// Retrieves the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_operations_get_execute()` or `compute_global_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/operations/{operation}
/// Retrieves the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_operations_get_task()`.
/// For the simplest API, use `compute_global_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: operation
    pub operation: String,
}

/// GET projects/{project}/global/operations/{operation}
/// Retrieves the specified Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_get_builder()` + `compute_global_operations_get_execute()`.
/// For task-level control, use `compute_global_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_get(
    client: &SimpleHttpClient,
    args: &ComputeGlobalOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_operations_get_builder(client, &args.project, &args.operation)?;
    compute_global_operations_get_execute(builder)
}

/// GET projects/{project}/global/operations
/// Retrieves a list of Operation resources contained within the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_list_execute()` to send, or `compute_global_operations_list` for simplest API.

pub fn compute_global_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/operations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/operations
/// Retrieves a list of Operation resources contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_operations_list_execute()` or `compute_global_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/operations
/// Retrieves a list of Operation resources contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_operations_list_task()`.
/// For the simplest API, use `compute_global_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/operations
/// Retrieves a list of Operation resources contained within the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_list_builder()` + `compute_global_operations_list_execute()`.
/// For task-level control, use `compute_global_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_list(
    client: &SimpleHttpClient,
    args: &ComputeGlobalOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_operations_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_global_operations_list_execute(builder)
}

/// POST projects/{project}/global/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_wait_execute()` to send, or `compute_global_operations_wait` for simplest API.

pub fn compute_global_operations_wait_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/operations/{}/wait",
        project, operation,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_operations_wait_execute()` or `compute_global_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_operations_wait_task()`.
/// For the simplest API, use `compute_global_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsWaitArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: operation
    pub operation: String,
}

/// POST projects/{project}/global/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_wait_builder()` + `compute_global_operations_wait_execute()`.
/// For task-level control, use `compute_global_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_wait(
    client: &SimpleHttpClient,
    args: &ComputeGlobalOperationsWaitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_operations_wait_builder(client, &args.project, &args.operation)?;
    compute_global_operations_wait_execute(builder)
}

/// DELETE locations/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_organization_operations_delete_execute()` to send, or `compute_global_organization_operations_delete` for simplest API.

pub fn compute_global_organization_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    operation: &String,
    parentId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/operations/{}",
        operation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE locations/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_organization_operations_delete_execute()` or `compute_global_organization_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_organization_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE locations/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_organization_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_organization_operations_delete_task()`.
/// For the simplest API, use `compute_global_organization_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_organization_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_organization_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_organization_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_organization_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalOrganizationOperationsDeleteArgs {
    /// Path parameter: operation
    pub operation: String,
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
}

/// DELETE locations/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_organization_operations_delete_builder()` + `compute_global_organization_operations_delete_execute()`.
/// For task-level control, use `compute_global_organization_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_delete(
    client: &SimpleHttpClient,
    args: &ComputeGlobalOrganizationOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_organization_operations_delete_builder(
        client,
        &args.operation,
        &args.parentId,
    )?;
    compute_global_organization_operations_delete_execute(builder)
}

/// GET locations/global/operations/{operation}
/// Retrieves the specified Operations resource. Gets a list of operations by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_organization_operations_get_execute()` to send, or `compute_global_organization_operations_get` for simplest API.

pub fn compute_global_organization_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    operation: &String,
    parentId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/operations/{}",
        operation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/operations/{operation}
/// Retrieves the specified Operations resource. Gets a list of operations by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_organization_operations_get_execute()` or `compute_global_organization_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_organization_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/operations/{operation}
/// Retrieves the specified Operations resource. Gets a list of operations by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_organization_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_organization_operations_get_task()`.
/// For the simplest API, use `compute_global_organization_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_organization_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_organization_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_organization_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_organization_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalOrganizationOperationsGetArgs {
    /// Path parameter: operation
    pub operation: String,
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
}

/// GET locations/global/operations/{operation}
/// Retrieves the specified Operations resource. Gets a list of operations by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_organization_operations_get_builder()` + `compute_global_organization_operations_get_execute()`.
/// For task-level control, use `compute_global_organization_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_get(
    client: &SimpleHttpClient,
    args: &ComputeGlobalOrganizationOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_organization_operations_get_builder(
        client,
        &args.operation,
        &args.parentId,
    )?;
    compute_global_organization_operations_get_execute(builder)
}

/// GET locations/global/operations
/// Retrieves a list of Operation resources contained within the specified organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_organization_operations_list_execute()` to send, or `compute_global_organization_operations_list` for simplest API.

pub fn compute_global_organization_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    parentId: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/operations
/// Retrieves a list of Operation resources contained within the specified organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_organization_operations_list_execute()` or `compute_global_organization_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_organization_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/operations
/// Retrieves a list of Operation resources contained within the specified organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_organization_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_organization_operations_list_task()`.
/// For the simplest API, use `compute_global_organization_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_organization_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_organization_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_organization_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_organization_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalOrganizationOperationsListArgs {
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET locations/global/operations
/// Retrieves a list of Operation resources contained within the specified organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_organization_operations_list_builder()` + `compute_global_organization_operations_list_execute()`.
/// For task-level control, use `compute_global_organization_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_list(
    client: &SimpleHttpClient,
    args: &ComputeGlobalOrganizationOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_organization_operations_list_builder(
        client,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.parentId,
        &args.returnPartialSuccess,
    )?;
    compute_global_organization_operations_list_execute(builder)
}

/// DELETE projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified global PublicDelegatedPrefix.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_delete_execute()` to send, or `compute_global_public_delegated_prefixes_delete` for simplest API.

pub fn compute_global_public_delegated_prefixes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    publicDelegatedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified global PublicDelegatedPrefix.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_public_delegated_prefixes_delete_execute()` or `compute_global_public_delegated_prefixes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified global PublicDelegatedPrefix.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_public_delegated_prefixes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_public_delegated_prefixes_delete_task()`.
/// For the simplest API, use `compute_global_public_delegated_prefixes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_public_delegated_prefixes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_public_delegated_prefixes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_public_delegated_prefixes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: publicDelegatedPrefix
    pub publicDelegatedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified global PublicDelegatedPrefix.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_delete_builder()` + `compute_global_public_delegated_prefixes_delete_execute()`.
/// For task-level control, use `compute_global_public_delegated_prefixes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_delete(
    client: &SimpleHttpClient,
    args: &ComputeGlobalPublicDelegatedPrefixesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_delete_builder(
        client,
        &args.project,
        &args.publicDelegatedPrefix,
        &args.requestId,
    )?;
    compute_global_public_delegated_prefixes_delete_execute(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified global PublicDelegatedPrefix resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_get_execute()` to send, or `compute_global_public_delegated_prefixes_get` for simplest API.

pub fn compute_global_public_delegated_prefixes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    publicDelegatedPrefix: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified global PublicDelegatedPrefix resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_public_delegated_prefixes_get_execute()` or `compute_global_public_delegated_prefixes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified global PublicDelegatedPrefix resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_public_delegated_prefixes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_public_delegated_prefixes_get_task()`.
/// For the simplest API, use `compute_global_public_delegated_prefixes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_public_delegated_prefixes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_public_delegated_prefixes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_public_delegated_prefixes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: publicDelegatedPrefix
    pub publicDelegatedPrefix: String,
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified global PublicDelegatedPrefix resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_get_builder()` + `compute_global_public_delegated_prefixes_get_execute()`.
/// For task-level control, use `compute_global_public_delegated_prefixes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_get(
    client: &SimpleHttpClient,
    args: &ComputeGlobalPublicDelegatedPrefixesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_get_builder(
        client,
        &args.project,
        &args.publicDelegatedPrefix,
    )?;
    compute_global_public_delegated_prefixes_get_execute(builder)
}

/// POST projects/{project}/global/publicDelegatedPrefixes
/// Creates a global PublicDelegatedPrefix in the specified project using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_insert_execute()` to send, or `compute_global_public_delegated_prefixes_insert` for simplest API.

pub fn compute_global_public_delegated_prefixes_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/publicDelegatedPrefixes
/// Creates a global PublicDelegatedPrefix in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_public_delegated_prefixes_insert_execute()` or `compute_global_public_delegated_prefixes_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/publicDelegatedPrefixes
/// Creates a global PublicDelegatedPrefix in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_public_delegated_prefixes_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_public_delegated_prefixes_insert_task()`.
/// For the simplest API, use `compute_global_public_delegated_prefixes_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_public_delegated_prefixes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_public_delegated_prefixes_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_public_delegated_prefixes_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/publicDelegatedPrefixes
/// Creates a global PublicDelegatedPrefix in the specified project using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_insert_builder()` + `compute_global_public_delegated_prefixes_insert_execute()`.
/// For task-level control, use `compute_global_public_delegated_prefixes_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_insert(
    client: &SimpleHttpClient,
    args: &ComputeGlobalPublicDelegatedPrefixesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_insert_builder(
        client,
        &args.project,
        &args.requestId,
    )?;
    compute_global_public_delegated_prefixes_insert_execute(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Lists the global PublicDelegatedPrefixes for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_list_execute()` to send, or `compute_global_public_delegated_prefixes_list` for simplest API.

pub fn compute_global_public_delegated_prefixes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Lists the global PublicDelegatedPrefixes for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_public_delegated_prefixes_list_execute()` or `compute_global_public_delegated_prefixes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Lists the global PublicDelegatedPrefixes for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_public_delegated_prefixes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_public_delegated_prefixes_list_task()`.
/// For the simplest API, use `compute_global_public_delegated_prefixes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_public_delegated_prefixes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_global_public_delegated_prefixes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_public_delegated_prefixes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Lists the global PublicDelegatedPrefixes for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_list_builder()` + `compute_global_public_delegated_prefixes_list_execute()`.
/// For task-level control, use `compute_global_public_delegated_prefixes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_list(
    client: &SimpleHttpClient,
    args: &ComputeGlobalPublicDelegatedPrefixesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_global_public_delegated_prefixes_list_execute(builder)
}

/// PATCH projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified global PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_patch_execute()` to send, or `compute_global_public_delegated_prefixes_patch` for simplest API.

pub fn compute_global_public_delegated_prefixes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    publicDelegatedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified global PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_global_public_delegated_prefixes_patch_execute()` or `compute_global_public_delegated_prefixes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified global PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_global_public_delegated_prefixes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_global_public_delegated_prefixes_patch_task()`.
/// For the simplest API, use `compute_global_public_delegated_prefixes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_global_public_delegated_prefixes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_global_public_delegated_prefixes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_global_public_delegated_prefixes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_global_public_delegated_prefixes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: publicDelegatedPrefix
    pub publicDelegatedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified global PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_patch_builder()` + `compute_global_public_delegated_prefixes_patch_execute()`.
/// For task-level control, use `compute_global_public_delegated_prefixes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_patch(
    client: &SimpleHttpClient,
    args: &ComputeGlobalPublicDelegatedPrefixesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_patch_builder(
        client,
        &args.project,
        &args.publicDelegatedPrefix,
        &args.requestId,
    )?;
    compute_global_public_delegated_prefixes_patch_execute(builder)
}

/// GET projects/{project}/aggregated/healthChecks
/// Retrieves the list of all HealthCheck resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_aggregated_list_execute()` to send, or `compute_health_checks_aggregated_list` for simplest API.

pub fn compute_health_checks_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/healthChecks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/healthChecks
/// Retrieves the list of all HealthCheck resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_health_checks_aggregated_list_execute()` or `compute_health_checks_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthChecksAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthChecksAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/healthChecks
/// Retrieves the list of all HealthCheck resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_health_checks_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_health_checks_aggregated_list_task()`.
/// For the simplest API, use `compute_health_checks_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_health_checks_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthChecksAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_health_checks_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_health_checks_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHealthChecksAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/healthChecks
/// Retrieves the list of all HealthCheck resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_aggregated_list_builder()` + `compute_health_checks_aggregated_list_execute()`.
/// For task-level control, use `compute_health_checks_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeHealthChecksAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthChecksAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_health_checks_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_health_checks_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_delete_execute()` to send, or `compute_health_checks_delete` for simplest API.

pub fn compute_health_checks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    healthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}",
        project, healthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_health_checks_delete_execute()` or `compute_health_checks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_health_checks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_health_checks_delete_task()`.
/// For the simplest API, use `compute_health_checks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_health_checks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_health_checks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHealthChecksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: healthCheck
    pub healthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_delete_builder()` + `compute_health_checks_delete_execute()`.
/// For task-level control, use `compute_health_checks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_delete(
    client: &SimpleHttpClient,
    args: &ComputeHealthChecksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_health_checks_delete_builder(
        client,
        &args.project,
        &args.healthCheck,
        &args.requestId,
    )?;
    compute_health_checks_delete_execute(builder)
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_get_execute()` to send, or `compute_health_checks_get` for simplest API.

pub fn compute_health_checks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    healthCheck: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}",
        project, healthCheck,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_health_checks_get_execute()` or `compute_health_checks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheck>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_health_checks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_health_checks_get_task()`.
/// For the simplest API, use `compute_health_checks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheck>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_health_checks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_health_checks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHealthChecksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: healthCheck
    pub healthCheck: String,
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_get_builder()` + `compute_health_checks_get_execute()`.
/// For task-level control, use `compute_health_checks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_get(
    client: &SimpleHttpClient,
    args: &ComputeHealthChecksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheck>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_health_checks_get_builder(client, &args.project, &args.healthCheck)?;
    compute_health_checks_get_execute(builder)
}

/// POST projects/{project}/global/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_insert_execute()` to send, or `compute_health_checks_insert` for simplest API.

pub fn compute_health_checks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_health_checks_insert_execute()` or `compute_health_checks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_health_checks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_health_checks_insert_task()`.
/// For the simplest API, use `compute_health_checks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_health_checks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_health_checks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHealthChecksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_insert_builder()` + `compute_health_checks_insert_execute()`.
/// For task-level control, use `compute_health_checks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_insert(
    client: &SimpleHttpClient,
    args: &ComputeHealthChecksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_health_checks_insert_builder(client, &args.project, &args.requestId)?;
    compute_health_checks_insert_execute(builder)
}

/// GET projects/{project}/global/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_list_execute()` to send, or `compute_health_checks_list` for simplest API.

pub fn compute_health_checks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_health_checks_list_execute()` or `compute_health_checks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_health_checks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_health_checks_list_task()`.
/// For the simplest API, use `compute_health_checks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_health_checks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_health_checks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHealthChecksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_list_builder()` + `compute_health_checks_list_execute()`.
/// For task-level control, use `compute_health_checks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_list(
    client: &SimpleHttpClient,
    args: &ComputeHealthChecksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_health_checks_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_health_checks_list_execute(builder)
}

/// PATCH projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_patch_execute()` to send, or `compute_health_checks_patch` for simplest API.

pub fn compute_health_checks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    healthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}",
        project, healthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_health_checks_patch_execute()` or `compute_health_checks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_health_checks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_health_checks_patch_task()`.
/// For the simplest API, use `compute_health_checks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_health_checks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_health_checks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHealthChecksPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: healthCheck
    pub healthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_patch_builder()` + `compute_health_checks_patch_execute()`.
/// For task-level control, use `compute_health_checks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_patch(
    client: &SimpleHttpClient,
    args: &ComputeHealthChecksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_health_checks_patch_builder(
        client,
        &args.project,
        &args.healthCheck,
        &args.requestId,
    )?;
    compute_health_checks_patch_execute(builder)
}

/// POST projects/{project}/global/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_test_iam_permissions_execute()` to send, or `compute_health_checks_test_iam_permissions` for simplest API.

pub fn compute_health_checks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_health_checks_test_iam_permissions_execute()` or `compute_health_checks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_health_checks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_health_checks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_health_checks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_health_checks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_health_checks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHealthChecksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_test_iam_permissions_builder()` + `compute_health_checks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_health_checks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeHealthChecksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_health_checks_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_health_checks_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_update_execute()` to send, or `compute_health_checks_update` for simplest API.

pub fn compute_health_checks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    healthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}",
        project, healthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_health_checks_update_execute()` or `compute_health_checks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_health_checks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_health_checks_update_task()`.
/// For the simplest API, use `compute_health_checks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_health_checks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_health_checks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_health_checks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_health_checks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHealthChecksUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: healthCheck
    pub healthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_update_builder()` + `compute_health_checks_update_execute()`.
/// For task-level control, use `compute_health_checks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_update(
    client: &SimpleHttpClient,
    args: &ComputeHealthChecksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_health_checks_update_builder(
        client,
        &args.project,
        &args.healthCheck,
        &args.requestId,
    )?;
    compute_health_checks_update_execute(builder)
}

/// DELETE projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Deletes the specified HttpHealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_delete_execute()` to send, or `compute_http_health_checks_delete` for simplest API.

pub fn compute_http_health_checks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    httpHealthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}",
        project, httpHealthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Deletes the specified HttpHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_http_health_checks_delete_execute()` or `compute_http_health_checks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Deletes the specified HttpHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_http_health_checks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_http_health_checks_delete_task()`.
/// For the simplest API, use `compute_http_health_checks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_http_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_http_health_checks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_http_health_checks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: httpHealthCheck
    pub httpHealthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Deletes the specified HttpHealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_delete_builder()` + `compute_http_health_checks_delete_execute()`.
/// For task-level control, use `compute_http_health_checks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_delete(
    client: &SimpleHttpClient,
    args: &ComputeHttpHealthChecksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_delete_builder(
        client,
        &args.project,
        &args.httpHealthCheck,
        &args.requestId,
    )?;
    compute_http_health_checks_delete_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Returns the specified HttpHealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_get_execute()` to send, or `compute_http_health_checks_get` for simplest API.

pub fn compute_http_health_checks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    httpHealthCheck: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}",
        project, httpHealthCheck,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Returns the specified HttpHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_http_health_checks_get_execute()` or `compute_http_health_checks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpHealthCheck>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpHealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Returns the specified HttpHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_http_health_checks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_http_health_checks_get_task()`.
/// For the simplest API, use `compute_http_health_checks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_http_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_http_health_checks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_http_health_checks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: httpHealthCheck
    pub httpHealthCheck: String,
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Returns the specified HttpHealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_get_builder()` + `compute_http_health_checks_get_execute()`.
/// For task-level control, use `compute_http_health_checks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_get(
    client: &SimpleHttpClient,
    args: &ComputeHttpHealthChecksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_http_health_checks_get_builder(client, &args.project, &args.httpHealthCheck)?;
    compute_http_health_checks_get_execute(builder)
}

/// POST projects/{project}/global/httpHealthChecks
/// Creates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_insert_execute()` to send, or `compute_http_health_checks_insert` for simplest API.

pub fn compute_http_health_checks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/httpHealthChecks
/// Creates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_http_health_checks_insert_execute()` or `compute_http_health_checks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/httpHealthChecks
/// Creates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_http_health_checks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_http_health_checks_insert_task()`.
/// For the simplest API, use `compute_http_health_checks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_http_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_http_health_checks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_http_health_checks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/httpHealthChecks
/// Creates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_insert_builder()` + `compute_http_health_checks_insert_execute()`.
/// For task-level control, use `compute_http_health_checks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_insert(
    client: &SimpleHttpClient,
    args: &ComputeHttpHealthChecksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_http_health_checks_insert_builder(client, &args.project, &args.requestId)?;
    compute_http_health_checks_insert_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks
/// Retrieves the list of HttpHealthCheck resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_list_execute()` to send, or `compute_http_health_checks_list` for simplest API.

pub fn compute_http_health_checks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/httpHealthChecks
/// Retrieves the list of HttpHealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_http_health_checks_list_execute()` or `compute_http_health_checks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpHealthCheckList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpHealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/httpHealthChecks
/// Retrieves the list of HttpHealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_http_health_checks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_http_health_checks_list_task()`.
/// For the simplest API, use `compute_http_health_checks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_http_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_http_health_checks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_http_health_checks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/httpHealthChecks
/// Retrieves the list of HttpHealthCheck resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_list_builder()` + `compute_http_health_checks_list_execute()`.
/// For task-level control, use `compute_http_health_checks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_list(
    client: &SimpleHttpClient,
    args: &ComputeHttpHealthChecksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_http_health_checks_list_execute(builder)
}

/// PATCH projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_patch_execute()` to send, or `compute_http_health_checks_patch` for simplest API.

pub fn compute_http_health_checks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    httpHealthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}",
        project, httpHealthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_http_health_checks_patch_execute()` or `compute_http_health_checks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_http_health_checks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_http_health_checks_patch_task()`.
/// For the simplest API, use `compute_http_health_checks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_http_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_http_health_checks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_http_health_checks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: httpHealthCheck
    pub httpHealthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_patch_builder()` + `compute_http_health_checks_patch_execute()`.
/// For task-level control, use `compute_http_health_checks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_patch(
    client: &SimpleHttpClient,
    args: &ComputeHttpHealthChecksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_patch_builder(
        client,
        &args.project,
        &args.httpHealthCheck,
        &args.requestId,
    )?;
    compute_http_health_checks_patch_execute(builder)
}

/// POST projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_test_iam_permissions_execute()` to send, or `compute_http_health_checks_test_iam_permissions` for simplest API.

pub fn compute_http_health_checks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_http_health_checks_test_iam_permissions_execute()` or `compute_http_health_checks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_http_health_checks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_http_health_checks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_http_health_checks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_http_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_http_health_checks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_http_health_checks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_test_iam_permissions_builder()` + `compute_http_health_checks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_http_health_checks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeHttpHealthChecksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_http_health_checks_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_update_execute()` to send, or `compute_http_health_checks_update` for simplest API.

pub fn compute_http_health_checks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    httpHealthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}",
        project, httpHealthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_http_health_checks_update_execute()` or `compute_http_health_checks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_http_health_checks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_http_health_checks_update_task()`.
/// For the simplest API, use `compute_http_health_checks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_http_health_checks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_http_health_checks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_http_health_checks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_http_health_checks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: httpHealthCheck
    pub httpHealthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_update_builder()` + `compute_http_health_checks_update_execute()`.
/// For task-level control, use `compute_http_health_checks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_update(
    client: &SimpleHttpClient,
    args: &ComputeHttpHealthChecksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_update_builder(
        client,
        &args.project,
        &args.httpHealthCheck,
        &args.requestId,
    )?;
    compute_http_health_checks_update_execute(builder)
}

/// DELETE projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Deletes the specified HttpsHealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_delete_execute()` to send, or `compute_https_health_checks_delete` for simplest API.

pub fn compute_https_health_checks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    httpsHealthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}",
        project, httpsHealthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Deletes the specified HttpsHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_https_health_checks_delete_execute()` or `compute_https_health_checks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Deletes the specified HttpsHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_https_health_checks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_https_health_checks_delete_task()`.
/// For the simplest API, use `compute_https_health_checks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_https_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_https_health_checks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_https_health_checks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: httpsHealthCheck
    pub httpsHealthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Deletes the specified HttpsHealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_delete_builder()` + `compute_https_health_checks_delete_execute()`.
/// For task-level control, use `compute_https_health_checks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_delete(
    client: &SimpleHttpClient,
    args: &ComputeHttpsHealthChecksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_delete_builder(
        client,
        &args.project,
        &args.httpsHealthCheck,
        &args.requestId,
    )?;
    compute_https_health_checks_delete_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Returns the specified HttpsHealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_get_execute()` to send, or `compute_https_health_checks_get` for simplest API.

pub fn compute_https_health_checks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    httpsHealthCheck: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}",
        project, httpsHealthCheck,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Returns the specified HttpsHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_https_health_checks_get_execute()` or `compute_https_health_checks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpsHealthCheck>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpsHealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Returns the specified HttpsHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_https_health_checks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_https_health_checks_get_task()`.
/// For the simplest API, use `compute_https_health_checks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_https_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpsHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_https_health_checks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_https_health_checks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: httpsHealthCheck
    pub httpsHealthCheck: String,
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Returns the specified HttpsHealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_get_builder()` + `compute_https_health_checks_get_execute()`.
/// For task-level control, use `compute_https_health_checks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_get(
    client: &SimpleHttpClient,
    args: &ComputeHttpsHealthChecksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpsHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_https_health_checks_get_builder(client, &args.project, &args.httpsHealthCheck)?;
    compute_https_health_checks_get_execute(builder)
}

/// POST projects/{project}/global/httpsHealthChecks
/// Creates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_insert_execute()` to send, or `compute_https_health_checks_insert` for simplest API.

pub fn compute_https_health_checks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/httpsHealthChecks
/// Creates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_https_health_checks_insert_execute()` or `compute_https_health_checks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/httpsHealthChecks
/// Creates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_https_health_checks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_https_health_checks_insert_task()`.
/// For the simplest API, use `compute_https_health_checks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_https_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_https_health_checks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_https_health_checks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/httpsHealthChecks
/// Creates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_insert_builder()` + `compute_https_health_checks_insert_execute()`.
/// For task-level control, use `compute_https_health_checks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_insert(
    client: &SimpleHttpClient,
    args: &ComputeHttpsHealthChecksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_https_health_checks_insert_builder(client, &args.project, &args.requestId)?;
    compute_https_health_checks_insert_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks
/// Retrieves the list of HttpsHealthCheck resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_list_execute()` to send, or `compute_https_health_checks_list` for simplest API.

pub fn compute_https_health_checks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/httpsHealthChecks
/// Retrieves the list of HttpsHealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_https_health_checks_list_execute()` or `compute_https_health_checks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpsHealthCheckList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpsHealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/httpsHealthChecks
/// Retrieves the list of HttpsHealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_https_health_checks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_https_health_checks_list_task()`.
/// For the simplest API, use `compute_https_health_checks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_https_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpsHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_https_health_checks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_https_health_checks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/httpsHealthChecks
/// Retrieves the list of HttpsHealthCheck resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_list_builder()` + `compute_https_health_checks_list_execute()`.
/// For task-level control, use `compute_https_health_checks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_list(
    client: &SimpleHttpClient,
    args: &ComputeHttpsHealthChecksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpsHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_https_health_checks_list_execute(builder)
}

/// PATCH projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_patch_execute()` to send, or `compute_https_health_checks_patch` for simplest API.

pub fn compute_https_health_checks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    httpsHealthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}",
        project, httpsHealthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_https_health_checks_patch_execute()` or `compute_https_health_checks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_https_health_checks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_https_health_checks_patch_task()`.
/// For the simplest API, use `compute_https_health_checks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_https_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_https_health_checks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_https_health_checks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: httpsHealthCheck
    pub httpsHealthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_patch_builder()` + `compute_https_health_checks_patch_execute()`.
/// For task-level control, use `compute_https_health_checks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_patch(
    client: &SimpleHttpClient,
    args: &ComputeHttpsHealthChecksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_patch_builder(
        client,
        &args.project,
        &args.httpsHealthCheck,
        &args.requestId,
    )?;
    compute_https_health_checks_patch_execute(builder)
}

/// POST projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_test_iam_permissions_execute()` to send, or `compute_https_health_checks_test_iam_permissions` for simplest API.

pub fn compute_https_health_checks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_https_health_checks_test_iam_permissions_execute()` or `compute_https_health_checks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_https_health_checks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_https_health_checks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_https_health_checks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_https_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_https_health_checks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_https_health_checks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_test_iam_permissions_builder()` + `compute_https_health_checks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_https_health_checks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeHttpsHealthChecksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_https_health_checks_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_update_execute()` to send, or `compute_https_health_checks_update` for simplest API.

pub fn compute_https_health_checks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    httpsHealthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}",
        project, httpsHealthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_https_health_checks_update_execute()` or `compute_https_health_checks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_https_health_checks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_https_health_checks_update_task()`.
/// For the simplest API, use `compute_https_health_checks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_https_health_checks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_https_health_checks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_https_health_checks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_https_health_checks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: httpsHealthCheck
    pub httpsHealthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_update_builder()` + `compute_https_health_checks_update_execute()`.
/// For task-level control, use `compute_https_health_checks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_update(
    client: &SimpleHttpClient,
    args: &ComputeHttpsHealthChecksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_update_builder(
        client,
        &args.project,
        &args.httpsHealthCheck,
        &args.requestId,
    )?;
    compute_https_health_checks_update_execute(builder)
}

/// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}
/// Returns the latest image that is part of an image family, is not deprecated and is rolled out in the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_image_family_views_get_execute()` to send, or `compute_image_family_views_get` for simplest API.

pub fn compute_image_family_views_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    family: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/imageFamilyViews/{}",
        project, zone, family,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}
/// Returns the latest image that is part of an image family, is not deprecated and is rolled out in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_image_family_views_get_execute()` or `compute_image_family_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_image_family_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_image_family_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ImageFamilyView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImageFamilyView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}
/// Returns the latest image that is part of an image family, is not deprecated and is rolled out in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_image_family_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_image_family_views_get_task()`.
/// For the simplest API, use `compute_image_family_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_image_family_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_image_family_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageFamilyView>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_image_family_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_image_family_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImageFamilyViewsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: family
    pub family: String,
}

/// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}
/// Returns the latest image that is part of an image family, is not deprecated and is rolled out in the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_image_family_views_get_builder()` + `compute_image_family_views_get_execute()`.
/// For task-level control, use `compute_image_family_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_image_family_views_get(
    client: &SimpleHttpClient,
    args: &ComputeImageFamilyViewsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageFamilyView>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_image_family_views_get_builder(client, &args.project, &args.zone, &args.family)?;
    compute_image_family_views_get_execute(builder)
}

/// DELETE projects/{project}/global/images/{image}
/// Deletes the specified image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_delete_execute()` to send, or `compute_images_delete` for simplest API.

pub fn compute_images_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    image: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}",
        project, image,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/images/{image}
/// Deletes the specified image.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_delete_execute()` or `compute_images_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/images/{image}
/// Deletes the specified image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_delete_task()`.
/// For the simplest API, use `compute_images_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: image
    pub image: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/images/{image}
/// Deletes the specified image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_delete_builder()` + `compute_images_delete_execute()`.
/// For task-level control, use `compute_images_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_delete(
    client: &SimpleHttpClient,
    args: &ComputeImagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_images_delete_builder(client, &args.project, &args.image, &args.requestId)?;
    compute_images_delete_execute(builder)
}

/// POST projects/{project}/global/images/{image}/deprecate
/// Sets the deprecation status of an image.  If an empty request body is given, clears the deprecation status instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_deprecate_execute()` to send, or `compute_images_deprecate` for simplest API.

pub fn compute_images_deprecate_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    image: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/deprecate",
        project, image,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/images/{image}/deprecate
/// Sets the deprecation status of an image.  If an empty request body is given, clears the deprecation status instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_deprecate_execute()` or `compute_images_deprecate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_deprecate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_deprecate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/images/{image}/deprecate
/// Sets the deprecation status of an image.  If an empty request body is given, clears the deprecation status instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_deprecate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_deprecate_task()`.
/// For the simplest API, use `compute_images_deprecate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_deprecate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_deprecate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_deprecate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_deprecate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesDeprecateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: image
    pub image: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/images/{image}/deprecate
/// Sets the deprecation status of an image.  If an empty request body is given, clears the deprecation status instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_deprecate_builder()` + `compute_images_deprecate_execute()`.
/// For task-level control, use `compute_images_deprecate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_deprecate(
    client: &SimpleHttpClient,
    args: &ComputeImagesDeprecateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_images_deprecate_builder(client, &args.project, &args.image, &args.requestId)?;
    compute_images_deprecate_execute(builder)
}

/// GET projects/{project}/global/images/{image}
/// Returns the specified image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_get_execute()` to send, or `compute_images_get` for simplest API.

pub fn compute_images_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    image: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}",
        project, image,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/images/{image}
/// Returns the specified image.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_get_execute()` or `compute_images_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Image>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Image = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/images/{image}
/// Returns the specified image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_get_task()`.
/// For the simplest API, use `compute_images_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Image>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: image
    pub image: String,
}

/// GET projects/{project}/global/images/{image}
/// Returns the specified image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_get_builder()` + `compute_images_get_execute()`.
/// For task-level control, use `compute_images_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get(
    client: &SimpleHttpClient,
    args: &ComputeImagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Image>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_get_builder(client, &args.project, &args.image)?;
    compute_images_get_execute(builder)
}

/// GET projects/{project}/global/images/family/{family}
/// Returns the latest image that is part of an image family and is not deprecated. For more information on image families, `seePublic` image families documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_get_from_family_execute()` to send, or `compute_images_get_from_family` for simplest API.

pub fn compute_images_get_from_family_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    family: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/family/{}",
        project, family,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/images/family/{family}
/// Returns the latest image that is part of an image family and is not deprecated. For more information on image families, `seePublic` image families documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_get_from_family_execute()` or `compute_images_get_from_family`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_get_from_family_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_from_family_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Image>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Image = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/images/family/{family}
/// Returns the latest image that is part of an image family and is not deprecated. For more information on image families, `seePublic` image families documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_get_from_family_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_get_from_family_task()`.
/// For the simplest API, use `compute_images_get_from_family()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_get_from_family_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_get_from_family_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Image>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_get_from_family_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_get_from_family`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesGetFromFamilyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: family
    pub family: String,
}

/// GET projects/{project}/global/images/family/{family}
/// Returns the latest image that is part of an image family and is not deprecated. For more information on image families, `seePublic` image families documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_get_from_family_builder()` + `compute_images_get_from_family_execute()`.
/// For task-level control, use `compute_images_get_from_family_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_from_family(
    client: &SimpleHttpClient,
    args: &ComputeImagesGetFromFamilyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Image>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_get_from_family_builder(client, &args.project, &args.family)?;
    compute_images_get_from_family_execute(builder)
}

/// GET projects/{project}/global/images/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_get_iam_policy_execute()` to send, or `compute_images_get_iam_policy` for simplest API.

pub fn compute_images_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/images/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_get_iam_policy_execute()` or `compute_images_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/images/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_get_iam_policy_task()`.
/// For the simplest API, use `compute_images_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/images/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_get_iam_policy_builder()` + `compute_images_get_iam_policy_execute()`.
/// For task-level control, use `compute_images_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeImagesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_images_get_iam_policy_execute(builder)
}

/// POST projects/{project}/global/images
/// Creates an image in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_insert_execute()` to send, or `compute_images_insert` for simplest API.

pub fn compute_images_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    forceCreate: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = forceCreate.as_ref() {
        query_parts.push(format!("forceCreate={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/images
/// Creates an image in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_insert_execute()` or `compute_images_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/images
/// Creates an image in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_insert_task()`.
/// For the simplest API, use `compute_images_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: forceCreate
    pub forceCreate: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/images
/// Creates an image in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_insert_builder()` + `compute_images_insert_execute()`.
/// For task-level control, use `compute_images_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_insert(
    client: &SimpleHttpClient,
    args: &ComputeImagesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_images_insert_builder(client, &args.project, &args.forceCreate, &args.requestId)?;
    compute_images_insert_execute(builder)
}

/// GET projects/{project}/global/images
/// Retrieves the list of custom images available to the specified project. Custom images are images you create that belong to your project. This method does not get any images that belong to other projects, including publicly-available images, like Debian 8. If you want to get a list of publicly-available images, use this method to make a request to the respective image project, such as debian-cloud or windows-cloud.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_list_execute()` to send, or `compute_images_list` for simplest API.

pub fn compute_images_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/images
/// Retrieves the list of custom images available to the specified project. Custom images are images you create that belong to your project. This method does not get any images that belong to other projects, including publicly-available images, like Debian 8. If you want to get a list of publicly-available images, use this method to make a request to the respective image project, such as debian-cloud or windows-cloud.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_list_execute()` or `compute_images_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ImageList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImageList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/images
/// Retrieves the list of custom images available to the specified project. Custom images are images you create that belong to your project. This method does not get any images that belong to other projects, including publicly-available images, like Debian 8. If you want to get a list of publicly-available images, use this method to make a request to the respective image project, such as debian-cloud or windows-cloud.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_list_task()`.
/// For the simplest API, use `compute_images_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/images
/// Retrieves the list of custom images available to the specified project. Custom images are images you create that belong to your project. This method does not get any images that belong to other projects, including publicly-available images, like Debian 8. If you want to get a list of publicly-available images, use this method to make a request to the respective image project, such as debian-cloud or windows-cloud.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_list_builder()` + `compute_images_list_execute()`.
/// For task-level control, use `compute_images_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_list(
    client: &SimpleHttpClient,
    args: &ComputeImagesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_images_list_execute(builder)
}

/// PATCH projects/{project}/global/images/{image}
/// Patches the specified image with the data included in the request. Only the following fields can be modified: family, description, deprecation status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_patch_execute()` to send, or `compute_images_patch` for simplest API.

pub fn compute_images_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    image: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}",
        project, image,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/images/{image}
/// Patches the specified image with the data included in the request. Only the following fields can be modified: family, description, deprecation status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_patch_execute()` or `compute_images_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/images/{image}
/// Patches the specified image with the data included in the request. Only the following fields can be modified: family, description, deprecation status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_patch_task()`.
/// For the simplest API, use `compute_images_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: image
    pub image: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/images/{image}
/// Patches the specified image with the data included in the request. Only the following fields can be modified: family, description, deprecation status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_patch_builder()` + `compute_images_patch_execute()`.
/// For task-level control, use `compute_images_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_patch(
    client: &SimpleHttpClient,
    args: &ComputeImagesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_images_patch_builder(client, &args.project, &args.image, &args.requestId)?;
    compute_images_patch_execute(builder)
}

/// POST projects/{project}/global/images/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_set_iam_policy_execute()` to send, or `compute_images_set_iam_policy` for simplest API.

pub fn compute_images_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/images/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_set_iam_policy_execute()` or `compute_images_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/images/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_set_iam_policy_task()`.
/// For the simplest API, use `compute_images_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/images/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_set_iam_policy_builder()` + `compute_images_set_iam_policy_execute()`.
/// For task-level control, use `compute_images_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeImagesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_set_iam_policy_builder(client, &args.project, &args.resource)?;
    compute_images_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/images/{resource}/setLabels
/// Sets the labels on an image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_set_labels_execute()` to send, or `compute_images_set_labels` for simplest API.

pub fn compute_images_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/setLabels",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/images/{resource}/setLabels
/// Sets the labels on an image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_set_labels_execute()` or `compute_images_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/images/{resource}/setLabels
/// Sets the labels on an image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_set_labels_task()`.
/// For the simplest API, use `compute_images_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_images_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/images/{resource}/setLabels
/// Sets the labels on an image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_set_labels_builder()` + `compute_images_set_labels_execute()`.
/// For task-level control, use `compute_images_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeImagesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_set_labels_builder(client, &args.project, &args.resource)?;
    compute_images_set_labels_execute(builder)
}

/// POST projects/{project}/global/images/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_test_iam_permissions_execute()` to send, or `compute_images_test_iam_permissions` for simplest API.

pub fn compute_images_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/images/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_images_test_iam_permissions_execute()` or `compute_images_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/images/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_images_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_images_test_iam_permissions_task()`.
/// For the simplest API, use `compute_images_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_images_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_images_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_images_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_images_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeImagesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/images/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_test_iam_permissions_builder()` + `compute_images_test_iam_permissions_execute()`.
/// For task-level control, use `compute_images_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeImagesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_images_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_images_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request and removes it from the queue. Cancelled resize request does no longer wait for the resources to be provisioned. Cancel is only possible for requests that are accepted in the queue.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_cancel_execute()` to send, or `compute_instance_group_manager_resize_requests_cancel` for simplest API.

pub fn compute_instance_group_manager_resize_requests_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    resizeRequest: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}/cancel",
        project,
        zone,
        instanceGroupManager,
        resizeRequest,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request and removes it from the queue. Cancelled resize request does no longer wait for the resources to be provisioned. Cancel is only possible for requests that are accepted in the queue.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_manager_resize_requests_cancel_execute()` or `compute_instance_group_manager_resize_requests_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request and removes it from the queue. Cancelled resize request does no longer wait for the resources to be provisioned. Cancel is only possible for requests that are accepted in the queue.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_manager_resize_requests_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_manager_resize_requests_cancel_task()`.
/// For the simplest API, use `compute_instance_group_manager_resize_requests_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_manager_resize_requests_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_manager_resize_requests_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_manager_resize_requests_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsCancelArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Path parameter: resizeRequest
    pub resizeRequest: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request and removes it from the queue. Cancelled resize request does no longer wait for the resources to be provisioned. Cancel is only possible for requests that are accepted in the queue.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_cancel_builder()` + `compute_instance_group_manager_resize_requests_cancel_execute()`.
/// For task-level control, use `compute_instance_group_manager_resize_requests_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_cancel(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagerResizeRequestsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_cancel_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.resizeRequest,
        &args.requestId,
    )?;
    compute_instance_group_manager_resize_requests_cancel_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_delete_execute()` to send, or `compute_instance_group_manager_resize_requests_delete` for simplest API.

pub fn compute_instance_group_manager_resize_requests_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    resizeRequest: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}",
        project,
        zone,
        instanceGroupManager,
        resizeRequest,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_manager_resize_requests_delete_execute()` or `compute_instance_group_manager_resize_requests_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_manager_resize_requests_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_manager_resize_requests_delete_task()`.
/// For the simplest API, use `compute_instance_group_manager_resize_requests_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_manager_resize_requests_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_manager_resize_requests_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_manager_resize_requests_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Path parameter: resizeRequest
    pub resizeRequest: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_delete_builder()` + `compute_instance_group_manager_resize_requests_delete_execute()`.
/// For task-level control, use `compute_instance_group_manager_resize_requests_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_delete(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagerResizeRequestsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.resizeRequest,
        &args.requestId,
    )?;
    compute_instance_group_manager_resize_requests_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_get_execute()` to send, or `compute_instance_group_manager_resize_requests_get` for simplest API.

pub fn compute_instance_group_manager_resize_requests_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    resizeRequest: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}",
        project,
        zone,
        instanceGroupManager,
        resizeRequest,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_manager_resize_requests_get_execute()` or `compute_instance_group_manager_resize_requests_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagerResizeRequest>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerResizeRequest = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_manager_resize_requests_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_manager_resize_requests_get_task()`.
/// For the simplest API, use `compute_instance_group_manager_resize_requests_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_manager_resize_requests_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequest>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_group_manager_resize_requests_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_manager_resize_requests_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Path parameter: resizeRequest
    pub resizeRequest: String,
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_get_builder()` + `compute_instance_group_manager_resize_requests_get_execute()`.
/// For task-level control, use `compute_instance_group_manager_resize_requests_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_get(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagerResizeRequestsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequest>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.resizeRequest,
    )?;
    compute_instance_group_manager_resize_requests_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new resize request that starts provisioning VMs immediately or queues VM creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_insert_execute()` to send, or `compute_instance_group_manager_resize_requests_insert` for simplest API.

pub fn compute_instance_group_manager_resize_requests_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new resize request that starts provisioning VMs immediately or queues VM creation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_manager_resize_requests_insert_execute()` or `compute_instance_group_manager_resize_requests_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new resize request that starts provisioning VMs immediately or queues VM creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_manager_resize_requests_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_manager_resize_requests_insert_task()`.
/// For the simplest API, use `compute_instance_group_manager_resize_requests_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_manager_resize_requests_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_manager_resize_requests_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_manager_resize_requests_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new resize request that starts provisioning VMs immediately or queues VM creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_insert_builder()` + `compute_instance_group_manager_resize_requests_insert_execute()`.
/// For task-level control, use `compute_instance_group_manager_resize_requests_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_insert(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagerResizeRequestsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_manager_resize_requests_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of resize requests that are contained in the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_list_execute()` to send, or `compute_instance_group_manager_resize_requests_list` for simplest API.

pub fn compute_instance_group_manager_resize_requests_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of resize requests that are contained in the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_manager_resize_requests_list_execute()` or `compute_instance_group_manager_resize_requests_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagerResizeRequestsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerResizeRequestsListResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of resize requests that are contained in the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_manager_resize_requests_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_manager_resize_requests_list_task()`.
/// For the simplest API, use `compute_instance_group_manager_resize_requests_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_manager_resize_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_manager_resize_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequestsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_group_manager_resize_requests_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_manager_resize_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of resize requests that are contained in the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_list_builder()` + `compute_instance_group_manager_resize_requests_list_execute()`.
/// For task-level control, use `compute_instance_group_manager_resize_requests_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_list(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagerResizeRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequestsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instance_group_manager_resize_requests_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_abandon_instances_execute()` to send, or `compute_instance_group_managers_abandon_instances` for simplest API.

pub fn compute_instance_group_managers_abandon_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/abandonInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_abandon_instances_execute()` or `compute_instance_group_managers_abandon_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_abandon_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_abandon_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_abandon_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_abandon_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_abandon_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_abandon_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_abandon_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_abandon_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_abandon_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersAbandonInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_abandon_instances_builder()` + `compute_instance_group_managers_abandon_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_abandon_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_abandon_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersAbandonInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_abandon_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_abandon_instances_execute(builder)
}

/// GET projects/{project}/aggregated/instanceGroupManagers
/// Retrieves the list of managed instance groups and groups them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_aggregated_list_execute()` to send, or `compute_instance_group_managers_aggregated_list` for simplest API.

pub fn compute_instance_group_managers_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instanceGroupManagers",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/instanceGroupManagers
/// Retrieves the list of managed instance groups and groups them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_aggregated_list_execute()` or `compute_instance_group_managers_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagerAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/instanceGroupManagers
/// Retrieves the list of managed instance groups and groups them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_aggregated_list_task()`.
/// For the simplest API, use `compute_instance_group_managers_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/instanceGroupManagers
/// Retrieves the list of managed instance groups and groups them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_aggregated_list_builder()` + `compute_instance_group_managers_aggregated_list_execute()`.
/// For task-level control, use `compute_instance_group_managers_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_instance_group_managers_aggregated_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Applies changes to selected instances on the managed instance group. This method can be used to apply new overrides `and/or` new versions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_apply_updates_to_instances_execute()` to send, or `compute_instance_group_managers_apply_updates_to_instances` for simplest API.

pub fn compute_instance_group_managers_apply_updates_to_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/applyUpdatesToInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Applies changes to selected instances on the managed instance group. This method can be used to apply new overrides `and/or` new versions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_apply_updates_to_instances_execute()` or `compute_instance_group_managers_apply_updates_to_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_apply_updates_to_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_apply_updates_to_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Applies changes to selected instances on the managed instance group. This method can be used to apply new overrides `and/or` new versions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_apply_updates_to_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_apply_updates_to_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_apply_updates_to_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_apply_updates_to_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_apply_updates_to_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_apply_updates_to_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_apply_updates_to_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Applies changes to selected instances on the managed instance group. This method can be used to apply new overrides `and/or` new versions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_apply_updates_to_instances_builder()` + `compute_instance_group_managers_apply_updates_to_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_apply_updates_to_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_apply_updates_to_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_apply_updates_to_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
    )?;
    compute_instance_group_managers_apply_updates_to_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this managed instance group. Instances are created using the current instance template. Thecreate instances operation is marked DONE if `thecreateInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_create_instances_execute()` to send, or `compute_instance_group_managers_create_instances` for simplest API.

pub fn compute_instance_group_managers_create_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/createInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this managed instance group. Instances are created using the current instance template. Thecreate instances operation is marked DONE if `thecreateInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_create_instances_execute()` or `compute_instance_group_managers_create_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_create_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_create_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this managed instance group. Instances are created using the current instance template. Thecreate instances operation is marked DONE if `thecreateInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_create_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_create_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_create_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_create_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_create_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_create_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_create_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersCreateInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this managed instance group. Instances are created using the current instance template. Thecreate instances operation is marked DONE if `thecreateInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_create_instances_builder()` + `compute_instance_group_managers_create_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_create_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_create_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersCreateInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_create_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_create_instances_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group. Note that the instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_delete_execute()` to send, or `compute_instance_group_managers_delete` for simplest API.

pub fn compute_instance_group_managers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}",
        project, zone, instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group. Note that the instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_delete_execute()` or `compute_instance_group_managers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group. Note that the instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_delete_task()`.
/// For the simplest API, use `compute_instance_group_managers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group. Note that the instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_delete_builder()` + `compute_instance_group_managers_delete_execute()`.
/// For task-level control, use `compute_instance_group_managers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_delete_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group for immediate deletion. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. This operation is marked as DONE when the action is scheduled even if the instances are still being deleted. You must separately verify the status of the deleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_delete_instances_execute()` to send, or `compute_instance_group_managers_delete_instances` for simplest API.

pub fn compute_instance_group_managers_delete_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/deleteInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group for immediate deletion. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. This operation is marked as DONE when the action is scheduled even if the instances are still being deleted. You must separately verify the status of the deleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_delete_instances_execute()` or `compute_instance_group_managers_delete_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_delete_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group for immediate deletion. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. This operation is marked as DONE when the action is scheduled even if the instances are still being deleted. You must separately verify the status of the deleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_delete_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_delete_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_delete_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_delete_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_delete_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_delete_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_delete_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersDeleteInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group for immediate deletion. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. This operation is marked as DONE when the action is scheduled even if the instances are still being deleted. You must separately verify the status of the deleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_delete_instances_builder()` + `compute_instance_group_managers_delete_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_delete_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersDeleteInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_delete_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_delete_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_delete_per_instance_configs_execute()` to send, or `compute_instance_group_managers_delete_per_instance_configs` for simplest API.

pub fn compute_instance_group_managers_delete_per_instance_configs_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/deletePerInstanceConfigs",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_delete_per_instance_configs_execute()` or `compute_instance_group_managers_delete_per_instance_configs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_delete_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_per_instance_configs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_delete_per_instance_configs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_delete_per_instance_configs_task()`.
/// For the simplest API, use `compute_instance_group_managers_delete_per_instance_configs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_delete_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_delete_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_delete_per_instance_configs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_delete_per_instance_configs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_delete_per_instance_configs_builder()` + `compute_instance_group_managers_delete_per_instance_configs_execute()`.
/// For task-level control, use `compute_instance_group_managers_delete_per_instance_configs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_per_instance_configs(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_delete_per_instance_configs_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
    )?;
    compute_instance_group_managers_delete_per_instance_configs_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_get_execute()` to send, or `compute_instance_group_managers_get` for simplest API.

pub fn compute_instance_group_managers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}",
        project, zone, instanceGroupManager,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_get_execute()` or `compute_instance_group_managers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManager>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManager = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_get_task()`.
/// For the simplest API, use `compute_instance_group_managers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManager>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_get_builder()` + `compute_instance_group_managers_get_execute()`.
/// For task-level control, use `compute_instance_group_managers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_get(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManager>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
    )?;
    compute_instance_group_managers_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A managed instance group can have up to 1000 VM instances per group. Please contact Cloud Support if you need an increase in this limit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_insert_execute()` to send, or `compute_instance_group_managers_insert` for simplest API.

pub fn compute_instance_group_managers_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A managed instance group can have up to 1000 VM instances per group. Please contact Cloud Support if you need an increase in this limit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_insert_execute()` or `compute_instance_group_managers_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A managed instance group can have up to 1000 VM instances per group. Please contact Cloud Support if you need an increase in this limit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_insert_task()`.
/// For the simplest API, use `compute_instance_group_managers_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A managed instance group can have up to 1000 VM instances per group. Please contact Cloud Support if you need an increase in this limit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_insert_builder()` + `compute_instance_group_managers_insert_execute()`.
/// For task-level control, use `compute_instance_group_managers_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_insert(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
    )?;
    compute_instance_group_managers_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Retrieves a list of managed instance groups that are contained within the specified project and zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_list_execute()` to send, or `compute_instance_group_managers_list` for simplest API.

pub fn compute_instance_group_managers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Retrieves a list of managed instance groups that are contained within the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_list_execute()` or `compute_instance_group_managers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagerList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Retrieves a list of managed instance groups that are contained within the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_list_task()`.
/// For the simplest API, use `compute_instance_group_managers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManagerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Retrieves a list of managed instance groups that are contained within the specified project and zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_list_builder()` + `compute_instance_group_managers_list_execute()`.
/// For task-level control, use `compute_instance_group_managers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManagerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instance_group_managers_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given managed instance group. The filter and `orderBy` query parameters are not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_list_errors_execute()` to send, or `compute_instance_group_managers_list_errors` for simplest API.

pub fn compute_instance_group_managers_list_errors_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/listErrors",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given managed instance group. The filter and `orderBy` query parameters are not supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_list_errors_execute()` or `compute_instance_group_managers_list_errors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_list_errors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_errors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagersListErrorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListErrorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given managed instance group. The filter and `orderBy` query parameters are not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_list_errors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_list_errors_task()`.
/// For the simplest API, use `compute_instance_group_managers_list_errors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_list_errors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_list_errors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_list_errors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_list_errors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersListErrorsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given managed instance group. The filter and `orderBy` query parameters are not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_list_errors_builder()` + `compute_instance_group_managers_list_errors_execute()`.
/// For task-level control, use `compute_instance_group_managers_list_errors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_errors(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersListErrorsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_list_errors_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instance_group_managers_list_errors_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists all of the instances in the managed instance group. Each instance in the list has a `currentAction`, which indicates the action that the managed instance group is performing on the instance. For example, if the group is still creating an instance, the `currentAction` is CREATING. If a previous action failed, the list displays the errors for that failed action. The `orderBy` query parameter is not supported. The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_list_managed_instances_execute()` to send, or `compute_instance_group_managers_list_managed_instances` for simplest API.

pub fn compute_instance_group_managers_list_managed_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/listManagedInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists all of the instances in the managed instance group. Each instance in the list has a `currentAction`, which indicates the action that the managed instance group is performing on the instance. For example, if the group is still creating an instance, the `currentAction` is CREATING. If a previous action failed, the list displays the errors for that failed action. The `orderBy` query parameter is not supported. The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_list_managed_instances_execute()` or `compute_instance_group_managers_list_managed_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_list_managed_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_managed_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstanceGroupManagersListManagedInstancesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListManagedInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists all of the instances in the managed instance group. Each instance in the list has a `currentAction`, which indicates the action that the managed instance group is performing on the instance. For example, if the group is still creating an instance, the `currentAction` is CREATING. If a previous action failed, the list displays the errors for that failed action. The `orderBy` query parameter is not supported. The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_list_managed_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_list_managed_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_list_managed_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_list_managed_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_list_managed_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListManagedInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_list_managed_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_list_managed_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersListManagedInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists all of the instances in the managed instance group. Each instance in the list has a `currentAction`, which indicates the action that the managed instance group is performing on the instance. For example, if the group is still creating an instance, the `currentAction` is CREATING. If a previous action failed, the list displays the errors for that failed action. The `orderBy` query parameter is not supported. The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_list_managed_instances_builder()` + `compute_instance_group_managers_list_managed_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_list_managed_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_managed_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersListManagedInstancesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListManagedInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_list_managed_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instance_group_managers_list_managed_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_list_per_instance_configs_execute()` to send, or `compute_instance_group_managers_list_per_instance_configs` for simplest API.

pub fn compute_instance_group_managers_list_per_instance_configs_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/listPerInstanceConfigs",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_list_per_instance_configs_execute()` or `compute_instance_group_managers_list_per_instance_configs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_list_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_per_instance_configs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagersListPerInstanceConfigsResp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListPerInstanceConfigsResp =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_list_per_instance_configs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_list_per_instance_configs_task()`.
/// For the simplest API, use `compute_instance_group_managers_list_per_instance_configs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_list_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_list_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListPerInstanceConfigsResp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_list_per_instance_configs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_list_per_instance_configs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersListPerInstanceConfigsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_list_per_instance_configs_builder()` + `compute_instance_group_managers_list_per_instance_configs_execute()`.
/// For task-level control, use `compute_instance_group_managers_list_per_instance_configs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_per_instance_configs(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersListPerInstanceConfigsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListPerInstanceConfigsResp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_list_per_instance_configs_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instance_group_managers_list_per_instance_configs_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with `thelistManagedInstances` method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_patch_execute()` to send, or `compute_instance_group_managers_patch` for simplest API.

pub fn compute_instance_group_managers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}",
        project, zone, instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with `thelistManagedInstances` method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_patch_execute()` or `compute_instance_group_managers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with `thelistManagedInstances` method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_patch_task()`.
/// For the simplest API, use `compute_instance_group_managers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with `thelistManagedInstances` method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_patch_builder()` + `compute_instance_group_managers_patch_execute()`.
/// For task-level control, use `compute_instance_group_managers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_patch(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_patch_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_patch_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_patch_per_instance_configs_execute()` to send, or `compute_instance_group_managers_patch_per_instance_configs` for simplest API.

pub fn compute_instance_group_managers_patch_per_instance_configs_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/patchPerInstanceConfigs",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_patch_per_instance_configs_execute()` or `compute_instance_group_managers_patch_per_instance_configs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_patch_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_patch_per_instance_configs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_patch_per_instance_configs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_patch_per_instance_configs_task()`.
/// For the simplest API, use `compute_instance_group_managers_patch_per_instance_configs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_patch_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_patch_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_patch_per_instance_configs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_patch_per_instance_configs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_patch_per_instance_configs_builder()` + `compute_instance_group_managers_patch_per_instance_configs_execute()`.
/// For task-level control, use `compute_instance_group_managers_patch_per_instance_configs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_patch_per_instance_configs(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_patch_per_instance_configs_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_patch_per_instance_configs_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_recreate_instances_execute()` to send, or `compute_instance_group_managers_recreate_instances` for simplest API.

pub fn compute_instance_group_managers_recreate_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/recreateInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_recreate_instances_execute()` or `compute_instance_group_managers_recreate_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_recreate_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_recreate_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_recreate_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_recreate_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_recreate_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_recreate_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_recreate_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_recreate_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_recreate_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersRecreateInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_recreate_instances_builder()` + `compute_instance_group_managers_recreate_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_recreate_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_recreate_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersRecreateInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_recreate_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_recreate_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize
/// Resizes the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes instances. The resize operation is `markedDONE` when the resize actions are scheduled even if the group has not yet added or deleted any instances. You must separately verify the status of the creating or deleting actions with thelistmanagedinstances method.  When resizing down, the instance group arbitrarily chooses the order in which VMs are deleted. The group takes into account some VM attributes when making the selection including:  + The status of the VM instance. + The health of the VM instance. + The instance template version the VM is based on. + For regional managed instance groups, the location of the VM instance.  This list is subject to change.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_resize_execute()` to send, or `compute_instance_group_managers_resize` for simplest API.

pub fn compute_instance_group_managers_resize_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
    size: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resize",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = size.as_ref() {
        query_parts.push(format!("size={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize
/// Resizes the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes instances. The resize operation is `markedDONE` when the resize actions are scheduled even if the group has not yet added or deleted any instances. You must separately verify the status of the creating or deleting actions with thelistmanagedinstances method.  When resizing down, the instance group arbitrarily chooses the order in which VMs are deleted. The group takes into account some VM attributes when making the selection including:  + The status of the VM instance. + The health of the VM instance. + The instance template version the VM is based on. + For regional managed instance groups, the location of the VM instance.  This list is subject to change.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_resize_execute()` or `compute_instance_group_managers_resize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_resize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize
/// Resizes the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes instances. The resize operation is `markedDONE` when the resize actions are scheduled even if the group has not yet added or deleted any instances. You must separately verify the status of the creating or deleting actions with thelistmanagedinstances method.  When resizing down, the instance group arbitrarily chooses the order in which VMs are deleted. The group takes into account some VM attributes when making the selection including:  + The status of the VM instance. + The health of the VM instance. + The instance template version the VM is based on. + For regional managed instance groups, the location of the VM instance.  This list is subject to change.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_resize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_resize_task()`.
/// For the simplest API, use `compute_instance_group_managers_resize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_resize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_resize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersResizeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: size
    pub size: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize
/// Resizes the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes instances. The resize operation is `markedDONE` when the resize actions are scheduled even if the group has not yet added or deleted any instances. You must separately verify the status of the creating or deleting actions with thelistmanagedinstances method.  When resizing down, the instance group arbitrarily chooses the order in which VMs are deleted. The group takes into account some VM attributes when making the selection including:  + The status of the VM instance. + The health of the VM instance. + The instance template version the VM is based on. + For regional managed instance groups, the location of the VM instance.  This list is subject to change.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_resize_builder()` + `compute_instance_group_managers_resize_execute()`.
/// For task-level control, use `compute_instance_group_managers_resize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_resize(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersResizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_resize_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
        &args.size,
    )?;
    compute_instance_group_managers_resize_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_resume_instances_execute()` to send, or `compute_instance_group_managers_resume_instances` for simplest API.

pub fn compute_instance_group_managers_resume_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resumeInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_resume_instances_execute()` or `compute_instance_group_managers_resume_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_resume_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_resume_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_resume_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_resume_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_resume_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_resume_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_resume_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_resume_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_resume_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersResumeInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_resume_instances_builder()` + `compute_instance_group_managers_resume_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_resume_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_resume_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersResumeInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_resume_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_resume_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Specifies the instance template to use when creating new instances in this group. The templates for existing instances in the group do not change unless you run `recreateInstances`, `runapplyUpdatesToInstances`, or set the group'`supdatePolicy`.type to PROACTIVE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_set_instance_template_execute()` to send, or `compute_instance_group_managers_set_instance_template` for simplest API.

pub fn compute_instance_group_managers_set_instance_template_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/setInstanceTemplate",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Specifies the instance template to use when creating new instances in this group. The templates for existing instances in the group do not change unless you run `recreateInstances`, `runapplyUpdatesToInstances`, or set the group'`supdatePolicy`.type to PROACTIVE.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_set_instance_template_execute()` or `compute_instance_group_managers_set_instance_template`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_set_instance_template_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_set_instance_template_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Specifies the instance template to use when creating new instances in this group. The templates for existing instances in the group do not change unless you run `recreateInstances`, `runapplyUpdatesToInstances`, or set the group'`supdatePolicy`.type to PROACTIVE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_set_instance_template_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_set_instance_template_task()`.
/// For the simplest API, use `compute_instance_group_managers_set_instance_template()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_set_instance_template_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_set_instance_template_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_set_instance_template_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_set_instance_template`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersSetInstanceTemplateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Specifies the instance template to use when creating new instances in this group. The templates for existing instances in the group do not change unless you run `recreateInstances`, `runapplyUpdatesToInstances`, or set the group'`supdatePolicy`.type to PROACTIVE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_set_instance_template_builder()` + `compute_instance_group_managers_set_instance_template_execute()`.
/// For task-level control, use `compute_instance_group_managers_set_instance_template_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_set_instance_template(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersSetInstanceTemplateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_set_instance_template_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_set_instance_template_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all instances in this managed instance group are assigned. The target pools automatically apply to all of the instances in the managed instance group. This operation is `markedDONE` when you make the request even if the instances have not yet been added to their target pools. The change might take some time to apply to all of the instances in the group depending on the size of the group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_set_target_pools_execute()` to send, or `compute_instance_group_managers_set_target_pools` for simplest API.

pub fn compute_instance_group_managers_set_target_pools_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/setTargetPools",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all instances in this managed instance group are assigned. The target pools automatically apply to all of the instances in the managed instance group. This operation is `markedDONE` when you make the request even if the instances have not yet been added to their target pools. The change might take some time to apply to all of the instances in the group depending on the size of the group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_set_target_pools_execute()` or `compute_instance_group_managers_set_target_pools`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_set_target_pools_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_set_target_pools_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all instances in this managed instance group are assigned. The target pools automatically apply to all of the instances in the managed instance group. This operation is `markedDONE` when you make the request even if the instances have not yet been added to their target pools. The change might take some time to apply to all of the instances in the group depending on the size of the group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_set_target_pools_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_set_target_pools_task()`.
/// For the simplest API, use `compute_instance_group_managers_set_target_pools()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_set_target_pools_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_set_target_pools_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_set_target_pools_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_set_target_pools`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersSetTargetPoolsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all instances in this managed instance group are assigned. The target pools automatically apply to all of the instances in the managed instance group. This operation is `markedDONE` when you make the request even if the instances have not yet been added to their target pools. The change might take some time to apply to all of the instances in the group depending on the size of the group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_set_target_pools_builder()` + `compute_instance_group_managers_set_target_pools_execute()`.
/// For task-level control, use `compute_instance_group_managers_set_target_pools_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_set_target_pools(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersSetTargetPoolsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_set_target_pools_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_set_target_pools_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_start_instances_execute()` to send, or `compute_instance_group_managers_start_instances` for simplest API.

pub fn compute_instance_group_managers_start_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/startInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_start_instances_execute()` or `compute_instance_group_managers_start_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_start_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_start_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_start_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_start_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_start_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_start_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_start_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_start_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_start_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersStartInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_start_instances_builder()` + `compute_instance_group_managers_start_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_start_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_start_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersStartInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_start_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_start_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_stop_instances_execute()` to send, or `compute_instance_group_managers_stop_instances` for simplest API.

pub fn compute_instance_group_managers_stop_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/stopInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_stop_instances_execute()` or `compute_instance_group_managers_stop_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_stop_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_stop_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_stop_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_stop_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_stop_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_stop_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_stop_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_stop_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_stop_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersStopInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_stop_instances_builder()` + `compute_instance_group_managers_stop_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_stop_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_stop_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersStopInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_stop_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_stop_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_suspend_instances_execute()` to send, or `compute_instance_group_managers_suspend_instances` for simplest API.

pub fn compute_instance_group_managers_suspend_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/suspendInstances",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_suspend_instances_execute()` or `compute_instance_group_managers_suspend_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_suspend_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_suspend_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_suspend_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_suspend_instances_task()`.
/// For the simplest API, use `compute_instance_group_managers_suspend_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_suspend_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_suspend_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_suspend_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_suspend_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersSuspendInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_suspend_instances_builder()` + `compute_instance_group_managers_suspend_instances_execute()`.
/// For task-level control, use `compute_instance_group_managers_suspend_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_suspend_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersSuspendInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_suspend_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_suspend_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_update_per_instance_configs_execute()` to send, or `compute_instance_group_managers_update_per_instance_configs` for simplest API.

pub fn compute_instance_group_managers_update_per_instance_configs_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/updatePerInstanceConfigs",
        project,
        zone,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_group_managers_update_per_instance_configs_execute()` or `compute_instance_group_managers_update_per_instance_configs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_update_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_update_per_instance_configs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_group_managers_update_per_instance_configs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_group_managers_update_per_instance_configs_task()`.
/// For the simplest API, use `compute_instance_group_managers_update_per_instance_configs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_group_managers_update_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_group_managers_update_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_group_managers_update_per_instance_configs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_group_managers_update_per_instance_configs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_update_per_instance_configs_builder()` + `compute_instance_group_managers_update_per_instance_configs_execute()`.
/// For task-level control, use `compute_instance_group_managers_update_per_instance_configs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_update_per_instance_configs(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_update_per_instance_configs_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_instance_group_managers_update_per_instance_configs_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances
/// Adds a list of instances to the specified instance group.  All of the instances in the instance group must be in the same `network/subnetwork`. Read Adding instances for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_add_instances_execute()` to send, or `compute_instance_groups_add_instances` for simplest API.

pub fn compute_instance_groups_add_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/addInstances",
        project,
        zone,
        instanceGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances
/// Adds a list of instances to the specified instance group.  All of the instances in the instance group must be in the same `network/subnetwork`. Read Adding instances for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_add_instances_execute()` or `compute_instance_groups_add_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_add_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_add_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances
/// Adds a list of instances to the specified instance group.  All of the instances in the instance group must be in the same `network/subnetwork`. Read Adding instances for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_add_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_add_instances_task()`.
/// For the simplest API, use `compute_instance_groups_add_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_add_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_add_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_groups_add_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_add_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsAddInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances
/// Adds a list of instances to the specified instance group.  All of the instances in the instance group must be in the same `network/subnetwork`. Read Adding instances for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_add_instances_builder()` + `compute_instance_groups_add_instances_execute()`.
/// For task-level control, use `compute_instance_groups_add_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_add_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsAddInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_add_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroup,
        &args.requestId,
    )?;
    compute_instance_groups_add_instances_execute(builder)
}

/// GET projects/{project}/aggregated/instanceGroups
/// Retrieves the list of instance groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_aggregated_list_execute()` to send, or `compute_instance_groups_aggregated_list` for simplest API.

pub fn compute_instance_groups_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instanceGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/instanceGroups
/// Retrieves the list of instance groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_aggregated_list_execute()` or `compute_instance_groups_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/instanceGroups
/// Retrieves the list of instance groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_aggregated_list_task()`.
/// For the simplest API, use `compute_instance_groups_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_groups_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/instanceGroups
/// Retrieves the list of instance groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_aggregated_list_builder()` + `compute_instance_groups_aggregated_list_execute()`.
/// For task-level control, use `compute_instance_groups_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_instance_groups_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Deletes the specified instance group. The instances in the group are not deleted. Note that instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_delete_execute()` to send, or `compute_instance_groups_delete` for simplest API.

pub fn compute_instance_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}",
        project, zone, instanceGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Deletes the specified instance group. The instances in the group are not deleted. Note that instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_delete_execute()` or `compute_instance_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Deletes the specified instance group. The instances in the group are not deleted. Note that instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_delete_task()`.
/// For the simplest API, use `compute_instance_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Deletes the specified instance group. The instances in the group are not deleted. Note that instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_delete_builder()` + `compute_instance_groups_delete_execute()`.
/// For task-level control, use `compute_instance_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroup,
        &args.requestId,
    )?;
    compute_instance_groups_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Returns the specified zonal instance group. Get a list of available zonal instance groups by making a list() request.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_get_execute()` to send, or `compute_instance_groups_get` for simplest API.

pub fn compute_instance_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}",
        project, zone, instanceGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Returns the specified zonal instance group. Get a list of available zonal instance groups by making a list() request.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_get_execute()` or `compute_instance_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Returns the specified zonal instance group. Get a list of available zonal instance groups by making a list() request.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_get_task()`.
/// For the simplest API, use `compute_instance_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Returns the specified zonal instance group. Get a list of available zonal instance groups by making a list() request.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_get_builder()` + `compute_instance_groups_get_execute()`.
/// For task-level control, use `compute_instance_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroup,
    )?;
    compute_instance_groups_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups
/// Creates an instance group in the specified project using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_insert_execute()` to send, or `compute_instance_groups_insert` for simplest API.

pub fn compute_instance_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups
/// Creates an instance group in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_insert_execute()` or `compute_instance_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroups
/// Creates an instance group in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_insert_task()`.
/// For the simplest API, use `compute_instance_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroups
/// Creates an instance group in the specified project using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_insert_builder()` + `compute_instance_groups_insert_execute()`.
/// For task-level control, use `compute_instance_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instance_groups_insert_builder(client, &args.project, &args.zone, &args.requestId)?;
    compute_instance_groups_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Retrieves the list of zonal instance group resources contained within the specified zone.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_list_execute()` to send, or `compute_instance_groups_list` for simplest API.

pub fn compute_instance_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Retrieves the list of zonal instance group resources contained within the specified zone.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_list_execute()` or `compute_instance_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Retrieves the list of zonal instance group resources contained within the specified zone.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_list_task()`.
/// For the simplest API, use `compute_instance_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Retrieves the list of zonal instance group resources contained within the specified zone.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_list_builder()` + `compute_instance_groups_list_execute()`.
/// For task-level control, use `compute_instance_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instance_groups_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group. The `orderBy` query parameter is not supported. The filter query parameter is supported, but only for expressions that use eq (equal) or ne (not equal) operators.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_list_instances_execute()` to send, or `compute_instance_groups_list_instances` for simplest API.

pub fn compute_instance_groups_list_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroup: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/listInstances",
        project,
        zone,
        instanceGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group. The `orderBy` query parameter is not supported. The filter query parameter is supported, but only for expressions that use eq (equal) or ne (not equal) operators.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_list_instances_execute()` or `compute_instance_groups_list_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_list_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_list_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupsListInstances>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupsListInstances = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group. The `orderBy` query parameter is not supported. The filter query parameter is supported, but only for expressions that use eq (equal) or ne (not equal) operators.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_list_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_list_instances_task()`.
/// For the simplest API, use `compute_instance_groups_list_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_list_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_list_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupsListInstances>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_groups_list_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_list_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsListInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group. The `orderBy` query parameter is not supported. The filter query parameter is supported, but only for expressions that use eq (equal) or ne (not equal) operators.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_list_instances_builder()` + `compute_instance_groups_list_instances_execute()`.
/// For task-level control, use `compute_instance_groups_list_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_list_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsListInstancesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupsListInstances>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_list_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroup,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instance_groups_list_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances
/// Removes one or more instances from the specified instance group, but does not delete those instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration before the VM instance is removed or deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_remove_instances_execute()` to send, or `compute_instance_groups_remove_instances` for simplest API.

pub fn compute_instance_groups_remove_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/removeInstances",
        project,
        zone,
        instanceGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances
/// Removes one or more instances from the specified instance group, but does not delete those instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_remove_instances_execute()` or `compute_instance_groups_remove_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_remove_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_remove_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances
/// Removes one or more instances from the specified instance group, but does not delete those instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_remove_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_remove_instances_task()`.
/// For the simplest API, use `compute_instance_groups_remove_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_remove_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_remove_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_groups_remove_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_remove_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsRemoveInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances
/// Removes one or more instances from the specified instance group, but does not delete those instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration before the VM instance is removed or deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_remove_instances_builder()` + `compute_instance_groups_remove_instances_execute()`.
/// For task-level control, use `compute_instance_groups_remove_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_remove_instances(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsRemoveInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_remove_instances_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroup,
        &args.requestId,
    )?;
    compute_instance_groups_remove_instances_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_set_named_ports_execute()` to send, or `compute_instance_groups_set_named_ports` for simplest API.

pub fn compute_instance_groups_set_named_ports_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instanceGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/setNamedPorts",
        project,
        zone,
        instanceGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_set_named_ports_execute()` or `compute_instance_groups_set_named_ports`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_set_named_ports_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_set_named_ports_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_set_named_ports_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_set_named_ports_task()`.
/// For the simplest API, use `compute_instance_groups_set_named_ports()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_set_named_ports_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_set_named_ports_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_groups_set_named_ports_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_set_named_ports`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsSetNamedPortsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_set_named_ports_builder()` + `compute_instance_groups_set_named_ports_execute()`.
/// For task-level control, use `compute_instance_groups_set_named_ports_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_set_named_ports(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsSetNamedPortsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_set_named_ports_builder(
        client,
        &args.project,
        &args.zone,
        &args.instanceGroup,
        &args.requestId,
    )?;
    compute_instance_groups_set_named_ports_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_test_iam_permissions_execute()` to send, or `compute_instance_groups_test_iam_permissions` for simplest API.

pub fn compute_instance_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_groups_test_iam_permissions_execute()` or `compute_instance_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_groups_test_iam_permissions_task()`.
/// For the simplest API, use `compute_instance_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_test_iam_permissions_builder()` + `compute_instance_groups_test_iam_permissions_execute()`.
/// For task-level control, use `compute_instance_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeInstanceGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_instance_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Get Instance settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_settings_get_execute()` to send, or `compute_instance_settings_get` for simplest API.

pub fn compute_instance_settings_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceSettings",
        project, zone,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Get Instance settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_settings_get_execute()` or `compute_instance_settings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_settings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Get Instance settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_settings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_settings_get_task()`.
/// For the simplest API, use `compute_instance_settings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_settings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_settings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_settings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceSettingsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Get Instance settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_settings_get_builder()` + `compute_instance_settings_get_execute()`.
/// For task-level control, use `compute_instance_settings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_settings_get(
    client: &SimpleHttpClient,
    args: &ComputeInstanceSettingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_settings_get_builder(client, &args.project, &args.zone)?;
    compute_instance_settings_get_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/instanceSettings
/// Patch Instance settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_settings_patch_execute()` to send, or `compute_instance_settings_patch` for simplest API.

pub fn compute_instance_settings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceSettings",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/instanceSettings
/// Patch Instance settings
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_settings_patch_execute()` or `compute_instance_settings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_settings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_settings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/instanceSettings
/// Patch Instance settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_settings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_settings_patch_task()`.
/// For the simplest API, use `compute_instance_settings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_settings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_settings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_settings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_settings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceSettingsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/instanceSettings
/// Patch Instance settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_settings_patch_builder()` + `compute_instance_settings_patch_execute()`.
/// For task-level control, use `compute_instance_settings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_settings_patch(
    client: &SimpleHttpClient,
    args: &ComputeInstanceSettingsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_settings_patch_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_instance_settings_patch_execute(builder)
}

/// GET projects/{project}/aggregated/instanceTemplates
/// Retrieves the list of all InstanceTemplates resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_aggregated_list_execute()` to send, or `compute_instance_templates_aggregated_list` for simplest API.

pub fn compute_instance_templates_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instanceTemplates",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/instanceTemplates
/// Retrieves the list of all InstanceTemplates resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_templates_aggregated_list_execute()` or `compute_instance_templates_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplateAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplateAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/instanceTemplates
/// Retrieves the list of all InstanceTemplates resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_templates_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_templates_aggregated_list_task()`.
/// For the simplest API, use `compute_instance_templates_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_templates_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceTemplateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_templates_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_templates_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/instanceTemplates
/// Retrieves the list of all InstanceTemplates resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_aggregated_list_builder()` + `compute_instance_templates_aggregated_list_execute()`.
/// For task-level control, use `compute_instance_templates_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeInstanceTemplatesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceTemplateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_instance_templates_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone. It is not possible to delete templates that are already in use by a managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_delete_execute()` to send, or `compute_instance_templates_delete` for simplest API.

pub fn compute_instance_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instanceTemplate: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}",
        project, instanceTemplate,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone. It is not possible to delete templates that are already in use by a managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_templates_delete_execute()` or `compute_instance_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone. It is not possible to delete templates that are already in use by a managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_templates_delete_task()`.
/// For the simplest API, use `compute_instance_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instanceTemplate
    pub instanceTemplate: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone. It is not possible to delete templates that are already in use by a managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_delete_builder()` + `compute_instance_templates_delete_execute()`.
/// For task-level control, use `compute_instance_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_delete(
    client: &SimpleHttpClient,
    args: &ComputeInstanceTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_delete_builder(
        client,
        &args.project,
        &args.instanceTemplate,
        &args.requestId,
    )?;
    compute_instance_templates_delete_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_get_execute()` to send, or `compute_instance_templates_get` for simplest API.

pub fn compute_instance_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instanceTemplate: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}",
        project, instanceTemplate,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_templates_get_execute()` or `compute_instance_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_templates_get_task()`.
/// For the simplest API, use `compute_instance_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instanceTemplate
    pub instanceTemplate: String,
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_get_builder()` + `compute_instance_templates_get_execute()`.
/// For task-level control, use `compute_instance_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_get(
    client: &SimpleHttpClient,
    args: &ComputeInstanceTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_instance_templates_get_builder(client, &args.project, &args.instanceTemplate)?;
    compute_instance_templates_get_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_get_iam_policy_execute()` to send, or `compute_instance_templates_get_iam_policy` for simplest API.

pub fn compute_instance_templates_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}/getIamPolicy",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_templates_get_iam_policy_execute()` or `compute_instance_templates_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_templates_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_templates_get_iam_policy_task()`.
/// For the simplest API, use `compute_instance_templates_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_templates_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_templates_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_get_iam_policy_builder()` + `compute_instance_templates_get_iam_policy_execute()`.
/// For task-level control, use `compute_instance_templates_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstanceTemplatesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_instance_templates_get_iam_policy_execute(builder)
}

/// POST projects/{project}/global/instanceTemplates
/// Creates an instance template in the specified project using the data that is included in the request. If you are creating a new template to update an existing instance group, your new instance template must use the same network or, if applicable, the same subnetwork as the original template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_insert_execute()` to send, or `compute_instance_templates_insert` for simplest API.

pub fn compute_instance_templates_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/instanceTemplates
/// Creates an instance template in the specified project using the data that is included in the request. If you are creating a new template to update an existing instance group, your new instance template must use the same network or, if applicable, the same subnetwork as the original template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_templates_insert_execute()` or `compute_instance_templates_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/instanceTemplates
/// Creates an instance template in the specified project using the data that is included in the request. If you are creating a new template to update an existing instance group, your new instance template must use the same network or, if applicable, the same subnetwork as the original template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_templates_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_templates_insert_task()`.
/// For the simplest API, use `compute_instance_templates_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_templates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_templates_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_templates_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/instanceTemplates
/// Creates an instance template in the specified project using the data that is included in the request. If you are creating a new template to update an existing instance group, your new instance template must use the same network or, if applicable, the same subnetwork as the original template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_insert_builder()` + `compute_instance_templates_insert_execute()`.
/// For task-level control, use `compute_instance_templates_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_insert(
    client: &SimpleHttpClient,
    args: &ComputeInstanceTemplatesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instance_templates_insert_builder(client, &args.project, &args.requestId)?;
    compute_instance_templates_insert_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_list_execute()` to send, or `compute_instance_templates_list` for simplest API.

pub fn compute_instance_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_templates_list_execute()` or `compute_instance_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplateList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_templates_list_task()`.
/// For the simplest API, use `compute_instance_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_list_builder()` + `compute_instance_templates_list_execute()`.
/// For task-level control, use `compute_instance_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_list(
    client: &SimpleHttpClient,
    args: &ComputeInstanceTemplatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instance_templates_list_execute(builder)
}

/// POST projects/{project}/global/instanceTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_set_iam_policy_execute()` to send, or `compute_instance_templates_set_iam_policy` for simplest API.

pub fn compute_instance_templates_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}/setIamPolicy",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/instanceTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_templates_set_iam_policy_execute()` or `compute_instance_templates_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/instanceTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_templates_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_templates_set_iam_policy_task()`.
/// For the simplest API, use `compute_instance_templates_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instance_templates_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_templates_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/instanceTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_set_iam_policy_builder()` + `compute_instance_templates_set_iam_policy_execute()`.
/// For task-level control, use `compute_instance_templates_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstanceTemplatesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instance_templates_set_iam_policy_builder(client, &args.project, &args.resource)?;
    compute_instance_templates_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/instanceTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_test_iam_permissions_execute()` to send, or `compute_instance_templates_test_iam_permissions` for simplest API.

pub fn compute_instance_templates_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/instanceTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instance_templates_test_iam_permissions_execute()` or `compute_instance_templates_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/instanceTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instance_templates_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instance_templates_test_iam_permissions_task()`.
/// For the simplest API, use `compute_instance_templates_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instance_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instance_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instance_templates_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instance_templates_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/instanceTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_test_iam_permissions_builder()` + `compute_instance_templates_test_iam_permissions_execute()`.
/// For task-level control, use `compute_instance_templates_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeInstanceTemplatesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_instance_templates_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig
/// Adds an access config to an instance's network interface.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_add_access_config_execute()` to send, or `compute_instances_add_access_config` for simplest API.

pub fn compute_instances_add_access_config_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    networkInterface: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/addAccessConfig",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = networkInterface.as_ref() {
        query_parts.push(format!("networkInterface={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig
/// Adds an access config to an instance's network interface.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_add_access_config_execute()` or `compute_instances_add_access_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_add_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_access_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig
/// Adds an access config to an instance's network interface.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_add_access_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_add_access_config_task()`.
/// For the simplest API, use `compute_instances_add_access_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_add_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_add_access_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_add_access_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_add_access_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesAddAccessConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: networkInterface
    pub networkInterface: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig
/// Adds an access config to an instance's network interface.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_add_access_config_builder()` + `compute_instances_add_access_config_execute()`.
/// For task-level control, use `compute_instances_add_access_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_access_config(
    client: &SimpleHttpClient,
    args: &ComputeInstancesAddAccessConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_add_access_config_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.networkInterface,
        &args.requestId,
    )?;
    compute_instances_add_access_config_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface
/// Adds one dynamic network interface to an active instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_add_network_interface_execute()` to send, or `compute_instances_add_network_interface` for simplest API.

pub fn compute_instances_add_network_interface_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/addNetworkInterface",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface
/// Adds one dynamic network interface to an active instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_add_network_interface_execute()` or `compute_instances_add_network_interface`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_add_network_interface_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_network_interface_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface
/// Adds one dynamic network interface to an active instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_add_network_interface_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_add_network_interface_task()`.
/// For the simplest API, use `compute_instances_add_network_interface()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_add_network_interface_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_add_network_interface_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_add_network_interface_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_add_network_interface`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesAddNetworkInterfaceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface
/// Adds one dynamic network interface to an active instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_add_network_interface_builder()` + `compute_instances_add_network_interface_execute()`.
/// For task-level control, use `compute_instances_add_network_interface_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_network_interface(
    client: &SimpleHttpClient,
    args: &ComputeInstancesAddNetworkInterfaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_add_network_interface_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_add_network_interface_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies
/// Adds existing resource policies to an instance. You can only add one policy right now which will be applied to this instance for scheduling live migrations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_add_resource_policies_execute()` to send, or `compute_instances_add_resource_policies` for simplest API.

pub fn compute_instances_add_resource_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/addResourcePolicies",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies
/// Adds existing resource policies to an instance. You can only add one policy right now which will be applied to this instance for scheduling live migrations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_add_resource_policies_execute()` or `compute_instances_add_resource_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_add_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_resource_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies
/// Adds existing resource policies to an instance. You can only add one policy right now which will be applied to this instance for scheduling live migrations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_add_resource_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_add_resource_policies_task()`.
/// For the simplest API, use `compute_instances_add_resource_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_add_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_add_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_add_resource_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_add_resource_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesAddResourcePoliciesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies
/// Adds existing resource policies to an instance. You can only add one policy right now which will be applied to this instance for scheduling live migrations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_add_resource_policies_builder()` + `compute_instances_add_resource_policies_execute()`.
/// For task-level control, use `compute_instances_add_resource_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_resource_policies(
    client: &SimpleHttpClient,
    args: &ComputeInstancesAddResourcePoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_add_resource_policies_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_add_resource_policies_execute(builder)
}

/// GET projects/{project}/aggregated/instances
/// Retrieves an aggregated list of all of the instances in your project across all regions and zones.  The performance of this method degrades when a filter is specified on a project that has a very large number of instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_aggregated_list_execute()` to send, or `compute_instances_aggregated_list` for simplest API.

pub fn compute_instances_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instances",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/instances
/// Retrieves an aggregated list of all of the instances in your project across all regions and zones.  The performance of this method degrades when a filter is specified on a project that has a very large number of instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_aggregated_list_execute()` or `compute_instances_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/instances
/// Retrieves an aggregated list of all of the instances in your project across all regions and zones.  The performance of this method degrades when a filter is specified on a project that has a very large number of instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_aggregated_list_task()`.
/// For the simplest API, use `compute_instances_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instances_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/instances
/// Retrieves an aggregated list of all of the instances in your project across all regions and zones.  The performance of this method degrades when a filter is specified on a project that has a very large number of instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_aggregated_list_builder()` + `compute_instances_aggregated_list_execute()`.
/// For task-level control, use `compute_instances_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeInstancesAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_instances_aggregated_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/attachDisk
/// Attaches an existing Disk resource to an instance. You must first create the disk before you can attach it. It is not possible to create and attach a disk at the same time. For more information, `readAdding` a persistent disk to your instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_attach_disk_execute()` to send, or `compute_instances_attach_disk` for simplest API.

pub fn compute_instances_attach_disk_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    forceAttach: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/attachDisk",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = forceAttach.as_ref() {
        query_parts.push(format!("forceAttach={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/attachDisk
/// Attaches an existing Disk resource to an instance. You must first create the disk before you can attach it. It is not possible to create and attach a disk at the same time. For more information, `readAdding` a persistent disk to your instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_attach_disk_execute()` or `compute_instances_attach_disk`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_attach_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_attach_disk_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/attachDisk
/// Attaches an existing Disk resource to an instance. You must first create the disk before you can attach it. It is not possible to create and attach a disk at the same time. For more information, `readAdding` a persistent disk to your instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_attach_disk_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_attach_disk_task()`.
/// For the simplest API, use `compute_instances_attach_disk()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_attach_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_attach_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_attach_disk_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_attach_disk`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesAttachDiskArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: forceAttach
    pub forceAttach: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/attachDisk
/// Attaches an existing Disk resource to an instance. You must first create the disk before you can attach it. It is not possible to create and attach a disk at the same time. For more information, `readAdding` a persistent disk to your instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_attach_disk_builder()` + `compute_instances_attach_disk_execute()`.
/// For task-level control, use `compute_instances_attach_disk_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_attach_disk(
    client: &SimpleHttpClient,
    args: &ComputeInstancesAttachDiskArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_attach_disk_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.forceAttach,
        &args.requestId,
    )?;
    compute_instances_attach_disk_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/bulkInsert
/// Creates multiple instances. Count specifies the number of instances to create. For more information, `seeAbout` bulk creation of VMs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_bulk_insert_execute()` to send, or `compute_instances_bulk_insert` for simplest API.

pub fn compute_instances_bulk_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/bulkInsert",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/bulkInsert
/// Creates multiple instances. Count specifies the number of instances to create. For more information, `seeAbout` bulk creation of VMs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_bulk_insert_execute()` or `compute_instances_bulk_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_bulk_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_bulk_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/bulkInsert
/// Creates multiple instances. Count specifies the number of instances to create. For more information, `seeAbout` bulk creation of VMs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_bulk_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_bulk_insert_task()`.
/// For the simplest API, use `compute_instances_bulk_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_bulk_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_bulk_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_bulk_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_bulk_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesBulkInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/bulkInsert
/// Creates multiple instances. Count specifies the number of instances to create. For more information, `seeAbout` bulk creation of VMs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_bulk_insert_builder()` + `compute_instances_bulk_insert_execute()`.
/// For task-level control, use `compute_instances_bulk_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_bulk_insert(
    client: &SimpleHttpClient,
    args: &ComputeInstancesBulkInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instances_bulk_insert_builder(client, &args.project, &args.zone, &args.requestId)?;
    compute_instances_bulk_insert_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/instances/{instance}
/// Deletes the specified Instance resource. For more information, `seeDeleting` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_delete_execute()` to send, or `compute_instances_delete` for simplest API.

pub fn compute_instances_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/instances/{instance}
/// Deletes the specified Instance resource. For more information, `seeDeleting` an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_delete_execute()` or `compute_instances_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/instances/{instance}
/// Deletes the specified Instance resource. For more information, `seeDeleting` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_delete_task()`.
/// For the simplest API, use `compute_instances_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/instances/{instance}
/// Deletes the specified Instance resource. For more information, `seeDeleting` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_delete_builder()` + `compute_instances_delete_execute()`.
/// For task-level control, use `compute_instances_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete(
    client: &SimpleHttpClient,
    args: &ComputeInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_delete_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig
/// Deletes an access config from an instance's network interface.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_delete_access_config_execute()` to send, or `compute_instances_delete_access_config` for simplest API.

pub fn compute_instances_delete_access_config_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    accessConfig: &Option<Option<String>>,
    networkInterface: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/deleteAccessConfig",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accessConfig.as_ref() {
        query_parts.push(format!("accessConfig={}", val));
    }
    if let Some(val) = networkInterface.as_ref() {
        query_parts.push(format!("networkInterface={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig
/// Deletes an access config from an instance's network interface.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_delete_access_config_execute()` or `compute_instances_delete_access_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_delete_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_access_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig
/// Deletes an access config from an instance's network interface.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_delete_access_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_delete_access_config_task()`.
/// For the simplest API, use `compute_instances_delete_access_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_delete_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_delete_access_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_delete_access_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_delete_access_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesDeleteAccessConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: accessConfig
    pub accessConfig: Option<Option<String>>,
    /// Query parameter: networkInterface
    pub networkInterface: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig
/// Deletes an access config from an instance's network interface.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_delete_access_config_builder()` + `compute_instances_delete_access_config_execute()`.
/// For task-level control, use `compute_instances_delete_access_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_access_config(
    client: &SimpleHttpClient,
    args: &ComputeInstancesDeleteAccessConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_delete_access_config_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.accessConfig,
        &args.networkInterface,
        &args.requestId,
    )?;
    compute_instances_delete_access_config_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface
/// Deletes one dynamic network interface from an active instance. InstancesDeleteNetworkInterfaceRequest indicates: - instance from which to delete, using project+zone+resource_id fields; - dynamic network interface to be deleted, using network_interface_name field;
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_delete_network_interface_execute()` to send, or `compute_instances_delete_network_interface` for simplest API.

pub fn compute_instances_delete_network_interface_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    networkInterfaceName: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/deleteNetworkInterface",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = networkInterfaceName.as_ref() {
        query_parts.push(format!("networkInterfaceName={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface
/// Deletes one dynamic network interface from an active instance. InstancesDeleteNetworkInterfaceRequest indicates: - instance from which to delete, using project+zone+resource_id fields; - dynamic network interface to be deleted, using network_interface_name field;
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_delete_network_interface_execute()` or `compute_instances_delete_network_interface`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_delete_network_interface_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_network_interface_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface
/// Deletes one dynamic network interface from an active instance. InstancesDeleteNetworkInterfaceRequest indicates: - instance from which to delete, using project+zone+resource_id fields; - dynamic network interface to be deleted, using network_interface_name field;
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_delete_network_interface_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_delete_network_interface_task()`.
/// For the simplest API, use `compute_instances_delete_network_interface()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_delete_network_interface_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_delete_network_interface_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_delete_network_interface_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_delete_network_interface`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesDeleteNetworkInterfaceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: networkInterfaceName
    pub networkInterfaceName: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface
/// Deletes one dynamic network interface from an active instance. InstancesDeleteNetworkInterfaceRequest indicates: - instance from which to delete, using project+zone+resource_id fields; - dynamic network interface to be deleted, using network_interface_name field;
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_delete_network_interface_builder()` + `compute_instances_delete_network_interface_execute()`.
/// For task-level control, use `compute_instances_delete_network_interface_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_network_interface(
    client: &SimpleHttpClient,
    args: &ComputeInstancesDeleteNetworkInterfaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_delete_network_interface_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.networkInterfaceName,
        &args.requestId,
    )?;
    compute_instances_delete_network_interface_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/detachDisk
/// Detaches a disk from an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_detach_disk_execute()` to send, or `compute_instances_detach_disk` for simplest API.

pub fn compute_instances_detach_disk_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    deviceName: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/detachDisk",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deviceName.as_ref() {
        query_parts.push(format!("deviceName={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/detachDisk
/// Detaches a disk from an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_detach_disk_execute()` or `compute_instances_detach_disk`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_detach_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_detach_disk_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/detachDisk
/// Detaches a disk from an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_detach_disk_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_detach_disk_task()`.
/// For the simplest API, use `compute_instances_detach_disk()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_detach_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_detach_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_detach_disk_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_detach_disk`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesDetachDiskArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: deviceName
    pub deviceName: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/detachDisk
/// Detaches a disk from an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_detach_disk_builder()` + `compute_instances_detach_disk_execute()`.
/// For task-level control, use `compute_instances_detach_disk_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_detach_disk(
    client: &SimpleHttpClient,
    args: &ComputeInstancesDetachDiskArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_detach_disk_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.deviceName,
        &args.requestId,
    )?;
    compute_instances_detach_disk_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Returns the specified Instance resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_execute()` to send, or `compute_instances_get` for simplest API.

pub fn compute_instances_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}",
        project, zone, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Returns the specified Instance resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_get_execute()` or `compute_instances_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Instance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Returns the specified Instance resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_get_task()`.
/// For the simplest API, use `compute_instances_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Returns the specified Instance resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_builder()` + `compute_instances_get_execute()`.
/// For task-level control, use `compute_instances_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get(
    client: &SimpleHttpClient,
    args: &ComputeInstancesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_get_builder(client, &args.project, &args.zone, &args.instance)?;
    compute_instances_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls
/// Returns effective firewalls applied to an interface of the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_effective_firewalls_execute()` to send, or `compute_instances_get_effective_firewalls` for simplest API.

pub fn compute_instances_get_effective_firewalls_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    networkInterface: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/getEffectiveFirewalls",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = networkInterface.as_ref() {
        query_parts.push(format!("networkInterface={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls
/// Returns effective firewalls applied to an interface of the instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_get_effective_firewalls_execute()` or `compute_instances_get_effective_firewalls`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_effective_firewalls_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_effective_firewalls_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstancesGetEffectiveFirewallsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesGetEffectiveFirewallsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls
/// Returns effective firewalls applied to an interface of the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_get_effective_firewalls_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_get_effective_firewalls_task()`.
/// For the simplest API, use `compute_instances_get_effective_firewalls()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_effective_firewalls_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_get_effective_firewalls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesGetEffectiveFirewallsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instances_get_effective_firewalls_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_get_effective_firewalls`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesGetEffectiveFirewallsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: networkInterface
    pub networkInterface: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls
/// Returns effective firewalls applied to an interface of the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_effective_firewalls_builder()` + `compute_instances_get_effective_firewalls_execute()`.
/// For task-level control, use `compute_instances_get_effective_firewalls_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_effective_firewalls(
    client: &SimpleHttpClient,
    args: &ComputeInstancesGetEffectiveFirewallsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesGetEffectiveFirewallsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_get_effective_firewalls_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.networkInterface,
    )?;
    compute_instances_get_effective_firewalls_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes
/// Returns the specified guest attributes entry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_guest_attributes_execute()` to send, or `compute_instances_get_guest_attributes` for simplest API.

pub fn compute_instances_get_guest_attributes_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    queryPath: &Option<Option<String>>,
    variableKey: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/getGuestAttributes",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = queryPath.as_ref() {
        query_parts.push(format!("queryPath={}", val));
    }
    if let Some(val) = variableKey.as_ref() {
        query_parts.push(format!("variableKey={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes
/// Returns the specified guest attributes entry.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_get_guest_attributes_execute()` or `compute_instances_get_guest_attributes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_guest_attributes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_guest_attributes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GuestAttributes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GuestAttributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes
/// Returns the specified guest attributes entry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_get_guest_attributes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_get_guest_attributes_task()`.
/// For the simplest API, use `compute_instances_get_guest_attributes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_guest_attributes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_get_guest_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuestAttributes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instances_get_guest_attributes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_get_guest_attributes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesGetGuestAttributesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: queryPath
    pub queryPath: Option<Option<String>>,
    /// Query parameter: variableKey
    pub variableKey: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes
/// Returns the specified guest attributes entry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_guest_attributes_builder()` + `compute_instances_get_guest_attributes_execute()`.
/// For task-level control, use `compute_instances_get_guest_attributes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_guest_attributes(
    client: &SimpleHttpClient,
    args: &ComputeInstancesGetGuestAttributesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuestAttributes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_get_guest_attributes_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.queryPath,
        &args.variableKey,
    )?;
    compute_instances_get_guest_attributes_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_iam_policy_execute()` to send, or `compute_instances_get_iam_policy` for simplest API.

pub fn compute_instances_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/getIamPolicy",
        project, zone, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_get_iam_policy_execute()` or `compute_instances_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_get_iam_policy_task()`.
/// For the simplest API, use `compute_instances_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_iam_policy_builder()` + `compute_instances_get_iam_policy_execute()`.
/// For task-level control, use `compute_instances_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstancesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_instances_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot
/// Returns the screenshot from the specified instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_screenshot_execute()` to send, or `compute_instances_get_screenshot` for simplest API.

pub fn compute_instances_get_screenshot_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/screenshot",
        project, zone, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot
/// Returns the screenshot from the specified instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_get_screenshot_execute()` or `compute_instances_get_screenshot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_screenshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_screenshot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Screenshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Screenshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot
/// Returns the screenshot from the specified instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_get_screenshot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_get_screenshot_task()`.
/// For the simplest API, use `compute_instances_get_screenshot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_screenshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_get_screenshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Screenshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_get_screenshot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_get_screenshot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesGetScreenshotArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot
/// Returns the screenshot from the specified instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_screenshot_builder()` + `compute_instances_get_screenshot_execute()`.
/// For task-level control, use `compute_instances_get_screenshot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_screenshot(
    client: &SimpleHttpClient,
    args: &ComputeInstancesGetScreenshotArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Screenshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_get_screenshot_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
    )?;
    compute_instances_get_screenshot_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort
/// Returns the last 1 MB of serial port output from the specified instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_serial_port_output_execute()` to send, or `compute_instances_get_serial_port_output` for simplest API.

pub fn compute_instances_get_serial_port_output_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    port: &Option<Option<String>>,
    start: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/serialPort",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = port.as_ref() {
        query_parts.push(format!("port={}", val));
    }
    if let Some(val) = start.as_ref() {
        query_parts.push(format!("start={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort
/// Returns the last 1 MB of serial port output from the specified instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_get_serial_port_output_execute()` or `compute_instances_get_serial_port_output`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_serial_port_output_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_serial_port_output_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SerialPortOutput>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SerialPortOutput = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort
/// Returns the last 1 MB of serial port output from the specified instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_get_serial_port_output_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_get_serial_port_output_task()`.
/// For the simplest API, use `compute_instances_get_serial_port_output()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_serial_port_output_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_get_serial_port_output_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SerialPortOutput>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instances_get_serial_port_output_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_get_serial_port_output`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesGetSerialPortOutputArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: port
    pub port: Option<Option<String>>,
    /// Query parameter: start
    pub start: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort
/// Returns the last 1 MB of serial port output from the specified instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_serial_port_output_builder()` + `compute_instances_get_serial_port_output_execute()`.
/// For task-level control, use `compute_instances_get_serial_port_output_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_serial_port_output(
    client: &SimpleHttpClient,
    args: &ComputeInstancesGetSerialPortOutputArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SerialPortOutput>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_get_serial_port_output_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.port,
        &args.start,
    )?;
    compute_instances_get_serial_port_output_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity
/// Returns the Shielded Instance Identity of an instance
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_shielded_instance_identity_execute()` to send, or `compute_instances_get_shielded_instance_identity` for simplest API.

pub fn compute_instances_get_shielded_instance_identity_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/getShieldedInstanceIdentity",
        project,
        zone,
        instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity
/// Returns the Shielded Instance Identity of an instance
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_get_shielded_instance_identity_execute()` or `compute_instances_get_shielded_instance_identity`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_shielded_instance_identity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_shielded_instance_identity_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShieldedInstanceIdentity>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShieldedInstanceIdentity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity
/// Returns the Shielded Instance Identity of an instance
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_get_shielded_instance_identity_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_get_shielded_instance_identity_task()`.
/// For the simplest API, use `compute_instances_get_shielded_instance_identity()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_get_shielded_instance_identity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_get_shielded_instance_identity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShieldedInstanceIdentity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instances_get_shielded_instance_identity_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_get_shielded_instance_identity`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesGetShieldedInstanceIdentityArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity
/// Returns the Shielded Instance Identity of an instance
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_shielded_instance_identity_builder()` + `compute_instances_get_shielded_instance_identity_execute()`.
/// For task-level control, use `compute_instances_get_shielded_instance_identity_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_shielded_instance_identity(
    client: &SimpleHttpClient,
    args: &ComputeInstancesGetShieldedInstanceIdentityArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShieldedInstanceIdentity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_get_shielded_instance_identity_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
    )?;
    compute_instances_get_shielded_instance_identity_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances
/// Creates an instance resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_insert_execute()` to send, or `compute_instances_insert` for simplest API.

pub fn compute_instances_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
    sourceInstanceTemplate: &Option<Option<String>>,
    sourceMachineImage: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceInstanceTemplate.as_ref() {
        query_parts.push(format!("sourceInstanceTemplate={}", val));
    }
    if let Some(val) = sourceMachineImage.as_ref() {
        query_parts.push(format!("sourceMachineImage={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances
/// Creates an instance resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_insert_execute()` or `compute_instances_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances
/// Creates an instance resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_insert_task()`.
/// For the simplest API, use `compute_instances_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceInstanceTemplate
    pub sourceInstanceTemplate: Option<Option<String>>,
    /// Query parameter: sourceMachineImage
    pub sourceMachineImage: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances
/// Creates an instance resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_insert_builder()` + `compute_instances_insert_execute()`.
/// For task-level control, use `compute_instances_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_insert(
    client: &SimpleHttpClient,
    args: &ComputeInstancesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
        &args.sourceInstanceTemplate,
        &args.sourceMachineImage,
    )?;
    compute_instances_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances
/// Retrieves the list of instances contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_list_execute()` to send, or `compute_instances_list` for simplest API.

pub fn compute_instances_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances
/// Retrieves the list of instances contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_list_execute()` or `compute_instances_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances
/// Retrieves the list of instances contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_list_task()`.
/// For the simplest API, use `compute_instances_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instances_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instances
/// Retrieves the list of instances contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_list_builder()` + `compute_instances_list_execute()`.
/// For task-level control, use `compute_instances_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_list(
    client: &SimpleHttpClient,
    args: &ComputeInstancesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instances_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/referrers
/// Retrieves a list of resources that refer to the VM instance specified in the request. For example, if the VM instance is part of a managed or unmanaged instance group, the referrers list includes the instance group. For more information, `readViewing` referrers to VM instances.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_list_referrers_execute()` to send, or `compute_instances_list_referrers` for simplest API.

pub fn compute_instances_list_referrers_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/referrers",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/referrers
/// Retrieves a list of resources that refer to the VM instance specified in the request. For example, if the VM instance is part of a managed or unmanaged instance group, the referrers list includes the instance group. For more information, `readViewing` referrers to VM instances.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_list_referrers_execute()` or `compute_instances_list_referrers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_list_referrers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_list_referrers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceListReferrers>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceListReferrers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/referrers
/// Retrieves a list of resources that refer to the VM instance specified in the request. For example, if the VM instance is part of a managed or unmanaged instance group, the referrers list includes the instance group. For more information, `readViewing` referrers to VM instances.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_list_referrers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_list_referrers_task()`.
/// For the simplest API, use `compute_instances_list_referrers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_list_referrers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_list_referrers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceListReferrers>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instances_list_referrers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_list_referrers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesListReferrersArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/referrers
/// Retrieves a list of resources that refer to the VM instance specified in the request. For example, if the VM instance is part of a managed or unmanaged instance group, the referrers list includes the instance group. For more information, `readViewing` referrers to VM instances.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_list_referrers_builder()` + `compute_instances_list_referrers_execute()`.
/// For task-level control, use `compute_instances_list_referrers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_list_referrers(
    client: &SimpleHttpClient,
    args: &ComputeInstancesListReferrersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceListReferrers>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_list_referrers_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instances_list_referrers_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/performMaintenance
/// Perform a manual maintenance on the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_perform_maintenance_execute()` to send, or `compute_instances_perform_maintenance` for simplest API.

pub fn compute_instances_perform_maintenance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/performMaintenance",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/performMaintenance
/// Perform a manual maintenance on the instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_perform_maintenance_execute()` or `compute_instances_perform_maintenance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_perform_maintenance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/performMaintenance
/// Perform a manual maintenance on the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_perform_maintenance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_perform_maintenance_task()`.
/// For the simplest API, use `compute_instances_perform_maintenance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_perform_maintenance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_perform_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesPerformMaintenanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/performMaintenance
/// Perform a manual maintenance on the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_perform_maintenance_builder()` + `compute_instances_perform_maintenance_execute()`.
/// For task-level control, use `compute_instances_perform_maintenance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_perform_maintenance(
    client: &SimpleHttpClient,
    args: &ComputeInstancesPerformMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_perform_maintenance_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_perform_maintenance_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies
/// Removes resource policies from an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_remove_resource_policies_execute()` to send, or `compute_instances_remove_resource_policies` for simplest API.

pub fn compute_instances_remove_resource_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/removeResourcePolicies",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies
/// Removes resource policies from an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_remove_resource_policies_execute()` or `compute_instances_remove_resource_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_remove_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_remove_resource_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies
/// Removes resource policies from an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_remove_resource_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_remove_resource_policies_task()`.
/// For the simplest API, use `compute_instances_remove_resource_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_remove_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_remove_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_remove_resource_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_remove_resource_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesRemoveResourcePoliciesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies
/// Removes resource policies from an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_remove_resource_policies_builder()` + `compute_instances_remove_resource_policies_execute()`.
/// For task-level control, use `compute_instances_remove_resource_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_remove_resource_policies(
    client: &SimpleHttpClient,
    args: &ComputeInstancesRemoveResourcePoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_remove_resource_policies_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_remove_resource_policies_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty
/// Mark the host as faulty and try to restart the instance on a new host.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_report_host_as_faulty_execute()` to send, or `compute_instances_report_host_as_faulty` for simplest API.

pub fn compute_instances_report_host_as_faulty_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/reportHostAsFaulty",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty
/// Mark the host as faulty and try to restart the instance on a new host.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_report_host_as_faulty_execute()` or `compute_instances_report_host_as_faulty`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_report_host_as_faulty_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_report_host_as_faulty_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty
/// Mark the host as faulty and try to restart the instance on a new host.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_report_host_as_faulty_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_report_host_as_faulty_task()`.
/// For the simplest API, use `compute_instances_report_host_as_faulty()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_report_host_as_faulty_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_report_host_as_faulty_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_report_host_as_faulty_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_report_host_as_faulty`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesReportHostAsFaultyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty
/// Mark the host as faulty and try to restart the instance on a new host.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_report_host_as_faulty_builder()` + `compute_instances_report_host_as_faulty_execute()`.
/// For task-level control, use `compute_instances_report_host_as_faulty_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_report_host_as_faulty(
    client: &SimpleHttpClient,
    args: &ComputeInstancesReportHostAsFaultyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_report_host_as_faulty_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_report_host_as_faulty_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/reset
/// Performs a reset on the instance. This is a hard reset. The VM does not do a graceful shutdown. For more information, `seeResetting` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_reset_execute()` to send, or `compute_instances_reset` for simplest API.

pub fn compute_instances_reset_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/reset",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/reset
/// Performs a reset on the instance. This is a hard reset. The VM does not do a graceful shutdown. For more information, `seeResetting` an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_reset_execute()` or `compute_instances_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/reset
/// Performs a reset on the instance. This is a hard reset. The VM does not do a graceful shutdown. For more information, `seeResetting` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_reset_task()`.
/// For the simplest API, use `compute_instances_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesResetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/reset
/// Performs a reset on the instance. This is a hard reset. The VM does not do a graceful shutdown. For more information, `seeResetting` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_reset_builder()` + `compute_instances_reset_execute()`.
/// For task-level control, use `compute_instances_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_reset(
    client: &SimpleHttpClient,
    args: &ComputeInstancesResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_reset_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_reset_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/resume
/// Resumes an instance that was suspended using theinstances().suspend method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_resume_execute()` to send, or `compute_instances_resume` for simplest API.

pub fn compute_instances_resume_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/resume",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/resume
/// Resumes an instance that was suspended using theinstances().suspend method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_resume_execute()` or `compute_instances_resume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_resume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/resume
/// Resumes an instance that was suspended using theinstances().suspend method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_resume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_resume_task()`.
/// For the simplest API, use `compute_instances_resume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_resume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesResumeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/resume
/// Resumes an instance that was suspended using theinstances().suspend method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_resume_builder()` + `compute_instances_resume_execute()`.
/// For task-level control, use `compute_instances_resume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_resume(
    client: &SimpleHttpClient,
    args: &ComputeInstancesResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_resume_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_resume_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt
/// Sends diagnostic interrupt to the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_send_diagnostic_interrupt_execute()` to send, or `compute_instances_send_diagnostic_interrupt` for simplest API.

pub fn compute_instances_send_diagnostic_interrupt_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/sendDiagnosticInterrupt",
        project,
        zone,
        instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt
/// Sends diagnostic interrupt to the instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_send_diagnostic_interrupt_execute()` or `compute_instances_send_diagnostic_interrupt`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_send_diagnostic_interrupt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_send_diagnostic_interrupt_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt
/// Sends diagnostic interrupt to the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_send_diagnostic_interrupt_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_send_diagnostic_interrupt_task()`.
/// For the simplest API, use `compute_instances_send_diagnostic_interrupt()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_send_diagnostic_interrupt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_send_diagnostic_interrupt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_send_diagnostic_interrupt_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_send_diagnostic_interrupt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSendDiagnosticInterruptArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt
/// Sends diagnostic interrupt to the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_send_diagnostic_interrupt_builder()` + `compute_instances_send_diagnostic_interrupt_execute()`.
/// For task-level control, use `compute_instances_send_diagnostic_interrupt_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_send_diagnostic_interrupt(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSendDiagnosticInterruptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_send_diagnostic_interrupt_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
    )?;
    compute_instances_send_diagnostic_interrupt_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection
/// Sets deletion protection on the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_deletion_protection_execute()` to send, or `compute_instances_set_deletion_protection` for simplest API.

pub fn compute_instances_set_deletion_protection_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    deletionProtection: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setDeletionProtection",
        project,
        zone,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deletionProtection.as_ref() {
        query_parts.push(format!("deletionProtection={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection
/// Sets deletion protection on the instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_deletion_protection_execute()` or `compute_instances_set_deletion_protection`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_deletion_protection_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_deletion_protection_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection
/// Sets deletion protection on the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_deletion_protection_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_deletion_protection_task()`.
/// For the simplest API, use `compute_instances_set_deletion_protection()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_deletion_protection_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_deletion_protection_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_deletion_protection_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_deletion_protection`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetDeletionProtectionArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: deletionProtection
    pub deletionProtection: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection
/// Sets deletion protection on the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_deletion_protection_builder()` + `compute_instances_set_deletion_protection_execute()`.
/// For task-level control, use `compute_instances_set_deletion_protection_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_deletion_protection(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetDeletionProtectionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_deletion_protection_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.deletionProtection,
        &args.requestId,
    )?;
    compute_instances_set_deletion_protection_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete
/// Sets the auto-delete flag for a disk attached to an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_disk_auto_delete_execute()` to send, or `compute_instances_set_disk_auto_delete` for simplest API.

pub fn compute_instances_set_disk_auto_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    autoDelete: &Option<Option<String>>,
    deviceName: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setDiskAutoDelete",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = autoDelete.as_ref() {
        query_parts.push(format!("autoDelete={}", val));
    }
    if let Some(val) = deviceName.as_ref() {
        query_parts.push(format!("deviceName={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete
/// Sets the auto-delete flag for a disk attached to an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_disk_auto_delete_execute()` or `compute_instances_set_disk_auto_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_disk_auto_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_disk_auto_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete
/// Sets the auto-delete flag for a disk attached to an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_disk_auto_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_disk_auto_delete_task()`.
/// For the simplest API, use `compute_instances_set_disk_auto_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_disk_auto_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_disk_auto_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_disk_auto_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_disk_auto_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetDiskAutoDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: autoDelete
    pub autoDelete: Option<Option<String>>,
    /// Query parameter: deviceName
    pub deviceName: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete
/// Sets the auto-delete flag for a disk attached to an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_disk_auto_delete_builder()` + `compute_instances_set_disk_auto_delete_execute()`.
/// For task-level control, use `compute_instances_set_disk_auto_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_disk_auto_delete(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetDiskAutoDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_disk_auto_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.autoDelete,
        &args.deviceName,
        &args.requestId,
    )?;
    compute_instances_set_disk_auto_delete_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_iam_policy_execute()` to send, or `compute_instances_set_iam_policy` for simplest API.

pub fn compute_instances_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setIamPolicy",
        project, zone, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_iam_policy_execute()` or `compute_instances_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_iam_policy_task()`.
/// For the simplest API, use `compute_instances_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_iam_policy_builder()` + `compute_instances_set_iam_policy_execute()`.
/// For task-level control, use `compute_instances_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_instances_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setLabels
/// Sets labels on an instance.  To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_labels_execute()` to send, or `compute_instances_set_labels` for simplest API.

pub fn compute_instances_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setLabels",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setLabels
/// Sets labels on an instance.  To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_labels_execute()` or `compute_instances_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setLabels
/// Sets labels on an instance.  To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_labels_task()`.
/// For the simplest API, use `compute_instances_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setLabels
/// Sets labels on an instance.  To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_labels_builder()` + `compute_instances_set_labels_execute()`.
/// For task-level control, use `compute_instances_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_labels_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_labels_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineResources
/// Changes the number `and/or` type of accelerator for a stopped instance to the values specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_machine_resources_execute()` to send, or `compute_instances_set_machine_resources` for simplest API.

pub fn compute_instances_set_machine_resources_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setMachineResources",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineResources
/// Changes the number `and/or` type of accelerator for a stopped instance to the values specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_machine_resources_execute()` or `compute_instances_set_machine_resources`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_machine_resources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_machine_resources_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineResources
/// Changes the number `and/or` type of accelerator for a stopped instance to the values specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_machine_resources_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_machine_resources_task()`.
/// For the simplest API, use `compute_instances_set_machine_resources()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_machine_resources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_machine_resources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_machine_resources_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_machine_resources`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetMachineResourcesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineResources
/// Changes the number `and/or` type of accelerator for a stopped instance to the values specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_machine_resources_builder()` + `compute_instances_set_machine_resources_execute()`.
/// For task-level control, use `compute_instances_set_machine_resources_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_machine_resources(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetMachineResourcesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_machine_resources_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_machine_resources_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineType
/// Changes the machine type for a stopped instance to the machine type specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_machine_type_execute()` to send, or `compute_instances_set_machine_type` for simplest API.

pub fn compute_instances_set_machine_type_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setMachineType",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineType
/// Changes the machine type for a stopped instance to the machine type specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_machine_type_execute()` or `compute_instances_set_machine_type`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_machine_type_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_machine_type_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineType
/// Changes the machine type for a stopped instance to the machine type specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_machine_type_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_machine_type_task()`.
/// For the simplest API, use `compute_instances_set_machine_type()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_machine_type_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_machine_type_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_machine_type_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_machine_type`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetMachineTypeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineType
/// Changes the machine type for a stopped instance to the machine type specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_machine_type_builder()` + `compute_instances_set_machine_type_execute()`.
/// For task-level control, use `compute_instances_set_machine_type_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_machine_type(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetMachineTypeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_machine_type_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_machine_type_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMetadata
/// Sets metadata for the specified instance to the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_metadata_execute()` to send, or `compute_instances_set_metadata` for simplest API.

pub fn compute_instances_set_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setMetadata",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMetadata
/// Sets metadata for the specified instance to the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_metadata_execute()` or `compute_instances_set_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMetadata
/// Sets metadata for the specified instance to the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_metadata_task()`.
/// For the simplest API, use `compute_instances_set_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetMetadataArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMetadata
/// Sets metadata for the specified instance to the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_metadata_builder()` + `compute_instances_set_metadata_execute()`.
/// For task-level control, use `compute_instances_set_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_metadata(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_metadata_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_metadata_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform
/// Changes the minimum CPU platform that this instance should use. This method can only be called on a stopped instance. For more information, `readSpecifying` a Minimum CPU Platform.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_min_cpu_platform_execute()` to send, or `compute_instances_set_min_cpu_platform` for simplest API.

pub fn compute_instances_set_min_cpu_platform_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setMinCpuPlatform",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform
/// Changes the minimum CPU platform that this instance should use. This method can only be called on a stopped instance. For more information, `readSpecifying` a Minimum CPU Platform.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_min_cpu_platform_execute()` or `compute_instances_set_min_cpu_platform`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_min_cpu_platform_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_min_cpu_platform_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform
/// Changes the minimum CPU platform that this instance should use. This method can only be called on a stopped instance. For more information, `readSpecifying` a Minimum CPU Platform.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_min_cpu_platform_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_min_cpu_platform_task()`.
/// For the simplest API, use `compute_instances_set_min_cpu_platform()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_min_cpu_platform_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_min_cpu_platform_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_min_cpu_platform_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_min_cpu_platform`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetMinCpuPlatformArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform
/// Changes the minimum CPU platform that this instance should use. This method can only be called on a stopped instance. For more information, `readSpecifying` a Minimum CPU Platform.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_min_cpu_platform_builder()` + `compute_instances_set_min_cpu_platform_execute()`.
/// For task-level control, use `compute_instances_set_min_cpu_platform_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_min_cpu_platform(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetMinCpuPlatformArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_min_cpu_platform_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_min_cpu_platform_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setName
/// Sets name of an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_name_execute()` to send, or `compute_instances_set_name` for simplest API.

pub fn compute_instances_set_name_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setName",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setName
/// Sets name of an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_name_execute()` or `compute_instances_set_name`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_name_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_name_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setName
/// Sets name of an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_name_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_name_task()`.
/// For the simplest API, use `compute_instances_set_name()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_name_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_name_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_name_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_name`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetNameArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setName
/// Sets name of an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_name_builder()` + `compute_instances_set_name_execute()`.
/// For task-level control, use `compute_instances_set_name_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_name(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetNameArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_name_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_name_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setScheduling
/// Sets an instance's scheduling options. You can only call this method on astopped instance, that is, a VM instance that is in a TERMINATED state. SeeInstance Life Cycle for more information on the possible instance states. For more information about setting scheduling options for a VM, `seeSet` VM host maintenance policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_scheduling_execute()` to send, or `compute_instances_set_scheduling` for simplest API.

pub fn compute_instances_set_scheduling_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setScheduling",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setScheduling
/// Sets an instance's scheduling options. You can only call this method on astopped instance, that is, a VM instance that is in a TERMINATED state. SeeInstance Life Cycle for more information on the possible instance states. For more information about setting scheduling options for a VM, `seeSet` VM host maintenance policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_scheduling_execute()` or `compute_instances_set_scheduling`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_scheduling_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_scheduling_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setScheduling
/// Sets an instance's scheduling options. You can only call this method on astopped instance, that is, a VM instance that is in a TERMINATED state. SeeInstance Life Cycle for more information on the possible instance states. For more information about setting scheduling options for a VM, `seeSet` VM host maintenance policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_scheduling_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_scheduling_task()`.
/// For the simplest API, use `compute_instances_set_scheduling()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_scheduling_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_scheduling_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_scheduling_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_scheduling`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetSchedulingArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setScheduling
/// Sets an instance's scheduling options. You can only call this method on astopped instance, that is, a VM instance that is in a TERMINATED state. SeeInstance Life Cycle for more information on the possible instance states. For more information about setting scheduling options for a VM, `seeSet` VM host maintenance policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_scheduling_builder()` + `compute_instances_set_scheduling_execute()`.
/// For task-level control, use `compute_instances_set_scheduling_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_scheduling(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetSchedulingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_scheduling_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_scheduling_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_security_policy_execute()` to send, or `compute_instances_set_security_policy` for simplest API.

pub fn compute_instances_set_security_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setSecurityPolicy",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_security_policy_execute()` or `compute_instances_set_security_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_security_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_security_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_security_policy_task()`.
/// For the simplest API, use `compute_instances_set_security_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_security_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_security_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetSecurityPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_security_policy_builder()` + `compute_instances_set_security_policy_execute()`.
/// For task-level control, use `compute_instances_set_security_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_security_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetSecurityPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_security_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_security_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount
/// Sets the service account on the instance. For more information, `readChanging` the service account and access scopes for an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_service_account_execute()` to send, or `compute_instances_set_service_account` for simplest API.

pub fn compute_instances_set_service_account_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setServiceAccount",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount
/// Sets the service account on the instance. For more information, `readChanging` the service account and access scopes for an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_service_account_execute()` or `compute_instances_set_service_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_service_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount
/// Sets the service account on the instance. For more information, `readChanging` the service account and access scopes for an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_service_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_service_account_task()`.
/// For the simplest API, use `compute_instances_set_service_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_service_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_service_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_service_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetServiceAccountArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount
/// Sets the service account on the instance. For more information, `readChanging` the service account and access scopes for an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_service_account_builder()` + `compute_instances_set_service_account_execute()`.
/// For task-level control, use `compute_instances_set_service_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_service_account(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetServiceAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_service_account_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_service_account_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy
/// Sets the Shielded Instance integrity policy for an instance. You can only use this method on a running instance. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_shielded_instance_integrity_policy_execute()` to send, or `compute_instances_set_shielded_instance_integrity_policy` for simplest API.

pub fn compute_instances_set_shielded_instance_integrity_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setShieldedInstanceIntegrityPolicy",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy
/// Sets the Shielded Instance integrity policy for an instance. You can only use this method on a running instance. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_shielded_instance_integrity_policy_execute()` or `compute_instances_set_shielded_instance_integrity_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_shielded_instance_integrity_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_shielded_instance_integrity_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy
/// Sets the Shielded Instance integrity policy for an instance. You can only use this method on a running instance. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_shielded_instance_integrity_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_shielded_instance_integrity_policy_task()`.
/// For the simplest API, use `compute_instances_set_shielded_instance_integrity_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_shielded_instance_integrity_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_shielded_instance_integrity_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_shielded_instance_integrity_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_shielded_instance_integrity_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy
/// Sets the Shielded Instance integrity policy for an instance. You can only use this method on a running instance. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_shielded_instance_integrity_policy_builder()` + `compute_instances_set_shielded_instance_integrity_policy_execute()`.
/// For task-level control, use `compute_instances_set_shielded_instance_integrity_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_shielded_instance_integrity_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_shielded_instance_integrity_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_shielded_instance_integrity_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setTags
/// Sets network tags for the specified instance to the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_tags_execute()` to send, or `compute_instances_set_tags` for simplest API.

pub fn compute_instances_set_tags_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setTags",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setTags
/// Sets network tags for the specified instance to the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_set_tags_execute()` or `compute_instances_set_tags`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_tags_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_tags_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setTags
/// Sets network tags for the specified instance to the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_set_tags_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_set_tags_task()`.
/// For the simplest API, use `compute_instances_set_tags()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_set_tags_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_set_tags_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_set_tags_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_set_tags`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSetTagsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/setTags
/// Sets network tags for the specified instance to the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_tags_builder()` + `compute_instances_set_tags_execute()`.
/// For task-level control, use `compute_instances_set_tags_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_tags(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSetTagsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_tags_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_set_tags_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent
/// Simulates a host maintenance event on a VM. For more information, see Simulate a host maintenance event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_simulate_maintenance_event_execute()` to send, or `compute_instances_simulate_maintenance_event` for simplest API.

pub fn compute_instances_simulate_maintenance_event_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
    withExtendedNotifications: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/simulateMaintenanceEvent",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = withExtendedNotifications.as_ref() {
        query_parts.push(format!("withExtendedNotifications={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent
/// Simulates a host maintenance event on a VM. For more information, see Simulate a host maintenance event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_simulate_maintenance_event_execute()` or `compute_instances_simulate_maintenance_event`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_simulate_maintenance_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_simulate_maintenance_event_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent
/// Simulates a host maintenance event on a VM. For more information, see Simulate a host maintenance event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_simulate_maintenance_event_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_simulate_maintenance_event_task()`.
/// For the simplest API, use `compute_instances_simulate_maintenance_event()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_simulate_maintenance_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_simulate_maintenance_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_simulate_maintenance_event_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_simulate_maintenance_event`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSimulateMaintenanceEventArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: withExtendedNotifications
    pub withExtendedNotifications: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent
/// Simulates a host maintenance event on a VM. For more information, see Simulate a host maintenance event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_simulate_maintenance_event_builder()` + `compute_instances_simulate_maintenance_event_execute()`.
/// For task-level control, use `compute_instances_simulate_maintenance_event_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_simulate_maintenance_event(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSimulateMaintenanceEventArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_simulate_maintenance_event_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
        &args.withExtendedNotifications,
    )?;
    compute_instances_simulate_maintenance_event_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/start
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_start_execute()` to send, or `compute_instances_start` for simplest API.

pub fn compute_instances_start_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/start",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/start
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_start_execute()` or `compute_instances_start`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_start_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/start
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_start_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_start_task()`.
/// For the simplest API, use `compute_instances_start()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_start_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_start`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesStartArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/start
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_start_builder()` + `compute_instances_start_execute()`.
/// For task-level control, use `compute_instances_start_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_start(
    client: &SimpleHttpClient,
    args: &ComputeInstancesStartArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_start_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_start_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_start_with_encryption_key_execute()` to send, or `compute_instances_start_with_encryption_key` for simplest API.

pub fn compute_instances_start_with_encryption_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/startWithEncryptionKey",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_start_with_encryption_key_execute()` or `compute_instances_start_with_encryption_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_start_with_encryption_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_start_with_encryption_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_start_with_encryption_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_start_with_encryption_key_task()`.
/// For the simplest API, use `compute_instances_start_with_encryption_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_start_with_encryption_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_start_with_encryption_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_start_with_encryption_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_start_with_encryption_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesStartWithEncryptionKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_start_with_encryption_key_builder()` + `compute_instances_start_with_encryption_key_execute()`.
/// For task-level control, use `compute_instances_start_with_encryption_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_start_with_encryption_key(
    client: &SimpleHttpClient,
    args: &ComputeInstancesStartWithEncryptionKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_start_with_encryption_key_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_start_with_encryption_key_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/stop
/// Stops a running instance, shutting it down cleanly, and allows you to restart the instance at a later time. Stopped instances do not incur VM usage charges while they are stopped. However, resources that the VM is using, such as persistent disks and static IP addresses, will continue to be charged until they are deleted. For more information, `seeStopping` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_stop_execute()` to send, or `compute_instances_stop` for simplest API.

pub fn compute_instances_stop_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    discardLocalSsd: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/stop",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = discardLocalSsd.as_ref() {
        query_parts.push(format!("discardLocalSsd={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/stop
/// Stops a running instance, shutting it down cleanly, and allows you to restart the instance at a later time. Stopped instances do not incur VM usage charges while they are stopped. However, resources that the VM is using, such as persistent disks and static IP addresses, will continue to be charged until they are deleted. For more information, `seeStopping` an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_stop_execute()` or `compute_instances_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/stop
/// Stops a running instance, shutting it down cleanly, and allows you to restart the instance at a later time. Stopped instances do not incur VM usage charges while they are stopped. However, resources that the VM is using, such as persistent disks and static IP addresses, will continue to be charged until they are deleted. For more information, `seeStopping` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_stop_task()`.
/// For the simplest API, use `compute_instances_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesStopArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: discardLocalSsd
    pub discardLocalSsd: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/stop
/// Stops a running instance, shutting it down cleanly, and allows you to restart the instance at a later time. Stopped instances do not incur VM usage charges while they are stopped. However, resources that the VM is using, such as persistent disks and static IP addresses, will continue to be charged until they are deleted. For more information, `seeStopping` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_stop_builder()` + `compute_instances_stop_execute()`.
/// For task-level control, use `compute_instances_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_stop(
    client: &SimpleHttpClient,
    args: &ComputeInstancesStopArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_stop_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.discardLocalSsd,
        &args.requestId,
    )?;
    compute_instances_stop_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/suspend
/// This method suspends a running instance, saving its state to persistent storage, and allows you to resume the instance at a later time. Suspended instances have no compute costs (cores or RAM), and incur only storage charges for the saved VM memory and `localSSD` data. Any charged resources the virtual machine was using, such as persistent disks and static IP addresses, will continue to be charged while the instance is suspended. For more information, see Suspending and resuming an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_suspend_execute()` to send, or `compute_instances_suspend` for simplest API.

pub fn compute_instances_suspend_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    discardLocalSsd: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/suspend",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = discardLocalSsd.as_ref() {
        query_parts.push(format!("discardLocalSsd={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/suspend
/// This method suspends a running instance, saving its state to persistent storage, and allows you to resume the instance at a later time. Suspended instances have no compute costs (cores or RAM), and incur only storage charges for the saved VM memory and `localSSD` data. Any charged resources the virtual machine was using, such as persistent disks and static IP addresses, will continue to be charged while the instance is suspended. For more information, see Suspending and resuming an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_suspend_execute()` or `compute_instances_suspend`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_suspend_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_suspend_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/suspend
/// This method suspends a running instance, saving its state to persistent storage, and allows you to resume the instance at a later time. Suspended instances have no compute costs (cores or RAM), and incur only storage charges for the saved VM memory and `localSSD` data. Any charged resources the virtual machine was using, such as persistent disks and static IP addresses, will continue to be charged while the instance is suspended. For more information, see Suspending and resuming an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_suspend_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_suspend_task()`.
/// For the simplest API, use `compute_instances_suspend()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_suspend_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_suspend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_suspend_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_suspend`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesSuspendArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: discardLocalSsd
    pub discardLocalSsd: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/suspend
/// This method suspends a running instance, saving its state to persistent storage, and allows you to resume the instance at a later time. Suspended instances have no compute costs (cores or RAM), and incur only storage charges for the saved VM memory and `localSSD` data. Any charged resources the virtual machine was using, such as persistent disks and static IP addresses, will continue to be charged while the instance is suspended. For more information, see Suspending and resuming an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_suspend_builder()` + `compute_instances_suspend_execute()`.
/// For task-level control, use `compute_instances_suspend_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_suspend(
    client: &SimpleHttpClient,
    args: &ComputeInstancesSuspendArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_suspend_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.discardLocalSsd,
        &args.requestId,
    )?;
    compute_instances_suspend_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_test_iam_permissions_execute()` to send, or `compute_instances_test_iam_permissions` for simplest API.

pub fn compute_instances_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_test_iam_permissions_execute()` or `compute_instances_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_test_iam_permissions_task()`.
/// For the simplest API, use `compute_instances_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instances_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_test_iam_permissions_builder()` + `compute_instances_test_iam_permissions_execute()`.
/// For task-level control, use `compute_instances_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeInstancesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_instances_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/zones/{zone}/instances/{instance}
/// Updates an instance only if the necessary resources are available. This method can update only a specific set of instance properties. See Updating a running instance for a list of updatable instance properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_execute()` to send, or `compute_instances_update` for simplest API.

pub fn compute_instances_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    discardLocalSsd: &Option<Option<String>>,
    minimalAction: &Option<Option<String>>,
    mostDisruptiveAllowedAction: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}",
        project, zone, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = discardLocalSsd.as_ref() {
        query_parts.push(format!("discardLocalSsd={}", val));
    }
    if let Some(val) = minimalAction.as_ref() {
        query_parts.push(format!("minimalAction={}", val));
    }
    if let Some(val) = mostDisruptiveAllowedAction.as_ref() {
        query_parts.push(format!("mostDisruptiveAllowedAction={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/zones/{zone}/instances/{instance}
/// Updates an instance only if the necessary resources are available. This method can update only a specific set of instance properties. See Updating a running instance for a list of updatable instance properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_update_execute()` or `compute_instances_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/zones/{zone}/instances/{instance}
/// Updates an instance only if the necessary resources are available. This method can update only a specific set of instance properties. See Updating a running instance for a list of updatable instance properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_update_task()`.
/// For the simplest API, use `compute_instances_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: discardLocalSsd
    pub discardLocalSsd: Option<Option<String>>,
    /// Query parameter: minimalAction
    pub minimalAction: Option<Option<String>>,
    /// Query parameter: mostDisruptiveAllowedAction
    pub mostDisruptiveAllowedAction: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/zones/{zone}/instances/{instance}
/// Updates an instance only if the necessary resources are available. This method can update only a specific set of instance properties. See Updating a running instance for a list of updatable instance properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_builder()` + `compute_instances_update_execute()`.
/// For task-level control, use `compute_instances_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update(
    client: &SimpleHttpClient,
    args: &ComputeInstancesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.discardLocalSsd,
        &args.minimalAction,
        &args.mostDisruptiveAllowedAction,
        &args.requestId,
    )?;
    compute_instances_update_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig
/// Updates the specified access config from an instance's network interface with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_access_config_execute()` to send, or `compute_instances_update_access_config` for simplest API.

pub fn compute_instances_update_access_config_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    networkInterface: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/updateAccessConfig",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = networkInterface.as_ref() {
        query_parts.push(format!("networkInterface={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig
/// Updates the specified access config from an instance's network interface with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_update_access_config_execute()` or `compute_instances_update_access_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_access_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig
/// Updates the specified access config from an instance's network interface with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_update_access_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_update_access_config_task()`.
/// For the simplest API, use `compute_instances_update_access_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_update_access_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_update_access_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_update_access_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateAccessConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: networkInterface
    pub networkInterface: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig
/// Updates the specified access config from an instance's network interface with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_access_config_builder()` + `compute_instances_update_access_config_execute()`.
/// For task-level control, use `compute_instances_update_access_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_access_config(
    client: &SimpleHttpClient,
    args: &ComputeInstancesUpdateAccessConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_access_config_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.networkInterface,
        &args.requestId,
    )?;
    compute_instances_update_access_config_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice
/// Updates the Display config for a VM instance. You can only use this method on a stopped VM instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_display_device_execute()` to send, or `compute_instances_update_display_device` for simplest API.

pub fn compute_instances_update_display_device_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/updateDisplayDevice",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice
/// Updates the Display config for a VM instance. You can only use this method on a stopped VM instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_update_display_device_execute()` or `compute_instances_update_display_device`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_display_device_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_display_device_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice
/// Updates the Display config for a VM instance. You can only use this method on a stopped VM instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_update_display_device_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_update_display_device_task()`.
/// For the simplest API, use `compute_instances_update_display_device()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_display_device_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_update_display_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_update_display_device_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_update_display_device`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateDisplayDeviceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice
/// Updates the Display config for a VM instance. You can only use this method on a stopped VM instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_display_device_builder()` + `compute_instances_update_display_device_execute()`.
/// For task-level control, use `compute_instances_update_display_device_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_display_device(
    client: &SimpleHttpClient,
    args: &ComputeInstancesUpdateDisplayDeviceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_display_device_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_update_display_device_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface
/// Updates an instance's network interface. This method can only update an interface's alias IP range and attached network. See Modifying alias IP ranges for an existing instance for instructions on changing alias IP ranges. See Migrating a VM between networks for instructions on migrating an interface. This method follows PATCH semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_network_interface_execute()` to send, or `compute_instances_update_network_interface` for simplest API.

pub fn compute_instances_update_network_interface_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    networkInterface: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/updateNetworkInterface",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = networkInterface.as_ref() {
        query_parts.push(format!("networkInterface={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface
/// Updates an instance's network interface. This method can only update an interface's alias IP range and attached network. See Modifying alias IP ranges for an existing instance for instructions on changing alias IP ranges. See Migrating a VM between networks for instructions on migrating an interface. This method follows PATCH semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_update_network_interface_execute()` or `compute_instances_update_network_interface`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_network_interface_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_network_interface_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface
/// Updates an instance's network interface. This method can only update an interface's alias IP range and attached network. See Modifying alias IP ranges for an existing instance for instructions on changing alias IP ranges. See Migrating a VM between networks for instructions on migrating an interface. This method follows PATCH semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_update_network_interface_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_update_network_interface_task()`.
/// For the simplest API, use `compute_instances_update_network_interface()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_network_interface_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_update_network_interface_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_update_network_interface_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_update_network_interface`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateNetworkInterfaceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: networkInterface
    pub networkInterface: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface
/// Updates an instance's network interface. This method can only update an interface's alias IP range and attached network. See Modifying alias IP ranges for an existing instance for instructions on changing alias IP ranges. See Migrating a VM between networks for instructions on migrating an interface. This method follows PATCH semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_network_interface_builder()` + `compute_instances_update_network_interface_execute()`.
/// For task-level control, use `compute_instances_update_network_interface_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_network_interface(
    client: &SimpleHttpClient,
    args: &ComputeInstancesUpdateNetworkInterfaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_network_interface_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.networkInterface,
        &args.requestId,
    )?;
    compute_instances_update_network_interface_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig
/// Updates the Shielded Instance config for an instance. You can only use this method on a stopped instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_shielded_instance_config_execute()` to send, or `compute_instances_update_shielded_instance_config` for simplest API.

pub fn compute_instances_update_shielded_instance_config_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/updateShieldedInstanceConfig",
        project,
        zone,
        instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig
/// Updates the Shielded Instance config for an instance. You can only use this method on a stopped instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instances_update_shielded_instance_config_execute()` or `compute_instances_update_shielded_instance_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_shielded_instance_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_shielded_instance_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig
/// Updates the Shielded Instance config for an instance. You can only use this method on a stopped instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instances_update_shielded_instance_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instances_update_shielded_instance_config_task()`.
/// For the simplest API, use `compute_instances_update_shielded_instance_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instances_update_shielded_instance_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instances_update_shielded_instance_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instances_update_shielded_instance_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instances_update_shielded_instance_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateShieldedInstanceConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig
/// Updates the Shielded Instance config for an instance. You can only use this method on a stopped instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_shielded_instance_config_builder()` + `compute_instances_update_shielded_instance_config_execute()`.
/// For task-level control, use `compute_instances_update_shielded_instance_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_shielded_instance_config(
    client: &SimpleHttpClient,
    args: &ComputeInstancesUpdateShieldedInstanceConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_shielded_instance_config_builder(
        client,
        &args.project,
        &args.zone,
        &args.instance,
        &args.requestId,
    )?;
    compute_instances_update_shielded_instance_config_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
/// deletes a Zonal InstantSnapshotGroup resource
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshot_groups_delete_execute()` to send, or `compute_instant_snapshot_groups_delete` for simplest API.

pub fn compute_instant_snapshot_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instantSnapshotGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}",
        project, zone, instantSnapshotGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
/// deletes a Zonal InstantSnapshotGroup resource
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshot_groups_delete_execute()` or `compute_instant_snapshot_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
/// deletes a Zonal InstantSnapshotGroup resource
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshot_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshot_groups_delete_task()`.
/// For the simplest API, use `compute_instant_snapshot_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshot_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshot_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshot_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instantSnapshotGroup
    pub instantSnapshotGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
/// deletes a Zonal InstantSnapshotGroup resource
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshot_groups_delete_builder()` + `compute_instant_snapshot_groups_delete_execute()`.
/// For task-level control, use `compute_instant_snapshot_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshot_groups_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.instantSnapshotGroup,
        &args.requestId,
    )?;
    compute_instant_snapshot_groups_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
/// returns the specified InstantSnapshotGroup resource in the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshot_groups_get_execute()` to send, or `compute_instant_snapshot_groups_get` for simplest API.

pub fn compute_instant_snapshot_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instantSnapshotGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}",
        project, zone, instantSnapshotGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
/// returns the specified InstantSnapshotGroup resource in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshot_groups_get_execute()` or `compute_instant_snapshot_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshotGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
/// returns the specified InstantSnapshotGroup resource in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshot_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshot_groups_get_task()`.
/// For the simplest API, use `compute_instant_snapshot_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshot_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instant_snapshot_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshot_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instantSnapshotGroup
    pub instantSnapshotGroup: String,
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
/// returns the specified InstantSnapshotGroup resource in the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshot_groups_get_builder()` + `compute_instant_snapshot_groups_get_execute()`.
/// For task-level control, use `compute_instant_snapshot_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshot_groups_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.instantSnapshotGroup,
    )?;
    compute_instant_snapshot_groups_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshot_groups_get_iam_policy_execute()` to send, or `compute_instant_snapshot_groups_get_iam_policy` for simplest API.

pub fn compute_instant_snapshot_groups_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}/getIamPolicy",
        project,
        zone,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshot_groups_get_iam_policy_execute()` or `compute_instant_snapshot_groups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshot_groups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshot_groups_get_iam_policy_task()`.
/// For the simplest API, use `compute_instant_snapshot_groups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshot_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshot_groups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshot_groups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshot_groups_get_iam_policy_builder()` + `compute_instant_snapshot_groups_get_iam_policy_execute()`.
/// For task-level control, use `compute_instant_snapshot_groups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotGroupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshot_groups_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_instant_snapshot_groups_get_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups
/// inserts a Zonal InstantSnapshotGroup resource
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshot_groups_insert_execute()` to send, or `compute_instant_snapshot_groups_insert` for simplest API.

pub fn compute_instant_snapshot_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
    sourceConsistencyGroup: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceConsistencyGroup.as_ref() {
        query_parts.push(format!("sourceConsistencyGroup={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups
/// inserts a Zonal InstantSnapshotGroup resource
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshot_groups_insert_execute()` or `compute_instant_snapshot_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups
/// inserts a Zonal InstantSnapshotGroup resource
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshot_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshot_groups_insert_task()`.
/// For the simplest API, use `compute_instant_snapshot_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshot_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshot_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshot_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceConsistencyGroup
    pub sourceConsistencyGroup: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups
/// inserts a Zonal InstantSnapshotGroup resource
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshot_groups_insert_builder()` + `compute_instant_snapshot_groups_insert_execute()`.
/// For task-level control, use `compute_instant_snapshot_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshot_groups_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
        &args.sourceConsistencyGroup,
    )?;
    compute_instant_snapshot_groups_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups
/// retrieves the list of InstantSnapshotGroup resources contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshot_groups_list_execute()` to send, or `compute_instant_snapshot_groups_list` for simplest API.

pub fn compute_instant_snapshot_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups
/// retrieves the list of InstantSnapshotGroup resources contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshot_groups_list_execute()` or `compute_instant_snapshot_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInstantSnapshotGroups>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInstantSnapshotGroups = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups
/// retrieves the list of InstantSnapshotGroup resources contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshot_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshot_groups_list_task()`.
/// For the simplest API, use `compute_instant_snapshot_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshot_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstantSnapshotGroups>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instant_snapshot_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshot_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups
/// retrieves the list of InstantSnapshotGroup resources contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshot_groups_list_builder()` + `compute_instant_snapshot_groups_list_execute()`.
/// For task-level control, use `compute_instant_snapshot_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstantSnapshotGroups>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshot_groups_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instant_snapshot_groups_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshot_groups_set_iam_policy_execute()` to send, or `compute_instant_snapshot_groups_set_iam_policy` for simplest API.

pub fn compute_instant_snapshot_groups_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}/setIamPolicy",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshot_groups_set_iam_policy_execute()` or `compute_instant_snapshot_groups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshot_groups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshot_groups_set_iam_policy_task()`.
/// For the simplest API, use `compute_instant_snapshot_groups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshot_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshot_groups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshot_groups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshot_groups_set_iam_policy_builder()` + `compute_instant_snapshot_groups_set_iam_policy_execute()`.
/// For task-level control, use `compute_instant_snapshot_groups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotGroupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshot_groups_set_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_instant_snapshot_groups_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshot_groups_test_iam_permissions_execute()` to send, or `compute_instant_snapshot_groups_test_iam_permissions` for simplest API.

pub fn compute_instant_snapshot_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshot_groups_test_iam_permissions_execute()` or `compute_instant_snapshot_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshot_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshot_groups_test_iam_permissions_task()`.
/// For the simplest API, use `compute_instant_snapshot_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshot_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshot_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instant_snapshot_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshot_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshot_groups_test_iam_permissions_builder()` + `compute_instant_snapshot_groups_test_iam_permissions_execute()`.
/// For task-level control, use `compute_instant_snapshot_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshot_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshot_groups_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_instant_snapshot_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/instantSnapshots
/// Retrieves an aggregated list of `instantSnapshots`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_aggregated_list_execute()` to send, or `compute_instant_snapshots_aggregated_list` for simplest API.

pub fn compute_instant_snapshots_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instantSnapshots",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/instantSnapshots
/// Retrieves an aggregated list of `instantSnapshots`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_aggregated_list_execute()` or `compute_instant_snapshots_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshotAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/instantSnapshots
/// Retrieves an aggregated list of `instantSnapshots`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_aggregated_list_task()`.
/// For the simplest API, use `compute_instant_snapshots_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstantSnapshotAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/instantSnapshots
/// Retrieves an aggregated list of `instantSnapshots`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_aggregated_list_builder()` + `compute_instant_snapshots_aggregated_list_execute()`.
/// For task-level control, use `compute_instant_snapshots_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstantSnapshotAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_instant_snapshots_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_delete_execute()` to send, or `compute_instant_snapshots_delete` for simplest API.

pub fn compute_instant_snapshots_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instantSnapshot: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}",
        project, zone, instantSnapshot,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_delete_execute()` or `compute_instant_snapshots_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_delete_task()`.
/// For the simplest API, use `compute_instant_snapshots_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instantSnapshot
    pub instantSnapshot: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_delete_builder()` + `compute_instant_snapshots_delete_execute()`.
/// For task-level control, use `compute_instant_snapshots_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_delete(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.instantSnapshot,
        &args.requestId,
    )?;
    compute_instant_snapshots_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_get_execute()` to send, or `compute_instant_snapshots_get` for simplest API.

pub fn compute_instant_snapshots_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    instantSnapshot: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}",
        project, zone, instantSnapshot,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_get_execute()` or `compute_instant_snapshots_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_get_task()`.
/// For the simplest API, use `compute_instant_snapshots_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: instantSnapshot
    pub instantSnapshot: String,
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_get_builder()` + `compute_instant_snapshots_get_execute()`.
/// For task-level control, use `compute_instant_snapshots_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_get(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.instantSnapshot,
    )?;
    compute_instant_snapshots_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_get_iam_policy_execute()` to send, or `compute_instant_snapshots_get_iam_policy` for simplest API.

pub fn compute_instant_snapshots_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}/getIamPolicy",
        project,
        zone,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_get_iam_policy_execute()` or `compute_instant_snapshots_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_get_iam_policy_task()`.
/// For the simplest API, use `compute_instant_snapshots_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_get_iam_policy_builder()` + `compute_instant_snapshots_get_iam_policy_execute()`.
/// For task-level control, use `compute_instant_snapshots_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_instant_snapshots_get_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshots
/// Creates an instant snapshot in the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_insert_execute()` to send, or `compute_instant_snapshots_insert` for simplest API.

pub fn compute_instant_snapshots_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshots
/// Creates an instant snapshot in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_insert_execute()` or `compute_instant_snapshots_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instantSnapshots
/// Creates an instant snapshot in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_insert_task()`.
/// For the simplest API, use `compute_instant_snapshots_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instantSnapshots
/// Creates an instant snapshot in the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_insert_builder()` + `compute_instant_snapshots_insert_execute()`.
/// For task-level control, use `compute_instant_snapshots_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_insert(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
    )?;
    compute_instant_snapshots_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_list_execute()` to send, or `compute_instant_snapshots_list` for simplest API.

pub fn compute_instant_snapshots_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_list_execute()` or `compute_instant_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshotList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_list_task()`.
/// For the simplest API, use `compute_instant_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_list_builder()` + `compute_instant_snapshots_list_execute()`.
/// For task-level control, use `compute_instant_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_list(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_instant_snapshots_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_set_iam_policy_execute()` to send, or `compute_instant_snapshots_set_iam_policy` for simplest API.

pub fn compute_instant_snapshots_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}/setIamPolicy",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_set_iam_policy_execute()` or `compute_instant_snapshots_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_set_iam_policy_task()`.
/// For the simplest API, use `compute_instant_snapshots_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_set_iam_policy_builder()` + `compute_instant_snapshots_set_iam_policy_execute()`.
/// For task-level control, use `compute_instant_snapshots_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_set_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_instant_snapshots_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given zone. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_set_labels_execute()` to send, or `compute_instant_snapshots_set_labels` for simplest API.

pub fn compute_instant_snapshots_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}/setLabels",
        project,
        zone,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given zone. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_set_labels_execute()` or `compute_instant_snapshots_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given zone. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_set_labels_task()`.
/// For the simplest API, use `compute_instant_snapshots_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given zone. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_set_labels_builder()` + `compute_instant_snapshots_set_labels_execute()`.
/// For task-level control, use `compute_instant_snapshots_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_set_labels_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.requestId,
    )?;
    compute_instant_snapshots_set_labels_execute(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_test_iam_permissions_execute()` to send, or `compute_instant_snapshots_test_iam_permissions` for simplest API.

pub fn compute_instant_snapshots_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_instant_snapshots_test_iam_permissions_execute()` or `compute_instant_snapshots_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_instant_snapshots_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_instant_snapshots_test_iam_permissions_task()`.
/// For the simplest API, use `compute_instant_snapshots_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_instant_snapshots_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_instant_snapshots_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_instant_snapshots_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_instant_snapshots_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_test_iam_permissions_builder()` + `compute_instant_snapshots_test_iam_permissions_execute()`.
/// For task-level control, use `compute_instant_snapshots_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeInstantSnapshotsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_instant_snapshots_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Deletes the specified InterconnectAttachmentGroup in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_delete_execute()` to send, or `compute_interconnect_attachment_groups_delete` for simplest API.

pub fn compute_interconnect_attachment_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectAttachmentGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}",
        project,
        interconnectAttachmentGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Deletes the specified InterconnectAttachmentGroup in the given scope
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_delete_execute()` or `compute_interconnect_attachment_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Deletes the specified InterconnectAttachmentGroup in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_delete_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectAttachmentGroup
    pub interconnectAttachmentGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Deletes the specified InterconnectAttachmentGroup in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_delete_builder()` + `compute_interconnect_attachment_groups_delete_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_delete_builder(
        client,
        &args.project,
        &args.interconnectAttachmentGroup,
        &args.requestId,
    )?;
    compute_interconnect_attachment_groups_delete_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Returns the specified InterconnectAttachmentGroup resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_get_execute()` to send, or `compute_interconnect_attachment_groups_get` for simplest API.

pub fn compute_interconnect_attachment_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectAttachmentGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}",
        project,
        interconnectAttachmentGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Returns the specified InterconnectAttachmentGroup resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_get_execute()` or `compute_interconnect_attachment_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectAttachmentGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Returns the specified InterconnectAttachmentGroup resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_get_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectAttachmentGroup
    pub interconnectAttachmentGroup: String,
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Returns the specified InterconnectAttachmentGroup resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_get_builder()` + `compute_interconnect_attachment_groups_get_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_get_builder(
        client,
        &args.project,
        &args.interconnectAttachmentGroup,
    )?;
    compute_interconnect_attachment_groups_get_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_get_iam_policy_execute()` to send, or `compute_interconnect_attachment_groups_get_iam_policy` for simplest API.

pub fn compute_interconnect_attachment_groups_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}/getIamPolicy",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_get_iam_policy_execute()` or `compute_interconnect_attachment_groups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_get_iam_policy_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_get_iam_policy_builder()` + `compute_interconnect_attachment_groups_get_iam_policy_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_interconnect_attachment_groups_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus
/// Returns the InterconnectAttachmentStatuses for the specified InterconnectAttachmentGroup resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_get_operational_status_execute()` to send, or `compute_interconnect_attachment_groups_get_operational_status` for simplest API.

pub fn compute_interconnect_attachment_groups_get_operational_status_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectAttachmentGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}/getOperationalStatus",
        project,
        interconnectAttachmentGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus
/// Returns the InterconnectAttachmentStatuses for the specified InterconnectAttachmentGroup resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_get_operational_status_execute()` or `compute_interconnect_attachment_groups_get_operational_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_get_operational_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_operational_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InterconnectAttachmentGroupsGetOperationalStatusResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroupsGetOperationalStatusResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus
/// Returns the InterconnectAttachmentStatuses for the specified InterconnectAttachmentGroup resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_get_operational_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_get_operational_status_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_get_operational_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_get_operational_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_get_operational_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<InterconnectAttachmentGroupsGetOperationalStatusResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_get_operational_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_get_operational_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectAttachmentGroup
    pub interconnectAttachmentGroup: String,
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus
/// Returns the InterconnectAttachmentStatuses for the specified InterconnectAttachmentGroup resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_get_operational_status_builder()` + `compute_interconnect_attachment_groups_get_operational_status_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_get_operational_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_operational_status(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<InterconnectAttachmentGroupsGetOperationalStatusResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_get_operational_status_builder(
        client,
        &args.project,
        &args.interconnectAttachmentGroup,
    )?;
    compute_interconnect_attachment_groups_get_operational_status_execute(builder)
}

/// POST projects/{project}/global/interconnectAttachmentGroups
/// Creates a InterconnectAttachmentGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_insert_execute()` to send, or `compute_interconnect_attachment_groups_insert` for simplest API.

pub fn compute_interconnect_attachment_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnectAttachmentGroups
/// Creates a InterconnectAttachmentGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_insert_execute()` or `compute_interconnect_attachment_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnectAttachmentGroups
/// Creates a InterconnectAttachmentGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_insert_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/interconnectAttachmentGroups
/// Creates a InterconnectAttachmentGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_insert_builder()` + `compute_interconnect_attachment_groups_insert_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_insert_builder(
        client,
        &args.project,
        &args.requestId,
    )?;
    compute_interconnect_attachment_groups_insert_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Lists the InterconnectAttachmentGroups for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_list_execute()` to send, or `compute_interconnect_attachment_groups_list` for simplest API.

pub fn compute_interconnect_attachment_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Lists the InterconnectAttachmentGroups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_list_execute()` or `compute_interconnect_attachment_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectAttachmentGroupsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Lists the InterconnectAttachmentGroups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_list_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Lists the InterconnectAttachmentGroups for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_list_builder()` + `compute_interconnect_attachment_groups_list_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_interconnect_attachment_groups_list_execute(builder)
}

/// PATCH projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Patches the specified InterconnectAttachmentGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_patch_execute()` to send, or `compute_interconnect_attachment_groups_patch` for simplest API.

pub fn compute_interconnect_attachment_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectAttachmentGroup: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}",
        project,
        interconnectAttachmentGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Patches the specified InterconnectAttachmentGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_patch_execute()` or `compute_interconnect_attachment_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Patches the specified InterconnectAttachmentGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_patch_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectAttachmentGroup
    pub interconnectAttachmentGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Patches the specified InterconnectAttachmentGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_patch_builder()` + `compute_interconnect_attachment_groups_patch_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_patch(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_patch_builder(
        client,
        &args.project,
        &args.interconnectAttachmentGroup,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_interconnect_attachment_groups_patch_execute(builder)
}

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_set_iam_policy_execute()` to send, or `compute_interconnect_attachment_groups_set_iam_policy` for simplest API.

pub fn compute_interconnect_attachment_groups_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}/setIamPolicy",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_set_iam_policy_execute()` or `compute_interconnect_attachment_groups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_set_iam_policy_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_set_iam_policy_builder()` + `compute_interconnect_attachment_groups_set_iam_policy_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_set_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_interconnect_attachment_groups_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_test_iam_permissions_execute()` to send, or `compute_interconnect_attachment_groups_test_iam_permissions` for simplest API.

pub fn compute_interconnect_attachment_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachment_groups_test_iam_permissions_execute()` or `compute_interconnect_attachment_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachment_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachment_groups_test_iam_permissions_task()`.
/// For the simplest API, use `compute_interconnect_attachment_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachment_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachment_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachment_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachment_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_test_iam_permissions_builder()` + `compute_interconnect_attachment_groups_test_iam_permissions_execute()`.
/// For task-level control, use `compute_interconnect_attachment_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_interconnect_attachment_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/interconnectAttachments
/// Retrieves an aggregated list of interconnect attachments.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_aggregated_list_execute()` to send, or `compute_interconnect_attachments_aggregated_list` for simplest API.

pub fn compute_interconnect_attachments_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/interconnectAttachments",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/interconnectAttachments
/// Retrieves an aggregated list of interconnect attachments.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachments_aggregated_list_execute()` or `compute_interconnect_attachments_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectAttachmentAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/interconnectAttachments
/// Retrieves an aggregated list of interconnect attachments.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachments_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachments_aggregated_list_task()`.
/// For the simplest API, use `compute_interconnect_attachments_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachments_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachments_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachments_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/interconnectAttachments
/// Retrieves an aggregated list of interconnect attachments.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_aggregated_list_builder()` + `compute_interconnect_attachments_aggregated_list_execute()`.
/// For task-level control, use `compute_interconnect_attachments_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_interconnect_attachments_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Deletes the specified interconnect attachment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_delete_execute()` to send, or `compute_interconnect_attachments_delete` for simplest API.

pub fn compute_interconnect_attachments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    interconnectAttachment: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments/{}",
        project,
        region,
        interconnectAttachment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Deletes the specified interconnect attachment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachments_delete_execute()` or `compute_interconnect_attachments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Deletes the specified interconnect attachment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachments_delete_task()`.
/// For the simplest API, use `compute_interconnect_attachments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: interconnectAttachment
    pub interconnectAttachment: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Deletes the specified interconnect attachment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_delete_builder()` + `compute_interconnect_attachments_delete_execute()`.
/// For task-level control, use `compute_interconnect_attachments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_delete(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.interconnectAttachment,
        &args.requestId,
    )?;
    compute_interconnect_attachments_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Returns the specified interconnect attachment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_get_execute()` to send, or `compute_interconnect_attachments_get` for simplest API.

pub fn compute_interconnect_attachments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    interconnectAttachment: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments/{}",
        project,
        region,
        interconnectAttachment,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Returns the specified interconnect attachment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachments_get_execute()` or `compute_interconnect_attachments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectAttachment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Returns the specified interconnect attachment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachments_get_task()`.
/// For the simplest API, use `compute_interconnect_attachments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: interconnectAttachment
    pub interconnectAttachment: String,
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Returns the specified interconnect attachment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_get_builder()` + `compute_interconnect_attachments_get_execute()`.
/// For task-level control, use `compute_interconnect_attachments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_get(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_get_builder(
        client,
        &args.project,
        &args.region,
        &args.interconnectAttachment,
    )?;
    compute_interconnect_attachments_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/interconnectAttachments
/// Creates an InterconnectAttachment in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_insert_execute()` to send, or `compute_interconnect_attachments_insert` for simplest API.

pub fn compute_interconnect_attachments_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/interconnectAttachments
/// Creates an InterconnectAttachment in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachments_insert_execute()` or `compute_interconnect_attachments_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/interconnectAttachments
/// Creates an InterconnectAttachment in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachments_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachments_insert_task()`.
/// For the simplest API, use `compute_interconnect_attachments_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachments_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachments_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/interconnectAttachments
/// Creates an InterconnectAttachment in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_insert_builder()` + `compute_interconnect_attachments_insert_execute()`.
/// For task-level control, use `compute_interconnect_attachments_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_insert(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
        &args.validateOnly,
    )?;
    compute_interconnect_attachments_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Retrieves the list of interconnect attachments contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_list_execute()` to send, or `compute_interconnect_attachments_list` for simplest API.

pub fn compute_interconnect_attachments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Retrieves the list of interconnect attachments contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachments_list_execute()` or `compute_interconnect_attachments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectAttachmentList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Retrieves the list of interconnect attachments contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachments_list_task()`.
/// For the simplest API, use `compute_interconnect_attachments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Retrieves the list of interconnect attachments contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_list_builder()` + `compute_interconnect_attachments_list_execute()`.
/// For task-level control, use `compute_interconnect_attachments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_list(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_interconnect_attachments_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Updates the specified interconnect attachment with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_patch_execute()` to send, or `compute_interconnect_attachments_patch` for simplest API.

pub fn compute_interconnect_attachments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    interconnectAttachment: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments/{}",
        project,
        region,
        interconnectAttachment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Updates the specified interconnect attachment with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachments_patch_execute()` or `compute_interconnect_attachments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Updates the specified interconnect attachment with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachments_patch_task()`.
/// For the simplest API, use `compute_interconnect_attachments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: interconnectAttachment
    pub interconnectAttachment: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Updates the specified interconnect attachment with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_patch_builder()` + `compute_interconnect_attachments_patch_execute()`.
/// For task-level control, use `compute_interconnect_attachments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_patch(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.interconnectAttachment,
        &args.requestId,
    )?;
    compute_interconnect_attachments_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels
/// Sets the labels on an InterconnectAttachment. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_set_labels_execute()` to send, or `compute_interconnect_attachments_set_labels` for simplest API.

pub fn compute_interconnect_attachments_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments/{}/setLabels",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels
/// Sets the labels on an InterconnectAttachment. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_attachments_set_labels_execute()` or `compute_interconnect_attachments_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels
/// Sets the labels on an InterconnectAttachment. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_attachments_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_attachments_set_labels_task()`.
/// For the simplest API, use `compute_interconnect_attachments_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_attachments_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_attachments_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_attachments_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_attachments_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels
/// Sets the labels on an InterconnectAttachment. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_set_labels_builder()` + `compute_interconnect_attachments_set_labels_execute()`.
/// For task-level control, use `compute_interconnect_attachments_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectAttachmentsSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_interconnect_attachments_set_labels_execute(builder)
}

/// POST projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers
/// Create Interconnects with redundancy by creating them in a specified interconnect group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_create_members_execute()` to send, or `compute_interconnect_groups_create_members` for simplest API.

pub fn compute_interconnect_groups_create_members_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/createMembers",
        project,
        interconnectGroup,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers
/// Create Interconnects with redundancy by creating them in a specified interconnect group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_create_members_execute()` or `compute_interconnect_groups_create_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_create_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_create_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers
/// Create Interconnects with redundancy by creating them in a specified interconnect group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_create_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_create_members_task()`.
/// For the simplest API, use `compute_interconnect_groups_create_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_create_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_create_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_create_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_create_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsCreateMembersArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectGroup
    pub interconnectGroup: String,
}

/// POST projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers
/// Create Interconnects with redundancy by creating them in a specified interconnect group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_create_members_builder()` + `compute_interconnect_groups_create_members_execute()`.
/// For task-level control, use `compute_interconnect_groups_create_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_create_members(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsCreateMembersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_create_members_builder(
        client,
        &args.project,
        &args.interconnectGroup,
    )?;
    compute_interconnect_groups_create_members_execute(builder)
}

/// DELETE projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Deletes the specified InterconnectGroup in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_delete_execute()` to send, or `compute_interconnect_groups_delete` for simplest API.

pub fn compute_interconnect_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}",
        project, interconnectGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Deletes the specified InterconnectGroup in the given scope
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_delete_execute()` or `compute_interconnect_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Deletes the specified InterconnectGroup in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_delete_task()`.
/// For the simplest API, use `compute_interconnect_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectGroup
    pub interconnectGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Deletes the specified InterconnectGroup in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_delete_builder()` + `compute_interconnect_groups_delete_execute()`.
/// For task-level control, use `compute_interconnect_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_delete_builder(
        client,
        &args.project,
        &args.interconnectGroup,
        &args.requestId,
    )?;
    compute_interconnect_groups_delete_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Returns the specified InterconnectGroup resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_get_execute()` to send, or `compute_interconnect_groups_get` for simplest API.

pub fn compute_interconnect_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}",
        project, interconnectGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Returns the specified InterconnectGroup resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_get_execute()` or `compute_interconnect_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Returns the specified InterconnectGroup resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_get_task()`.
/// For the simplest API, use `compute_interconnect_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectGroup
    pub interconnectGroup: String,
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Returns the specified InterconnectGroup resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_get_builder()` + `compute_interconnect_groups_get_execute()`.
/// For task-level control, use `compute_interconnect_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_interconnect_groups_get_builder(client, &args.project, &args.interconnectGroup)?;
    compute_interconnect_groups_get_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_get_iam_policy_execute()` to send, or `compute_interconnect_groups_get_iam_policy` for simplest API.

pub fn compute_interconnect_groups_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/getIamPolicy",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_get_iam_policy_execute()` or `compute_interconnect_groups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_get_iam_policy_task()`.
/// For the simplest API, use `compute_interconnect_groups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_get_iam_policy_builder()` + `compute_interconnect_groups_get_iam_policy_execute()`.
/// For task-level control, use `compute_interconnect_groups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_interconnect_groups_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus
/// Returns the `interconnectStatuses` for the specified InterconnectGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_get_operational_status_execute()` to send, or `compute_interconnect_groups_get_operational_status` for simplest API.

pub fn compute_interconnect_groups_get_operational_status_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/getOperationalStatus",
        project,
        interconnectGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus
/// Returns the `interconnectStatuses` for the specified InterconnectGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_get_operational_status_execute()` or `compute_interconnect_groups_get_operational_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_get_operational_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_operational_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectGroupsGetOperationalStatusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectGroupsGetOperationalStatusResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus
/// Returns the `interconnectStatuses` for the specified InterconnectGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_get_operational_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_get_operational_status_task()`.
/// For the simplest API, use `compute_interconnect_groups_get_operational_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_get_operational_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_get_operational_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectGroupsGetOperationalStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_get_operational_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_get_operational_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsGetOperationalStatusArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectGroup
    pub interconnectGroup: String,
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus
/// Returns the `interconnectStatuses` for the specified InterconnectGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_get_operational_status_builder()` + `compute_interconnect_groups_get_operational_status_execute()`.
/// For task-level control, use `compute_interconnect_groups_get_operational_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_operational_status(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsGetOperationalStatusArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectGroupsGetOperationalStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_get_operational_status_builder(
        client,
        &args.project,
        &args.interconnectGroup,
    )?;
    compute_interconnect_groups_get_operational_status_execute(builder)
}

/// POST projects/{project}/global/interconnectGroups
/// Creates a InterconnectGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_insert_execute()` to send, or `compute_interconnect_groups_insert` for simplest API.

pub fn compute_interconnect_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnectGroups
/// Creates a InterconnectGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_insert_execute()` or `compute_interconnect_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnectGroups
/// Creates a InterconnectGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_insert_task()`.
/// For the simplest API, use `compute_interconnect_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/interconnectGroups
/// Creates a InterconnectGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_insert_builder()` + `compute_interconnect_groups_insert_execute()`.
/// For task-level control, use `compute_interconnect_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_interconnect_groups_insert_builder(client, &args.project, &args.requestId)?;
    compute_interconnect_groups_insert_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups
/// Lists the InterconnectGroups for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_list_execute()` to send, or `compute_interconnect_groups_list` for simplest API.

pub fn compute_interconnect_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectGroups
/// Lists the InterconnectGroups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_list_execute()` or `compute_interconnect_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectGroupsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectGroups
/// Lists the InterconnectGroups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_list_task()`.
/// For the simplest API, use `compute_interconnect_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/interconnectGroups
/// Lists the InterconnectGroups for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_list_builder()` + `compute_interconnect_groups_list_execute()`.
/// For task-level control, use `compute_interconnect_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_interconnect_groups_list_execute(builder)
}

/// PATCH projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Patches the specified InterconnectGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_patch_execute()` to send, or `compute_interconnect_groups_patch` for simplest API.

pub fn compute_interconnect_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectGroup: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}",
        project, interconnectGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Patches the specified InterconnectGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_patch_execute()` or `compute_interconnect_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Patches the specified InterconnectGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_patch_task()`.
/// For the simplest API, use `compute_interconnect_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectGroup
    pub interconnectGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Patches the specified InterconnectGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_patch_builder()` + `compute_interconnect_groups_patch_execute()`.
/// For task-level control, use `compute_interconnect_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_patch(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_patch_builder(
        client,
        &args.project,
        &args.interconnectGroup,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_interconnect_groups_patch_execute(builder)
}

/// POST projects/{project}/global/interconnectGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_set_iam_policy_execute()` to send, or `compute_interconnect_groups_set_iam_policy` for simplest API.

pub fn compute_interconnect_groups_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/setIamPolicy",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnectGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_set_iam_policy_execute()` or `compute_interconnect_groups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnectGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_set_iam_policy_task()`.
/// For the simplest API, use `compute_interconnect_groups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/interconnectGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_set_iam_policy_builder()` + `compute_interconnect_groups_set_iam_policy_execute()`.
/// For task-level control, use `compute_interconnect_groups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_interconnect_groups_set_iam_policy_builder(client, &args.project, &args.resource)?;
    compute_interconnect_groups_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/interconnectGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_test_iam_permissions_execute()` to send, or `compute_interconnect_groups_test_iam_permissions` for simplest API.

pub fn compute_interconnect_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnectGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_groups_test_iam_permissions_execute()` or `compute_interconnect_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnectGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_groups_test_iam_permissions_task()`.
/// For the simplest API, use `compute_interconnect_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/interconnectGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_test_iam_permissions_builder()` + `compute_interconnect_groups_test_iam_permissions_execute()`.
/// For task-level control, use `compute_interconnect_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_interconnect_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/interconnectLocations/{interconnectLocation}
/// Returns the details for the specified interconnect location. Gets a list of available interconnect locations by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_locations_get_execute()` to send, or `compute_interconnect_locations_get` for simplest API.

pub fn compute_interconnect_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectLocation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectLocations/{}",
        project, interconnectLocation,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectLocations/{interconnectLocation}
/// Returns the details for the specified interconnect location. Gets a list of available interconnect locations by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_locations_get_execute()` or `compute_interconnect_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectLocation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectLocations/{interconnectLocation}
/// Returns the details for the specified interconnect location. Gets a list of available interconnect locations by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_locations_get_task()`.
/// For the simplest API, use `compute_interconnect_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectLocationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectLocation
    pub interconnectLocation: String,
}

/// GET projects/{project}/global/interconnectLocations/{interconnectLocation}
/// Returns the details for the specified interconnect location. Gets a list of available interconnect locations by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_locations_get_builder()` + `compute_interconnect_locations_get_execute()`.
/// For task-level control, use `compute_interconnect_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_locations_get(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_locations_get_builder(
        client,
        &args.project,
        &args.interconnectLocation,
    )?;
    compute_interconnect_locations_get_execute(builder)
}

/// GET projects/{project}/global/interconnectLocations
/// Retrieves the list of interconnect locations available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_locations_list_execute()` to send, or `compute_interconnect_locations_list` for simplest API.

pub fn compute_interconnect_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectLocations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectLocations
/// Retrieves the list of interconnect locations available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_locations_list_execute()` or `compute_interconnect_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectLocationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectLocationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectLocations
/// Retrieves the list of interconnect locations available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_locations_list_task()`.
/// For the simplest API, use `compute_interconnect_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectLocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectLocationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/interconnectLocations
/// Retrieves the list of interconnect locations available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_locations_list_builder()` + `compute_interconnect_locations_list_execute()`.
/// For task-level control, use `compute_interconnect_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_locations_list(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectLocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_locations_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_interconnect_locations_list_execute(builder)
}

/// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}
/// Returns the details for the specified interconnect remote location. Gets a list of available interconnect remote locations by making alist() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_remote_locations_get_execute()` to send, or `compute_interconnect_remote_locations_get` for simplest API.

pub fn compute_interconnect_remote_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnectRemoteLocation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectRemoteLocations/{}",
        project,
        interconnectRemoteLocation,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}
/// Returns the details for the specified interconnect remote location. Gets a list of available interconnect remote locations by making alist() request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_remote_locations_get_execute()` or `compute_interconnect_remote_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_remote_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_remote_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectRemoteLocation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectRemoteLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}
/// Returns the details for the specified interconnect remote location. Gets a list of available interconnect remote locations by making alist() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_remote_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_remote_locations_get_task()`.
/// For the simplest API, use `compute_interconnect_remote_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_remote_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_remote_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectRemoteLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_remote_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_remote_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectRemoteLocationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnectRemoteLocation
    pub interconnectRemoteLocation: String,
}

/// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}
/// Returns the details for the specified interconnect remote location. Gets a list of available interconnect remote locations by making alist() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_remote_locations_get_builder()` + `compute_interconnect_remote_locations_get_execute()`.
/// For task-level control, use `compute_interconnect_remote_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_remote_locations_get(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectRemoteLocationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectRemoteLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_remote_locations_get_builder(
        client,
        &args.project,
        &args.interconnectRemoteLocation,
    )?;
    compute_interconnect_remote_locations_get_execute(builder)
}

/// GET projects/{project}/global/interconnectRemoteLocations
/// Retrieves the list of interconnect remote locations available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_remote_locations_list_execute()` to send, or `compute_interconnect_remote_locations_list` for simplest API.

pub fn compute_interconnect_remote_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectRemoteLocations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnectRemoteLocations
/// Retrieves the list of interconnect remote locations available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnect_remote_locations_list_execute()` or `compute_interconnect_remote_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_remote_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_remote_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectRemoteLocationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectRemoteLocationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnectRemoteLocations
/// Retrieves the list of interconnect remote locations available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnect_remote_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnect_remote_locations_list_task()`.
/// For the simplest API, use `compute_interconnect_remote_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnect_remote_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnect_remote_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectRemoteLocationList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnect_remote_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnect_remote_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectRemoteLocationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/interconnectRemoteLocations
/// Retrieves the list of interconnect remote locations available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_remote_locations_list_builder()` + `compute_interconnect_remote_locations_list_execute()`.
/// For task-level control, use `compute_interconnect_remote_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_remote_locations_list(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectRemoteLocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectRemoteLocationList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_remote_locations_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_interconnect_remote_locations_list_execute(builder)
}

/// DELETE projects/{project}/global/interconnects/{interconnect}
/// Deletes the specified Interconnect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_delete_execute()` to send, or `compute_interconnects_delete` for simplest API.

pub fn compute_interconnects_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnect: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}",
        project, interconnect,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/interconnects/{interconnect}
/// Deletes the specified Interconnect.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnects_delete_execute()` or `compute_interconnects_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/interconnects/{interconnect}
/// Deletes the specified Interconnect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnects_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnects_delete_task()`.
/// For the simplest API, use `compute_interconnects_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnects_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnects_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnects_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnect
    pub interconnect: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/interconnects/{interconnect}
/// Deletes the specified Interconnect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_delete_builder()` + `compute_interconnects_delete_execute()`.
/// For task-level control, use `compute_interconnects_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_delete(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnects_delete_builder(
        client,
        &args.project,
        &args.interconnect,
        &args.requestId,
    )?;
    compute_interconnects_delete_execute(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Returns the specified Interconnect. Get a list of available Interconnects by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_get_execute()` to send, or `compute_interconnects_get` for simplest API.

pub fn compute_interconnects_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnect: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}",
        project, interconnect,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Returns the specified Interconnect. Get a list of available Interconnects by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnects_get_execute()` or `compute_interconnects_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Interconnect>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Interconnect = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Returns the specified Interconnect. Get a list of available Interconnects by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnects_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnects_get_task()`.
/// For the simplest API, use `compute_interconnects_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Interconnect>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnects_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnects_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnect
    pub interconnect: String,
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Returns the specified Interconnect. Get a list of available Interconnects by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_get_builder()` + `compute_interconnects_get_execute()`.
/// For task-level control, use `compute_interconnects_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Interconnect>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnects_get_builder(client, &args.project, &args.interconnect)?;
    compute_interconnects_get_execute(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics
/// Returns the `interconnectDiagnostics` for the specified Interconnect.  In the event of a global outage, do not use this API to make decisions about where to redirect your network traffic.  Unlike a VLAN attachment, which is regional, a Cloud Interconnect connection is a global resource. A global outage can prevent this API from functioning properly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_get_diagnostics_execute()` to send, or `compute_interconnects_get_diagnostics` for simplest API.

pub fn compute_interconnects_get_diagnostics_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnect: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}/getDiagnostics",
        project,
        interconnect,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics
/// Returns the `interconnectDiagnostics` for the specified Interconnect.  In the event of a global outage, do not use this API to make decisions about where to redirect your network traffic.  Unlike a VLAN attachment, which is regional, a Cloud Interconnect connection is a global resource. A global outage can prevent this API from functioning properly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnects_get_diagnostics_execute()` or `compute_interconnects_get_diagnostics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_get_diagnostics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_diagnostics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectsGetDiagnosticsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectsGetDiagnosticsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics
/// Returns the `interconnectDiagnostics` for the specified Interconnect.  In the event of a global outage, do not use this API to make decisions about where to redirect your network traffic.  Unlike a VLAN attachment, which is regional, a Cloud Interconnect connection is a global resource. A global outage can prevent this API from functioning properly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnects_get_diagnostics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnects_get_diagnostics_task()`.
/// For the simplest API, use `compute_interconnects_get_diagnostics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_get_diagnostics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnects_get_diagnostics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectsGetDiagnosticsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnects_get_diagnostics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnects_get_diagnostics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectsGetDiagnosticsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnect
    pub interconnect: String,
}

/// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics
/// Returns the `interconnectDiagnostics` for the specified Interconnect.  In the event of a global outage, do not use this API to make decisions about where to redirect your network traffic.  Unlike a VLAN attachment, which is regional, a Cloud Interconnect connection is a global resource. A global outage can prevent this API from functioning properly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_get_diagnostics_builder()` + `compute_interconnects_get_diagnostics_execute()`.
/// For task-level control, use `compute_interconnects_get_diagnostics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_diagnostics(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectsGetDiagnosticsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectsGetDiagnosticsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_interconnects_get_diagnostics_builder(client, &args.project, &args.interconnect)?;
    compute_interconnects_get_diagnostics_execute(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig
/// Returns the `interconnectMacsecConfig` for the specified Interconnect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_get_macsec_config_execute()` to send, or `compute_interconnects_get_macsec_config` for simplest API.

pub fn compute_interconnects_get_macsec_config_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnect: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}/getMacsecConfig",
        project,
        interconnect,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig
/// Returns the `interconnectMacsecConfig` for the specified Interconnect.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnects_get_macsec_config_execute()` or `compute_interconnects_get_macsec_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_get_macsec_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_macsec_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectsGetMacsecConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectsGetMacsecConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig
/// Returns the `interconnectMacsecConfig` for the specified Interconnect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnects_get_macsec_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnects_get_macsec_config_task()`.
/// For the simplest API, use `compute_interconnects_get_macsec_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_get_macsec_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnects_get_macsec_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectsGetMacsecConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnects_get_macsec_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnects_get_macsec_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectsGetMacsecConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnect
    pub interconnect: String,
}

/// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig
/// Returns the `interconnectMacsecConfig` for the specified Interconnect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_get_macsec_config_builder()` + `compute_interconnects_get_macsec_config_execute()`.
/// For task-level control, use `compute_interconnects_get_macsec_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_macsec_config(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectsGetMacsecConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectsGetMacsecConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_interconnects_get_macsec_config_builder(client, &args.project, &args.interconnect)?;
    compute_interconnects_get_macsec_config_execute(builder)
}

/// POST projects/{project}/global/interconnects
/// Creates an Interconnect in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_insert_execute()` to send, or `compute_interconnects_insert` for simplest API.

pub fn compute_interconnects_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnects
/// Creates an Interconnect in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnects_insert_execute()` or `compute_interconnects_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnects
/// Creates an Interconnect in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnects_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnects_insert_task()`.
/// For the simplest API, use `compute_interconnects_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnects_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnects_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnects_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/interconnects
/// Creates an Interconnect in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_insert_builder()` + `compute_interconnects_insert_execute()`.
/// For task-level control, use `compute_interconnects_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_insert(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnects_insert_builder(client, &args.project, &args.requestId)?;
    compute_interconnects_insert_execute(builder)
}

/// GET projects/{project}/global/interconnects
/// Retrieves the list of Interconnects available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_list_execute()` to send, or `compute_interconnects_list` for simplest API.

pub fn compute_interconnects_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/interconnects
/// Retrieves the list of Interconnects available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnects_list_execute()` or `compute_interconnects_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/interconnects
/// Retrieves the list of Interconnects available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnects_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnects_list_task()`.
/// For the simplest API, use `compute_interconnects_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_interconnects_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnects_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/interconnects
/// Retrieves the list of Interconnects available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_list_builder()` + `compute_interconnects_list_execute()`.
/// For task-level control, use `compute_interconnects_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_list(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnects_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_interconnects_list_execute(builder)
}

/// PATCH projects/{project}/global/interconnects/{interconnect}
/// Updates the specified Interconnect with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_patch_execute()` to send, or `compute_interconnects_patch` for simplest API.

pub fn compute_interconnects_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    interconnect: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}",
        project, interconnect,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/interconnects/{interconnect}
/// Updates the specified Interconnect with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnects_patch_execute()` or `compute_interconnects_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/interconnects/{interconnect}
/// Updates the specified Interconnect with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnects_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnects_patch_task()`.
/// For the simplest API, use `compute_interconnects_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnects_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnects_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnects_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: interconnect
    pub interconnect: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/interconnects/{interconnect}
/// Updates the specified Interconnect with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_patch_builder()` + `compute_interconnects_patch_execute()`.
/// For task-level control, use `compute_interconnects_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_patch(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnects_patch_builder(
        client,
        &args.project,
        &args.interconnect,
        &args.requestId,
    )?;
    compute_interconnects_patch_execute(builder)
}

/// POST projects/{project}/global/interconnects/{resource}/setLabels
/// Sets the labels on an Interconnect. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_set_labels_execute()` to send, or `compute_interconnects_set_labels` for simplest API.

pub fn compute_interconnects_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}/setLabels",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/interconnects/{resource}/setLabels
/// Sets the labels on an Interconnect. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_interconnects_set_labels_execute()` or `compute_interconnects_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/interconnects/{resource}/setLabels
/// Sets the labels on an Interconnect. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_interconnects_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_interconnects_set_labels_task()`.
/// For the simplest API, use `compute_interconnects_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_interconnects_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_interconnects_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_interconnects_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_interconnects_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeInterconnectsSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/interconnects/{resource}/setLabels
/// Sets the labels on an Interconnect. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_set_labels_builder()` + `compute_interconnects_set_labels_execute()`.
/// For task-level control, use `compute_interconnects_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeInterconnectsSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnects_set_labels_builder(client, &args.project, &args.resource)?;
    compute_interconnects_set_labels_execute(builder)
}

/// GET projects/{project}/global/licenseCodes/{licenseCode}
/// Return a specified license code. License codes are mirrored across all projects that have permissions to read the License Code. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_license_codes_get_execute()` to send, or `compute_license_codes_get` for simplest API.

pub fn compute_license_codes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    licenseCode: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenseCodes/{}",
        project, licenseCode,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/licenseCodes/{licenseCode}
/// Return a specified license code. License codes are mirrored across all projects that have permissions to read the License Code. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_license_codes_get_execute()` or `compute_license_codes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_license_codes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_license_codes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LicenseCode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseCode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/licenseCodes/{licenseCode}
/// Return a specified license code. License codes are mirrored across all projects that have permissions to read the License Code. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_license_codes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_license_codes_get_task()`.
/// For the simplest API, use `compute_license_codes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_license_codes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_license_codes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseCode>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_license_codes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_license_codes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicenseCodesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: licenseCode
    pub licenseCode: String,
}

/// GET projects/{project}/global/licenseCodes/{licenseCode}
/// Return a specified license code. License codes are mirrored across all projects that have permissions to read the License Code. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_license_codes_get_builder()` + `compute_license_codes_get_execute()`.
/// For task-level control, use `compute_license_codes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_license_codes_get(
    client: &SimpleHttpClient,
    args: &ComputeLicenseCodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseCode>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_license_codes_get_builder(client, &args.project, &args.licenseCode)?;
    compute_license_codes_get_execute(builder)
}

/// POST projects/{project}/global/licenseCodes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_license_codes_test_iam_permissions_execute()` to send, or `compute_license_codes_test_iam_permissions` for simplest API.

pub fn compute_license_codes_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenseCodes/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/licenseCodes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_license_codes_test_iam_permissions_execute()` or `compute_license_codes_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_license_codes_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_license_codes_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/licenseCodes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_license_codes_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_license_codes_test_iam_permissions_task()`.
/// For the simplest API, use `compute_license_codes_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_license_codes_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_license_codes_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_license_codes_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_license_codes_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicenseCodesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/licenseCodes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_license_codes_test_iam_permissions_builder()` + `compute_license_codes_test_iam_permissions_execute()`.
/// For task-level control, use `compute_license_codes_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_license_codes_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeLicenseCodesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_license_codes_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_license_codes_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/global/licenses/{license}
/// Deletes the specified license. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_delete_execute()` to send, or `compute_licenses_delete` for simplest API.

pub fn compute_licenses_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    license: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}",
        project, license,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/licenses/{license}
/// Deletes the specified license. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_licenses_delete_execute()` or `compute_licenses_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/licenses/{license}
/// Deletes the specified license. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_licenses_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_licenses_delete_task()`.
/// For the simplest API, use `compute_licenses_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_licenses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_licenses_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_licenses_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicensesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: license
    pub license: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/licenses/{license}
/// Deletes the specified license. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_delete_builder()` + `compute_licenses_delete_execute()`.
/// For task-level control, use `compute_licenses_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_delete(
    client: &SimpleHttpClient,
    args: &ComputeLicensesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_licenses_delete_builder(client, &args.project, &args.license, &args.requestId)?;
    compute_licenses_delete_execute(builder)
}

/// GET projects/{project}/global/licenses/{license}
/// Returns the specified License resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_get_execute()` to send, or `compute_licenses_get` for simplest API.

pub fn compute_licenses_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    license: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}",
        project, license,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/licenses/{license}
/// Returns the specified License resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_licenses_get_execute()` or `compute_licenses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<License>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: License = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/licenses/{license}
/// Returns the specified License resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_licenses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_licenses_get_task()`.
/// For the simplest API, use `compute_licenses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_licenses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<License>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_licenses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_licenses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicensesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: license
    pub license: String,
}

/// GET projects/{project}/global/licenses/{license}
/// Returns the specified License resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_get_builder()` + `compute_licenses_get_execute()`.
/// For task-level control, use `compute_licenses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_get(
    client: &SimpleHttpClient,
    args: &ComputeLicensesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<License>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_get_builder(client, &args.project, &args.license)?;
    compute_licenses_get_execute(builder)
}

/// GET projects/{project}/global/licenses/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_get_iam_policy_execute()` to send, or `compute_licenses_get_iam_policy` for simplest API.

pub fn compute_licenses_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/licenses/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_licenses_get_iam_policy_execute()` or `compute_licenses_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/licenses/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_licenses_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_licenses_get_iam_policy_task()`.
/// For the simplest API, use `compute_licenses_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_licenses_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_licenses_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_licenses_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicensesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/licenses/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_get_iam_policy_builder()` + `compute_licenses_get_iam_policy_execute()`.
/// For task-level control, use `compute_licenses_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeLicensesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_licenses_get_iam_policy_execute(builder)
}

/// POST projects/{project}/global/licenses
/// Create a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_insert_execute()` to send, or `compute_licenses_insert` for simplest API.

pub fn compute_licenses_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/licenses
/// Create a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_licenses_insert_execute()` or `compute_licenses_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/licenses
/// Create a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_licenses_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_licenses_insert_task()`.
/// For the simplest API, use `compute_licenses_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_licenses_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_licenses_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_licenses_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicensesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/licenses
/// Create a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_insert_builder()` + `compute_licenses_insert_execute()`.
/// For task-level control, use `compute_licenses_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_insert(
    client: &SimpleHttpClient,
    args: &ComputeLicensesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_insert_builder(client, &args.project, &args.requestId)?;
    compute_licenses_insert_execute(builder)
}

/// GET projects/{project}/global/licenses
/// Retrieves the list of licenses available in the specified project. This method does not get any licenses that belong to other projects, including licenses attached to publicly-available images, like Debian 9. If you want to get a list of publicly-available licenses, use this method to make a request to the respective image project, such as debian-cloud orwindows-cloud. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_list_execute()` to send, or `compute_licenses_list` for simplest API.

pub fn compute_licenses_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/licenses
/// Retrieves the list of licenses available in the specified project. This method does not get any licenses that belong to other projects, including licenses attached to publicly-available images, like Debian 9. If you want to get a list of publicly-available licenses, use this method to make a request to the respective image project, such as debian-cloud orwindows-cloud. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_licenses_list_execute()` or `compute_licenses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LicensesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicensesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/licenses
/// Retrieves the list of licenses available in the specified project. This method does not get any licenses that belong to other projects, including licenses attached to publicly-available images, like Debian 9. If you want to get a list of publicly-available licenses, use this method to make a request to the respective image project, such as debian-cloud orwindows-cloud. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_licenses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_licenses_list_task()`.
/// For the simplest API, use `compute_licenses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_licenses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicensesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_licenses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_licenses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicensesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/licenses
/// Retrieves the list of licenses available in the specified project. This method does not get any licenses that belong to other projects, including licenses attached to publicly-available images, like Debian 9. If you want to get a list of publicly-available licenses, use this method to make a request to the respective image project, such as debian-cloud orwindows-cloud. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_list_builder()` + `compute_licenses_list_execute()`.
/// For task-level control, use `compute_licenses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_list(
    client: &SimpleHttpClient,
    args: &ComputeLicensesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicensesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_licenses_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_licenses_list_execute(builder)
}

/// POST projects/{project}/global/licenses/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_set_iam_policy_execute()` to send, or `compute_licenses_set_iam_policy` for simplest API.

pub fn compute_licenses_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/licenses/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_licenses_set_iam_policy_execute()` or `compute_licenses_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/licenses/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_licenses_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_licenses_set_iam_policy_task()`.
/// For the simplest API, use `compute_licenses_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_licenses_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_licenses_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_licenses_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicensesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/licenses/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_set_iam_policy_builder()` + `compute_licenses_set_iam_policy_execute()`.
/// For task-level control, use `compute_licenses_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeLicensesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_set_iam_policy_builder(client, &args.project, &args.resource)?;
    compute_licenses_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/licenses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_test_iam_permissions_execute()` to send, or `compute_licenses_test_iam_permissions` for simplest API.

pub fn compute_licenses_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/licenses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_licenses_test_iam_permissions_execute()` or `compute_licenses_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/licenses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_licenses_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_licenses_test_iam_permissions_task()`.
/// For the simplest API, use `compute_licenses_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_licenses_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_licenses_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_licenses_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicensesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/licenses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_test_iam_permissions_builder()` + `compute_licenses_test_iam_permissions_execute()`.
/// For task-level control, use `compute_licenses_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeLicensesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_licenses_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_licenses_test_iam_permissions_execute(builder)
}

/// PATCH projects/{project}/global/licenses/{license}
/// Updates a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_update_execute()` to send, or `compute_licenses_update` for simplest API.

pub fn compute_licenses_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    license: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}",
        project, license,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/licenses/{license}
/// Updates a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_licenses_update_execute()` or `compute_licenses_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/licenses/{license}
/// Updates a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_licenses_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_licenses_update_task()`.
/// For the simplest API, use `compute_licenses_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_licenses_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_licenses_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_licenses_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_licenses_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeLicensesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: license
    pub license: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/global/licenses/{license}
/// Updates a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_update_builder()` + `compute_licenses_update_execute()`.
/// For task-level control, use `compute_licenses_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_update(
    client: &SimpleHttpClient,
    args: &ComputeLicensesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_update_builder(
        client,
        &args.project,
        &args.license,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_licenses_update_execute(builder)
}

/// DELETE projects/{project}/global/machineImages/{machineImage}
/// Deletes the specified machine image. Deleting a machine image is permanent and cannot be undone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_delete_execute()` to send, or `compute_machine_images_delete` for simplest API.

pub fn compute_machine_images_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    machineImage: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}",
        project, machineImage,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/machineImages/{machineImage}
/// Deletes the specified machine image. Deleting a machine image is permanent and cannot be undone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_images_delete_execute()` or `compute_machine_images_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/machineImages/{machineImage}
/// Deletes the specified machine image. Deleting a machine image is permanent and cannot be undone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_images_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_images_delete_task()`.
/// For the simplest API, use `compute_machine_images_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_images_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_machine_images_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_images_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineImagesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: machineImage
    pub machineImage: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/machineImages/{machineImage}
/// Deletes the specified machine image. Deleting a machine image is permanent and cannot be undone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_delete_builder()` + `compute_machine_images_delete_execute()`.
/// For task-level control, use `compute_machine_images_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_delete(
    client: &SimpleHttpClient,
    args: &ComputeMachineImagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_images_delete_builder(
        client,
        &args.project,
        &args.machineImage,
        &args.requestId,
    )?;
    compute_machine_images_delete_execute(builder)
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Returns the specified machine image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_get_execute()` to send, or `compute_machine_images_get` for simplest API.

pub fn compute_machine_images_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    machineImage: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}",
        project, machineImage,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Returns the specified machine image.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_images_get_execute()` or `compute_machine_images_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineImage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineImage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Returns the specified machine image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_images_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_images_get_task()`.
/// For the simplest API, use `compute_machine_images_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_images_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineImage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_machine_images_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_images_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineImagesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: machineImage
    pub machineImage: String,
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Returns the specified machine image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_get_builder()` + `compute_machine_images_get_execute()`.
/// For task-level control, use `compute_machine_images_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_get(
    client: &SimpleHttpClient,
    args: &ComputeMachineImagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineImage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_machine_images_get_builder(client, &args.project, &args.machineImage)?;
    compute_machine_images_get_execute(builder)
}

/// GET projects/{project}/global/machineImages/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_get_iam_policy_execute()` to send, or `compute_machine_images_get_iam_policy` for simplest API.

pub fn compute_machine_images_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}/getIamPolicy",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/machineImages/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_images_get_iam_policy_execute()` or `compute_machine_images_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/machineImages/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_images_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_images_get_iam_policy_task()`.
/// For the simplest API, use `compute_machine_images_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_images_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_machine_images_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_images_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineImagesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/machineImages/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_get_iam_policy_builder()` + `compute_machine_images_get_iam_policy_execute()`.
/// For task-level control, use `compute_machine_images_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeMachineImagesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_images_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_machine_images_get_iam_policy_execute(builder)
}

/// POST projects/{project}/global/machineImages
/// Creates a machine image in the specified project using the data that is included in the request. If you are creating a new machine image to update an existing instance, your new machine image should use the same network or, if applicable, the same subnetwork as the original instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_insert_execute()` to send, or `compute_machine_images_insert` for simplest API.

pub fn compute_machine_images_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
    sourceInstance: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceInstance.as_ref() {
        query_parts.push(format!("sourceInstance={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/machineImages
/// Creates a machine image in the specified project using the data that is included in the request. If you are creating a new machine image to update an existing instance, your new machine image should use the same network or, if applicable, the same subnetwork as the original instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_images_insert_execute()` or `compute_machine_images_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/machineImages
/// Creates a machine image in the specified project using the data that is included in the request. If you are creating a new machine image to update an existing instance, your new machine image should use the same network or, if applicable, the same subnetwork as the original instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_images_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_images_insert_task()`.
/// For the simplest API, use `compute_machine_images_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_images_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_machine_images_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_images_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineImagesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceInstance
    pub sourceInstance: Option<Option<String>>,
}

/// POST projects/{project}/global/machineImages
/// Creates a machine image in the specified project using the data that is included in the request. If you are creating a new machine image to update an existing instance, your new machine image should use the same network or, if applicable, the same subnetwork as the original instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_insert_builder()` + `compute_machine_images_insert_execute()`.
/// For task-level control, use `compute_machine_images_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_insert(
    client: &SimpleHttpClient,
    args: &ComputeMachineImagesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_images_insert_builder(
        client,
        &args.project,
        &args.requestId,
        &args.sourceInstance,
    )?;
    compute_machine_images_insert_execute(builder)
}

/// GET projects/{project}/global/machineImages
/// Retrieves a list of machine images that are contained within the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_list_execute()` to send, or `compute_machine_images_list` for simplest API.

pub fn compute_machine_images_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/machineImages
/// Retrieves a list of machine images that are contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_images_list_execute()` or `compute_machine_images_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineImageList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineImageList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/machineImages
/// Retrieves a list of machine images that are contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_images_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_images_list_task()`.
/// For the simplest API, use `compute_machine_images_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_images_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineImageList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_machine_images_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_images_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineImagesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/machineImages
/// Retrieves a list of machine images that are contained within the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_list_builder()` + `compute_machine_images_list_execute()`.
/// For task-level control, use `compute_machine_images_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_list(
    client: &SimpleHttpClient,
    args: &ComputeMachineImagesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineImageList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_machine_images_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_machine_images_list_execute(builder)
}

/// POST projects/{project}/global/machineImages/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_set_iam_policy_execute()` to send, or `compute_machine_images_set_iam_policy` for simplest API.

pub fn compute_machine_images_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}/setIamPolicy",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/machineImages/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_images_set_iam_policy_execute()` or `compute_machine_images_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/machineImages/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_images_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_images_set_iam_policy_task()`.
/// For the simplest API, use `compute_machine_images_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_images_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_machine_images_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_images_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineImagesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/machineImages/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_set_iam_policy_builder()` + `compute_machine_images_set_iam_policy_execute()`.
/// For task-level control, use `compute_machine_images_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeMachineImagesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_machine_images_set_iam_policy_builder(client, &args.project, &args.resource)?;
    compute_machine_images_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/machineImages/{resource}/setLabels
/// Sets the labels on a machine image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_set_labels_execute()` to send, or `compute_machine_images_set_labels` for simplest API.

pub fn compute_machine_images_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}/setLabels",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/machineImages/{resource}/setLabels
/// Sets the labels on a machine image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_images_set_labels_execute()` or `compute_machine_images_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/machineImages/{resource}/setLabels
/// Sets the labels on a machine image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_images_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_images_set_labels_task()`.
/// For the simplest API, use `compute_machine_images_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_images_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_machine_images_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_images_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineImagesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/machineImages/{resource}/setLabels
/// Sets the labels on a machine image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_set_labels_builder()` + `compute_machine_images_set_labels_execute()`.
/// For task-level control, use `compute_machine_images_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeMachineImagesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_images_set_labels_builder(client, &args.project, &args.resource)?;
    compute_machine_images_set_labels_execute(builder)
}

/// POST projects/{project}/global/machineImages/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_test_iam_permissions_execute()` to send, or `compute_machine_images_test_iam_permissions` for simplest API.

pub fn compute_machine_images_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/machineImages/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_images_test_iam_permissions_execute()` or `compute_machine_images_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/machineImages/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_images_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_images_test_iam_permissions_task()`.
/// For the simplest API, use `compute_machine_images_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_images_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_images_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_machine_images_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_images_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineImagesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/machineImages/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_test_iam_permissions_builder()` + `compute_machine_images_test_iam_permissions_execute()`.
/// For task-level control, use `compute_machine_images_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeMachineImagesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_machine_images_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_machine_images_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/machineTypes
/// Retrieves an aggregated list of machine types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_types_aggregated_list_execute()` to send, or `compute_machine_types_aggregated_list` for simplest API.

pub fn compute_machine_types_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/machineTypes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/machineTypes
/// Retrieves an aggregated list of machine types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_types_aggregated_list_execute()` or `compute_machine_types_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineTypeAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/machineTypes
/// Retrieves an aggregated list of machine types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_types_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_types_aggregated_list_task()`.
/// For the simplest API, use `compute_machine_types_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_machine_types_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_types_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineTypesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/machineTypes
/// Retrieves an aggregated list of machine types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_types_aggregated_list_builder()` + `compute_machine_types_aggregated_list_execute()`.
/// For task-level control, use `compute_machine_types_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeMachineTypesAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_machine_types_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_machine_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/machineTypes/{machineType}
/// Returns the specified machine type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_types_get_execute()` to send, or `compute_machine_types_get` for simplest API.

pub fn compute_machine_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    machineType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/machineTypes/{}",
        project, zone, machineType,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/machineTypes/{machineType}
/// Returns the specified machine type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_types_get_execute()` or `compute_machine_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/machineTypes/{machineType}
/// Returns the specified machine type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_types_get_task()`.
/// For the simplest API, use `compute_machine_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_machine_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineTypesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: machineType
    pub machineType: String,
}

/// GET projects/{project}/zones/{zone}/machineTypes/{machineType}
/// Returns the specified machine type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_types_get_builder()` + `compute_machine_types_get_execute()`.
/// For task-level control, use `compute_machine_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_get(
    client: &SimpleHttpClient,
    args: &ComputeMachineTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_machine_types_get_builder(client, &args.project, &args.zone, &args.machineType)?;
    compute_machine_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/machineTypes
/// Retrieves a list of machine types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_types_list_execute()` to send, or `compute_machine_types_list` for simplest API.

pub fn compute_machine_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/machineTypes",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/machineTypes
/// Retrieves a list of machine types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_machine_types_list_execute()` or `compute_machine_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineTypeList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/machineTypes
/// Retrieves a list of machine types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_machine_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_machine_types_list_task()`.
/// For the simplest API, use `compute_machine_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_machine_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_machine_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_machine_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_machine_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeMachineTypesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/machineTypes
/// Retrieves a list of machine types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_types_list_builder()` + `compute_machine_types_list_execute()`.
/// For task-level control, use `compute_machine_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_list(
    client: &SimpleHttpClient,
    args: &ComputeMachineTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_machine_types_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_machine_types_list_execute(builder)
}

/// GET projects/{project}/aggregated/networkAttachments
/// Retrieves the list of all NetworkAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_aggregated_list_execute()` to send, or `compute_network_attachments_aggregated_list` for simplest API.

pub fn compute_network_attachments_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/networkAttachments",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/networkAttachments
/// Retrieves the list of all NetworkAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_aggregated_list_execute()` or `compute_network_attachments_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkAttachmentAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkAttachmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/networkAttachments
/// Retrieves the list of all NetworkAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_aggregated_list_task()`.
/// For the simplest API, use `compute_network_attachments_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_attachments_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/networkAttachments
/// Retrieves the list of all NetworkAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_aggregated_list_builder()` + `compute_network_attachments_aggregated_list_execute()`.
/// For task-level control, use `compute_network_attachments_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_network_attachments_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Deletes the specified NetworkAttachment in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_delete_execute()` to send, or `compute_network_attachments_delete` for simplest API.

pub fn compute_network_attachments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkAttachment: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}",
        project, region, networkAttachment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Deletes the specified NetworkAttachment in the given scope
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_delete_execute()` or `compute_network_attachments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Deletes the specified NetworkAttachment in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_delete_task()`.
/// For the simplest API, use `compute_network_attachments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_attachments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkAttachment
    pub networkAttachment: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Deletes the specified NetworkAttachment in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_delete_builder()` + `compute_network_attachments_delete_execute()`.
/// For task-level control, use `compute_network_attachments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_delete(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.networkAttachment,
        &args.requestId,
    )?;
    compute_network_attachments_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Returns the specified NetworkAttachment resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_get_execute()` to send, or `compute_network_attachments_get` for simplest API.

pub fn compute_network_attachments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkAttachment: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}",
        project, region, networkAttachment,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Returns the specified NetworkAttachment resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_get_execute()` or `compute_network_attachments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkAttachment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Returns the specified NetworkAttachment resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_get_task()`.
/// For the simplest API, use `compute_network_attachments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_attachments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkAttachment
    pub networkAttachment: String,
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Returns the specified NetworkAttachment resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_get_builder()` + `compute_network_attachments_get_execute()`.
/// For task-level control, use `compute_network_attachments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_get(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_get_builder(
        client,
        &args.project,
        &args.region,
        &args.networkAttachment,
    )?;
    compute_network_attachments_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_get_iam_policy_execute()` to send, or `compute_network_attachments_get_iam_policy` for simplest API.

pub fn compute_network_attachments_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_get_iam_policy_execute()` or `compute_network_attachments_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_get_iam_policy_task()`.
/// For the simplest API, use `compute_network_attachments_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_attachments_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_get_iam_policy_builder()` + `compute_network_attachments_get_iam_policy_execute()`.
/// For task-level control, use `compute_network_attachments_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_network_attachments_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/networkAttachments
/// Creates a NetworkAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_insert_execute()` to send, or `compute_network_attachments_insert` for simplest API.

pub fn compute_network_attachments_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/networkAttachments
/// Creates a NetworkAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_insert_execute()` or `compute_network_attachments_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/networkAttachments
/// Creates a NetworkAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_insert_task()`.
/// For the simplest API, use `compute_network_attachments_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_attachments_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/networkAttachments
/// Creates a NetworkAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_insert_builder()` + `compute_network_attachments_insert_execute()`.
/// For task-level control, use `compute_network_attachments_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_insert(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_network_attachments_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Lists the NetworkAttachments for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_list_execute()` to send, or `compute_network_attachments_list` for simplest API.

pub fn compute_network_attachments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Lists the NetworkAttachments for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_list_execute()` or `compute_network_attachments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkAttachmentList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkAttachmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Lists the NetworkAttachments for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_list_task()`.
/// For the simplest API, use `compute_network_attachments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkAttachmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_attachments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Lists the NetworkAttachments for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_list_builder()` + `compute_network_attachments_list_execute()`.
/// For task-level control, use `compute_network_attachments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkAttachmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_network_attachments_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Patches the specified NetworkAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_patch_execute()` to send, or `compute_network_attachments_patch` for simplest API.

pub fn compute_network_attachments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkAttachment: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}",
        project, region, networkAttachment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Patches the specified NetworkAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_patch_execute()` or `compute_network_attachments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Patches the specified NetworkAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_patch_task()`.
/// For the simplest API, use `compute_network_attachments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_attachments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkAttachment
    pub networkAttachment: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Patches the specified NetworkAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_patch_builder()` + `compute_network_attachments_patch_execute()`.
/// For task-level control, use `compute_network_attachments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_patch(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.networkAttachment,
        &args.requestId,
    )?;
    compute_network_attachments_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_set_iam_policy_execute()` to send, or `compute_network_attachments_set_iam_policy` for simplest API.

pub fn compute_network_attachments_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_set_iam_policy_execute()` or `compute_network_attachments_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_set_iam_policy_task()`.
/// For the simplest API, use `compute_network_attachments_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_attachments_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_set_iam_policy_builder()` + `compute_network_attachments_set_iam_policy_execute()`.
/// For task-level control, use `compute_network_attachments_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_network_attachments_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_test_iam_permissions_execute()` to send, or `compute_network_attachments_test_iam_permissions` for simplest API.

pub fn compute_network_attachments_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_attachments_test_iam_permissions_execute()` or `compute_network_attachments_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_attachments_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_attachments_test_iam_permissions_task()`.
/// For the simplest API, use `compute_network_attachments_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_attachments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_attachments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_attachments_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_attachments_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_test_iam_permissions_builder()` + `compute_network_attachments_test_iam_permissions_execute()`.
/// For task-level control, use `compute_network_attachments_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeNetworkAttachmentsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_network_attachments_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/networkEdgeSecurityServices
/// Retrieves the list of all NetworkEdgeSecurityService resources available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_aggregated_list_execute()` to send, or `compute_network_edge_security_services_aggregated_list` for simplest API.

pub fn compute_network_edge_security_services_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/networkEdgeSecurityServices",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/networkEdgeSecurityServices
/// Retrieves the list of all NetworkEdgeSecurityService resources available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_edge_security_services_aggregated_list_execute()` or `compute_network_edge_security_services_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEdgeSecurityServiceAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEdgeSecurityServiceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/networkEdgeSecurityServices
/// Retrieves the list of all NetworkEdgeSecurityService resources available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_edge_security_services_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_edge_security_services_aggregated_list_task()`.
/// For the simplest API, use `compute_network_edge_security_services_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_edge_security_services_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEdgeSecurityServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_edge_security_services_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_edge_security_services_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/networkEdgeSecurityServices
/// Retrieves the list of all NetworkEdgeSecurityService resources available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_aggregated_list_builder()` + `compute_network_edge_security_services_aggregated_list_execute()`.
/// For task-level control, use `compute_network_edge_security_services_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEdgeSecurityServicesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEdgeSecurityServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_network_edge_security_services_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Deletes the specified service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_delete_execute()` to send, or `compute_network_edge_security_services_delete` for simplest API.

pub fn compute_network_edge_security_services_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkEdgeSecurityService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEdgeSecurityServices/{}",
        project,
        region,
        networkEdgeSecurityService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Deletes the specified service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_edge_security_services_delete_execute()` or `compute_network_edge_security_services_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Deletes the specified service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_edge_security_services_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_edge_security_services_delete_task()`.
/// For the simplest API, use `compute_network_edge_security_services_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_edge_security_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_edge_security_services_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_edge_security_services_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkEdgeSecurityService
    pub networkEdgeSecurityService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Deletes the specified service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_delete_builder()` + `compute_network_edge_security_services_delete_execute()`.
/// For task-level control, use `compute_network_edge_security_services_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_delete(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEdgeSecurityServicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.networkEdgeSecurityService,
        &args.requestId,
    )?;
    compute_network_edge_security_services_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Gets a specified NetworkEdgeSecurityService.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_get_execute()` to send, or `compute_network_edge_security_services_get` for simplest API.

pub fn compute_network_edge_security_services_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkEdgeSecurityService: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEdgeSecurityServices/{}",
        project,
        region,
        networkEdgeSecurityService,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Gets a specified NetworkEdgeSecurityService.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_edge_security_services_get_execute()` or `compute_network_edge_security_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEdgeSecurityService>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEdgeSecurityService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Gets a specified NetworkEdgeSecurityService.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_edge_security_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_edge_security_services_get_task()`.
/// For the simplest API, use `compute_network_edge_security_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_edge_security_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEdgeSecurityService>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_edge_security_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_edge_security_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkEdgeSecurityService
    pub networkEdgeSecurityService: String,
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Gets a specified NetworkEdgeSecurityService.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_get_builder()` + `compute_network_edge_security_services_get_execute()`.
/// For task-level control, use `compute_network_edge_security_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_get(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEdgeSecurityServicesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEdgeSecurityService>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_get_builder(
        client,
        &args.project,
        &args.region,
        &args.networkEdgeSecurityService,
    )?;
    compute_network_edge_security_services_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/networkEdgeSecurityServices
/// Creates a new service in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_insert_execute()` to send, or `compute_network_edge_security_services_insert` for simplest API.

pub fn compute_network_edge_security_services_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEdgeSecurityServices",
        project,
        region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/networkEdgeSecurityServices
/// Creates a new service in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_edge_security_services_insert_execute()` or `compute_network_edge_security_services_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/networkEdgeSecurityServices
/// Creates a new service in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_edge_security_services_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_edge_security_services_insert_task()`.
/// For the simplest API, use `compute_network_edge_security_services_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_edge_security_services_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_edge_security_services_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_edge_security_services_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/networkEdgeSecurityServices
/// Creates a new service in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_insert_builder()` + `compute_network_edge_security_services_insert_execute()`.
/// For task-level control, use `compute_network_edge_security_services_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_insert(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEdgeSecurityServicesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
        &args.validateOnly,
    )?;
    compute_network_edge_security_services_insert_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Patches the specified policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_patch_execute()` to send, or `compute_network_edge_security_services_patch` for simplest API.

pub fn compute_network_edge_security_services_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkEdgeSecurityService: &String,
    paths: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEdgeSecurityServices/{}",
        project,
        region,
        networkEdgeSecurityService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = paths.as_ref() {
        query_parts.push(format!("paths={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_edge_security_services_patch_execute()` or `compute_network_edge_security_services_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_edge_security_services_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_edge_security_services_patch_task()`.
/// For the simplest API, use `compute_network_edge_security_services_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_edge_security_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_edge_security_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_edge_security_services_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_edge_security_services_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkEdgeSecurityService
    pub networkEdgeSecurityService: String,
    /// Query parameter: paths
    pub paths: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Patches the specified policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_patch_builder()` + `compute_network_edge_security_services_patch_execute()`.
/// For task-level control, use `compute_network_edge_security_services_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_patch(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEdgeSecurityServicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.networkEdgeSecurityService,
        &args.paths,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_network_edge_security_services_patch_execute(builder)
}

/// GET projects/{project}/aggregated/networkEndpointGroups
/// Retrieves the list of network endpoint groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_aggregated_list_execute()` to send, or `compute_network_endpoint_groups_aggregated_list` for simplest API.

pub fn compute_network_endpoint_groups_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/networkEndpointGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/networkEndpointGroups
/// Retrieves the list of network endpoint groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_aggregated_list_execute()` or `compute_network_endpoint_groups_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/networkEndpointGroups
/// Retrieves the list of network endpoint groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_aggregated_list_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/networkEndpointGroups
/// Retrieves the list of network endpoint groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_aggregated_list_builder()` + `compute_network_endpoint_groups_aggregated_list_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_network_endpoint_groups_aggregated_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_attach_network_endpoints_execute()` to send, or `compute_network_endpoint_groups_attach_network_endpoints` for simplest API.

pub fn compute_network_endpoint_groups_attach_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}/attachNetworkEndpoints",
        project,
        zone,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_attach_network_endpoints_execute()` or `compute_network_endpoint_groups_attach_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_attach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_attach_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_attach_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_attach_network_endpoints_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_attach_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_attach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_attach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_attach_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_attach_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_attach_network_endpoints_builder()` + `compute_network_endpoint_groups_attach_network_endpoints_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_attach_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_attach_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_attach_network_endpoints_builder(
        client,
        &args.project,
        &args.zone,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_network_endpoint_groups_attach_network_endpoints_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. The network endpoints in the NEG and the VM instances they belong to are not terminated when the NEG is deleted. Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_delete_execute()` to send, or `compute_network_endpoint_groups_delete` for simplest API.

pub fn compute_network_endpoint_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}",
        project, zone, networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. The network endpoints in the NEG and the VM instances they belong to are not terminated when the NEG is deleted. Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_delete_execute()` or `compute_network_endpoint_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. The network endpoints in the NEG and the VM instances they belong to are not terminated when the NEG is deleted. Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_delete_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. The network endpoints in the NEG and the VM instances they belong to are not terminated when the NEG is deleted. Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_delete_builder()` + `compute_network_endpoint_groups_delete_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_network_endpoint_groups_delete_execute(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach a list of network endpoints from the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_detach_network_endpoints_execute()` to send, or `compute_network_endpoint_groups_detach_network_endpoints` for simplest API.

pub fn compute_network_endpoint_groups_detach_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}/detachNetworkEndpoints",
        project,
        zone,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach a list of network endpoints from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_detach_network_endpoints_execute()` or `compute_network_endpoint_groups_detach_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_detach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_detach_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach a list of network endpoints from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_detach_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_detach_network_endpoints_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_detach_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_detach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_detach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_detach_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_detach_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach a list of network endpoints from the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_detach_network_endpoints_builder()` + `compute_network_endpoint_groups_detach_network_endpoints_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_detach_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_detach_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_detach_network_endpoints_builder(
        client,
        &args.project,
        &args.zone,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_network_endpoint_groups_detach_network_endpoints_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_get_execute()` to send, or `compute_network_endpoint_groups_get` for simplest API.

pub fn compute_network_endpoint_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    networkEndpointGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}",
        project, zone, networkEndpointGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_get_execute()` or `compute_network_endpoint_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_get_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_get_builder()` + `compute_network_endpoint_groups_get_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.networkEndpointGroup,
    )?;
    compute_network_endpoint_groups_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_insert_execute()` to send, or `compute_network_endpoint_groups_insert` for simplest API.

pub fn compute_network_endpoint_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_insert_execute()` or `compute_network_endpoint_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_insert_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_insert_builder()` + `compute_network_endpoint_groups_insert_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
    )?;
    compute_network_endpoint_groups_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project and zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_list_execute()` to send, or `compute_network_endpoint_groups_list` for simplest API.

pub fn compute_network_endpoint_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_list_execute()` or `compute_network_endpoint_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_list_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project and zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_list_builder()` + `compute_network_endpoint_groups_list_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_network_endpoint_groups_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_list_network_endpoints_execute()` to send, or `compute_network_endpoint_groups_list_network_endpoints` for simplest API.

pub fn compute_network_endpoint_groups_list_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    networkEndpointGroup: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}/listNetworkEndpoints",
        project,
        zone,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_list_network_endpoints_execute()` or `compute_network_endpoint_groups_list_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_list_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_list_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_list_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_list_network_endpoints_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_list_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_list_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_list_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_list_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_list_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsListNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_list_network_endpoints_builder()` + `compute_network_endpoint_groups_list_network_endpoints_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_list_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_list_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsListNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_list_network_endpoints_builder(
        client,
        &args.project,
        &args.zone,
        &args.networkEndpointGroup,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_network_endpoint_groups_list_network_endpoints_execute(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_test_iam_permissions_execute()` to send, or `compute_network_endpoint_groups_test_iam_permissions` for simplest API.

pub fn compute_network_endpoint_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_endpoint_groups_test_iam_permissions_execute()` or `compute_network_endpoint_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_endpoint_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_endpoint_groups_test_iam_permissions_task()`.
/// For the simplest API, use `compute_network_endpoint_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_endpoint_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_endpoint_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_endpoint_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_endpoint_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_test_iam_permissions_builder()` + `compute_network_endpoint_groups_test_iam_permissions_execute()`.
/// For task-level control, use `compute_network_endpoint_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeNetworkEndpointGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_network_endpoint_groups_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_add_association_execute()` to send, or `compute_network_firewall_policies_add_association` for simplest API.

pub fn compute_network_firewall_policies_add_association_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    replaceExistingAssociation: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/addAssociation",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = replaceExistingAssociation.as_ref() {
        query_parts.push(format!("replaceExistingAssociation={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_add_association_execute()` or `compute_network_firewall_policies_add_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_add_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_add_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_add_association_task()`.
/// For the simplest API, use `compute_network_firewall_policies_add_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_add_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_add_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_add_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_add_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesAddAssociationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: replaceExistingAssociation
    pub replaceExistingAssociation: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_add_association_builder()` + `compute_network_firewall_policies_add_association_execute()`.
/// For task-level control, use `compute_network_firewall_policies_add_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_association(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesAddAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_add_association_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.replaceExistingAssociation,
        &args.requestId,
    )?;
    compute_network_firewall_policies_add_association_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule
/// Inserts a packet mirroring rule into a firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_add_packet_mirroring_rule_execute()` to send, or `compute_network_firewall_policies_add_packet_mirroring_rule` for simplest API.

pub fn compute_network_firewall_policies_add_packet_mirroring_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    maxPriority: &Option<Option<String>>,
    minPriority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/addPacketMirroringRule",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxPriority.as_ref() {
        query_parts.push(format!("maxPriority={}", val));
    }
    if let Some(val) = minPriority.as_ref() {
        query_parts.push(format!("minPriority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule
/// Inserts a packet mirroring rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_add_packet_mirroring_rule_execute()` or `compute_network_firewall_policies_add_packet_mirroring_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_add_packet_mirroring_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_packet_mirroring_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule
/// Inserts a packet mirroring rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_add_packet_mirroring_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_add_packet_mirroring_rule_task()`.
/// For the simplest API, use `compute_network_firewall_policies_add_packet_mirroring_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_add_packet_mirroring_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_add_packet_mirroring_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_add_packet_mirroring_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_add_packet_mirroring_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: maxPriority
    pub maxPriority: Option<Option<String>>,
    /// Query parameter: minPriority
    pub minPriority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule
/// Inserts a packet mirroring rule into a firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_add_packet_mirroring_rule_builder()` + `compute_network_firewall_policies_add_packet_mirroring_rule_execute()`.
/// For task-level control, use `compute_network_firewall_policies_add_packet_mirroring_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_packet_mirroring_rule(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_add_packet_mirroring_rule_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.maxPriority,
        &args.minPriority,
        &args.requestId,
    )?;
    compute_network_firewall_policies_add_packet_mirroring_rule_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_add_rule_execute()` to send, or `compute_network_firewall_policies_add_rule` for simplest API.

pub fn compute_network_firewall_policies_add_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    maxPriority: &Option<Option<String>>,
    minPriority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/addRule",
        project, firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxPriority.as_ref() {
        query_parts.push(format!("maxPriority={}", val));
    }
    if let Some(val) = minPriority.as_ref() {
        query_parts.push(format!("minPriority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_add_rule_execute()` or `compute_network_firewall_policies_add_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_add_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_add_rule_task()`.
/// For the simplest API, use `compute_network_firewall_policies_add_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_add_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_add_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesAddRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: maxPriority
    pub maxPriority: Option<Option<String>>,
    /// Query parameter: minPriority
    pub minPriority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_add_rule_builder()` + `compute_network_firewall_policies_add_rule_execute()`.
/// For task-level control, use `compute_network_firewall_policies_add_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_rule(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesAddRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_add_rule_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.maxPriority,
        &args.minPriority,
        &args.requestId,
    )?;
    compute_network_firewall_policies_add_rule_execute(builder)
}

/// GET projects/{project}/aggregated/firewallPolicies
/// Retrieves an aggregated list of network firewall policies, listing network firewall policies from all applicable scopes (global and regional) and grouping the results per scope.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_aggregated_list_execute()` to send, or `compute_network_firewall_policies_aggregated_list` for simplest API.

pub fn compute_network_firewall_policies_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/firewallPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/firewallPolicies
/// Retrieves an aggregated list of network firewall policies, listing network firewall policies from all applicable scopes (global and regional) and grouping the results per scope.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_aggregated_list_execute()` or `compute_network_firewall_policies_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkFirewallPolicyAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkFirewallPolicyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/firewallPolicies
/// Retrieves an aggregated list of network firewall policies, listing network firewall policies from all applicable scopes (global and regional) and grouping the results per scope.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_aggregated_list_task()`.
/// For the simplest API, use `compute_network_firewall_policies_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkFirewallPolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/firewallPolicies
/// Retrieves an aggregated list of network firewall policies, listing network firewall policies from all applicable scopes (global and regional) and grouping the results per scope.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_aggregated_list_builder()` + `compute_network_firewall_policies_aggregated_list_execute()`.
/// For task-level control, use `compute_network_firewall_policies_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkFirewallPolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_network_firewall_policies_aggregated_list_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_clone_rules_execute()` to send, or `compute_network_firewall_policies_clone_rules` for simplest API.

pub fn compute_network_firewall_policies_clone_rules_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
    sourceFirewallPolicy: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/cloneRules",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceFirewallPolicy.as_ref() {
        query_parts.push(format!("sourceFirewallPolicy={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_clone_rules_execute()` or `compute_network_firewall_policies_clone_rules`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_clone_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_clone_rules_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_clone_rules_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_clone_rules_task()`.
/// For the simplest API, use `compute_network_firewall_policies_clone_rules()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_clone_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_clone_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_clone_rules_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_clone_rules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesCloneRulesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceFirewallPolicy
    pub sourceFirewallPolicy: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_clone_rules_builder()` + `compute_network_firewall_policies_clone_rules_execute()`.
/// For task-level control, use `compute_network_firewall_policies_clone_rules_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_clone_rules(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesCloneRulesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_clone_rules_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.requestId,
        &args.sourceFirewallPolicy,
    )?;
    compute_network_firewall_policies_clone_rules_execute(builder)
}

/// DELETE projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_delete_execute()` to send, or `compute_network_firewall_policies_delete` for simplest API.

pub fn compute_network_firewall_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}",
        project, firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_delete_execute()` or `compute_network_firewall_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_delete_task()`.
/// For the simplest API, use `compute_network_firewall_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_delete_builder()` + `compute_network_firewall_policies_delete_execute()`.
/// For task-level control, use `compute_network_firewall_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_delete_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.requestId,
    )?;
    compute_network_firewall_policies_delete_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_execute()` to send, or `compute_network_firewall_policies_get` for simplest API.

pub fn compute_network_firewall_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}",
        project, firewallPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_get_execute()` or `compute_network_firewall_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_get_task()`.
/// For the simplest API, use `compute_network_firewall_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_builder()` + `compute_network_firewall_policies_get_execute()`.
/// For task-level control, use `compute_network_firewall_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_network_firewall_policies_get_builder(client, &args.project, &args.firewallPolicy)?;
    compute_network_firewall_policies_get_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_association_execute()` to send, or `compute_network_firewall_policies_get_association` for simplest API.

pub fn compute_network_firewall_policies_get_association_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    name: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/getAssociation",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_get_association_execute()` or `compute_network_firewall_policies_get_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_get_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_get_association_task()`.
/// For the simplest API, use `compute_network_firewall_policies_get_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_get_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_get_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_get_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetAssociationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_association_builder()` + `compute_network_firewall_policies_get_association_execute()`.
/// For task-level control, use `compute_network_firewall_policies_get_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_association(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesGetAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_association_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.name,
    )?;
    compute_network_firewall_policies_get_association_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_iam_policy_execute()` to send, or `compute_network_firewall_policies_get_iam_policy` for simplest API.

pub fn compute_network_firewall_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/getIamPolicy",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_get_iam_policy_execute()` or `compute_network_firewall_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_get_iam_policy_task()`.
/// For the simplest API, use `compute_network_firewall_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_iam_policy_builder()` + `compute_network_firewall_policies_get_iam_policy_execute()`.
/// For task-level control, use `compute_network_firewall_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_network_firewall_policies_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule
/// Gets a packet mirroring rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_packet_mirroring_rule_execute()` to send, or `compute_network_firewall_policies_get_packet_mirroring_rule` for simplest API.

pub fn compute_network_firewall_policies_get_packet_mirroring_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/getPacketMirroringRule",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule
/// Gets a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_get_packet_mirroring_rule_execute()` or `compute_network_firewall_policies_get_packet_mirroring_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_packet_mirroring_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_packet_mirroring_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule
/// Gets a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_get_packet_mirroring_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_get_packet_mirroring_rule_task()`.
/// For the simplest API, use `compute_network_firewall_policies_get_packet_mirroring_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_packet_mirroring_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_get_packet_mirroring_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_get_packet_mirroring_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_get_packet_mirroring_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule
/// Gets a packet mirroring rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_packet_mirroring_rule_builder()` + `compute_network_firewall_policies_get_packet_mirroring_rule_execute()`.
/// For task-level control, use `compute_network_firewall_policies_get_packet_mirroring_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_packet_mirroring_rule(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_packet_mirroring_rule_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.priority,
    )?;
    compute_network_firewall_policies_get_packet_mirroring_rule_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_rule_execute()` to send, or `compute_network_firewall_policies_get_rule` for simplest API.

pub fn compute_network_firewall_policies_get_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/getRule",
        project, firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_get_rule_execute()` or `compute_network_firewall_policies_get_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_get_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_get_rule_task()`.
/// For the simplest API, use `compute_network_firewall_policies_get_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_get_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_get_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_rule_builder()` + `compute_network_firewall_policies_get_rule_execute()`.
/// For task-level control, use `compute_network_firewall_policies_get_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_rule(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesGetRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_rule_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.priority,
    )?;
    compute_network_firewall_policies_get_rule_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_insert_execute()` to send, or `compute_network_firewall_policies_insert` for simplest API.

pub fn compute_network_firewall_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_insert_execute()` or `compute_network_firewall_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_insert_task()`.
/// For the simplest API, use `compute_network_firewall_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_insert_builder()` + `compute_network_firewall_policies_insert_execute()`.
/// For task-level control, use `compute_network_firewall_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_network_firewall_policies_insert_builder(client, &args.project, &args.requestId)?;
    compute_network_firewall_policies_insert_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies
/// Lists all the policies that have been configured for the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_list_execute()` to send, or `compute_network_firewall_policies_list` for simplest API.

pub fn compute_network_firewall_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/firewallPolicies
/// Lists all the policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_list_execute()` or `compute_network_firewall_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/firewallPolicies
/// Lists all the policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_list_task()`.
/// For the simplest API, use `compute_network_firewall_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/firewallPolicies
/// Lists all the policies that have been configured for the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_list_builder()` + `compute_network_firewall_policies_list_execute()`.
/// For task-level control, use `compute_network_firewall_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_network_firewall_policies_list_execute(builder)
}

/// PATCH projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_patch_execute()` to send, or `compute_network_firewall_policies_patch` for simplest API.

pub fn compute_network_firewall_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}",
        project, firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_patch_execute()` or `compute_network_firewall_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_patch_task()`.
/// For the simplest API, use `compute_network_firewall_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_patch_builder()` + `compute_network_firewall_policies_patch_execute()`.
/// For task-level control, use `compute_network_firewall_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_patch_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.requestId,
    )?;
    compute_network_firewall_policies_patch_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule
/// Patches a packet mirroring rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_patch_packet_mirroring_rule_execute()` to send, or `compute_network_firewall_policies_patch_packet_mirroring_rule` for simplest API.

pub fn compute_network_firewall_policies_patch_packet_mirroring_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/patchPacketMirroringRule",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule
/// Patches a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_patch_packet_mirroring_rule_execute()` or `compute_network_firewall_policies_patch_packet_mirroring_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_patch_packet_mirroring_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_packet_mirroring_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule
/// Patches a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_patch_packet_mirroring_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_patch_packet_mirroring_rule_task()`.
/// For the simplest API, use `compute_network_firewall_policies_patch_packet_mirroring_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_patch_packet_mirroring_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_patch_packet_mirroring_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_patch_packet_mirroring_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_patch_packet_mirroring_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule
/// Patches a packet mirroring rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_patch_packet_mirroring_rule_builder()` + `compute_network_firewall_policies_patch_packet_mirroring_rule_execute()`.
/// For task-level control, use `compute_network_firewall_policies_patch_packet_mirroring_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_packet_mirroring_rule(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_patch_packet_mirroring_rule_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_network_firewall_policies_patch_packet_mirroring_rule_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_patch_rule_execute()` to send, or `compute_network_firewall_policies_patch_rule` for simplest API.

pub fn compute_network_firewall_policies_patch_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/patchRule",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_patch_rule_execute()` or `compute_network_firewall_policies_patch_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_patch_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_patch_rule_task()`.
/// For the simplest API, use `compute_network_firewall_policies_patch_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_patch_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_patch_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesPatchRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_patch_rule_builder()` + `compute_network_firewall_policies_patch_rule_execute()`.
/// For task-level control, use `compute_network_firewall_policies_patch_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_rule(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesPatchRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_patch_rule_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_network_firewall_policies_patch_rule_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_remove_association_execute()` to send, or `compute_network_firewall_policies_remove_association` for simplest API.

pub fn compute_network_firewall_policies_remove_association_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    name: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/removeAssociation",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_remove_association_execute()` or `compute_network_firewall_policies_remove_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_remove_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_remove_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_remove_association_task()`.
/// For the simplest API, use `compute_network_firewall_policies_remove_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_remove_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_remove_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_remove_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_remove_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesRemoveAssociationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_remove_association_builder()` + `compute_network_firewall_policies_remove_association_execute()`.
/// For task-level control, use `compute_network_firewall_policies_remove_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_association(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesRemoveAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_remove_association_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.name,
        &args.requestId,
    )?;
    compute_network_firewall_policies_remove_association_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule
/// Deletes a packet mirroring rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_remove_packet_mirroring_rule_execute()` to send, or `compute_network_firewall_policies_remove_packet_mirroring_rule` for simplest API.

pub fn compute_network_firewall_policies_remove_packet_mirroring_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/removePacketMirroringRule",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule
/// Deletes a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_remove_packet_mirroring_rule_execute()` or `compute_network_firewall_policies_remove_packet_mirroring_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_remove_packet_mirroring_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_packet_mirroring_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule
/// Deletes a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_remove_packet_mirroring_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_remove_packet_mirroring_rule_task()`.
/// For the simplest API, use `compute_network_firewall_policies_remove_packet_mirroring_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_remove_packet_mirroring_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_remove_packet_mirroring_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_remove_packet_mirroring_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_remove_packet_mirroring_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule
/// Deletes a packet mirroring rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_remove_packet_mirroring_rule_builder()` + `compute_network_firewall_policies_remove_packet_mirroring_rule_execute()`.
/// For task-level control, use `compute_network_firewall_policies_remove_packet_mirroring_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_packet_mirroring_rule(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_remove_packet_mirroring_rule_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_network_firewall_policies_remove_packet_mirroring_rule_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_remove_rule_execute()` to send, or `compute_network_firewall_policies_remove_rule` for simplest API.

pub fn compute_network_firewall_policies_remove_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/removeRule",
        project,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_remove_rule_execute()` or `compute_network_firewall_policies_remove_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_remove_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_remove_rule_task()`.
/// For the simplest API, use `compute_network_firewall_policies_remove_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_remove_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_remove_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesRemoveRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_remove_rule_builder()` + `compute_network_firewall_policies_remove_rule_execute()`.
/// For task-level control, use `compute_network_firewall_policies_remove_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_rule(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesRemoveRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_remove_rule_builder(
        client,
        &args.project,
        &args.firewallPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_network_firewall_policies_remove_rule_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_set_iam_policy_execute()` to send, or `compute_network_firewall_policies_set_iam_policy` for simplest API.

pub fn compute_network_firewall_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/setIamPolicy",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_set_iam_policy_execute()` or `compute_network_firewall_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_set_iam_policy_task()`.
/// For the simplest API, use `compute_network_firewall_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_set_iam_policy_builder()` + `compute_network_firewall_policies_set_iam_policy_execute()`.
/// For task-level control, use `compute_network_firewall_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_set_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_network_firewall_policies_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_test_iam_permissions_execute()` to send, or `compute_network_firewall_policies_test_iam_permissions` for simplest API.

pub fn compute_network_firewall_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_firewall_policies_test_iam_permissions_execute()` or `compute_network_firewall_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_firewall_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_firewall_policies_test_iam_permissions_task()`.
/// For the simplest API, use `compute_network_firewall_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_firewall_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_firewall_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_firewall_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_firewall_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_test_iam_permissions_builder()` + `compute_network_firewall_policies_test_iam_permissions_execute()`.
/// For task-level control, use `compute_network_firewall_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeNetworkFirewallPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_network_firewall_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/networkProfiles/{networkProfile}
/// Returns the specified network profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_profiles_get_execute()` to send, or `compute_network_profiles_get` for simplest API.

pub fn compute_network_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    networkProfile: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkProfiles/{}",
        project, networkProfile,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/networkProfiles/{networkProfile}
/// Returns the specified network profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_profiles_get_execute()` or `compute_network_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/networkProfiles/{networkProfile}
/// Returns the specified network profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_profiles_get_task()`.
/// For the simplest API, use `compute_network_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkProfilesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: networkProfile
    pub networkProfile: String,
}

/// GET projects/{project}/global/networkProfiles/{networkProfile}
/// Returns the specified network profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_profiles_get_builder()` + `compute_network_profiles_get_execute()`.
/// For task-level control, use `compute_network_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_profiles_get(
    client: &SimpleHttpClient,
    args: &ComputeNetworkProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_network_profiles_get_builder(client, &args.project, &args.networkProfile)?;
    compute_network_profiles_get_execute(builder)
}

/// GET projects/{project}/global/networkProfiles
/// Retrieves a list of network profiles available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_profiles_list_execute()` to send, or `compute_network_profiles_list` for simplest API.

pub fn compute_network_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkProfiles",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/networkProfiles
/// Retrieves a list of network profiles available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_network_profiles_list_execute()` or `compute_network_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkProfilesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkProfilesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/networkProfiles
/// Retrieves a list of network profiles available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_network_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_network_profiles_list_task()`.
/// For the simplest API, use `compute_network_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_network_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_network_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkProfilesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_network_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_network_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworkProfilesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/networkProfiles
/// Retrieves a list of network profiles available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_profiles_list_builder()` + `compute_network_profiles_list_execute()`.
/// For task-level control, use `compute_network_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_profiles_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworkProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkProfilesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_profiles_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_network_profiles_list_execute(builder)
}

/// POST projects/{project}/global/networks/{network}/addPeering
/// Adds a peering to the specified network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_add_peering_execute()` to send, or `compute_networks_add_peering` for simplest API.

pub fn compute_networks_add_peering_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/addPeering",
        project, network,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networks/{network}/addPeering
/// Adds a peering to the specified network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_add_peering_execute()` or `compute_networks_add_peering`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_add_peering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_add_peering_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networks/{network}/addPeering
/// Adds a peering to the specified network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_add_peering_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_add_peering_task()`.
/// For the simplest API, use `compute_networks_add_peering()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_add_peering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_add_peering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_add_peering_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_add_peering`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksAddPeeringArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/networks/{network}/addPeering
/// Adds a peering to the specified network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_add_peering_builder()` + `compute_networks_add_peering_execute()`.
/// For task-level control, use `compute_networks_add_peering_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_add_peering(
    client: &SimpleHttpClient,
    args: &ComputeNetworksAddPeeringArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_add_peering_builder(
        client,
        &args.project,
        &args.network,
        &args.requestId,
    )?;
    compute_networks_add_peering_execute(builder)
}

/// DELETE projects/{project}/global/networks/{network}
/// Deletes the specified network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_delete_execute()` to send, or `compute_networks_delete` for simplest API.

pub fn compute_networks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}",
        project, network,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/networks/{network}
/// Deletes the specified network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_delete_execute()` or `compute_networks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/networks/{network}
/// Deletes the specified network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_delete_task()`.
/// For the simplest API, use `compute_networks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/networks/{network}
/// Deletes the specified network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_delete_builder()` + `compute_networks_delete_execute()`.
/// For task-level control, use `compute_networks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_delete(
    client: &SimpleHttpClient,
    args: &ComputeNetworksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_networks_delete_builder(client, &args.project, &args.network, &args.requestId)?;
    compute_networks_delete_execute(builder)
}

/// GET projects/{project}/global/networks/{network}
/// Returns the specified network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_get_execute()` to send, or `compute_networks_get` for simplest API.

pub fn compute_networks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}",
        project, network,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/networks/{network}
/// Returns the specified network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_get_execute()` or `compute_networks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Network>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Network = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/networks/{network}
/// Returns the specified network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_get_task()`.
/// For the simplest API, use `compute_networks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
}

/// GET projects/{project}/global/networks/{network}
/// Returns the specified network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_get_builder()` + `compute_networks_get_execute()`.
/// For task-level control, use `compute_networks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_get(
    client: &SimpleHttpClient,
    args: &ComputeNetworksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_get_builder(client, &args.project, &args.network)?;
    compute_networks_get_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_get_effective_firewalls_execute()` to send, or `compute_networks_get_effective_firewalls` for simplest API.

pub fn compute_networks_get_effective_firewalls_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/getEffectiveFirewalls",
        project,
        network,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_get_effective_firewalls_execute()` or `compute_networks_get_effective_firewalls`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_get_effective_firewalls_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_get_effective_firewalls_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworksGetEffectiveFirewallsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworksGetEffectiveFirewallsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_get_effective_firewalls_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_get_effective_firewalls_task()`.
/// For the simplest API, use `compute_networks_get_effective_firewalls()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_get_effective_firewalls_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_get_effective_firewalls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworksGetEffectiveFirewallsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_networks_get_effective_firewalls_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_get_effective_firewalls`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksGetEffectiveFirewallsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
}

/// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_get_effective_firewalls_builder()` + `compute_networks_get_effective_firewalls_execute()`.
/// For task-level control, use `compute_networks_get_effective_firewalls_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_get_effective_firewalls(
    client: &SimpleHttpClient,
    args: &ComputeNetworksGetEffectiveFirewallsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworksGetEffectiveFirewallsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_networks_get_effective_firewalls_builder(client, &args.project, &args.network)?;
    compute_networks_get_effective_firewalls_execute(builder)
}

/// POST projects/{project}/global/networks
/// Creates a network in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_insert_execute()` to send, or `compute_networks_insert` for simplest API.

pub fn compute_networks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networks
/// Creates a network in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_insert_execute()` or `compute_networks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networks
/// Creates a network in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_insert_task()`.
/// For the simplest API, use `compute_networks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/networks
/// Creates a network in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_insert_builder()` + `compute_networks_insert_execute()`.
/// For task-level control, use `compute_networks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_insert(
    client: &SimpleHttpClient,
    args: &ComputeNetworksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_insert_builder(client, &args.project, &args.requestId)?;
    compute_networks_insert_execute(builder)
}

/// GET projects/{project}/global/networks
/// Retrieves the list of networks available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_list_execute()` to send, or `compute_networks_list` for simplest API.

pub fn compute_networks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/networks
/// Retrieves the list of networks available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_list_execute()` or `compute_networks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/networks
/// Retrieves the list of networks available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_list_task()`.
/// For the simplest API, use `compute_networks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/networks
/// Retrieves the list of networks available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_list_builder()` + `compute_networks_list_execute()`.
/// For task-level control, use `compute_networks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_list(
    client: &SimpleHttpClient,
    args: &ComputeNetworksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_networks_list_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/listPeeringRoutes
/// Lists the peering routes exchanged over peering connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_list_peering_routes_execute()` to send, or `compute_networks_list_peering_routes` for simplest API.

pub fn compute_networks_list_peering_routes_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
    direction: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    peeringName: &Option<Option<String>>,
    region: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/listPeeringRoutes",
        project,
        network,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = direction.as_ref() {
        query_parts.push(format!("direction={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = peeringName.as_ref() {
        query_parts.push(format!("peeringName={}", val));
    }
    if let Some(val) = region.as_ref() {
        query_parts.push(format!("region={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/networks/{network}/listPeeringRoutes
/// Lists the peering routes exchanged over peering connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_list_peering_routes_execute()` or `compute_networks_list_peering_routes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_list_peering_routes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_list_peering_routes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExchangedPeeringRoutesList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExchangedPeeringRoutesList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/networks/{network}/listPeeringRoutes
/// Lists the peering routes exchanged over peering connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_list_peering_routes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_list_peering_routes_task()`.
/// For the simplest API, use `compute_networks_list_peering_routes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_list_peering_routes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_list_peering_routes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExchangedPeeringRoutesList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_networks_list_peering_routes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_list_peering_routes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksListPeeringRoutesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
    /// Query parameter: direction
    pub direction: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: peeringName
    pub peeringName: Option<Option<String>>,
    /// Query parameter: region
    pub region: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/networks/{network}/listPeeringRoutes
/// Lists the peering routes exchanged over peering connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_list_peering_routes_builder()` + `compute_networks_list_peering_routes_execute()`.
/// For task-level control, use `compute_networks_list_peering_routes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_list_peering_routes(
    client: &SimpleHttpClient,
    args: &ComputeNetworksListPeeringRoutesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExchangedPeeringRoutesList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_networks_list_peering_routes_builder(
        client,
        &args.project,
        &args.network,
        &args.direction,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.peeringName,
        &args.region,
        &args.returnPartialSuccess,
    )?;
    compute_networks_list_peering_routes_execute(builder)
}

/// PATCH projects/{project}/global/networks/{network}
/// Patches the specified network with the data included in the request. Only `routingConfig` can be modified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_patch_execute()` to send, or `compute_networks_patch` for simplest API.

pub fn compute_networks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}",
        project, network,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/networks/{network}
/// Patches the specified network with the data included in the request. Only `routingConfig` can be modified.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_patch_execute()` or `compute_networks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/networks/{network}
/// Patches the specified network with the data included in the request. Only `routingConfig` can be modified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_patch_task()`.
/// For the simplest API, use `compute_networks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/networks/{network}
/// Patches the specified network with the data included in the request. Only `routingConfig` can be modified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_patch_builder()` + `compute_networks_patch_execute()`.
/// For task-level control, use `compute_networks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_patch(
    client: &SimpleHttpClient,
    args: &ComputeNetworksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_networks_patch_builder(client, &args.project, &args.network, &args.requestId)?;
    compute_networks_patch_execute(builder)
}

/// POST projects/{project}/global/networks/{network}/removePeering
/// Removes a peering from the specified network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_remove_peering_execute()` to send, or `compute_networks_remove_peering` for simplest API.

pub fn compute_networks_remove_peering_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/removePeering",
        project, network,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networks/{network}/removePeering
/// Removes a peering from the specified network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_remove_peering_execute()` or `compute_networks_remove_peering`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_remove_peering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_remove_peering_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networks/{network}/removePeering
/// Removes a peering from the specified network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_remove_peering_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_remove_peering_task()`.
/// For the simplest API, use `compute_networks_remove_peering()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_remove_peering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_remove_peering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_remove_peering_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_remove_peering`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksRemovePeeringArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/networks/{network}/removePeering
/// Removes a peering from the specified network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_remove_peering_builder()` + `compute_networks_remove_peering_execute()`.
/// For task-level control, use `compute_networks_remove_peering_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_remove_peering(
    client: &SimpleHttpClient,
    args: &ComputeNetworksRemovePeeringArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_remove_peering_builder(
        client,
        &args.project,
        &args.network,
        &args.requestId,
    )?;
    compute_networks_remove_peering_execute(builder)
}

/// POST projects/{project}/global/networks/{network}/requestRemovePeering
/// Requests to remove a peering from the specified network. Applicable only for PeeringConnection with update_strategy=CONSENSUS.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_request_remove_peering_execute()` to send, or `compute_networks_request_remove_peering` for simplest API.

pub fn compute_networks_request_remove_peering_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/requestRemovePeering",
        project,
        network,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networks/{network}/requestRemovePeering
/// Requests to remove a peering from the specified network. Applicable only for PeeringConnection with update_strategy=CONSENSUS.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_request_remove_peering_execute()` or `compute_networks_request_remove_peering`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_request_remove_peering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_request_remove_peering_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networks/{network}/requestRemovePeering
/// Requests to remove a peering from the specified network. Applicable only for PeeringConnection with update_strategy=CONSENSUS.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_request_remove_peering_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_request_remove_peering_task()`.
/// For the simplest API, use `compute_networks_request_remove_peering()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_request_remove_peering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_request_remove_peering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_request_remove_peering_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_request_remove_peering`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksRequestRemovePeeringArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/networks/{network}/requestRemovePeering
/// Requests to remove a peering from the specified network. Applicable only for PeeringConnection with update_strategy=CONSENSUS.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_request_remove_peering_builder()` + `compute_networks_request_remove_peering_execute()`.
/// For task-level control, use `compute_networks_request_remove_peering_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_request_remove_peering(
    client: &SimpleHttpClient,
    args: &ComputeNetworksRequestRemovePeeringArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_request_remove_peering_builder(
        client,
        &args.project,
        &args.network,
        &args.requestId,
    )?;
    compute_networks_request_remove_peering_execute(builder)
}

/// POST projects/{project}/global/networks/{network}/switchToCustomMode
/// Switches the network mode from auto subnet mode to custom subnet mode.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_switch_to_custom_mode_execute()` to send, or `compute_networks_switch_to_custom_mode` for simplest API.

pub fn compute_networks_switch_to_custom_mode_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/switchToCustomMode",
        project,
        network,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/networks/{network}/switchToCustomMode
/// Switches the network mode from auto subnet mode to custom subnet mode.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_switch_to_custom_mode_execute()` or `compute_networks_switch_to_custom_mode`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_switch_to_custom_mode_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_switch_to_custom_mode_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/networks/{network}/switchToCustomMode
/// Switches the network mode from auto subnet mode to custom subnet mode.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_switch_to_custom_mode_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_switch_to_custom_mode_task()`.
/// For the simplest API, use `compute_networks_switch_to_custom_mode()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_switch_to_custom_mode_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_switch_to_custom_mode_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_switch_to_custom_mode_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_switch_to_custom_mode`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksSwitchToCustomModeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/networks/{network}/switchToCustomMode
/// Switches the network mode from auto subnet mode to custom subnet mode.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_switch_to_custom_mode_builder()` + `compute_networks_switch_to_custom_mode_execute()`.
/// For task-level control, use `compute_networks_switch_to_custom_mode_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_switch_to_custom_mode(
    client: &SimpleHttpClient,
    args: &ComputeNetworksSwitchToCustomModeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_switch_to_custom_mode_builder(
        client,
        &args.project,
        &args.network,
        &args.requestId,
    )?;
    compute_networks_switch_to_custom_mode_execute(builder)
}

/// PATCH projects/{project}/global/networks/{network}/updatePeering
/// Updates the specified network peering with the data included in the request. You can only modify the NetworkPeering.export_custom_routes field and the NetworkPeering.import_custom_routes field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_update_peering_execute()` to send, or `compute_networks_update_peering` for simplest API.

pub fn compute_networks_update_peering_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    network: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/updatePeering",
        project, network,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/networks/{network}/updatePeering
/// Updates the specified network peering with the data included in the request. You can only modify the NetworkPeering.export_custom_routes field and the NetworkPeering.import_custom_routes field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_networks_update_peering_execute()` or `compute_networks_update_peering`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_update_peering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_update_peering_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/networks/{network}/updatePeering
/// Updates the specified network peering with the data included in the request. You can only modify the NetworkPeering.export_custom_routes field and the NetworkPeering.import_custom_routes field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_networks_update_peering_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_networks_update_peering_task()`.
/// For the simplest API, use `compute_networks_update_peering()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_networks_update_peering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_networks_update_peering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_networks_update_peering_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_networks_update_peering`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNetworksUpdatePeeringArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: network
    pub network: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/networks/{network}/updatePeering
/// Updates the specified network peering with the data included in the request. You can only modify the NetworkPeering.export_custom_routes field and the NetworkPeering.import_custom_routes field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_update_peering_builder()` + `compute_networks_update_peering_execute()`.
/// For task-level control, use `compute_networks_update_peering_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_update_peering(
    client: &SimpleHttpClient,
    args: &ComputeNetworksUpdatePeeringArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_update_peering_builder(
        client,
        &args.project,
        &args.network,
        &args.requestId,
    )?;
    compute_networks_update_peering_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes
/// Adds specified number of nodes to the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_add_nodes_execute()` to send, or `compute_node_groups_add_nodes` for simplest API.

pub fn compute_node_groups_add_nodes_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/addNodes",
        project, zone, nodeGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes
/// Adds specified number of nodes to the node group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_add_nodes_execute()` or `compute_node_groups_add_nodes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_add_nodes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_add_nodes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes
/// Adds specified number of nodes to the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_add_nodes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_add_nodes_task()`.
/// For the simplest API, use `compute_node_groups_add_nodes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_add_nodes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_add_nodes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_add_nodes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_add_nodes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsAddNodesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes
/// Adds specified number of nodes to the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_add_nodes_builder()` + `compute_node_groups_add_nodes_execute()`.
/// For task-level control, use `compute_node_groups_add_nodes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_add_nodes(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsAddNodesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_add_nodes_builder(
        client,
        &args.project,
        &args.zone,
        &args.nodeGroup,
        &args.requestId,
    )?;
    compute_node_groups_add_nodes_execute(builder)
}

/// GET projects/{project}/aggregated/nodeGroups
/// Retrieves an aggregated list of node groups. Note: use `nodeGroups`.`listNodes` for more details about each group.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_aggregated_list_execute()` to send, or `compute_node_groups_aggregated_list` for simplest API.

pub fn compute_node_groups_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/nodeGroups",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/nodeGroups
/// Retrieves an aggregated list of node groups. Note: use `nodeGroups`.`listNodes` for more details about each group.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_aggregated_list_execute()` or `compute_node_groups_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroupAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroupAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/nodeGroups
/// Retrieves an aggregated list of node groups. Note: use `nodeGroups`.`listNodes` for more details about each group.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_aggregated_list_task()`.
/// For the simplest API, use `compute_node_groups_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_groups_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/nodeGroups
/// Retrieves an aggregated list of node groups. Note: use `nodeGroups`.`listNodes` for more details about each group.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_aggregated_list_builder()` + `compute_node_groups_aggregated_list_execute()`.
/// For task-level control, use `compute_node_groups_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_groups_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_node_groups_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Deletes the specified NodeGroup resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_delete_execute()` to send, or `compute_node_groups_delete` for simplest API.

pub fn compute_node_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}",
        project, zone, nodeGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Deletes the specified NodeGroup resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_delete_execute()` or `compute_node_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Deletes the specified NodeGroup resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_delete_task()`.
/// For the simplest API, use `compute_node_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Deletes the specified NodeGroup resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_delete_builder()` + `compute_node_groups_delete_execute()`.
/// For task-level control, use `compute_node_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.nodeGroup,
        &args.requestId,
    )?;
    compute_node_groups_delete_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes
/// Deletes specified nodes from the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_delete_nodes_execute()` to send, or `compute_node_groups_delete_nodes` for simplest API.

pub fn compute_node_groups_delete_nodes_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/deleteNodes",
        project, zone, nodeGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes
/// Deletes specified nodes from the node group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_delete_nodes_execute()` or `compute_node_groups_delete_nodes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_delete_nodes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_delete_nodes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes
/// Deletes specified nodes from the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_delete_nodes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_delete_nodes_task()`.
/// For the simplest API, use `compute_node_groups_delete_nodes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_delete_nodes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_delete_nodes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_delete_nodes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_delete_nodes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsDeleteNodesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes
/// Deletes specified nodes from the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_delete_nodes_builder()` + `compute_node_groups_delete_nodes_execute()`.
/// For task-level control, use `compute_node_groups_delete_nodes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_delete_nodes(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsDeleteNodesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_delete_nodes_builder(
        client,
        &args.project,
        &args.zone,
        &args.nodeGroup,
        &args.requestId,
    )?;
    compute_node_groups_delete_nodes_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Returns the specified NodeGroup. Get a list of available NodeGroups by making a list() request. Note: the "nodes" field should not be used. Use `nodeGroups`.`listNodes` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_get_execute()` to send, or `compute_node_groups_get` for simplest API.

pub fn compute_node_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}",
        project, zone, nodeGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Returns the specified NodeGroup. Get a list of available NodeGroups by making a list() request. Note: the "nodes" field should not be used. Use `nodeGroups`.`listNodes` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_get_execute()` or `compute_node_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Returns the specified NodeGroup. Get a list of available NodeGroups by making a list() request. Note: the "nodes" field should not be used. Use `nodeGroups`.`listNodes` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_get_task()`.
/// For the simplest API, use `compute_node_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Returns the specified NodeGroup. Get a list of available NodeGroups by making a list() request. Note: the "nodes" field should not be used. Use `nodeGroups`.`listNodes` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_get_builder()` + `compute_node_groups_get_execute()`.
/// For task-level control, use `compute_node_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_node_groups_get_builder(client, &args.project, &args.zone, &args.nodeGroup)?;
    compute_node_groups_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_get_iam_policy_execute()` to send, or `compute_node_groups_get_iam_policy` for simplest API.

pub fn compute_node_groups_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/getIamPolicy",
        project, zone, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_get_iam_policy_execute()` or `compute_node_groups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_get_iam_policy_task()`.
/// For the simplest API, use `compute_node_groups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_get_iam_policy_builder()` + `compute_node_groups_get_iam_policy_execute()`.
/// For task-level control, use `compute_node_groups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_node_groups_get_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups
/// Creates a NodeGroup resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_insert_execute()` to send, or `compute_node_groups_insert` for simplest API.

pub fn compute_node_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    initialNodeCount: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = initialNodeCount.as_ref() {
        query_parts.push(format!("initialNodeCount={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups
/// Creates a NodeGroup resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_insert_execute()` or `compute_node_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups
/// Creates a NodeGroup resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_insert_task()`.
/// For the simplest API, use `compute_node_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: initialNodeCount
    pub initialNodeCount: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/nodeGroups
/// Creates a NodeGroup resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_insert_builder()` + `compute_node_groups_insert_execute()`.
/// For task-level control, use `compute_node_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.initialNodeCount,
        &args.requestId,
    )?;
    compute_node_groups_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Retrieves a list of node groups available to the specified project. Note: use `nodeGroups`.`listNodes` for more details about each group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_list_execute()` to send, or `compute_node_groups_list` for simplest API.

pub fn compute_node_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Retrieves a list of node groups available to the specified project. Note: use `nodeGroups`.`listNodes` for more details about each group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_list_execute()` or `compute_node_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroupList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Retrieves a list of node groups available to the specified project. Note: use `nodeGroups`.`listNodes` for more details about each group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_list_task()`.
/// For the simplest API, use `compute_node_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Retrieves a list of node groups available to the specified project. Note: use `nodeGroups`.`listNodes` for more details about each group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_list_builder()` + `compute_node_groups_list_execute()`.
/// For task-level control, use `compute_node_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_groups_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_node_groups_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes
/// Lists nodes in the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_list_nodes_execute()` to send, or `compute_node_groups_list_nodes` for simplest API.

pub fn compute_node_groups_list_nodes_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/listNodes",
        project, zone, nodeGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes
/// Lists nodes in the node group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_list_nodes_execute()` or `compute_node_groups_list_nodes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_list_nodes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_list_nodes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroupsListNodes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroupsListNodes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes
/// Lists nodes in the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_list_nodes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_list_nodes_task()`.
/// For the simplest API, use `compute_node_groups_list_nodes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_list_nodes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_list_nodes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupsListNodes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_groups_list_nodes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_list_nodes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsListNodesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes
/// Lists nodes in the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_list_nodes_builder()` + `compute_node_groups_list_nodes_execute()`.
/// For task-level control, use `compute_node_groups_list_nodes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_list_nodes(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsListNodesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupsListNodes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_groups_list_nodes_builder(
        client,
        &args.project,
        &args.zone,
        &args.nodeGroup,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_node_groups_list_nodes_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Updates the specified node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_patch_execute()` to send, or `compute_node_groups_patch` for simplest API.

pub fn compute_node_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}",
        project, zone, nodeGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Updates the specified node group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_patch_execute()` or `compute_node_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Updates the specified node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_patch_task()`.
/// For the simplest API, use `compute_node_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Updates the specified node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_patch_builder()` + `compute_node_groups_patch_execute()`.
/// For task-level control, use `compute_node_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_patch(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_patch_builder(
        client,
        &args.project,
        &args.zone,
        &args.nodeGroup,
        &args.requestId,
    )?;
    compute_node_groups_patch_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance
/// Perform maintenance on a subset of nodes in the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_perform_maintenance_execute()` to send, or `compute_node_groups_perform_maintenance` for simplest API.

pub fn compute_node_groups_perform_maintenance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/performMaintenance",
        project,
        zone,
        nodeGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance
/// Perform maintenance on a subset of nodes in the node group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_perform_maintenance_execute()` or `compute_node_groups_perform_maintenance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_perform_maintenance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance
/// Perform maintenance on a subset of nodes in the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_perform_maintenance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_perform_maintenance_task()`.
/// For the simplest API, use `compute_node_groups_perform_maintenance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_perform_maintenance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_perform_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsPerformMaintenanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance
/// Perform maintenance on a subset of nodes in the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_perform_maintenance_builder()` + `compute_node_groups_perform_maintenance_execute()`.
/// For task-level control, use `compute_node_groups_perform_maintenance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_perform_maintenance(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsPerformMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_perform_maintenance_builder(
        client,
        &args.project,
        &args.zone,
        &args.nodeGroup,
        &args.requestId,
    )?;
    compute_node_groups_perform_maintenance_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_set_iam_policy_execute()` to send, or `compute_node_groups_set_iam_policy` for simplest API.

pub fn compute_node_groups_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/setIamPolicy",
        project, zone, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_set_iam_policy_execute()` or `compute_node_groups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_set_iam_policy_task()`.
/// For the simplest API, use `compute_node_groups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_set_iam_policy_builder()` + `compute_node_groups_set_iam_policy_execute()`.
/// For task-level control, use `compute_node_groups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_set_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_node_groups_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate
/// Updates the node template of the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_set_node_template_execute()` to send, or `compute_node_groups_set_node_template` for simplest API.

pub fn compute_node_groups_set_node_template_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/setNodeTemplate",
        project,
        zone,
        nodeGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate
/// Updates the node template of the node group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_set_node_template_execute()` or `compute_node_groups_set_node_template`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_set_node_template_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_set_node_template_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate
/// Updates the node template of the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_set_node_template_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_set_node_template_task()`.
/// For the simplest API, use `compute_node_groups_set_node_template()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_set_node_template_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_set_node_template_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_set_node_template_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_set_node_template`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsSetNodeTemplateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate
/// Updates the node template of the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_set_node_template_builder()` + `compute_node_groups_set_node_template_execute()`.
/// For task-level control, use `compute_node_groups_set_node_template_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_set_node_template(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsSetNodeTemplateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_set_node_template_builder(
        client,
        &args.project,
        &args.zone,
        &args.nodeGroup,
        &args.requestId,
    )?;
    compute_node_groups_set_node_template_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent
/// Simulates maintenance event on specified nodes from the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_simulate_maintenance_event_execute()` to send, or `compute_node_groups_simulate_maintenance_event` for simplest API.

pub fn compute_node_groups_simulate_maintenance_event_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/simulateMaintenanceEvent",
        project,
        zone,
        nodeGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent
/// Simulates maintenance event on specified nodes from the node group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_simulate_maintenance_event_execute()` or `compute_node_groups_simulate_maintenance_event`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_simulate_maintenance_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_simulate_maintenance_event_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent
/// Simulates maintenance event on specified nodes from the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_simulate_maintenance_event_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_simulate_maintenance_event_task()`.
/// For the simplest API, use `compute_node_groups_simulate_maintenance_event()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_simulate_maintenance_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_simulate_maintenance_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_groups_simulate_maintenance_event_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_simulate_maintenance_event`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsSimulateMaintenanceEventArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeGroup
    pub nodeGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent
/// Simulates maintenance event on specified nodes from the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_simulate_maintenance_event_builder()` + `compute_node_groups_simulate_maintenance_event_execute()`.
/// For task-level control, use `compute_node_groups_simulate_maintenance_event_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_simulate_maintenance_event(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsSimulateMaintenanceEventArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_simulate_maintenance_event_builder(
        client,
        &args.project,
        &args.zone,
        &args.nodeGroup,
        &args.requestId,
    )?;
    compute_node_groups_simulate_maintenance_event_execute(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_test_iam_permissions_execute()` to send, or `compute_node_groups_test_iam_permissions` for simplest API.

pub fn compute_node_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_groups_test_iam_permissions_execute()` or `compute_node_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_groups_test_iam_permissions_task()`.
/// For the simplest API, use `compute_node_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeGroupsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_test_iam_permissions_builder()` + `compute_node_groups_test_iam_permissions_execute()`.
/// For task-level control, use `compute_node_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeNodeGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_groups_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_node_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/nodeTemplates
/// Retrieves an aggregated list of node templates.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_aggregated_list_execute()` to send, or `compute_node_templates_aggregated_list` for simplest API.

pub fn compute_node_templates_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/nodeTemplates",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/nodeTemplates
/// Retrieves an aggregated list of node templates.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_templates_aggregated_list_execute()` or `compute_node_templates_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTemplateAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTemplateAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/nodeTemplates
/// Retrieves an aggregated list of node templates.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_templates_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_templates_aggregated_list_task()`.
/// For the simplest API, use `compute_node_templates_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_templates_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NodeTemplateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_templates_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_templates_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/nodeTemplates
/// Retrieves an aggregated list of node templates.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_aggregated_list_builder()` + `compute_node_templates_aggregated_list_execute()`.
/// For task-level control, use `compute_node_templates_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeNodeTemplatesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NodeTemplateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_templates_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_node_templates_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Deletes the specified NodeTemplate resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_delete_execute()` to send, or `compute_node_templates_delete` for simplest API.

pub fn compute_node_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    nodeTemplate: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}",
        project, region, nodeTemplate,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Deletes the specified NodeTemplate resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_templates_delete_execute()` or `compute_node_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Deletes the specified NodeTemplate resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_templates_delete_task()`.
/// For the simplest API, use `compute_node_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: nodeTemplate
    pub nodeTemplate: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Deletes the specified NodeTemplate resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_delete_builder()` + `compute_node_templates_delete_execute()`.
/// For task-level control, use `compute_node_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_delete(
    client: &SimpleHttpClient,
    args: &ComputeNodeTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_templates_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.nodeTemplate,
        &args.requestId,
    )?;
    compute_node_templates_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Returns the specified node template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_get_execute()` to send, or `compute_node_templates_get` for simplest API.

pub fn compute_node_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    nodeTemplate: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}",
        project, region, nodeTemplate,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Returns the specified node template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_templates_get_execute()` or `compute_node_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Returns the specified node template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_templates_get_task()`.
/// For the simplest API, use `compute_node_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: nodeTemplate
    pub nodeTemplate: String,
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Returns the specified node template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_get_builder()` + `compute_node_templates_get_execute()`.
/// For task-level control, use `compute_node_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_get(
    client: &SimpleHttpClient,
    args: &ComputeNodeTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_templates_get_builder(
        client,
        &args.project,
        &args.region,
        &args.nodeTemplate,
    )?;
    compute_node_templates_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_get_iam_policy_execute()` to send, or `compute_node_templates_get_iam_policy` for simplest API.

pub fn compute_node_templates_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_templates_get_iam_policy_execute()` or `compute_node_templates_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_templates_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_templates_get_iam_policy_task()`.
/// For the simplest API, use `compute_node_templates_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_templates_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_templates_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_get_iam_policy_builder()` + `compute_node_templates_get_iam_policy_execute()`.
/// For task-level control, use `compute_node_templates_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeNodeTemplatesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_templates_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_node_templates_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/nodeTemplates
/// Creates a NodeTemplate resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_insert_execute()` to send, or `compute_node_templates_insert` for simplest API.

pub fn compute_node_templates_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/nodeTemplates
/// Creates a NodeTemplate resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_templates_insert_execute()` or `compute_node_templates_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/nodeTemplates
/// Creates a NodeTemplate resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_templates_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_templates_insert_task()`.
/// For the simplest API, use `compute_node_templates_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_templates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_templates_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_templates_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/nodeTemplates
/// Creates a NodeTemplate resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_insert_builder()` + `compute_node_templates_insert_execute()`.
/// For task-level control, use `compute_node_templates_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_insert(
    client: &SimpleHttpClient,
    args: &ComputeNodeTemplatesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_templates_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_node_templates_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Retrieves a list of node templates available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_list_execute()` to send, or `compute_node_templates_list` for simplest API.

pub fn compute_node_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Retrieves a list of node templates available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_templates_list_execute()` or `compute_node_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTemplateList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTemplateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Retrieves a list of node templates available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_templates_list_task()`.
/// For the simplest API, use `compute_node_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Retrieves a list of node templates available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_list_builder()` + `compute_node_templates_list_execute()`.
/// For task-level control, use `compute_node_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_list(
    client: &SimpleHttpClient,
    args: &ComputeNodeTemplatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_templates_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_node_templates_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_set_iam_policy_execute()` to send, or `compute_node_templates_set_iam_policy` for simplest API.

pub fn compute_node_templates_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_templates_set_iam_policy_execute()` or `compute_node_templates_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_templates_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_templates_set_iam_policy_task()`.
/// For the simplest API, use `compute_node_templates_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_templates_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_templates_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_set_iam_policy_builder()` + `compute_node_templates_set_iam_policy_execute()`.
/// For task-level control, use `compute_node_templates_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeNodeTemplatesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_templates_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_node_templates_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_test_iam_permissions_execute()` to send, or `compute_node_templates_test_iam_permissions` for simplest API.

pub fn compute_node_templates_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_templates_test_iam_permissions_execute()` or `compute_node_templates_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_templates_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_templates_test_iam_permissions_task()`.
/// For the simplest API, use `compute_node_templates_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_templates_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_templates_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_test_iam_permissions_builder()` + `compute_node_templates_test_iam_permissions_execute()`.
/// For task-level control, use `compute_node_templates_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeNodeTemplatesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_templates_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_node_templates_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/nodeTypes
/// Retrieves an aggregated list of node types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_types_aggregated_list_execute()` to send, or `compute_node_types_aggregated_list` for simplest API.

pub fn compute_node_types_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/nodeTypes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/nodeTypes
/// Retrieves an aggregated list of node types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_types_aggregated_list_execute()` or `compute_node_types_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTypeAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/nodeTypes
/// Retrieves an aggregated list of node types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_types_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_types_aggregated_list_task()`.
/// For the simplest API, use `compute_node_types_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_types_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_types_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTypesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/nodeTypes
/// Retrieves an aggregated list of node types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_types_aggregated_list_builder()` + `compute_node_types_aggregated_list_execute()`.
/// For task-level control, use `compute_node_types_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeNodeTypesAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_types_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_node_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}
/// Returns the specified node type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_types_get_execute()` to send, or `compute_node_types_get` for simplest API.

pub fn compute_node_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    nodeType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeTypes/{}",
        project, zone, nodeType,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}
/// Returns the specified node type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_types_get_execute()` or `compute_node_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}
/// Returns the specified node type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_types_get_task()`.
/// For the simplest API, use `compute_node_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_node_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTypesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: nodeType
    pub nodeType: String,
}

/// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}
/// Returns the specified node type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_types_get_builder()` + `compute_node_types_get_execute()`.
/// For task-level control, use `compute_node_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_get(
    client: &SimpleHttpClient,
    args: &ComputeNodeTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_node_types_get_builder(client, &args.project, &args.zone, &args.nodeType)?;
    compute_node_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeTypes
/// Retrieves a list of node types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_types_list_execute()` to send, or `compute_node_types_list` for simplest API.

pub fn compute_node_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeTypes",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/nodeTypes
/// Retrieves a list of node types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_node_types_list_execute()` or `compute_node_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTypeList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/nodeTypes
/// Retrieves a list of node types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_node_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_node_types_list_task()`.
/// For the simplest API, use `compute_node_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_node_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_node_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_node_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_node_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeNodeTypesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/nodeTypes
/// Retrieves a list of node types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_types_list_builder()` + `compute_node_types_list_execute()`.
/// For task-level control, use `compute_node_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_list(
    client: &SimpleHttpClient,
    args: &ComputeNodeTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_types_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_node_types_list_execute(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/addAssociation
/// Inserts an association for the specified security policy.  This has billing implications.  Projects in the hierarchy with effective hierarchical security policies will be automatically enrolled into Cloud Armor Enterprise if not already enrolled.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addAssociation` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_add_association_execute()` to send, or `compute_organization_security_policies_add_association` for simplest API.

pub fn compute_organization_security_policies_add_association_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    replaceExistingAssociation: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/addAssociation",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = replaceExistingAssociation.as_ref() {
        query_parts.push(format!("replaceExistingAssociation={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/addAssociation
/// Inserts an association for the specified security policy.  This has billing implications.  Projects in the hierarchy with effective hierarchical security policies will be automatically enrolled into Cloud Armor Enterprise if not already enrolled.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_add_association_execute()` or `compute_organization_security_policies_add_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_add_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_add_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/securityPolicies/{securityPolicy}/addAssociation
/// Inserts an association for the specified security policy.  This has billing implications.  Projects in the hierarchy with effective hierarchical security policies will be automatically enrolled into Cloud Armor Enterprise if not already enrolled.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_add_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_add_association_task()`.
/// For the simplest API, use `compute_organization_security_policies_add_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_add_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_add_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_add_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_add_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesAddAssociationArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: replaceExistingAssociation
    pub replaceExistingAssociation: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/securityPolicies/{securityPolicy}/addAssociation
/// Inserts an association for the specified security policy.  This has billing implications.  Projects in the hierarchy with effective hierarchical security policies will be automatically enrolled into Cloud Armor Enterprise if not already enrolled.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addAssociation` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_add_association_builder()` + `compute_organization_security_policies_add_association_execute()`.
/// For task-level control, use `compute_organization_security_policies_add_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_add_association(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesAddAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_add_association_builder(
        client,
        &args.securityPolicy,
        &args.replaceExistingAssociation,
        &args.requestId,
    )?;
    compute_organization_security_policies_add_association_execute(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_add_rule_execute()` to send, or `compute_organization_security_policies_add_rule` for simplest API.

pub fn compute_organization_security_policies_add_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/addRule",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_add_rule_execute()` or `compute_organization_security_policies_add_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_add_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_add_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_add_rule_task()`.
/// For the simplest API, use `compute_organization_security_policies_add_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_add_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_add_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesAddRuleArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_add_rule_builder()` + `compute_organization_security_policies_add_rule_execute()`.
/// For task-level control, use `compute_organization_security_policies_add_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_add_rule(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesAddRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_add_rule_builder(
        client,
        &args.securityPolicy,
        &args.requestId,
    )?;
    compute_organization_security_policies_add_rule_execute(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/copyRules
/// Copies rules to the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`cloneRules` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_copy_rules_execute()` to send, or `compute_organization_security_policies_copy_rules` for simplest API.

pub fn compute_organization_security_policies_copy_rules_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    requestId: &Option<Option<String>>,
    sourceSecurityPolicy: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/copyRules",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceSecurityPolicy.as_ref() {
        query_parts.push(format!("sourceSecurityPolicy={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/copyRules
/// Copies rules to the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`cloneRules` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_copy_rules_execute()` or `compute_organization_security_policies_copy_rules`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_copy_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_copy_rules_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/securityPolicies/{securityPolicy}/copyRules
/// Copies rules to the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`cloneRules` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_copy_rules_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_copy_rules_task()`.
/// For the simplest API, use `compute_organization_security_policies_copy_rules()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_copy_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_copy_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_copy_rules_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_copy_rules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesCopyRulesArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceSecurityPolicy
    pub sourceSecurityPolicy: Option<Option<String>>,
}

/// POST locations/global/securityPolicies/{securityPolicy}/copyRules
/// Copies rules to the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`cloneRules` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_copy_rules_builder()` + `compute_organization_security_policies_copy_rules_execute()`.
/// For task-level control, use `compute_organization_security_policies_copy_rules_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_copy_rules(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesCopyRulesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_copy_rules_builder(
        client,
        &args.securityPolicy,
        &args.requestId,
        &args.sourceSecurityPolicy,
    )?;
    compute_organization_security_policies_copy_rules_execute(builder)
}

/// DELETE locations/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.  Use this API to remove Cloud Armor policies. Previously, alpha and beta versions of this API were used to remove firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.delete instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_delete_execute()` to send, or `compute_organization_security_policies_delete` for simplest API.

pub fn compute_organization_security_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE locations/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.  Use this API to remove Cloud Armor policies. Previously, alpha and beta versions of this API were used to remove firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.delete instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_delete_execute()` or `compute_organization_security_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE locations/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.  Use this API to remove Cloud Armor policies. Previously, alpha and beta versions of this API were used to remove firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.delete instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_delete_task()`.
/// For the simplest API, use `compute_organization_security_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesDeleteArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE locations/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.  Use this API to remove Cloud Armor policies. Previously, alpha and beta versions of this API were used to remove firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.delete instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_delete_builder()` + `compute_organization_security_policies_delete_execute()`.
/// For task-level control, use `compute_organization_security_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_delete_builder(
        client,
        &args.securityPolicy,
        &args.requestId,
    )?;
    compute_organization_security_policies_delete_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.get instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_get_execute()` to send, or `compute_organization_security_policies_get` for simplest API.

pub fn compute_organization_security_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}",
        securityPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.get instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_get_execute()` or `compute_organization_security_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.get instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_get_task()`.
/// For the simplest API, use `compute_organization_security_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesGetArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.get instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_get_builder()` + `compute_organization_security_policies_get_execute()`.
/// For task-level control, use `compute_organization_security_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_get_builder(client, &args.securityPolicy)?;
    compute_organization_security_policies_get_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/getAssociation
/// Gets an association with the specified name.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getAssociation` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_get_association_execute()` to send, or `compute_organization_security_policies_get_association` for simplest API.

pub fn compute_organization_security_policies_get_association_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    name: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/getAssociation",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/getAssociation
/// Gets an association with the specified name.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_get_association_execute()` or `compute_organization_security_policies_get_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_get_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/securityPolicies/{securityPolicy}/getAssociation
/// Gets an association with the specified name.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_get_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_get_association_task()`.
/// For the simplest API, use `compute_organization_security_policies_get_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_get_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_get_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_get_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_get_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesGetAssociationArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
}

/// GET locations/global/securityPolicies/{securityPolicy}/getAssociation
/// Gets an association with the specified name.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getAssociation` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_get_association_builder()` + `compute_organization_security_policies_get_association_execute()`.
/// For task-level control, use `compute_organization_security_policies_get_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_association(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesGetAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_get_association_builder(
        client,
        &args.securityPolicy,
        &args.name,
    )?;
    compute_organization_security_policies_get_association_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_get_rule_execute()` to send, or `compute_organization_security_policies_get_rule` for simplest API.

pub fn compute_organization_security_policies_get_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/getRule",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_get_rule_execute()` or `compute_organization_security_policies_get_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_get_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_get_rule_task()`.
/// For the simplest API, use `compute_organization_security_policies_get_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_get_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_get_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesGetRuleArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// GET locations/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_get_rule_builder()` + `compute_organization_security_policies_get_rule_execute()`.
/// For task-level control, use `compute_organization_security_policies_get_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_rule(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesGetRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_get_rule_builder(
        client,
        &args.securityPolicy,
        &args.priority,
    )?;
    compute_organization_security_policies_get_rule_execute(builder)
}

/// POST locations/global/securityPolicies
/// Creates a new policy in the specified organization using the data included in the request.  Use this API to add Cloud Armor policies. Previously, alpha and beta versions of this API were used to add firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.insert instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_insert_execute()` to send, or `compute_organization_security_policies_insert` for simplest API.

pub fn compute_organization_security_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    parentId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/securityPolicies",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/securityPolicies
/// Creates a new policy in the specified organization using the data included in the request.  Use this API to add Cloud Armor policies. Previously, alpha and beta versions of this API were used to add firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.insert instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_insert_execute()` or `compute_organization_security_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/securityPolicies
/// Creates a new policy in the specified organization using the data included in the request.  Use this API to add Cloud Armor policies. Previously, alpha and beta versions of this API were used to add firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.insert instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_insert_task()`.
/// For the simplest API, use `compute_organization_security_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesInsertArgs {
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/securityPolicies
/// Creates a new policy in the specified organization using the data included in the request.  Use this API to add Cloud Armor policies. Previously, alpha and beta versions of this API were used to add firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.insert instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_insert_builder()` + `compute_organization_security_policies_insert_execute()`.
/// For task-level control, use `compute_organization_security_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_insert_builder(
        client,
        &args.parentId,
        &args.requestId,
    )?;
    compute_organization_security_policies_insert_execute(builder)
}

/// GET locations/global/securityPolicies
/// List all the policies that have been configured for the specified organization.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.list instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_list_execute()` to send, or `compute_organization_security_policies_list` for simplest API.

pub fn compute_organization_security_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    parentId: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/securityPolicies",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/securityPolicies
/// List all the policies that have been configured for the specified organization.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.list instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_list_execute()` or `compute_organization_security_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/securityPolicies
/// List all the policies that have been configured for the specified organization.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.list instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_list_task()`.
/// For the simplest API, use `compute_organization_security_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesListArgs {
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET locations/global/securityPolicies
/// List all the policies that have been configured for the specified organization.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.list instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_list_builder()` + `compute_organization_security_policies_list_execute()`.
/// For task-level control, use `compute_organization_security_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_list_builder(
        client,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.parentId,
        &args.returnPartialSuccess,
    )?;
    compute_organization_security_policies_list_execute(builder)
}

/// GET locations/global/securityPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`listAssociations` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_list_associations_execute()` to send, or `compute_organization_security_policies_list_associations` for simplest API.

pub fn compute_organization_security_policies_list_associations_builder<R>(
    client: &SimpleHttpClient<R>,
    targetResource: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/listAssociations",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = targetResource.as_ref() {
        query_parts.push(format!("targetResource={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/securityPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`listAssociations` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_list_associations_execute()` or `compute_organization_security_policies_list_associations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_list_associations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_associations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<OrganizationSecurityPoliciesListAssociationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationSecurityPoliciesListAssociationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/securityPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`listAssociations` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_list_associations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_list_associations_task()`.
/// For the simplest API, use `compute_organization_security_policies_list_associations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_list_associations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_list_associations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrganizationSecurityPoliciesListAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_list_associations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_list_associations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesListAssociationsArgs {
    /// Query parameter: targetResource
    pub targetResource: Option<Option<String>>,
}

/// GET locations/global/securityPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`listAssociations` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_list_associations_builder()` + `compute_organization_security_policies_list_associations_execute()`.
/// For task-level control, use `compute_organization_security_policies_list_associations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_associations(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesListAssociationsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrganizationSecurityPoliciesListAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_list_associations_builder(
        client,
        &args.targetResource,
    )?;
    compute_organization_security_policies_list_associations_execute(builder)
}

/// GET locations/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_list_preconfigured_expression_sets_execute()` to send, or `compute_organization_security_policies_list_preconfigured_expression_sets` for simplest API.

pub fn compute_organization_security_policies_list_preconfigured_expression_sets_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    parentId: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/listPreconfiguredExpressionSets",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET locations/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_list_preconfigured_expression_sets_execute()` or `compute_organization_security_policies_list_preconfigured_expression_sets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_list_preconfigured_expression_sets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_preconfigured_expression_sets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPoliciesListPreconfiguredExpressionSetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET locations/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_list_preconfigured_expression_sets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_list_preconfigured_expression_sets_task()`.
/// For the simplest API, use `compute_organization_security_policies_list_preconfigured_expression_sets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_list_preconfigured_expression_sets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_list_preconfigured_expression_sets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        compute_organization_security_policies_list_preconfigured_expression_sets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_list_preconfigured_expression_sets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs {
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET locations/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_list_preconfigured_expression_sets_builder()` + `compute_organization_security_policies_list_preconfigured_expression_sets_execute()`.
/// For task-level control, use `compute_organization_security_policies_list_preconfigured_expression_sets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_preconfigured_expression_sets(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_organization_security_policies_list_preconfigured_expression_sets_builder(
            client,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.parentId,
            &args.returnPartialSuccess,
        )?;
    compute_organization_security_policies_list_preconfigured_expression_sets_execute(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/move
/// Moves the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.move instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_move_execute()` to send, or `compute_organization_security_policies_move` for simplest API.

pub fn compute_organization_security_policies_move_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    parentId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/move",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parentId.as_ref() {
        query_parts.push(format!("parentId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/move
/// Moves the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.move instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_move_execute()` or `compute_organization_security_policies_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/securityPolicies/{securityPolicy}/move
/// Moves the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.move instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_move_task()`.
/// For the simplest API, use `compute_organization_security_policies_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesMoveArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: parentId
    pub parentId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/securityPolicies/{securityPolicy}/move
/// Moves the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.move instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_move_builder()` + `compute_organization_security_policies_move_execute()`.
/// For task-level control, use `compute_organization_security_policies_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_move(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_move_builder(
        client,
        &args.securityPolicy,
        &args.parentId,
        &args.requestId,
    )?;
    compute_organization_security_policies_move_execute(builder)
}

/// PATCH locations/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.patch instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_patch_execute()` to send, or `compute_organization_security_policies_patch` for simplest API.

pub fn compute_organization_security_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH locations/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.patch instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_patch_execute()` or `compute_organization_security_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH locations/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.patch instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_patch_task()`.
/// For the simplest API, use `compute_organization_security_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesPatchArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH locations/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.patch instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_patch_builder()` + `compute_organization_security_policies_patch_execute()`.
/// For task-level control, use `compute_organization_security_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_patch_builder(
        client,
        &args.securityPolicy,
        &args.requestId,
    )?;
    compute_organization_security_policies_patch_execute(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`patchRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_patch_rule_execute()` to send, or `compute_organization_security_policies_patch_rule` for simplest API.

pub fn compute_organization_security_policies_patch_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/patchRule",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`patchRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_patch_rule_execute()` or `compute_organization_security_policies_patch_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_patch_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`patchRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_patch_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_patch_rule_task()`.
/// For the simplest API, use `compute_organization_security_policies_patch_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_patch_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_patch_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesPatchRuleArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`patchRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_patch_rule_builder()` + `compute_organization_security_policies_patch_rule_execute()`.
/// For task-level control, use `compute_organization_security_policies_patch_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_patch_rule(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesPatchRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_patch_rule_builder(
        client,
        &args.securityPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_organization_security_policies_patch_rule_execute(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/removeAssociation
/// Removes an association for the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeAssociation` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_remove_association_execute()` to send, or `compute_organization_security_policies_remove_association` for simplest API.

pub fn compute_organization_security_policies_remove_association_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    name: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/removeAssociation",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/removeAssociation
/// Removes an association for the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_remove_association_execute()` or `compute_organization_security_policies_remove_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_remove_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_remove_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/securityPolicies/{securityPolicy}/removeAssociation
/// Removes an association for the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_remove_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_remove_association_task()`.
/// For the simplest API, use `compute_organization_security_policies_remove_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_remove_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_remove_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_remove_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_remove_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesRemoveAssociationArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/securityPolicies/{securityPolicy}/removeAssociation
/// Removes an association for the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeAssociation` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_remove_association_builder()` + `compute_organization_security_policies_remove_association_execute()`.
/// For task-level control, use `compute_organization_security_policies_remove_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_remove_association(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesRemoveAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_remove_association_builder(
        client,
        &args.securityPolicy,
        &args.name,
        &args.requestId,
    )?;
    compute_organization_security_policies_remove_association_execute(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_remove_rule_execute()` to send, or `compute_organization_security_policies_remove_rule` for simplest API.

pub fn compute_organization_security_policies_remove_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/removeRule",
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST locations/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_organization_security_policies_remove_rule_execute()` or `compute_organization_security_policies_remove_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_remove_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST locations/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_organization_security_policies_remove_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_organization_security_policies_remove_rule_task()`.
/// For the simplest API, use `compute_organization_security_policies_remove_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_organization_security_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_organization_security_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_organization_security_policies_remove_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_organization_security_policies_remove_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesRemoveRuleArgs {
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST locations/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_remove_rule_builder()` + `compute_organization_security_policies_remove_rule_execute()`.
/// For task-level control, use `compute_organization_security_policies_remove_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_remove_rule(
    client: &SimpleHttpClient,
    args: &ComputeOrganizationSecurityPoliciesRemoveRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_remove_rule_builder(
        client,
        &args.securityPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_organization_security_policies_remove_rule_execute(builder)
}

/// GET projects/{project}/aggregated/packetMirrorings
/// Retrieves an aggregated list of `packetMirrorings`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_aggregated_list_execute()` to send, or `compute_packet_mirrorings_aggregated_list` for simplest API.

pub fn compute_packet_mirrorings_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/packetMirrorings",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/packetMirrorings
/// Retrieves an aggregated list of `packetMirrorings`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_packet_mirrorings_aggregated_list_execute()` or `compute_packet_mirrorings_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PacketMirroringAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PacketMirroringAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/packetMirrorings
/// Retrieves an aggregated list of `packetMirrorings`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_packet_mirrorings_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_packet_mirrorings_aggregated_list_task()`.
/// For the simplest API, use `compute_packet_mirrorings_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_packet_mirrorings_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PacketMirroringAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_packet_mirrorings_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_packet_mirrorings_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePacketMirroringsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/packetMirrorings
/// Retrieves an aggregated list of `packetMirrorings`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_aggregated_list_builder()` + `compute_packet_mirrorings_aggregated_list_execute()`.
/// For task-level control, use `compute_packet_mirrorings_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputePacketMirroringsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PacketMirroringAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_packet_mirrorings_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Deletes the specified PacketMirroring resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_delete_execute()` to send, or `compute_packet_mirrorings_delete` for simplest API.

pub fn compute_packet_mirrorings_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    packetMirroring: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings/{}",
        project, region, packetMirroring,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Deletes the specified PacketMirroring resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_packet_mirrorings_delete_execute()` or `compute_packet_mirrorings_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Deletes the specified PacketMirroring resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_packet_mirrorings_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_packet_mirrorings_delete_task()`.
/// For the simplest API, use `compute_packet_mirrorings_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_packet_mirrorings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_packet_mirrorings_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_packet_mirrorings_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePacketMirroringsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: packetMirroring
    pub packetMirroring: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Deletes the specified PacketMirroring resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_delete_builder()` + `compute_packet_mirrorings_delete_execute()`.
/// For task-level control, use `compute_packet_mirrorings_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_delete(
    client: &SimpleHttpClient,
    args: &ComputePacketMirroringsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.packetMirroring,
        &args.requestId,
    )?;
    compute_packet_mirrorings_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Returns the specified PacketMirroring resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_get_execute()` to send, or `compute_packet_mirrorings_get` for simplest API.

pub fn compute_packet_mirrorings_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    packetMirroring: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings/{}",
        project, region, packetMirroring,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Returns the specified PacketMirroring resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_packet_mirrorings_get_execute()` or `compute_packet_mirrorings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PacketMirroring>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PacketMirroring = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Returns the specified PacketMirroring resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_packet_mirrorings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_packet_mirrorings_get_task()`.
/// For the simplest API, use `compute_packet_mirrorings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_packet_mirrorings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PacketMirroring>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_packet_mirrorings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_packet_mirrorings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePacketMirroringsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: packetMirroring
    pub packetMirroring: String,
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Returns the specified PacketMirroring resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_get_builder()` + `compute_packet_mirrorings_get_execute()`.
/// For task-level control, use `compute_packet_mirrorings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_get(
    client: &SimpleHttpClient,
    args: &ComputePacketMirroringsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PacketMirroring>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_get_builder(
        client,
        &args.project,
        &args.region,
        &args.packetMirroring,
    )?;
    compute_packet_mirrorings_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/packetMirrorings
/// Creates a PacketMirroring resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_insert_execute()` to send, or `compute_packet_mirrorings_insert` for simplest API.

pub fn compute_packet_mirrorings_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/packetMirrorings
/// Creates a PacketMirroring resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_packet_mirrorings_insert_execute()` or `compute_packet_mirrorings_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/packetMirrorings
/// Creates a PacketMirroring resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_packet_mirrorings_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_packet_mirrorings_insert_task()`.
/// For the simplest API, use `compute_packet_mirrorings_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_packet_mirrorings_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_packet_mirrorings_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_packet_mirrorings_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePacketMirroringsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/packetMirrorings
/// Creates a PacketMirroring resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_insert_builder()` + `compute_packet_mirrorings_insert_execute()`.
/// For task-level control, use `compute_packet_mirrorings_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_insert(
    client: &SimpleHttpClient,
    args: &ComputePacketMirroringsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_packet_mirrorings_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Retrieves a list of PacketMirroring resources available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_list_execute()` to send, or `compute_packet_mirrorings_list` for simplest API.

pub fn compute_packet_mirrorings_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Retrieves a list of PacketMirroring resources available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_packet_mirrorings_list_execute()` or `compute_packet_mirrorings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PacketMirroringList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PacketMirroringList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Retrieves a list of PacketMirroring resources available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_packet_mirrorings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_packet_mirrorings_list_task()`.
/// For the simplest API, use `compute_packet_mirrorings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_packet_mirrorings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PacketMirroringList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_packet_mirrorings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_packet_mirrorings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePacketMirroringsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Retrieves a list of PacketMirroring resources available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_list_builder()` + `compute_packet_mirrorings_list_execute()`.
/// For task-level control, use `compute_packet_mirrorings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_list(
    client: &SimpleHttpClient,
    args: &ComputePacketMirroringsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PacketMirroringList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_packet_mirrorings_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Patches the specified PacketMirroring resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_patch_execute()` to send, or `compute_packet_mirrorings_patch` for simplest API.

pub fn compute_packet_mirrorings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    packetMirroring: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings/{}",
        project, region, packetMirroring,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Patches the specified PacketMirroring resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_packet_mirrorings_patch_execute()` or `compute_packet_mirrorings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Patches the specified PacketMirroring resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_packet_mirrorings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_packet_mirrorings_patch_task()`.
/// For the simplest API, use `compute_packet_mirrorings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_packet_mirrorings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_packet_mirrorings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_packet_mirrorings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePacketMirroringsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: packetMirroring
    pub packetMirroring: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Patches the specified PacketMirroring resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_patch_builder()` + `compute_packet_mirrorings_patch_execute()`.
/// For task-level control, use `compute_packet_mirrorings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_patch(
    client: &SimpleHttpClient,
    args: &ComputePacketMirroringsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.packetMirroring,
        &args.requestId,
    )?;
    compute_packet_mirrorings_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_test_iam_permissions_execute()` to send, or `compute_packet_mirrorings_test_iam_permissions` for simplest API.

pub fn compute_packet_mirrorings_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_packet_mirrorings_test_iam_permissions_execute()` or `compute_packet_mirrorings_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_packet_mirrorings_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_packet_mirrorings_test_iam_permissions_task()`.
/// For the simplest API, use `compute_packet_mirrorings_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_packet_mirrorings_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_packet_mirrorings_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_packet_mirrorings_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_packet_mirrorings_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePacketMirroringsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_test_iam_permissions_builder()` + `compute_packet_mirrorings_test_iam_permissions_execute()`.
/// For task-level control, use `compute_packet_mirrorings_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputePacketMirroringsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_packet_mirrorings_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Returns the details of the given PreviewFeature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_preview_features_get_execute()` to send, or `compute_preview_features_get` for simplest API.

pub fn compute_preview_features_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    previewFeature: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/previewFeatures/{}",
        project, previewFeature,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Returns the details of the given PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_preview_features_get_execute()` or `compute_preview_features_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_preview_features_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PreviewFeature>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PreviewFeature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Returns the details of the given PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_preview_features_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_preview_features_get_task()`.
/// For the simplest API, use `compute_preview_features_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_preview_features_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_preview_features_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreviewFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_preview_features_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_preview_features_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePreviewFeaturesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: previewFeature
    pub previewFeature: String,
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Returns the details of the given PreviewFeature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_preview_features_get_builder()` + `compute_preview_features_get_execute()`.
/// For task-level control, use `compute_preview_features_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_get(
    client: &SimpleHttpClient,
    args: &ComputePreviewFeaturesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreviewFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_preview_features_get_builder(client, &args.project, &args.previewFeature)?;
    compute_preview_features_get_execute(builder)
}

/// GET projects/{project}/global/previewFeatures
/// Returns the details of the given PreviewFeature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_preview_features_list_execute()` to send, or `compute_preview_features_list` for simplest API.

pub fn compute_preview_features_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/previewFeatures",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/previewFeatures
/// Returns the details of the given PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_preview_features_list_execute()` or `compute_preview_features_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_preview_features_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PreviewFeatureList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PreviewFeatureList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/previewFeatures
/// Returns the details of the given PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_preview_features_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_preview_features_list_task()`.
/// For the simplest API, use `compute_preview_features_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_preview_features_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_preview_features_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreviewFeatureList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_preview_features_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_preview_features_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePreviewFeaturesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/previewFeatures
/// Returns the details of the given PreviewFeature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_preview_features_list_builder()` + `compute_preview_features_list_execute()`.
/// For task-level control, use `compute_preview_features_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_list(
    client: &SimpleHttpClient,
    args: &ComputePreviewFeaturesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreviewFeatureList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_preview_features_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_preview_features_list_execute(builder)
}

/// PATCH projects/{project}/global/previewFeatures/{previewFeature}
/// Patches the given PreviewFeature. This method is used to enable or disable a PreviewFeature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_preview_features_update_execute()` to send, or `compute_preview_features_update` for simplest API.

pub fn compute_preview_features_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    previewFeature: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/previewFeatures/{}",
        project, previewFeature,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/previewFeatures/{previewFeature}
/// Patches the given PreviewFeature. This method is used to enable or disable a PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_preview_features_update_execute()` or `compute_preview_features_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_preview_features_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/previewFeatures/{previewFeature}
/// Patches the given PreviewFeature. This method is used to enable or disable a PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_preview_features_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_preview_features_update_task()`.
/// For the simplest API, use `compute_preview_features_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_preview_features_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_preview_features_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_preview_features_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_preview_features_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePreviewFeaturesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: previewFeature
    pub previewFeature: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/previewFeatures/{previewFeature}
/// Patches the given PreviewFeature. This method is used to enable or disable a PreviewFeature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_preview_features_update_builder()` + `compute_preview_features_update_execute()`.
/// For task-level control, use `compute_preview_features_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_update(
    client: &SimpleHttpClient,
    args: &ComputePreviewFeaturesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_preview_features_update_builder(
        client,
        &args.project,
        &args.previewFeature,
        &args.requestId,
    )?;
    compute_preview_features_update_execute(builder)
}

/// POST projects/{project}/disableXpnHost
/// Disable this project as a shared VPC host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_disable_xpn_host_execute()` to send, or `compute_projects_disable_xpn_host` for simplest API.

pub fn compute_projects_disable_xpn_host_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/disableXpnHost",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/disableXpnHost
/// Disable this project as a shared VPC host project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_disable_xpn_host_execute()` or `compute_projects_disable_xpn_host`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_disable_xpn_host_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_disable_xpn_host_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/disableXpnHost
/// Disable this project as a shared VPC host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_disable_xpn_host_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_disable_xpn_host_task()`.
/// For the simplest API, use `compute_projects_disable_xpn_host()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_disable_xpn_host_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_disable_xpn_host_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_disable_xpn_host_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_disable_xpn_host`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsDisableXpnHostArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/disableXpnHost
/// Disable this project as a shared VPC host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_disable_xpn_host_builder()` + `compute_projects_disable_xpn_host_execute()`.
/// For task-level control, use `compute_projects_disable_xpn_host_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_disable_xpn_host(
    client: &SimpleHttpClient,
    args: &ComputeProjectsDisableXpnHostArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_disable_xpn_host_builder(client, &args.project, &args.requestId)?;
    compute_projects_disable_xpn_host_execute(builder)
}

/// POST projects/{project}/disableXpnResource
/// Disable a service resource (also known as service project) associated with this host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_disable_xpn_resource_execute()` to send, or `compute_projects_disable_xpn_resource` for simplest API.

pub fn compute_projects_disable_xpn_resource_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/disableXpnResource",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/disableXpnResource
/// Disable a service resource (also known as service project) associated with this host project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_disable_xpn_resource_execute()` or `compute_projects_disable_xpn_resource`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_disable_xpn_resource_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_disable_xpn_resource_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/disableXpnResource
/// Disable a service resource (also known as service project) associated with this host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_disable_xpn_resource_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_disable_xpn_resource_task()`.
/// For the simplest API, use `compute_projects_disable_xpn_resource()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_disable_xpn_resource_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_disable_xpn_resource_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_disable_xpn_resource_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_disable_xpn_resource`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsDisableXpnResourceArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/disableXpnResource
/// Disable a service resource (also known as service project) associated with this host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_disable_xpn_resource_builder()` + `compute_projects_disable_xpn_resource_execute()`.
/// For task-level control, use `compute_projects_disable_xpn_resource_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_disable_xpn_resource(
    client: &SimpleHttpClient,
    args: &ComputeProjectsDisableXpnResourceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_disable_xpn_resource_builder(client, &args.project, &args.requestId)?;
    compute_projects_disable_xpn_resource_execute(builder)
}

/// POST projects/{project}/enableXpnHost
/// Enable this project as a shared VPC host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_enable_xpn_host_execute()` to send, or `compute_projects_enable_xpn_host` for simplest API.

pub fn compute_projects_enable_xpn_host_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/enableXpnHost",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/enableXpnHost
/// Enable this project as a shared VPC host project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_enable_xpn_host_execute()` or `compute_projects_enable_xpn_host`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_enable_xpn_host_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_enable_xpn_host_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/enableXpnHost
/// Enable this project as a shared VPC host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_enable_xpn_host_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_enable_xpn_host_task()`.
/// For the simplest API, use `compute_projects_enable_xpn_host()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_enable_xpn_host_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_enable_xpn_host_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_enable_xpn_host_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_enable_xpn_host`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsEnableXpnHostArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/enableXpnHost
/// Enable this project as a shared VPC host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_enable_xpn_host_builder()` + `compute_projects_enable_xpn_host_execute()`.
/// For task-level control, use `compute_projects_enable_xpn_host_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_enable_xpn_host(
    client: &SimpleHttpClient,
    args: &ComputeProjectsEnableXpnHostArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_enable_xpn_host_builder(client, &args.project, &args.requestId)?;
    compute_projects_enable_xpn_host_execute(builder)
}

/// POST projects/{project}/enableXpnResource
/// Enable service resource (a.k.a service project) for a host project, so that subnets in the host project can be used by instances in the service project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_enable_xpn_resource_execute()` to send, or `compute_projects_enable_xpn_resource` for simplest API.

pub fn compute_projects_enable_xpn_resource_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/enableXpnResource",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/enableXpnResource
/// Enable service resource (a.k.a service project) for a host project, so that subnets in the host project can be used by instances in the service project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_enable_xpn_resource_execute()` or `compute_projects_enable_xpn_resource`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_enable_xpn_resource_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_enable_xpn_resource_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/enableXpnResource
/// Enable service resource (a.k.a service project) for a host project, so that subnets in the host project can be used by instances in the service project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_enable_xpn_resource_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_enable_xpn_resource_task()`.
/// For the simplest API, use `compute_projects_enable_xpn_resource()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_enable_xpn_resource_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_enable_xpn_resource_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_enable_xpn_resource_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_enable_xpn_resource`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsEnableXpnResourceArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/enableXpnResource
/// Enable service resource (a.k.a service project) for a host project, so that subnets in the host project can be used by instances in the service project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_enable_xpn_resource_builder()` + `compute_projects_enable_xpn_resource_execute()`.
/// For task-level control, use `compute_projects_enable_xpn_resource_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_enable_xpn_resource(
    client: &SimpleHttpClient,
    args: &ComputeProjectsEnableXpnResourceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_enable_xpn_resource_builder(client, &args.project, &args.requestId)?;
    compute_projects_enable_xpn_resource_execute(builder)
}

/// GET projects/{project}
/// Returns the specified Project resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_get_execute()` to send, or `compute_projects_get` for simplest API.

pub fn compute_projects_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}",
        project,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}
/// Returns the specified Project resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_get_execute()` or `compute_projects_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Project>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Project = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}
/// Returns the specified Project resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_get_task()`.
/// For the simplest API, use `compute_projects_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsGetArgs {
    /// Path parameter: project
    pub project: String,
}

/// GET projects/{project}
/// Returns the specified Project resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_get_builder()` + `compute_projects_get_execute()`.
/// For task-level control, use `compute_projects_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get(
    client: &SimpleHttpClient,
    args: &ComputeProjectsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_get_builder(client, &args.project)?;
    compute_projects_get_execute(builder)
}

/// GET projects/{project}/getXpnHost
/// Gets the shared VPC host project that this project links to. May be empty if no link exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_get_xpn_host_execute()` to send, or `compute_projects_get_xpn_host` for simplest API.

pub fn compute_projects_get_xpn_host_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/getXpnHost",
        project,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/getXpnHost
/// Gets the shared VPC host project that this project links to. May be empty if no link exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_get_xpn_host_execute()` or `compute_projects_get_xpn_host`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_get_xpn_host_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_xpn_host_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Project>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Project = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/getXpnHost
/// Gets the shared VPC host project that this project links to. May be empty if no link exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_get_xpn_host_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_get_xpn_host_task()`.
/// For the simplest API, use `compute_projects_get_xpn_host()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_get_xpn_host_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_get_xpn_host_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_get_xpn_host_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_get_xpn_host`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsGetXpnHostArgs {
    /// Path parameter: project
    pub project: String,
}

/// GET projects/{project}/getXpnHost
/// Gets the shared VPC host project that this project links to. May be empty if no link exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_get_xpn_host_builder()` + `compute_projects_get_xpn_host_execute()`.
/// For task-level control, use `compute_projects_get_xpn_host_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_xpn_host(
    client: &SimpleHttpClient,
    args: &ComputeProjectsGetXpnHostArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_get_xpn_host_builder(client, &args.project)?;
    compute_projects_get_xpn_host_execute(builder)
}

/// GET projects/{project}/getXpnResources
/// Gets service resources (a.k.a service project) associated with this host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_get_xpn_resources_execute()` to send, or `compute_projects_get_xpn_resources` for simplest API.

pub fn compute_projects_get_xpn_resources_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/getXpnResources",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/getXpnResources
/// Gets service resources (a.k.a service project) associated with this host project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_get_xpn_resources_execute()` or `compute_projects_get_xpn_resources`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_get_xpn_resources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_xpn_resources_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProjectsGetXpnResources>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectsGetXpnResources = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/getXpnResources
/// Gets service resources (a.k.a service project) associated with this host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_get_xpn_resources_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_get_xpn_resources_task()`.
/// For the simplest API, use `compute_projects_get_xpn_resources()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_get_xpn_resources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_get_xpn_resources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectsGetXpnResources>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_projects_get_xpn_resources_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_get_xpn_resources`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsGetXpnResourcesArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/getXpnResources
/// Gets service resources (a.k.a service project) associated with this host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_get_xpn_resources_builder()` + `compute_projects_get_xpn_resources_execute()`.
/// For task-level control, use `compute_projects_get_xpn_resources_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_xpn_resources(
    client: &SimpleHttpClient,
    args: &ComputeProjectsGetXpnResourcesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectsGetXpnResources>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_projects_get_xpn_resources_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_projects_get_xpn_resources_execute(builder)
}

/// POST projects/{project}/listXpnHosts
/// Lists all shared VPC host projects visible to the user in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_list_xpn_hosts_execute()` to send, or `compute_projects_list_xpn_hosts` for simplest API.

pub fn compute_projects_list_xpn_hosts_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/listXpnHosts",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/listXpnHosts
/// Lists all shared VPC host projects visible to the user in an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_list_xpn_hosts_execute()` or `compute_projects_list_xpn_hosts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_list_xpn_hosts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_list_xpn_hosts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<XpnHostList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: XpnHostList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/listXpnHosts
/// Lists all shared VPC host projects visible to the user in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_list_xpn_hosts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_list_xpn_hosts_task()`.
/// For the simplest API, use `compute_projects_list_xpn_hosts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_list_xpn_hosts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_list_xpn_hosts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<XpnHostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_list_xpn_hosts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_list_xpn_hosts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsListXpnHostsArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/listXpnHosts
/// Lists all shared VPC host projects visible to the user in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_list_xpn_hosts_builder()` + `compute_projects_list_xpn_hosts_execute()`.
/// For task-level control, use `compute_projects_list_xpn_hosts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_list_xpn_hosts(
    client: &SimpleHttpClient,
    args: &ComputeProjectsListXpnHostsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<XpnHostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_list_xpn_hosts_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_projects_list_xpn_hosts_execute(builder)
}

/// POST projects/{project}/moveDisk
/// Moves a persistent disk from one zone to another. *Note*: The `moveDisk` API will be deprecated on September 29, 2026.  Starting September 29, 2025, you can't use the `moveDisk` API on new projects. To move a disk to a different region or zone, follow the steps in [Change the location of a disk](<https://cloud.google.`com/compute/docs/disks/migrate-to-hyperdisk`#migrate-to-hd>).  Projects that already use the `moveDisk` API can continue usage until September 29, 2026.  Starting November 1, 2025, API responses will include a warning message in the response body about the upcoming deprecation. You can skip the message to continue using the service without interruption.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_move_disk_execute()` to send, or `compute_projects_move_disk` for simplest API.

pub fn compute_projects_move_disk_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/moveDisk",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/moveDisk
/// Moves a persistent disk from one zone to another. *Note*: The `moveDisk` API will be deprecated on September 29, 2026.  Starting September 29, 2025, you can't use the `moveDisk` API on new projects. To move a disk to a different region or zone, follow the steps in [Change the location of a disk](<https://cloud.google.`com/compute/docs/disks/migrate-to-hyperdisk`#migrate-to-hd>).  Projects that already use the `moveDisk` API can continue usage until September 29, 2026.  Starting November 1, 2025, API responses will include a warning message in the response body about the upcoming deprecation. You can skip the message to continue using the service without interruption.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_move_disk_execute()` or `compute_projects_move_disk`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_move_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_move_disk_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/moveDisk
/// Moves a persistent disk from one zone to another. *Note*: The `moveDisk` API will be deprecated on September 29, 2026.  Starting September 29, 2025, you can't use the `moveDisk` API on new projects. To move a disk to a different region or zone, follow the steps in [Change the location of a disk](<https://cloud.google.`com/compute/docs/disks/migrate-to-hyperdisk`#migrate-to-hd>).  Projects that already use the `moveDisk` API can continue usage until September 29, 2026.  Starting November 1, 2025, API responses will include a warning message in the response body about the upcoming deprecation. You can skip the message to continue using the service without interruption.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_move_disk_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_move_disk_task()`.
/// For the simplest API, use `compute_projects_move_disk()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_move_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_move_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_move_disk_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_move_disk`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsMoveDiskArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/moveDisk
/// Moves a persistent disk from one zone to another. *Note*: The `moveDisk` API will be deprecated on September 29, 2026.  Starting September 29, 2025, you can't use the `moveDisk` API on new projects. To move a disk to a different region or zone, follow the steps in [Change the location of a disk](<https://cloud.google.`com/compute/docs/disks/migrate-to-hyperdisk`#migrate-to-hd>).  Projects that already use the `moveDisk` API can continue usage until September 29, 2026.  Starting November 1, 2025, API responses will include a warning message in the response body about the upcoming deprecation. You can skip the message to continue using the service without interruption.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_move_disk_builder()` + `compute_projects_move_disk_execute()`.
/// For task-level control, use `compute_projects_move_disk_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_move_disk(
    client: &SimpleHttpClient,
    args: &ComputeProjectsMoveDiskArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_move_disk_builder(client, &args.project, &args.requestId)?;
    compute_projects_move_disk_execute(builder)
}

/// POST projects/{project}/moveInstance
/// Moves an instance and its attached persistent disks from one zone to another. *Note*: Moving VMs or disks by using this method might cause unexpected behavior. For more information, see the [known issue](/`compute/docs/troubleshooting/known-issues`#moving_vms_or_disks_using_the_moveinstance_api_or_the_causes_unexpected_behavior). [Deprecated] This method is deprecated. See [moving instance across zones](/`compute/docs/instances/moving-instance-across-zones`) instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_move_instance_execute()` to send, or `compute_projects_move_instance` for simplest API.

pub fn compute_projects_move_instance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/moveInstance",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/moveInstance
/// Moves an instance and its attached persistent disks from one zone to another. *Note*: Moving VMs or disks by using this method might cause unexpected behavior. For more information, see the [known issue](/`compute/docs/troubleshooting/known-issues`#moving_vms_or_disks_using_the_moveinstance_api_or_the_causes_unexpected_behavior). [Deprecated] This method is deprecated. See [moving instance across zones](/`compute/docs/instances/moving-instance-across-zones`) instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_move_instance_execute()` or `compute_projects_move_instance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_move_instance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_move_instance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/moveInstance
/// Moves an instance and its attached persistent disks from one zone to another. *Note*: Moving VMs or disks by using this method might cause unexpected behavior. For more information, see the [known issue](/`compute/docs/troubleshooting/known-issues`#moving_vms_or_disks_using_the_moveinstance_api_or_the_causes_unexpected_behavior). [Deprecated] This method is deprecated. See [moving instance across zones](/`compute/docs/instances/moving-instance-across-zones`) instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_move_instance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_move_instance_task()`.
/// For the simplest API, use `compute_projects_move_instance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_move_instance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_move_instance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_move_instance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_move_instance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsMoveInstanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/moveInstance
/// Moves an instance and its attached persistent disks from one zone to another. *Note*: Moving VMs or disks by using this method might cause unexpected behavior. For more information, see the [known issue](/`compute/docs/troubleshooting/known-issues`#moving_vms_or_disks_using_the_moveinstance_api_or_the_causes_unexpected_behavior). [Deprecated] This method is deprecated. See [moving instance across zones](/`compute/docs/instances/moving-instance-across-zones`) instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_move_instance_builder()` + `compute_projects_move_instance_execute()`.
/// For task-level control, use `compute_projects_move_instance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_move_instance(
    client: &SimpleHttpClient,
    args: &ComputeProjectsMoveInstanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_move_instance_builder(client, &args.project, &args.requestId)?;
    compute_projects_move_instance_execute(builder)
}

/// POST projects/{project}/setCloudArmorTier
/// Sets the Cloud Armor tier of the project. To set ENTERPRISE or above the billing account of the project must be subscribed to Cloud Armor Enterprise. See Subscribing to Cloud Armor Enterprise for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_set_cloud_armor_tier_execute()` to send, or `compute_projects_set_cloud_armor_tier` for simplest API.

pub fn compute_projects_set_cloud_armor_tier_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/setCloudArmorTier",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/setCloudArmorTier
/// Sets the Cloud Armor tier of the project. To set ENTERPRISE or above the billing account of the project must be subscribed to Cloud Armor Enterprise. See Subscribing to Cloud Armor Enterprise for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_set_cloud_armor_tier_execute()` or `compute_projects_set_cloud_armor_tier`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_set_cloud_armor_tier_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_cloud_armor_tier_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/setCloudArmorTier
/// Sets the Cloud Armor tier of the project. To set ENTERPRISE or above the billing account of the project must be subscribed to Cloud Armor Enterprise. See Subscribing to Cloud Armor Enterprise for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_set_cloud_armor_tier_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_set_cloud_armor_tier_task()`.
/// For the simplest API, use `compute_projects_set_cloud_armor_tier()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_set_cloud_armor_tier_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_set_cloud_armor_tier_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_set_cloud_armor_tier_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_set_cloud_armor_tier`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsSetCloudArmorTierArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/setCloudArmorTier
/// Sets the Cloud Armor tier of the project. To set ENTERPRISE or above the billing account of the project must be subscribed to Cloud Armor Enterprise. See Subscribing to Cloud Armor Enterprise for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_set_cloud_armor_tier_builder()` + `compute_projects_set_cloud_armor_tier_execute()`.
/// For task-level control, use `compute_projects_set_cloud_armor_tier_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_cloud_armor_tier(
    client: &SimpleHttpClient,
    args: &ComputeProjectsSetCloudArmorTierArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_set_cloud_armor_tier_builder(client, &args.project, &args.requestId)?;
    compute_projects_set_cloud_armor_tier_execute(builder)
}

/// POST projects/{project}/setCommonInstanceMetadata
/// Sets metadata common to all instances within the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_set_common_instance_metadata_execute()` to send, or `compute_projects_set_common_instance_metadata` for simplest API.

pub fn compute_projects_set_common_instance_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/setCommonInstanceMetadata",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/setCommonInstanceMetadata
/// Sets metadata common to all instances within the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_set_common_instance_metadata_execute()` or `compute_projects_set_common_instance_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_set_common_instance_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_common_instance_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/setCommonInstanceMetadata
/// Sets metadata common to all instances within the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_set_common_instance_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_set_common_instance_metadata_task()`.
/// For the simplest API, use `compute_projects_set_common_instance_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_set_common_instance_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_set_common_instance_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_set_common_instance_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_set_common_instance_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsSetCommonInstanceMetadataArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/setCommonInstanceMetadata
/// Sets metadata common to all instances within the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_set_common_instance_metadata_builder()` + `compute_projects_set_common_instance_metadata_execute()`.
/// For task-level control, use `compute_projects_set_common_instance_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_common_instance_metadata(
    client: &SimpleHttpClient,
    args: &ComputeProjectsSetCommonInstanceMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_set_common_instance_metadata_builder(
        client,
        &args.project,
        &args.requestId,
    )?;
    compute_projects_set_common_instance_metadata_execute(builder)
}

/// POST projects/{project}/setDefaultNetworkTier
/// Sets the default network tier of the project. The default network tier is used when an `address/`forwardingRule`/instance` is created without specifying the network tier field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_set_default_network_tier_execute()` to send, or `compute_projects_set_default_network_tier` for simplest API.

pub fn compute_projects_set_default_network_tier_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/setDefaultNetworkTier",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/setDefaultNetworkTier
/// Sets the default network tier of the project. The default network tier is used when an `address/`forwardingRule`/instance` is created without specifying the network tier field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_set_default_network_tier_execute()` or `compute_projects_set_default_network_tier`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_set_default_network_tier_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_default_network_tier_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/setDefaultNetworkTier
/// Sets the default network tier of the project. The default network tier is used when an `address/`forwardingRule`/instance` is created without specifying the network tier field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_set_default_network_tier_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_set_default_network_tier_task()`.
/// For the simplest API, use `compute_projects_set_default_network_tier()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_set_default_network_tier_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_set_default_network_tier_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_set_default_network_tier_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_set_default_network_tier`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsSetDefaultNetworkTierArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/setDefaultNetworkTier
/// Sets the default network tier of the project. The default network tier is used when an `address/`forwardingRule`/instance` is created without specifying the network tier field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_set_default_network_tier_builder()` + `compute_projects_set_default_network_tier_execute()`.
/// For task-level control, use `compute_projects_set_default_network_tier_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_default_network_tier(
    client: &SimpleHttpClient,
    args: &ComputeProjectsSetDefaultNetworkTierArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_set_default_network_tier_builder(client, &args.project, &args.requestId)?;
    compute_projects_set_default_network_tier_execute(builder)
}

/// POST projects/{project}/setUsageExportBucket
/// Enables the usage export feature and sets theusage export bucket where reports are stored. If you provide an empty request body using this method, the usage export feature will be disabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_set_usage_export_bucket_execute()` to send, or `compute_projects_set_usage_export_bucket` for simplest API.

pub fn compute_projects_set_usage_export_bucket_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/setUsageExportBucket",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/setUsageExportBucket
/// Enables the usage export feature and sets theusage export bucket where reports are stored. If you provide an empty request body using this method, the usage export feature will be disabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_projects_set_usage_export_bucket_execute()` or `compute_projects_set_usage_export_bucket`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_set_usage_export_bucket_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_usage_export_bucket_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/setUsageExportBucket
/// Enables the usage export feature and sets theusage export bucket where reports are stored. If you provide an empty request body using this method, the usage export feature will be disabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_projects_set_usage_export_bucket_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_projects_set_usage_export_bucket_task()`.
/// For the simplest API, use `compute_projects_set_usage_export_bucket()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_projects_set_usage_export_bucket_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_projects_set_usage_export_bucket_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_projects_set_usage_export_bucket_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_projects_set_usage_export_bucket`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeProjectsSetUsageExportBucketArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/setUsageExportBucket
/// Enables the usage export feature and sets theusage export bucket where reports are stored. If you provide an empty request body using this method, the usage export feature will be disabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_set_usage_export_bucket_builder()` + `compute_projects_set_usage_export_bucket_execute()`.
/// For task-level control, use `compute_projects_set_usage_export_bucket_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_usage_export_bucket(
    client: &SimpleHttpClient,
    args: &ComputeProjectsSetUsageExportBucketArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_set_usage_export_bucket_builder(client, &args.project, &args.requestId)?;
    compute_projects_set_usage_export_bucket_execute(builder)
}

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce
/// Announces the specified PublicAdvertisedPrefix
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_announce_execute()` to send, or `compute_public_advertised_prefixes_announce` for simplest API.

pub fn compute_public_advertised_prefixes_announce_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    publicAdvertisedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}/announce",
        project,
        publicAdvertisedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce
/// Announces the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_advertised_prefixes_announce_execute()` or `compute_public_advertised_prefixes_announce`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_announce_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_announce_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce
/// Announces the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_advertised_prefixes_announce_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_advertised_prefixes_announce_task()`.
/// For the simplest API, use `compute_public_advertised_prefixes_announce()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_announce_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_advertised_prefixes_announce_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_advertised_prefixes_announce_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_advertised_prefixes_announce`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesAnnounceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: publicAdvertisedPrefix
    pub publicAdvertisedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce
/// Announces the specified PublicAdvertisedPrefix
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_announce_builder()` + `compute_public_advertised_prefixes_announce_execute()`.
/// For task-level control, use `compute_public_advertised_prefixes_announce_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_announce(
    client: &SimpleHttpClient,
    args: &ComputePublicAdvertisedPrefixesAnnounceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_announce_builder(
        client,
        &args.project,
        &args.publicAdvertisedPrefix,
        &args.requestId,
    )?;
    compute_public_advertised_prefixes_announce_execute(builder)
}

/// DELETE projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Deletes the specified PublicAdvertisedPrefix
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_delete_execute()` to send, or `compute_public_advertised_prefixes_delete` for simplest API.

pub fn compute_public_advertised_prefixes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    publicAdvertisedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}",
        project, publicAdvertisedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Deletes the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_advertised_prefixes_delete_execute()` or `compute_public_advertised_prefixes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Deletes the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_advertised_prefixes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_advertised_prefixes_delete_task()`.
/// For the simplest API, use `compute_public_advertised_prefixes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_advertised_prefixes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_advertised_prefixes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_advertised_prefixes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: publicAdvertisedPrefix
    pub publicAdvertisedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Deletes the specified PublicAdvertisedPrefix
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_delete_builder()` + `compute_public_advertised_prefixes_delete_execute()`.
/// For task-level control, use `compute_public_advertised_prefixes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_delete(
    client: &SimpleHttpClient,
    args: &ComputePublicAdvertisedPrefixesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_delete_builder(
        client,
        &args.project,
        &args.publicAdvertisedPrefix,
        &args.requestId,
    )?;
    compute_public_advertised_prefixes_delete_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Returns the specified PublicAdvertisedPrefix resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_get_execute()` to send, or `compute_public_advertised_prefixes_get` for simplest API.

pub fn compute_public_advertised_prefixes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    publicAdvertisedPrefix: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}",
        project, publicAdvertisedPrefix,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Returns the specified PublicAdvertisedPrefix resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_advertised_prefixes_get_execute()` or `compute_public_advertised_prefixes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicAdvertisedPrefix>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicAdvertisedPrefix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Returns the specified PublicAdvertisedPrefix resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_advertised_prefixes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_advertised_prefixes_get_task()`.
/// For the simplest API, use `compute_public_advertised_prefixes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_advertised_prefixes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicAdvertisedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_public_advertised_prefixes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_advertised_prefixes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: publicAdvertisedPrefix
    pub publicAdvertisedPrefix: String,
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Returns the specified PublicAdvertisedPrefix resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_get_builder()` + `compute_public_advertised_prefixes_get_execute()`.
/// For task-level control, use `compute_public_advertised_prefixes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_get(
    client: &SimpleHttpClient,
    args: &ComputePublicAdvertisedPrefixesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicAdvertisedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_get_builder(
        client,
        &args.project,
        &args.publicAdvertisedPrefix,
    )?;
    compute_public_advertised_prefixes_get_execute(builder)
}

/// POST projects/{project}/global/publicAdvertisedPrefixes
/// Creates a PublicAdvertisedPrefix in the specified project using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_insert_execute()` to send, or `compute_public_advertised_prefixes_insert` for simplest API.

pub fn compute_public_advertised_prefixes_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/publicAdvertisedPrefixes
/// Creates a PublicAdvertisedPrefix in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_advertised_prefixes_insert_execute()` or `compute_public_advertised_prefixes_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/publicAdvertisedPrefixes
/// Creates a PublicAdvertisedPrefix in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_advertised_prefixes_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_advertised_prefixes_insert_task()`.
/// For the simplest API, use `compute_public_advertised_prefixes_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_advertised_prefixes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_advertised_prefixes_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_advertised_prefixes_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/publicAdvertisedPrefixes
/// Creates a PublicAdvertisedPrefix in the specified project using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_insert_builder()` + `compute_public_advertised_prefixes_insert_execute()`.
/// For task-level control, use `compute_public_advertised_prefixes_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_insert(
    client: &SimpleHttpClient,
    args: &ComputePublicAdvertisedPrefixesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_public_advertised_prefixes_insert_builder(client, &args.project, &args.requestId)?;
    compute_public_advertised_prefixes_insert_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Lists the PublicAdvertisedPrefixes for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_list_execute()` to send, or `compute_public_advertised_prefixes_list` for simplest API.

pub fn compute_public_advertised_prefixes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Lists the PublicAdvertisedPrefixes for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_advertised_prefixes_list_execute()` or `compute_public_advertised_prefixes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicAdvertisedPrefixList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicAdvertisedPrefixList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Lists the PublicAdvertisedPrefixes for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_advertised_prefixes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_advertised_prefixes_list_task()`.
/// For the simplest API, use `compute_public_advertised_prefixes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_advertised_prefixes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublicAdvertisedPrefixList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_public_advertised_prefixes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_advertised_prefixes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Lists the PublicAdvertisedPrefixes for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_list_builder()` + `compute_public_advertised_prefixes_list_execute()`.
/// For task-level control, use `compute_public_advertised_prefixes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_list(
    client: &SimpleHttpClient,
    args: &ComputePublicAdvertisedPrefixesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublicAdvertisedPrefixList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_public_advertised_prefixes_list_execute(builder)
}

/// PATCH projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_patch_execute()` to send, or `compute_public_advertised_prefixes_patch` for simplest API.

pub fn compute_public_advertised_prefixes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    publicAdvertisedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}",
        project, publicAdvertisedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_advertised_prefixes_patch_execute()` or `compute_public_advertised_prefixes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_advertised_prefixes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_advertised_prefixes_patch_task()`.
/// For the simplest API, use `compute_public_advertised_prefixes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_advertised_prefixes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_advertised_prefixes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_advertised_prefixes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: publicAdvertisedPrefix
    pub publicAdvertisedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_patch_builder()` + `compute_public_advertised_prefixes_patch_execute()`.
/// For task-level control, use `compute_public_advertised_prefixes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_patch(
    client: &SimpleHttpClient,
    args: &ComputePublicAdvertisedPrefixesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_patch_builder(
        client,
        &args.project,
        &args.publicAdvertisedPrefix,
        &args.requestId,
    )?;
    compute_public_advertised_prefixes_patch_execute(builder)
}

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw
/// Withdraws the specified PublicAdvertisedPrefix
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_withdraw_execute()` to send, or `compute_public_advertised_prefixes_withdraw` for simplest API.

pub fn compute_public_advertised_prefixes_withdraw_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    publicAdvertisedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}/withdraw",
        project,
        publicAdvertisedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw
/// Withdraws the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_advertised_prefixes_withdraw_execute()` or `compute_public_advertised_prefixes_withdraw`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_withdraw_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_withdraw_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw
/// Withdraws the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_advertised_prefixes_withdraw_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_advertised_prefixes_withdraw_task()`.
/// For the simplest API, use `compute_public_advertised_prefixes_withdraw()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_advertised_prefixes_withdraw_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_advertised_prefixes_withdraw_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_advertised_prefixes_withdraw_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_advertised_prefixes_withdraw`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesWithdrawArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: publicAdvertisedPrefix
    pub publicAdvertisedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw
/// Withdraws the specified PublicAdvertisedPrefix
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_withdraw_builder()` + `compute_public_advertised_prefixes_withdraw_execute()`.
/// For task-level control, use `compute_public_advertised_prefixes_withdraw_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_withdraw(
    client: &SimpleHttpClient,
    args: &ComputePublicAdvertisedPrefixesWithdrawArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_withdraw_builder(
        client,
        &args.project,
        &args.publicAdvertisedPrefix,
        &args.requestId,
    )?;
    compute_public_advertised_prefixes_withdraw_execute(builder)
}

/// GET projects/{project}/aggregated/publicDelegatedPrefixes
/// Lists all PublicDelegatedPrefix resources owned by the specific project across all scopes.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_aggregated_list_execute()` to send, or `compute_public_delegated_prefixes_aggregated_list` for simplest API.

pub fn compute_public_delegated_prefixes_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/publicDelegatedPrefixes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/publicDelegatedPrefixes
/// Lists all PublicDelegatedPrefix resources owned by the specific project across all scopes.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_delegated_prefixes_aggregated_list_execute()` or `compute_public_delegated_prefixes_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefixAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/publicDelegatedPrefixes
/// Lists all PublicDelegatedPrefix resources owned by the specific project across all scopes.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_delegated_prefixes_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_delegated_prefixes_aggregated_list_task()`.
/// For the simplest API, use `compute_public_delegated_prefixes_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_delegated_prefixes_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublicDelegatedPrefixAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_public_delegated_prefixes_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_delegated_prefixes_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/publicDelegatedPrefixes
/// Lists all PublicDelegatedPrefix resources owned by the specific project across all scopes.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_aggregated_list_builder()` + `compute_public_delegated_prefixes_aggregated_list_execute()`.
/// For task-level control, use `compute_public_delegated_prefixes_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputePublicDelegatedPrefixesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublicDelegatedPrefixAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_public_delegated_prefixes_aggregated_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce
/// Announces the specified PublicDelegatedPrefix in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_announce_execute()` to send, or `compute_public_delegated_prefixes_announce` for simplest API.

pub fn compute_public_delegated_prefixes_announce_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    publicDelegatedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}/announce",
        project,
        region,
        publicDelegatedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce
/// Announces the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_delegated_prefixes_announce_execute()` or `compute_public_delegated_prefixes_announce`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_announce_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_announce_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce
/// Announces the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_delegated_prefixes_announce_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_delegated_prefixes_announce_task()`.
/// For the simplest API, use `compute_public_delegated_prefixes_announce()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_announce_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_delegated_prefixes_announce_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_delegated_prefixes_announce_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_delegated_prefixes_announce`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesAnnounceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: publicDelegatedPrefix
    pub publicDelegatedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce
/// Announces the specified PublicDelegatedPrefix in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_announce_builder()` + `compute_public_delegated_prefixes_announce_execute()`.
/// For task-level control, use `compute_public_delegated_prefixes_announce_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_announce(
    client: &SimpleHttpClient,
    args: &ComputePublicDelegatedPrefixesAnnounceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_announce_builder(
        client,
        &args.project,
        &args.region,
        &args.publicDelegatedPrefix,
        &args.requestId,
    )?;
    compute_public_delegated_prefixes_announce_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified PublicDelegatedPrefix in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_delete_execute()` to send, or `compute_public_delegated_prefixes_delete` for simplest API.

pub fn compute_public_delegated_prefixes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    publicDelegatedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}",
        project,
        region,
        publicDelegatedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_delegated_prefixes_delete_execute()` or `compute_public_delegated_prefixes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_delegated_prefixes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_delegated_prefixes_delete_task()`.
/// For the simplest API, use `compute_public_delegated_prefixes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_delegated_prefixes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_delegated_prefixes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_delegated_prefixes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: publicDelegatedPrefix
    pub publicDelegatedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified PublicDelegatedPrefix in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_delete_builder()` + `compute_public_delegated_prefixes_delete_execute()`.
/// For task-level control, use `compute_public_delegated_prefixes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_delete(
    client: &SimpleHttpClient,
    args: &ComputePublicDelegatedPrefixesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.publicDelegatedPrefix,
        &args.requestId,
    )?;
    compute_public_delegated_prefixes_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified PublicDelegatedPrefix resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_get_execute()` to send, or `compute_public_delegated_prefixes_get` for simplest API.

pub fn compute_public_delegated_prefixes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    publicDelegatedPrefix: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}",
        project,
        region,
        publicDelegatedPrefix,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified PublicDelegatedPrefix resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_delegated_prefixes_get_execute()` or `compute_public_delegated_prefixes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified PublicDelegatedPrefix resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_delegated_prefixes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_delegated_prefixes_get_task()`.
/// For the simplest API, use `compute_public_delegated_prefixes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_delegated_prefixes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_public_delegated_prefixes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_delegated_prefixes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: publicDelegatedPrefix
    pub publicDelegatedPrefix: String,
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified PublicDelegatedPrefix resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_get_builder()` + `compute_public_delegated_prefixes_get_execute()`.
/// For task-level control, use `compute_public_delegated_prefixes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_get(
    client: &SimpleHttpClient,
    args: &ComputePublicDelegatedPrefixesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_get_builder(
        client,
        &args.project,
        &args.region,
        &args.publicDelegatedPrefix,
    )?;
    compute_public_delegated_prefixes_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Creates a PublicDelegatedPrefix in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_insert_execute()` to send, or `compute_public_delegated_prefixes_insert` for simplest API.

pub fn compute_public_delegated_prefixes_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Creates a PublicDelegatedPrefix in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_delegated_prefixes_insert_execute()` or `compute_public_delegated_prefixes_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Creates a PublicDelegatedPrefix in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_delegated_prefixes_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_delegated_prefixes_insert_task()`.
/// For the simplest API, use `compute_public_delegated_prefixes_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_delegated_prefixes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_delegated_prefixes_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_delegated_prefixes_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Creates a PublicDelegatedPrefix in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_insert_builder()` + `compute_public_delegated_prefixes_insert_execute()`.
/// For task-level control, use `compute_public_delegated_prefixes_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_insert(
    client: &SimpleHttpClient,
    args: &ComputePublicDelegatedPrefixesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_public_delegated_prefixes_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Lists the PublicDelegatedPrefixes for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_list_execute()` to send, or `compute_public_delegated_prefixes_list` for simplest API.

pub fn compute_public_delegated_prefixes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Lists the PublicDelegatedPrefixes for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_delegated_prefixes_list_execute()` or `compute_public_delegated_prefixes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Lists the PublicDelegatedPrefixes for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_delegated_prefixes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_delegated_prefixes_list_task()`.
/// For the simplest API, use `compute_public_delegated_prefixes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_delegated_prefixes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_public_delegated_prefixes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_delegated_prefixes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Lists the PublicDelegatedPrefixes for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_list_builder()` + `compute_public_delegated_prefixes_list_execute()`.
/// For task-level control, use `compute_public_delegated_prefixes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_list(
    client: &SimpleHttpClient,
    args: &ComputePublicDelegatedPrefixesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_public_delegated_prefixes_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_patch_execute()` to send, or `compute_public_delegated_prefixes_patch` for simplest API.

pub fn compute_public_delegated_prefixes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    publicDelegatedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}",
        project,
        region,
        publicDelegatedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_delegated_prefixes_patch_execute()` or `compute_public_delegated_prefixes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_delegated_prefixes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_delegated_prefixes_patch_task()`.
/// For the simplest API, use `compute_public_delegated_prefixes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_delegated_prefixes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_delegated_prefixes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_delegated_prefixes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: publicDelegatedPrefix
    pub publicDelegatedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_patch_builder()` + `compute_public_delegated_prefixes_patch_execute()`.
/// For task-level control, use `compute_public_delegated_prefixes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_patch(
    client: &SimpleHttpClient,
    args: &ComputePublicDelegatedPrefixesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.publicDelegatedPrefix,
        &args.requestId,
    )?;
    compute_public_delegated_prefixes_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw
/// Withdraws the specified PublicDelegatedPrefix in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_withdraw_execute()` to send, or `compute_public_delegated_prefixes_withdraw` for simplest API.

pub fn compute_public_delegated_prefixes_withdraw_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    publicDelegatedPrefix: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}/withdraw",
        project,
        region,
        publicDelegatedPrefix,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw
/// Withdraws the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_public_delegated_prefixes_withdraw_execute()` or `compute_public_delegated_prefixes_withdraw`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_withdraw_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_withdraw_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw
/// Withdraws the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_public_delegated_prefixes_withdraw_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_public_delegated_prefixes_withdraw_task()`.
/// For the simplest API, use `compute_public_delegated_prefixes_withdraw()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_public_delegated_prefixes_withdraw_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_public_delegated_prefixes_withdraw_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_public_delegated_prefixes_withdraw_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_public_delegated_prefixes_withdraw`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesWithdrawArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: publicDelegatedPrefix
    pub publicDelegatedPrefix: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw
/// Withdraws the specified PublicDelegatedPrefix in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_withdraw_builder()` + `compute_public_delegated_prefixes_withdraw_execute()`.
/// For task-level control, use `compute_public_delegated_prefixes_withdraw_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_withdraw(
    client: &SimpleHttpClient,
    args: &ComputePublicDelegatedPrefixesWithdrawArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_withdraw_builder(
        client,
        &args.project,
        &args.region,
        &args.publicDelegatedPrefix,
        &args.requestId,
    )?;
    compute_public_delegated_prefixes_withdraw_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_delete_execute()` to send, or `compute_region_autoscalers_delete` for simplest API.

pub fn compute_region_autoscalers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    autoscaler: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers/{}",
        project, region, autoscaler,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_autoscalers_delete_execute()` or `compute_region_autoscalers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_autoscalers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_autoscalers_delete_task()`.
/// For the simplest API, use `compute_region_autoscalers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_autoscalers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_autoscalers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_autoscalers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: autoscaler
    pub autoscaler: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_delete_builder()` + `compute_region_autoscalers_delete_execute()`.
/// For task-level control, use `compute_region_autoscalers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionAutoscalersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.autoscaler,
        &args.requestId,
    )?;
    compute_region_autoscalers_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Returns the specified autoscaler.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_get_execute()` to send, or `compute_region_autoscalers_get` for simplest API.

pub fn compute_region_autoscalers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    autoscaler: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers/{}",
        project, region, autoscaler,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Returns the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_autoscalers_get_execute()` or `compute_region_autoscalers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Autoscaler>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Autoscaler = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Returns the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_autoscalers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_autoscalers_get_task()`.
/// For the simplest API, use `compute_region_autoscalers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_autoscalers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Autoscaler>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_autoscalers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_autoscalers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: autoscaler
    pub autoscaler: String,
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Returns the specified autoscaler.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_get_builder()` + `compute_region_autoscalers_get_execute()`.
/// For task-level control, use `compute_region_autoscalers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionAutoscalersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Autoscaler>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_get_builder(
        client,
        &args.project,
        &args.region,
        &args.autoscaler,
    )?;
    compute_region_autoscalers_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_insert_execute()` to send, or `compute_region_autoscalers_insert` for simplest API.

pub fn compute_region_autoscalers_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_autoscalers_insert_execute()` or `compute_region_autoscalers_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_autoscalers_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_autoscalers_insert_task()`.
/// For the simplest API, use `compute_region_autoscalers_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_autoscalers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_autoscalers_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_autoscalers_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_insert_builder()` + `compute_region_autoscalers_insert_execute()`.
/// For task-level control, use `compute_region_autoscalers_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionAutoscalersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_autoscalers_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Retrieves a list of autoscalers contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_list_execute()` to send, or `compute_region_autoscalers_list` for simplest API.

pub fn compute_region_autoscalers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Retrieves a list of autoscalers contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_autoscalers_list_execute()` or `compute_region_autoscalers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionAutoscalerList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionAutoscalerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Retrieves a list of autoscalers contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_autoscalers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_autoscalers_list_task()`.
/// For the simplest API, use `compute_region_autoscalers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_autoscalers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionAutoscalerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_autoscalers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_autoscalers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Retrieves a list of autoscalers contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_list_builder()` + `compute_region_autoscalers_list_execute()`.
/// For task-level control, use `compute_region_autoscalers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionAutoscalersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionAutoscalerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_autoscalers_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_patch_execute()` to send, or `compute_region_autoscalers_patch` for simplest API.

pub fn compute_region_autoscalers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    autoscaler: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = autoscaler.as_ref() {
        query_parts.push(format!("autoscaler={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_autoscalers_patch_execute()` or `compute_region_autoscalers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_autoscalers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_autoscalers_patch_task()`.
/// For the simplest API, use `compute_region_autoscalers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_autoscalers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_autoscalers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_autoscalers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: autoscaler
    pub autoscaler: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_patch_builder()` + `compute_region_autoscalers_patch_execute()`.
/// For task-level control, use `compute_region_autoscalers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionAutoscalersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.autoscaler,
        &args.requestId,
    )?;
    compute_region_autoscalers_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_test_iam_permissions_execute()` to send, or `compute_region_autoscalers_test_iam_permissions` for simplest API.

pub fn compute_region_autoscalers_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_autoscalers_test_iam_permissions_execute()` or `compute_region_autoscalers_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_autoscalers_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_autoscalers_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_autoscalers_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_autoscalers_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_autoscalers_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_autoscalers_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_test_iam_permissions_builder()` + `compute_region_autoscalers_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_autoscalers_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionAutoscalersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_autoscalers_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_update_execute()` to send, or `compute_region_autoscalers_update` for simplest API.

pub fn compute_region_autoscalers_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    autoscaler: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = autoscaler.as_ref() {
        query_parts.push(format!("autoscaler={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_autoscalers_update_execute()` or `compute_region_autoscalers_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_autoscalers_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_autoscalers_update_task()`.
/// For the simplest API, use `compute_region_autoscalers_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_autoscalers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_autoscalers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_autoscalers_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_autoscalers_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: autoscaler
    pub autoscaler: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_update_builder()` + `compute_region_autoscalers_update_execute()`.
/// For task-level control, use `compute_region_autoscalers_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_update(
    client: &SimpleHttpClient,
    args: &ComputeRegionAutoscalersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_update_builder(
        client,
        &args.project,
        &args.region,
        &args.autoscaler,
        &args.requestId,
    )?;
    compute_region_autoscalers_update_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Deletes the specified regional BackendBucket resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_delete_execute()` to send, or `compute_region_backend_buckets_delete` for simplest API.

pub fn compute_region_backend_buckets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendBucket: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}",
        project, region, backendBucket,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Deletes the specified regional BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_delete_execute()` or `compute_region_backend_buckets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Deletes the specified regional BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_delete_task()`.
/// For the simplest API, use `compute_region_backend_buckets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Deletes the specified regional BackendBucket resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_delete_builder()` + `compute_region_backend_buckets_delete_execute()`.
/// For task-level control, use `compute_region_backend_buckets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.backendBucket,
        &args.requestId,
    )?;
    compute_region_backend_buckets_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Returns the specified regional BackendBucket resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_get_execute()` to send, or `compute_region_backend_buckets_get` for simplest API.

pub fn compute_region_backend_buckets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendBucket: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}",
        project, region, backendBucket,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Returns the specified regional BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_get_execute()` or `compute_region_backend_buckets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Returns the specified regional BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_get_task()`.
/// For the simplest API, use `compute_region_backend_buckets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucket>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
}

/// GET projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Returns the specified regional BackendBucket resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_get_builder()` + `compute_region_backend_buckets_get_execute()`.
/// For task-level control, use `compute_region_backend_buckets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucket>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_get_builder(
        client,
        &args.project,
        &args.region,
        &args.backendBucket,
    )?;
    compute_region_backend_buckets_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_get_iam_policy_execute()` to send, or `compute_region_backend_buckets_get_iam_policy` for simplest API.

pub fn compute_region_backend_buckets_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_get_iam_policy_execute()` or `compute_region_backend_buckets_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_get_iam_policy_task()`.
/// For the simplest API, use `compute_region_backend_buckets_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_get_iam_policy_builder()` + `compute_region_backend_buckets_get_iam_policy_execute()`.
/// For task-level control, use `compute_region_backend_buckets_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_region_backend_buckets_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/backendBuckets
/// Creates a RegionBackendBucket in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_insert_execute()` to send, or `compute_region_backend_buckets_insert` for simplest API.

pub fn compute_region_backend_buckets_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/backendBuckets
/// Creates a RegionBackendBucket in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_insert_execute()` or `compute_region_backend_buckets_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/backendBuckets
/// Creates a RegionBackendBucket in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_insert_task()`.
/// For the simplest API, use `compute_region_backend_buckets_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/backendBuckets
/// Creates a RegionBackendBucket in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_insert_builder()` + `compute_region_backend_buckets_insert_execute()`.
/// For task-level control, use `compute_region_backend_buckets_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_backend_buckets_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_list_execute()` to send, or `compute_region_backend_buckets_list` for simplest API.

pub fn compute_region_backend_buckets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_list_execute()` or `compute_region_backend_buckets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucketList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_list_task()`.
/// For the simplest API, use `compute_region_backend_buckets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_list_builder()` + `compute_region_backend_buckets_list_execute()`.
/// For task-level control, use `compute_region_backend_buckets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_backend_buckets_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendBuckets/listUsable
/// Retrieves a list of all usable backend buckets in the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_list_usable_execute()` to send, or `compute_region_backend_buckets_list_usable` for simplest API.

pub fn compute_region_backend_buckets_list_usable_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/listUsable",
        project,
        region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/backendBuckets/listUsable
/// Retrieves a list of all usable backend buckets in the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_list_usable_execute()` or `compute_region_backend_buckets_list_usable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_list_usable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketListUsable>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucketListUsable = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/backendBuckets/listUsable
/// Retrieves a list of all usable backend buckets in the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_list_usable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_list_usable_task()`.
/// For the simplest API, use `compute_region_backend_buckets_list_usable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_list_usable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_list_usable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_list_usable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsListUsableArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/backendBuckets/listUsable
/// Retrieves a list of all usable backend buckets in the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_list_usable_builder()` + `compute_region_backend_buckets_list_usable_execute()`.
/// For task-level control, use `compute_region_backend_buckets_list_usable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_list_usable(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsListUsableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_list_usable_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_backend_buckets_list_usable_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Updates the specified  BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_patch_execute()` to send, or `compute_region_backend_buckets_patch` for simplest API.

pub fn compute_region_backend_buckets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendBucket: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}",
        project, region, backendBucket,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Updates the specified  BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_patch_execute()` or `compute_region_backend_buckets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Updates the specified  BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_patch_task()`.
/// For the simplest API, use `compute_region_backend_buckets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendBucket
    pub backendBucket: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/backendBuckets/{backendBucket}
/// Updates the specified  BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_patch_builder()` + `compute_region_backend_buckets_patch_execute()`.
/// For task-level control, use `compute_region_backend_buckets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.backendBucket,
        &args.requestId,
    )?;
    compute_region_backend_buckets_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_set_iam_policy_execute()` to send, or `compute_region_backend_buckets_set_iam_policy` for simplest API.

pub fn compute_region_backend_buckets_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_set_iam_policy_execute()` or `compute_region_backend_buckets_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_set_iam_policy_task()`.
/// For the simplest API, use `compute_region_backend_buckets_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_set_iam_policy_builder()` + `compute_region_backend_buckets_set_iam_policy_execute()`.
/// For task-level control, use `compute_region_backend_buckets_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_backend_buckets_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_buckets_test_iam_permissions_execute()` to send, or `compute_region_backend_buckets_test_iam_permissions` for simplest API.

pub fn compute_region_backend_buckets_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_buckets_test_iam_permissions_execute()` or `compute_region_backend_buckets_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_buckets_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_buckets_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_backend_buckets_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_buckets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_buckets_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_buckets_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_buckets_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_buckets_test_iam_permissions_builder()` + `compute_region_backend_buckets_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_backend_buckets_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_buckets_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendBucketsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_buckets_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_backend_buckets_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/backendServices/{backendService}
/// Deletes the specified regional BackendService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_delete_execute()` to send, or `compute_region_backend_services_delete` for simplest API.

pub fn compute_region_backend_services_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}",
        project, region, backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/backendServices/{backendService}
/// Deletes the specified regional BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_delete_execute()` or `compute_region_backend_services_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/backendServices/{backendService}
/// Deletes the specified regional BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_delete_task()`.
/// For the simplest API, use `compute_region_backend_services_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/backendServices/{backendService}
/// Deletes the specified regional BackendService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_delete_builder()` + `compute_region_backend_services_delete_execute()`.
/// For task-level control, use `compute_region_backend_services_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.backendService,
        &args.requestId,
    )?;
    compute_region_backend_services_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Returns the specified regional BackendService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_get_execute()` to send, or `compute_region_backend_services_get` for simplest API.

pub fn compute_region_backend_services_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendService: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}",
        project, region, backendService,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Returns the specified regional BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_get_execute()` or `compute_region_backend_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendService>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Returns the specified regional BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_get_task()`.
/// For the simplest API, use `compute_region_backend_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendService
    pub backendService: String,
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Returns the specified regional BackendService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_get_builder()` + `compute_region_backend_services_get_execute()`.
/// For task-level control, use `compute_region_backend_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_get_builder(
        client,
        &args.project,
        &args.region,
        &args.backendService,
    )?;
    compute_region_backend_services_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this regional BackendService.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_get_health_execute()` to send, or `compute_region_backend_services_get_health` for simplest API.

pub fn compute_region_backend_services_get_health_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendService: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/getHealth",
        project,
        region,
        backendService,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this regional BackendService.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_get_health_execute()` or `compute_region_backend_services_get_health`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_health_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceGroupHealth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this regional BackendService.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_get_health_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_get_health_task()`.
/// For the simplest API, use `compute_region_backend_services_get_health()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_get_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_get_health_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_get_health`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesGetHealthArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendService
    pub backendService: String,
}

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this regional BackendService.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_get_health_builder()` + `compute_region_backend_services_get_health_execute()`.
/// For task-level control, use `compute_region_backend_services_get_health_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_health(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesGetHealthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_get_health_builder(
        client,
        &args.project,
        &args.region,
        &args.backendService,
    )?;
    compute_region_backend_services_get_health_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_get_iam_policy_execute()` to send, or `compute_region_backend_services_get_iam_policy` for simplest API.

pub fn compute_region_backend_services_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_get_iam_policy_execute()` or `compute_region_backend_services_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_get_iam_policy_task()`.
/// For the simplest API, use `compute_region_backend_services_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_get_iam_policy_builder()` + `compute_region_backend_services_get_iam_policy_execute()`.
/// For task-level control, use `compute_region_backend_services_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_region_backend_services_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/backendServices
/// Creates a regional BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_insert_execute()` to send, or `compute_region_backend_services_insert` for simplest API.

pub fn compute_region_backend_services_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/backendServices
/// Creates a regional BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_insert_execute()` or `compute_region_backend_services_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/backendServices
/// Creates a regional BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_insert_task()`.
/// For the simplest API, use `compute_region_backend_services_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/backendServices
/// Creates a regional BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_insert_builder()` + `compute_region_backend_services_insert_execute()`.
/// For task-level control, use `compute_region_backend_services_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_backend_services_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices
/// Retrieves the list of regional BackendService resources available to the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_list_execute()` to send, or `compute_region_backend_services_list` for simplest API.

pub fn compute_region_backend_services_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/backendServices
/// Retrieves the list of regional BackendService resources available to the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_list_execute()` or `compute_region_backend_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/backendServices
/// Retrieves the list of regional BackendService resources available to the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_list_task()`.
/// For the simplest API, use `compute_region_backend_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/backendServices
/// Retrieves the list of regional BackendService resources available to the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_list_builder()` + `compute_region_backend_services_list_execute()`.
/// For task-level control, use `compute_region_backend_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_backend_services_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_list_usable_execute()` to send, or `compute_region_backend_services_list_usable` for simplest API.

pub fn compute_region_backend_services_list_usable_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/listUsable",
        project,
        region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_list_usable_execute()` or `compute_region_backend_services_list_usable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_list_usable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceListUsable>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceListUsable = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_list_usable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_list_usable_task()`.
/// For the simplest API, use `compute_region_backend_services_list_usable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_list_usable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_list_usable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_list_usable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesListUsableArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_list_usable_builder()` + `compute_region_backend_services_list_usable_execute()`.
/// For task-level control, use `compute_region_backend_services_list_usable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_list_usable(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesListUsableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_list_usable_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_backend_services_list_usable_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Understanding backend services This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_patch_execute()` to send, or `compute_region_backend_services_patch` for simplest API.

pub fn compute_region_backend_services_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}",
        project, region, backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Understanding backend services This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_patch_execute()` or `compute_region_backend_services_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Understanding backend services This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_patch_task()`.
/// For the simplest API, use `compute_region_backend_services_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Understanding backend services This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_patch_builder()` + `compute_region_backend_services_patch_execute()`.
/// For task-level control, use `compute_region_backend_services_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.backendService,
        &args.requestId,
    )?;
    compute_region_backend_services_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_set_iam_policy_execute()` to send, or `compute_region_backend_services_set_iam_policy` for simplest API.

pub fn compute_region_backend_services_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_set_iam_policy_execute()` or `compute_region_backend_services_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_set_iam_policy_task()`.
/// For the simplest API, use `compute_region_backend_services_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_set_iam_policy_builder()` + `compute_region_backend_services_set_iam_policy_execute()`.
/// For task-level control, use `compute_region_backend_services_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_backend_services_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_set_security_policy_execute()` to send, or `compute_region_backend_services_set_security_policy` for simplest API.

pub fn compute_region_backend_services_set_security_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/setSecurityPolicy",
        project,
        region,
        backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_set_security_policy_execute()` or `compute_region_backend_services_set_security_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_set_security_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_set_security_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_set_security_policy_task()`.
/// For the simplest API, use `compute_region_backend_services_set_security_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_set_security_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_set_security_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesSetSecurityPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_set_security_policy_builder()` + `compute_region_backend_services_set_security_policy_execute()`.
/// For task-level control, use `compute_region_backend_services_set_security_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_set_security_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesSetSecurityPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_set_security_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.backendService,
        &args.requestId,
    )?;
    compute_region_backend_services_set_security_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_test_iam_permissions_execute()` to send, or `compute_region_backend_services_test_iam_permissions` for simplest API.

pub fn compute_region_backend_services_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_test_iam_permissions_execute()` or `compute_region_backend_services_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_backend_services_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_test_iam_permissions_builder()` + `compute_region_backend_services_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_backend_services_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_backend_services_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Backend services overview.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_update_execute()` to send, or `compute_region_backend_services_update` for simplest API.

pub fn compute_region_backend_services_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    backendService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}",
        project, region, backendService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_backend_services_update_execute()` or `compute_region_backend_services_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_backend_services_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_backend_services_update_task()`.
/// For the simplest API, use `compute_region_backend_services_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_backend_services_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_backend_services_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_backend_services_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_backend_services_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: backendService
    pub backendService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Backend services overview.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_update_builder()` + `compute_region_backend_services_update_execute()`.
/// For task-level control, use `compute_region_backend_services_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_update(
    client: &SimpleHttpClient,
    args: &ComputeRegionBackendServicesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_update_builder(
        client,
        &args.project,
        &args.region,
        &args.backendService,
        &args.requestId,
    )?;
    compute_region_backend_services_update_execute(builder)
}

/// GET projects/{project}/aggregated/commitments
/// Retrieves an aggregated list of commitments by region.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_aggregated_list_execute()` to send, or `compute_region_commitments_aggregated_list` for simplest API.

pub fn compute_region_commitments_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/commitments",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/commitments
/// Retrieves an aggregated list of commitments by region.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_commitments_aggregated_list_execute()` or `compute_region_commitments_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitmentAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/commitments
/// Retrieves an aggregated list of commitments by region.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_commitments_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_commitments_aggregated_list_task()`.
/// For the simplest API, use `compute_region_commitments_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_commitments_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitmentAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_commitments_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_commitments_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/commitments
/// Retrieves an aggregated list of commitments by region.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_aggregated_list_builder()` + `compute_region_commitments_aggregated_list_execute()`.
/// For task-level control, use `compute_region_commitments_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionCommitmentsAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitmentAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_region_commitments_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Returns the specified commitment resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_get_execute()` to send, or `compute_region_commitments_get` for simplest API.

pub fn compute_region_commitments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    commitment: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/commitments/{}",
        project, region, commitment,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Returns the specified commitment resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_commitments_get_execute()` or `compute_region_commitments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Commitment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Commitment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Returns the specified commitment resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_commitments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_commitments_get_task()`.
/// For the simplest API, use `compute_region_commitments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_commitments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Commitment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_commitments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_commitments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: commitment
    pub commitment: String,
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Returns the specified commitment resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_get_builder()` + `compute_region_commitments_get_execute()`.
/// For task-level control, use `compute_region_commitments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionCommitmentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Commitment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_get_builder(
        client,
        &args.project,
        &args.region,
        &args.commitment,
    )?;
    compute_region_commitments_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/commitments
/// Creates a commitment in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_insert_execute()` to send, or `compute_region_commitments_insert` for simplest API.

pub fn compute_region_commitments_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/commitments",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/commitments
/// Creates a commitment in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_commitments_insert_execute()` or `compute_region_commitments_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/commitments
/// Creates a commitment in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_commitments_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_commitments_insert_task()`.
/// For the simplest API, use `compute_region_commitments_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_commitments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_commitments_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_commitments_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/commitments
/// Creates a commitment in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_insert_builder()` + `compute_region_commitments_insert_execute()`.
/// For task-level control, use `compute_region_commitments_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionCommitmentsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_commitments_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/commitments
/// Retrieves a list of commitments contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_list_execute()` to send, or `compute_region_commitments_list` for simplest API.

pub fn compute_region_commitments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/commitments",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/commitments
/// Retrieves a list of commitments contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_commitments_list_execute()` or `compute_region_commitments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitmentList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/commitments
/// Retrieves a list of commitments contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_commitments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_commitments_list_task()`.
/// For the simplest API, use `compute_region_commitments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_commitments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_commitments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_commitments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/commitments
/// Retrieves a list of commitments contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_list_builder()` + `compute_region_commitments_list_execute()`.
/// For task-level control, use `compute_region_commitments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionCommitmentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_commitments_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/commitments/{commitment}
/// Updates the specified commitment with the data included in the request. Update is performed only on selected fields included as part of update-mask. Only the following fields can be updated: auto_renew and plan.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_update_execute()` to send, or `compute_region_commitments_update` for simplest API.

pub fn compute_region_commitments_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    commitment: &String,
    paths: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/commitments/{}",
        project, region, commitment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = paths.as_ref() {
        query_parts.push(format!("paths={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/commitments/{commitment}
/// Updates the specified commitment with the data included in the request. Update is performed only on selected fields included as part of update-mask. Only the following fields can be updated: auto_renew and plan.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_commitments_update_execute()` or `compute_region_commitments_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/commitments/{commitment}
/// Updates the specified commitment with the data included in the request. Update is performed only on selected fields included as part of update-mask. Only the following fields can be updated: auto_renew and plan.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_commitments_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_commitments_update_task()`.
/// For the simplest API, use `compute_region_commitments_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_commitments_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_commitments_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_commitments_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_commitments_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: commitment
    pub commitment: String,
    /// Query parameter: paths
    pub paths: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/commitments/{commitment}
/// Updates the specified commitment with the data included in the request. Update is performed only on selected fields included as part of update-mask. Only the following fields can be updated: auto_renew and plan.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_update_builder()` + `compute_region_commitments_update_execute()`.
/// For task-level control, use `compute_region_commitments_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_update(
    client: &SimpleHttpClient,
    args: &ComputeRegionCommitmentsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_update_builder(
        client,
        &args.project,
        &args.region,
        &args.commitment,
        &args.paths,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_region_commitments_update_execute(builder)
}

/// GET projects/{project}/aggregated/compositeHealthChecks
/// Retrieves the list of all CompositeHealthCheck resources (all regional) available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_aggregated_list_execute()` to send, or `compute_region_composite_health_checks_aggregated_list` for simplest API.

pub fn compute_region_composite_health_checks_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/compositeHealthChecks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/compositeHealthChecks
/// Retrieves the list of all CompositeHealthCheck resources (all regional) available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_composite_health_checks_aggregated_list_execute()` or `compute_region_composite_health_checks_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompositeHealthCheckAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompositeHealthCheckAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/compositeHealthChecks
/// Retrieves the list of all CompositeHealthCheck resources (all regional) available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_composite_health_checks_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_composite_health_checks_aggregated_list_task()`.
/// For the simplest API, use `compute_region_composite_health_checks_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_composite_health_checks_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CompositeHealthCheckAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_composite_health_checks_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_composite_health_checks_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/compositeHealthChecks
/// Retrieves the list of all CompositeHealthCheck resources (all regional) available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_aggregated_list_builder()` + `compute_region_composite_health_checks_aggregated_list_execute()`.
/// For task-level control, use `compute_region_composite_health_checks_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionCompositeHealthChecksAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CompositeHealthCheckAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_region_composite_health_checks_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Deletes the specified CompositeHealthCheck in the given region
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_delete_execute()` to send, or `compute_region_composite_health_checks_delete` for simplest API.

pub fn compute_region_composite_health_checks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    compositeHealthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}",
        project, region, compositeHealthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Deletes the specified CompositeHealthCheck in the given region
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_composite_health_checks_delete_execute()` or `compute_region_composite_health_checks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Deletes the specified CompositeHealthCheck in the given region
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_composite_health_checks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_composite_health_checks_delete_task()`.
/// For the simplest API, use `compute_region_composite_health_checks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_composite_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_composite_health_checks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_composite_health_checks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: compositeHealthCheck
    pub compositeHealthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Deletes the specified CompositeHealthCheck in the given region
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_delete_builder()` + `compute_region_composite_health_checks_delete_execute()`.
/// For task-level control, use `compute_region_composite_health_checks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionCompositeHealthChecksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.compositeHealthCheck,
        &args.requestId,
    )?;
    compute_region_composite_health_checks_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Returns the specified CompositeHealthCheck resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_get_execute()` to send, or `compute_region_composite_health_checks_get` for simplest API.

pub fn compute_region_composite_health_checks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    compositeHealthCheck: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}",
        project, region, compositeHealthCheck,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Returns the specified CompositeHealthCheck resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_composite_health_checks_get_execute()` or `compute_region_composite_health_checks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompositeHealthCheck>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompositeHealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Returns the specified CompositeHealthCheck resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_composite_health_checks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_composite_health_checks_get_task()`.
/// For the simplest API, use `compute_region_composite_health_checks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_composite_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompositeHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_composite_health_checks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_composite_health_checks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: compositeHealthCheck
    pub compositeHealthCheck: String,
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Returns the specified CompositeHealthCheck resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_get_builder()` + `compute_region_composite_health_checks_get_execute()`.
/// For task-level control, use `compute_region_composite_health_checks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionCompositeHealthChecksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompositeHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_get_builder(
        client,
        &args.project,
        &args.region,
        &args.compositeHealthCheck,
    )?;
    compute_region_composite_health_checks_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}/getHealth
/// Gets the most recent health check results for this regional CompositeHealthCheck.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_get_health_execute()` to send, or `compute_region_composite_health_checks_get_health` for simplest API.

pub fn compute_region_composite_health_checks_get_health_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    compositeHealthCheck: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}/getHealth",
        project,
        region,
        compositeHealthCheck,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}/getHealth
/// Gets the most recent health check results for this regional CompositeHealthCheck.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_composite_health_checks_get_health_execute()` or `compute_region_composite_health_checks_get_health`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_get_health_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompositeHealthCheckHealth>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompositeHealthCheckHealth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}/getHealth
/// Gets the most recent health check results for this regional CompositeHealthCheck.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_composite_health_checks_get_health_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_composite_health_checks_get_health_task()`.
/// For the simplest API, use `compute_region_composite_health_checks_get_health()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_composite_health_checks_get_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CompositeHealthCheckHealth>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_composite_health_checks_get_health_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_composite_health_checks_get_health`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksGetHealthArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: compositeHealthCheck
    pub compositeHealthCheck: String,
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}/getHealth
/// Gets the most recent health check results for this regional CompositeHealthCheck.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_get_health_builder()` + `compute_region_composite_health_checks_get_health_execute()`.
/// For task-level control, use `compute_region_composite_health_checks_get_health_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_get_health(
    client: &SimpleHttpClient,
    args: &ComputeRegionCompositeHealthChecksGetHealthArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CompositeHealthCheckHealth>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_get_health_builder(
        client,
        &args.project,
        &args.region,
        &args.compositeHealthCheck,
    )?;
    compute_region_composite_health_checks_get_health_execute(builder)
}

/// POST projects/{project}/regions/{region}/compositeHealthChecks
/// Create a CompositeHealthCheck in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_insert_execute()` to send, or `compute_region_composite_health_checks_insert` for simplest API.

pub fn compute_region_composite_health_checks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/compositeHealthChecks
/// Create a CompositeHealthCheck in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_composite_health_checks_insert_execute()` or `compute_region_composite_health_checks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/compositeHealthChecks
/// Create a CompositeHealthCheck in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_composite_health_checks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_composite_health_checks_insert_task()`.
/// For the simplest API, use `compute_region_composite_health_checks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_composite_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_composite_health_checks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_composite_health_checks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/compositeHealthChecks
/// Create a CompositeHealthCheck in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_insert_builder()` + `compute_region_composite_health_checks_insert_execute()`.
/// For task-level control, use `compute_region_composite_health_checks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionCompositeHealthChecksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_composite_health_checks_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Lists the CompositeHealthChecks for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_list_execute()` to send, or `compute_region_composite_health_checks_list` for simplest API.

pub fn compute_region_composite_health_checks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Lists the CompositeHealthChecks for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_composite_health_checks_list_execute()` or `compute_region_composite_health_checks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompositeHealthCheckList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompositeHealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Lists the CompositeHealthChecks for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_composite_health_checks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_composite_health_checks_list_task()`.
/// For the simplest API, use `compute_region_composite_health_checks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_composite_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompositeHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_composite_health_checks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_composite_health_checks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Lists the CompositeHealthChecks for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_list_builder()` + `compute_region_composite_health_checks_list_execute()`.
/// For task-level control, use `compute_region_composite_health_checks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionCompositeHealthChecksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompositeHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_composite_health_checks_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Updates the specified regional CompositeHealthCheck resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_patch_execute()` to send, or `compute_region_composite_health_checks_patch` for simplest API.

pub fn compute_region_composite_health_checks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    compositeHealthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}",
        project, region, compositeHealthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Updates the specified regional CompositeHealthCheck resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_composite_health_checks_patch_execute()` or `compute_region_composite_health_checks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Updates the specified regional CompositeHealthCheck resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_composite_health_checks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_composite_health_checks_patch_task()`.
/// For the simplest API, use `compute_region_composite_health_checks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_composite_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_composite_health_checks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_composite_health_checks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: compositeHealthCheck
    pub compositeHealthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Updates the specified regional CompositeHealthCheck resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_patch_builder()` + `compute_region_composite_health_checks_patch_execute()`.
/// For task-level control, use `compute_region_composite_health_checks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionCompositeHealthChecksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.compositeHealthCheck,
        &args.requestId,
    )?;
    compute_region_composite_health_checks_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_test_iam_permissions_execute()` to send, or `compute_region_composite_health_checks_test_iam_permissions` for simplest API.

pub fn compute_region_composite_health_checks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_composite_health_checks_test_iam_permissions_execute()` or `compute_region_composite_health_checks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_composite_health_checks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_composite_health_checks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_composite_health_checks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_composite_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_composite_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_composite_health_checks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_composite_health_checks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_test_iam_permissions_builder()` + `compute_region_composite_health_checks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_composite_health_checks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionCompositeHealthChecksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_composite_health_checks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/diskTypes/{diskType}
/// Returns the specified regional disk type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disk_types_get_execute()` to send, or `compute_region_disk_types_get` for simplest API.

pub fn compute_region_disk_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    diskType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/diskTypes/{}",
        project, region, diskType,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/diskTypes/{diskType}
/// Returns the specified regional disk type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disk_types_get_execute()` or `compute_region_disk_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disk_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disk_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/diskTypes/{diskType}
/// Returns the specified regional disk type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disk_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disk_types_get_task()`.
/// For the simplest API, use `compute_region_disk_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disk_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disk_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disk_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disk_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDiskTypesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: diskType
    pub diskType: String,
}

/// GET projects/{project}/regions/{region}/diskTypes/{diskType}
/// Returns the specified regional disk type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disk_types_get_builder()` + `compute_region_disk_types_get_execute()`.
/// For task-level control, use `compute_region_disk_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disk_types_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionDiskTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_disk_types_get_builder(client, &args.project, &args.region, &args.diskType)?;
    compute_region_disk_types_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/diskTypes
/// Retrieves a list of regional disk types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disk_types_list_execute()` to send, or `compute_region_disk_types_list` for simplest API.

pub fn compute_region_disk_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/diskTypes",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/diskTypes
/// Retrieves a list of regional disk types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disk_types_list_execute()` or `compute_region_disk_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disk_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disk_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionDiskTypeList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionDiskTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/diskTypes
/// Retrieves a list of regional disk types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disk_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disk_types_list_task()`.
/// For the simplest API, use `compute_region_disk_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disk_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disk_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionDiskTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_disk_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disk_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDiskTypesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/diskTypes
/// Retrieves a list of regional disk types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disk_types_list_builder()` + `compute_region_disk_types_list_execute()`.
/// For task-level control, use `compute_region_disk_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disk_types_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionDiskTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionDiskTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_disk_types_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_disk_types_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a regional disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_add_resource_policies_execute()` to send, or `compute_region_disks_add_resource_policies` for simplest API.

pub fn compute_region_disks_add_resource_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/addResourcePolicies",
        project,
        region,
        disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a regional disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_add_resource_policies_execute()` or `compute_region_disks_add_resource_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_add_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_add_resource_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a regional disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_add_resource_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_add_resource_policies_task()`.
/// For the simplest API, use `compute_region_disks_add_resource_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_add_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_add_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_add_resource_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_add_resource_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksAddResourcePoliciesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a regional disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_add_resource_policies_builder()` + `compute_region_disks_add_resource_policies_execute()`.
/// For task-level control, use `compute_region_disks_add_resource_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_add_resource_policies(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksAddResourcePoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_add_resource_policies_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.requestId,
    )?;
    compute_region_disks_add_resource_policies_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_bulk_insert_execute()` to send, or `compute_region_disks_bulk_insert` for simplest API.

pub fn compute_region_disks_bulk_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/bulkInsert",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_bulk_insert_execute()` or `compute_region_disks_bulk_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_bulk_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_bulk_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_bulk_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_bulk_insert_task()`.
/// For the simplest API, use `compute_region_disks_bulk_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_bulk_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_bulk_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_bulk_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_bulk_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksBulkInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_bulk_insert_builder()` + `compute_region_disks_bulk_insert_execute()`.
/// For task-level control, use `compute_region_disks_bulk_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_bulk_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksBulkInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_bulk_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_disks_bulk_insert_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_create_snapshot_execute()` to send, or `compute_region_disks_create_snapshot` for simplest API.

pub fn compute_region_disks_create_snapshot_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/createSnapshot",
        project, region, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_create_snapshot_execute()` or `compute_region_disks_create_snapshot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_create_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_create_snapshot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_create_snapshot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_create_snapshot_task()`.
/// For the simplest API, use `compute_region_disks_create_snapshot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_create_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_create_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_create_snapshot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_create_snapshot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksCreateSnapshotArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_create_snapshot_builder()` + `compute_region_disks_create_snapshot_execute()`.
/// For task-level control, use `compute_region_disks_create_snapshot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_create_snapshot(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksCreateSnapshotArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_create_snapshot_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.requestId,
    )?;
    compute_region_disks_create_snapshot_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/disks/{disk}
/// Deletes the specified regional persistent disk. Deleting a regional disk removes all the replicas of its data permanently and is irreversible. However, deleting a disk does not delete anysnapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_delete_execute()` to send, or `compute_region_disks_delete` for simplest API.

pub fn compute_region_disks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}",
        project, region, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/disks/{disk}
/// Deletes the specified regional persistent disk. Deleting a regional disk removes all the replicas of its data permanently and is irreversible. However, deleting a disk does not delete anysnapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_delete_execute()` or `compute_region_disks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/disks/{disk}
/// Deletes the specified regional persistent disk. Deleting a regional disk removes all the replicas of its data permanently and is irreversible. However, deleting a disk does not delete anysnapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_delete_task()`.
/// For the simplest API, use `compute_region_disks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/disks/{disk}
/// Deletes the specified regional persistent disk. Deleting a regional disk removes all the replicas of its data permanently and is irreversible. However, deleting a disk does not delete anysnapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_delete_builder()` + `compute_region_disks_delete_execute()`.
/// For task-level control, use `compute_region_disks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.requestId,
    )?;
    compute_region_disks_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Returns a specified regional persistent disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_get_execute()` to send, or `compute_region_disks_get` for simplest API.

pub fn compute_region_disks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}",
        project, region, disk,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Returns a specified regional persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_get_execute()` or `compute_region_disks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Disk>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Disk = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Returns a specified regional persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_get_task()`.
/// For the simplest API, use `compute_region_disks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Disk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Returns a specified regional persistent disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_get_builder()` + `compute_region_disks_get_execute()`.
/// For task-level control, use `compute_region_disks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Disk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_disks_get_builder(client, &args.project, &args.region, &args.disk)?;
    compute_region_disks_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_get_iam_policy_execute()` to send, or `compute_region_disks_get_iam_policy` for simplest API.

pub fn compute_region_disks_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_get_iam_policy_execute()` or `compute_region_disks_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_get_iam_policy_task()`.
/// For the simplest API, use `compute_region_disks_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_get_iam_policy_builder()` + `compute_region_disks_get_iam_policy_execute()`.
/// For task-level control, use `compute_region_disks_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_region_disks_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks
/// Creates a persistent regional disk in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_insert_execute()` to send, or `compute_region_disks_insert` for simplest API.

pub fn compute_region_disks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
    sourceImage: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceImage.as_ref() {
        query_parts.push(format!("sourceImage={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks
/// Creates a persistent regional disk in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_insert_execute()` or `compute_region_disks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks
/// Creates a persistent regional disk in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_insert_task()`.
/// For the simplest API, use `compute_region_disks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceImage
    pub sourceImage: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks
/// Creates a persistent regional disk in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_insert_builder()` + `compute_region_disks_insert_execute()`.
/// For task-level control, use `compute_region_disks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
        &args.sourceImage,
    )?;
    compute_region_disks_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks
/// Retrieves the list of persistent disks contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_list_execute()` to send, or `compute_region_disks_list` for simplest API.

pub fn compute_region_disks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/disks
/// Retrieves the list of persistent disks contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_list_execute()` or `compute_region_disks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/disks
/// Retrieves the list of persistent disks contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_list_task()`.
/// For the simplest API, use `compute_region_disks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/disks
/// Retrieves the list of persistent disks contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_list_builder()` + `compute_region_disks_list_execute()`.
/// For task-level control, use `compute_region_disks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_disks_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a regional disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_remove_resource_policies_execute()` to send, or `compute_region_disks_remove_resource_policies` for simplest API.

pub fn compute_region_disks_remove_resource_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/removeResourcePolicies",
        project,
        region,
        disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a regional disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_remove_resource_policies_execute()` or `compute_region_disks_remove_resource_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_remove_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_remove_resource_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a regional disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_remove_resource_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_remove_resource_policies_task()`.
/// For the simplest API, use `compute_region_disks_remove_resource_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_remove_resource_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_remove_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_remove_resource_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_remove_resource_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksRemoveResourcePoliciesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a regional disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_remove_resource_policies_builder()` + `compute_region_disks_remove_resource_policies_execute()`.
/// For task-level control, use `compute_region_disks_remove_resource_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_remove_resource_policies(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksRemoveResourcePoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_remove_resource_policies_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.requestId,
    )?;
    compute_region_disks_remove_resource_policies_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/resize
/// Resizes the specified regional persistent disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_resize_execute()` to send, or `compute_region_disks_resize` for simplest API.

pub fn compute_region_disks_resize_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/resize",
        project, region, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/resize
/// Resizes the specified regional persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_resize_execute()` or `compute_region_disks_resize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_resize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{disk}/resize
/// Resizes the specified regional persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_resize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_resize_task()`.
/// For the simplest API, use `compute_region_disks_resize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_resize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_resize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksResizeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/{disk}/resize
/// Resizes the specified regional persistent disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_resize_builder()` + `compute_region_disks_resize_execute()`.
/// For task-level control, use `compute_region_disks_resize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_resize(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksResizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_resize_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.requestId,
    )?;
    compute_region_disks_resize_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_set_iam_policy_execute()` to send, or `compute_region_disks_set_iam_policy` for simplest API.

pub fn compute_region_disks_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_set_iam_policy_execute()` or `compute_region_disks_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_set_iam_policy_task()`.
/// For the simplest API, use `compute_region_disks_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_set_iam_policy_builder()` + `compute_region_disks_set_iam_policy_execute()`.
/// For task-level control, use `compute_region_disks_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_disks_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{resource}/setLabels
/// Sets the labels on the target regional disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_set_labels_execute()` to send, or `compute_region_disks_set_labels` for simplest API.

pub fn compute_region_disks_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/setLabels",
        project, region, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{resource}/setLabels
/// Sets the labels on the target regional disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_set_labels_execute()` or `compute_region_disks_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{resource}/setLabels
/// Sets the labels on the target regional disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_set_labels_task()`.
/// For the simplest API, use `compute_region_disks_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/{resource}/setLabels
/// Sets the labels on the target regional disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_set_labels_builder()` + `compute_region_disks_set_labels_execute()`.
/// For task-level control, use `compute_region_disks_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_region_disks_set_labels_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_start_async_replication_execute()` to send, or `compute_region_disks_start_async_replication` for simplest API.

pub fn compute_region_disks_start_async_replication_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/startAsyncReplication",
        project,
        region,
        disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_start_async_replication_execute()` or `compute_region_disks_start_async_replication`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_start_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_start_async_replication_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_start_async_replication_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_start_async_replication_task()`.
/// For the simplest API, use `compute_region_disks_start_async_replication()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_start_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_start_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_start_async_replication_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_start_async_replication`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksStartAsyncReplicationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_start_async_replication_builder()` + `compute_region_disks_start_async_replication_execute()`.
/// For task-level control, use `compute_region_disks_start_async_replication_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_start_async_replication(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksStartAsyncReplicationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_start_async_replication_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.requestId,
    )?;
    compute_region_disks_start_async_replication_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_stop_async_replication_execute()` to send, or `compute_region_disks_stop_async_replication` for simplest API.

pub fn compute_region_disks_stop_async_replication_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/stopAsyncReplication",
        project,
        region,
        disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_stop_async_replication_execute()` or `compute_region_disks_stop_async_replication`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_stop_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_stop_async_replication_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_stop_async_replication_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_stop_async_replication_task()`.
/// For the simplest API, use `compute_region_disks_stop_async_replication()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_stop_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_stop_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_stop_async_replication_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_stop_async_replication`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksStopAsyncReplicationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_stop_async_replication_builder()` + `compute_region_disks_stop_async_replication_execute()`.
/// For task-level control, use `compute_region_disks_stop_async_replication_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_stop_async_replication(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksStopAsyncReplicationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_stop_async_replication_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.requestId,
    )?;
    compute_region_disks_stop_async_replication_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_stop_group_async_replication_execute()` to send, or `compute_region_disks_stop_group_async_replication` for simplest API.

pub fn compute_region_disks_stop_group_async_replication_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/stopGroupAsyncReplication",
        project,
        region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_stop_group_async_replication_execute()` or `compute_region_disks_stop_group_async_replication`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_stop_group_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_stop_group_async_replication_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_stop_group_async_replication_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_stop_group_async_replication_task()`.
/// For the simplest API, use `compute_region_disks_stop_group_async_replication()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_stop_group_async_replication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_stop_group_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_stop_group_async_replication_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_stop_group_async_replication`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksStopGroupAsyncReplicationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_stop_group_async_replication_builder()` + `compute_region_disks_stop_group_async_replication_execute()`.
/// For task-level control, use `compute_region_disks_stop_group_async_replication_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_stop_group_async_replication(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksStopGroupAsyncReplicationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_stop_group_async_replication_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_disks_stop_group_async_replication_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_test_iam_permissions_execute()` to send, or `compute_region_disks_test_iam_permissions` for simplest API.

pub fn compute_region_disks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_test_iam_permissions_execute()` or `compute_region_disks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_disks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_disks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_test_iam_permissions_builder()` + `compute_region_disks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_disks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_disks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_disks_test_iam_permissions_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/disks/{disk}
/// Update the specified disk with the data included in the request. Update is performed only on selected fields included as part of update-mask.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_update_execute()` to send, or `compute_region_disks_update` for simplest API.

pub fn compute_region_disks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    paths: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}",
        project, region, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = paths.as_ref() {
        query_parts.push(format!("paths={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/disks/{disk}
/// Update the specified disk with the data included in the request. Update is performed only on selected fields included as part of update-mask.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_update_execute()` or `compute_region_disks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/disks/{disk}
/// Update the specified disk with the data included in the request. Update is performed only on selected fields included as part of update-mask.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_update_task()`.
/// For the simplest API, use `compute_region_disks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: paths
    pub paths: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/disks/{disk}
/// Update the specified disk with the data included in the request. Update is performed only on selected fields included as part of update-mask.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_update_builder()` + `compute_region_disks_update_execute()`.
/// For task-level control, use `compute_region_disks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_update(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_update_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.paths,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_region_disks_update_execute(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified persistent disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_update_kms_key_execute()` to send, or `compute_region_disks_update_kms_key` for simplest API.

pub fn compute_region_disks_update_kms_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    disk: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/updateKmsKey",
        project, region, disk,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/disks/{disk}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_disks_update_kms_key_execute()` or `compute_region_disks_update_kms_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_update_kms_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_update_kms_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/disks/{disk}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_disks_update_kms_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_disks_update_kms_key_task()`.
/// For the simplest API, use `compute_region_disks_update_kms_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_disks_update_kms_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_disks_update_kms_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_disks_update_kms_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_disks_update_kms_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionDisksUpdateKmsKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: disk
    pub disk: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/disks/{disk}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified persistent disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_update_kms_key_builder()` + `compute_region_disks_update_kms_key_execute()`.
/// For task-level control, use `compute_region_disks_update_kms_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_update_kms_key(
    client: &SimpleHttpClient,
    args: &ComputeRegionDisksUpdateKmsKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_update_kms_key_builder(
        client,
        &args.project,
        &args.region,
        &args.disk,
        &args.requestId,
    )?;
    compute_region_disks_update_kms_key_execute(builder)
}

/// GET projects/{project}/aggregated/healthAggregationPolicies
/// Retrieves the list of all HealthAggregationPolicy resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_aggregated_list_execute()` to send, or `compute_region_health_aggregation_policies_aggregated_list` for simplest API.

pub fn compute_region_health_aggregation_policies_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/healthAggregationPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/healthAggregationPolicies
/// Retrieves the list of all HealthAggregationPolicy resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_aggregation_policies_aggregated_list_execute()` or `compute_region_health_aggregation_policies_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthAggregationPolicyAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/healthAggregationPolicies
/// Retrieves the list of all HealthAggregationPolicy resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_aggregation_policies_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_aggregation_policies_aggregated_list_task()`.
/// For the simplest API, use `compute_region_health_aggregation_policies_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_aggregation_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthAggregationPolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_aggregation_policies_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_aggregation_policies_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/healthAggregationPolicies
/// Retrieves the list of all HealthAggregationPolicy resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_aggregated_list_builder()` + `compute_region_health_aggregation_policies_aggregated_list_execute()`.
/// For task-level control, use `compute_region_health_aggregation_policies_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthAggregationPoliciesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthAggregationPolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_region_health_aggregation_policies_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Deletes the specified HealthAggregationPolicy in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_delete_execute()` to send, or `compute_region_health_aggregation_policies_delete` for simplest API.

pub fn compute_region_health_aggregation_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthAggregationPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies/{}",
        project,
        region,
        healthAggregationPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Deletes the specified HealthAggregationPolicy in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_aggregation_policies_delete_execute()` or `compute_region_health_aggregation_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Deletes the specified HealthAggregationPolicy in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_aggregation_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_aggregation_policies_delete_task()`.
/// For the simplest API, use `compute_region_health_aggregation_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_aggregation_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_aggregation_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_aggregation_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthAggregationPolicy
    pub healthAggregationPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Deletes the specified HealthAggregationPolicy in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_delete_builder()` + `compute_region_health_aggregation_policies_delete_execute()`.
/// For task-level control, use `compute_region_health_aggregation_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthAggregationPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.healthAggregationPolicy,
        &args.requestId,
    )?;
    compute_region_health_aggregation_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Returns the specified HealthAggregationPolicy resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_get_execute()` to send, or `compute_region_health_aggregation_policies_get` for simplest API.

pub fn compute_region_health_aggregation_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthAggregationPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies/{}",
        project,
        region,
        healthAggregationPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Returns the specified HealthAggregationPolicy resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_aggregation_policies_get_execute()` or `compute_region_health_aggregation_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthAggregationPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Returns the specified HealthAggregationPolicy resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_aggregation_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_aggregation_policies_get_task()`.
/// For the simplest API, use `compute_region_health_aggregation_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_aggregation_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthAggregationPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_aggregation_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_aggregation_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthAggregationPolicy
    pub healthAggregationPolicy: String,
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Returns the specified HealthAggregationPolicy resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_get_builder()` + `compute_region_health_aggregation_policies_get_execute()`.
/// For task-level control, use `compute_region_health_aggregation_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthAggregationPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthAggregationPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_get_builder(
        client,
        &args.project,
        &args.region,
        &args.healthAggregationPolicy,
    )?;
    compute_region_health_aggregation_policies_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/healthAggregationPolicies
/// Create a HealthAggregationPolicy in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_insert_execute()` to send, or `compute_region_health_aggregation_policies_insert` for simplest API.

pub fn compute_region_health_aggregation_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies",
        project,
        region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/healthAggregationPolicies
/// Create a HealthAggregationPolicy in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_aggregation_policies_insert_execute()` or `compute_region_health_aggregation_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/healthAggregationPolicies
/// Create a HealthAggregationPolicy in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_aggregation_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_aggregation_policies_insert_task()`.
/// For the simplest API, use `compute_region_health_aggregation_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_aggregation_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_aggregation_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_aggregation_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/healthAggregationPolicies
/// Create a HealthAggregationPolicy in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_insert_builder()` + `compute_region_health_aggregation_policies_insert_execute()`.
/// For task-level control, use `compute_region_health_aggregation_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthAggregationPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_health_aggregation_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Lists the HealthAggregationPolicies for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_list_execute()` to send, or `compute_region_health_aggregation_policies_list` for simplest API.

pub fn compute_region_health_aggregation_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies",
        project,
        region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Lists the HealthAggregationPolicies for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_aggregation_policies_list_execute()` or `compute_region_health_aggregation_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthAggregationPolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Lists the HealthAggregationPolicies for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_aggregation_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_aggregation_policies_list_task()`.
/// For the simplest API, use `compute_region_health_aggregation_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_aggregation_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthAggregationPolicyList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_aggregation_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_aggregation_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Lists the HealthAggregationPolicies for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_list_builder()` + `compute_region_health_aggregation_policies_list_execute()`.
/// For task-level control, use `compute_region_health_aggregation_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthAggregationPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthAggregationPolicyList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_health_aggregation_policies_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Updates the specified regional HealthAggregationPolicy resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_patch_execute()` to send, or `compute_region_health_aggregation_policies_patch` for simplest API.

pub fn compute_region_health_aggregation_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthAggregationPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies/{}",
        project,
        region,
        healthAggregationPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Updates the specified regional HealthAggregationPolicy resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_aggregation_policies_patch_execute()` or `compute_region_health_aggregation_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Updates the specified regional HealthAggregationPolicy resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_aggregation_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_aggregation_policies_patch_task()`.
/// For the simplest API, use `compute_region_health_aggregation_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_aggregation_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_aggregation_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_aggregation_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthAggregationPolicy
    pub healthAggregationPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Updates the specified regional HealthAggregationPolicy resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_patch_builder()` + `compute_region_health_aggregation_policies_patch_execute()`.
/// For task-level control, use `compute_region_health_aggregation_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthAggregationPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.healthAggregationPolicy,
        &args.requestId,
    )?;
    compute_region_health_aggregation_policies_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_test_iam_permissions_execute()` to send, or `compute_region_health_aggregation_policies_test_iam_permissions` for simplest API.

pub fn compute_region_health_aggregation_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_aggregation_policies_test_iam_permissions_execute()` or `compute_region_health_aggregation_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_aggregation_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_aggregation_policies_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_health_aggregation_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_aggregation_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_aggregation_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_aggregation_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_aggregation_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_test_iam_permissions_builder()` + `compute_region_health_aggregation_policies_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_health_aggregation_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_health_aggregation_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/healthCheckServices
/// Retrieves the list of all HealthCheckService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_aggregated_list_execute()` to send, or `compute_region_health_check_services_aggregated_list` for simplest API.

pub fn compute_region_health_check_services_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/healthCheckServices",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/healthCheckServices
/// Retrieves the list of all HealthCheckService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_check_services_aggregated_list_execute()` or `compute_region_health_check_services_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckServiceAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckServiceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/healthCheckServices
/// Retrieves the list of all HealthCheckService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_check_services_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_check_services_aggregated_list_task()`.
/// For the simplest API, use `compute_region_health_check_services_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_check_services_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthCheckServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_check_services_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_check_services_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/healthCheckServices
/// Retrieves the list of all HealthCheckService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_aggregated_list_builder()` + `compute_region_health_check_services_aggregated_list_execute()`.
/// For task-level control, use `compute_region_health_check_services_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthCheckServicesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthCheckServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_region_health_check_services_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Deletes the specified regional HealthCheckService.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_delete_execute()` to send, or `compute_region_health_check_services_delete` for simplest API.

pub fn compute_region_health_check_services_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthCheckService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices/{}",
        project, region, healthCheckService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Deletes the specified regional HealthCheckService.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_check_services_delete_execute()` or `compute_region_health_check_services_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Deletes the specified regional HealthCheckService.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_check_services_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_check_services_delete_task()`.
/// For the simplest API, use `compute_region_health_check_services_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_check_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_check_services_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_check_services_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthCheckService
    pub healthCheckService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Deletes the specified regional HealthCheckService.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_delete_builder()` + `compute_region_health_check_services_delete_execute()`.
/// For task-level control, use `compute_region_health_check_services_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthCheckServicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.healthCheckService,
        &args.requestId,
    )?;
    compute_region_health_check_services_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Returns the specified regional HealthCheckService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_get_execute()` to send, or `compute_region_health_check_services_get` for simplest API.

pub fn compute_region_health_check_services_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthCheckService: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices/{}",
        project, region, healthCheckService,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Returns the specified regional HealthCheckService resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_check_services_get_execute()` or `compute_region_health_check_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckService>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Returns the specified regional HealthCheckService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_check_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_check_services_get_task()`.
/// For the simplest API, use `compute_region_health_check_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_check_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_check_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_check_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthCheckService
    pub healthCheckService: String,
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Returns the specified regional HealthCheckService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_get_builder()` + `compute_region_health_check_services_get_execute()`.
/// For task-level control, use `compute_region_health_check_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthCheckServicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_get_builder(
        client,
        &args.project,
        &args.region,
        &args.healthCheckService,
    )?;
    compute_region_health_check_services_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/healthCheckServices
/// Creates a regional HealthCheckService resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_insert_execute()` to send, or `compute_region_health_check_services_insert` for simplest API.

pub fn compute_region_health_check_services_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/healthCheckServices
/// Creates a regional HealthCheckService resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_check_services_insert_execute()` or `compute_region_health_check_services_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/healthCheckServices
/// Creates a regional HealthCheckService resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_check_services_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_check_services_insert_task()`.
/// For the simplest API, use `compute_region_health_check_services_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_check_services_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_check_services_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_check_services_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/healthCheckServices
/// Creates a regional HealthCheckService resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_insert_builder()` + `compute_region_health_check_services_insert_execute()`.
/// For task-level control, use `compute_region_health_check_services_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthCheckServicesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_health_check_services_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Lists all the HealthCheckService resources that have been configured for the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_list_execute()` to send, or `compute_region_health_check_services_list` for simplest API.

pub fn compute_region_health_check_services_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Lists all the HealthCheckService resources that have been configured for the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_check_services_list_execute()` or `compute_region_health_check_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckServicesList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckServicesList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Lists all the HealthCheckService resources that have been configured for the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_check_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_check_services_list_task()`.
/// For the simplest API, use `compute_region_health_check_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_check_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckServicesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_check_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_check_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Lists all the HealthCheckService resources that have been configured for the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_list_builder()` + `compute_region_health_check_services_list_execute()`.
/// For task-level control, use `compute_region_health_check_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthCheckServicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckServicesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_health_check_services_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Updates the specified regional HealthCheckService resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_patch_execute()` to send, or `compute_region_health_check_services_patch` for simplest API.

pub fn compute_region_health_check_services_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthCheckService: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices/{}",
        project, region, healthCheckService,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Updates the specified regional HealthCheckService resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_check_services_patch_execute()` or `compute_region_health_check_services_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Updates the specified regional HealthCheckService resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_check_services_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_check_services_patch_task()`.
/// For the simplest API, use `compute_region_health_check_services_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_check_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_check_services_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_check_services_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthCheckService
    pub healthCheckService: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Updates the specified regional HealthCheckService resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_patch_builder()` + `compute_region_health_check_services_patch_execute()`.
/// For task-level control, use `compute_region_health_check_services_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthCheckServicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.healthCheckService,
        &args.requestId,
    )?;
    compute_region_health_check_services_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_test_iam_permissions_execute()` to send, or `compute_region_health_check_services_test_iam_permissions` for simplest API.

pub fn compute_region_health_check_services_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_check_services_test_iam_permissions_execute()` or `compute_region_health_check_services_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_check_services_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_check_services_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_health_check_services_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_check_services_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_check_services_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_check_services_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_check_services_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_test_iam_permissions_builder()` + `compute_region_health_check_services_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_health_check_services_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthCheckServicesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_health_check_services_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_delete_execute()` to send, or `compute_region_health_checks_delete` for simplest API.

pub fn compute_region_health_checks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}",
        project, region, healthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_checks_delete_execute()` or `compute_region_health_checks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_checks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_checks_delete_task()`.
/// For the simplest API, use `compute_region_health_checks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_checks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_checks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthCheck
    pub healthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_delete_builder()` + `compute_region_health_checks_delete_execute()`.
/// For task-level control, use `compute_region_health_checks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthChecksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.healthCheck,
        &args.requestId,
    )?;
    compute_region_health_checks_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_get_execute()` to send, or `compute_region_health_checks_get` for simplest API.

pub fn compute_region_health_checks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthCheck: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}",
        project, region, healthCheck,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_checks_get_execute()` or `compute_region_health_checks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheck>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_checks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_checks_get_task()`.
/// For the simplest API, use `compute_region_health_checks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheck>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_checks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_checks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthCheck
    pub healthCheck: String,
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_get_builder()` + `compute_region_health_checks_get_execute()`.
/// For task-level control, use `compute_region_health_checks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthChecksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheck>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_get_builder(
        client,
        &args.project,
        &args.region,
        &args.healthCheck,
    )?;
    compute_region_health_checks_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_insert_execute()` to send, or `compute_region_health_checks_insert` for simplest API.

pub fn compute_region_health_checks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_checks_insert_execute()` or `compute_region_health_checks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_checks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_checks_insert_task()`.
/// For the simplest API, use `compute_region_health_checks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_checks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_checks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_insert_builder()` + `compute_region_health_checks_insert_execute()`.
/// For task-level control, use `compute_region_health_checks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthChecksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_health_checks_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_list_execute()` to send, or `compute_region_health_checks_list` for simplest API.

pub fn compute_region_health_checks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_checks_list_execute()` or `compute_region_health_checks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_checks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_checks_list_task()`.
/// For the simplest API, use `compute_region_health_checks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_checks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_checks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_list_builder()` + `compute_region_health_checks_list_execute()`.
/// For task-level control, use `compute_region_health_checks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthChecksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_health_checks_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_patch_execute()` to send, or `compute_region_health_checks_patch` for simplest API.

pub fn compute_region_health_checks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}",
        project, region, healthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_checks_patch_execute()` or `compute_region_health_checks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_checks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_checks_patch_task()`.
/// For the simplest API, use `compute_region_health_checks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_checks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_checks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthCheck
    pub healthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_patch_builder()` + `compute_region_health_checks_patch_execute()`.
/// For task-level control, use `compute_region_health_checks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthChecksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.healthCheck,
        &args.requestId,
    )?;
    compute_region_health_checks_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_test_iam_permissions_execute()` to send, or `compute_region_health_checks_test_iam_permissions` for simplest API.

pub fn compute_region_health_checks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_checks_test_iam_permissions_execute()` or `compute_region_health_checks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_checks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_checks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_health_checks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_checks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_checks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_test_iam_permissions_builder()` + `compute_region_health_checks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_health_checks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthChecksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_health_checks_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_update_execute()` to send, or `compute_region_health_checks_update` for simplest API.

pub fn compute_region_health_checks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthCheck: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}",
        project, region, healthCheck,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_checks_update_execute()` or `compute_region_health_checks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_checks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_checks_update_task()`.
/// For the simplest API, use `compute_region_health_checks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_checks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_checks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_checks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_checks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthCheck
    pub healthCheck: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_update_builder()` + `compute_region_health_checks_update_execute()`.
/// For task-level control, use `compute_region_health_checks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_update(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthChecksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_update_builder(
        client,
        &args.project,
        &args.region,
        &args.healthCheck,
        &args.requestId,
    )?;
    compute_region_health_checks_update_execute(builder)
}

/// GET projects/{project}/aggregated/healthSources
/// Retrieves the list of all HealthSource resources (all regional) available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_aggregated_list_execute()` to send, or `compute_region_health_sources_aggregated_list` for simplest API.

pub fn compute_region_health_sources_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/healthSources",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/healthSources
/// Retrieves the list of all HealthSource resources (all regional) available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_sources_aggregated_list_execute()` or `compute_region_health_sources_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthSourceAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthSourceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/healthSources
/// Retrieves the list of all HealthSource resources (all regional) available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_sources_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_sources_aggregated_list_task()`.
/// For the simplest API, use `compute_region_health_sources_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_sources_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthSourceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_sources_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_sources_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/healthSources
/// Retrieves the list of all HealthSource resources (all regional) available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_aggregated_list_builder()` + `compute_region_health_sources_aggregated_list_execute()`.
/// For task-level control, use `compute_region_health_sources_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthSourcesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthSourceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_region_health_sources_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/healthSources/{healthSource}
/// Deletes the specified HealthSource in the given region
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_delete_execute()` to send, or `compute_region_health_sources_delete` for simplest API.

pub fn compute_region_health_sources_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthSource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}",
        project, region, healthSource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/healthSources/{healthSource}
/// Deletes the specified HealthSource in the given region
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_sources_delete_execute()` or `compute_region_health_sources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/healthSources/{healthSource}
/// Deletes the specified HealthSource in the given region
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_sources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_sources_delete_task()`.
/// For the simplest API, use `compute_region_health_sources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_sources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_sources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_sources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthSource
    pub healthSource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/healthSources/{healthSource}
/// Deletes the specified HealthSource in the given region
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_delete_builder()` + `compute_region_health_sources_delete_execute()`.
/// For task-level control, use `compute_region_health_sources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthSourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.healthSource,
        &args.requestId,
    )?;
    compute_region_health_sources_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Returns the specified HealthSource resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_get_execute()` to send, or `compute_region_health_sources_get` for simplest API.

pub fn compute_region_health_sources_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthSource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}",
        project, region, healthSource,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Returns the specified HealthSource resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_sources_get_execute()` or `compute_region_health_sources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthSource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Returns the specified HealthSource resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_sources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_sources_get_task()`.
/// For the simplest API, use `compute_region_health_sources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_sources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_sources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthSource
    pub healthSource: String,
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Returns the specified HealthSource resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_get_builder()` + `compute_region_health_sources_get_execute()`.
/// For task-level control, use `compute_region_health_sources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthSourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_get_builder(
        client,
        &args.project,
        &args.region,
        &args.healthSource,
    )?;
    compute_region_health_sources_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}/getHealth
/// Gets the most recent health check results for this regional HealthSource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_get_health_execute()` to send, or `compute_region_health_sources_get_health` for simplest API.

pub fn compute_region_health_sources_get_health_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthSource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}/getHealth",
        project,
        region,
        healthSource,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}/getHealth
/// Gets the most recent health check results for this regional HealthSource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_sources_get_health_execute()` or `compute_region_health_sources_get_health`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_get_health_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthSourceHealth>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthSourceHealth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}/getHealth
/// Gets the most recent health check results for this regional HealthSource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_sources_get_health_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_sources_get_health_task()`.
/// For the simplest API, use `compute_region_health_sources_get_health()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_sources_get_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSourceHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_sources_get_health_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_sources_get_health`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesGetHealthArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthSource
    pub healthSource: String,
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}/getHealth
/// Gets the most recent health check results for this regional HealthSource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_get_health_builder()` + `compute_region_health_sources_get_health_execute()`.
/// For task-level control, use `compute_region_health_sources_get_health_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_get_health(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthSourcesGetHealthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSourceHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_get_health_builder(
        client,
        &args.project,
        &args.region,
        &args.healthSource,
    )?;
    compute_region_health_sources_get_health_execute(builder)
}

/// POST projects/{project}/regions/{region}/healthSources
/// Create a HealthSource in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_insert_execute()` to send, or `compute_region_health_sources_insert` for simplest API.

pub fn compute_region_health_sources_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/healthSources
/// Create a HealthSource in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_sources_insert_execute()` or `compute_region_health_sources_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/healthSources
/// Create a HealthSource in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_sources_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_sources_insert_task()`.
/// For the simplest API, use `compute_region_health_sources_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_sources_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_sources_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_sources_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/healthSources
/// Create a HealthSource in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_insert_builder()` + `compute_region_health_sources_insert_execute()`.
/// For task-level control, use `compute_region_health_sources_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthSourcesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_health_sources_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources
/// Lists the HealthSources for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_list_execute()` to send, or `compute_region_health_sources_list` for simplest API.

pub fn compute_region_health_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/healthSources
/// Lists the HealthSources for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_sources_list_execute()` or `compute_region_health_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthSourceList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthSourceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/healthSources
/// Lists the HealthSources for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_sources_list_task()`.
/// For the simplest API, use `compute_region_health_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSourceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/healthSources
/// Lists the HealthSources for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_list_builder()` + `compute_region_health_sources_list_execute()`.
/// For task-level control, use `compute_region_health_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSourceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_health_sources_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/healthSources/{healthSource}
/// Updates the specified regional HealthSource resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_patch_execute()` to send, or `compute_region_health_sources_patch` for simplest API.

pub fn compute_region_health_sources_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    healthSource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}",
        project, region, healthSource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/healthSources/{healthSource}
/// Updates the specified regional HealthSource resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_sources_patch_execute()` or `compute_region_health_sources_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/healthSources/{healthSource}
/// Updates the specified regional HealthSource resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_sources_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_sources_patch_task()`.
/// For the simplest API, use `compute_region_health_sources_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_sources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_health_sources_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_sources_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: healthSource
    pub healthSource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/healthSources/{healthSource}
/// Updates the specified regional HealthSource resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_patch_builder()` + `compute_region_health_sources_patch_execute()`.
/// For task-level control, use `compute_region_health_sources_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthSourcesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.healthSource,
        &args.requestId,
    )?;
    compute_region_health_sources_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_test_iam_permissions_execute()` to send, or `compute_region_health_sources_test_iam_permissions` for simplest API.

pub fn compute_region_health_sources_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_health_sources_test_iam_permissions_execute()` or `compute_region_health_sources_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_health_sources_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_health_sources_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_health_sources_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_health_sources_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_health_sources_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_health_sources_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_health_sources_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_test_iam_permissions_builder()` + `compute_region_health_sources_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_health_sources_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionHealthSourcesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_health_sources_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request. Cancelled resize request no longer waits for the resources to be provisioned. Cancel is only possible for requests that are in accepted state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_manager_resize_requests_cancel_execute()` to send, or `compute_region_instance_group_manager_resize_requests_cancel` for simplest API.

pub fn compute_region_instance_group_manager_resize_requests_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    resizeRequest: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests/{}/cancel",
        project,
        region,
        instanceGroupManager,
        resizeRequest,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request. Cancelled resize request no longer waits for the resources to be provisioned. Cancel is only possible for requests that are in accepted state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_manager_resize_requests_cancel_execute()` or `compute_region_instance_group_manager_resize_requests_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request. Cancelled resize request no longer waits for the resources to be provisioned. Cancel is only possible for requests that are in accepted state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_manager_resize_requests_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_cancel_task()`.
/// For the simplest API, use `compute_region_instance_group_manager_resize_requests_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_manager_resize_requests_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_manager_resize_requests_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_manager_resize_requests_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Path parameter: resizeRequest
    pub resizeRequest: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request. Cancelled resize request no longer waits for the resources to be provisioned. Cancel is only possible for requests that are in accepted state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_manager_resize_requests_cancel_builder()` + `compute_region_instance_group_manager_resize_requests_cancel_execute()`.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_cancel(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_manager_resize_requests_cancel_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.resizeRequest,
        &args.requestId,
    )?;
    compute_region_instance_group_manager_resize_requests_cancel_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_manager_resize_requests_delete_execute()` to send, or `compute_region_instance_group_manager_resize_requests_delete` for simplest API.

pub fn compute_region_instance_group_manager_resize_requests_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    resizeRequest: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests/{}",
        project,
        region,
        instanceGroupManager,
        resizeRequest,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_manager_resize_requests_delete_execute()` or `compute_region_instance_group_manager_resize_requests_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_manager_resize_requests_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_delete_task()`.
/// For the simplest API, use `compute_region_instance_group_manager_resize_requests_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_manager_resize_requests_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_manager_resize_requests_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_manager_resize_requests_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Path parameter: resizeRequest
    pub resizeRequest: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_manager_resize_requests_delete_builder()` + `compute_region_instance_group_manager_resize_requests_delete_execute()`.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_manager_resize_requests_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.resizeRequest,
        &args.requestId,
    )?;
    compute_region_instance_group_manager_resize_requests_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_manager_resize_requests_get_execute()` to send, or `compute_region_instance_group_manager_resize_requests_get` for simplest API.

pub fn compute_region_instance_group_manager_resize_requests_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    resizeRequest: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests/{}",
        project,
        region,
        instanceGroupManager,
        resizeRequest,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_manager_resize_requests_get_execute()` or `compute_region_instance_group_manager_resize_requests_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagerResizeRequest>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerResizeRequest = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_manager_resize_requests_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_get_task()`.
/// For the simplest API, use `compute_region_instance_group_manager_resize_requests_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_manager_resize_requests_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequest>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_manager_resize_requests_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_manager_resize_requests_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Path parameter: resizeRequest
    pub resizeRequest: String,
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_manager_resize_requests_get_builder()` + `compute_region_instance_group_manager_resize_requests_get_execute()`.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequest>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_manager_resize_requests_get_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.resizeRequest,
    )?;
    compute_region_instance_group_manager_resize_requests_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new Resize Request that starts provisioning VMs immediately or queues VM creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_manager_resize_requests_insert_execute()` to send, or `compute_region_instance_group_manager_resize_requests_insert` for simplest API.

pub fn compute_region_instance_group_manager_resize_requests_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new Resize Request that starts provisioning VMs immediately or queues VM creation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_manager_resize_requests_insert_execute()` or `compute_region_instance_group_manager_resize_requests_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new Resize Request that starts provisioning VMs immediately or queues VM creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_manager_resize_requests_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_insert_task()`.
/// For the simplest API, use `compute_region_instance_group_manager_resize_requests_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_manager_resize_requests_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_manager_resize_requests_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_manager_resize_requests_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new Resize Request that starts provisioning VMs immediately or queues VM creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_manager_resize_requests_insert_builder()` + `compute_region_instance_group_manager_resize_requests_insert_execute()`.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_manager_resize_requests_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_manager_resize_requests_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of Resize Requests that are contained in the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_manager_resize_requests_list_execute()` to send, or `compute_region_instance_group_manager_resize_requests_list` for simplest API.

pub fn compute_region_instance_group_manager_resize_requests_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of Resize Requests that are contained in the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_manager_resize_requests_list_execute()` or `compute_region_instance_group_manager_resize_requests_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionInstanceGroupManagerResizeRequestsListResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagerResizeRequestsListResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of Resize Requests that are contained in the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_manager_resize_requests_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_list_task()`.
/// For the simplest API, use `compute_region_instance_group_manager_resize_requests_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_manager_resize_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_manager_resize_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagerResizeRequestsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_manager_resize_requests_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_manager_resize_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of Resize Requests that are contained in the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_manager_resize_requests_list_builder()` + `compute_region_instance_group_manager_resize_requests_list_execute()`.
/// For task-level control, use `compute_region_instance_group_manager_resize_requests_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_manager_resize_requests_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagerResizeRequestsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_manager_resize_requests_list_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instance_group_manager_resize_requests_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be immediately removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_abandon_instances_execute()` to send, or `compute_region_instance_group_managers_abandon_instances` for simplest API.

pub fn compute_region_instance_group_managers_abandon_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/abandonInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be immediately removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_abandon_instances_execute()` or `compute_region_instance_group_managers_abandon_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_abandon_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_abandon_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be immediately removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_abandon_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_abandon_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_abandon_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_abandon_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_abandon_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_abandon_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_abandon_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersAbandonInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be immediately removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_abandon_instances_builder()` + `compute_region_instance_group_managers_abandon_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_abandon_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_abandon_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersAbandonInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_abandon_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_abandon_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Apply updates to selected instances the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_apply_updates_to_instances_execute()` to send, or `compute_region_instance_group_managers_apply_updates_to_instances` for simplest API.

pub fn compute_region_instance_group_managers_apply_updates_to_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/applyUpdatesToInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Apply updates to selected instances the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_apply_updates_to_instances_execute()` or `compute_region_instance_group_managers_apply_updates_to_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_apply_updates_to_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_apply_updates_to_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Apply updates to selected instances the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_apply_updates_to_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_apply_updates_to_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_apply_updates_to_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_apply_updates_to_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_apply_updates_to_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_apply_updates_to_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_apply_updates_to_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Apply updates to selected instances the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_apply_updates_to_instances_builder()` + `compute_region_instance_group_managers_apply_updates_to_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_apply_updates_to_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_apply_updates_to_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_apply_updates_to_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
    )?;
    compute_region_instance_group_managers_apply_updates_to_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this regional managed instance group. Instances are created using the current instance template. The create instances operation is marked DONE if the `createInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_create_instances_execute()` to send, or `compute_region_instance_group_managers_create_instances` for simplest API.

pub fn compute_region_instance_group_managers_create_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/createInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this regional managed instance group. Instances are created using the current instance template. The create instances operation is marked DONE if the `createInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_create_instances_execute()` or `compute_region_instance_group_managers_create_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_create_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_create_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this regional managed instance group. Instances are created using the current instance template. The create instances operation is marked DONE if the `createInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_create_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_create_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_create_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_create_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_create_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_create_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_create_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersCreateInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this regional managed instance group. Instances are created using the current instance template. The create instances operation is marked DONE if the `createInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_create_instances_builder()` + `compute_region_instance_group_managers_create_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_create_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_create_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersCreateInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_create_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_create_instances_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_delete_execute()` to send, or `compute_region_instance_group_managers_delete` for simplest API.

pub fn compute_region_instance_group_managers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}",
        project, region, instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_delete_execute()` or `compute_region_instance_group_managers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_delete_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_delete_builder()` + `compute_region_instance_group_managers_delete_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_delete_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group to be immediately deleted. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. The `deleteInstances` operation is marked DONE if the `deleteInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thedeleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_delete_instances_execute()` to send, or `compute_region_instance_group_managers_delete_instances` for simplest API.

pub fn compute_region_instance_group_managers_delete_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/deleteInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group to be immediately deleted. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. The `deleteInstances` operation is marked DONE if the `deleteInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thedeleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_delete_instances_execute()` or `compute_region_instance_group_managers_delete_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_delete_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group to be immediately deleted. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. The `deleteInstances` operation is marked DONE if the `deleteInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thedeleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_delete_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_delete_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_delete_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_delete_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_delete_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_delete_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_delete_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersDeleteInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group to be immediately deleted. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. The `deleteInstances` operation is marked DONE if the `deleteInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thedeleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_delete_instances_builder()` + `compute_region_instance_group_managers_delete_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_delete_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersDeleteInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_delete_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_delete_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_delete_per_instance_configs_execute()` to send, or `compute_region_instance_group_managers_delete_per_instance_configs` for simplest API.

pub fn compute_region_instance_group_managers_delete_per_instance_configs_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/deletePerInstanceConfigs",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_delete_per_instance_configs_execute()` or `compute_region_instance_group_managers_delete_per_instance_configs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_delete_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_per_instance_configs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_delete_per_instance_configs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_delete_per_instance_configs_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_delete_per_instance_configs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_delete_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_delete_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_delete_per_instance_configs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_delete_per_instance_configs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_delete_per_instance_configs_builder()` + `compute_region_instance_group_managers_delete_per_instance_configs_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_delete_per_instance_configs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_per_instance_configs(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_delete_per_instance_configs_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
    )?;
    compute_region_instance_group_managers_delete_per_instance_configs_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_get_execute()` to send, or `compute_region_instance_group_managers_get` for simplest API.

pub fn compute_region_instance_group_managers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}",
        project, region, instanceGroupManager,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_get_execute()` or `compute_region_instance_group_managers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManager>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManager = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_get_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManager>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_get_builder()` + `compute_region_instance_group_managers_get_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManager>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_get_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
    )?;
    compute_region_instance_group_managers_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A regional managed instance group can contain up to 2000 instances.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_insert_execute()` to send, or `compute_region_instance_group_managers_insert` for simplest API.

pub fn compute_region_instance_group_managers_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A regional managed instance group can contain up to 2000 instances.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_insert_execute()` or `compute_region_instance_group_managers_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A regional managed instance group can contain up to 2000 instances.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_insert_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A regional managed instance group can contain up to 2000 instances.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_insert_builder()` + `compute_region_instance_group_managers_insert_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Retrieves the list of managed instance groups that are contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_list_execute()` to send, or `compute_region_instance_group_managers_list` for simplest API.

pub fn compute_region_instance_group_managers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Retrieves the list of managed instance groups that are contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_list_execute()` or `compute_region_instance_group_managers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionInstanceGroupManagerList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Retrieves the list of managed instance groups that are contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_list_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagerList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Retrieves the list of managed instance groups that are contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_list_builder()` + `compute_region_instance_group_managers_list_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagerList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instance_group_managers_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given regional managed instance group. The filter `andorderBy` query parameters are not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_list_errors_execute()` to send, or `compute_region_instance_group_managers_list_errors` for simplest API.

pub fn compute_region_instance_group_managers_list_errors_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/listErrors",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given regional managed instance group. The filter `andorderBy` query parameters are not supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_list_errors_execute()` or `compute_region_instance_group_managers_list_errors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_list_errors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_errors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionInstanceGroupManagersListErrorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListErrorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given regional managed instance group. The filter `andorderBy` query parameters are not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_list_errors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_list_errors_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_list_errors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_list_errors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_list_errors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_list_errors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_list_errors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersListErrorsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given regional managed instance group. The filter `andorderBy` query parameters are not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_list_errors_builder()` + `compute_region_instance_group_managers_list_errors_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_list_errors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_errors(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersListErrorsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_list_errors_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instance_group_managers_list_errors_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists the instances in the managed instance group and instances that are scheduled to be created. The list includes any current actions that the group has scheduled for its instances. The `orderBy` query parameter is not supported.   The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_list_managed_instances_execute()` to send, or `compute_region_instance_group_managers_list_managed_instances` for simplest API.

pub fn compute_region_instance_group_managers_list_managed_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/listManagedInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists the instances in the managed instance group and instances that are scheduled to be created. The list includes any current actions that the group has scheduled for its instances. The `orderBy` query parameter is not supported.   The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_list_managed_instances_execute()` or `compute_region_instance_group_managers_list_managed_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_list_managed_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_managed_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionInstanceGroupManagersListInstancesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists the instances in the managed instance group and instances that are scheduled to be created. The list includes any current actions that the group has scheduled for its instances. The `orderBy` query parameter is not supported.   The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_list_managed_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_list_managed_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_list_managed_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_list_managed_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_list_managed_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_list_managed_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_list_managed_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersListManagedInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists the instances in the managed instance group and instances that are scheduled to be created. The list includes any current actions that the group has scheduled for its instances. The `orderBy` query parameter is not supported.   The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_list_managed_instances_builder()` + `compute_region_instance_group_managers_list_managed_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_list_managed_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_managed_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersListManagedInstancesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_list_managed_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instance_group_managers_list_managed_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_list_per_instance_configs_execute()` to send, or `compute_region_instance_group_managers_list_per_instance_configs` for simplest API.

pub fn compute_region_instance_group_managers_list_per_instance_configs_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/listPerInstanceConfigs",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_list_per_instance_configs_execute()` or `compute_region_instance_group_managers_list_per_instance_configs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_list_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_per_instance_configs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionInstanceGroupManagersListInstanceConfigsResp>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListInstanceConfigsResp =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_list_per_instance_configs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_list_per_instance_configs_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_list_per_instance_configs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_list_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_list_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListInstanceConfigsResp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_list_per_instance_configs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_list_per_instance_configs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_list_per_instance_configs_builder()` + `compute_region_instance_group_managers_list_per_instance_configs_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_list_per_instance_configs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_per_instance_configs(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListInstanceConfigsResp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_list_per_instance_configs_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instance_group_managers_list_per_instance_configs_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with the listmanagedinstances method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_patch_execute()` to send, or `compute_region_instance_group_managers_patch` for simplest API.

pub fn compute_region_instance_group_managers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}",
        project, region, instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with the listmanagedinstances method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_patch_execute()` or `compute_region_instance_group_managers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with the listmanagedinstances method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_patch_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with the listmanagedinstances method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_patch_builder()` + `compute_region_instance_group_managers_patch_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_patch_per_instance_configs_execute()` to send, or `compute_region_instance_group_managers_patch_per_instance_configs` for simplest API.

pub fn compute_region_instance_group_managers_patch_per_instance_configs_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/patchPerInstanceConfigs",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_patch_per_instance_configs_execute()` or `compute_region_instance_group_managers_patch_per_instance_configs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_patch_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_patch_per_instance_configs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_patch_per_instance_configs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_patch_per_instance_configs_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_patch_per_instance_configs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_patch_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_patch_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_patch_per_instance_configs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_patch_per_instance_configs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_patch_per_instance_configs_builder()` + `compute_region_instance_group_managers_patch_per_instance_configs_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_patch_per_instance_configs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_patch_per_instance_configs(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_patch_per_instance_configs_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_patch_per_instance_configs_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_recreate_instances_execute()` to send, or `compute_region_instance_group_managers_recreate_instances` for simplest API.

pub fn compute_region_instance_group_managers_recreate_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/recreateInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_recreate_instances_execute()` or `compute_region_instance_group_managers_recreate_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_recreate_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_recreate_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_recreate_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_recreate_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_recreate_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_recreate_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_recreate_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_recreate_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_recreate_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersRecreateInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_recreate_instances_builder()` + `compute_region_instance_group_managers_recreate_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_recreate_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_recreate_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersRecreateInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_recreate_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_recreate_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize
/// Changes the intended size of the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes one or more instances.  The resize operation is marked DONE if theresize request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or deleting actions with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_resize_execute()` to send, or `compute_region_instance_group_managers_resize` for simplest API.

pub fn compute_region_instance_group_managers_resize_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
    size: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resize",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = size.as_ref() {
        query_parts.push(format!("size={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize
/// Changes the intended size of the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes one or more instances.  The resize operation is marked DONE if theresize request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or deleting actions with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_resize_execute()` or `compute_region_instance_group_managers_resize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_resize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize
/// Changes the intended size of the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes one or more instances.  The resize operation is marked DONE if theresize request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or deleting actions with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_resize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_resize_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_resize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_resize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_resize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersResizeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: size
    pub size: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize
/// Changes the intended size of the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes one or more instances.  The resize operation is marked DONE if theresize request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or deleting actions with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_resize_builder()` + `compute_region_instance_group_managers_resize_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_resize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_resize(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersResizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_resize_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
        &args.size,
    )?;
    compute_region_instance_group_managers_resize_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_resume_instances_execute()` to send, or `compute_region_instance_group_managers_resume_instances` for simplest API.

pub fn compute_region_instance_group_managers_resume_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resumeInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_resume_instances_execute()` or `compute_region_instance_group_managers_resume_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_resume_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_resume_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_resume_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_resume_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_resume_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_resume_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_resume_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_resume_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_resume_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersResumeInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_resume_instances_builder()` + `compute_region_instance_group_managers_resume_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_resume_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_resume_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersResumeInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_resume_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_resume_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Sets the instance template to use when creating new instances or recreating instances in this group. Existing instances are not affected.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_set_instance_template_execute()` to send, or `compute_region_instance_group_managers_set_instance_template` for simplest API.

pub fn compute_region_instance_group_managers_set_instance_template_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/setInstanceTemplate",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Sets the instance template to use when creating new instances or recreating instances in this group. Existing instances are not affected.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_set_instance_template_execute()` or `compute_region_instance_group_managers_set_instance_template`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_set_instance_template_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_set_instance_template_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Sets the instance template to use when creating new instances or recreating instances in this group. Existing instances are not affected.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_set_instance_template_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_set_instance_template_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_set_instance_template()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_set_instance_template_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_set_instance_template_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_set_instance_template_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_set_instance_template`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Sets the instance template to use when creating new instances or recreating instances in this group. Existing instances are not affected.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_set_instance_template_builder()` + `compute_region_instance_group_managers_set_instance_template_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_set_instance_template_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_set_instance_template(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_set_instance_template_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_set_instance_template_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all new instances in this group are assigned. Existing instances in the group are not affected.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_set_target_pools_execute()` to send, or `compute_region_instance_group_managers_set_target_pools` for simplest API.

pub fn compute_region_instance_group_managers_set_target_pools_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/setTargetPools",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all new instances in this group are assigned. Existing instances in the group are not affected.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_set_target_pools_execute()` or `compute_region_instance_group_managers_set_target_pools`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_set_target_pools_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_set_target_pools_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all new instances in this group are assigned. Existing instances in the group are not affected.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_set_target_pools_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_set_target_pools_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_set_target_pools()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_set_target_pools_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_set_target_pools_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_set_target_pools_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_set_target_pools`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersSetTargetPoolsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all new instances in this group are assigned. Existing instances in the group are not affected.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_set_target_pools_builder()` + `compute_region_instance_group_managers_set_target_pools_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_set_target_pools_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_set_target_pools(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersSetTargetPoolsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_set_target_pools_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_set_target_pools_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_start_instances_execute()` to send, or `compute_region_instance_group_managers_start_instances` for simplest API.

pub fn compute_region_instance_group_managers_start_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/startInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_start_instances_execute()` or `compute_region_instance_group_managers_start_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_start_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_start_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_start_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_start_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_start_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_start_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_start_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_start_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_start_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersStartInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_start_instances_builder()` + `compute_region_instance_group_managers_start_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_start_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_start_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersStartInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_start_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_start_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_stop_instances_execute()` to send, or `compute_region_instance_group_managers_stop_instances` for simplest API.

pub fn compute_region_instance_group_managers_stop_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/stopInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_stop_instances_execute()` or `compute_region_instance_group_managers_stop_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_stop_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_stop_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_stop_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_stop_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_stop_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_stop_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_stop_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_stop_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_stop_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersStopInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_stop_instances_builder()` + `compute_region_instance_group_managers_stop_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_stop_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_stop_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersStopInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_stop_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_stop_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_suspend_instances_execute()` to send, or `compute_region_instance_group_managers_suspend_instances` for simplest API.

pub fn compute_region_instance_group_managers_suspend_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/suspendInstances",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_suspend_instances_execute()` or `compute_region_instance_group_managers_suspend_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_suspend_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_suspend_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_suspend_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_suspend_instances_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_suspend_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_suspend_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_suspend_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_suspend_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_suspend_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersSuspendInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_suspend_instances_builder()` + `compute_region_instance_group_managers_suspend_instances_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_suspend_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_suspend_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersSuspendInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_suspend_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_suspend_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_update_per_instance_configs_execute()` to send, or `compute_region_instance_group_managers_update_per_instance_configs` for simplest API.

pub fn compute_region_instance_group_managers_update_per_instance_configs_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroupManager: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/updatePerInstanceConfigs",
        project,
        region,
        instanceGroupManager,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_group_managers_update_per_instance_configs_execute()` or `compute_region_instance_group_managers_update_per_instance_configs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_update_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_update_per_instance_configs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_group_managers_update_per_instance_configs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_group_managers_update_per_instance_configs_task()`.
/// For the simplest API, use `compute_region_instance_group_managers_update_per_instance_configs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_group_managers_update_per_instance_configs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_group_managers_update_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_group_managers_update_per_instance_configs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_group_managers_update_per_instance_configs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroupManager
    pub instanceGroupManager: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_update_per_instance_configs_builder()` + `compute_region_instance_group_managers_update_per_instance_configs_execute()`.
/// For task-level control, use `compute_region_instance_group_managers_update_per_instance_configs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_update_per_instance_configs(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_update_per_instance_configs_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroupManager,
        &args.requestId,
    )?;
    compute_region_instance_group_managers_update_per_instance_configs_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}
/// Returns the specified instance group resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_get_execute()` to send, or `compute_region_instance_groups_get` for simplest API.

pub fn compute_region_instance_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups/{}",
        project, region, instanceGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}
/// Returns the specified instance group resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_groups_get_execute()` or `compute_region_instance_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}
/// Returns the specified instance group resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_groups_get_task()`.
/// For the simplest API, use `compute_region_instance_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}
/// Returns the specified instance group resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_get_builder()` + `compute_region_instance_groups_get_execute()`.
/// For task-level control, use `compute_region_instance_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_get_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroup,
    )?;
    compute_region_instance_groups_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups
/// Retrieves the list of instance group resources contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_list_execute()` to send, or `compute_region_instance_groups_list` for simplest API.

pub fn compute_region_instance_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups
/// Retrieves the list of instance group resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_groups_list_execute()` or `compute_region_instance_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionInstanceGroupList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceGroups
/// Retrieves the list of instance group resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_groups_list_task()`.
/// For the simplest API, use `compute_region_instance_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionInstanceGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instanceGroups
/// Retrieves the list of instance group resources contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_list_builder()` + `compute_region_instance_groups_list_execute()`.
/// For task-level control, use `compute_region_instance_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionInstanceGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instance_groups_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group and displays information about the named ports. Depending on the specified options, this method can list all instances or only the instances that are running. The `orderBy` query parameter is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_list_instances_execute()` to send, or `compute_region_instance_groups_list_instances` for simplest API.

pub fn compute_region_instance_groups_list_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroup: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups/{}/listInstances",
        project,
        region,
        instanceGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group and displays information about the named ports. Depending on the specified options, this method can list all instances or only the instances that are running. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_groups_list_instances_execute()` or `compute_region_instance_groups_list_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_list_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_list_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionInstanceGroupsListInstances>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupsListInstances = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group and displays information about the named ports. Depending on the specified options, this method can list all instances or only the instances that are running. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_groups_list_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_groups_list_instances_task()`.
/// For the simplest API, use `compute_region_instance_groups_list_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_list_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_groups_list_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupsListInstances>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_groups_list_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_groups_list_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsListInstancesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group and displays information about the named ports. Depending on the specified options, this method can list all instances or only the instances that are running. The `orderBy` query parameter is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_list_instances_builder()` + `compute_region_instance_groups_list_instances_execute()`.
/// For task-level control, use `compute_region_instance_groups_list_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_list_instances(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupsListInstancesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupsListInstances>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_list_instances_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroup,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instance_groups_list_instances_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified regional instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_set_named_ports_execute()` to send, or `compute_region_instance_groups_set_named_ports` for simplest API.

pub fn compute_region_instance_groups_set_named_ports_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups/{}/setNamedPorts",
        project,
        region,
        instanceGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified regional instance group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_groups_set_named_ports_execute()` or `compute_region_instance_groups_set_named_ports`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_set_named_ports_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_set_named_ports_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified regional instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_groups_set_named_ports_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_groups_set_named_ports_task()`.
/// For the simplest API, use `compute_region_instance_groups_set_named_ports()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_set_named_ports_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_groups_set_named_ports_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_groups_set_named_ports_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_groups_set_named_ports`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsSetNamedPortsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceGroup
    pub instanceGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified regional instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_set_named_ports_builder()` + `compute_region_instance_groups_set_named_ports_execute()`.
/// For task-level control, use `compute_region_instance_groups_set_named_ports_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_set_named_ports(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupsSetNamedPortsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_set_named_ports_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceGroup,
        &args.requestId,
    )?;
    compute_region_instance_groups_set_named_ports_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_test_iam_permissions_execute()` to send, or `compute_region_instance_groups_test_iam_permissions` for simplest API.

pub fn compute_region_instance_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_groups_test_iam_permissions_execute()` or `compute_region_instance_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_groups_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_instance_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_test_iam_permissions_builder()` + `compute_region_instance_groups_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_instance_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_instance_groups_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_templates_delete_execute()` to send, or `compute_region_instance_templates_delete` for simplest API.

pub fn compute_region_instance_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceTemplate: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceTemplates/{}",
        project, region, instanceTemplate,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_templates_delete_execute()` or `compute_region_instance_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_templates_delete_task()`.
/// For the simplest API, use `compute_region_instance_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceTemplatesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceTemplate
    pub instanceTemplate: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_templates_delete_builder()` + `compute_region_instance_templates_delete_execute()`.
/// For task-level control, use `compute_region_instance_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_templates_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceTemplate,
        &args.requestId,
    )?;
    compute_region_instance_templates_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_templates_get_execute()` to send, or `compute_region_instance_templates_get` for simplest API.

pub fn compute_region_instance_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instanceTemplate: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceTemplates/{}",
        project, region, instanceTemplate,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_templates_get_execute()` or `compute_region_instance_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_templates_get_task()`.
/// For the simplest API, use `compute_region_instance_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceTemplatesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instanceTemplate
    pub instanceTemplate: String,
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_templates_get_builder()` + `compute_region_instance_templates_get_execute()`.
/// For task-level control, use `compute_region_instance_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_templates_get_builder(
        client,
        &args.project,
        &args.region,
        &args.instanceTemplate,
    )?;
    compute_region_instance_templates_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/instanceTemplates
/// Creates an instance template in the specified project and region using the global instance template whose URL is included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_templates_insert_execute()` to send, or `compute_region_instance_templates_insert` for simplest API.

pub fn compute_region_instance_templates_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceTemplates",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instanceTemplates
/// Creates an instance template in the specified project and region using the global instance template whose URL is included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_templates_insert_execute()` or `compute_region_instance_templates_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_templates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instanceTemplates
/// Creates an instance template in the specified project and region using the global instance template whose URL is included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_templates_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_templates_insert_task()`.
/// For the simplest API, use `compute_region_instance_templates_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_templates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_templates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instance_templates_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_templates_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceTemplatesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instanceTemplates
/// Creates an instance template in the specified project and region using the global instance template whose URL is included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_templates_insert_builder()` + `compute_region_instance_templates_insert_execute()`.
/// For task-level control, use `compute_region_instance_templates_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceTemplatesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_templates_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_instance_templates_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_templates_list_execute()` to send, or `compute_region_instance_templates_list` for simplest API.

pub fn compute_region_instance_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceTemplates",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instance_templates_list_execute()` or `compute_region_instance_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplateList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instance_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instance_templates_list_task()`.
/// For the simplest API, use `compute_region_instance_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instance_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instance_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instance_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instance_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstanceTemplatesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_templates_list_builder()` + `compute_region_instance_templates_list_execute()`.
/// For task-level control, use `compute_region_instance_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstanceTemplatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_templates_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instance_templates_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/instances/bulkInsert
/// Creates multiple instances in a given region. Count specifies the number of instances to create.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instances_bulk_insert_execute()` to send, or `compute_region_instances_bulk_insert` for simplest API.

pub fn compute_region_instances_bulk_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instances/bulkInsert",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instances/bulkInsert
/// Creates multiple instances in a given region. Count specifies the number of instances to create.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instances_bulk_insert_execute()` or `compute_region_instances_bulk_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instances_bulk_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instances_bulk_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instances/bulkInsert
/// Creates multiple instances in a given region. Count specifies the number of instances to create.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instances_bulk_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instances_bulk_insert_task()`.
/// For the simplest API, use `compute_region_instances_bulk_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instances_bulk_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instances_bulk_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instances_bulk_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instances_bulk_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstancesBulkInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instances/bulkInsert
/// Creates multiple instances in a given region. Count specifies the number of instances to create.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instances_bulk_insert_builder()` + `compute_region_instances_bulk_insert_execute()`.
/// For task-level control, use `compute_region_instances_bulk_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instances_bulk_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstancesBulkInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instances_bulk_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_instances_bulk_insert_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
/// deletes a Regional InstantSnapshotGroup resource
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshot_groups_delete_execute()` to send, or `compute_region_instant_snapshot_groups_delete` for simplest API.

pub fn compute_region_instant_snapshot_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instantSnapshotGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}",
        project, region, instantSnapshotGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
/// deletes a Regional InstantSnapshotGroup resource
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshot_groups_delete_execute()` or `compute_region_instant_snapshot_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
/// deletes a Regional InstantSnapshotGroup resource
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshot_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshot_groups_delete_task()`.
/// For the simplest API, use `compute_region_instant_snapshot_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshot_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshot_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshot_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instantSnapshotGroup
    pub instantSnapshotGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
/// deletes a Regional InstantSnapshotGroup resource
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshot_groups_delete_builder()` + `compute_region_instant_snapshot_groups_delete_execute()`.
/// For task-level control, use `compute_region_instant_snapshot_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshot_groups_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.instantSnapshotGroup,
        &args.requestId,
    )?;
    compute_region_instant_snapshot_groups_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
/// returns the specified InstantSnapshotGroup resource in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshot_groups_get_execute()` to send, or `compute_region_instant_snapshot_groups_get` for simplest API.

pub fn compute_region_instant_snapshot_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instantSnapshotGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}",
        project, region, instantSnapshotGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
/// returns the specified InstantSnapshotGroup resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshot_groups_get_execute()` or `compute_region_instant_snapshot_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshotGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
/// returns the specified InstantSnapshotGroup resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshot_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshot_groups_get_task()`.
/// For the simplest API, use `compute_region_instant_snapshot_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshot_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshot_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshot_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instantSnapshotGroup
    pub instantSnapshotGroup: String,
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
/// returns the specified InstantSnapshotGroup resource in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshot_groups_get_builder()` + `compute_region_instant_snapshot_groups_get_execute()`.
/// For task-level control, use `compute_region_instant_snapshot_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshot_groups_get_builder(
        client,
        &args.project,
        &args.region,
        &args.instantSnapshotGroup,
    )?;
    compute_region_instant_snapshot_groups_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshot_groups_get_iam_policy_execute()` to send, or `compute_region_instant_snapshot_groups_get_iam_policy` for simplest API.

pub fn compute_region_instant_snapshot_groups_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshot_groups_get_iam_policy_execute()` or `compute_region_instant_snapshot_groups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshot_groups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshot_groups_get_iam_policy_task()`.
/// For the simplest API, use `compute_region_instant_snapshot_groups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshot_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshot_groups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshot_groups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshot_groups_get_iam_policy_builder()` + `compute_region_instant_snapshot_groups_get_iam_policy_execute()`.
/// For task-level control, use `compute_region_instant_snapshot_groups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshot_groups_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_region_instant_snapshot_groups_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups
/// creates a Regional InstantSnapshotGroup resource
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshot_groups_insert_execute()` to send, or `compute_region_instant_snapshot_groups_insert` for simplest API.

pub fn compute_region_instant_snapshot_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
    sourceConsistencyGroup: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceConsistencyGroup.as_ref() {
        query_parts.push(format!("sourceConsistencyGroup={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups
/// creates a Regional InstantSnapshotGroup resource
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshot_groups_insert_execute()` or `compute_region_instant_snapshot_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups
/// creates a Regional InstantSnapshotGroup resource
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshot_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshot_groups_insert_task()`.
/// For the simplest API, use `compute_region_instant_snapshot_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshot_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshot_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshot_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceConsistencyGroup
    pub sourceConsistencyGroup: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups
/// creates a Regional InstantSnapshotGroup resource
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshot_groups_insert_builder()` + `compute_region_instant_snapshot_groups_insert_execute()`.
/// For task-level control, use `compute_region_instant_snapshot_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshot_groups_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
        &args.sourceConsistencyGroup,
    )?;
    compute_region_instant_snapshot_groups_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups
/// retrieves the list of InstantSnapshotGroup resources contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshot_groups_list_execute()` to send, or `compute_region_instant_snapshot_groups_list` for simplest API.

pub fn compute_region_instant_snapshot_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups
/// retrieves the list of InstantSnapshotGroup resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshot_groups_list_execute()` or `compute_region_instant_snapshot_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInstantSnapshotGroups>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInstantSnapshotGroups = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups
/// retrieves the list of InstantSnapshotGroup resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshot_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshot_groups_list_task()`.
/// For the simplest API, use `compute_region_instant_snapshot_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshot_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstantSnapshotGroups>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshot_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshot_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instantSnapshotGroups
/// retrieves the list of InstantSnapshotGroup resources contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshot_groups_list_builder()` + `compute_region_instant_snapshot_groups_list_execute()`.
/// For task-level control, use `compute_region_instant_snapshot_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstantSnapshotGroups>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshot_groups_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instant_snapshot_groups_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshot_groups_set_iam_policy_execute()` to send, or `compute_region_instant_snapshot_groups_set_iam_policy` for simplest API.

pub fn compute_region_instant_snapshot_groups_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshot_groups_set_iam_policy_execute()` or `compute_region_instant_snapshot_groups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshot_groups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshot_groups_set_iam_policy_task()`.
/// For the simplest API, use `compute_region_instant_snapshot_groups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshot_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshot_groups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshot_groups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshot_groups_set_iam_policy_builder()` + `compute_region_instant_snapshot_groups_set_iam_policy_execute()`.
/// For task-level control, use `compute_region_instant_snapshot_groups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshot_groups_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_instant_snapshot_groups_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshot_groups_test_iam_permissions_execute()` to send, or `compute_region_instant_snapshot_groups_test_iam_permissions` for simplest API.

pub fn compute_region_instant_snapshot_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshot_groups_test_iam_permissions_execute()` or `compute_region_instant_snapshot_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshot_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshot_groups_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_instant_snapshot_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshot_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshot_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshot_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshot_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshot_groups_test_iam_permissions_builder()` + `compute_region_instant_snapshot_groups_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_instant_snapshot_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshot_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshot_groups_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_instant_snapshot_groups_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_delete_execute()` to send, or `compute_region_instant_snapshots_delete` for simplest API.

pub fn compute_region_instant_snapshots_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instantSnapshot: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}",
        project, region, instantSnapshot,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshots_delete_execute()` or `compute_region_instant_snapshots_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshots_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshots_delete_task()`.
/// For the simplest API, use `compute_region_instant_snapshots_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshots_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshots_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instantSnapshot
    pub instantSnapshot: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_delete_builder()` + `compute_region_instant_snapshots_delete_execute()`.
/// For task-level control, use `compute_region_instant_snapshots_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.instantSnapshot,
        &args.requestId,
    )?;
    compute_region_instant_snapshots_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_get_execute()` to send, or `compute_region_instant_snapshots_get` for simplest API.

pub fn compute_region_instant_snapshots_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    instantSnapshot: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}",
        project, region, instantSnapshot,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshots_get_execute()` or `compute_region_instant_snapshots_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshots_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshots_get_task()`.
/// For the simplest API, use `compute_region_instant_snapshots_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshots_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshots_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: instantSnapshot
    pub instantSnapshot: String,
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_get_builder()` + `compute_region_instant_snapshots_get_execute()`.
/// For task-level control, use `compute_region_instant_snapshots_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_get_builder(
        client,
        &args.project,
        &args.region,
        &args.instantSnapshot,
    )?;
    compute_region_instant_snapshots_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_get_iam_policy_execute()` to send, or `compute_region_instant_snapshots_get_iam_policy` for simplest API.

pub fn compute_region_instant_snapshots_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshots_get_iam_policy_execute()` or `compute_region_instant_snapshots_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshots_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshots_get_iam_policy_task()`.
/// For the simplest API, use `compute_region_instant_snapshots_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshots_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshots_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshots_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_get_iam_policy_builder()` + `compute_region_instant_snapshots_get_iam_policy_execute()`.
/// For task-level control, use `compute_region_instant_snapshots_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_region_instant_snapshots_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshots
/// Creates an instant snapshot in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_insert_execute()` to send, or `compute_region_instant_snapshots_insert` for simplest API.

pub fn compute_region_instant_snapshots_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshots
/// Creates an instant snapshot in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshots_insert_execute()` or `compute_region_instant_snapshots_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instantSnapshots
/// Creates an instant snapshot in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshots_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshots_insert_task()`.
/// For the simplest API, use `compute_region_instant_snapshots_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshots_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshots_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshots_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instantSnapshots
/// Creates an instant snapshot in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_insert_builder()` + `compute_region_instant_snapshots_insert_execute()`.
/// For task-level control, use `compute_region_instant_snapshots_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_instant_snapshots_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_list_execute()` to send, or `compute_region_instant_snapshots_list` for simplest API.

pub fn compute_region_instant_snapshots_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshots_list_execute()` or `compute_region_instant_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshotList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshots_list_task()`.
/// For the simplest API, use `compute_region_instant_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_list_builder()` + `compute_region_instant_snapshots_list_execute()`.
/// For task-level control, use `compute_region_instant_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_instant_snapshots_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_set_iam_policy_execute()` to send, or `compute_region_instant_snapshots_set_iam_policy` for simplest API.

pub fn compute_region_instant_snapshots_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshots_set_iam_policy_execute()` or `compute_region_instant_snapshots_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshots_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshots_set_iam_policy_task()`.
/// For the simplest API, use `compute_region_instant_snapshots_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshots_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshots_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshots_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_set_iam_policy_builder()` + `compute_region_instant_snapshots_set_iam_policy_execute()`.
/// For task-level control, use `compute_region_instant_snapshots_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_instant_snapshots_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given region. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_set_labels_execute()` to send, or `compute_region_instant_snapshots_set_labels` for simplest API.

pub fn compute_region_instant_snapshots_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}/setLabels",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given region. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshots_set_labels_execute()` or `compute_region_instant_snapshots_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given region. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshots_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshots_set_labels_task()`.
/// For the simplest API, use `compute_region_instant_snapshots_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshots_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshots_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshots_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given region. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_set_labels_builder()` + `compute_region_instant_snapshots_set_labels_execute()`.
/// For task-level control, use `compute_region_instant_snapshots_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotsSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_region_instant_snapshots_set_labels_execute(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_test_iam_permissions_execute()` to send, or `compute_region_instant_snapshots_test_iam_permissions` for simplest API.

pub fn compute_region_instant_snapshots_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_instant_snapshots_test_iam_permissions_execute()` or `compute_region_instant_snapshots_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_instant_snapshots_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_instant_snapshots_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_instant_snapshots_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_instant_snapshots_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_instant_snapshots_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_instant_snapshots_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_instant_snapshots_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_test_iam_permissions_builder()` + `compute_region_instant_snapshots_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_instant_snapshots_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionInstantSnapshotsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_instant_snapshots_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_attach_network_endpoints_execute()` to send, or `compute_region_network_endpoint_groups_attach_network_endpoints` for simplest API.

pub fn compute_region_network_endpoint_groups_attach_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}/attachNetworkEndpoints",
        project,
        region,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_endpoint_groups_attach_network_endpoints_execute()` or `compute_region_network_endpoint_groups_attach_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_attach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_attach_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_endpoint_groups_attach_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_endpoint_groups_attach_network_endpoints_task()`.
/// For the simplest API, use `compute_region_network_endpoint_groups_attach_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_attach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_endpoint_groups_attach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_endpoint_groups_attach_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_endpoint_groups_attach_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_attach_network_endpoints_builder()` + `compute_region_network_endpoint_groups_attach_network_endpoints_execute()`.
/// For task-level control, use `compute_region_network_endpoint_groups_attach_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_attach_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_attach_network_endpoints_builder(
        client,
        &args.project,
        &args.region,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_region_network_endpoint_groups_attach_network_endpoints_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. Note that the NEG cannot be deleted if it is configured as a backend of a backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_delete_execute()` to send, or `compute_region_network_endpoint_groups_delete` for simplest API.

pub fn compute_region_network_endpoint_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}",
        project, region, networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. Note that the NEG cannot be deleted if it is configured as a backend of a backend service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_endpoint_groups_delete_execute()` or `compute_region_network_endpoint_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. Note that the NEG cannot be deleted if it is configured as a backend of a backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_endpoint_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_endpoint_groups_delete_task()`.
/// For the simplest API, use `compute_region_network_endpoint_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_endpoint_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_endpoint_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_endpoint_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. Note that the NEG cannot be deleted if it is configured as a backend of a backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_delete_builder()` + `compute_region_network_endpoint_groups_delete_execute()`.
/// For task-level control, use `compute_region_network_endpoint_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkEndpointGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_region_network_endpoint_groups_delete_execute(builder)
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_detach_network_endpoints_execute()` to send, or `compute_region_network_endpoint_groups_detach_network_endpoints` for simplest API.

pub fn compute_region_network_endpoint_groups_detach_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkEndpointGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}/detachNetworkEndpoints",
        project,
        region,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_endpoint_groups_detach_network_endpoints_execute()` or `compute_region_network_endpoint_groups_detach_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_detach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_detach_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_endpoint_groups_detach_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_endpoint_groups_detach_network_endpoints_task()`.
/// For the simplest API, use `compute_region_network_endpoint_groups_detach_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_detach_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_endpoint_groups_detach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_endpoint_groups_detach_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_endpoint_groups_detach_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_detach_network_endpoints_builder()` + `compute_region_network_endpoint_groups_detach_network_endpoints_execute()`.
/// For task-level control, use `compute_region_network_endpoint_groups_detach_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_detach_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_detach_network_endpoints_builder(
        client,
        &args.project,
        &args.region,
        &args.networkEndpointGroup,
        &args.requestId,
    )?;
    compute_region_network_endpoint_groups_detach_network_endpoints_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_get_execute()` to send, or `compute_region_network_endpoint_groups_get` for simplest API.

pub fn compute_region_network_endpoint_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkEndpointGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}",
        project, region, networkEndpointGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_endpoint_groups_get_execute()` or `compute_region_network_endpoint_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_endpoint_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_endpoint_groups_get_task()`.
/// For the simplest API, use `compute_region_network_endpoint_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_endpoint_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_endpoint_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_endpoint_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_get_builder()` + `compute_region_network_endpoint_groups_get_execute()`.
/// For task-level control, use `compute_region_network_endpoint_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkEndpointGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_get_builder(
        client,
        &args.project,
        &args.region,
        &args.networkEndpointGroup,
    )?;
    compute_region_network_endpoint_groups_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_insert_execute()` to send, or `compute_region_network_endpoint_groups_insert` for simplest API.

pub fn compute_region_network_endpoint_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_endpoint_groups_insert_execute()` or `compute_region_network_endpoint_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_endpoint_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_endpoint_groups_insert_task()`.
/// For the simplest API, use `compute_region_network_endpoint_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_endpoint_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_endpoint_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_endpoint_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_insert_builder()` + `compute_region_network_endpoint_groups_insert_execute()`.
/// For task-level control, use `compute_region_network_endpoint_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkEndpointGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_network_endpoint_groups_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Retrieves the list of regional network endpoint groups available to the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_list_execute()` to send, or `compute_region_network_endpoint_groups_list` for simplest API.

pub fn compute_region_network_endpoint_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Retrieves the list of regional network endpoint groups available to the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_endpoint_groups_list_execute()` or `compute_region_network_endpoint_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Retrieves the list of regional network endpoint groups available to the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_endpoint_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_endpoint_groups_list_task()`.
/// For the simplest API, use `compute_region_network_endpoint_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_endpoint_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_endpoint_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_endpoint_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Retrieves the list of regional network endpoint groups available to the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_list_builder()` + `compute_region_network_endpoint_groups_list_execute()`.
/// For task-level control, use `compute_region_network_endpoint_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkEndpointGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_network_endpoint_groups_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_list_network_endpoints_execute()` to send, or `compute_region_network_endpoint_groups_list_network_endpoints` for simplest API.

pub fn compute_region_network_endpoint_groups_list_network_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    networkEndpointGroup: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}/listNetworkEndpoints",
        project,
        region,
        networkEndpointGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_endpoint_groups_list_network_endpoints_execute()` or `compute_region_network_endpoint_groups_list_network_endpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_list_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_list_network_endpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_endpoint_groups_list_network_endpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_endpoint_groups_list_network_endpoints_task()`.
/// For the simplest API, use `compute_region_network_endpoint_groups_list_network_endpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_endpoint_groups_list_network_endpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_endpoint_groups_list_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_endpoint_groups_list_network_endpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_endpoint_groups_list_network_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: networkEndpointGroup
    pub networkEndpointGroup: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_list_network_endpoints_builder()` + `compute_region_network_endpoint_groups_list_network_endpoints_execute()`.
/// For task-level control, use `compute_region_network_endpoint_groups_list_network_endpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_list_network_endpoints(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_list_network_endpoints_builder(
        client,
        &args.project,
        &args.region,
        &args.networkEndpointGroup,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_network_endpoint_groups_list_network_endpoints_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_add_association_execute()` to send, or `compute_region_network_firewall_policies_add_association` for simplest API.

pub fn compute_region_network_firewall_policies_add_association_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    replaceExistingAssociation: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/addAssociation",
        project,
        region,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = replaceExistingAssociation.as_ref() {
        query_parts.push(format!("replaceExistingAssociation={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_add_association_execute()` or `compute_region_network_firewall_policies_add_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_add_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_add_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_add_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_add_association_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_add_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_add_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_add_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_add_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_add_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesAddAssociationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: replaceExistingAssociation
    pub replaceExistingAssociation: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_add_association_builder()` + `compute_region_network_firewall_policies_add_association_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_add_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_add_association(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesAddAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_add_association_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.replaceExistingAssociation,
        &args.requestId,
    )?;
    compute_region_network_firewall_policies_add_association_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_add_rule_execute()` to send, or `compute_region_network_firewall_policies_add_rule` for simplest API.

pub fn compute_region_network_firewall_policies_add_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    maxPriority: &Option<Option<String>>,
    minPriority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/addRule",
        project,
        region,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxPriority.as_ref() {
        query_parts.push(format!("maxPriority={}", val));
    }
    if let Some(val) = minPriority.as_ref() {
        query_parts.push(format!("minPriority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_add_rule_execute()` or `compute_region_network_firewall_policies_add_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_add_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_add_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_add_rule_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_add_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_add_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_add_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesAddRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: maxPriority
    pub maxPriority: Option<Option<String>>,
    /// Query parameter: minPriority
    pub minPriority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_add_rule_builder()` + `compute_region_network_firewall_policies_add_rule_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_add_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_add_rule(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesAddRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_add_rule_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.maxPriority,
        &args.minPriority,
        &args.requestId,
    )?;
    compute_region_network_firewall_policies_add_rule_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_clone_rules_execute()` to send, or `compute_region_network_firewall_policies_clone_rules` for simplest API.

pub fn compute_region_network_firewall_policies_clone_rules_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
    sourceFirewallPolicy: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/cloneRules",
        project,
        region,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceFirewallPolicy.as_ref() {
        query_parts.push(format!("sourceFirewallPolicy={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_clone_rules_execute()` or `compute_region_network_firewall_policies_clone_rules`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_clone_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_clone_rules_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_clone_rules_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_clone_rules_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_clone_rules()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_clone_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_clone_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_clone_rules_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_clone_rules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesCloneRulesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceFirewallPolicy
    pub sourceFirewallPolicy: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_clone_rules_builder()` + `compute_region_network_firewall_policies_clone_rules_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_clone_rules_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_clone_rules(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesCloneRulesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_clone_rules_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.requestId,
        &args.sourceFirewallPolicy,
    )?;
    compute_region_network_firewall_policies_clone_rules_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Deletes the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_delete_execute()` to send, or `compute_region_network_firewall_policies_delete` for simplest API.

pub fn compute_region_network_firewall_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}",
        project, region, firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Deletes the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_delete_execute()` or `compute_region_network_firewall_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Deletes the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_delete_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Deletes the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_delete_builder()` + `compute_region_network_firewall_policies_delete_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.requestId,
    )?;
    compute_region_network_firewall_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_execute()` to send, or `compute_region_network_firewall_policies_get` for simplest API.

pub fn compute_region_network_firewall_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}",
        project, region, firewallPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_get_execute()` or `compute_region_network_firewall_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_get_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_builder()` + `compute_region_network_firewall_policies_get_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
    )?;
    compute_region_network_firewall_policies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_association_execute()` to send, or `compute_region_network_firewall_policies_get_association` for simplest API.

pub fn compute_region_network_firewall_policies_get_association_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    name: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/getAssociation",
        project,
        region,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_get_association_execute()` or `compute_region_network_firewall_policies_get_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_get_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_get_association_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_get_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_get_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_get_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_get_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetAssociationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_association_builder()` + `compute_region_network_firewall_policies_get_association_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_get_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_association(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesGetAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_association_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.name,
    )?;
    compute_region_network_firewall_policies_get_association_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_effective_firewalls_execute()` to send, or `compute_region_network_firewall_policies_get_effective_firewalls` for simplest API.

pub fn compute_region_network_firewall_policies_get_effective_firewalls_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    network: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/getEffectiveFirewalls",
        project,
        region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = network.as_ref() {
        query_parts.push(format!("network={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_get_effective_firewalls_execute()` or `compute_region_network_firewall_policies_get_effective_firewalls`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_effective_firewalls_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_effective_firewalls_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_get_effective_firewalls_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_get_effective_firewalls_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_get_effective_firewalls()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_effective_firewalls_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_get_effective_firewalls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_get_effective_firewalls_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_get_effective_firewalls`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: network
    pub network: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_effective_firewalls_builder()` + `compute_region_network_firewall_policies_get_effective_firewalls_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_get_effective_firewalls_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_effective_firewalls(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_effective_firewalls_builder(
        client,
        &args.project,
        &args.region,
        &args.network,
    )?;
    compute_region_network_firewall_policies_get_effective_firewalls_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_iam_policy_execute()` to send, or `compute_region_network_firewall_policies_get_iam_policy` for simplest API.

pub fn compute_region_network_firewall_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_get_iam_policy_execute()` or `compute_region_network_firewall_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_get_iam_policy_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_iam_policy_builder()` + `compute_region_network_firewall_policies_get_iam_policy_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_region_network_firewall_policies_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_rule_execute()` to send, or `compute_region_network_firewall_policies_get_rule` for simplest API.

pub fn compute_region_network_firewall_policies_get_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/getRule",
        project,
        region,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_get_rule_execute()` or `compute_region_network_firewall_policies_get_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_get_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_get_rule_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_get_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_get_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_get_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_rule_builder()` + `compute_region_network_firewall_policies_get_rule_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_get_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_rule(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesGetRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_rule_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.priority,
    )?;
    compute_region_network_firewall_policies_get_rule_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies
/// Creates a new network firewall policy in the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_insert_execute()` to send, or `compute_region_network_firewall_policies_insert` for simplest API.

pub fn compute_region_network_firewall_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies
/// Creates a new network firewall policy in the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_insert_execute()` or `compute_region_network_firewall_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies
/// Creates a new network firewall policy in the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_insert_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/firewallPolicies
/// Creates a new network firewall policy in the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_insert_builder()` + `compute_region_network_firewall_policies_insert_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_network_firewall_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Lists all the network firewall policies that have been configured for the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_list_execute()` to send, or `compute_region_network_firewall_policies_list` for simplest API.

pub fn compute_region_network_firewall_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Lists all the network firewall policies that have been configured for the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_list_execute()` or `compute_region_network_firewall_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Lists all the network firewall policies that have been configured for the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_list_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Lists all the network firewall policies that have been configured for the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_list_builder()` + `compute_region_network_firewall_policies_list_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_network_firewall_policies_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Patches the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_patch_execute()` to send, or `compute_region_network_firewall_policies_patch` for simplest API.

pub fn compute_region_network_firewall_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}",
        project, region, firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Patches the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_patch_execute()` or `compute_region_network_firewall_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Patches the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_patch_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Patches the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_patch_builder()` + `compute_region_network_firewall_policies_patch_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.requestId,
    )?;
    compute_region_network_firewall_policies_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_patch_rule_execute()` to send, or `compute_region_network_firewall_policies_patch_rule` for simplest API.

pub fn compute_region_network_firewall_policies_patch_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/patchRule",
        project,
        region,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_patch_rule_execute()` or `compute_region_network_firewall_policies_patch_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_patch_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_patch_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_patch_rule_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_patch_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_patch_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_patch_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesPatchRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_patch_rule_builder()` + `compute_region_network_firewall_policies_patch_rule_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_patch_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_patch_rule(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesPatchRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_patch_rule_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_region_network_firewall_policies_patch_rule_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_remove_association_execute()` to send, or `compute_region_network_firewall_policies_remove_association` for simplest API.

pub fn compute_region_network_firewall_policies_remove_association_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    name: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/removeAssociation",
        project,
        region,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_remove_association_execute()` or `compute_region_network_firewall_policies_remove_association`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_remove_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_remove_association_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_remove_association_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_remove_association_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_remove_association()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_remove_association_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_remove_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_remove_association_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_remove_association`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_remove_association_builder()` + `compute_region_network_firewall_policies_remove_association_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_remove_association_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_remove_association(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_remove_association_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.name,
        &args.requestId,
    )?;
    compute_region_network_firewall_policies_remove_association_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_remove_rule_execute()` to send, or `compute_region_network_firewall_policies_remove_rule` for simplest API.

pub fn compute_region_network_firewall_policies_remove_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    firewallPolicy: &String,
    priority: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/removeRule",
        project,
        region,
        firewallPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_remove_rule_execute()` or `compute_region_network_firewall_policies_remove_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_remove_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_remove_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_remove_rule_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_remove_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_remove_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_remove_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: firewallPolicy
    pub firewallPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_remove_rule_builder()` + `compute_region_network_firewall_policies_remove_rule_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_remove_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_remove_rule(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_remove_rule_builder(
        client,
        &args.project,
        &args.region,
        &args.firewallPolicy,
        &args.priority,
        &args.requestId,
    )?;
    compute_region_network_firewall_policies_remove_rule_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_set_iam_policy_execute()` to send, or `compute_region_network_firewall_policies_set_iam_policy` for simplest API.

pub fn compute_region_network_firewall_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_set_iam_policy_execute()` or `compute_region_network_firewall_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_set_iam_policy_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_set_iam_policy_builder()` + `compute_region_network_firewall_policies_set_iam_policy_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_network_firewall_policies_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_test_iam_permissions_execute()` to send, or `compute_region_network_firewall_policies_test_iam_permissions` for simplest API.

pub fn compute_region_network_firewall_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_network_firewall_policies_test_iam_permissions_execute()` or `compute_region_network_firewall_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_network_firewall_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_network_firewall_policies_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_network_firewall_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_network_firewall_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_network_firewall_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_network_firewall_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_network_firewall_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_test_iam_permissions_builder()` + `compute_region_network_firewall_policies_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_network_firewall_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_network_firewall_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/notificationEndpoints
/// Retrieves the list of all NotificationEndpoint resources, regional and global, available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_aggregated_list_execute()` to send, or `compute_region_notification_endpoints_aggregated_list` for simplest API.

pub fn compute_region_notification_endpoints_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/notificationEndpoints",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/notificationEndpoints
/// Retrieves the list of all NotificationEndpoint resources, regional and global, available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_notification_endpoints_aggregated_list_execute()` or `compute_region_notification_endpoints_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationEndpointAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationEndpointAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/notificationEndpoints
/// Retrieves the list of all NotificationEndpoint resources, regional and global, available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_notification_endpoints_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_notification_endpoints_aggregated_list_task()`.
/// For the simplest API, use `compute_region_notification_endpoints_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_notification_endpoints_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NotificationEndpointAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_notification_endpoints_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_notification_endpoints_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/notificationEndpoints
/// Retrieves the list of all NotificationEndpoint resources, regional and global, available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_aggregated_list_builder()` + `compute_region_notification_endpoints_aggregated_list_execute()`.
/// For task-level control, use `compute_region_notification_endpoints_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionNotificationEndpointsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NotificationEndpointAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_region_notification_endpoints_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Deletes the specified NotificationEndpoint in the given region
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_delete_execute()` to send, or `compute_region_notification_endpoints_delete` for simplest API.

pub fn compute_region_notification_endpoints_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    notificationEndpoint: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints/{}",
        project, region, notificationEndpoint,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Deletes the specified NotificationEndpoint in the given region
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_notification_endpoints_delete_execute()` or `compute_region_notification_endpoints_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Deletes the specified NotificationEndpoint in the given region
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_notification_endpoints_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_notification_endpoints_delete_task()`.
/// For the simplest API, use `compute_region_notification_endpoints_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_notification_endpoints_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_notification_endpoints_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_notification_endpoints_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: notificationEndpoint
    pub notificationEndpoint: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Deletes the specified NotificationEndpoint in the given region
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_delete_builder()` + `compute_region_notification_endpoints_delete_execute()`.
/// For task-level control, use `compute_region_notification_endpoints_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionNotificationEndpointsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.notificationEndpoint,
        &args.requestId,
    )?;
    compute_region_notification_endpoints_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Returns the specified NotificationEndpoint resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_get_execute()` to send, or `compute_region_notification_endpoints_get` for simplest API.

pub fn compute_region_notification_endpoints_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    notificationEndpoint: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints/{}",
        project, region, notificationEndpoint,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Returns the specified NotificationEndpoint resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_notification_endpoints_get_execute()` or `compute_region_notification_endpoints_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationEndpoint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationEndpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Returns the specified NotificationEndpoint resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_notification_endpoints_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_notification_endpoints_get_task()`.
/// For the simplest API, use `compute_region_notification_endpoints_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_notification_endpoints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationEndpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_notification_endpoints_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_notification_endpoints_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: notificationEndpoint
    pub notificationEndpoint: String,
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Returns the specified NotificationEndpoint resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_get_builder()` + `compute_region_notification_endpoints_get_execute()`.
/// For task-level control, use `compute_region_notification_endpoints_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionNotificationEndpointsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationEndpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_get_builder(
        client,
        &args.project,
        &args.region,
        &args.notificationEndpoint,
    )?;
    compute_region_notification_endpoints_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/notificationEndpoints
/// Create a NotificationEndpoint in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_insert_execute()` to send, or `compute_region_notification_endpoints_insert` for simplest API.

pub fn compute_region_notification_endpoints_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/notificationEndpoints
/// Create a NotificationEndpoint in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_notification_endpoints_insert_execute()` or `compute_region_notification_endpoints_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/notificationEndpoints
/// Create a NotificationEndpoint in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_notification_endpoints_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_notification_endpoints_insert_task()`.
/// For the simplest API, use `compute_region_notification_endpoints_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_notification_endpoints_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_notification_endpoints_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_notification_endpoints_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/notificationEndpoints
/// Create a NotificationEndpoint in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_insert_builder()` + `compute_region_notification_endpoints_insert_execute()`.
/// For task-level control, use `compute_region_notification_endpoints_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionNotificationEndpointsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_notification_endpoints_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Lists the NotificationEndpoints for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_list_execute()` to send, or `compute_region_notification_endpoints_list` for simplest API.

pub fn compute_region_notification_endpoints_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Lists the NotificationEndpoints for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_notification_endpoints_list_execute()` or `compute_region_notification_endpoints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationEndpointList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationEndpointList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Lists the NotificationEndpoints for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_notification_endpoints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_notification_endpoints_list_task()`.
/// For the simplest API, use `compute_region_notification_endpoints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_notification_endpoints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationEndpointList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_notification_endpoints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_notification_endpoints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Lists the NotificationEndpoints for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_list_builder()` + `compute_region_notification_endpoints_list_execute()`.
/// For task-level control, use `compute_region_notification_endpoints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionNotificationEndpointsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationEndpointList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_notification_endpoints_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_test_iam_permissions_execute()` to send, or `compute_region_notification_endpoints_test_iam_permissions` for simplest API.

pub fn compute_region_notification_endpoints_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_notification_endpoints_test_iam_permissions_execute()` or `compute_region_notification_endpoints_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_notification_endpoints_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_notification_endpoints_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_notification_endpoints_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_notification_endpoints_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_notification_endpoints_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_notification_endpoints_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_notification_endpoints_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_test_iam_permissions_builder()` + `compute_region_notification_endpoints_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_notification_endpoints_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionNotificationEndpointsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_notification_endpoints_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/operations/{operation}
/// Deletes the specified region-specific Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_operations_delete_execute()` to send, or `compute_region_operations_delete` for simplest API.

pub fn compute_region_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/operations/{}",
        project, region, operation,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/operations/{operation}
/// Deletes the specified region-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_operations_delete_execute()` or `compute_region_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/operations/{operation}
/// Deletes the specified region-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_operations_delete_task()`.
/// For the simplest API, use `compute_region_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionOperationsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: operation
    pub operation: String,
}

/// DELETE projects/{project}/regions/{region}/operations/{operation}
/// Deletes the specified region-specific Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_operations_delete_builder()` + `compute_region_operations_delete_execute()`.
/// For task-level control, use `compute_region_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_operations_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.operation,
    )?;
    compute_region_operations_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Retrieves the specified region-specific Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_operations_get_execute()` to send, or `compute_region_operations_get` for simplest API.

pub fn compute_region_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/operations/{}",
        project, region, operation,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Retrieves the specified region-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_operations_get_execute()` or `compute_region_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Retrieves the specified region-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_operations_get_task()`.
/// For the simplest API, use `compute_region_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionOperationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: operation
    pub operation: String,
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Retrieves the specified region-specific Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_operations_get_builder()` + `compute_region_operations_get_execute()`.
/// For task-level control, use `compute_region_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_operations_get_builder(
        client,
        &args.project,
        &args.region,
        &args.operation,
    )?;
    compute_region_operations_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/operations
/// Retrieves a list of Operation resources contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_operations_list_execute()` to send, or `compute_region_operations_list` for simplest API.

pub fn compute_region_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/operations",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/operations
/// Retrieves a list of Operation resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_operations_list_execute()` or `compute_region_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/operations
/// Retrieves a list of Operation resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_operations_list_task()`.
/// For the simplest API, use `compute_region_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionOperationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/operations
/// Retrieves a list of Operation resources contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_operations_list_builder()` + `compute_region_operations_list_execute()`.
/// For task-level control, use `compute_region_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_operations_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_operations_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_operations_wait_execute()` to send, or `compute_region_operations_wait` for simplest API.

pub fn compute_region_operations_wait_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/operations/{}/wait",
        project, region, operation,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_operations_wait_execute()` or `compute_region_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_operations_wait_task()`.
/// For the simplest API, use `compute_region_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionOperationsWaitArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: operation
    pub operation: String,
}

/// POST projects/{project}/regions/{region}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_operations_wait_builder()` + `compute_region_operations_wait_execute()`.
/// For task-level control, use `compute_region_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_wait(
    client: &SimpleHttpClient,
    args: &ComputeRegionOperationsWaitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_operations_wait_builder(
        client,
        &args.project,
        &args.region,
        &args.operation,
    )?;
    compute_region_operations_wait_execute(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_add_rule_execute()` to send, or `compute_region_security_policies_add_rule` for simplest API.

pub fn compute_region_security_policies_add_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    securityPolicy: &String,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/addRule",
        project,
        region,
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_add_rule_execute()` or `compute_region_security_policies_add_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_add_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_add_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_add_rule_task()`.
/// For the simplest API, use `compute_region_security_policies_add_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_add_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_add_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesAddRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_add_rule_builder()` + `compute_region_security_policies_add_rule_execute()`.
/// For task-level control, use `compute_region_security_policies_add_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_add_rule(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesAddRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_add_rule_builder(
        client,
        &args.project,
        &args.region,
        &args.securityPolicy,
        &args.validateOnly,
    )?;
    compute_region_security_policies_add_rule_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_delete_execute()` to send, or `compute_region_security_policies_delete` for simplest API.

pub fn compute_region_security_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    securityPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}",
        project, region, securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_delete_execute()` or `compute_region_security_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_delete_task()`.
/// For the simplest API, use `compute_region_security_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_delete_builder()` + `compute_region_security_policies_delete_execute()`.
/// For task-level control, use `compute_region_security_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.securityPolicy,
        &args.requestId,
    )?;
    compute_region_security_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_get_execute()` to send, or `compute_region_security_policies_get` for simplest API.

pub fn compute_region_security_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    securityPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}",
        project, region, securityPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_get_execute()` or `compute_region_security_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_get_task()`.
/// For the simplest API, use `compute_region_security_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_get_builder()` + `compute_region_security_policies_get_execute()`.
/// For task-level control, use `compute_region_security_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_get_builder(
        client,
        &args.project,
        &args.region,
        &args.securityPolicy,
    )?;
    compute_region_security_policies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_get_rule_execute()` to send, or `compute_region_security_policies_get_rule` for simplest API.

pub fn compute_region_security_policies_get_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/getRule",
        project,
        region,
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_get_rule_execute()` or `compute_region_security_policies_get_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_get_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_get_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_get_rule_task()`.
/// For the simplest API, use `compute_region_security_policies_get_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_get_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_get_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesGetRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_get_rule_builder()` + `compute_region_security_policies_get_rule_execute()`.
/// For task-level control, use `compute_region_security_policies_get_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_get_rule(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesGetRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_get_rule_builder(
        client,
        &args.project,
        &args.region,
        &args.securityPolicy,
        &args.priority,
    )?;
    compute_region_security_policies_get_rule_execute(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_insert_execute()` to send, or `compute_region_security_policies_insert` for simplest API.

pub fn compute_region_security_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_insert_execute()` or `compute_region_security_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_insert_task()`.
/// For the simplest API, use `compute_region_security_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_insert_builder()` + `compute_region_security_policies_insert_execute()`.
/// For task-level control, use `compute_region_security_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
        &args.validateOnly,
    )?;
    compute_region_security_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// List all the policies that have been configured for the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_list_execute()` to send, or `compute_region_security_policies_list` for simplest API.

pub fn compute_region_security_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// List all the policies that have been configured for the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_list_execute()` or `compute_region_security_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// List all the policies that have been configured for the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_list_task()`.
/// For the simplest API, use `compute_region_security_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// List all the policies that have been configured for the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_list_builder()` + `compute_region_security_policies_list_execute()`.
/// For task-level control, use `compute_region_security_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_security_policies_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_patch_execute()` to send, or `compute_region_security_policies_patch` for simplest API.

pub fn compute_region_security_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    securityPolicy: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}",
        project, region, securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_patch_execute()` or `compute_region_security_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_patch_task()`.
/// For the simplest API, use `compute_region_security_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_patch_builder()` + `compute_region_security_policies_patch_execute()`.
/// For task-level control, use `compute_region_security_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.securityPolicy,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_region_security_policies_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_patch_rule_execute()` to send, or `compute_region_security_policies_patch_rule` for simplest API.

pub fn compute_region_security_policies_patch_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/patchRule",
        project,
        region,
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_patch_rule_execute()` or `compute_region_security_policies_patch_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_patch_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_patch_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_patch_rule_task()`.
/// For the simplest API, use `compute_region_security_policies_patch_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_patch_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_patch_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesPatchRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_patch_rule_builder()` + `compute_region_security_policies_patch_rule_execute()`.
/// For task-level control, use `compute_region_security_policies_patch_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_patch_rule(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesPatchRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_patch_rule_builder(
        client,
        &args.project,
        &args.region,
        &args.securityPolicy,
        &args.priority,
        &args.updateMask,
        &args.validateOnly,
    )?;
    compute_region_security_policies_patch_rule_execute(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_remove_rule_execute()` to send, or `compute_region_security_policies_remove_rule` for simplest API.

pub fn compute_region_security_policies_remove_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/removeRule",
        project,
        region,
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_remove_rule_execute()` or `compute_region_security_policies_remove_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_remove_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_remove_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_remove_rule_task()`.
/// For the simplest API, use `compute_region_security_policies_remove_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_remove_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_remove_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesRemoveRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_remove_rule_builder()` + `compute_region_security_policies_remove_rule_execute()`.
/// For task-level control, use `compute_region_security_policies_remove_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_remove_rule(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesRemoveRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_remove_rule_builder(
        client,
        &args.project,
        &args.region,
        &args.securityPolicy,
        &args.priority,
    )?;
    compute_region_security_policies_remove_rule_execute(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_set_labels_execute()` to send, or `compute_region_security_policies_set_labels` for simplest API.

pub fn compute_region_security_policies_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/setLabels",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_security_policies_set_labels_execute()` or `compute_region_security_policies_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_security_policies_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_security_policies_set_labels_task()`.
/// For the simplest API, use `compute_region_security_policies_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_security_policies_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_security_policies_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_security_policies_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_security_policies_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_set_labels_builder()` + `compute_region_security_policies_set_labels_execute()`.
/// For task-level control, use `compute_region_security_policies_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeRegionSecurityPoliciesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_region_security_policies_set_labels_execute(builder)
}

/// GET projects/{project}/regions/{region}/snapshotSettings
/// Get region snapshot settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshot_settings_get_execute()` to send, or `compute_region_snapshot_settings_get` for simplest API.

pub fn compute_region_snapshot_settings_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshotSettings",
        project, region,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/snapshotSettings
/// Get region snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshot_settings_get_execute()` or `compute_region_snapshot_settings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshot_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshot_settings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnapshotSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/snapshotSettings
/// Get region snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshot_settings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshot_settings_get_task()`.
/// For the simplest API, use `compute_region_snapshot_settings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshot_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshot_settings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_snapshot_settings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshot_settings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotSettingsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
}

/// GET projects/{project}/regions/{region}/snapshotSettings
/// Get region snapshot settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshot_settings_get_builder()` + `compute_region_snapshot_settings_get_execute()`.
/// For task-level control, use `compute_region_snapshot_settings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshot_settings_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotSettingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_snapshot_settings_get_builder(client, &args.project, &args.region)?;
    compute_region_snapshot_settings_get_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/snapshotSettings
/// Patch region snapshot settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshot_settings_patch_execute()` to send, or `compute_region_snapshot_settings_patch` for simplest API.

pub fn compute_region_snapshot_settings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshotSettings",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/snapshotSettings
/// Patch region snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshot_settings_patch_execute()` or `compute_region_snapshot_settings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshot_settings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshot_settings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/snapshotSettings
/// Patch region snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshot_settings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshot_settings_patch_task()`.
/// For the simplest API, use `compute_region_snapshot_settings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshot_settings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshot_settings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_snapshot_settings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshot_settings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotSettingsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/snapshotSettings
/// Patch region snapshot settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshot_settings_patch_builder()` + `compute_region_snapshot_settings_patch_execute()`.
/// For task-level control, use `compute_region_snapshot_settings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshot_settings_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotSettingsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_snapshot_settings_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_region_snapshot_settings_patch_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_delete_execute()` to send, or `compute_region_snapshots_delete` for simplest API.

pub fn compute_region_snapshots_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    snapshot: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}",
        project, region, snapshot,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_delete_execute()` or `compute_region_snapshots_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_delete_task()`.
/// For the simplest API, use `compute_region_snapshots_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: snapshot
    pub snapshot: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_delete_builder()` + `compute_region_snapshots_delete_execute()`.
/// For task-level control, use `compute_region_snapshots_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_snapshots_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.snapshot,
        &args.requestId,
    )?;
    compute_region_snapshots_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_get_execute()` to send, or `compute_region_snapshots_get` for simplest API.

pub fn compute_region_snapshots_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    snapshot: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}",
        project, region, snapshot,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_get_execute()` or `compute_region_snapshots_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_get_task()`.
/// For the simplest API, use `compute_region_snapshots_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: snapshot
    pub snapshot: String,
}

/// GET projects/{project}/regions/{region}/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_get_builder()` + `compute_region_snapshots_get_execute()`.
/// For task-level control, use `compute_region_snapshots_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_snapshots_get_builder(client, &args.project, &args.region, &args.snapshot)?;
    compute_region_snapshots_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_get_iam_policy_execute()` to send, or `compute_region_snapshots_get_iam_policy` for simplest API.

pub fn compute_region_snapshots_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_get_iam_policy_execute()` or `compute_region_snapshots_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_get_iam_policy_task()`.
/// For the simplest API, use `compute_region_snapshots_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_get_iam_policy_builder()` + `compute_region_snapshots_get_iam_policy_execute()`.
/// For task-level control, use `compute_region_snapshots_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_snapshots_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_region_snapshots_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/snapshots
/// Creates a snapshot in the specified region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_insert_execute()` to send, or `compute_region_snapshots_insert` for simplest API.

pub fn compute_region_snapshots_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/snapshots
/// Creates a snapshot in the specified region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_insert_execute()` or `compute_region_snapshots_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/snapshots
/// Creates a snapshot in the specified region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_insert_task()`.
/// For the simplest API, use `compute_region_snapshots_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/snapshots
/// Creates a snapshot in the specified region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_insert_builder()` + `compute_region_snapshots_insert_execute()`.
/// For task-level control, use `compute_region_snapshots_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_snapshots_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_snapshots_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/snapshots
/// Retrieves the list of Snapshot resources contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_list_execute()` to send, or `compute_region_snapshots_list` for simplest API.

pub fn compute_region_snapshots_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/snapshots
/// Retrieves the list of Snapshot resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_list_execute()` or `compute_region_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnapshotList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/snapshots
/// Retrieves the list of Snapshot resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_list_task()`.
/// For the simplest API, use `compute_region_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/snapshots
/// Retrieves the list of Snapshot resources contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_list_builder()` + `compute_region_snapshots_list_execute()`.
/// For task-level control, use `compute_region_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_snapshots_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_snapshots_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_set_iam_policy_execute()` to send, or `compute_region_snapshots_set_iam_policy` for simplest API.

pub fn compute_region_snapshots_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_set_iam_policy_execute()` or `compute_region_snapshots_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_set_iam_policy_task()`.
/// For the simplest API, use `compute_region_snapshots_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_set_iam_policy_builder()` + `compute_region_snapshots_set_iam_policy_execute()`.
/// For task-level control, use `compute_region_snapshots_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_snapshots_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_snapshots_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setLabels
/// Sets the labels on a regional snapshot. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_set_labels_execute()` to send, or `compute_region_snapshots_set_labels` for simplest API.

pub fn compute_region_snapshots_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/setLabels",
        project, region, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setLabels
/// Sets the labels on a regional snapshot. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_set_labels_execute()` or `compute_region_snapshots_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setLabels
/// Sets the labels on a regional snapshot. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_set_labels_task()`.
/// For the simplest API, use `compute_region_snapshots_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setLabels
/// Sets the labels on a regional snapshot. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_set_labels_builder()` + `compute_region_snapshots_set_labels_execute()`.
/// For task-level control, use `compute_region_snapshots_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_snapshots_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_region_snapshots_set_labels_execute(builder)
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_test_iam_permissions_execute()` to send, or `compute_region_snapshots_test_iam_permissions` for simplest API.

pub fn compute_region_snapshots_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_test_iam_permissions_execute()` or `compute_region_snapshots_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_test_iam_permissions_task()`.
/// For the simplest API, use `compute_region_snapshots_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_test_iam_permissions_builder()` + `compute_region_snapshots_test_iam_permissions_execute()`.
/// For task-level control, use `compute_region_snapshots_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_snapshots_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_region_snapshots_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/regions/{region}/snapshots/{snapshot}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_snapshots_update_kms_key_execute()` to send, or `compute_region_snapshots_update_kms_key` for simplest API.

pub fn compute_region_snapshots_update_kms_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    snapshot: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/updateKmsKey",
        project,
        region,
        snapshot,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/snapshots/{snapshot}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified snapshot.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_snapshots_update_kms_key_execute()` or `compute_region_snapshots_update_kms_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_update_kms_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_update_kms_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/snapshots/{snapshot}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_snapshots_update_kms_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_snapshots_update_kms_key_task()`.
/// For the simplest API, use `compute_region_snapshots_update_kms_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_snapshots_update_kms_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_snapshots_update_kms_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_snapshots_update_kms_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_snapshots_update_kms_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsUpdateKmsKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: snapshot
    pub snapshot: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/snapshots/{snapshot}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_snapshots_update_kms_key_builder()` + `compute_region_snapshots_update_kms_key_execute()`.
/// For task-level control, use `compute_region_snapshots_update_kms_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_snapshots_update_kms_key(
    client: &SimpleHttpClient,
    args: &ComputeRegionSnapshotsUpdateKmsKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_snapshots_update_kms_key_builder(
        client,
        &args.project,
        &args.region,
        &args.snapshot,
        &args.requestId,
    )?;
    compute_region_snapshots_update_kms_key_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource in the region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_certificates_delete_execute()` to send, or `compute_region_ssl_certificates_delete` for simplest API.

pub fn compute_region_ssl_certificates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    sslCertificate: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslCertificates/{}",
        project, region, sslCertificate,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource in the region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_certificates_delete_execute()` or `compute_region_ssl_certificates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource in the region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_certificates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_certificates_delete_task()`.
/// For the simplest API, use `compute_region_ssl_certificates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_ssl_certificates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_certificates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslCertificatesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: sslCertificate
    pub sslCertificate: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource in the region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_certificates_delete_builder()` + `compute_region_ssl_certificates_delete_execute()`.
/// For task-level control, use `compute_region_ssl_certificates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslCertificatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_certificates_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.sslCertificate,
        &args.requestId,
    )?;
    compute_region_ssl_certificates_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource in the specified region. Get a list of available SSL certificates by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_certificates_get_execute()` to send, or `compute_region_ssl_certificates_get` for simplest API.

pub fn compute_region_ssl_certificates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    sslCertificate: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslCertificates/{}",
        project, region, sslCertificate,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource in the specified region. Get a list of available SSL certificates by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_certificates_get_execute()` or `compute_region_ssl_certificates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource in the specified region. Get a list of available SSL certificates by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_certificates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_certificates_get_task()`.
/// For the simplest API, use `compute_region_ssl_certificates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_ssl_certificates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_certificates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslCertificatesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: sslCertificate
    pub sslCertificate: String,
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource in the specified region. Get a list of available SSL certificates by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_certificates_get_builder()` + `compute_region_ssl_certificates_get_execute()`.
/// For task-level control, use `compute_region_ssl_certificates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslCertificatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_certificates_get_builder(
        client,
        &args.project,
        &args.region,
        &args.sslCertificate,
    )?;
    compute_region_ssl_certificates_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/sslCertificates
/// Creates a SslCertificate resource in the specified project and region using the data included in the request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_certificates_insert_execute()` to send, or `compute_region_ssl_certificates_insert` for simplest API.

pub fn compute_region_ssl_certificates_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslCertificates",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/sslCertificates
/// Creates a SslCertificate resource in the specified project and region using the data included in the request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_certificates_insert_execute()` or `compute_region_ssl_certificates_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_certificates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/sslCertificates
/// Creates a SslCertificate resource in the specified project and region using the data included in the request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_certificates_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_certificates_insert_task()`.
/// For the simplest API, use `compute_region_ssl_certificates_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_certificates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_certificates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_ssl_certificates_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_certificates_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslCertificatesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/sslCertificates
/// Creates a SslCertificate resource in the specified project and region using the data included in the request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_certificates_insert_builder()` + `compute_region_ssl_certificates_insert_execute()`.
/// For task-level control, use `compute_region_ssl_certificates_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslCertificatesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_certificates_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_ssl_certificates_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_certificates_list_execute()` to send, or `compute_region_ssl_certificates_list` for simplest API.

pub fn compute_region_ssl_certificates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslCertificates",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_certificates_list_execute()` or `compute_region_ssl_certificates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificateList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_certificates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_certificates_list_task()`.
/// For the simplest API, use `compute_region_ssl_certificates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_ssl_certificates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_certificates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslCertificatesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_certificates_list_builder()` + `compute_region_ssl_certificates_list_execute()`.
/// For task-level control, use `compute_region_ssl_certificates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslCertificatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_certificates_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_ssl_certificates_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_delete_execute()` to send, or `compute_region_ssl_policies_delete` for simplest API.

pub fn compute_region_ssl_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    sslPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies/{}",
        project, region, sslPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_policies_delete_execute()` or `compute_region_ssl_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_policies_delete_task()`.
/// For the simplest API, use `compute_region_ssl_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_ssl_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: sslPolicy
    pub sslPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_delete_builder()` + `compute_region_ssl_policies_delete_execute()`.
/// For task-level control, use `compute_region_ssl_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.sslPolicy,
        &args.requestId,
    )?;
    compute_region_ssl_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_get_execute()` to send, or `compute_region_ssl_policies_get` for simplest API.

pub fn compute_region_ssl_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    sslPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies/{}",
        project, region, sslPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_policies_get_execute()` or `compute_region_ssl_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_policies_get_task()`.
/// For the simplest API, use `compute_region_ssl_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_ssl_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: sslPolicy
    pub sslPolicy: String,
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_get_builder()` + `compute_region_ssl_policies_get_execute()`.
/// For task-level control, use `compute_region_ssl_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_get_builder(
        client,
        &args.project,
        &args.region,
        &args.sslPolicy,
    )?;
    compute_region_ssl_policies_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/sslPolicies
/// Creates a new policy in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_insert_execute()` to send, or `compute_region_ssl_policies_insert` for simplest API.

pub fn compute_region_ssl_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/sslPolicies
/// Creates a new policy in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_policies_insert_execute()` or `compute_region_ssl_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/sslPolicies
/// Creates a new policy in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_policies_insert_task()`.
/// For the simplest API, use `compute_region_ssl_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_ssl_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/sslPolicies
/// Creates a new policy in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_insert_builder()` + `compute_region_ssl_policies_insert_execute()`.
/// For task-level control, use `compute_region_ssl_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_ssl_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_list_execute()` to send, or `compute_region_ssl_policies_list` for simplest API.

pub fn compute_region_ssl_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_policies_list_execute()` or `compute_region_ssl_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPoliciesList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_policies_list_task()`.
/// For the simplest API, use `compute_region_ssl_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_ssl_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_list_builder()` + `compute_region_ssl_policies_list_execute()`.
/// For task-level control, use `compute_region_ssl_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_ssl_policies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_list_available_features_execute()` to send, or `compute_region_ssl_policies_list_available_features` for simplest API.

pub fn compute_region_ssl_policies_list_available_features_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies/listAvailableFeatures",
        project,
        region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_policies_list_available_features_execute()` or `compute_region_ssl_policies_list_available_features`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_list_available_features_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_list_available_features_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesListAvailableFeaturesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_policies_list_available_features_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_policies_list_available_features_task()`.
/// For the simplest API, use `compute_region_ssl_policies_list_available_features()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_list_available_features_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_policies_list_available_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_ssl_policies_list_available_features_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_policies_list_available_features`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesListAvailableFeaturesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_list_available_features_builder()` + `compute_region_ssl_policies_list_available_features_execute()`.
/// For task-level control, use `compute_region_ssl_policies_list_available_features_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_list_available_features(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslPoliciesListAvailableFeaturesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_list_available_features_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_ssl_policies_list_available_features_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_patch_execute()` to send, or `compute_region_ssl_policies_patch` for simplest API.

pub fn compute_region_ssl_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    sslPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies/{}",
        project, region, sslPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_ssl_policies_patch_execute()` or `compute_region_ssl_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_ssl_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_ssl_policies_patch_task()`.
/// For the simplest API, use `compute_region_ssl_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_ssl_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_ssl_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_ssl_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_ssl_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: sslPolicy
    pub sslPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_patch_builder()` + `compute_region_ssl_policies_patch_execute()`.
/// For task-level control, use `compute_region_ssl_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionSslPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.sslPolicy,
        &args.requestId,
    )?;
    compute_region_ssl_policies_patch_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_delete_execute()` to send, or `compute_region_target_http_proxies_delete` for simplest API.

pub fn compute_region_target_http_proxies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetHttpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies/{}",
        project, region, targetHttpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_http_proxies_delete_execute()` or `compute_region_target_http_proxies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_http_proxies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_http_proxies_delete_task()`.
/// For the simplest API, use `compute_region_target_http_proxies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_http_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_http_proxies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_http_proxies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetHttpProxy
    pub targetHttpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_delete_builder()` + `compute_region_target_http_proxies_delete_execute()`.
/// For task-level control, use `compute_region_target_http_proxies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpProxiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.targetHttpProxy,
        &args.requestId,
    )?;
    compute_region_target_http_proxies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_get_execute()` to send, or `compute_region_target_http_proxies_get` for simplest API.

pub fn compute_region_target_http_proxies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetHttpProxy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies/{}",
        project, region, targetHttpProxy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_http_proxies_get_execute()` or `compute_region_target_http_proxies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_http_proxies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_http_proxies_get_task()`.
/// For the simplest API, use `compute_region_target_http_proxies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_http_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_target_http_proxies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_http_proxies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetHttpProxy
    pub targetHttpProxy: String,
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_get_builder()` + `compute_region_target_http_proxies_get_execute()`.
/// For task-level control, use `compute_region_target_http_proxies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpProxiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_get_builder(
        client,
        &args.project,
        &args.region,
        &args.targetHttpProxy,
    )?;
    compute_region_target_http_proxies_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_insert_execute()` to send, or `compute_region_target_http_proxies_insert` for simplest API.

pub fn compute_region_target_http_proxies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_http_proxies_insert_execute()` or `compute_region_target_http_proxies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_http_proxies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_http_proxies_insert_task()`.
/// For the simplest API, use `compute_region_target_http_proxies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_http_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_http_proxies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_http_proxies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_insert_builder()` + `compute_region_target_http_proxies_insert_execute()`.
/// For task-level control, use `compute_region_target_http_proxies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpProxiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_target_http_proxies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_list_execute()` to send, or `compute_region_target_http_proxies_list` for simplest API.

pub fn compute_region_target_http_proxies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_http_proxies_list_execute()` or `compute_region_target_http_proxies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_http_proxies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_http_proxies_list_task()`.
/// For the simplest API, use `compute_region_target_http_proxies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_http_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_target_http_proxies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_http_proxies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_list_builder()` + `compute_region_target_http_proxies_list_execute()`.
/// For task-level control, use `compute_region_target_http_proxies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpProxiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_target_http_proxies_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_set_url_map_execute()` to send, or `compute_region_target_http_proxies_set_url_map` for simplest API.

pub fn compute_region_target_http_proxies_set_url_map_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetHttpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies/{}/setUrlMap",
        project,
        region,
        targetHttpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_http_proxies_set_url_map_execute()` or `compute_region_target_http_proxies_set_url_map`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_set_url_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_set_url_map_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_http_proxies_set_url_map_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_http_proxies_set_url_map_task()`.
/// For the simplest API, use `compute_region_target_http_proxies_set_url_map()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_http_proxies_set_url_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_http_proxies_set_url_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_http_proxies_set_url_map_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_http_proxies_set_url_map`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesSetUrlMapArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetHttpProxy
    pub targetHttpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_set_url_map_builder()` + `compute_region_target_http_proxies_set_url_map_execute()`.
/// For task-level control, use `compute_region_target_http_proxies_set_url_map_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_set_url_map(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpProxiesSetUrlMapArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_set_url_map_builder(
        client,
        &args.project,
        &args.region,
        &args.targetHttpProxy,
        &args.requestId,
    )?;
    compute_region_target_http_proxies_set_url_map_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_delete_execute()` to send, or `compute_region_target_https_proxies_delete` for simplest API.

pub fn compute_region_target_https_proxies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}",
        project, region, targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_https_proxies_delete_execute()` or `compute_region_target_https_proxies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_https_proxies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_https_proxies_delete_task()`.
/// For the simplest API, use `compute_region_target_https_proxies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_https_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_https_proxies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_https_proxies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_delete_builder()` + `compute_region_target_https_proxies_delete_execute()`.
/// For task-level control, use `compute_region_target_https_proxies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpsProxiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_region_target_https_proxies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_get_execute()` to send, or `compute_region_target_https_proxies_get` for simplest API.

pub fn compute_region_target_https_proxies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetHttpsProxy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}",
        project, region, targetHttpsProxy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_https_proxies_get_execute()` or `compute_region_target_https_proxies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_https_proxies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_https_proxies_get_task()`.
/// For the simplest API, use `compute_region_target_https_proxies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_https_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_target_https_proxies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_https_proxies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_get_builder()` + `compute_region_target_https_proxies_get_execute()`.
/// For task-level control, use `compute_region_target_https_proxies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpsProxiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_get_builder(
        client,
        &args.project,
        &args.region,
        &args.targetHttpsProxy,
    )?;
    compute_region_target_https_proxies_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_insert_execute()` to send, or `compute_region_target_https_proxies_insert` for simplest API.

pub fn compute_region_target_https_proxies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_https_proxies_insert_execute()` or `compute_region_target_https_proxies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_https_proxies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_https_proxies_insert_task()`.
/// For the simplest API, use `compute_region_target_https_proxies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_https_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_https_proxies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_https_proxies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_insert_builder()` + `compute_region_target_https_proxies_insert_execute()`.
/// For task-level control, use `compute_region_target_https_proxies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpsProxiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_target_https_proxies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_list_execute()` to send, or `compute_region_target_https_proxies_list` for simplest API.

pub fn compute_region_target_https_proxies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_https_proxies_list_execute()` or `compute_region_target_https_proxies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_https_proxies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_https_proxies_list_task()`.
/// For the simplest API, use `compute_region_target_https_proxies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_https_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_target_https_proxies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_https_proxies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_list_builder()` + `compute_region_target_https_proxies_list_execute()`.
/// For task-level control, use `compute_region_target_https_proxies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpsProxiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_target_https_proxies_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified regional TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_patch_execute()` to send, or `compute_region_target_https_proxies_patch` for simplest API.

pub fn compute_region_target_https_proxies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}",
        project, region, targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified regional TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_https_proxies_patch_execute()` or `compute_region_target_https_proxies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified regional TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_https_proxies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_https_proxies_patch_task()`.
/// For the simplest API, use `compute_region_target_https_proxies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_https_proxies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_https_proxies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_https_proxies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified regional TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_patch_builder()` + `compute_region_target_https_proxies_patch_execute()`.
/// For task-level control, use `compute_region_target_https_proxies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpsProxiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_region_target_https_proxies_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_set_ssl_certificates_execute()` to send, or `compute_region_target_https_proxies_set_ssl_certificates` for simplest API.

pub fn compute_region_target_https_proxies_set_ssl_certificates_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}/setSslCertificates",
        project,
        region,
        targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_https_proxies_set_ssl_certificates_execute()` or `compute_region_target_https_proxies_set_ssl_certificates`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_set_ssl_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_set_ssl_certificates_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_https_proxies_set_ssl_certificates_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_https_proxies_set_ssl_certificates_task()`.
/// For the simplest API, use `compute_region_target_https_proxies_set_ssl_certificates()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_set_ssl_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_https_proxies_set_ssl_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_https_proxies_set_ssl_certificates_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_https_proxies_set_ssl_certificates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_set_ssl_certificates_builder()` + `compute_region_target_https_proxies_set_ssl_certificates_execute()`.
/// For task-level control, use `compute_region_target_https_proxies_set_ssl_certificates_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_set_ssl_certificates(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_set_ssl_certificates_builder(
        client,
        &args.project,
        &args.region,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_region_target_https_proxies_set_ssl_certificates_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_set_url_map_execute()` to send, or `compute_region_target_https_proxies_set_url_map` for simplest API.

pub fn compute_region_target_https_proxies_set_url_map_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}/setUrlMap",
        project,
        region,
        targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_https_proxies_set_url_map_execute()` or `compute_region_target_https_proxies_set_url_map`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_set_url_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_set_url_map_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_https_proxies_set_url_map_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_https_proxies_set_url_map_task()`.
/// For the simplest API, use `compute_region_target_https_proxies_set_url_map()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_https_proxies_set_url_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_https_proxies_set_url_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_https_proxies_set_url_map_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_https_proxies_set_url_map`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesSetUrlMapArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_set_url_map_builder()` + `compute_region_target_https_proxies_set_url_map_execute()`.
/// For task-level control, use `compute_region_target_https_proxies_set_url_map_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_set_url_map(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetHttpsProxiesSetUrlMapArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_set_url_map_builder(
        client,
        &args.project,
        &args.region,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_region_target_https_proxies_set_url_map_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_tcp_proxies_delete_execute()` to send, or `compute_region_target_tcp_proxies_delete` for simplest API.

pub fn compute_region_target_tcp_proxies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetTcpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetTcpProxies/{}",
        project, region, targetTcpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_tcp_proxies_delete_execute()` or `compute_region_target_tcp_proxies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_tcp_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_tcp_proxies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_tcp_proxies_delete_task()`.
/// For the simplest API, use `compute_region_target_tcp_proxies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_tcp_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_tcp_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_tcp_proxies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_tcp_proxies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetTcpProxiesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetTcpProxy
    pub targetTcpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_tcp_proxies_delete_builder()` + `compute_region_target_tcp_proxies_delete_execute()`.
/// For task-level control, use `compute_region_target_tcp_proxies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetTcpProxiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_tcp_proxies_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.targetTcpProxy,
        &args.requestId,
    )?;
    compute_region_target_tcp_proxies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_tcp_proxies_get_execute()` to send, or `compute_region_target_tcp_proxies_get` for simplest API.

pub fn compute_region_target_tcp_proxies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetTcpProxy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetTcpProxies/{}",
        project, region, targetTcpProxy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_tcp_proxies_get_execute()` or `compute_region_target_tcp_proxies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_tcp_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_tcp_proxies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_tcp_proxies_get_task()`.
/// For the simplest API, use `compute_region_target_tcp_proxies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_tcp_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_tcp_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_target_tcp_proxies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_tcp_proxies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetTcpProxiesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetTcpProxy
    pub targetTcpProxy: String,
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_tcp_proxies_get_builder()` + `compute_region_target_tcp_proxies_get_execute()`.
/// For task-level control, use `compute_region_target_tcp_proxies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetTcpProxiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_tcp_proxies_get_builder(
        client,
        &args.project,
        &args.region,
        &args.targetTcpProxy,
    )?;
    compute_region_target_tcp_proxies_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_tcp_proxies_insert_execute()` to send, or `compute_region_target_tcp_proxies_insert` for simplest API.

pub fn compute_region_target_tcp_proxies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetTcpProxies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_tcp_proxies_insert_execute()` or `compute_region_target_tcp_proxies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_tcp_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_tcp_proxies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_tcp_proxies_insert_task()`.
/// For the simplest API, use `compute_region_target_tcp_proxies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_tcp_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_tcp_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_target_tcp_proxies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_tcp_proxies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetTcpProxiesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_tcp_proxies_insert_builder()` + `compute_region_target_tcp_proxies_insert_execute()`.
/// For task-level control, use `compute_region_target_tcp_proxies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetTcpProxiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_tcp_proxies_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_target_tcp_proxies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Retrieves a list of TargetTcpProxy resources available to the specified project in a given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_tcp_proxies_list_execute()` to send, or `compute_region_target_tcp_proxies_list` for simplest API.

pub fn compute_region_target_tcp_proxies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetTcpProxies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Retrieves a list of TargetTcpProxy resources available to the specified project in a given region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_target_tcp_proxies_list_execute()` or `compute_region_target_tcp_proxies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_tcp_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Retrieves a list of TargetTcpProxy resources available to the specified project in a given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_target_tcp_proxies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_target_tcp_proxies_list_task()`.
/// For the simplest API, use `compute_region_target_tcp_proxies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_target_tcp_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_target_tcp_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_target_tcp_proxies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_target_tcp_proxies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetTcpProxiesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Retrieves a list of TargetTcpProxy resources available to the specified project in a given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_tcp_proxies_list_builder()` + `compute_region_target_tcp_proxies_list_execute()`.
/// For task-level control, use `compute_region_target_tcp_proxies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionTargetTcpProxiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_tcp_proxies_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_target_tcp_proxies_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_delete_execute()` to send, or `compute_region_url_maps_delete` for simplest API.

pub fn compute_region_url_maps_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    urlMap: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}",
        project, region, urlMap,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_url_maps_delete_execute()` or `compute_region_url_maps_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_url_maps_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_url_maps_delete_task()`.
/// For the simplest API, use `compute_region_url_maps_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_url_maps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_url_maps_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_url_maps_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: urlMap
    pub urlMap: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_delete_builder()` + `compute_region_url_maps_delete_execute()`.
/// For task-level control, use `compute_region_url_maps_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_delete(
    client: &SimpleHttpClient,
    args: &ComputeRegionUrlMapsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.urlMap,
        &args.requestId,
    )?;
    compute_region_url_maps_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_get_execute()` to send, or `compute_region_url_maps_get` for simplest API.

pub fn compute_region_url_maps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    urlMap: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}",
        project, region, urlMap,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_url_maps_get_execute()` or `compute_region_url_maps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMap>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_url_maps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_url_maps_get_task()`.
/// For the simplest API, use `compute_region_url_maps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_url_maps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_url_maps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_url_maps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: urlMap
    pub urlMap: String,
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_get_builder()` + `compute_region_url_maps_get_execute()`.
/// For task-level control, use `compute_region_url_maps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionUrlMapsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_url_maps_get_builder(client, &args.project, &args.region, &args.urlMap)?;
    compute_region_url_maps_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_insert_execute()` to send, or `compute_region_url_maps_insert` for simplest API.

pub fn compute_region_url_maps_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_url_maps_insert_execute()` or `compute_region_url_maps_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_url_maps_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_url_maps_insert_task()`.
/// For the simplest API, use `compute_region_url_maps_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_url_maps_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_url_maps_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_url_maps_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_insert_builder()` + `compute_region_url_maps_insert_execute()`.
/// For task-level control, use `compute_region_url_maps_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_insert(
    client: &SimpleHttpClient,
    args: &ComputeRegionUrlMapsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_region_url_maps_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_list_execute()` to send, or `compute_region_url_maps_list` for simplest API.

pub fn compute_region_url_maps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_url_maps_list_execute()` or `compute_region_url_maps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_url_maps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_url_maps_list_task()`.
/// For the simplest API, use `compute_region_url_maps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_url_maps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_url_maps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_url_maps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_list_builder()` + `compute_region_url_maps_list_execute()`.
/// For task-level control, use `compute_region_url_maps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionUrlMapsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_url_maps_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_patch_execute()` to send, or `compute_region_url_maps_patch` for simplest API.

pub fn compute_region_url_maps_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    urlMap: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}",
        project, region, urlMap,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_url_maps_patch_execute()` or `compute_region_url_maps_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_url_maps_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_url_maps_patch_task()`.
/// For the simplest API, use `compute_region_url_maps_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_url_maps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_url_maps_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_url_maps_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: urlMap
    pub urlMap: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_patch_builder()` + `compute_region_url_maps_patch_execute()`.
/// For task-level control, use `compute_region_url_maps_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_patch(
    client: &SimpleHttpClient,
    args: &ComputeRegionUrlMapsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.urlMap,
        &args.requestId,
    )?;
    compute_region_url_maps_patch_execute(builder)
}

/// PUT projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_update_execute()` to send, or `compute_region_url_maps_update` for simplest API.

pub fn compute_region_url_maps_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    urlMap: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}",
        project, region, urlMap,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_url_maps_update_execute()` or `compute_region_url_maps_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_url_maps_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_url_maps_update_task()`.
/// For the simplest API, use `compute_region_url_maps_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_url_maps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_url_maps_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_url_maps_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: urlMap
    pub urlMap: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_update_builder()` + `compute_region_url_maps_update_execute()`.
/// For task-level control, use `compute_region_url_maps_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_update(
    client: &SimpleHttpClient,
    args: &ComputeRegionUrlMapsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_update_builder(
        client,
        &args.project,
        &args.region,
        &args.urlMap,
        &args.requestId,
    )?;
    compute_region_url_maps_update_execute(builder)
}

/// POST projects/{project}/regions/{region}/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_validate_execute()` to send, or `compute_region_url_maps_validate` for simplest API.

pub fn compute_region_url_maps_validate_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    urlMap: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}/validate",
        project, region, urlMap,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_url_maps_validate_execute()` or `compute_region_url_maps_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapsValidateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_url_maps_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_url_maps_validate_task()`.
/// For the simplest API, use `compute_region_url_maps_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_url_maps_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_url_maps_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_region_url_maps_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_url_maps_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsValidateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: urlMap
    pub urlMap: String,
}

/// POST projects/{project}/regions/{region}/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_validate_builder()` + `compute_region_url_maps_validate_execute()`.
/// For task-level control, use `compute_region_url_maps_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_validate(
    client: &SimpleHttpClient,
    args: &ComputeRegionUrlMapsValidateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_validate_builder(
        client,
        &args.project,
        &args.region,
        &args.urlMap,
    )?;
    compute_region_url_maps_validate_execute(builder)
}

/// GET projects/{project}/regions/{region}/zones
/// Retrieves the list of Zone resources under the specific region available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_zones_list_execute()` to send, or `compute_region_zones_list` for simplest API.

pub fn compute_region_zones_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/zones",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/zones
/// Retrieves the list of Zone resources under the specific region available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_region_zones_list_execute()` or `compute_region_zones_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_zones_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_zones_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ZoneList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ZoneList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/zones
/// Retrieves the list of Zone resources under the specific region available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_region_zones_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_region_zones_list_task()`.
/// For the simplest API, use `compute_region_zones_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_region_zones_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_region_zones_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ZoneList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_region_zones_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_region_zones_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionZonesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/zones
/// Retrieves the list of Zone resources under the specific region available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_zones_list_builder()` + `compute_region_zones_list_execute()`.
/// For task-level control, use `compute_region_zones_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_zones_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionZonesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ZoneList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_zones_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_region_zones_list_execute(builder)
}

/// GET projects/{project}/regions/{region}
/// Returns the specified Region resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_regions_get_execute()` to send, or `compute_regions_get` for simplest API.

pub fn compute_regions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}",
        project, region,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}
/// Returns the specified Region resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_regions_get_execute()` or `compute_regions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_regions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_regions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Region>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Region = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}
/// Returns the specified Region resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_regions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_regions_get_task()`.
/// For the simplest API, use `compute_regions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_regions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_regions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Region>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_regions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_regions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
}

/// GET projects/{project}/regions/{region}
/// Returns the specified Region resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_regions_get_builder()` + `compute_regions_get_execute()`.
/// For task-level control, use `compute_regions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_regions_get(
    client: &SimpleHttpClient,
    args: &ComputeRegionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Region>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_regions_get_builder(client, &args.project, &args.region)?;
    compute_regions_get_execute(builder)
}

/// GET projects/{project}/regions
/// Retrieves the list of region resources available to the specified project.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the items.quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_regions_list_execute()` to send, or `compute_regions_list` for simplest API.

pub fn compute_regions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions
/// Retrieves the list of region resources available to the specified project.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the items.quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_regions_list_execute()` or `compute_regions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_regions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_regions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions
/// Retrieves the list of region resources available to the specified project.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the items.quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_regions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_regions_list_task()`.
/// For the simplest API, use `compute_regions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_regions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_regions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_regions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_regions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions
/// Retrieves the list of region resources available to the specified project.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the items.quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_regions_list_builder()` + `compute_regions_list_execute()`.
/// For task-level control, use `compute_regions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_regions_list(
    client: &SimpleHttpClient,
    args: &ComputeRegionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_regions_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_regions_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
/// Retrieves information about the specified reservation block.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_get_execute()` to send, or `compute_reservation_blocks_get` for simplest API.

pub fn compute_reservation_blocks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    reservation: &String,
    reservationBlock: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}",
        project,
        zone,
        reservation,
        reservationBlock,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
/// Retrieves information about the specified reservation block.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_blocks_get_execute()` or `compute_reservation_blocks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationBlocksGetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationBlocksGetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
/// Retrieves information about the specified reservation block.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_blocks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_blocks_get_task()`.
/// For the simplest API, use `compute_reservation_blocks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_blocks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationBlocksGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservation_blocks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_blocks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationBlocksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: reservation
    pub reservation: String,
    /// Path parameter: reservationBlock
    pub reservationBlock: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
/// Retrieves information about the specified reservation block.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_get_builder()` + `compute_reservation_blocks_get_execute()`.
/// For task-level control, use `compute_reservation_blocks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_get(
    client: &SimpleHttpClient,
    args: &ComputeReservationBlocksGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationBlocksGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.reservation,
        &args.reservationBlock,
        &args.view,
    )?;
    compute_reservation_blocks_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_get_iam_policy_execute()` to send, or `compute_reservation_blocks_get_iam_policy` for simplest API.

pub fn compute_reservation_blocks_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentResource: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/getIamPolicy",
        project,
        zone,
        parentResource,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_blocks_get_iam_policy_execute()` or `compute_reservation_blocks_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_blocks_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_blocks_get_iam_policy_task()`.
/// For the simplest API, use `compute_reservation_blocks_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_blocks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_blocks_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_blocks_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationBlocksGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentResource
    pub parentResource: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_get_iam_policy_builder()` + `compute_reservation_blocks_get_iam_policy_execute()`.
/// For task-level control, use `compute_reservation_blocks_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeReservationBlocksGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentResource,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_reservation_blocks_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
/// Retrieves a list of reservation blocks under a single reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_list_execute()` to send, or `compute_reservation_blocks_list` for simplest API.

pub fn compute_reservation_blocks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    reservation: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks",
        project,
        zone,
        reservation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
/// Retrieves a list of reservation blocks under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_blocks_list_execute()` or `compute_reservation_blocks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationBlocksListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationBlocksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
/// Retrieves a list of reservation blocks under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_blocks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_blocks_list_task()`.
/// For the simplest API, use `compute_reservation_blocks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_blocks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationBlocksListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservation_blocks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_blocks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationBlocksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: reservation
    pub reservation: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
/// Retrieves a list of reservation blocks under a single reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_list_builder()` + `compute_reservation_blocks_list_execute()`.
/// For task-level control, use `compute_reservation_blocks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_list(
    client: &SimpleHttpClient,
    args: &ComputeReservationBlocksListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationBlocksListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.reservation,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_reservation_blocks_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation block
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_perform_maintenance_execute()` to send, or `compute_reservation_blocks_perform_maintenance` for simplest API.

pub fn compute_reservation_blocks_perform_maintenance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    reservation: &String,
    reservationBlock: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/performMaintenance",
        project,
        zone,
        reservation,
        reservationBlock,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation block
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_blocks_perform_maintenance_execute()` or `compute_reservation_blocks_perform_maintenance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_perform_maintenance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation block
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_blocks_perform_maintenance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_blocks_perform_maintenance_task()`.
/// For the simplest API, use `compute_reservation_blocks_perform_maintenance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_blocks_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_blocks_perform_maintenance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_blocks_perform_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationBlocksPerformMaintenanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: reservation
    pub reservation: String,
    /// Path parameter: reservationBlock
    pub reservationBlock: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation block
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_perform_maintenance_builder()` + `compute_reservation_blocks_perform_maintenance_execute()`.
/// For task-level control, use `compute_reservation_blocks_perform_maintenance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_perform_maintenance(
    client: &SimpleHttpClient,
    args: &ComputeReservationBlocksPerformMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_perform_maintenance_builder(
        client,
        &args.project,
        &args.zone,
        &args.reservation,
        &args.reservationBlock,
        &args.requestId,
    )?;
    compute_reservation_blocks_perform_maintenance_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_set_iam_policy_execute()` to send, or `compute_reservation_blocks_set_iam_policy` for simplest API.

pub fn compute_reservation_blocks_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentResource: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/setIamPolicy",
        project,
        zone,
        parentResource,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_blocks_set_iam_policy_execute()` or `compute_reservation_blocks_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_blocks_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_blocks_set_iam_policy_task()`.
/// For the simplest API, use `compute_reservation_blocks_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_blocks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_blocks_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_blocks_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationBlocksSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentResource
    pub parentResource: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_set_iam_policy_builder()` + `compute_reservation_blocks_set_iam_policy_execute()`.
/// For task-level control, use `compute_reservation_blocks_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeReservationBlocksSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_set_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentResource,
        &args.resource,
    )?;
    compute_reservation_blocks_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_test_iam_permissions_execute()` to send, or `compute_reservation_blocks_test_iam_permissions` for simplest API.

pub fn compute_reservation_blocks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentResource: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/testIamPermissions",
        project,
        zone,
        parentResource,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_blocks_test_iam_permissions_execute()` or `compute_reservation_blocks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_blocks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_blocks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_reservation_blocks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_blocks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_blocks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservation_blocks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_blocks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationBlocksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentResource
    pub parentResource: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_test_iam_permissions_builder()` + `compute_reservation_blocks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_reservation_blocks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeReservationBlocksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentResource,
        &args.resource,
    )?;
    compute_reservation_blocks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}
/// Retrieves information about the specified reservation slot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_slots_get_execute()` to send, or `compute_reservation_slots_get` for simplest API.

pub fn compute_reservation_slots_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    reservationSlot: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}",
        project,
        zone,
        parentName,
        reservationSlot,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}
/// Retrieves information about the specified reservation slot.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_slots_get_execute()` or `compute_reservation_slots_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_slots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationSlotsGetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationSlotsGetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}
/// Retrieves information about the specified reservation slot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_slots_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_slots_get_task()`.
/// For the simplest API, use `compute_reservation_slots_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_slots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_slots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSlotsGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservation_slots_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_slots_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSlotsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Path parameter: reservationSlot
    pub reservationSlot: String,
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}
/// Retrieves information about the specified reservation slot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_slots_get_builder()` + `compute_reservation_slots_get_execute()`.
/// For task-level control, use `compute_reservation_slots_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_get(
    client: &SimpleHttpClient,
    args: &ComputeReservationSlotsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSlotsGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_slots_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.reservationSlot,
    )?;
    compute_reservation_slots_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}/getVersion
/// Allows customers to get SBOM versions of a reservation slot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_slots_get_version_execute()` to send, or `compute_reservation_slots_get_version` for simplest API.

pub fn compute_reservation_slots_get_version_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    reservationSlot: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}/getVersion",
        project,
        zone,
        parentName,
        reservationSlot,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}/getVersion
/// Allows customers to get SBOM versions of a reservation slot.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_slots_get_version_execute()` or `compute_reservation_slots_get_version`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_slots_get_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_get_version_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}/getVersion
/// Allows customers to get SBOM versions of a reservation slot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_slots_get_version_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_slots_get_version_task()`.
/// For the simplest API, use `compute_reservation_slots_get_version()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_slots_get_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_slots_get_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_slots_get_version_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_slots_get_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSlotsGetVersionArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Path parameter: reservationSlot
    pub reservationSlot: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}/getVersion
/// Allows customers to get SBOM versions of a reservation slot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_slots_get_version_builder()` + `compute_reservation_slots_get_version_execute()`.
/// For task-level control, use `compute_reservation_slots_get_version_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_get_version(
    client: &SimpleHttpClient,
    args: &ComputeReservationSlotsGetVersionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_slots_get_version_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.reservationSlot,
        &args.requestId,
    )?;
    compute_reservation_slots_get_version_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots
/// Retrieves a list of reservation slots under a single reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_slots_list_execute()` to send, or `compute_reservation_slots_list` for simplest API.

pub fn compute_reservation_slots_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots",
        project,
        zone,
        parentName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots
/// Retrieves a list of reservation slots under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_slots_list_execute()` or `compute_reservation_slots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_slots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationSlotsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationSlotsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots
/// Retrieves a list of reservation slots under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_slots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_slots_list_task()`.
/// For the simplest API, use `compute_reservation_slots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_slots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_slots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSlotsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservation_slots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_slots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSlotsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots
/// Retrieves a list of reservation slots under a single reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_slots_list_builder()` + `compute_reservation_slots_list_execute()`.
/// For task-level control, use `compute_reservation_slots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_list(
    client: &SimpleHttpClient,
    args: &ComputeReservationSlotsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSlotsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_slots_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_reservation_slots_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}
/// Update a reservation slot in the specified sub-block.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_slots_update_execute()` to send, or `compute_reservation_slots_update` for simplest API.

pub fn compute_reservation_slots_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    reservationSlot: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}",
        project,
        zone,
        parentName,
        reservationSlot,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}
/// Update a reservation slot in the specified sub-block.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_slots_update_execute()` or `compute_reservation_slots_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_slots_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}
/// Update a reservation slot in the specified sub-block.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_slots_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_slots_update_task()`.
/// For the simplest API, use `compute_reservation_slots_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_slots_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_slots_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_slots_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_slots_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSlotsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Path parameter: reservationSlot
    pub reservationSlot: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlocksId}/reservationSlots/{reservationSlot}
/// Update a reservation slot in the specified sub-block.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_slots_update_builder()` + `compute_reservation_slots_update_execute()`.
/// For task-level control, use `compute_reservation_slots_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_update(
    client: &SimpleHttpClient,
    args: &ComputeReservationSlotsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_slots_update_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.reservationSlot,
        &args.updateMask,
    )?;
    compute_reservation_slots_update_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}
/// Retrieves information about the specified reservation `subBlock`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_get_execute()` to send, or `compute_reservation_sub_blocks_get` for simplest API.

pub fn compute_reservation_sub_blocks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    reservationSubBlock: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{reservationSubBlock}",
        project,
        zone,
        parentName,
        reservationSubBlock,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}
/// Retrieves information about the specified reservation `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_sub_blocks_get_execute()` or `compute_reservation_sub_blocks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationSubBlocksGetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationSubBlocksGetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}
/// Retrieves information about the specified reservation `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_sub_blocks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_sub_blocks_get_task()`.
/// For the simplest API, use `compute_reservation_sub_blocks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_sub_blocks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSubBlocksGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservation_sub_blocks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_sub_blocks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Path parameter: reservationSubBlock
    pub reservationSubBlock: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}
/// Retrieves information about the specified reservation `subBlock`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_get_builder()` + `compute_reservation_sub_blocks_get_execute()`.
/// For task-level control, use `compute_reservation_sub_blocks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get(
    client: &SimpleHttpClient,
    args: &ComputeReservationSubBlocksGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSubBlocksGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.reservationSubBlock,
        &args.view,
    )?;
    compute_reservation_sub_blocks_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_get_iam_policy_execute()` to send, or `compute_reservation_sub_blocks_get_iam_policy` for simplest API.

pub fn compute_reservation_sub_blocks_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentResource: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{resource}/getIamPolicy",
        project,
        zone,
        parentResource,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_sub_blocks_get_iam_policy_execute()` or `compute_reservation_sub_blocks_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_sub_blocks_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_sub_blocks_get_iam_policy_task()`.
/// For the simplest API, use `compute_reservation_sub_blocks_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_sub_blocks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_sub_blocks_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_sub_blocks_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentResource
    pub parentResource: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_get_iam_policy_builder()` + `compute_reservation_sub_blocks_get_iam_policy_execute()`.
/// For task-level control, use `compute_reservation_sub_blocks_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeReservationSubBlocksGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentResource,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_reservation_sub_blocks_get_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/getVersion
/// Allows customers to get SBOM versions of a reservation `subBlock`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_get_version_execute()` to send, or `compute_reservation_sub_blocks_get_version` for simplest API.

pub fn compute_reservation_sub_blocks_get_version_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    reservationSubBlock: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{reservationSubBlock}/getVersion",
        project,
        zone,
        parentName,
        reservationSubBlock,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/getVersion
/// Allows customers to get SBOM versions of a reservation `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_sub_blocks_get_version_execute()` or `compute_reservation_sub_blocks_get_version`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_get_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_version_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/getVersion
/// Allows customers to get SBOM versions of a reservation `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_sub_blocks_get_version_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_sub_blocks_get_version_task()`.
/// For the simplest API, use `compute_reservation_sub_blocks_get_version()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_get_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_sub_blocks_get_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_sub_blocks_get_version_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_sub_blocks_get_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksGetVersionArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Path parameter: reservationSubBlock
    pub reservationSubBlock: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/getVersion
/// Allows customers to get SBOM versions of a reservation `subBlock`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_get_version_builder()` + `compute_reservation_sub_blocks_get_version_execute()`.
/// For task-level control, use `compute_reservation_sub_blocks_get_version_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_version(
    client: &SimpleHttpClient,
    args: &ComputeReservationSubBlocksGetVersionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_get_version_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.reservationSubBlock,
        &args.requestId,
    )?;
    compute_reservation_sub_blocks_get_version_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks
/// Retrieves a list of reservation `subBlocks` under a single reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_list_execute()` to send, or `compute_reservation_sub_blocks_list` for simplest API.

pub fn compute_reservation_sub_blocks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks",
        project,
        zone,
        parentName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks
/// Retrieves a list of reservation `subBlocks` under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_sub_blocks_list_execute()` or `compute_reservation_sub_blocks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationSubBlocksListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationSubBlocksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks
/// Retrieves a list of reservation `subBlocks` under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_sub_blocks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_sub_blocks_list_task()`.
/// For the simplest API, use `compute_reservation_sub_blocks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_sub_blocks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSubBlocksListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservation_sub_blocks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_sub_blocks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks
/// Retrieves a list of reservation `subBlocks` under a single reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_list_builder()` + `compute_reservation_sub_blocks_list_execute()`.
/// For task-level control, use `compute_reservation_sub_blocks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_list(
    client: &SimpleHttpClient,
    args: &ComputeReservationSubBlocksListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSubBlocksListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_reservation_sub_blocks_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation `subBlock`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_perform_maintenance_execute()` to send, or `compute_reservation_sub_blocks_perform_maintenance` for simplest API.

pub fn compute_reservation_sub_blocks_perform_maintenance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    reservationSubBlock: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{reservationSubBlock}/performMaintenance",
        project,
        zone,
        parentName,
        reservationSubBlock,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation `subBlock`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_sub_blocks_perform_maintenance_execute()` or `compute_reservation_sub_blocks_perform_maintenance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_perform_maintenance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation `subBlock`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_sub_blocks_perform_maintenance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_sub_blocks_perform_maintenance_task()`.
/// For the simplest API, use `compute_reservation_sub_blocks_perform_maintenance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_sub_blocks_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_sub_blocks_perform_maintenance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_sub_blocks_perform_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksPerformMaintenanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Path parameter: reservationSubBlock
    pub reservationSubBlock: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation `subBlock`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_perform_maintenance_builder()` + `compute_reservation_sub_blocks_perform_maintenance_execute()`.
/// For task-level control, use `compute_reservation_sub_blocks_perform_maintenance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_perform_maintenance(
    client: &SimpleHttpClient,
    args: &ComputeReservationSubBlocksPerformMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_perform_maintenance_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.reservationSubBlock,
        &args.requestId,
    )?;
    compute_reservation_sub_blocks_perform_maintenance_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/reportFaulty
/// Allows customers to report a faulty `subBlock`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_report_faulty_execute()` to send, or `compute_reservation_sub_blocks_report_faulty` for simplest API.

pub fn compute_reservation_sub_blocks_report_faulty_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentName: &String,
    reservationSubBlock: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{reservationSubBlock}/reportFaulty",
        project,
        zone,
        parentName,
        reservationSubBlock,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/reportFaulty
/// Allows customers to report a faulty `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_sub_blocks_report_faulty_execute()` or `compute_reservation_sub_blocks_report_faulty`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_report_faulty_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_report_faulty_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/reportFaulty
/// Allows customers to report a faulty `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_sub_blocks_report_faulty_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_sub_blocks_report_faulty_task()`.
/// For the simplest API, use `compute_reservation_sub_blocks_report_faulty()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_report_faulty_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_sub_blocks_report_faulty_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_sub_blocks_report_faulty_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_sub_blocks_report_faulty`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksReportFaultyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentName
    pub parentName: String,
    /// Path parameter: reservationSubBlock
    pub reservationSubBlock: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{reservationSubBlock}/reportFaulty
/// Allows customers to report a faulty `subBlock`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_report_faulty_builder()` + `compute_reservation_sub_blocks_report_faulty_execute()`.
/// For task-level control, use `compute_reservation_sub_blocks_report_faulty_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_report_faulty(
    client: &SimpleHttpClient,
    args: &ComputeReservationSubBlocksReportFaultyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_report_faulty_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentName,
        &args.reservationSubBlock,
        &args.requestId,
    )?;
    compute_reservation_sub_blocks_report_faulty_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_set_iam_policy_execute()` to send, or `compute_reservation_sub_blocks_set_iam_policy` for simplest API.

pub fn compute_reservation_sub_blocks_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentResource: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{resource}/setIamPolicy",
        project,
        zone,
        parentResource,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_sub_blocks_set_iam_policy_execute()` or `compute_reservation_sub_blocks_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_sub_blocks_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_sub_blocks_set_iam_policy_task()`.
/// For the simplest API, use `compute_reservation_sub_blocks_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_sub_blocks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservation_sub_blocks_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_sub_blocks_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentResource
    pub parentResource: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_set_iam_policy_builder()` + `compute_reservation_sub_blocks_set_iam_policy_execute()`.
/// For task-level control, use `compute_reservation_sub_blocks_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeReservationSubBlocksSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_set_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentResource,
        &args.resource,
    )?;
    compute_reservation_sub_blocks_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_test_iam_permissions_execute()` to send, or `compute_reservation_sub_blocks_test_iam_permissions` for simplest API.

pub fn compute_reservation_sub_blocks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    parentResource: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/reservationSubBlocks/{resource}/testIamPermissions",
        project,
        zone,
        parentResource,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservation_sub_blocks_test_iam_permissions_execute()` or `compute_reservation_sub_blocks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservation_sub_blocks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservation_sub_blocks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_reservation_sub_blocks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservation_sub_blocks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservation_sub_blocks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservation_sub_blocks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservation_sub_blocks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: parentResource
    pub parentResource: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservationsId}/reservationBlocks/{reservationBlocksId}/reservationSubBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_test_iam_permissions_builder()` + `compute_reservation_sub_blocks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_reservation_sub_blocks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeReservationSubBlocksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.parentResource,
        &args.resource,
    )?;
    compute_reservation_sub_blocks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/reservations
/// Retrieves an aggregated list of reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_aggregated_list_execute()` to send, or `compute_reservations_aggregated_list` for simplest API.

pub fn compute_reservations_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/reservations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/reservations
/// Retrieves an aggregated list of reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_aggregated_list_execute()` or `compute_reservations_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/reservations
/// Retrieves an aggregated list of reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_aggregated_list_task()`.
/// For the simplest API, use `compute_reservations_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservations_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/reservations
/// Retrieves an aggregated list of reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_aggregated_list_builder()` + `compute_reservations_aggregated_list_execute()`.
/// For task-level control, use `compute_reservations_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeReservationsAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservations_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_reservations_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/reservations/{reservation}
/// Deletes the specified reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_delete_execute()` to send, or `compute_reservations_delete` for simplest API.

pub fn compute_reservations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    reservation: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}",
        project, zone, reservation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/reservations/{reservation}
/// Deletes the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_delete_execute()` or `compute_reservations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/reservations/{reservation}
/// Deletes the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_delete_task()`.
/// For the simplest API, use `compute_reservations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: reservation
    pub reservation: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/reservations/{reservation}
/// Deletes the specified reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_delete_builder()` + `compute_reservations_delete_execute()`.
/// For task-level control, use `compute_reservations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_delete(
    client: &SimpleHttpClient,
    args: &ComputeReservationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.reservation,
        &args.requestId,
    )?;
    compute_reservations_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Retrieves information about the specified reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_get_execute()` to send, or `compute_reservations_get` for simplest API.

pub fn compute_reservations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    reservation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}",
        project, zone, reservation,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Retrieves information about the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_get_execute()` or `compute_reservations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Retrieves information about the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_get_task()`.
/// For the simplest API, use `compute_reservations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: reservation
    pub reservation: String,
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Retrieves information about the specified reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_get_builder()` + `compute_reservations_get_execute()`.
/// For task-level control, use `compute_reservations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_get(
    client: &SimpleHttpClient,
    args: &ComputeReservationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_reservations_get_builder(client, &args.project, &args.zone, &args.reservation)?;
    compute_reservations_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_get_iam_policy_execute()` to send, or `compute_reservations_get_iam_policy` for simplest API.

pub fn compute_reservations_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/getIamPolicy",
        project,
        zone,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_get_iam_policy_execute()` or `compute_reservations_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_get_iam_policy_task()`.
/// For the simplest API, use `compute_reservations_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservations_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_get_iam_policy_builder()` + `compute_reservations_get_iam_policy_execute()`.
/// For task-level control, use `compute_reservations_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeReservationsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_reservations_get_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations
/// Creates a new reservation. For more information, `readReserving` zonal resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_insert_execute()` to send, or `compute_reservations_insert` for simplest API.

pub fn compute_reservations_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations
/// Creates a new reservation. For more information, `readReserving` zonal resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_insert_execute()` or `compute_reservations_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations
/// Creates a new reservation. For more information, `readReserving` zonal resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_insert_task()`.
/// For the simplest API, use `compute_reservations_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservations_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations
/// Creates a new reservation. For more information, `readReserving` zonal resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_insert_builder()` + `compute_reservations_insert_execute()`.
/// For task-level control, use `compute_reservations_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_insert(
    client: &SimpleHttpClient,
    args: &ComputeReservationsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_reservations_insert_builder(client, &args.project, &args.zone, &args.requestId)?;
    compute_reservations_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations
/// A list of all the reservations that have been configured for the specified project in specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_list_execute()` to send, or `compute_reservations_list` for simplest API.

pub fn compute_reservations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/reservations
/// A list of all the reservations that have been configured for the specified project in specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_list_execute()` or `compute_reservations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/reservations
/// A list of all the reservations that have been configured for the specified project in specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_list_task()`.
/// For the simplest API, use `compute_reservations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/reservations
/// A list of all the reservations that have been configured for the specified project in specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_list_builder()` + `compute_reservations_list_execute()`.
/// For task-level control, use `compute_reservations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_list(
    client: &SimpleHttpClient,
    args: &ComputeReservationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservations_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_reservations_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance
/// Perform maintenance on an extended reservation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_perform_maintenance_execute()` to send, or `compute_reservations_perform_maintenance` for simplest API.

pub fn compute_reservations_perform_maintenance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    reservation: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/performMaintenance",
        project,
        zone,
        reservation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance
/// Perform maintenance on an extended reservation
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_perform_maintenance_execute()` or `compute_reservations_perform_maintenance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_perform_maintenance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance
/// Perform maintenance on an extended reservation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_perform_maintenance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_perform_maintenance_task()`.
/// For the simplest API, use `compute_reservations_perform_maintenance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_perform_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservations_perform_maintenance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_perform_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsPerformMaintenanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: reservation
    pub reservation: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance
/// Perform maintenance on an extended reservation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_perform_maintenance_builder()` + `compute_reservations_perform_maintenance_execute()`.
/// For task-level control, use `compute_reservations_perform_maintenance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_perform_maintenance(
    client: &SimpleHttpClient,
    args: &ComputeReservationsPerformMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_perform_maintenance_builder(
        client,
        &args.project,
        &args.zone,
        &args.reservation,
        &args.requestId,
    )?;
    compute_reservations_perform_maintenance_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/resize
/// Resizes the reservation (applicable to standalone reservations only). For more information, `readModifying` reservations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_resize_execute()` to send, or `compute_reservations_resize` for simplest API.

pub fn compute_reservations_resize_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    reservation: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/resize",
        project, zone, reservation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/resize
/// Resizes the reservation (applicable to standalone reservations only). For more information, `readModifying` reservations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_resize_execute()` or `compute_reservations_resize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_resize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/resize
/// Resizes the reservation (applicable to standalone reservations only). For more information, `readModifying` reservations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_resize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_resize_task()`.
/// For the simplest API, use `compute_reservations_resize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservations_resize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_resize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsResizeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: reservation
    pub reservation: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/resize
/// Resizes the reservation (applicable to standalone reservations only). For more information, `readModifying` reservations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_resize_builder()` + `compute_reservations_resize_execute()`.
/// For task-level control, use `compute_reservations_resize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_resize(
    client: &SimpleHttpClient,
    args: &ComputeReservationsResizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_resize_builder(
        client,
        &args.project,
        &args.zone,
        &args.reservation,
        &args.requestId,
    )?;
    compute_reservations_resize_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_set_iam_policy_execute()` to send, or `compute_reservations_set_iam_policy` for simplest API.

pub fn compute_reservations_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/setIamPolicy",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_set_iam_policy_execute()` or `compute_reservations_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_set_iam_policy_task()`.
/// For the simplest API, use `compute_reservations_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservations_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_set_iam_policy_builder()` + `compute_reservations_set_iam_policy_execute()`.
/// For task-level control, use `compute_reservations_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeReservationsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_set_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_reservations_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_test_iam_permissions_execute()` to send, or `compute_reservations_test_iam_permissions` for simplest API.

pub fn compute_reservations_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_test_iam_permissions_execute()` or `compute_reservations_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_test_iam_permissions_task()`.
/// For the simplest API, use `compute_reservations_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_reservations_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_test_iam_permissions_builder()` + `compute_reservations_test_iam_permissions_execute()`.
/// For task-level control, use `compute_reservations_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeReservationsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservations_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_reservations_test_iam_permissions_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/reservations/{reservation}
/// Update share settings of the reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_update_execute()` to send, or `compute_reservations_update` for simplest API.

pub fn compute_reservations_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    reservation: &String,
    paths: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}",
        project, zone, reservation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = paths.as_ref() {
        query_parts.push(format!("paths={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/reservations/{reservation}
/// Update share settings of the reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_reservations_update_execute()` or `compute_reservations_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/reservations/{reservation}
/// Update share settings of the reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_reservations_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_reservations_update_task()`.
/// For the simplest API, use `compute_reservations_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_reservations_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_reservations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_reservations_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_reservations_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: reservation
    pub reservation: String,
    /// Query parameter: paths
    pub paths: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/reservations/{reservation}
/// Update share settings of the reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_update_builder()` + `compute_reservations_update_execute()`.
/// For task-level control, use `compute_reservations_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_update(
    client: &SimpleHttpClient,
    args: &ComputeReservationsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_update_builder(
        client,
        &args.project,
        &args.zone,
        &args.reservation,
        &args.paths,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_reservations_update_execute(builder)
}

/// GET projects/{project}/aggregated/resourcePolicies
/// Retrieves an aggregated list of resource policies.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_aggregated_list_execute()` to send, or `compute_resource_policies_aggregated_list` for simplest API.

pub fn compute_resource_policies_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/resourcePolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/resourcePolicies
/// Retrieves an aggregated list of resource policies.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_aggregated_list_execute()` or `compute_resource_policies_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResourcePolicyAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourcePolicyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/resourcePolicies
/// Retrieves an aggregated list of resource policies.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_aggregated_list_task()`.
/// For the simplest API, use `compute_resource_policies_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResourcePolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_resource_policies_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/resourcePolicies
/// Retrieves an aggregated list of resource policies.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_aggregated_list_builder()` + `compute_resource_policies_aggregated_list_execute()`.
/// For task-level control, use `compute_resource_policies_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResourcePolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_resource_policies_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Deletes the specified resource policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_delete_execute()` to send, or `compute_resource_policies_delete` for simplest API.

pub fn compute_resource_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resourcePolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}",
        project, region, resourcePolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Deletes the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_delete_execute()` or `compute_resource_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Deletes the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_delete_task()`.
/// For the simplest API, use `compute_resource_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_resource_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resourcePolicy
    pub resourcePolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Deletes the specified resource policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_delete_builder()` + `compute_resource_policies_delete_execute()`.
/// For task-level control, use `compute_resource_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.resourcePolicy,
        &args.requestId,
    )?;
    compute_resource_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Retrieves all information of the specified resource policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_get_execute()` to send, or `compute_resource_policies_get` for simplest API.

pub fn compute_resource_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resourcePolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}",
        project, region, resourcePolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Retrieves all information of the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_get_execute()` or `compute_resource_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResourcePolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourcePolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Retrieves all information of the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_get_task()`.
/// For the simplest API, use `compute_resource_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcePolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_resource_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resourcePolicy
    pub resourcePolicy: String,
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Retrieves all information of the specified resource policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_get_builder()` + `compute_resource_policies_get_execute()`.
/// For task-level control, use `compute_resource_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcePolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_get_builder(
        client,
        &args.project,
        &args.region,
        &args.resourcePolicy,
    )?;
    compute_resource_policies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_get_iam_policy_execute()` to send, or `compute_resource_policies_get_iam_policy` for simplest API.

pub fn compute_resource_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_get_iam_policy_execute()` or `compute_resource_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_get_iam_policy_task()`.
/// For the simplest API, use `compute_resource_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_resource_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_get_iam_policy_builder()` + `compute_resource_policies_get_iam_policy_execute()`.
/// For task-level control, use `compute_resource_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_resource_policies_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/resourcePolicies
/// Creates a new resource policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_insert_execute()` to send, or `compute_resource_policies_insert` for simplest API.

pub fn compute_resource_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/resourcePolicies
/// Creates a new resource policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_insert_execute()` or `compute_resource_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/resourcePolicies
/// Creates a new resource policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_insert_task()`.
/// For the simplest API, use `compute_resource_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_resource_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/resourcePolicies
/// Creates a new resource policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_insert_builder()` + `compute_resource_policies_insert_execute()`.
/// For task-level control, use `compute_resource_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_resource_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// A list all the resource policies that have been configured for the specified project in specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_list_execute()` to send, or `compute_resource_policies_list` for simplest API.

pub fn compute_resource_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// A list all the resource policies that have been configured for the specified project in specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_list_execute()` or `compute_resource_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResourcePolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourcePolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// A list all the resource policies that have been configured for the specified project in specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_list_task()`.
/// For the simplest API, use `compute_resource_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcePolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_resource_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// A list all the resource policies that have been configured for the specified project in specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_list_builder()` + `compute_resource_policies_list_execute()`.
/// For task-level control, use `compute_resource_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcePolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_resource_policies_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Modify the specified resource policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_patch_execute()` to send, or `compute_resource_policies_patch` for simplest API.

pub fn compute_resource_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resourcePolicy: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}",
        project, region, resourcePolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Modify the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_patch_execute()` or `compute_resource_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Modify the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_patch_task()`.
/// For the simplest API, use `compute_resource_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_resource_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resourcePolicy
    pub resourcePolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Modify the specified resource policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_patch_builder()` + `compute_resource_policies_patch_execute()`.
/// For task-level control, use `compute_resource_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.resourcePolicy,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_resource_policies_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_set_iam_policy_execute()` to send, or `compute_resource_policies_set_iam_policy` for simplest API.

pub fn compute_resource_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_set_iam_policy_execute()` or `compute_resource_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_set_iam_policy_task()`.
/// For the simplest API, use `compute_resource_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_resource_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_set_iam_policy_builder()` + `compute_resource_policies_set_iam_policy_execute()`.
/// For task-level control, use `compute_resource_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_resource_policies_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_test_iam_permissions_execute()` to send, or `compute_resource_policies_test_iam_permissions` for simplest API.

pub fn compute_resource_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_resource_policies_test_iam_permissions_execute()` or `compute_resource_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_resource_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_resource_policies_test_iam_permissions_task()`.
/// For the simplest API, use `compute_resource_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_resource_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_resource_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_resource_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_resource_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_test_iam_permissions_builder()` + `compute_resource_policies_test_iam_permissions_execute()`.
/// For task-level control, use `compute_resource_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeResourcePoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_resource_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/routers
/// Retrieves an aggregated list of routers.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_aggregated_list_execute()` to send, or `compute_routers_aggregated_list` for simplest API.

pub fn compute_routers_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/routers",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/routers
/// Retrieves an aggregated list of routers.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_aggregated_list_execute()` or `compute_routers_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouterAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouterAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/routers
/// Retrieves an aggregated list of routers.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_aggregated_list_task()`.
/// For the simplest API, use `compute_routers_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_routers_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/routers
/// Retrieves an aggregated list of routers.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_aggregated_list_builder()` + `compute_routers_aggregated_list_execute()`.
/// For task-level control, use `compute_routers_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeRoutersAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_routers_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/routers/{router}
/// Deletes the specified Router resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_delete_execute()` to send, or `compute_routers_delete` for simplest API.

pub fn compute_routers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}",
        project, region, router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/routers/{router}
/// Deletes the specified Router resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_delete_execute()` or `compute_routers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/routers/{router}
/// Deletes the specified Router resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_delete_task()`.
/// For the simplest API, use `compute_routers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/routers/{router}
/// Deletes the specified Router resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_delete_builder()` + `compute_routers_delete_execute()`.
/// For task-level control, use `compute_routers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_delete(
    client: &SimpleHttpClient,
    args: &ComputeRoutersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.requestId,
    )?;
    compute_routers_delete_execute(builder)
}

/// POST projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy
/// Deletes Route Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_delete_route_policy_execute()` to send, or `compute_routers_delete_route_policy` for simplest API.

pub fn compute_routers_delete_route_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    policy: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/deleteRoutePolicy",
        project,
        region,
        router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = policy.as_ref() {
        query_parts.push(format!("policy={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy
/// Deletes Route Policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_delete_route_policy_execute()` or `compute_routers_delete_route_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_delete_route_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_delete_route_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy
/// Deletes Route Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_delete_route_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_delete_route_policy_task()`.
/// For the simplest API, use `compute_routers_delete_route_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_delete_route_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_delete_route_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_delete_route_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_delete_route_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersDeleteRoutePolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: policy
    pub policy: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy
/// Deletes Route Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_delete_route_policy_builder()` + `compute_routers_delete_route_policy_execute()`.
/// For task-level control, use `compute_routers_delete_route_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_delete_route_policy(
    client: &SimpleHttpClient,
    args: &ComputeRoutersDeleteRoutePolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_delete_route_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.policy,
        &args.requestId,
    )?;
    compute_routers_delete_route_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Returns the specified Router resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_execute()` to send, or `compute_routers_get` for simplest API.

pub fn compute_routers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}",
        project, region, router,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Returns the specified Router resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_get_execute()` or `compute_routers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Router>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Router = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Returns the specified Router resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_get_task()`.
/// For the simplest API, use `compute_routers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Router>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Returns the specified Router resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_builder()` + `compute_routers_get_execute()`.
/// For task-level control, use `compute_routers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get(
    client: &SimpleHttpClient,
    args: &ComputeRoutersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Router>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_get_builder(client, &args.project, &args.region, &args.router)?;
    compute_routers_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo
/// Retrieves runtime NAT IP information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_nat_ip_info_execute()` to send, or `compute_routers_get_nat_ip_info` for simplest API.

pub fn compute_routers_get_nat_ip_info_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    natName: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/getNatIpInfo",
        project, region, router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = natName.as_ref() {
        query_parts.push(format!("natName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo
/// Retrieves runtime NAT IP information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_get_nat_ip_info_execute()` or `compute_routers_get_nat_ip_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_nat_ip_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_nat_ip_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NatIpInfoResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NatIpInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo
/// Retrieves runtime NAT IP information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_get_nat_ip_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_get_nat_ip_info_task()`.
/// For the simplest API, use `compute_routers_get_nat_ip_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_nat_ip_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_get_nat_ip_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NatIpInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_routers_get_nat_ip_info_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_get_nat_ip_info`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersGetNatIpInfoArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: natName
    pub natName: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo
/// Retrieves runtime NAT IP information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_nat_ip_info_builder()` + `compute_routers_get_nat_ip_info_execute()`.
/// For task-level control, use `compute_routers_get_nat_ip_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_nat_ip_info(
    client: &SimpleHttpClient,
    args: &ComputeRoutersGetNatIpInfoArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NatIpInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_get_nat_ip_info_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.natName,
    )?;
    compute_routers_get_nat_ip_info_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo
/// Retrieves runtime Nat mapping information of VM endpoints.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_nat_mapping_info_execute()` to send, or `compute_routers_get_nat_mapping_info` for simplest API.

pub fn compute_routers_get_nat_mapping_info_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    natName: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/getNatMappingInfo",
        project,
        region,
        router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = natName.as_ref() {
        query_parts.push(format!("natName={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo
/// Retrieves runtime Nat mapping information of VM endpoints.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_get_nat_mapping_info_execute()` or `compute_routers_get_nat_mapping_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_nat_mapping_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_nat_mapping_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmEndpointNatMappingsList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmEndpointNatMappingsList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo
/// Retrieves runtime Nat mapping information of VM endpoints.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_get_nat_mapping_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_get_nat_mapping_info_task()`.
/// For the simplest API, use `compute_routers_get_nat_mapping_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_nat_mapping_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_get_nat_mapping_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmEndpointNatMappingsList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_routers_get_nat_mapping_info_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_get_nat_mapping_info`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersGetNatMappingInfoArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: natName
    pub natName: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo
/// Retrieves runtime Nat mapping information of VM endpoints.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_nat_mapping_info_builder()` + `compute_routers_get_nat_mapping_info_execute()`.
/// For task-level control, use `compute_routers_get_nat_mapping_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_nat_mapping_info(
    client: &SimpleHttpClient,
    args: &ComputeRoutersGetNatMappingInfoArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmEndpointNatMappingsList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_get_nat_mapping_info_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.filter,
        &args.maxResults,
        &args.natName,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_routers_get_nat_mapping_info_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy
/// Returns specified Route Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_route_policy_execute()` to send, or `compute_routers_get_route_policy` for simplest API.

pub fn compute_routers_get_route_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    policy: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/getRoutePolicy",
        project,
        region,
        router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = policy.as_ref() {
        query_parts.push(format!("policy={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy
/// Returns specified Route Policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_get_route_policy_execute()` or `compute_routers_get_route_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_route_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_route_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RoutersGetRoutePolicyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RoutersGetRoutePolicyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy
/// Returns specified Route Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_get_route_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_get_route_policy_task()`.
/// For the simplest API, use `compute_routers_get_route_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_route_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_get_route_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RoutersGetRoutePolicyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_routers_get_route_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_get_route_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersGetRoutePolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: policy
    pub policy: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy
/// Returns specified Route Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_route_policy_builder()` + `compute_routers_get_route_policy_execute()`.
/// For task-level control, use `compute_routers_get_route_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_route_policy(
    client: &SimpleHttpClient,
    args: &ComputeRoutersGetRoutePolicyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RoutersGetRoutePolicyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_get_route_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.policy,
    )?;
    compute_routers_get_route_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus
/// Retrieves runtime information of the specified router.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_router_status_execute()` to send, or `compute_routers_get_router_status` for simplest API.

pub fn compute_routers_get_router_status_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/getRouterStatus",
        project,
        region,
        router,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus
/// Retrieves runtime information of the specified router.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_get_router_status_execute()` or `compute_routers_get_router_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_router_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_router_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouterStatusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouterStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus
/// Retrieves runtime information of the specified router.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_get_router_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_get_router_status_task()`.
/// For the simplest API, use `compute_routers_get_router_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_get_router_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_get_router_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_routers_get_router_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_get_router_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersGetRouterStatusArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus
/// Retrieves runtime information of the specified router.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_router_status_builder()` + `compute_routers_get_router_status_execute()`.
/// For task-level control, use `compute_routers_get_router_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_router_status(
    client: &SimpleHttpClient,
    args: &ComputeRoutersGetRouterStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_get_router_status_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
    )?;
    compute_routers_get_router_status_execute(builder)
}

/// POST projects/{project}/regions/{region}/routers
/// Creates a Router resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_insert_execute()` to send, or `compute_routers_insert` for simplest API.

pub fn compute_routers_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/routers
/// Creates a Router resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_insert_execute()` or `compute_routers_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/routers
/// Creates a Router resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_insert_task()`.
/// For the simplest API, use `compute_routers_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/routers
/// Creates a Router resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_insert_builder()` + `compute_routers_insert_execute()`.
/// For task-level control, use `compute_routers_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_insert(
    client: &SimpleHttpClient,
    args: &ComputeRoutersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_routers_insert_builder(client, &args.project, &args.region, &args.requestId)?;
    compute_routers_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers
/// Retrieves a list of Router resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_list_execute()` to send, or `compute_routers_list` for simplest API.

pub fn compute_routers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/routers
/// Retrieves a list of Router resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_list_execute()` or `compute_routers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouterList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouterList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/routers
/// Retrieves a list of Router resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_list_task()`.
/// For the simplest API, use `compute_routers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/routers
/// Retrieves a list of Router resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_list_builder()` + `compute_routers_list_execute()`.
/// For task-level control, use `compute_routers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list(
    client: &SimpleHttpClient,
    args: &ComputeRoutersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_routers_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes
/// Retrieves a list of router bgp routes available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_list_bgp_routes_execute()` to send, or `compute_routers_list_bgp_routes` for simplest API.

pub fn compute_routers_list_bgp_routes_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    addressFamily: &Option<Option<String>>,
    destinationPrefix: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    peer: &Option<Option<String>>,
    policyApplied: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    routeType: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/listBgpRoutes",
        project, region, router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addressFamily.as_ref() {
        query_parts.push(format!("addressFamily={}", val));
    }
    if let Some(val) = destinationPrefix.as_ref() {
        query_parts.push(format!("destinationPrefix={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = peer.as_ref() {
        query_parts.push(format!("peer={}", val));
    }
    if let Some(val) = policyApplied.as_ref() {
        query_parts.push(format!("policyApplied={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = routeType.as_ref() {
        query_parts.push(format!("routeType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes
/// Retrieves a list of router bgp routes available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_list_bgp_routes_execute()` or `compute_routers_list_bgp_routes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_list_bgp_routes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_bgp_routes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RoutersListBgpRoutes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RoutersListBgpRoutes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes
/// Retrieves a list of router bgp routes available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_list_bgp_routes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_list_bgp_routes_task()`.
/// For the simplest API, use `compute_routers_list_bgp_routes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_list_bgp_routes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_list_bgp_routes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersListBgpRoutes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_routers_list_bgp_routes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_list_bgp_routes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersListBgpRoutesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: addressFamily
    pub addressFamily: Option<Option<String>>,
    /// Query parameter: destinationPrefix
    pub destinationPrefix: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: peer
    pub peer: Option<Option<String>>,
    /// Query parameter: policyApplied
    pub policyApplied: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: routeType
    pub routeType: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes
/// Retrieves a list of router bgp routes available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_list_bgp_routes_builder()` + `compute_routers_list_bgp_routes_execute()`.
/// For task-level control, use `compute_routers_list_bgp_routes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_bgp_routes(
    client: &SimpleHttpClient,
    args: &ComputeRoutersListBgpRoutesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersListBgpRoutes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_list_bgp_routes_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.addressFamily,
        &args.destinationPrefix,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.peer,
        &args.policyApplied,
        &args.returnPartialSuccess,
        &args.routeType,
    )?;
    compute_routers_list_bgp_routes_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies
/// Retrieves a list of router route policy subresources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_list_route_policies_execute()` to send, or `compute_routers_list_route_policies` for simplest API.

pub fn compute_routers_list_route_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/listRoutePolicies",
        project,
        region,
        router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies
/// Retrieves a list of router route policy subresources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_list_route_policies_execute()` or `compute_routers_list_route_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_list_route_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_route_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RoutersListRoutePolicies>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RoutersListRoutePolicies = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies
/// Retrieves a list of router route policy subresources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_list_route_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_list_route_policies_task()`.
/// For the simplest API, use `compute_routers_list_route_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_list_route_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_list_route_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersListRoutePolicies>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_routers_list_route_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_list_route_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersListRoutePoliciesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies
/// Retrieves a list of router route policy subresources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_list_route_policies_builder()` + `compute_routers_list_route_policies_execute()`.
/// For task-level control, use `compute_routers_list_route_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_route_policies(
    client: &SimpleHttpClient,
    args: &ComputeRoutersListRoutePoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersListRoutePolicies>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_list_route_policies_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_routers_list_route_policies_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/routers/{router}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_patch_execute()` to send, or `compute_routers_patch` for simplest API.

pub fn compute_routers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}",
        project, region, router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/routers/{router}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_patch_execute()` or `compute_routers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/routers/{router}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_patch_task()`.
/// For the simplest API, use `compute_routers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/routers/{router}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_patch_builder()` + `compute_routers_patch_execute()`.
/// For task-level control, use `compute_routers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_patch(
    client: &SimpleHttpClient,
    args: &ComputeRoutersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.requestId,
    )?;
    compute_routers_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy
/// Patches Route Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_patch_route_policy_execute()` to send, or `compute_routers_patch_route_policy` for simplest API.

pub fn compute_routers_patch_route_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/patchRoutePolicy",
        project,
        region,
        router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy
/// Patches Route Policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_patch_route_policy_execute()` or `compute_routers_patch_route_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_patch_route_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_patch_route_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy
/// Patches Route Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_patch_route_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_patch_route_policy_task()`.
/// For the simplest API, use `compute_routers_patch_route_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_patch_route_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_patch_route_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_patch_route_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_patch_route_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersPatchRoutePolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy
/// Patches Route Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_patch_route_policy_builder()` + `compute_routers_patch_route_policy_execute()`.
/// For task-level control, use `compute_routers_patch_route_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_patch_route_policy(
    client: &SimpleHttpClient,
    args: &ComputeRoutersPatchRoutePolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_patch_route_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.requestId,
    )?;
    compute_routers_patch_route_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/routers/{router}/preview
/// Preview fields auto-generated during router create andupdate operations. Calling this method does NOT create or update the router.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_preview_execute()` to send, or `compute_routers_preview` for simplest API.

pub fn compute_routers_preview_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/preview",
        project, region, router,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/routers/{router}/preview
/// Preview fields auto-generated during router create andupdate operations. Calling this method does NOT create or update the router.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_preview_execute()` or `compute_routers_preview`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_preview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_preview_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RoutersPreviewResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RoutersPreviewResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/routers/{router}/preview
/// Preview fields auto-generated during router create andupdate operations. Calling this method does NOT create or update the router.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_preview_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_preview_task()`.
/// For the simplest API, use `compute_routers_preview()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_preview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_preview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersPreviewResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_routers_preview_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_preview`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersPreviewArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
}

/// POST projects/{project}/regions/{region}/routers/{router}/preview
/// Preview fields auto-generated during router create andupdate operations. Calling this method does NOT create or update the router.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_preview_builder()` + `compute_routers_preview_execute()`.
/// For task-level control, use `compute_routers_preview_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_preview(
    client: &SimpleHttpClient,
    args: &ComputeRoutersPreviewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersPreviewResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_routers_preview_builder(client, &args.project, &args.region, &args.router)?;
    compute_routers_preview_execute(builder)
}

/// PUT projects/{project}/regions/{region}/routers/{router}
/// Updates the specified Router resource with the data included in the request.  This method conforms `toPUT` semantics, which requests that the state of the target resource be created or replaced with the state defined by the representation enclosed in the request message payload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_update_execute()` to send, or `compute_routers_update` for simplest API.

pub fn compute_routers_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}",
        project, region, router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/regions/{region}/routers/{router}
/// Updates the specified Router resource with the data included in the request.  This method conforms `toPUT` semantics, which requests that the state of the target resource be created or replaced with the state defined by the representation enclosed in the request message payload.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_update_execute()` or `compute_routers_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/regions/{region}/routers/{router}
/// Updates the specified Router resource with the data included in the request.  This method conforms `toPUT` semantics, which requests that the state of the target resource be created or replaced with the state defined by the representation enclosed in the request message payload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_update_task()`.
/// For the simplest API, use `compute_routers_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/regions/{region}/routers/{router}
/// Updates the specified Router resource with the data included in the request.  This method conforms `toPUT` semantics, which requests that the state of the target resource be created or replaced with the state defined by the representation enclosed in the request message payload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_update_builder()` + `compute_routers_update_execute()`.
/// For task-level control, use `compute_routers_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_update(
    client: &SimpleHttpClient,
    args: &ComputeRoutersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_update_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.requestId,
    )?;
    compute_routers_update_execute(builder)
}

/// POST projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy
/// Updates or creates new Route Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_update_route_policy_execute()` to send, or `compute_routers_update_route_policy` for simplest API.

pub fn compute_routers_update_route_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    router: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/updateRoutePolicy",
        project,
        region,
        router,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy
/// Updates or creates new Route Policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routers_update_route_policy_execute()` or `compute_routers_update_route_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_update_route_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_update_route_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy
/// Updates or creates new Route Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routers_update_route_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routers_update_route_policy_task()`.
/// For the simplest API, use `compute_routers_update_route_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routers_update_route_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routers_update_route_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routers_update_route_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routers_update_route_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutersUpdateRoutePolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: router
    pub router: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy
/// Updates or creates new Route Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_update_route_policy_builder()` + `compute_routers_update_route_policy_execute()`.
/// For task-level control, use `compute_routers_update_route_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_update_route_policy(
    client: &SimpleHttpClient,
    args: &ComputeRoutersUpdateRoutePolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_update_route_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.router,
        &args.requestId,
    )?;
    compute_routers_update_route_policy_execute(builder)
}

/// DELETE projects/{project}/global/routes/{route}
/// Deletes the specified Route resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_delete_execute()` to send, or `compute_routes_delete` for simplest API.

pub fn compute_routes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    route: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes/{}",
        project, route,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/routes/{route}
/// Deletes the specified Route resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routes_delete_execute()` or `compute_routes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/routes/{route}
/// Deletes the specified Route resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routes_delete_task()`.
/// For the simplest API, use `compute_routes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: route
    pub route: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/routes/{route}
/// Deletes the specified Route resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_delete_builder()` + `compute_routes_delete_execute()`.
/// For task-level control, use `compute_routes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_delete(
    client: &SimpleHttpClient,
    args: &ComputeRoutesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_routes_delete_builder(client, &args.project, &args.route, &args.requestId)?;
    compute_routes_delete_execute(builder)
}

/// GET projects/{project}/global/routes/{route}
/// Returns the specified Route resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_get_execute()` to send, or `compute_routes_get` for simplest API.

pub fn compute_routes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    route: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes/{}",
        project, route,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/routes/{route}
/// Returns the specified Route resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routes_get_execute()` or `compute_routes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Route>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Route = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/routes/{route}
/// Returns the specified Route resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routes_get_task()`.
/// For the simplest API, use `compute_routes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Route>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: route
    pub route: String,
}

/// GET projects/{project}/global/routes/{route}
/// Returns the specified Route resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_get_builder()` + `compute_routes_get_execute()`.
/// For task-level control, use `compute_routes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_get(
    client: &SimpleHttpClient,
    args: &ComputeRoutesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Route>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routes_get_builder(client, &args.project, &args.route)?;
    compute_routes_get_execute(builder)
}

/// POST projects/{project}/global/routes
/// Creates a Route resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_insert_execute()` to send, or `compute_routes_insert` for simplest API.

pub fn compute_routes_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/routes
/// Creates a Route resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routes_insert_execute()` or `compute_routes_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/routes
/// Creates a Route resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routes_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routes_insert_task()`.
/// For the simplest API, use `compute_routes_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routes_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routes_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/routes
/// Creates a Route resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_insert_builder()` + `compute_routes_insert_execute()`.
/// For task-level control, use `compute_routes_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_insert(
    client: &SimpleHttpClient,
    args: &ComputeRoutesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routes_insert_builder(client, &args.project, &args.requestId)?;
    compute_routes_insert_execute(builder)
}

/// GET projects/{project}/global/routes
/// Retrieves the list of Route resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_list_execute()` to send, or `compute_routes_list` for simplest API.

pub fn compute_routes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/routes
/// Retrieves the list of Route resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routes_list_execute()` or `compute_routes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouteList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouteList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/routes
/// Retrieves the list of Route resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routes_list_task()`.
/// For the simplest API, use `compute_routes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouteList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_routes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/routes
/// Retrieves the list of Route resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_list_builder()` + `compute_routes_list_execute()`.
/// For task-level control, use `compute_routes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_list(
    client: &SimpleHttpClient,
    args: &ComputeRoutesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouteList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routes_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_routes_list_execute(builder)
}

/// POST projects/{project}/global/routes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_test_iam_permissions_execute()` to send, or `compute_routes_test_iam_permissions` for simplest API.

pub fn compute_routes_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/routes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_routes_test_iam_permissions_execute()` or `compute_routes_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/routes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_routes_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_routes_test_iam_permissions_task()`.
/// For the simplest API, use `compute_routes_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_routes_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_routes_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_routes_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_routes_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRoutesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/routes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_test_iam_permissions_builder()` + `compute_routes_test_iam_permissions_execute()`.
/// For task-level control, use `compute_routes_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeRoutesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_routes_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_routes_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_add_rule_execute()` to send, or `compute_security_policies_add_rule` for simplest API.

pub fn compute_security_policies_add_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    securityPolicy: &String,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/addRule",
        project, securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_add_rule_execute()` or `compute_security_policies_add_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_add_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_add_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_add_rule_task()`.
/// For the simplest API, use `compute_security_policies_add_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_add_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_security_policies_add_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_add_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesAddRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_add_rule_builder()` + `compute_security_policies_add_rule_execute()`.
/// For task-level control, use `compute_security_policies_add_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_add_rule(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesAddRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_add_rule_builder(
        client,
        &args.project,
        &args.securityPolicy,
        &args.validateOnly,
    )?;
    compute_security_policies_add_rule_execute(builder)
}

/// GET projects/{project}/aggregated/securityPolicies
/// Retrieves the list of all SecurityPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_aggregated_list_execute()` to send, or `compute_security_policies_aggregated_list` for simplest API.

pub fn compute_security_policies_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/securityPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/securityPolicies
/// Retrieves the list of all SecurityPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_aggregated_list_execute()` or `compute_security_policies_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPoliciesAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPoliciesAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/securityPolicies
/// Retrieves the list of all SecurityPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_aggregated_list_task()`.
/// For the simplest API, use `compute_security_policies_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SecurityPoliciesAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_security_policies_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/securityPolicies
/// Retrieves the list of all SecurityPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_aggregated_list_builder()` + `compute_security_policies_aggregated_list_execute()`.
/// For task-level control, use `compute_security_policies_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SecurityPoliciesAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_security_policies_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_security_policies_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_delete_execute()` to send, or `compute_security_policies_delete` for simplest API.

pub fn compute_security_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    securityPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}",
        project, securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_delete_execute()` or `compute_security_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_delete_task()`.
/// For the simplest API, use `compute_security_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_security_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_delete_builder()` + `compute_security_policies_delete_execute()`.
/// For task-level control, use `compute_security_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_delete_builder(
        client,
        &args.project,
        &args.securityPolicy,
        &args.requestId,
    )?;
    compute_security_policies_delete_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_get_execute()` to send, or `compute_security_policies_get` for simplest API.

pub fn compute_security_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    securityPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}",
        project, securityPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_get_execute()` or `compute_security_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_get_task()`.
/// For the simplest API, use `compute_security_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_security_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_get_builder()` + `compute_security_policies_get_execute()`.
/// For task-level control, use `compute_security_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_security_policies_get_builder(client, &args.project, &args.securityPolicy)?;
    compute_security_policies_get_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_get_rule_execute()` to send, or `compute_security_policies_get_rule` for simplest API.

pub fn compute_security_policies_get_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/getRule",
        project, securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_get_rule_execute()` or `compute_security_policies_get_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_get_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_get_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_get_rule_task()`.
/// For the simplest API, use `compute_security_policies_get_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_get_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_security_policies_get_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_get_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesGetRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_get_rule_builder()` + `compute_security_policies_get_rule_execute()`.
/// For task-level control, use `compute_security_policies_get_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_get_rule(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesGetRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_security_policies_get_rule_builder(
        client,
        &args.project,
        &args.securityPolicy,
        &args.priority,
    )?;
    compute_security_policies_get_rule_execute(builder)
}

/// POST projects/{project}/global/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_insert_execute()` to send, or `compute_security_policies_insert` for simplest API.

pub fn compute_security_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_insert_execute()` or `compute_security_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_insert_task()`.
/// For the simplest API, use `compute_security_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_security_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/global/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_insert_builder()` + `compute_security_policies_insert_execute()`.
/// For task-level control, use `compute_security_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_insert_builder(
        client,
        &args.project,
        &args.requestId,
        &args.validateOnly,
    )?;
    compute_security_policies_insert_execute(builder)
}

/// GET projects/{project}/global/securityPolicies
/// List all the policies that have been configured for the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_list_execute()` to send, or `compute_security_policies_list` for simplest API.

pub fn compute_security_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/securityPolicies
/// List all the policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_list_execute()` or `compute_security_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/securityPolicies
/// List all the policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_list_task()`.
/// For the simplest API, use `compute_security_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_security_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/securityPolicies
/// List all the policies that have been configured for the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_list_builder()` + `compute_security_policies_list_execute()`.
/// For task-level control, use `compute_security_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_security_policies_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_security_policies_list_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_list_preconfigured_expression_sets_execute()` to send, or `compute_security_policies_list_preconfigured_expression_sets` for simplest API.

pub fn compute_security_policies_list_preconfigured_expression_sets_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/listPreconfiguredExpressionSets",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_list_preconfigured_expression_sets_execute()` or `compute_security_policies_list_preconfigured_expression_sets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_list_preconfigured_expression_sets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_list_preconfigured_expression_sets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPoliciesListPreconfiguredExpressionSetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_list_preconfigured_expression_sets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_list_preconfigured_expression_sets_task()`.
/// For the simplest API, use `compute_security_policies_list_preconfigured_expression_sets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_list_preconfigured_expression_sets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_list_preconfigured_expression_sets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_security_policies_list_preconfigured_expression_sets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_list_preconfigured_expression_sets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_list_preconfigured_expression_sets_builder()` + `compute_security_policies_list_preconfigured_expression_sets_execute()`.
/// For task-level control, use `compute_security_policies_list_preconfigured_expression_sets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_list_preconfigured_expression_sets(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_security_policies_list_preconfigured_expression_sets_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_security_policies_list_preconfigured_expression_sets_execute(builder)
}

/// PATCH projects/{project}/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_patch_execute()` to send, or `compute_security_policies_patch` for simplest API.

pub fn compute_security_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    securityPolicy: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}",
        project, securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_patch_execute()` or `compute_security_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_patch_task()`.
/// For the simplest API, use `compute_security_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_security_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_patch_builder()` + `compute_security_policies_patch_execute()`.
/// For task-level control, use `compute_security_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_patch_builder(
        client,
        &args.project,
        &args.securityPolicy,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_security_policies_patch_execute(builder)
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_patch_rule_execute()` to send, or `compute_security_policies_patch_rule` for simplest API.

pub fn compute_security_policies_patch_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/patchRule",
        project,
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_patch_rule_execute()` or `compute_security_policies_patch_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_patch_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_patch_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_patch_rule_task()`.
/// For the simplest API, use `compute_security_policies_patch_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_patch_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_security_policies_patch_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_patch_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesPatchRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_patch_rule_builder()` + `compute_security_policies_patch_rule_execute()`.
/// For task-level control, use `compute_security_policies_patch_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_patch_rule(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesPatchRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_patch_rule_builder(
        client,
        &args.project,
        &args.securityPolicy,
        &args.priority,
        &args.updateMask,
        &args.validateOnly,
    )?;
    compute_security_policies_patch_rule_execute(builder)
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_remove_rule_execute()` to send, or `compute_security_policies_remove_rule` for simplest API.

pub fn compute_security_policies_remove_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    securityPolicy: &String,
    priority: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/removeRule",
        project,
        securityPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = priority.as_ref() {
        query_parts.push(format!("priority={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_remove_rule_execute()` or `compute_security_policies_remove_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_remove_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_remove_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_remove_rule_task()`.
/// For the simplest API, use `compute_security_policies_remove_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_remove_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_security_policies_remove_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_remove_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesRemoveRuleArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: securityPolicy
    pub securityPolicy: String,
    /// Query parameter: priority
    pub priority: Option<Option<String>>,
}

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_remove_rule_builder()` + `compute_security_policies_remove_rule_execute()`.
/// For task-level control, use `compute_security_policies_remove_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_remove_rule(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesRemoveRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_remove_rule_builder(
        client,
        &args.project,
        &args.securityPolicy,
        &args.priority,
    )?;
    compute_security_policies_remove_rule_execute(builder)
}

/// POST projects/{project}/global/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_set_labels_execute()` to send, or `compute_security_policies_set_labels` for simplest API.

pub fn compute_security_policies_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/setLabels",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_security_policies_set_labels_execute()` or `compute_security_policies_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_security_policies_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_security_policies_set_labels_task()`.
/// For the simplest API, use `compute_security_policies_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_security_policies_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_security_policies_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_security_policies_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_security_policies_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_set_labels_builder()` + `compute_security_policies_set_labels_execute()`.
/// For task-level control, use `compute_security_policies_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeSecurityPoliciesSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_security_policies_set_labels_builder(client, &args.project, &args.resource)?;
    compute_security_policies_set_labels_execute(builder)
}

/// GET projects/{project}/aggregated/serviceAttachments
/// Retrieves the list of all ServiceAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_aggregated_list_execute()` to send, or `compute_service_attachments_aggregated_list` for simplest API.

pub fn compute_service_attachments_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/serviceAttachments",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/serviceAttachments
/// Retrieves the list of all ServiceAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_aggregated_list_execute()` or `compute_service_attachments_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAttachmentAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAttachmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/serviceAttachments
/// Retrieves the list of all ServiceAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_aggregated_list_task()`.
/// For the simplest API, use `compute_service_attachments_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_service_attachments_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/serviceAttachments
/// Retrieves the list of all ServiceAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_aggregated_list_builder()` + `compute_service_attachments_aggregated_list_execute()`.
/// For task-level control, use `compute_service_attachments_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_service_attachments_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Deletes the specified ServiceAttachment in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_delete_execute()` to send, or `compute_service_attachments_delete` for simplest API.

pub fn compute_service_attachments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    serviceAttachment: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}",
        project, region, serviceAttachment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Deletes the specified ServiceAttachment in the given scope
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_delete_execute()` or `compute_service_attachments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Deletes the specified ServiceAttachment in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_delete_task()`.
/// For the simplest API, use `compute_service_attachments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_service_attachments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: serviceAttachment
    pub serviceAttachment: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Deletes the specified ServiceAttachment in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_delete_builder()` + `compute_service_attachments_delete_execute()`.
/// For task-level control, use `compute_service_attachments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_delete(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.serviceAttachment,
        &args.requestId,
    )?;
    compute_service_attachments_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Returns the specified ServiceAttachment resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_get_execute()` to send, or `compute_service_attachments_get` for simplest API.

pub fn compute_service_attachments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    serviceAttachment: &String,
    showNatIps: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}",
        project, region, serviceAttachment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = showNatIps.as_ref() {
        query_parts.push(format!("showNatIps={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Returns the specified ServiceAttachment resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_get_execute()` or `compute_service_attachments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAttachment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Returns the specified ServiceAttachment resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_get_task()`.
/// For the simplest API, use `compute_service_attachments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_service_attachments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: serviceAttachment
    pub serviceAttachment: String,
    /// Query parameter: showNatIps
    pub showNatIps: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Returns the specified ServiceAttachment resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_get_builder()` + `compute_service_attachments_get_execute()`.
/// For task-level control, use `compute_service_attachments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_get(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_get_builder(
        client,
        &args.project,
        &args.region,
        &args.serviceAttachment,
        &args.showNatIps,
    )?;
    compute_service_attachments_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_get_iam_policy_execute()` to send, or `compute_service_attachments_get_iam_policy` for simplest API.

pub fn compute_service_attachments_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_get_iam_policy_execute()` or `compute_service_attachments_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_get_iam_policy_task()`.
/// For the simplest API, use `compute_service_attachments_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_service_attachments_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_get_iam_policy_builder()` + `compute_service_attachments_get_iam_policy_execute()`.
/// For task-level control, use `compute_service_attachments_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_service_attachments_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/serviceAttachments
/// Creates a ServiceAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_insert_execute()` to send, or `compute_service_attachments_insert` for simplest API.

pub fn compute_service_attachments_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/serviceAttachments
/// Creates a ServiceAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_insert_execute()` or `compute_service_attachments_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/serviceAttachments
/// Creates a ServiceAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_insert_task()`.
/// For the simplest API, use `compute_service_attachments_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_service_attachments_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/serviceAttachments
/// Creates a ServiceAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_insert_builder()` + `compute_service_attachments_insert_execute()`.
/// For task-level control, use `compute_service_attachments_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_insert(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_service_attachments_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Lists the ServiceAttachments for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_list_execute()` to send, or `compute_service_attachments_list` for simplest API.

pub fn compute_service_attachments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Lists the ServiceAttachments for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_list_execute()` or `compute_service_attachments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAttachmentList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAttachmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Lists the ServiceAttachments for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_list_task()`.
/// For the simplest API, use `compute_service_attachments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAttachmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_service_attachments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Lists the ServiceAttachments for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_list_builder()` + `compute_service_attachments_list_execute()`.
/// For task-level control, use `compute_service_attachments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_list(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAttachmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_service_attachments_list_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Patches the specified ServiceAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_patch_execute()` to send, or `compute_service_attachments_patch` for simplest API.

pub fn compute_service_attachments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    serviceAttachment: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}",
        project, region, serviceAttachment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Patches the specified ServiceAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_patch_execute()` or `compute_service_attachments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Patches the specified ServiceAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_patch_task()`.
/// For the simplest API, use `compute_service_attachments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_service_attachments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: serviceAttachment
    pub serviceAttachment: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Patches the specified ServiceAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_patch_builder()` + `compute_service_attachments_patch_execute()`.
/// For task-level control, use `compute_service_attachments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_patch(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.serviceAttachment,
        &args.requestId,
    )?;
    compute_service_attachments_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_set_iam_policy_execute()` to send, or `compute_service_attachments_set_iam_policy` for simplest API.

pub fn compute_service_attachments_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_set_iam_policy_execute()` or `compute_service_attachments_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_set_iam_policy_task()`.
/// For the simplest API, use `compute_service_attachments_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_service_attachments_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_set_iam_policy_builder()` + `compute_service_attachments_set_iam_policy_execute()`.
/// For task-level control, use `compute_service_attachments_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_service_attachments_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_test_iam_permissions_execute()` to send, or `compute_service_attachments_test_iam_permissions` for simplest API.

pub fn compute_service_attachments_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_service_attachments_test_iam_permissions_execute()` or `compute_service_attachments_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_service_attachments_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_service_attachments_test_iam_permissions_task()`.
/// For the simplest API, use `compute_service_attachments_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_service_attachments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_service_attachments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_service_attachments_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_service_attachments_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_test_iam_permissions_builder()` + `compute_service_attachments_test_iam_permissions_execute()`.
/// For task-level control, use `compute_service_attachments_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeServiceAttachmentsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_service_attachments_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/snapshotSettings
/// Get snapshot settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshot_settings_get_execute()` to send, or `compute_snapshot_settings_get` for simplest API.

pub fn compute_snapshot_settings_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshotSettings",
        project,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/snapshotSettings
/// Get snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshot_settings_get_execute()` or `compute_snapshot_settings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshot_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshot_settings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnapshotSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/snapshotSettings
/// Get snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshot_settings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshot_settings_get_task()`.
/// For the simplest API, use `compute_snapshot_settings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshot_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshot_settings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_snapshot_settings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshot_settings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotSettingsGetArgs {
    /// Path parameter: project
    pub project: String,
}

/// GET projects/{project}/global/snapshotSettings
/// Get snapshot settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshot_settings_get_builder()` + `compute_snapshot_settings_get_execute()`.
/// For task-level control, use `compute_snapshot_settings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshot_settings_get(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotSettingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_snapshot_settings_get_builder(client, &args.project)?;
    compute_snapshot_settings_get_execute(builder)
}

/// PATCH projects/{project}/global/snapshotSettings
/// Patch snapshot settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshot_settings_patch_execute()` to send, or `compute_snapshot_settings_patch` for simplest API.

pub fn compute_snapshot_settings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshotSettings",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/snapshotSettings
/// Patch snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshot_settings_patch_execute()` or `compute_snapshot_settings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshot_settings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshot_settings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/snapshotSettings
/// Patch snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshot_settings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshot_settings_patch_task()`.
/// For the simplest API, use `compute_snapshot_settings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshot_settings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshot_settings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_snapshot_settings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshot_settings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotSettingsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/global/snapshotSettings
/// Patch snapshot settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshot_settings_patch_builder()` + `compute_snapshot_settings_patch_execute()`.
/// For task-level control, use `compute_snapshot_settings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshot_settings_patch(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotSettingsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshot_settings_patch_builder(
        client,
        &args.project,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_snapshot_settings_patch_execute(builder)
}

/// DELETE projects/{project}/global/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_delete_execute()` to send, or `compute_snapshots_delete` for simplest API.

pub fn compute_snapshots_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    snapshot: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}",
        project, snapshot,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_delete_execute()` or `compute_snapshots_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_delete_task()`.
/// For the simplest API, use `compute_snapshots_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_snapshots_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: snapshot
    pub snapshot: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_delete_builder()` + `compute_snapshots_delete_execute()`.
/// For task-level control, use `compute_snapshots_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_delete(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_snapshots_delete_builder(client, &args.project, &args.snapshot, &args.requestId)?;
    compute_snapshots_delete_execute(builder)
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_get_execute()` to send, or `compute_snapshots_get` for simplest API.

pub fn compute_snapshots_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    snapshot: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}",
        project, snapshot,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_get_execute()` or `compute_snapshots_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_get_task()`.
/// For the simplest API, use `compute_snapshots_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_snapshots_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: snapshot
    pub snapshot: String,
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_get_builder()` + `compute_snapshots_get_execute()`.
/// For task-level control, use `compute_snapshots_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_get(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_get_builder(client, &args.project, &args.snapshot)?;
    compute_snapshots_get_execute(builder)
}

/// GET projects/{project}/global/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_get_iam_policy_execute()` to send, or `compute_snapshots_get_iam_policy` for simplest API.

pub fn compute_snapshots_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_get_iam_policy_execute()` or `compute_snapshots_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_get_iam_policy_task()`.
/// For the simplest API, use `compute_snapshots_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_snapshots_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/global/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_get_iam_policy_builder()` + `compute_snapshots_get_iam_policy_execute()`.
/// For task-level control, use `compute_snapshots_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_snapshots_get_iam_policy_execute(builder)
}

/// POST projects/{project}/global/snapshots
/// Creates a snapshot in the specified project using the data included in the request. For regular snapshot creation, consider using this method instead of disks.`createSnapshot`, as this method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_insert_execute()` to send, or `compute_snapshots_insert` for simplest API.

pub fn compute_snapshots_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/snapshots
/// Creates a snapshot in the specified project using the data included in the request. For regular snapshot creation, consider using this method instead of disks.`createSnapshot`, as this method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_insert_execute()` or `compute_snapshots_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/snapshots
/// Creates a snapshot in the specified project using the data included in the request. For regular snapshot creation, consider using this method instead of disks.`createSnapshot`, as this method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_insert_task()`.
/// For the simplest API, use `compute_snapshots_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_snapshots_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/snapshots
/// Creates a snapshot in the specified project using the data included in the request. For regular snapshot creation, consider using this method instead of disks.`createSnapshot`, as this method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_insert_builder()` + `compute_snapshots_insert_execute()`.
/// For task-level control, use `compute_snapshots_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_insert(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_insert_builder(client, &args.project, &args.requestId)?;
    compute_snapshots_insert_execute(builder)
}

/// GET projects/{project}/global/snapshots
/// Retrieves the list of Snapshot resources contained within the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_list_execute()` to send, or `compute_snapshots_list` for simplest API.

pub fn compute_snapshots_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/snapshots
/// Retrieves the list of Snapshot resources contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_list_execute()` or `compute_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnapshotList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/snapshots
/// Retrieves the list of Snapshot resources contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_list_task()`.
/// For the simplest API, use `compute_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/snapshots
/// Retrieves the list of Snapshot resources contained within the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_list_builder()` + `compute_snapshots_list_execute()`.
/// For task-level control, use `compute_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_list(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_snapshots_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_snapshots_list_execute(builder)
}

/// POST projects/{project}/global/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_set_iam_policy_execute()` to send, or `compute_snapshots_set_iam_policy` for simplest API.

pub fn compute_snapshots_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_set_iam_policy_execute()` or `compute_snapshots_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_set_iam_policy_task()`.
/// For the simplest API, use `compute_snapshots_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_snapshots_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_set_iam_policy_builder()` + `compute_snapshots_set_iam_policy_execute()`.
/// For task-level control, use `compute_snapshots_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_set_iam_policy_builder(client, &args.project, &args.resource)?;
    compute_snapshots_set_iam_policy_execute(builder)
}

/// POST projects/{project}/global/snapshots/{resource}/setLabels
/// Sets the labels on a snapshot. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_set_labels_execute()` to send, or `compute_snapshots_set_labels` for simplest API.

pub fn compute_snapshots_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/setLabels",
        project, resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/snapshots/{resource}/setLabels
/// Sets the labels on a snapshot. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_set_labels_execute()` or `compute_snapshots_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/snapshots/{resource}/setLabels
/// Sets the labels on a snapshot. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_set_labels_task()`.
/// For the simplest API, use `compute_snapshots_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_snapshots_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/snapshots/{resource}/setLabels
/// Sets the labels on a snapshot. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_set_labels_builder()` + `compute_snapshots_set_labels_execute()`.
/// For task-level control, use `compute_snapshots_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_set_labels_builder(client, &args.project, &args.resource)?;
    compute_snapshots_set_labels_execute(builder)
}

/// POST projects/{project}/global/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_test_iam_permissions_execute()` to send, or `compute_snapshots_test_iam_permissions` for simplest API.

pub fn compute_snapshots_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_test_iam_permissions_execute()` or `compute_snapshots_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_test_iam_permissions_task()`.
/// For the simplest API, use `compute_snapshots_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_snapshots_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_test_iam_permissions_builder()` + `compute_snapshots_test_iam_permissions_execute()`.
/// For task-level control, use `compute_snapshots_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_snapshots_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_snapshots_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/global/snapshots/{snapshot}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_update_kms_key_execute()` to send, or `compute_snapshots_update_kms_key` for simplest API.

pub fn compute_snapshots_update_kms_key_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    snapshot: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/updateKmsKey",
        project, snapshot,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/snapshots/{snapshot}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified snapshot.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_snapshots_update_kms_key_execute()` or `compute_snapshots_update_kms_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_update_kms_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_update_kms_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/snapshots/{snapshot}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_snapshots_update_kms_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_snapshots_update_kms_key_task()`.
/// For the simplest API, use `compute_snapshots_update_kms_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_snapshots_update_kms_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_snapshots_update_kms_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_snapshots_update_kms_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_snapshots_update_kms_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSnapshotsUpdateKmsKeyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: snapshot
    pub snapshot: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/snapshots/{snapshot}/updateKmsKey
/// Rotates the customer-managed encryption key to the latest version for the specified snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_update_kms_key_builder()` + `compute_snapshots_update_kms_key_execute()`.
/// For task-level control, use `compute_snapshots_update_kms_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_update_kms_key(
    client: &SimpleHttpClient,
    args: &ComputeSnapshotsUpdateKmsKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_update_kms_key_builder(
        client,
        &args.project,
        &args.snapshot,
        &args.requestId,
    )?;
    compute_snapshots_update_kms_key_execute(builder)
}

/// GET projects/{project}/aggregated/sslCertificates
/// Retrieves the list of all SslCertificate resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_aggregated_list_execute()` to send, or `compute_ssl_certificates_aggregated_list` for simplest API.

pub fn compute_ssl_certificates_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/sslCertificates",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/sslCertificates
/// Retrieves the list of all SslCertificate resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_certificates_aggregated_list_execute()` or `compute_ssl_certificates_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificateAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificateAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/sslCertificates
/// Retrieves the list of all SslCertificate resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_certificates_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_certificates_aggregated_list_task()`.
/// For the simplest API, use `compute_ssl_certificates_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_certificates_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslCertificateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_ssl_certificates_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_certificates_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslCertificatesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/sslCertificates
/// Retrieves the list of all SslCertificate resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_aggregated_list_builder()` + `compute_ssl_certificates_aggregated_list_execute()`.
/// For task-level control, use `compute_ssl_certificates_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeSslCertificatesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslCertificateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_certificates_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_ssl_certificates_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_delete_execute()` to send, or `compute_ssl_certificates_delete` for simplest API.

pub fn compute_ssl_certificates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    sslCertificate: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslCertificates/{}",
        project, sslCertificate,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_certificates_delete_execute()` or `compute_ssl_certificates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_certificates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_certificates_delete_task()`.
/// For the simplest API, use `compute_ssl_certificates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_ssl_certificates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_certificates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslCertificatesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: sslCertificate
    pub sslCertificate: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_delete_builder()` + `compute_ssl_certificates_delete_execute()`.
/// For task-level control, use `compute_ssl_certificates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_delete(
    client: &SimpleHttpClient,
    args: &ComputeSslCertificatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_certificates_delete_builder(
        client,
        &args.project,
        &args.sslCertificate,
        &args.requestId,
    )?;
    compute_ssl_certificates_delete_execute(builder)
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_get_execute()` to send, or `compute_ssl_certificates_get` for simplest API.

pub fn compute_ssl_certificates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    sslCertificate: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslCertificates/{}",
        project, sslCertificate,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_certificates_get_execute()` or `compute_ssl_certificates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_certificates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_certificates_get_task()`.
/// For the simplest API, use `compute_ssl_certificates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_ssl_certificates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_certificates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslCertificatesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: sslCertificate
    pub sslCertificate: String,
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_get_builder()` + `compute_ssl_certificates_get_execute()`.
/// For task-level control, use `compute_ssl_certificates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_get(
    client: &SimpleHttpClient,
    args: &ComputeSslCertificatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_ssl_certificates_get_builder(client, &args.project, &args.sslCertificate)?;
    compute_ssl_certificates_get_execute(builder)
}

/// POST projects/{project}/global/sslCertificates
/// Creates a SslCertificate resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_insert_execute()` to send, or `compute_ssl_certificates_insert` for simplest API.

pub fn compute_ssl_certificates_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslCertificates",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/sslCertificates
/// Creates a SslCertificate resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_certificates_insert_execute()` or `compute_ssl_certificates_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/sslCertificates
/// Creates a SslCertificate resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_certificates_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_certificates_insert_task()`.
/// For the simplest API, use `compute_ssl_certificates_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_certificates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_ssl_certificates_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_certificates_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslCertificatesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/sslCertificates
/// Creates a SslCertificate resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_insert_builder()` + `compute_ssl_certificates_insert_execute()`.
/// For task-level control, use `compute_ssl_certificates_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_insert(
    client: &SimpleHttpClient,
    args: &ComputeSslCertificatesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_certificates_insert_builder(client, &args.project, &args.requestId)?;
    compute_ssl_certificates_insert_execute(builder)
}

/// GET projects/{project}/global/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_list_execute()` to send, or `compute_ssl_certificates_list` for simplest API.

pub fn compute_ssl_certificates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslCertificates",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_certificates_list_execute()` or `compute_ssl_certificates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificateList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_certificates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_certificates_list_task()`.
/// For the simplest API, use `compute_ssl_certificates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_ssl_certificates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_certificates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslCertificatesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_list_builder()` + `compute_ssl_certificates_list_execute()`.
/// For task-level control, use `compute_ssl_certificates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_list(
    client: &SimpleHttpClient,
    args: &ComputeSslCertificatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_certificates_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_ssl_certificates_list_execute(builder)
}

/// GET projects/{project}/aggregated/sslPolicies
/// Retrieves the list of all SslPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_aggregated_list_execute()` to send, or `compute_ssl_policies_aggregated_list` for simplest API.

pub fn compute_ssl_policies_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/sslPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/sslPolicies
/// Retrieves the list of all SslPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_policies_aggregated_list_execute()` or `compute_ssl_policies_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPoliciesAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/sslPolicies
/// Retrieves the list of all SslPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_policies_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_policies_aggregated_list_task()`.
/// For the simplest API, use `compute_ssl_policies_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_ssl_policies_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_policies_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslPoliciesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/sslPolicies
/// Retrieves the list of all SslPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_aggregated_list_builder()` + `compute_ssl_policies_aggregated_list_execute()`.
/// For task-level control, use `compute_ssl_policies_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeSslPoliciesAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_ssl_policies_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_delete_execute()` to send, or `compute_ssl_policies_delete` for simplest API.

pub fn compute_ssl_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    sslPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies/{}",
        project, sslPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_policies_delete_execute()` or `compute_ssl_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_policies_delete_task()`.
/// For the simplest API, use `compute_ssl_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_ssl_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: sslPolicy
    pub sslPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_delete_builder()` + `compute_ssl_policies_delete_execute()`.
/// For task-level control, use `compute_ssl_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeSslPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_delete_builder(
        client,
        &args.project,
        &args.sslPolicy,
        &args.requestId,
    )?;
    compute_ssl_policies_delete_execute(builder)
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_get_execute()` to send, or `compute_ssl_policies_get` for simplest API.

pub fn compute_ssl_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    sslPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies/{}",
        project, sslPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_policies_get_execute()` or `compute_ssl_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_policies_get_task()`.
/// For the simplest API, use `compute_ssl_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_ssl_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: sslPolicy
    pub sslPolicy: String,
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_get_builder()` + `compute_ssl_policies_get_execute()`.
/// For task-level control, use `compute_ssl_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeSslPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_get_builder(client, &args.project, &args.sslPolicy)?;
    compute_ssl_policies_get_execute(builder)
}

/// POST projects/{project}/global/sslPolicies
/// Returns the specified SSL policy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_insert_execute()` to send, or `compute_ssl_policies_insert` for simplest API.

pub fn compute_ssl_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/sslPolicies
/// Returns the specified SSL policy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_policies_insert_execute()` or `compute_ssl_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/sslPolicies
/// Returns the specified SSL policy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_policies_insert_task()`.
/// For the simplest API, use `compute_ssl_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_ssl_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslPoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/sslPolicies
/// Returns the specified SSL policy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_insert_builder()` + `compute_ssl_policies_insert_execute()`.
/// For task-level control, use `compute_ssl_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeSslPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_insert_builder(client, &args.project, &args.requestId)?;
    compute_ssl_policies_insert_execute(builder)
}

/// GET projects/{project}/global/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_list_execute()` to send, or `compute_ssl_policies_list` for simplest API.

pub fn compute_ssl_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_policies_list_execute()` or `compute_ssl_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPoliciesList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_policies_list_task()`.
/// For the simplest API, use `compute_ssl_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_ssl_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_list_builder()` + `compute_ssl_policies_list_execute()`.
/// For task-level control, use `compute_ssl_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeSslPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_ssl_policies_list_execute(builder)
}

/// GET projects/{project}/global/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_list_available_features_execute()` to send, or `compute_ssl_policies_list_available_features` for simplest API.

pub fn compute_ssl_policies_list_available_features_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies/listAvailableFeatures",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_policies_list_available_features_execute()` or `compute_ssl_policies_list_available_features`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_list_available_features_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_list_available_features_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesListAvailableFeaturesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_policies_list_available_features_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_policies_list_available_features_task()`.
/// For the simplest API, use `compute_ssl_policies_list_available_features()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_list_available_features_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_policies_list_available_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_ssl_policies_list_available_features_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_policies_list_available_features`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslPoliciesListAvailableFeaturesArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_list_available_features_builder()` + `compute_ssl_policies_list_available_features_execute()`.
/// For task-level control, use `compute_ssl_policies_list_available_features_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_list_available_features(
    client: &SimpleHttpClient,
    args: &ComputeSslPoliciesListAvailableFeaturesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_list_available_features_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_ssl_policies_list_available_features_execute(builder)
}

/// PATCH projects/{project}/global/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_patch_execute()` to send, or `compute_ssl_policies_patch` for simplest API.

pub fn compute_ssl_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    sslPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies/{}",
        project, sslPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_ssl_policies_patch_execute()` or `compute_ssl_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_ssl_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_ssl_policies_patch_task()`.
/// For the simplest API, use `compute_ssl_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_ssl_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_ssl_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_ssl_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_ssl_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSslPoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: sslPolicy
    pub sslPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_patch_builder()` + `compute_ssl_policies_patch_execute()`.
/// For task-level control, use `compute_ssl_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_patch(
    client: &SimpleHttpClient,
    args: &ComputeSslPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_patch_builder(
        client,
        &args.project,
        &args.sslPolicy,
        &args.requestId,
    )?;
    compute_ssl_policies_patch_execute(builder)
}

/// GET projects/{project}/aggregated/storagePoolTypes
/// Retrieves an aggregated list of storage pool types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pool_types_aggregated_list_execute()` to send, or `compute_storage_pool_types_aggregated_list` for simplest API.

pub fn compute_storage_pool_types_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/storagePoolTypes",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/storagePoolTypes
/// Retrieves an aggregated list of storage pool types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pool_types_aggregated_list_execute()` or `compute_storage_pool_types_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pool_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolTypeAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/storagePoolTypes
/// Retrieves an aggregated list of storage pool types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pool_types_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pool_types_aggregated_list_task()`.
/// For the simplest API, use `compute_storage_pool_types_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pool_types_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pool_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoragePoolTypeAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_storage_pool_types_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pool_types_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolTypesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/storagePoolTypes
/// Retrieves an aggregated list of storage pool types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pool_types_aggregated_list_builder()` + `compute_storage_pool_types_aggregated_list_execute()`.
/// For task-level control, use `compute_storage_pool_types_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolTypesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoragePoolTypeAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pool_types_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_storage_pool_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}
/// Returns the specified storage pool type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pool_types_get_execute()` to send, or `compute_storage_pool_types_get` for simplest API.

pub fn compute_storage_pool_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    storagePoolType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePoolTypes/{}",
        project, zone, storagePoolType,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}
/// Returns the specified storage pool type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pool_types_get_execute()` or `compute_storage_pool_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pool_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}
/// Returns the specified storage pool type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pool_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pool_types_get_task()`.
/// For the simplest API, use `compute_storage_pool_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pool_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pool_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_storage_pool_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pool_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolTypesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: storagePoolType
    pub storagePoolType: String,
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}
/// Returns the specified storage pool type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pool_types_get_builder()` + `compute_storage_pool_types_get_execute()`.
/// For task-level control, use `compute_storage_pool_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_get(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pool_types_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.storagePoolType,
    )?;
    compute_storage_pool_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes
/// Retrieves a list of storage pool types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pool_types_list_execute()` to send, or `compute_storage_pool_types_list` for simplest API.

pub fn compute_storage_pool_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePoolTypes",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes
/// Retrieves a list of storage pool types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pool_types_list_execute()` or `compute_storage_pool_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pool_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolTypeList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes
/// Retrieves a list of storage pool types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pool_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pool_types_list_task()`.
/// For the simplest API, use `compute_storage_pool_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pool_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pool_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_storage_pool_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pool_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolTypesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes
/// Retrieves a list of storage pool types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pool_types_list_builder()` + `compute_storage_pool_types_list_execute()`.
/// For task-level control, use `compute_storage_pool_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_list(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pool_types_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_storage_pool_types_list_execute(builder)
}

/// GET projects/{project}/aggregated/storagePools
/// Retrieves an aggregated list of storage pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_aggregated_list_execute()` to send, or `compute_storage_pools_aggregated_list` for simplest API.

pub fn compute_storage_pools_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/storagePools",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/storagePools
/// Retrieves an aggregated list of storage pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_aggregated_list_execute()` or `compute_storage_pools_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/storagePools
/// Retrieves an aggregated list of storage pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_aggregated_list_task()`.
/// For the simplest API, use `compute_storage_pools_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_storage_pools_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/storagePools
/// Retrieves an aggregated list of storage pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_aggregated_list_builder()` + `compute_storage_pools_aggregated_list_execute()`.
/// For task-level control, use `compute_storage_pools_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_storage_pools_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Deletes the specified storage pool. Deleting a `storagePool` removes its data permanently and is irreversible. However, deleting a `storagePool` does not delete any snapshots previously made from the `storagePool`. You must separately delete snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_delete_execute()` to send, or `compute_storage_pools_delete` for simplest API.

pub fn compute_storage_pools_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    storagePool: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}",
        project, zone, storagePool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Deletes the specified storage pool. Deleting a `storagePool` removes its data permanently and is irreversible. However, deleting a `storagePool` does not delete any snapshots previously made from the `storagePool`. You must separately delete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_delete_execute()` or `compute_storage_pools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Deletes the specified storage pool. Deleting a `storagePool` removes its data permanently and is irreversible. However, deleting a `storagePool` does not delete any snapshots previously made from the `storagePool`. You must separately delete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_delete_task()`.
/// For the simplest API, use `compute_storage_pools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_storage_pools_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: storagePool
    pub storagePool: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Deletes the specified storage pool. Deleting a `storagePool` removes its data permanently and is irreversible. However, deleting a `storagePool` does not delete any snapshots previously made from the `storagePool`. You must separately delete snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_delete_builder()` + `compute_storage_pools_delete_execute()`.
/// For task-level control, use `compute_storage_pools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_delete(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.storagePool,
        &args.requestId,
    )?;
    compute_storage_pools_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Returns a specified storage pool. Gets a list of available storage pools by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_get_execute()` to send, or `compute_storage_pools_get` for simplest API.

pub fn compute_storage_pools_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    storagePool: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}",
        project, zone, storagePool,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Returns a specified storage pool. Gets a list of available storage pools by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_get_execute()` or `compute_storage_pools_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePool>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Returns a specified storage pool. Gets a list of available storage pools by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_get_task()`.
/// For the simplest API, use `compute_storage_pools_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_storage_pools_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: storagePool
    pub storagePool: String,
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Returns a specified storage pool. Gets a list of available storage pools by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_get_builder()` + `compute_storage_pools_get_execute()`.
/// For task-level control, use `compute_storage_pools_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_get(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_storage_pools_get_builder(client, &args.project, &args.zone, &args.storagePool)?;
    compute_storage_pools_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_get_iam_policy_execute()` to send, or `compute_storage_pools_get_iam_policy` for simplest API.

pub fn compute_storage_pools_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}/getIamPolicy",
        project,
        zone,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_get_iam_policy_execute()` or `compute_storage_pools_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_get_iam_policy_task()`.
/// For the simplest API, use `compute_storage_pools_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_storage_pools_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_get_iam_policy_builder()` + `compute_storage_pools_get_iam_policy_execute()`.
/// For task-level control, use `compute_storage_pools_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_get_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_storage_pools_get_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/storagePools
/// Creates a storage pool in the specified project using the data in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_insert_execute()` to send, or `compute_storage_pools_insert` for simplest API.

pub fn compute_storage_pools_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/storagePools
/// Creates a storage pool in the specified project using the data in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_insert_execute()` or `compute_storage_pools_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/storagePools
/// Creates a storage pool in the specified project using the data in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_insert_task()`.
/// For the simplest API, use `compute_storage_pools_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_storage_pools_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/storagePools
/// Creates a storage pool in the specified project using the data in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_insert_builder()` + `compute_storage_pools_insert_execute()`.
/// For task-level control, use `compute_storage_pools_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_insert(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_storage_pools_insert_builder(client, &args.project, &args.zone, &args.requestId)?;
    compute_storage_pools_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Retrieves a list of storage pools contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_list_execute()` to send, or `compute_storage_pools_list` for simplest API.

pub fn compute_storage_pools_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Retrieves a list of storage pools contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_list_execute()` or `compute_storage_pools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Retrieves a list of storage pools contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_list_task()`.
/// For the simplest API, use `compute_storage_pools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_storage_pools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Retrieves a list of storage pools contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_list_builder()` + `compute_storage_pools_list_execute()`.
/// For task-level control, use `compute_storage_pools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_list(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_storage_pools_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks
/// Lists the disks in a specified storage pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_list_disks_execute()` to send, or `compute_storage_pools_list_disks` for simplest API.

pub fn compute_storage_pools_list_disks_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    storagePool: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}/listDisks",
        project, zone, storagePool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks
/// Lists the disks in a specified storage pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_list_disks_execute()` or `compute_storage_pools_list_disks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_list_disks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_list_disks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolListDisks>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolListDisks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks
/// Lists the disks in a specified storage pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_list_disks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_list_disks_task()`.
/// For the simplest API, use `compute_storage_pools_list_disks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_list_disks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_list_disks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolListDisks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_storage_pools_list_disks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_list_disks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsListDisksArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: storagePool
    pub storagePool: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks
/// Lists the disks in a specified storage pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_list_disks_builder()` + `compute_storage_pools_list_disks_execute()`.
/// For task-level control, use `compute_storage_pools_list_disks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_list_disks(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsListDisksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolListDisks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_list_disks_builder(
        client,
        &args.project,
        &args.zone,
        &args.storagePool,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_storage_pools_list_disks_execute(builder)
}

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_set_iam_policy_execute()` to send, or `compute_storage_pools_set_iam_policy` for simplest API.

pub fn compute_storage_pools_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}/setIamPolicy",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_set_iam_policy_execute()` or `compute_storage_pools_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_set_iam_policy_task()`.
/// For the simplest API, use `compute_storage_pools_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_storage_pools_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_set_iam_policy_builder()` + `compute_storage_pools_set_iam_policy_execute()`.
/// For task-level control, use `compute_storage_pools_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_set_iam_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_storage_pools_set_iam_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_test_iam_permissions_execute()` to send, or `compute_storage_pools_test_iam_permissions` for simplest API.

pub fn compute_storage_pools_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_test_iam_permissions_execute()` or `compute_storage_pools_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_test_iam_permissions_task()`.
/// For the simplest API, use `compute_storage_pools_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_storage_pools_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_test_iam_permissions_builder()` + `compute_storage_pools_test_iam_permissions_execute()`.
/// For task-level control, use `compute_storage_pools_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_storage_pools_test_iam_permissions_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Updates the specified `storagePool` with the data included in the request. The update is performed only on selected fields included as part of update-mask. Only the following fields can be modified: pool_provisioned_capacity_gb, pool_provisioned_iops and pool_provisioned_throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_update_execute()` to send, or `compute_storage_pools_update` for simplest API.

pub fn compute_storage_pools_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    storagePool: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}",
        project, zone, storagePool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Updates the specified `storagePool` with the data included in the request. The update is performed only on selected fields included as part of update-mask. Only the following fields can be modified: pool_provisioned_capacity_gb, pool_provisioned_iops and pool_provisioned_throughput.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_storage_pools_update_execute()` or `compute_storage_pools_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Updates the specified `storagePool` with the data included in the request. The update is performed only on selected fields included as part of update-mask. Only the following fields can be modified: pool_provisioned_capacity_gb, pool_provisioned_iops and pool_provisioned_throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_storage_pools_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_storage_pools_update_task()`.
/// For the simplest API, use `compute_storage_pools_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_storage_pools_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_storage_pools_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_storage_pools_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_storage_pools_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeStoragePoolsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: storagePool
    pub storagePool: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Updates the specified `storagePool` with the data included in the request. The update is performed only on selected fields included as part of update-mask. Only the following fields can be modified: pool_provisioned_capacity_gb, pool_provisioned_iops and pool_provisioned_throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_update_builder()` + `compute_storage_pools_update_execute()`.
/// For task-level control, use `compute_storage_pools_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_update(
    client: &SimpleHttpClient,
    args: &ComputeStoragePoolsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_update_builder(
        client,
        &args.project,
        &args.zone,
        &args.storagePool,
        &args.requestId,
        &args.updateMask,
    )?;
    compute_storage_pools_update_execute(builder)
}

/// GET projects/{project}/aggregated/subnetworks
/// Retrieves an aggregated list of subnetworks.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_aggregated_list_execute()` to send, or `compute_subnetworks_aggregated_list` for simplest API.

pub fn compute_subnetworks_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
    views: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/subnetworks",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }
    if let Some(val) = views.as_ref() {
        query_parts.push(format!("views={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/subnetworks
/// Retrieves an aggregated list of subnetworks.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_aggregated_list_execute()` or `compute_subnetworks_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubnetworkAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubnetworkAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/subnetworks
/// Retrieves an aggregated list of subnetworks.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_aggregated_list_task()`.
/// For the simplest API, use `compute_subnetworks_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubnetworkAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_subnetworks_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
    /// Query parameter: views
    pub views: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/subnetworks
/// Retrieves an aggregated list of subnetworks.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_aggregated_list_builder()` + `compute_subnetworks_aggregated_list_execute()`.
/// For task-level control, use `compute_subnetworks_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubnetworkAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
        &args.views,
    )?;
    compute_subnetworks_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Deletes the specified subnetwork.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_delete_execute()` to send, or `compute_subnetworks_delete` for simplest API.

pub fn compute_subnetworks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    subnetwork: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}",
        project, region, subnetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Deletes the specified subnetwork.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_delete_execute()` or `compute_subnetworks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Deletes the specified subnetwork.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_delete_task()`.
/// For the simplest API, use `compute_subnetworks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_subnetworks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: subnetwork
    pub subnetwork: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Deletes the specified subnetwork.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_delete_builder()` + `compute_subnetworks_delete_execute()`.
/// For task-level control, use `compute_subnetworks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_delete(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.subnetwork,
        &args.requestId,
    )?;
    compute_subnetworks_delete_execute(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange
/// Expands the IP CIDR range of the subnetwork to a specified value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_expand_ip_cidr_range_execute()` to send, or `compute_subnetworks_expand_ip_cidr_range` for simplest API.

pub fn compute_subnetworks_expand_ip_cidr_range_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    subnetwork: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/expandIpCidrRange",
        project,
        region,
        subnetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange
/// Expands the IP CIDR range of the subnetwork to a specified value.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_expand_ip_cidr_range_execute()` or `compute_subnetworks_expand_ip_cidr_range`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_expand_ip_cidr_range_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_expand_ip_cidr_range_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange
/// Expands the IP CIDR range of the subnetwork to a specified value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_expand_ip_cidr_range_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_expand_ip_cidr_range_task()`.
/// For the simplest API, use `compute_subnetworks_expand_ip_cidr_range()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_expand_ip_cidr_range_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_expand_ip_cidr_range_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_subnetworks_expand_ip_cidr_range_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_expand_ip_cidr_range`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksExpandIpCidrRangeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: subnetwork
    pub subnetwork: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange
/// Expands the IP CIDR range of the subnetwork to a specified value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_expand_ip_cidr_range_builder()` + `compute_subnetworks_expand_ip_cidr_range_execute()`.
/// For task-level control, use `compute_subnetworks_expand_ip_cidr_range_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_expand_ip_cidr_range(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksExpandIpCidrRangeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_expand_ip_cidr_range_builder(
        client,
        &args.project,
        &args.region,
        &args.subnetwork,
        &args.requestId,
    )?;
    compute_subnetworks_expand_ip_cidr_range_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Returns the specified subnetwork.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_get_execute()` to send, or `compute_subnetworks_get` for simplest API.

pub fn compute_subnetworks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    subnetwork: &String,
    views: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}",
        project, region, subnetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = views.as_ref() {
        query_parts.push(format!("views={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Returns the specified subnetwork.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_get_execute()` or `compute_subnetworks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subnetwork>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subnetwork = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Returns the specified subnetwork.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_get_task()`.
/// For the simplest API, use `compute_subnetworks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subnetwork>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_subnetworks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: subnetwork
    pub subnetwork: String,
    /// Query parameter: views
    pub views: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Returns the specified subnetwork.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_get_builder()` + `compute_subnetworks_get_execute()`.
/// For task-level control, use `compute_subnetworks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_get(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subnetwork>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_get_builder(
        client,
        &args.project,
        &args.region,
        &args.subnetwork,
        &args.views,
    )?;
    compute_subnetworks_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_get_iam_policy_execute()` to send, or `compute_subnetworks_get_iam_policy` for simplest API.

pub fn compute_subnetworks_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    optionsRequestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/getIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_get_iam_policy_execute()` or `compute_subnetworks_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_get_iam_policy_task()`.
/// For the simplest API, use `compute_subnetworks_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_subnetworks_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_get_iam_policy_builder()` + `compute_subnetworks_get_iam_policy_execute()`.
/// For task-level control, use `compute_subnetworks_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_get_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.optionsRequestedPolicyVersion,
    )?;
    compute_subnetworks_get_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks
/// Creates a subnetwork in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_insert_execute()` to send, or `compute_subnetworks_insert` for simplest API.

pub fn compute_subnetworks_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks
/// Creates a subnetwork in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_insert_execute()` or `compute_subnetworks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/subnetworks
/// Creates a subnetwork in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_insert_task()`.
/// For the simplest API, use `compute_subnetworks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_subnetworks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/subnetworks
/// Creates a subnetwork in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_insert_builder()` + `compute_subnetworks_insert_execute()`.
/// For task-level control, use `compute_subnetworks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_insert(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_subnetworks_insert_builder(client, &args.project, &args.region, &args.requestId)?;
    compute_subnetworks_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Retrieves a list of subnetworks available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_list_execute()` to send, or `compute_subnetworks_list` for simplest API.

pub fn compute_subnetworks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    views: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = views.as_ref() {
        query_parts.push(format!("views={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Retrieves a list of subnetworks available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_list_execute()` or `compute_subnetworks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubnetworkList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubnetworkList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Retrieves a list of subnetworks available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_list_task()`.
/// For the simplest API, use `compute_subnetworks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubnetworkList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_subnetworks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: views
    pub views: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Retrieves a list of subnetworks available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_list_builder()` + `compute_subnetworks_list_execute()`.
/// For task-level control, use `compute_subnetworks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_list(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubnetworkList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.views,
    )?;
    compute_subnetworks_list_execute(builder)
}

/// GET projects/{project}/aggregated/subnetworks/listUsable
/// Retrieves an aggregated list of all usable subnetworks in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_list_usable_execute()` to send, or `compute_subnetworks_list_usable` for simplest API.

pub fn compute_subnetworks_list_usable_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProject: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/subnetworks/listUsable",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProject.as_ref() {
        query_parts.push(format!("serviceProject={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/subnetworks/listUsable
/// Retrieves an aggregated list of all usable subnetworks in the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_list_usable_execute()` or `compute_subnetworks_list_usable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_list_usable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UsableSubnetworksAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsableSubnetworksAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/subnetworks/listUsable
/// Retrieves an aggregated list of all usable subnetworks in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_list_usable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_list_usable_task()`.
/// For the simplest API, use `compute_subnetworks_list_usable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_list_usable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_list_usable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UsableSubnetworksAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_subnetworks_list_usable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_list_usable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksListUsableArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProject
    pub serviceProject: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/subnetworks/listUsable
/// Retrieves an aggregated list of all usable subnetworks in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_list_usable_builder()` + `compute_subnetworks_list_usable_execute()`.
/// For task-level control, use `compute_subnetworks_list_usable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_list_usable(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksListUsableArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UsableSubnetworksAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_list_usable_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProject,
    )?;
    compute_subnetworks_list_usable_execute(builder)
}

/// PATCH projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Patches the specified subnetwork with the data included in the request. Only certain fields can be updated with a patch request as indicated in the field descriptions. You must specify the current fingerprint of the subnetwork resource being patched.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_patch_execute()` to send, or `compute_subnetworks_patch` for simplest API.

pub fn compute_subnetworks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    subnetwork: &String,
    drainTimeoutSeconds: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}",
        project, region, subnetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = drainTimeoutSeconds.as_ref() {
        query_parts.push(format!("drainTimeoutSeconds={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Patches the specified subnetwork with the data included in the request. Only certain fields can be updated with a patch request as indicated in the field descriptions. You must specify the current fingerprint of the subnetwork resource being patched.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_patch_execute()` or `compute_subnetworks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Patches the specified subnetwork with the data included in the request. Only certain fields can be updated with a patch request as indicated in the field descriptions. You must specify the current fingerprint of the subnetwork resource being patched.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_patch_task()`.
/// For the simplest API, use `compute_subnetworks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_subnetworks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: subnetwork
    pub subnetwork: String,
    /// Query parameter: drainTimeoutSeconds
    pub drainTimeoutSeconds: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Patches the specified subnetwork with the data included in the request. Only certain fields can be updated with a patch request as indicated in the field descriptions. You must specify the current fingerprint of the subnetwork resource being patched.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_patch_builder()` + `compute_subnetworks_patch_execute()`.
/// For task-level control, use `compute_subnetworks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_patch(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_patch_builder(
        client,
        &args.project,
        &args.region,
        &args.subnetwork,
        &args.drainTimeoutSeconds,
        &args.requestId,
    )?;
    compute_subnetworks_patch_execute(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_set_iam_policy_execute()` to send, or `compute_subnetworks_set_iam_policy` for simplest API.

pub fn compute_subnetworks_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/setIamPolicy",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_set_iam_policy_execute()` or `compute_subnetworks_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_set_iam_policy_task()`.
/// For the simplest API, use `compute_subnetworks_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_subnetworks_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_set_iam_policy_builder()` + `compute_subnetworks_set_iam_policy_execute()`.
/// For task-level control, use `compute_subnetworks_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_set_iam_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_subnetworks_set_iam_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess
/// Set whether VMs in this subnet can access Google services without assigning external IP addresses through Private Google Access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_set_private_ip_google_access_execute()` to send, or `compute_subnetworks_set_private_ip_google_access` for simplest API.

pub fn compute_subnetworks_set_private_ip_google_access_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    subnetwork: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/setPrivateIpGoogleAccess",
        project,
        region,
        subnetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess
/// Set whether VMs in this subnet can access Google services without assigning external IP addresses through Private Google Access.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_set_private_ip_google_access_execute()` or `compute_subnetworks_set_private_ip_google_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_set_private_ip_google_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_set_private_ip_google_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess
/// Set whether VMs in this subnet can access Google services without assigning external IP addresses through Private Google Access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_set_private_ip_google_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_set_private_ip_google_access_task()`.
/// For the simplest API, use `compute_subnetworks_set_private_ip_google_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_set_private_ip_google_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_set_private_ip_google_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_subnetworks_set_private_ip_google_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_set_private_ip_google_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksSetPrivateIpGoogleAccessArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: subnetwork
    pub subnetwork: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess
/// Set whether VMs in this subnet can access Google services without assigning external IP addresses through Private Google Access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_set_private_ip_google_access_builder()` + `compute_subnetworks_set_private_ip_google_access_execute()`.
/// For task-level control, use `compute_subnetworks_set_private_ip_google_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_set_private_ip_google_access(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksSetPrivateIpGoogleAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_set_private_ip_google_access_builder(
        client,
        &args.project,
        &args.region,
        &args.subnetwork,
        &args.requestId,
    )?;
    compute_subnetworks_set_private_ip_google_access_execute(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_test_iam_permissions_execute()` to send, or `compute_subnetworks_test_iam_permissions` for simplest API.

pub fn compute_subnetworks_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_subnetworks_test_iam_permissions_execute()` or `compute_subnetworks_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_subnetworks_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_subnetworks_test_iam_permissions_task()`.
/// For the simplest API, use `compute_subnetworks_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_subnetworks_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_subnetworks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_subnetworks_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_subnetworks_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeSubnetworksTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_test_iam_permissions_builder()` + `compute_subnetworks_test_iam_permissions_execute()`.
/// For task-level control, use `compute_subnetworks_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeSubnetworksTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_subnetworks_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Deletes the specified TargetGrpcProxy in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_delete_execute()` to send, or `compute_target_grpc_proxies_delete` for simplest API.

pub fn compute_target_grpc_proxies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetGrpcProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies/{}",
        project, targetGrpcProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Deletes the specified TargetGrpcProxy in the given scope
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_grpc_proxies_delete_execute()` or `compute_target_grpc_proxies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Deletes the specified TargetGrpcProxy in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_grpc_proxies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_grpc_proxies_delete_task()`.
/// For the simplest API, use `compute_target_grpc_proxies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_grpc_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_grpc_proxies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_grpc_proxies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetGrpcProxy
    pub targetGrpcProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Deletes the specified TargetGrpcProxy in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_delete_builder()` + `compute_target_grpc_proxies_delete_execute()`.
/// For task-level control, use `compute_target_grpc_proxies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_delete(
    client: &SimpleHttpClient,
    args: &ComputeTargetGrpcProxiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_grpc_proxies_delete_builder(
        client,
        &args.project,
        &args.targetGrpcProxy,
        &args.requestId,
    )?;
    compute_target_grpc_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Returns the specified TargetGrpcProxy resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_get_execute()` to send, or `compute_target_grpc_proxies_get` for simplest API.

pub fn compute_target_grpc_proxies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetGrpcProxy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies/{}",
        project, targetGrpcProxy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Returns the specified TargetGrpcProxy resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_grpc_proxies_get_execute()` or `compute_target_grpc_proxies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetGrpcProxy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetGrpcProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Returns the specified TargetGrpcProxy resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_grpc_proxies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_grpc_proxies_get_task()`.
/// For the simplest API, use `compute_target_grpc_proxies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_grpc_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetGrpcProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_grpc_proxies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_grpc_proxies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetGrpcProxy
    pub targetGrpcProxy: String,
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Returns the specified TargetGrpcProxy resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_get_builder()` + `compute_target_grpc_proxies_get_execute()`.
/// For task-level control, use `compute_target_grpc_proxies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_get(
    client: &SimpleHttpClient,
    args: &ComputeTargetGrpcProxiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetGrpcProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_grpc_proxies_get_builder(client, &args.project, &args.targetGrpcProxy)?;
    compute_target_grpc_proxies_get_execute(builder)
}

/// POST projects/{project}/global/targetGrpcProxies
/// Creates a TargetGrpcProxy in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_insert_execute()` to send, or `compute_target_grpc_proxies_insert` for simplest API.

pub fn compute_target_grpc_proxies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetGrpcProxies
/// Creates a TargetGrpcProxy in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_grpc_proxies_insert_execute()` or `compute_target_grpc_proxies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetGrpcProxies
/// Creates a TargetGrpcProxy in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_grpc_proxies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_grpc_proxies_insert_task()`.
/// For the simplest API, use `compute_target_grpc_proxies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_grpc_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_grpc_proxies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_grpc_proxies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetGrpcProxies
/// Creates a TargetGrpcProxy in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_insert_builder()` + `compute_target_grpc_proxies_insert_execute()`.
/// For task-level control, use `compute_target_grpc_proxies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_insert(
    client: &SimpleHttpClient,
    args: &ComputeTargetGrpcProxiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_grpc_proxies_insert_builder(client, &args.project, &args.requestId)?;
    compute_target_grpc_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetGrpcProxies
/// Lists the TargetGrpcProxies for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_list_execute()` to send, or `compute_target_grpc_proxies_list` for simplest API.

pub fn compute_target_grpc_proxies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetGrpcProxies
/// Lists the TargetGrpcProxies for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_grpc_proxies_list_execute()` or `compute_target_grpc_proxies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetGrpcProxyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetGrpcProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetGrpcProxies
/// Lists the TargetGrpcProxies for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_grpc_proxies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_grpc_proxies_list_task()`.
/// For the simplest API, use `compute_target_grpc_proxies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_grpc_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetGrpcProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_grpc_proxies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_grpc_proxies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/targetGrpcProxies
/// Lists the TargetGrpcProxies for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_list_builder()` + `compute_target_grpc_proxies_list_execute()`.
/// For task-level control, use `compute_target_grpc_proxies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetGrpcProxiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetGrpcProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_grpc_proxies_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_target_grpc_proxies_list_execute(builder)
}

/// PATCH projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Patches the specified TargetGrpcProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_patch_execute()` to send, or `compute_target_grpc_proxies_patch` for simplest API.

pub fn compute_target_grpc_proxies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetGrpcProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies/{}",
        project, targetGrpcProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Patches the specified TargetGrpcProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_grpc_proxies_patch_execute()` or `compute_target_grpc_proxies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Patches the specified TargetGrpcProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_grpc_proxies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_grpc_proxies_patch_task()`.
/// For the simplest API, use `compute_target_grpc_proxies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_grpc_proxies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_grpc_proxies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_grpc_proxies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_grpc_proxies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetGrpcProxy
    pub targetGrpcProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Patches the specified TargetGrpcProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_patch_builder()` + `compute_target_grpc_proxies_patch_execute()`.
/// For task-level control, use `compute_target_grpc_proxies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_patch(
    client: &SimpleHttpClient,
    args: &ComputeTargetGrpcProxiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_grpc_proxies_patch_builder(
        client,
        &args.project,
        &args.targetGrpcProxy,
        &args.requestId,
    )?;
    compute_target_grpc_proxies_patch_execute(builder)
}

/// GET projects/{project}/aggregated/targetHttpProxies
/// Retrieves the list of all TargetHttpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_aggregated_list_execute()` to send, or `compute_target_http_proxies_aggregated_list` for simplest API.

pub fn compute_target_http_proxies_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetHttpProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/targetHttpProxies
/// Retrieves the list of all TargetHttpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_http_proxies_aggregated_list_execute()` or `compute_target_http_proxies_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxyAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/targetHttpProxies
/// Retrieves the list of all TargetHttpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_http_proxies_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_http_proxies_aggregated_list_task()`.
/// For the simplest API, use `compute_target_http_proxies_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_http_proxies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetHttpProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_http_proxies_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_http_proxies_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/targetHttpProxies
/// Retrieves the list of all TargetHttpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_aggregated_list_builder()` + `compute_target_http_proxies_aggregated_list_execute()`.
/// For task-level control, use `compute_target_http_proxies_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpProxiesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetHttpProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_target_http_proxies_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_delete_execute()` to send, or `compute_target_http_proxies_delete` for simplest API.

pub fn compute_target_http_proxies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies/{}",
        project, targetHttpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_http_proxies_delete_execute()` or `compute_target_http_proxies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_http_proxies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_http_proxies_delete_task()`.
/// For the simplest API, use `compute_target_http_proxies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_http_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_http_proxies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_http_proxies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpProxy
    pub targetHttpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_delete_builder()` + `compute_target_http_proxies_delete_execute()`.
/// For task-level control, use `compute_target_http_proxies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_delete(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpProxiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_delete_builder(
        client,
        &args.project,
        &args.targetHttpProxy,
        &args.requestId,
    )?;
    compute_target_http_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_get_execute()` to send, or `compute_target_http_proxies_get` for simplest API.

pub fn compute_target_http_proxies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpProxy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies/{}",
        project, targetHttpProxy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_http_proxies_get_execute()` or `compute_target_http_proxies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_http_proxies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_http_proxies_get_task()`.
/// For the simplest API, use `compute_target_http_proxies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_http_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_http_proxies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_http_proxies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpProxy
    pub targetHttpProxy: String,
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_get_builder()` + `compute_target_http_proxies_get_execute()`.
/// For task-level control, use `compute_target_http_proxies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_get(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpProxiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_http_proxies_get_builder(client, &args.project, &args.targetHttpProxy)?;
    compute_target_http_proxies_get_execute(builder)
}

/// POST projects/{project}/global/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_insert_execute()` to send, or `compute_target_http_proxies_insert` for simplest API.

pub fn compute_target_http_proxies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_http_proxies_insert_execute()` or `compute_target_http_proxies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_http_proxies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_http_proxies_insert_task()`.
/// For the simplest API, use `compute_target_http_proxies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_http_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_http_proxies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_http_proxies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_insert_builder()` + `compute_target_http_proxies_insert_execute()`.
/// For task-level control, use `compute_target_http_proxies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_insert(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpProxiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_http_proxies_insert_builder(client, &args.project, &args.requestId)?;
    compute_target_http_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_list_execute()` to send, or `compute_target_http_proxies_list` for simplest API.

pub fn compute_target_http_proxies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_http_proxies_list_execute()` or `compute_target_http_proxies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_http_proxies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_http_proxies_list_task()`.
/// For the simplest API, use `compute_target_http_proxies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_http_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_http_proxies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_http_proxies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_list_builder()` + `compute_target_http_proxies_list_execute()`.
/// For task-level control, use `compute_target_http_proxies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpProxiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_target_http_proxies_list_execute(builder)
}

/// PATCH projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Patches the specified TargetHttpProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_patch_execute()` to send, or `compute_target_http_proxies_patch` for simplest API.

pub fn compute_target_http_proxies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies/{}",
        project, targetHttpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Patches the specified TargetHttpProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_http_proxies_patch_execute()` or `compute_target_http_proxies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Patches the specified TargetHttpProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_http_proxies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_http_proxies_patch_task()`.
/// For the simplest API, use `compute_target_http_proxies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_http_proxies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_http_proxies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_http_proxies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpProxy
    pub targetHttpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Patches the specified TargetHttpProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_patch_builder()` + `compute_target_http_proxies_patch_execute()`.
/// For task-level control, use `compute_target_http_proxies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_patch(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpProxiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_patch_builder(
        client,
        &args.project,
        &args.targetHttpProxy,
        &args.requestId,
    )?;
    compute_target_http_proxies_patch_execute(builder)
}

/// POST projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_set_url_map_execute()` to send, or `compute_target_http_proxies_set_url_map` for simplest API.

pub fn compute_target_http_proxies_set_url_map_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/targetHttpProxies/{}/setUrlMap",
        project, targetHttpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_http_proxies_set_url_map_execute()` or `compute_target_http_proxies_set_url_map`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_set_url_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_set_url_map_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_http_proxies_set_url_map_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_http_proxies_set_url_map_task()`.
/// For the simplest API, use `compute_target_http_proxies_set_url_map()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_http_proxies_set_url_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_http_proxies_set_url_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_http_proxies_set_url_map_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_http_proxies_set_url_map`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesSetUrlMapArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpProxy
    pub targetHttpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_set_url_map_builder()` + `compute_target_http_proxies_set_url_map_execute()`.
/// For task-level control, use `compute_target_http_proxies_set_url_map_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_set_url_map(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpProxiesSetUrlMapArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_set_url_map_builder(
        client,
        &args.project,
        &args.targetHttpProxy,
        &args.requestId,
    )?;
    compute_target_http_proxies_set_url_map_execute(builder)
}

/// GET projects/{project}/aggregated/targetHttpsProxies
/// Retrieves the list of all TargetHttpsProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_aggregated_list_execute()` to send, or `compute_target_https_proxies_aggregated_list` for simplest API.

pub fn compute_target_https_proxies_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetHttpsProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/targetHttpsProxies
/// Retrieves the list of all TargetHttpsProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_aggregated_list_execute()` or `compute_target_https_proxies_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxyAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/targetHttpsProxies
/// Retrieves the list of all TargetHttpsProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_aggregated_list_task()`.
/// For the simplest API, use `compute_target_https_proxies_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetHttpsProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/targetHttpsProxies
/// Retrieves the list of all TargetHttpsProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_aggregated_list_builder()` + `compute_target_https_proxies_aggregated_list_execute()`.
/// For task-level control, use `compute_target_https_proxies_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetHttpsProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_target_https_proxies_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_delete_execute()` to send, or `compute_target_https_proxies_delete` for simplest API.

pub fn compute_target_https_proxies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}",
        project, targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_delete_execute()` or `compute_target_https_proxies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_delete_task()`.
/// For the simplest API, use `compute_target_https_proxies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_delete_builder()` + `compute_target_https_proxies_delete_execute()`.
/// For task-level control, use `compute_target_https_proxies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_delete(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_delete_builder(
        client,
        &args.project,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_target_https_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_get_execute()` to send, or `compute_target_https_proxies_get` for simplest API.

pub fn compute_target_https_proxies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpsProxy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}",
        project, targetHttpsProxy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_get_execute()` or `compute_target_https_proxies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_get_task()`.
/// For the simplest API, use `compute_target_https_proxies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_get_builder()` + `compute_target_https_proxies_get_execute()`.
/// For task-level control, use `compute_target_https_proxies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_get(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_https_proxies_get_builder(client, &args.project, &args.targetHttpsProxy)?;
    compute_target_https_proxies_get_execute(builder)
}

/// POST projects/{project}/global/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_insert_execute()` to send, or `compute_target_https_proxies_insert` for simplest API.

pub fn compute_target_https_proxies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_insert_execute()` or `compute_target_https_proxies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_insert_task()`.
/// For the simplest API, use `compute_target_https_proxies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_insert_builder()` + `compute_target_https_proxies_insert_execute()`.
/// For task-level control, use `compute_target_https_proxies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_insert(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_https_proxies_insert_builder(client, &args.project, &args.requestId)?;
    compute_target_https_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_list_execute()` to send, or `compute_target_https_proxies_list` for simplest API.

pub fn compute_target_https_proxies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_list_execute()` or `compute_target_https_proxies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_list_task()`.
/// For the simplest API, use `compute_target_https_proxies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_list_builder()` + `compute_target_https_proxies_list_execute()`.
/// For task-level control, use `compute_target_https_proxies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_target_https_proxies_list_execute(builder)
}

/// PATCH projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_patch_execute()` to send, or `compute_target_https_proxies_patch` for simplest API.

pub fn compute_target_https_proxies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}",
        project, targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_patch_execute()` or `compute_target_https_proxies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_patch_task()`.
/// For the simplest API, use `compute_target_https_proxies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_patch_builder()` + `compute_target_https_proxies_patch_execute()`.
/// For task-level control, use `compute_target_https_proxies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_patch(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_patch_builder(
        client,
        &args.project,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_target_https_proxies_patch_execute(builder)
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap
/// Changes the Certificate Map for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_certificate_map_execute()` to send, or `compute_target_https_proxies_set_certificate_map` for simplest API.

pub fn compute_target_https_proxies_set_certificate_map_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}/setCertificateMap",
        project,
        targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap
/// Changes the Certificate Map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_set_certificate_map_execute()` or `compute_target_https_proxies_set_certificate_map`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_certificate_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_certificate_map_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap
/// Changes the Certificate Map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_set_certificate_map_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_set_certificate_map_task()`.
/// For the simplest API, use `compute_target_https_proxies_set_certificate_map()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_certificate_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_set_certificate_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_set_certificate_map_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_set_certificate_map`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetCertificateMapArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap
/// Changes the Certificate Map for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_certificate_map_builder()` + `compute_target_https_proxies_set_certificate_map_execute()`.
/// For task-level control, use `compute_target_https_proxies_set_certificate_map_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_certificate_map(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesSetCertificateMapArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_certificate_map_builder(
        client,
        &args.project,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_target_https_proxies_set_certificate_map_execute(builder)
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride
/// Sets the QUIC override policy for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_quic_override_execute()` to send, or `compute_target_https_proxies_set_quic_override` for simplest API.

pub fn compute_target_https_proxies_set_quic_override_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}/setQuicOverride",
        project,
        targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride
/// Sets the QUIC override policy for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_set_quic_override_execute()` or `compute_target_https_proxies_set_quic_override`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_quic_override_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_quic_override_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride
/// Sets the QUIC override policy for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_set_quic_override_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_set_quic_override_task()`.
/// For the simplest API, use `compute_target_https_proxies_set_quic_override()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_quic_override_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_set_quic_override_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_set_quic_override_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_set_quic_override`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetQuicOverrideArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride
/// Sets the QUIC override policy for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_quic_override_builder()` + `compute_target_https_proxies_set_quic_override_execute()`.
/// For task-level control, use `compute_target_https_proxies_set_quic_override_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_quic_override(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesSetQuicOverrideArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_quic_override_builder(
        client,
        &args.project,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_target_https_proxies_set_quic_override_execute(builder)
}

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_ssl_certificates_execute()` to send, or `compute_target_https_proxies_set_ssl_certificates` for simplest API.

pub fn compute_target_https_proxies_set_ssl_certificates_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/targetHttpsProxies/{}/setSslCertificates",
        project,
        targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_set_ssl_certificates_execute()` or `compute_target_https_proxies_set_ssl_certificates`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_ssl_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_ssl_certificates_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_set_ssl_certificates_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_set_ssl_certificates_task()`.
/// For the simplest API, use `compute_target_https_proxies_set_ssl_certificates()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_ssl_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_set_ssl_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_set_ssl_certificates_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_set_ssl_certificates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetSslCertificatesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_ssl_certificates_builder()` + `compute_target_https_proxies_set_ssl_certificates_execute()`.
/// For task-level control, use `compute_target_https_proxies_set_ssl_certificates_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_ssl_certificates(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesSetSslCertificatesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_ssl_certificates_builder(
        client,
        &args.project,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_target_https_proxies_set_ssl_certificates_execute(builder)
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy
/// Sets the SSL policy for TargetHttpsProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the HTTPS proxy load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_ssl_policy_execute()` to send, or `compute_target_https_proxies_set_ssl_policy` for simplest API.

pub fn compute_target_https_proxies_set_ssl_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}/setSslPolicy",
        project,
        targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy
/// Sets the SSL policy for TargetHttpsProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the HTTPS proxy load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_set_ssl_policy_execute()` or `compute_target_https_proxies_set_ssl_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_ssl_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_ssl_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy
/// Sets the SSL policy for TargetHttpsProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the HTTPS proxy load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_set_ssl_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_set_ssl_policy_task()`.
/// For the simplest API, use `compute_target_https_proxies_set_ssl_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_ssl_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_set_ssl_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_set_ssl_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_set_ssl_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetSslPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy
/// Sets the SSL policy for TargetHttpsProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the HTTPS proxy load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_ssl_policy_builder()` + `compute_target_https_proxies_set_ssl_policy_execute()`.
/// For task-level control, use `compute_target_https_proxies_set_ssl_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_ssl_policy(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesSetSslPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_ssl_policy_builder(
        client,
        &args.project,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_target_https_proxies_set_ssl_policy_execute(builder)
}

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_url_map_execute()` to send, or `compute_target_https_proxies_set_url_map` for simplest API.

pub fn compute_target_https_proxies_set_url_map_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetHttpsProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/targetHttpsProxies/{}/setUrlMap",
        project, targetHttpsProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_https_proxies_set_url_map_execute()` or `compute_target_https_proxies_set_url_map`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_url_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_url_map_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_https_proxies_set_url_map_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_https_proxies_set_url_map_task()`.
/// For the simplest API, use `compute_target_https_proxies_set_url_map()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_https_proxies_set_url_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_https_proxies_set_url_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_https_proxies_set_url_map_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_https_proxies_set_url_map`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetUrlMapArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetHttpsProxy
    pub targetHttpsProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_url_map_builder()` + `compute_target_https_proxies_set_url_map_execute()`.
/// For task-level control, use `compute_target_https_proxies_set_url_map_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_url_map(
    client: &SimpleHttpClient,
    args: &ComputeTargetHttpsProxiesSetUrlMapArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_url_map_builder(
        client,
        &args.project,
        &args.targetHttpsProxy,
        &args.requestId,
    )?;
    compute_target_https_proxies_set_url_map_execute(builder)
}

/// GET projects/{project}/aggregated/targetInstances
/// Retrieves an aggregated list of target instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_aggregated_list_execute()` to send, or `compute_target_instances_aggregated_list` for simplest API.

pub fn compute_target_instances_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetInstances",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/targetInstances
/// Retrieves an aggregated list of target instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_instances_aggregated_list_execute()` or `compute_target_instances_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetInstanceAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetInstanceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/targetInstances
/// Retrieves an aggregated list of target instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_instances_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_instances_aggregated_list_task()`.
/// For the simplest API, use `compute_target_instances_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_instances_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetInstanceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_instances_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_instances_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetInstancesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/targetInstances
/// Retrieves an aggregated list of target instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_aggregated_list_builder()` + `compute_target_instances_aggregated_list_execute()`.
/// For task-level control, use `compute_target_instances_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetInstancesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetInstanceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_instances_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_target_instances_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Deletes the specified TargetInstance resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_delete_execute()` to send, or `compute_target_instances_delete` for simplest API.

pub fn compute_target_instances_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    targetInstance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances/{}",
        project, zone, targetInstance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Deletes the specified TargetInstance resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_instances_delete_execute()` or `compute_target_instances_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Deletes the specified TargetInstance resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_instances_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_instances_delete_task()`.
/// For the simplest API, use `compute_target_instances_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_instances_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetInstancesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: targetInstance
    pub targetInstance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Deletes the specified TargetInstance resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_delete_builder()` + `compute_target_instances_delete_execute()`.
/// For task-level control, use `compute_target_instances_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_delete(
    client: &SimpleHttpClient,
    args: &ComputeTargetInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_instances_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.targetInstance,
        &args.requestId,
    )?;
    compute_target_instances_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Returns the specified TargetInstance resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_get_execute()` to send, or `compute_target_instances_get` for simplest API.

pub fn compute_target_instances_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    targetInstance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances/{}",
        project, zone, targetInstance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Returns the specified TargetInstance resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_instances_get_execute()` or `compute_target_instances_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetInstance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Returns the specified TargetInstance resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_instances_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_instances_get_task()`.
/// For the simplest API, use `compute_target_instances_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_instances_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetInstancesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: targetInstance
    pub targetInstance: String,
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Returns the specified TargetInstance resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_get_builder()` + `compute_target_instances_get_execute()`.
/// For task-level control, use `compute_target_instances_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_get(
    client: &SimpleHttpClient,
    args: &ComputeTargetInstancesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_instances_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.targetInstance,
    )?;
    compute_target_instances_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/targetInstances
/// Creates a TargetInstance resource in the specified project and zone using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_insert_execute()` to send, or `compute_target_instances_insert` for simplest API.

pub fn compute_target_instances_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/targetInstances
/// Creates a TargetInstance resource in the specified project and zone using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_instances_insert_execute()` or `compute_target_instances_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/targetInstances
/// Creates a TargetInstance resource in the specified project and zone using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_instances_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_instances_insert_task()`.
/// For the simplest API, use `compute_target_instances_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_instances_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_instances_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_instances_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetInstancesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/targetInstances
/// Creates a TargetInstance resource in the specified project and zone using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_insert_builder()` + `compute_target_instances_insert_execute()`.
/// For task-level control, use `compute_target_instances_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_insert(
    client: &SimpleHttpClient,
    args: &ComputeTargetInstancesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_instances_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
    )?;
    compute_target_instances_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Retrieves a list of TargetInstance resources available to the specified project and zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_list_execute()` to send, or `compute_target_instances_list` for simplest API.

pub fn compute_target_instances_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Retrieves a list of TargetInstance resources available to the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_instances_list_execute()` or `compute_target_instances_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetInstanceList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetInstanceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Retrieves a list of TargetInstance resources available to the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_instances_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_instances_list_task()`.
/// For the simplest API, use `compute_target_instances_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetInstanceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_instances_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetInstancesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Retrieves a list of TargetInstance resources available to the specified project and zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_list_builder()` + `compute_target_instances_list_execute()`.
/// For task-level control, use `compute_target_instances_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetInstancesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetInstanceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_instances_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_target_instances_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_set_security_policy_execute()` to send, or `compute_target_instances_set_security_policy` for simplest API.

pub fn compute_target_instances_set_security_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    targetInstance: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances/{}/setSecurityPolicy",
        project,
        zone,
        targetInstance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_instances_set_security_policy_execute()` or `compute_target_instances_set_security_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_set_security_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_instances_set_security_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_instances_set_security_policy_task()`.
/// For the simplest API, use `compute_target_instances_set_security_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_instances_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_instances_set_security_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_instances_set_security_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetInstancesSetSecurityPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: targetInstance
    pub targetInstance: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_set_security_policy_builder()` + `compute_target_instances_set_security_policy_execute()`.
/// For task-level control, use `compute_target_instances_set_security_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_set_security_policy(
    client: &SimpleHttpClient,
    args: &ComputeTargetInstancesSetSecurityPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_instances_set_security_policy_builder(
        client,
        &args.project,
        &args.zone,
        &args.targetInstance,
        &args.requestId,
    )?;
    compute_target_instances_set_security_policy_execute(builder)
}

/// POST projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_test_iam_permissions_execute()` to send, or `compute_target_instances_test_iam_permissions` for simplest API.

pub fn compute_target_instances_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances/{}/testIamPermissions",
        project,
        zone,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_instances_test_iam_permissions_execute()` or `compute_target_instances_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_instances_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_instances_test_iam_permissions_task()`.
/// For the simplest API, use `compute_target_instances_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_instances_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_instances_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_instances_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_instances_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetInstancesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_test_iam_permissions_builder()` + `compute_target_instances_test_iam_permissions_execute()`.
/// For task-level control, use `compute_target_instances_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeTargetInstancesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_instances_test_iam_permissions_builder(
        client,
        &args.project,
        &args.zone,
        &args.resource,
    )?;
    compute_target_instances_test_iam_permissions_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck
/// Adds health check URLs to a target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_add_health_check_execute()` to send, or `compute_target_pools_add_health_check` for simplest API.

pub fn compute_target_pools_add_health_check_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/addHealthCheck",
        project,
        region,
        targetPool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck
/// Adds health check URLs to a target pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_add_health_check_execute()` or `compute_target_pools_add_health_check`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_add_health_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_add_health_check_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck
/// Adds health check URLs to a target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_add_health_check_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_add_health_check_task()`.
/// For the simplest API, use `compute_target_pools_add_health_check()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_add_health_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_add_health_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_add_health_check_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_add_health_check`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsAddHealthCheckArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck
/// Adds health check URLs to a target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_add_health_check_builder()` + `compute_target_pools_add_health_check_execute()`.
/// For task-level control, use `compute_target_pools_add_health_check_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_add_health_check(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsAddHealthCheckArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_add_health_check_builder(
        client,
        &args.project,
        &args.region,
        &args.targetPool,
        &args.requestId,
    )?;
    compute_target_pools_add_health_check_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance
/// Adds an instance to a target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_add_instance_execute()` to send, or `compute_target_pools_add_instance` for simplest API.

pub fn compute_target_pools_add_instance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/addInstance",
        project,
        region,
        targetPool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance
/// Adds an instance to a target pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_add_instance_execute()` or `compute_target_pools_add_instance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_add_instance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_add_instance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance
/// Adds an instance to a target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_add_instance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_add_instance_task()`.
/// For the simplest API, use `compute_target_pools_add_instance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_add_instance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_add_instance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_add_instance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_add_instance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsAddInstanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance
/// Adds an instance to a target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_add_instance_builder()` + `compute_target_pools_add_instance_execute()`.
/// For task-level control, use `compute_target_pools_add_instance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_add_instance(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsAddInstanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_add_instance_builder(
        client,
        &args.project,
        &args.region,
        &args.targetPool,
        &args.requestId,
    )?;
    compute_target_pools_add_instance_execute(builder)
}

/// GET projects/{project}/aggregated/targetPools
/// Retrieves an aggregated list of target pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_aggregated_list_execute()` to send, or `compute_target_pools_aggregated_list` for simplest API.

pub fn compute_target_pools_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetPools",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/targetPools
/// Retrieves an aggregated list of target pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_aggregated_list_execute()` or `compute_target_pools_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetPoolAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetPoolAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/targetPools
/// Retrieves an aggregated list of target pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_aggregated_list_task()`.
/// For the simplest API, use `compute_target_pools_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_pools_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/targetPools
/// Retrieves an aggregated list of target pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_aggregated_list_builder()` + `compute_target_pools_aggregated_list_execute()`.
/// For task-level control, use `compute_target_pools_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_pools_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_target_pools_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/targetPools/{targetPool}
/// Deletes the specified target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_delete_execute()` to send, or `compute_target_pools_delete` for simplest API.

pub fn compute_target_pools_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}",
        project, region, targetPool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/targetPools/{targetPool}
/// Deletes the specified target pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_delete_execute()` or `compute_target_pools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/targetPools/{targetPool}
/// Deletes the specified target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_delete_task()`.
/// For the simplest API, use `compute_target_pools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/targetPools/{targetPool}
/// Deletes the specified target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_delete_builder()` + `compute_target_pools_delete_execute()`.
/// For task-level control, use `compute_target_pools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_delete(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.targetPool,
        &args.requestId,
    )?;
    compute_target_pools_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Returns the specified target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_get_execute()` to send, or `compute_target_pools_get` for simplest API.

pub fn compute_target_pools_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}",
        project, region, targetPool,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Returns the specified target pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_get_execute()` or `compute_target_pools_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetPool>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetPool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Returns the specified target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_get_task()`.
/// For the simplest API, use `compute_target_pools_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Returns the specified target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_get_builder()` + `compute_target_pools_get_execute()`.
/// For task-level control, use `compute_target_pools_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_get(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_pools_get_builder(client, &args.project, &args.region, &args.targetPool)?;
    compute_target_pools_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth
/// Gets the most recent health check results for each IP for the instance that is referenced by the given target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_get_health_execute()` to send, or `compute_target_pools_get_health` for simplest API.

pub fn compute_target_pools_get_health_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/getHealth",
        project, region, targetPool,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth
/// Gets the most recent health check results for each IP for the instance that is referenced by the given target pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_get_health_execute()` or `compute_target_pools_get_health`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_get_health_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetPoolInstanceHealth>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetPoolInstanceHealth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth
/// Gets the most recent health check results for each IP for the instance that is referenced by the given target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_get_health_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_get_health_task()`.
/// For the simplest API, use `compute_target_pools_get_health()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_get_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_get_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolInstanceHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_pools_get_health_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_get_health`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsGetHealthArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth
/// Gets the most recent health check results for each IP for the instance that is referenced by the given target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_get_health_builder()` + `compute_target_pools_get_health_execute()`.
/// For task-level control, use `compute_target_pools_get_health_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_get_health(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsGetHealthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolInstanceHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_pools_get_health_builder(
        client,
        &args.project,
        &args.region,
        &args.targetPool,
    )?;
    compute_target_pools_get_health_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools
/// Creates a target pool in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_insert_execute()` to send, or `compute_target_pools_insert` for simplest API.

pub fn compute_target_pools_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools
/// Creates a target pool in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_insert_execute()` or `compute_target_pools_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools
/// Creates a target pool in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_insert_task()`.
/// For the simplest API, use `compute_target_pools_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetPools
/// Creates a target pool in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_insert_builder()` + `compute_target_pools_insert_execute()`.
/// For task-level control, use `compute_target_pools_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_insert(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_pools_insert_builder(client, &args.project, &args.region, &args.requestId)?;
    compute_target_pools_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools
/// Retrieves a list of target pools available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_list_execute()` to send, or `compute_target_pools_list` for simplest API.

pub fn compute_target_pools_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetPools
/// Retrieves a list of target pools available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_list_execute()` or `compute_target_pools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetPoolList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetPoolList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetPools
/// Retrieves a list of target pools available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_list_task()`.
/// For the simplest API, use `compute_target_pools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_pools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/targetPools
/// Retrieves a list of target pools available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_list_builder()` + `compute_target_pools_list_execute()`.
/// For task-level control, use `compute_target_pools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_pools_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_target_pools_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck
/// Removes health check URL from a target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_remove_health_check_execute()` to send, or `compute_target_pools_remove_health_check` for simplest API.

pub fn compute_target_pools_remove_health_check_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/removeHealthCheck",
        project,
        region,
        targetPool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck
/// Removes health check URL from a target pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_remove_health_check_execute()` or `compute_target_pools_remove_health_check`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_remove_health_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_remove_health_check_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck
/// Removes health check URL from a target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_remove_health_check_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_remove_health_check_task()`.
/// For the simplest API, use `compute_target_pools_remove_health_check()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_remove_health_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_remove_health_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_remove_health_check_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_remove_health_check`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsRemoveHealthCheckArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck
/// Removes health check URL from a target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_remove_health_check_builder()` + `compute_target_pools_remove_health_check_execute()`.
/// For task-level control, use `compute_target_pools_remove_health_check_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_remove_health_check(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsRemoveHealthCheckArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_remove_health_check_builder(
        client,
        &args.project,
        &args.region,
        &args.targetPool,
        &args.requestId,
    )?;
    compute_target_pools_remove_health_check_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance
/// Removes instance URL from a target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_remove_instance_execute()` to send, or `compute_target_pools_remove_instance` for simplest API.

pub fn compute_target_pools_remove_instance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/removeInstance",
        project,
        region,
        targetPool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance
/// Removes instance URL from a target pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_remove_instance_execute()` or `compute_target_pools_remove_instance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_remove_instance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_remove_instance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance
/// Removes instance URL from a target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_remove_instance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_remove_instance_task()`.
/// For the simplest API, use `compute_target_pools_remove_instance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_remove_instance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_remove_instance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_remove_instance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_remove_instance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsRemoveInstanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance
/// Removes instance URL from a target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_remove_instance_builder()` + `compute_target_pools_remove_instance_execute()`.
/// For task-level control, use `compute_target_pools_remove_instance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_remove_instance(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsRemoveInstanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_remove_instance_builder(
        client,
        &args.project,
        &args.region,
        &args.targetPool,
        &args.requestId,
    )?;
    compute_target_pools_remove_instance_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup
/// Changes a backup target pool's configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_set_backup_execute()` to send, or `compute_target_pools_set_backup` for simplest API.

pub fn compute_target_pools_set_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
    failoverRatio: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/setBackup",
        project, region, targetPool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = failoverRatio.as_ref() {
        query_parts.push(format!("failoverRatio={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup
/// Changes a backup target pool's configurations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_set_backup_execute()` or `compute_target_pools_set_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_set_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_set_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup
/// Changes a backup target pool's configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_set_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_set_backup_task()`.
/// For the simplest API, use `compute_target_pools_set_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_set_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_set_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_set_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_set_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsSetBackupArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
    /// Query parameter: failoverRatio
    pub failoverRatio: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup
/// Changes a backup target pool's configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_set_backup_builder()` + `compute_target_pools_set_backup_execute()`.
/// For task-level control, use `compute_target_pools_set_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_set_backup(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsSetBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_set_backup_builder(
        client,
        &args.project,
        &args.region,
        &args.targetPool,
        &args.failoverRatio,
        &args.requestId,
    )?;
    compute_target_pools_set_backup_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target pool. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_set_security_policy_execute()` to send, or `compute_target_pools_set_security_policy` for simplest API.

pub fn compute_target_pools_set_security_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetPool: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/setSecurityPolicy",
        project,
        region,
        targetPool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target pool. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_set_security_policy_execute()` or `compute_target_pools_set_security_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_set_security_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target pool. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_set_security_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_set_security_policy_task()`.
/// For the simplest API, use `compute_target_pools_set_security_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_set_security_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_pools_set_security_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_set_security_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsSetSecurityPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetPool
    pub targetPool: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target pool. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_set_security_policy_builder()` + `compute_target_pools_set_security_policy_execute()`.
/// For task-level control, use `compute_target_pools_set_security_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_set_security_policy(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsSetSecurityPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_set_security_policy_builder(
        client,
        &args.project,
        &args.region,
        &args.targetPool,
        &args.requestId,
    )?;
    compute_target_pools_set_security_policy_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_test_iam_permissions_execute()` to send, or `compute_target_pools_test_iam_permissions` for simplest API.

pub fn compute_target_pools_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_pools_test_iam_permissions_execute()` or `compute_target_pools_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_pools_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_pools_test_iam_permissions_task()`.
/// For the simplest API, use `compute_target_pools_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_pools_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_pools_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetPoolsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_test_iam_permissions_builder()` + `compute_target_pools_test_iam_permissions_execute()`.
/// For task-level control, use `compute_target_pools_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeTargetPoolsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_pools_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_target_pools_test_iam_permissions_execute(builder)
}

/// DELETE projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Deletes the specified TargetSslProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_delete_execute()` to send, or `compute_target_ssl_proxies_delete` for simplest API.

pub fn compute_target_ssl_proxies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetSslProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}",
        project, targetSslProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Deletes the specified TargetSslProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_delete_execute()` or `compute_target_ssl_proxies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Deletes the specified TargetSslProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_delete_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetSslProxy
    pub targetSslProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Deletes the specified TargetSslProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_delete_builder()` + `compute_target_ssl_proxies_delete_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_delete(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_delete_builder(
        client,
        &args.project,
        &args.targetSslProxy,
        &args.requestId,
    )?;
    compute_target_ssl_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Returns the specified TargetSslProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_get_execute()` to send, or `compute_target_ssl_proxies_get` for simplest API.

pub fn compute_target_ssl_proxies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetSslProxy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}",
        project, targetSslProxy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Returns the specified TargetSslProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_get_execute()` or `compute_target_ssl_proxies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetSslProxy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetSslProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Returns the specified TargetSslProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_get_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetSslProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetSslProxy
    pub targetSslProxy: String,
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Returns the specified TargetSslProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_get_builder()` + `compute_target_ssl_proxies_get_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_get(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetSslProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_ssl_proxies_get_builder(client, &args.project, &args.targetSslProxy)?;
    compute_target_ssl_proxies_get_execute(builder)
}

/// POST projects/{project}/global/targetSslProxies
/// Creates a TargetSslProxy resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_insert_execute()` to send, or `compute_target_ssl_proxies_insert` for simplest API.

pub fn compute_target_ssl_proxies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetSslProxies
/// Creates a TargetSslProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_insert_execute()` or `compute_target_ssl_proxies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetSslProxies
/// Creates a TargetSslProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_insert_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetSslProxies
/// Creates a TargetSslProxy resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_insert_builder()` + `compute_target_ssl_proxies_insert_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_insert(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_ssl_proxies_insert_builder(client, &args.project, &args.requestId)?;
    compute_target_ssl_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies
/// Retrieves the list of TargetSslProxy resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_list_execute()` to send, or `compute_target_ssl_proxies_list` for simplest API.

pub fn compute_target_ssl_proxies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetSslProxies
/// Retrieves the list of TargetSslProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_list_execute()` or `compute_target_ssl_proxies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetSslProxyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetSslProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetSslProxies
/// Retrieves the list of TargetSslProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_list_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetSslProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/targetSslProxies
/// Retrieves the list of TargetSslProxy resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_list_builder()` + `compute_target_ssl_proxies_list_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetSslProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_target_ssl_proxies_list_execute(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService
/// Changes the BackendService for TargetSslProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_backend_service_execute()` to send, or `compute_target_ssl_proxies_set_backend_service` for simplest API.

pub fn compute_target_ssl_proxies_set_backend_service_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetSslProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setBackendService",
        project,
        targetSslProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService
/// Changes the BackendService for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_set_backend_service_execute()` or `compute_target_ssl_proxies_set_backend_service`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_backend_service_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_backend_service_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService
/// Changes the BackendService for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_set_backend_service_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_set_backend_service_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_set_backend_service()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_backend_service_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_set_backend_service_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_set_backend_service_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_set_backend_service`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetBackendServiceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetSslProxy
    pub targetSslProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService
/// Changes the BackendService for TargetSslProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_backend_service_builder()` + `compute_target_ssl_proxies_set_backend_service_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_set_backend_service_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_backend_service(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesSetBackendServiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_backend_service_builder(
        client,
        &args.project,
        &args.targetSslProxy,
        &args.requestId,
    )?;
    compute_target_ssl_proxies_set_backend_service_execute(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap
/// Changes the Certificate Map for TargetSslProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_certificate_map_execute()` to send, or `compute_target_ssl_proxies_set_certificate_map` for simplest API.

pub fn compute_target_ssl_proxies_set_certificate_map_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetSslProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setCertificateMap",
        project,
        targetSslProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap
/// Changes the Certificate Map for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_set_certificate_map_execute()` or `compute_target_ssl_proxies_set_certificate_map`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_certificate_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_certificate_map_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap
/// Changes the Certificate Map for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_set_certificate_map_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_set_certificate_map_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_set_certificate_map()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_certificate_map_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_set_certificate_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_set_certificate_map_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_set_certificate_map`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetCertificateMapArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetSslProxy
    pub targetSslProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap
/// Changes the Certificate Map for TargetSslProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_certificate_map_builder()` + `compute_target_ssl_proxies_set_certificate_map_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_set_certificate_map_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_certificate_map(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesSetCertificateMapArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_certificate_map_builder(
        client,
        &args.project,
        &args.targetSslProxy,
        &args.requestId,
    )?;
    compute_target_ssl_proxies_set_certificate_map_execute(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetSslProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_proxy_header_execute()` to send, or `compute_target_ssl_proxies_set_proxy_header` for simplest API.

pub fn compute_target_ssl_proxies_set_proxy_header_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetSslProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setProxyHeader",
        project,
        targetSslProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_set_proxy_header_execute()` or `compute_target_ssl_proxies_set_proxy_header`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_proxy_header_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_proxy_header_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_set_proxy_header_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_set_proxy_header_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_set_proxy_header()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_proxy_header_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_set_proxy_header_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_set_proxy_header_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_set_proxy_header`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetProxyHeaderArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetSslProxy
    pub targetSslProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetSslProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_proxy_header_builder()` + `compute_target_ssl_proxies_set_proxy_header_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_set_proxy_header_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_proxy_header(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesSetProxyHeaderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_proxy_header_builder(
        client,
        &args.project,
        &args.targetSslProxy,
        &args.requestId,
    )?;
    compute_target_ssl_proxies_set_proxy_header_execute(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates
/// Changes SslCertificates for TargetSslProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_ssl_certificates_execute()` to send, or `compute_target_ssl_proxies_set_ssl_certificates` for simplest API.

pub fn compute_target_ssl_proxies_set_ssl_certificates_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetSslProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setSslCertificates",
        project,
        targetSslProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates
/// Changes SslCertificates for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_set_ssl_certificates_execute()` or `compute_target_ssl_proxies_set_ssl_certificates`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_ssl_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_ssl_certificates_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates
/// Changes SslCertificates for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_set_ssl_certificates_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_set_ssl_certificates_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_set_ssl_certificates()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_ssl_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_set_ssl_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_set_ssl_certificates_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_set_ssl_certificates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetSslCertificatesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetSslProxy
    pub targetSslProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates
/// Changes SslCertificates for TargetSslProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_ssl_certificates_builder()` + `compute_target_ssl_proxies_set_ssl_certificates_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_set_ssl_certificates_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_ssl_certificates(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesSetSslCertificatesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_ssl_certificates_builder(
        client,
        &args.project,
        &args.targetSslProxy,
        &args.requestId,
    )?;
    compute_target_ssl_proxies_set_ssl_certificates_execute(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy
/// Sets the SSL policy for TargetSslProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_ssl_policy_execute()` to send, or `compute_target_ssl_proxies_set_ssl_policy` for simplest API.

pub fn compute_target_ssl_proxies_set_ssl_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetSslProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setSslPolicy",
        project,
        targetSslProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy
/// Sets the SSL policy for TargetSslProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_set_ssl_policy_execute()` or `compute_target_ssl_proxies_set_ssl_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_ssl_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_ssl_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy
/// Sets the SSL policy for TargetSslProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_set_ssl_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_set_ssl_policy_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_set_ssl_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_set_ssl_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_set_ssl_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_set_ssl_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_set_ssl_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetSslPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetSslProxy
    pub targetSslProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy
/// Sets the SSL policy for TargetSslProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_ssl_policy_builder()` + `compute_target_ssl_proxies_set_ssl_policy_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_set_ssl_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_ssl_policy(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesSetSslPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_ssl_policy_builder(
        client,
        &args.project,
        &args.targetSslProxy,
        &args.requestId,
    )?;
    compute_target_ssl_proxies_set_ssl_policy_execute(builder)
}

/// POST projects/{project}/global/targetSslProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_test_iam_permissions_execute()` to send, or `compute_target_ssl_proxies_test_iam_permissions` for simplest API.

pub fn compute_target_ssl_proxies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetSslProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_ssl_proxies_test_iam_permissions_execute()` or `compute_target_ssl_proxies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetSslProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_ssl_proxies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_ssl_proxies_test_iam_permissions_task()`.
/// For the simplest API, use `compute_target_ssl_proxies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_ssl_proxies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_ssl_proxies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_ssl_proxies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_ssl_proxies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/targetSslProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_test_iam_permissions_builder()` + `compute_target_ssl_proxies_test_iam_permissions_execute()`.
/// For task-level control, use `compute_target_ssl_proxies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeTargetSslProxiesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_target_ssl_proxies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/targetTcpProxies
/// Retrieves the list of all TargetTcpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_aggregated_list_execute()` to send, or `compute_target_tcp_proxies_aggregated_list` for simplest API.

pub fn compute_target_tcp_proxies_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetTcpProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/targetTcpProxies
/// Retrieves the list of all TargetTcpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_tcp_proxies_aggregated_list_execute()` or `compute_target_tcp_proxies_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxyAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/targetTcpProxies
/// Retrieves the list of all TargetTcpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_tcp_proxies_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_tcp_proxies_aggregated_list_task()`.
/// For the simplest API, use `compute_target_tcp_proxies_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_tcp_proxies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetTcpProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_tcp_proxies_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_tcp_proxies_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/targetTcpProxies
/// Retrieves the list of all TargetTcpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_aggregated_list_builder()` + `compute_target_tcp_proxies_aggregated_list_execute()`.
/// For task-level control, use `compute_target_tcp_proxies_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetTcpProxiesAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetTcpProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_target_tcp_proxies_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_delete_execute()` to send, or `compute_target_tcp_proxies_delete` for simplest API.

pub fn compute_target_tcp_proxies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetTcpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}",
        project, targetTcpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_tcp_proxies_delete_execute()` or `compute_target_tcp_proxies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_tcp_proxies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_tcp_proxies_delete_task()`.
/// For the simplest API, use `compute_target_tcp_proxies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_tcp_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_tcp_proxies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_tcp_proxies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetTcpProxy
    pub targetTcpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_delete_builder()` + `compute_target_tcp_proxies_delete_execute()`.
/// For task-level control, use `compute_target_tcp_proxies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_delete(
    client: &SimpleHttpClient,
    args: &ComputeTargetTcpProxiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_delete_builder(
        client,
        &args.project,
        &args.targetTcpProxy,
        &args.requestId,
    )?;
    compute_target_tcp_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_get_execute()` to send, or `compute_target_tcp_proxies_get` for simplest API.

pub fn compute_target_tcp_proxies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetTcpProxy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}",
        project, targetTcpProxy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_tcp_proxies_get_execute()` or `compute_target_tcp_proxies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_tcp_proxies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_tcp_proxies_get_task()`.
/// For the simplest API, use `compute_target_tcp_proxies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_tcp_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_tcp_proxies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_tcp_proxies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetTcpProxy
    pub targetTcpProxy: String,
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_get_builder()` + `compute_target_tcp_proxies_get_execute()`.
/// For task-level control, use `compute_target_tcp_proxies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_get(
    client: &SimpleHttpClient,
    args: &ComputeTargetTcpProxiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_tcp_proxies_get_builder(client, &args.project, &args.targetTcpProxy)?;
    compute_target_tcp_proxies_get_execute(builder)
}

/// POST projects/{project}/global/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_insert_execute()` to send, or `compute_target_tcp_proxies_insert` for simplest API.

pub fn compute_target_tcp_proxies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_tcp_proxies_insert_execute()` or `compute_target_tcp_proxies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_tcp_proxies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_tcp_proxies_insert_task()`.
/// For the simplest API, use `compute_target_tcp_proxies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_tcp_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_tcp_proxies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_tcp_proxies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_insert_builder()` + `compute_target_tcp_proxies_insert_execute()`.
/// For task-level control, use `compute_target_tcp_proxies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_insert(
    client: &SimpleHttpClient,
    args: &ComputeTargetTcpProxiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_tcp_proxies_insert_builder(client, &args.project, &args.requestId)?;
    compute_target_tcp_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies
/// Retrieves the list of TargetTcpProxy resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_list_execute()` to send, or `compute_target_tcp_proxies_list` for simplest API.

pub fn compute_target_tcp_proxies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/targetTcpProxies
/// Retrieves the list of TargetTcpProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_tcp_proxies_list_execute()` or `compute_target_tcp_proxies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/targetTcpProxies
/// Retrieves the list of TargetTcpProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_tcp_proxies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_tcp_proxies_list_task()`.
/// For the simplest API, use `compute_target_tcp_proxies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_tcp_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_tcp_proxies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_tcp_proxies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/targetTcpProxies
/// Retrieves the list of TargetTcpProxy resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_list_builder()` + `compute_target_tcp_proxies_list_execute()`.
/// For task-level control, use `compute_target_tcp_proxies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetTcpProxiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_target_tcp_proxies_list_execute(builder)
}

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService
/// Changes the BackendService for TargetTcpProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_set_backend_service_execute()` to send, or `compute_target_tcp_proxies_set_backend_service` for simplest API.

pub fn compute_target_tcp_proxies_set_backend_service_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetTcpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}/setBackendService",
        project,
        targetTcpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService
/// Changes the BackendService for TargetTcpProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_tcp_proxies_set_backend_service_execute()` or `compute_target_tcp_proxies_set_backend_service`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_set_backend_service_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_set_backend_service_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService
/// Changes the BackendService for TargetTcpProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_tcp_proxies_set_backend_service_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_tcp_proxies_set_backend_service_task()`.
/// For the simplest API, use `compute_target_tcp_proxies_set_backend_service()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_set_backend_service_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_tcp_proxies_set_backend_service_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_tcp_proxies_set_backend_service_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_tcp_proxies_set_backend_service`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesSetBackendServiceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetTcpProxy
    pub targetTcpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService
/// Changes the BackendService for TargetTcpProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_set_backend_service_builder()` + `compute_target_tcp_proxies_set_backend_service_execute()`.
/// For task-level control, use `compute_target_tcp_proxies_set_backend_service_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_set_backend_service(
    client: &SimpleHttpClient,
    args: &ComputeTargetTcpProxiesSetBackendServiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_set_backend_service_builder(
        client,
        &args.project,
        &args.targetTcpProxy,
        &args.requestId,
    )?;
    compute_target_tcp_proxies_set_backend_service_execute(builder)
}

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetTcpProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_set_proxy_header_execute()` to send, or `compute_target_tcp_proxies_set_proxy_header` for simplest API.

pub fn compute_target_tcp_proxies_set_proxy_header_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    targetTcpProxy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}/setProxyHeader",
        project,
        targetTcpProxy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetTcpProxy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_tcp_proxies_set_proxy_header_execute()` or `compute_target_tcp_proxies_set_proxy_header`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_set_proxy_header_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_set_proxy_header_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetTcpProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_tcp_proxies_set_proxy_header_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_tcp_proxies_set_proxy_header_task()`.
/// For the simplest API, use `compute_target_tcp_proxies_set_proxy_header()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_set_proxy_header_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_tcp_proxies_set_proxy_header_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_tcp_proxies_set_proxy_header_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_tcp_proxies_set_proxy_header`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesSetProxyHeaderArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: targetTcpProxy
    pub targetTcpProxy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetTcpProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_set_proxy_header_builder()` + `compute_target_tcp_proxies_set_proxy_header_execute()`.
/// For task-level control, use `compute_target_tcp_proxies_set_proxy_header_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_set_proxy_header(
    client: &SimpleHttpClient,
    args: &ComputeTargetTcpProxiesSetProxyHeaderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_set_proxy_header_builder(
        client,
        &args.project,
        &args.targetTcpProxy,
        &args.requestId,
    )?;
    compute_target_tcp_proxies_set_proxy_header_execute(builder)
}

/// POST projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_test_iam_permissions_execute()` to send, or `compute_target_tcp_proxies_test_iam_permissions` for simplest API.

pub fn compute_target_tcp_proxies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_tcp_proxies_test_iam_permissions_execute()` or `compute_target_tcp_proxies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_tcp_proxies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_tcp_proxies_test_iam_permissions_task()`.
/// For the simplest API, use `compute_target_tcp_proxies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_tcp_proxies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_tcp_proxies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_tcp_proxies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_tcp_proxies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_test_iam_permissions_builder()` + `compute_target_tcp_proxies_test_iam_permissions_execute()`.
/// For task-level control, use `compute_target_tcp_proxies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeTargetTcpProxiesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
    )?;
    compute_target_tcp_proxies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/targetVpnGateways
/// Retrieves an aggregated list of target VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_aggregated_list_execute()` to send, or `compute_target_vpn_gateways_aggregated_list` for simplest API.

pub fn compute_target_vpn_gateways_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetVpnGateways",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/targetVpnGateways
/// Retrieves an aggregated list of target VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_vpn_gateways_aggregated_list_execute()` or `compute_target_vpn_gateways_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetVpnGatewayAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetVpnGatewayAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/targetVpnGateways
/// Retrieves an aggregated list of target VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_vpn_gateways_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_vpn_gateways_aggregated_list_task()`.
/// For the simplest API, use `compute_target_vpn_gateways_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_vpn_gateways_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetVpnGatewayAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_vpn_gateways_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_vpn_gateways_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/targetVpnGateways
/// Retrieves an aggregated list of target VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_aggregated_list_builder()` + `compute_target_vpn_gateways_aggregated_list_execute()`.
/// For task-level control, use `compute_target_vpn_gateways_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetVpnGatewaysAggregatedListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetVpnGatewayAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_target_vpn_gateways_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Deletes the specified target VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_delete_execute()` to send, or `compute_target_vpn_gateways_delete` for simplest API.

pub fn compute_target_vpn_gateways_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetVpnGateway: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways/{}",
        project, region, targetVpnGateway,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Deletes the specified target VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_vpn_gateways_delete_execute()` or `compute_target_vpn_gateways_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Deletes the specified target VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_vpn_gateways_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_vpn_gateways_delete_task()`.
/// For the simplest API, use `compute_target_vpn_gateways_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_vpn_gateways_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_vpn_gateways_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_vpn_gateways_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetVpnGateway
    pub targetVpnGateway: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Deletes the specified target VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_delete_builder()` + `compute_target_vpn_gateways_delete_execute()`.
/// For task-level control, use `compute_target_vpn_gateways_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_delete(
    client: &SimpleHttpClient,
    args: &ComputeTargetVpnGatewaysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.targetVpnGateway,
        &args.requestId,
    )?;
    compute_target_vpn_gateways_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Returns the specified target VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_get_execute()` to send, or `compute_target_vpn_gateways_get` for simplest API.

pub fn compute_target_vpn_gateways_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    targetVpnGateway: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways/{}",
        project, region, targetVpnGateway,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Returns the specified target VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_vpn_gateways_get_execute()` or `compute_target_vpn_gateways_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetVpnGateway>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetVpnGateway = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Returns the specified target VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_vpn_gateways_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_vpn_gateways_get_task()`.
/// For the simplest API, use `compute_target_vpn_gateways_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_vpn_gateways_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetVpnGateway>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_vpn_gateways_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_vpn_gateways_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: targetVpnGateway
    pub targetVpnGateway: String,
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Returns the specified target VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_get_builder()` + `compute_target_vpn_gateways_get_execute()`.
/// For task-level control, use `compute_target_vpn_gateways_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_get(
    client: &SimpleHttpClient,
    args: &ComputeTargetVpnGatewaysGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetVpnGateway>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_get_builder(
        client,
        &args.project,
        &args.region,
        &args.targetVpnGateway,
    )?;
    compute_target_vpn_gateways_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetVpnGateways
/// Creates a target VPN gateway in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_insert_execute()` to send, or `compute_target_vpn_gateways_insert` for simplest API.

pub fn compute_target_vpn_gateways_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetVpnGateways
/// Creates a target VPN gateway in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_vpn_gateways_insert_execute()` or `compute_target_vpn_gateways_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetVpnGateways
/// Creates a target VPN gateway in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_vpn_gateways_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_vpn_gateways_insert_task()`.
/// For the simplest API, use `compute_target_vpn_gateways_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_vpn_gateways_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_vpn_gateways_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_vpn_gateways_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetVpnGateways
/// Creates a target VPN gateway in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_insert_builder()` + `compute_target_vpn_gateways_insert_execute()`.
/// For task-level control, use `compute_target_vpn_gateways_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_insert(
    client: &SimpleHttpClient,
    args: &ComputeTargetVpnGatewaysInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_insert_builder(
        client,
        &args.project,
        &args.region,
        &args.requestId,
    )?;
    compute_target_vpn_gateways_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Retrieves a list of target VPN gateways available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_list_execute()` to send, or `compute_target_vpn_gateways_list` for simplest API.

pub fn compute_target_vpn_gateways_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Retrieves a list of target VPN gateways available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_vpn_gateways_list_execute()` or `compute_target_vpn_gateways_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetVpnGatewayList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetVpnGatewayList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Retrieves a list of target VPN gateways available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_vpn_gateways_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_vpn_gateways_list_task()`.
/// For the simplest API, use `compute_target_vpn_gateways_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_vpn_gateways_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetVpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_target_vpn_gateways_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_vpn_gateways_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Retrieves a list of target VPN gateways available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_list_builder()` + `compute_target_vpn_gateways_list_execute()`.
/// For task-level control, use `compute_target_vpn_gateways_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_list(
    client: &SimpleHttpClient,
    args: &ComputeTargetVpnGatewaysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetVpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_target_vpn_gateways_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels
/// Sets the labels on a TargetVpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_set_labels_execute()` to send, or `compute_target_vpn_gateways_set_labels` for simplest API.

pub fn compute_target_vpn_gateways_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways/{}/setLabels",
        project,
        region,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels
/// Sets the labels on a TargetVpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_target_vpn_gateways_set_labels_execute()` or `compute_target_vpn_gateways_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels
/// Sets the labels on a TargetVpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_target_vpn_gateways_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_target_vpn_gateways_set_labels_task()`.
/// For the simplest API, use `compute_target_vpn_gateways_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_target_vpn_gateways_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_target_vpn_gateways_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_target_vpn_gateways_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_target_vpn_gateways_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels
/// Sets the labels on a TargetVpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_set_labels_builder()` + `compute_target_vpn_gateways_set_labels_execute()`.
/// For task-level control, use `compute_target_vpn_gateways_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeTargetVpnGatewaysSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_target_vpn_gateways_set_labels_execute(builder)
}

/// GET projects/{project}/aggregated/urlMaps
/// Retrieves the list of all UrlMap resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_aggregated_list_execute()` to send, or `compute_url_maps_aggregated_list` for simplest API.

pub fn compute_url_maps_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/urlMaps",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/urlMaps
/// Retrieves the list of all UrlMap resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_aggregated_list_execute()` or `compute_url_maps_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapsAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapsAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/urlMaps
/// Retrieves the list of all UrlMap resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_aggregated_list_task()`.
/// For the simplest API, use `compute_url_maps_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_url_maps_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/urlMaps
/// Retrieves the list of all UrlMap resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_aggregated_list_builder()` + `compute_url_maps_aggregated_list_execute()`.
/// For task-level control, use `compute_url_maps_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_url_maps_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_url_maps_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/global/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_delete_execute()` to send, or `compute_url_maps_delete` for simplest API.

pub fn compute_url_maps_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    urlMap: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}",
        project, urlMap,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_delete_execute()` or `compute_url_maps_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_delete_task()`.
/// For the simplest API, use `compute_url_maps_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_url_maps_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: urlMap
    pub urlMap: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_delete_builder()` + `compute_url_maps_delete_execute()`.
/// For task-level control, use `compute_url_maps_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_delete(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_url_maps_delete_builder(client, &args.project, &args.urlMap, &args.requestId)?;
    compute_url_maps_delete_execute(builder)
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_get_execute()` to send, or `compute_url_maps_get` for simplest API.

pub fn compute_url_maps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    urlMap: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}",
        project, urlMap,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_get_execute()` or `compute_url_maps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMap>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_get_task()`.
/// For the simplest API, use `compute_url_maps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_url_maps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: urlMap
    pub urlMap: String,
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_get_builder()` + `compute_url_maps_get_execute()`.
/// For task-level control, use `compute_url_maps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_get(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_get_builder(client, &args.project, &args.urlMap)?;
    compute_url_maps_get_execute(builder)
}

/// POST projects/{project}/global/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_insert_execute()` to send, or `compute_url_maps_insert` for simplest API.

pub fn compute_url_maps_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_insert_execute()` or `compute_url_maps_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_insert_task()`.
/// For the simplest API, use `compute_url_maps_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_url_maps_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_insert_builder()` + `compute_url_maps_insert_execute()`.
/// For task-level control, use `compute_url_maps_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_insert(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_insert_builder(client, &args.project, &args.requestId)?;
    compute_url_maps_insert_execute(builder)
}

/// POST projects/{project}/global/urlMaps/{urlMap}/invalidateCache
/// Initiates a cache invalidation operation, invalidating the specified path, scoped to the specified UrlMap.  For more information, see [Invalidating cached content](/`cdn/docs/invalidating-cached-content`).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_invalidate_cache_execute()` to send, or `compute_url_maps_invalidate_cache` for simplest API.

pub fn compute_url_maps_invalidate_cache_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    urlMap: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}/invalidateCache",
        project, urlMap,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/urlMaps/{urlMap}/invalidateCache
/// Initiates a cache invalidation operation, invalidating the specified path, scoped to the specified UrlMap.  For more information, see [Invalidating cached content](/`cdn/docs/invalidating-cached-content`).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_invalidate_cache_execute()` or `compute_url_maps_invalidate_cache`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_invalidate_cache_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_invalidate_cache_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/urlMaps/{urlMap}/invalidateCache
/// Initiates a cache invalidation operation, invalidating the specified path, scoped to the specified UrlMap.  For more information, see [Invalidating cached content](/`cdn/docs/invalidating-cached-content`).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_invalidate_cache_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_invalidate_cache_task()`.
/// For the simplest API, use `compute_url_maps_invalidate_cache()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_invalidate_cache_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_invalidate_cache_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_url_maps_invalidate_cache_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_invalidate_cache`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsInvalidateCacheArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: urlMap
    pub urlMap: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/global/urlMaps/{urlMap}/invalidateCache
/// Initiates a cache invalidation operation, invalidating the specified path, scoped to the specified UrlMap.  For more information, see [Invalidating cached content](/`cdn/docs/invalidating-cached-content`).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_invalidate_cache_builder()` + `compute_url_maps_invalidate_cache_execute()`.
/// For task-level control, use `compute_url_maps_invalidate_cache_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_invalidate_cache(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsInvalidateCacheArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_invalidate_cache_builder(
        client,
        &args.project,
        &args.urlMap,
        &args.requestId,
    )?;
    compute_url_maps_invalidate_cache_execute(builder)
}

/// GET projects/{project}/global/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_list_execute()` to send, or `compute_url_maps_list` for simplest API.

pub fn compute_url_maps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_list_execute()` or `compute_url_maps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_list_task()`.
/// For the simplest API, use `compute_url_maps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_url_maps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_list_builder()` + `compute_url_maps_list_execute()`.
/// For task-level control, use `compute_url_maps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_list(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_url_maps_list_execute(builder)
}

/// PATCH projects/{project}/global/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_patch_execute()` to send, or `compute_url_maps_patch` for simplest API.

pub fn compute_url_maps_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    urlMap: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}",
        project, urlMap,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_patch_execute()` or `compute_url_maps_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_patch_task()`.
/// For the simplest API, use `compute_url_maps_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_url_maps_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: urlMap
    pub urlMap: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/global/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_patch_builder()` + `compute_url_maps_patch_execute()`.
/// For task-level control, use `compute_url_maps_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_patch(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_url_maps_patch_builder(client, &args.project, &args.urlMap, &args.requestId)?;
    compute_url_maps_patch_execute(builder)
}

/// POST projects/{project}/global/urlMaps/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_test_iam_permissions_execute()` to send, or `compute_url_maps_test_iam_permissions` for simplest API.

pub fn compute_url_maps_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/urlMaps/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_test_iam_permissions_execute()` or `compute_url_maps_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/urlMaps/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_test_iam_permissions_task()`.
/// For the simplest API, use `compute_url_maps_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_url_maps_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/global/urlMaps/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_test_iam_permissions_builder()` + `compute_url_maps_test_iam_permissions_execute()`.
/// For task-level control, use `compute_url_maps_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_url_maps_test_iam_permissions_builder(client, &args.project, &args.resource)?;
    compute_url_maps_test_iam_permissions_execute(builder)
}

/// PUT projects/{project}/global/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_update_execute()` to send, or `compute_url_maps_update` for simplest API.

pub fn compute_url_maps_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    urlMap: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}",
        project, urlMap,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT projects/{project}/global/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_update_execute()` or `compute_url_maps_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT projects/{project}/global/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_update_task()`.
/// For the simplest API, use `compute_url_maps_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_url_maps_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: urlMap
    pub urlMap: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PUT projects/{project}/global/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_update_builder()` + `compute_url_maps_update_execute()`.
/// For task-level control, use `compute_url_maps_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_update(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_url_maps_update_builder(client, &args.project, &args.urlMap, &args.requestId)?;
    compute_url_maps_update_execute(builder)
}

/// POST projects/{project}/global/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_validate_execute()` to send, or `compute_url_maps_validate` for simplest API.

pub fn compute_url_maps_validate_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    urlMap: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}/validate",
        project, urlMap,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_url_maps_validate_execute()` or `compute_url_maps_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapsValidateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_url_maps_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_url_maps_validate_task()`.
/// For the simplest API, use `compute_url_maps_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_url_maps_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_url_maps_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_url_maps_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_url_maps_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeUrlMapsValidateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: urlMap
    pub urlMap: String,
}

/// POST projects/{project}/global/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_validate_builder()` + `compute_url_maps_validate_execute()`.
/// For task-level control, use `compute_url_maps_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_validate(
    client: &SimpleHttpClient,
    args: &ComputeUrlMapsValidateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_url_maps_validate_builder(client, &args.project, &args.urlMap)?;
    compute_url_maps_validate_execute(builder)
}

/// GET projects/{project}/aggregated/vpnGateways
/// Retrieves an aggregated list of VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_aggregated_list_execute()` to send, or `compute_vpn_gateways_aggregated_list` for simplest API.

pub fn compute_vpn_gateways_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/vpnGateways",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/vpnGateways
/// Retrieves an aggregated list of VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_gateways_aggregated_list_execute()` or `compute_vpn_gateways_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnGatewayAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnGatewayAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/vpnGateways
/// Retrieves an aggregated list of VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_gateways_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_gateways_aggregated_list_task()`.
/// For the simplest API, use `compute_vpn_gateways_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_gateways_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGatewayAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_vpn_gateways_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_gateways_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/vpnGateways
/// Retrieves an aggregated list of VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_aggregated_list_builder()` + `compute_vpn_gateways_aggregated_list_execute()`.
/// For task-level control, use `compute_vpn_gateways_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeVpnGatewaysAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGatewayAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_vpn_gateways_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Deletes the specified VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_delete_execute()` to send, or `compute_vpn_gateways_delete` for simplest API.

pub fn compute_vpn_gateways_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    vpnGateway: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}",
        project, region, vpnGateway,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Deletes the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_gateways_delete_execute()` or `compute_vpn_gateways_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Deletes the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_gateways_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_gateways_delete_task()`.
/// For the simplest API, use `compute_vpn_gateways_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_gateways_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_vpn_gateways_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_gateways_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: vpnGateway
    pub vpnGateway: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Deletes the specified VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_delete_builder()` + `compute_vpn_gateways_delete_execute()`.
/// For task-level control, use `compute_vpn_gateways_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_delete(
    client: &SimpleHttpClient,
    args: &ComputeVpnGatewaysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.vpnGateway,
        &args.requestId,
    )?;
    compute_vpn_gateways_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Returns the specified VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_get_execute()` to send, or `compute_vpn_gateways_get` for simplest API.

pub fn compute_vpn_gateways_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    vpnGateway: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}",
        project, region, vpnGateway,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Returns the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_gateways_get_execute()` or `compute_vpn_gateways_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnGateway>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnGateway = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Returns the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_gateways_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_gateways_get_task()`.
/// For the simplest API, use `compute_vpn_gateways_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_gateways_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGateway>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_vpn_gateways_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_gateways_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: vpnGateway
    pub vpnGateway: String,
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Returns the specified VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_get_builder()` + `compute_vpn_gateways_get_execute()`.
/// For task-level control, use `compute_vpn_gateways_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_get(
    client: &SimpleHttpClient,
    args: &ComputeVpnGatewaysGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGateway>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_vpn_gateways_get_builder(client, &args.project, &args.region, &args.vpnGateway)?;
    compute_vpn_gateways_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus
/// Returns the status for the specified VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_get_status_execute()` to send, or `compute_vpn_gateways_get_status` for simplest API.

pub fn compute_vpn_gateways_get_status_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    vpnGateway: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}/getStatus",
        project, region, vpnGateway,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus
/// Returns the status for the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_gateways_get_status_execute()` or `compute_vpn_gateways_get_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_get_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_get_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnGatewaysGetStatusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnGatewaysGetStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus
/// Returns the status for the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_gateways_get_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_gateways_get_status_task()`.
/// For the simplest API, use `compute_vpn_gateways_get_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_get_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_gateways_get_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VpnGatewaysGetStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = compute_vpn_gateways_get_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_gateways_get_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysGetStatusArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: vpnGateway
    pub vpnGateway: String,
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus
/// Returns the status for the specified VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_get_status_builder()` + `compute_vpn_gateways_get_status_execute()`.
/// For task-level control, use `compute_vpn_gateways_get_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_get_status(
    client: &SimpleHttpClient,
    args: &ComputeVpnGatewaysGetStatusArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VpnGatewaysGetStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_get_status_builder(
        client,
        &args.project,
        &args.region,
        &args.vpnGateway,
    )?;
    compute_vpn_gateways_get_status_execute(builder)
}

/// POST projects/{project}/regions/{region}/vpnGateways
/// Creates a VPN gateway in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_insert_execute()` to send, or `compute_vpn_gateways_insert` for simplest API.

pub fn compute_vpn_gateways_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/vpnGateways
/// Creates a VPN gateway in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_gateways_insert_execute()` or `compute_vpn_gateways_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/vpnGateways
/// Creates a VPN gateway in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_gateways_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_gateways_insert_task()`.
/// For the simplest API, use `compute_vpn_gateways_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_gateways_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_vpn_gateways_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_gateways_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/vpnGateways
/// Creates a VPN gateway in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_insert_builder()` + `compute_vpn_gateways_insert_execute()`.
/// For task-level control, use `compute_vpn_gateways_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_insert(
    client: &SimpleHttpClient,
    args: &ComputeVpnGatewaysInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_vpn_gateways_insert_builder(client, &args.project, &args.region, &args.requestId)?;
    compute_vpn_gateways_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Retrieves a list of VPN gateways available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_list_execute()` to send, or `compute_vpn_gateways_list` for simplest API.

pub fn compute_vpn_gateways_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Retrieves a list of VPN gateways available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_gateways_list_execute()` or `compute_vpn_gateways_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnGatewayList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnGatewayList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Retrieves a list of VPN gateways available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_gateways_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_gateways_list_task()`.
/// For the simplest API, use `compute_vpn_gateways_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_gateways_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_vpn_gateways_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_gateways_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Retrieves a list of VPN gateways available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_list_builder()` + `compute_vpn_gateways_list_execute()`.
/// For task-level control, use `compute_vpn_gateways_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_list(
    client: &SimpleHttpClient,
    args: &ComputeVpnGatewaysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_vpn_gateways_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels
/// Sets the labels on a VpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_set_labels_execute()` to send, or `compute_vpn_gateways_set_labels` for simplest API.

pub fn compute_vpn_gateways_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}/setLabels",
        project, region, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels
/// Sets the labels on a VpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_gateways_set_labels_execute()` or `compute_vpn_gateways_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels
/// Sets the labels on a VpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_gateways_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_gateways_set_labels_task()`.
/// For the simplest API, use `compute_vpn_gateways_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_gateways_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_vpn_gateways_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_gateways_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels
/// Sets the labels on a VpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_set_labels_builder()` + `compute_vpn_gateways_set_labels_execute()`.
/// For task-level control, use `compute_vpn_gateways_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeVpnGatewaysSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_vpn_gateways_set_labels_execute(builder)
}

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_test_iam_permissions_execute()` to send, or `compute_vpn_gateways_test_iam_permissions` for simplest API.

pub fn compute_vpn_gateways_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_gateways_test_iam_permissions_execute()` or `compute_vpn_gateways_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_gateways_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_gateways_test_iam_permissions_task()`.
/// For the simplest API, use `compute_vpn_gateways_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_gateways_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_gateways_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_vpn_gateways_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_gateways_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
}

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_test_iam_permissions_builder()` + `compute_vpn_gateways_test_iam_permissions_execute()`.
/// For task-level control, use `compute_vpn_gateways_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ComputeVpnGatewaysTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_test_iam_permissions_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
    )?;
    compute_vpn_gateways_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/vpnTunnels
/// Retrieves an aggregated list of VPN tunnels.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_aggregated_list_execute()` to send, or `compute_vpn_tunnels_aggregated_list` for simplest API.

pub fn compute_vpn_tunnels_aggregated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    includeAllScopes: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
    serviceProjectNumber: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/vpnTunnels",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = includeAllScopes.as_ref() {
        query_parts.push(format!("includeAllScopes={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }
    if let Some(val) = serviceProjectNumber.as_ref() {
        query_parts.push(format!("serviceProjectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/aggregated/vpnTunnels
/// Retrieves an aggregated list of VPN tunnels.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_tunnels_aggregated_list_execute()` or `compute_vpn_tunnels_aggregated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_aggregated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnTunnelAggregatedList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnTunnelAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/aggregated/vpnTunnels
/// Retrieves an aggregated list of VPN tunnels.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_tunnels_aggregated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_tunnels_aggregated_list_task()`.
/// For the simplest API, use `compute_vpn_tunnels_aggregated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_aggregated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_tunnels_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnelAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_vpn_tunnels_aggregated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_tunnels_aggregated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsAggregatedListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: includeAllScopes
    pub includeAllScopes: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
    /// Query parameter: serviceProjectNumber
    pub serviceProjectNumber: Option<Option<String>>,
}

/// GET projects/{project}/aggregated/vpnTunnels
/// Retrieves an aggregated list of VPN tunnels.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_aggregated_list_builder()` + `compute_vpn_tunnels_aggregated_list_execute()`.
/// For task-level control, use `compute_vpn_tunnels_aggregated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_aggregated_list(
    client: &SimpleHttpClient,
    args: &ComputeVpnTunnelsAggregatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnelAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_tunnels_aggregated_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.includeAllScopes,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
        &args.serviceProjectNumber,
    )?;
    compute_vpn_tunnels_aggregated_list_execute(builder)
}

/// DELETE projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Deletes the specified VpnTunnel resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_delete_execute()` to send, or `compute_vpn_tunnels_delete` for simplest API.

pub fn compute_vpn_tunnels_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    vpnTunnel: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels/{}",
        project, region, vpnTunnel,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Deletes the specified VpnTunnel resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_tunnels_delete_execute()` or `compute_vpn_tunnels_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Deletes the specified VpnTunnel resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_tunnels_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_tunnels_delete_task()`.
/// For the simplest API, use `compute_vpn_tunnels_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_tunnels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_vpn_tunnels_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_tunnels_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: vpnTunnel
    pub vpnTunnel: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Deletes the specified VpnTunnel resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_delete_builder()` + `compute_vpn_tunnels_delete_execute()`.
/// For task-level control, use `compute_vpn_tunnels_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_delete(
    client: &SimpleHttpClient,
    args: &ComputeVpnTunnelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_tunnels_delete_builder(
        client,
        &args.project,
        &args.region,
        &args.vpnTunnel,
        &args.requestId,
    )?;
    compute_vpn_tunnels_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Returns the specified VpnTunnel resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_get_execute()` to send, or `compute_vpn_tunnels_get` for simplest API.

pub fn compute_vpn_tunnels_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    vpnTunnel: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels/{}",
        project, region, vpnTunnel,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Returns the specified VpnTunnel resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_tunnels_get_execute()` or `compute_vpn_tunnels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnTunnel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnTunnel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Returns the specified VpnTunnel resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_tunnels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_tunnels_get_task()`.
/// For the simplest API, use `compute_vpn_tunnels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_tunnels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_vpn_tunnels_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_tunnels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: vpnTunnel
    pub vpnTunnel: String,
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Returns the specified VpnTunnel resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_get_builder()` + `compute_vpn_tunnels_get_execute()`.
/// For task-level control, use `compute_vpn_tunnels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_get(
    client: &SimpleHttpClient,
    args: &ComputeVpnTunnelsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_vpn_tunnels_get_builder(client, &args.project, &args.region, &args.vpnTunnel)?;
    compute_vpn_tunnels_get_execute(builder)
}

/// POST projects/{project}/regions/{region}/vpnTunnels
/// Creates a VpnTunnel resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_insert_execute()` to send, or `compute_vpn_tunnels_insert` for simplest API.

pub fn compute_vpn_tunnels_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/vpnTunnels
/// Creates a VpnTunnel resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_tunnels_insert_execute()` or `compute_vpn_tunnels_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/vpnTunnels
/// Creates a VpnTunnel resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_tunnels_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_tunnels_insert_task()`.
/// For the simplest API, use `compute_vpn_tunnels_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_tunnels_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_vpn_tunnels_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_tunnels_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/vpnTunnels
/// Creates a VpnTunnel resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_insert_builder()` + `compute_vpn_tunnels_insert_execute()`.
/// For task-level control, use `compute_vpn_tunnels_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_insert(
    client: &SimpleHttpClient,
    args: &ComputeVpnTunnelsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_vpn_tunnels_insert_builder(client, &args.project, &args.region, &args.requestId)?;
    compute_vpn_tunnels_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Retrieves a list of VpnTunnel resources contained in the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_list_execute()` to send, or `compute_vpn_tunnels_list` for simplest API.

pub fn compute_vpn_tunnels_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels",
        project, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Retrieves a list of VpnTunnel resources contained in the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_tunnels_list_execute()` or `compute_vpn_tunnels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnTunnelList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnTunnelList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Retrieves a list of VpnTunnel resources contained in the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_tunnels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_tunnels_list_task()`.
/// For the simplest API, use `compute_vpn_tunnels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_tunnels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnelList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_vpn_tunnels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_tunnels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Retrieves a list of VpnTunnel resources contained in the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_list_builder()` + `compute_vpn_tunnels_list_execute()`.
/// For task-level control, use `compute_vpn_tunnels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_list(
    client: &SimpleHttpClient,
    args: &ComputeVpnTunnelsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnelList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_tunnels_list_builder(
        client,
        &args.project,
        &args.region,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_vpn_tunnels_list_execute(builder)
}

/// POST projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels
/// Sets the labels on a VpnTunnel. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_set_labels_execute()` to send, or `compute_vpn_tunnels_set_labels` for simplest API.

pub fn compute_vpn_tunnels_set_labels_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    region: &String,
    resource: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels/{}/setLabels",
        project, region, resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels
/// Sets the labels on a VpnTunnel. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_vpn_tunnels_set_labels_execute()` or `compute_vpn_tunnels_set_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_set_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels
/// Sets the labels on a VpnTunnel. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_vpn_tunnels_set_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_vpn_tunnels_set_labels_task()`.
/// For the simplest API, use `compute_vpn_tunnels_set_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_vpn_tunnels_set_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_vpn_tunnels_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_vpn_tunnels_set_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_vpn_tunnels_set_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsSetLabelsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels
/// Sets the labels on a VpnTunnel. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_set_labels_builder()` + `compute_vpn_tunnels_set_labels_execute()`.
/// For task-level control, use `compute_vpn_tunnels_set_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_set_labels(
    client: &SimpleHttpClient,
    args: &ComputeVpnTunnelsSetLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_tunnels_set_labels_builder(
        client,
        &args.project,
        &args.region,
        &args.resource,
        &args.requestId,
    )?;
    compute_vpn_tunnels_set_labels_execute(builder)
}

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Deletes the specified wire group in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_delete_execute()` to send, or `compute_wire_groups_delete` for simplest API.

pub fn compute_wire_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    crossSiteNetwork: &String,
    wireGroup: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        project,
        crossSiteNetwork,
        wireGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Deletes the specified wire group in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_wire_groups_delete_execute()` or `compute_wire_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Deletes the specified wire group in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_wire_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_wire_groups_delete_task()`.
/// For the simplest API, use `compute_wire_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_wire_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_wire_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_wire_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeWireGroupsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: crossSiteNetwork
    pub crossSiteNetwork: String,
    /// Path parameter: wireGroup
    pub wireGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Deletes the specified wire group in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_delete_builder()` + `compute_wire_groups_delete_execute()`.
/// For task-level control, use `compute_wire_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_delete(
    client: &SimpleHttpClient,
    args: &ComputeWireGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_delete_builder(
        client,
        &args.project,
        &args.crossSiteNetwork,
        &args.wireGroup,
        &args.requestId,
    )?;
    compute_wire_groups_delete_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Gets the specified wire group resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_get_execute()` to send, or `compute_wire_groups_get` for simplest API.

pub fn compute_wire_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    crossSiteNetwork: &String,
    wireGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        project,
        crossSiteNetwork,
        wireGroup,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Gets the specified wire group resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_wire_groups_get_execute()` or `compute_wire_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WireGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WireGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Gets the specified wire group resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_wire_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_wire_groups_get_task()`.
/// For the simplest API, use `compute_wire_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_wire_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WireGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_wire_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_wire_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeWireGroupsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: crossSiteNetwork
    pub crossSiteNetwork: String,
    /// Path parameter: wireGroup
    pub wireGroup: String,
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Gets the specified wire group resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_get_builder()` + `compute_wire_groups_get_execute()`.
/// For task-level control, use `compute_wire_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_get(
    client: &SimpleHttpClient,
    args: &ComputeWireGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WireGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_get_builder(
        client,
        &args.project,
        &args.crossSiteNetwork,
        &args.wireGroup,
    )?;
    compute_wire_groups_get_execute(builder)
}

/// POST projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Creates a wire group in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_insert_execute()` to send, or `compute_wire_groups_insert` for simplest API.

pub fn compute_wire_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    crossSiteNetwork: &String,
    requestId: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups",
        project,
        crossSiteNetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Creates a wire group in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_wire_groups_insert_execute()` or `compute_wire_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Creates a wire group in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_wire_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_wire_groups_insert_task()`.
/// For the simplest API, use `compute_wire_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_wire_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_wire_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_wire_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeWireGroupsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: crossSiteNetwork
    pub crossSiteNetwork: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Creates a wire group in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_insert_builder()` + `compute_wire_groups_insert_execute()`.
/// For task-level control, use `compute_wire_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_insert(
    client: &SimpleHttpClient,
    args: &ComputeWireGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_insert_builder(
        client,
        &args.project,
        &args.crossSiteNetwork,
        &args.requestId,
        &args.validateOnly,
    )?;
    compute_wire_groups_insert_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Lists the wire groups for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_list_execute()` to send, or `compute_wire_groups_list` for simplest API.

pub fn compute_wire_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    crossSiteNetwork: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups",
        project,
        crossSiteNetwork,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Lists the wire groups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_wire_groups_list_execute()` or `compute_wire_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WireGroupList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WireGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Lists the wire groups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_wire_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_wire_groups_list_task()`.
/// For the simplest API, use `compute_wire_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_wire_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WireGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_wire_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_wire_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeWireGroupsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: crossSiteNetwork
    pub crossSiteNetwork: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Lists the wire groups for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_list_builder()` + `compute_wire_groups_list_execute()`.
/// For task-level control, use `compute_wire_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_list(
    client: &SimpleHttpClient,
    args: &ComputeWireGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WireGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_list_builder(
        client,
        &args.project,
        &args.crossSiteNetwork,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_wire_groups_list_execute(builder)
}

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Updates the specified wire group resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_patch_execute()` to send, or `compute_wire_groups_patch` for simplest API.

pub fn compute_wire_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    crossSiteNetwork: &String,
    wireGroup: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        project,
        crossSiteNetwork,
        wireGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Updates the specified wire group resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_wire_groups_patch_execute()` or `compute_wire_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Updates the specified wire group resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_wire_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_wire_groups_patch_task()`.
/// For the simplest API, use `compute_wire_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_wire_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_wire_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_wire_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_wire_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeWireGroupsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: crossSiteNetwork
    pub crossSiteNetwork: String,
    /// Path parameter: wireGroup
    pub wireGroup: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Updates the specified wire group resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_patch_builder()` + `compute_wire_groups_patch_execute()`.
/// For task-level control, use `compute_wire_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_patch(
    client: &SimpleHttpClient,
    args: &ComputeWireGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_patch_builder(
        client,
        &args.project,
        &args.crossSiteNetwork,
        &args.wireGroup,
        &args.requestId,
        &args.updateMask,
        &args.validateOnly,
    )?;
    compute_wire_groups_patch_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/operations/{operation}
/// Deletes the specified zone-specific Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_operations_delete_execute()` to send, or `compute_zone_operations_delete` for simplest API.

pub fn compute_zone_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/operations/{}",
        project, zone, operation,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/operations/{operation}
/// Deletes the specified zone-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_operations_delete_execute()` or `compute_zone_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/operations/{operation}
/// Deletes the specified zone-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_operations_delete_task()`.
/// For the simplest API, use `compute_zone_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_zone_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneOperationsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: operation
    pub operation: String,
}

/// DELETE projects/{project}/zones/{zone}/operations/{operation}
/// Deletes the specified zone-specific Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_operations_delete_builder()` + `compute_zone_operations_delete_execute()`.
/// For task-level control, use `compute_zone_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_delete(
    client: &SimpleHttpClient,
    args: &ComputeZoneOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_zone_operations_delete_builder(client, &args.project, &args.zone, &args.operation)?;
    compute_zone_operations_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Retrieves the specified zone-specific Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_operations_get_execute()` to send, or `compute_zone_operations_get` for simplest API.

pub fn compute_zone_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/operations/{}",
        project, zone, operation,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Retrieves the specified zone-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_operations_get_execute()` or `compute_zone_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Retrieves the specified zone-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_operations_get_task()`.
/// For the simplest API, use `compute_zone_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_zone_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneOperationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: operation
    pub operation: String,
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Retrieves the specified zone-specific Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_operations_get_builder()` + `compute_zone_operations_get_execute()`.
/// For task-level control, use `compute_zone_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_get(
    client: &SimpleHttpClient,
    args: &ComputeZoneOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_zone_operations_get_builder(client, &args.project, &args.zone, &args.operation)?;
    compute_zone_operations_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/operations
/// Retrieves a list of Operation resources contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_operations_list_execute()` to send, or `compute_zone_operations_list` for simplest API.

pub fn compute_zone_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/operations",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/operations
/// Retrieves a list of Operation resources contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_operations_list_execute()` or `compute_zone_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/operations
/// Retrieves a list of Operation resources contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_operations_list_task()`.
/// For the simplest API, use `compute_zone_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_zone_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneOperationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/operations
/// Retrieves a list of Operation resources contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_operations_list_builder()` + `compute_zone_operations_list_execute()`.
/// For task-level control, use `compute_zone_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_list(
    client: &SimpleHttpClient,
    args: &ComputeZoneOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_zone_operations_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_zone_operations_list_execute(builder)
}

/// POST projects/{project}/zones/{zone}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method waits for no more than the 2 minutes and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_operations_wait_execute()` to send, or `compute_zone_operations_wait` for simplest API.

pub fn compute_zone_operations_wait_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/operations/{}/wait",
        project, zone, operation,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method waits for no more than the 2 minutes and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_operations_wait_execute()` or `compute_zone_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method waits for no more than the 2 minutes and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_operations_wait_task()`.
/// For the simplest API, use `compute_zone_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_zone_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneOperationsWaitArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: operation
    pub operation: String,
}

/// POST projects/{project}/zones/{zone}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method waits for no more than the 2 minutes and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_operations_wait_builder()` + `compute_zone_operations_wait_execute()`.
/// For task-level control, use `compute_zone_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_wait(
    client: &SimpleHttpClient,
    args: &ComputeZoneOperationsWaitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_zone_operations_wait_builder(client, &args.project, &args.zone, &args.operation)?;
    compute_zone_operations_wait_execute(builder)
}

/// DELETE projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Deletes a specified zone VM extension policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_vm_extension_policies_delete_execute()` to send, or `compute_zone_vm_extension_policies_delete` for simplest API.

pub fn compute_zone_vm_extension_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    vmExtensionPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies/{}",
        project, zone, vmExtensionPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Deletes a specified zone VM extension policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_vm_extension_policies_delete_execute()` or `compute_zone_vm_extension_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Deletes a specified zone VM extension policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_vm_extension_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_vm_extension_policies_delete_task()`.
/// For the simplest API, use `compute_zone_vm_extension_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_vm_extension_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_zone_vm_extension_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_vm_extension_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: vmExtensionPolicy
    pub vmExtensionPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Deletes a specified zone VM extension policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_vm_extension_policies_delete_builder()` + `compute_zone_vm_extension_policies_delete_execute()`.
/// For task-level control, use `compute_zone_vm_extension_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_delete(
    client: &SimpleHttpClient,
    args: &ComputeZoneVmExtensionPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zone_vm_extension_policies_delete_builder(
        client,
        &args.project,
        &args.zone,
        &args.vmExtensionPolicy,
        &args.requestId,
    )?;
    compute_zone_vm_extension_policies_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Retrieves details of a specific zone VM extension policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_vm_extension_policies_get_execute()` to send, or `compute_zone_vm_extension_policies_get` for simplest API.

pub fn compute_zone_vm_extension_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    vmExtensionPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies/{}",
        project, zone, vmExtensionPolicy,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Retrieves details of a specific zone VM extension policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_vm_extension_policies_get_execute()` or `compute_zone_vm_extension_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmExtensionPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmExtensionPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Retrieves details of a specific zone VM extension policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_vm_extension_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_vm_extension_policies_get_task()`.
/// For the simplest API, use `compute_zone_vm_extension_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_vm_extension_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmExtensionPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_zone_vm_extension_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_vm_extension_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: vmExtensionPolicy
    pub vmExtensionPolicy: String,
}

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Retrieves details of a specific zone VM extension policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_vm_extension_policies_get_builder()` + `compute_zone_vm_extension_policies_get_execute()`.
/// For task-level control, use `compute_zone_vm_extension_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_get(
    client: &SimpleHttpClient,
    args: &ComputeZoneVmExtensionPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmExtensionPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_zone_vm_extension_policies_get_builder(
        client,
        &args.project,
        &args.zone,
        &args.vmExtensionPolicy,
    )?;
    compute_zone_vm_extension_policies_get_execute(builder)
}

/// POST projects/{project}/zones/{zone}/vmExtensionPolicies
/// Creates a new zone-level VM extension policy within a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_vm_extension_policies_insert_execute()` to send, or `compute_zone_vm_extension_policies_insert` for simplest API.

pub fn compute_zone_vm_extension_policies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST projects/{project}/zones/{zone}/vmExtensionPolicies
/// Creates a new zone-level VM extension policy within a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_vm_extension_policies_insert_execute()` or `compute_zone_vm_extension_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST projects/{project}/zones/{zone}/vmExtensionPolicies
/// Creates a new zone-level VM extension policy within a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_vm_extension_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_vm_extension_policies_insert_task()`.
/// For the simplest API, use `compute_zone_vm_extension_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_vm_extension_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_zone_vm_extension_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_vm_extension_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST projects/{project}/zones/{zone}/vmExtensionPolicies
/// Creates a new zone-level VM extension policy within a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_vm_extension_policies_insert_builder()` + `compute_zone_vm_extension_policies_insert_execute()`.
/// For task-level control, use `compute_zone_vm_extension_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_insert(
    client: &SimpleHttpClient,
    args: &ComputeZoneVmExtensionPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zone_vm_extension_policies_insert_builder(
        client,
        &args.project,
        &args.zone,
        &args.requestId,
    )?;
    compute_zone_vm_extension_policies_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies
/// Lists all VM extension policies within a specific zone for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_vm_extension_policies_list_execute()` to send, or `compute_zone_vm_extension_policies_list` for simplest API.

pub fn compute_zone_vm_extension_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies",
        project, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies
/// Lists all VM extension policies within a specific zone for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_vm_extension_policies_list_execute()` or `compute_zone_vm_extension_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmExtensionPolicyList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmExtensionPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies
/// Lists all VM extension policies within a specific zone for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_vm_extension_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_vm_extension_policies_list_task()`.
/// For the simplest API, use `compute_zone_vm_extension_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_vm_extension_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmExtensionPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = compute_zone_vm_extension_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_vm_extension_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies
/// Lists all VM extension policies within a specific zone for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_vm_extension_policies_list_builder()` + `compute_zone_vm_extension_policies_list_execute()`.
/// For task-level control, use `compute_zone_vm_extension_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_list(
    client: &SimpleHttpClient,
    args: &ComputeZoneVmExtensionPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmExtensionPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_zone_vm_extension_policies_list_builder(
        client,
        &args.project,
        &args.zone,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_zone_vm_extension_policies_list_execute(builder)
}

/// PATCH projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Modifies an existing zone VM extension policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_vm_extension_policies_update_execute()` to send, or `compute_zone_vm_extension_policies_update` for simplest API.

pub fn compute_zone_vm_extension_policies_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
    vmExtensionPolicy: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies/{}",
        project, zone, vmExtensionPolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Modifies an existing zone VM extension policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zone_vm_extension_policies_update_execute()` or `compute_zone_vm_extension_policies_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Modifies an existing zone VM extension policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zone_vm_extension_policies_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zone_vm_extension_policies_update_task()`.
/// For the simplest API, use `compute_zone_vm_extension_policies_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zone_vm_extension_policies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zone_vm_extension_policies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_zone_vm_extension_policies_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zone_vm_extension_policies_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: vmExtensionPolicy
    pub vmExtensionPolicy: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// PATCH projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
/// Modifies an existing zone VM extension policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_vm_extension_policies_update_builder()` + `compute_zone_vm_extension_policies_update_execute()`.
/// For task-level control, use `compute_zone_vm_extension_policies_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_vm_extension_policies_update(
    client: &SimpleHttpClient,
    args: &ComputeZoneVmExtensionPoliciesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zone_vm_extension_policies_update_builder(
        client,
        &args.project,
        &args.zone,
        &args.vmExtensionPolicy,
        &args.requestId,
    )?;
    compute_zone_vm_extension_policies_update_execute(builder)
}

/// GET projects/{project}/zones/{zone}
/// Returns the specified Zone resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zones_get_execute()` to send, or `compute_zones_get` for simplest API.

pub fn compute_zones_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    zone: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}",
        project, zone,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones/{zone}
/// Returns the specified Zone resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zones_get_execute()` or `compute_zones_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zones_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zones_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Zone>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Zone = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones/{zone}
/// Returns the specified Zone resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zones_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zones_get_task()`.
/// For the simplest API, use `compute_zones_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zones_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zones_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Zone>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_zones_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zones_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZonesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: zone
    pub zone: String,
}

/// GET projects/{project}/zones/{zone}
/// Returns the specified Zone resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zones_get_builder()` + `compute_zones_get_execute()`.
/// For task-level control, use `compute_zones_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zones_get(
    client: &SimpleHttpClient,
    args: &ComputeZonesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Zone>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zones_get_builder(client, &args.project, &args.zone)?;
    compute_zones_get_execute(builder)
}

/// GET projects/{project}/zones
/// Retrieves the list of Zone resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zones_list_execute()` to send, or `compute_zones_list` for simplest API.

pub fn compute_zones_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{project}/zones
/// Retrieves the list of Zone resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `compute_zones_list_execute()` or `compute_zones_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zones_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zones_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ZoneList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ZoneList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{project}/zones
/// Retrieves the list of Zone resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `compute_zones_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `compute_zones_list_task()`.
/// For the simplest API, use `compute_zones_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `compute_zones_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn compute_zones_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ZoneList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = compute_zones_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`compute_zones_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeZonesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET projects/{project}/zones
/// Retrieves the list of Zone resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zones_list_builder()` + `compute_zones_list_execute()`.
/// For task-level control, use `compute_zones_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zones_list(
    client: &SimpleHttpClient,
    args: &ComputeZonesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ZoneList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zones_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    compute_zones_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for AcceleratorTypeAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for AcceleratorTypeAggregatedList with ComputeAcceleratorTypesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAcceleratorTypesAggregatedListArgs>
    for AcceleratorTypeAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeAcceleratorTypesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::AcceleratorTypeAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::AcceleratorTypeAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AcceleratorType
// =============================================================================

/// ResourceIdentifier implementation for AcceleratorType with ComputeAcceleratorTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAcceleratorTypesGetArgs> for AcceleratorType {
    fn generate_resource_id(&self, input: &ComputeAcceleratorTypesGetArgs) -> String {
        format!(
            "gcp::compute::AcceleratorType/{}/{}/{}",
            input.project, input.zone, input.acceleratorType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::AcceleratorType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AcceleratorTypeList
// =============================================================================

/// ResourceIdentifier implementation for AcceleratorTypeList with ComputeAcceleratorTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAcceleratorTypesListArgs> for AcceleratorTypeList {
    fn generate_resource_id(&self, input: &ComputeAcceleratorTypesListArgs) -> String {
        format!(
            "gcp::compute::AcceleratorTypeList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::AcceleratorTypeList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AddressAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for AddressAggregatedList with ComputeAddressesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAddressesAggregatedListArgs> for AddressAggregatedList {
    fn generate_resource_id(&self, input: &ComputeAddressesAggregatedListArgs) -> String {
        format!("gcp::compute::AddressAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::AddressAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeAddressesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAddressesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeAddressesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.address
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Address
// =============================================================================

/// ResourceIdentifier implementation for Address with ComputeAddressesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAddressesGetArgs> for Address {
    fn generate_resource_id(&self, input: &ComputeAddressesGetArgs) -> String {
        format!(
            "gcp::compute::Address/{}/{}/{}",
            input.project, input.region, input.address
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Address"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeAddressesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAddressesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeAddressesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AddressList
// =============================================================================

/// ResourceIdentifier implementation for AddressList with ComputeAddressesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAddressesListArgs> for AddressList {
    fn generate_resource_id(&self, input: &ComputeAddressesListArgs) -> String {
        format!(
            "gcp::compute::AddressList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::AddressList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeAddressesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAddressesMoveArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeAddressesMoveArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.address
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeAddressesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAddressesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeAddressesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeAddressesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAddressesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeAddressesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CalendarModeAdviceResponse
// =============================================================================

/// ResourceIdentifier implementation for CalendarModeAdviceResponse with ComputeAdviceCalendarModeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAdviceCalendarModeArgs> for CalendarModeAdviceResponse {
    fn generate_resource_id(&self, input: &ComputeAdviceCalendarModeArgs) -> String {
        format!(
            "gcp::compute::CalendarModeAdviceResponse/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CalendarModeAdviceResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AutoscalerAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for AutoscalerAggregatedList with ComputeAutoscalersAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAutoscalersAggregatedListArgs> for AutoscalerAggregatedList {
    fn generate_resource_id(&self, input: &ComputeAutoscalersAggregatedListArgs) -> String {
        format!("gcp::compute::AutoscalerAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::AutoscalerAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeAutoscalersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAutoscalersDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeAutoscalersDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.autoscaler
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Autoscaler
// =============================================================================

/// ResourceIdentifier implementation for Autoscaler with ComputeAutoscalersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAutoscalersGetArgs> for Autoscaler {
    fn generate_resource_id(&self, input: &ComputeAutoscalersGetArgs) -> String {
        format!(
            "gcp::compute::Autoscaler/{}/{}/{}",
            input.project, input.zone, input.autoscaler
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Autoscaler"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeAutoscalersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAutoscalersInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeAutoscalersInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AutoscalerList
// =============================================================================

/// ResourceIdentifier implementation for AutoscalerList with ComputeAutoscalersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAutoscalersListArgs> for AutoscalerList {
    fn generate_resource_id(&self, input: &ComputeAutoscalersListArgs) -> String {
        format!(
            "gcp::compute::AutoscalerList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::AutoscalerList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeAutoscalersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAutoscalersPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeAutoscalersPatchArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeAutoscalersTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAutoscalersTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeAutoscalersTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeAutoscalersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeAutoscalersUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeAutoscalersUpdateArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendBucketsAddSignedUrlKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsAddSignedUrlKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsAddSignedUrlKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendBucketAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for BackendBucketAggregatedList with ComputeBackendBucketsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsAggregatedListArgs> for BackendBucketAggregatedList {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsAggregatedListArgs) -> String {
        format!(
            "gcp::compute::BackendBucketAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendBucketAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendBucketsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendBucketsDeleteSignedUrlKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsDeleteSignedUrlKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsDeleteSignedUrlKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendBucket
// =============================================================================

/// ResourceIdentifier implementation for BackendBucket with ComputeBackendBucketsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsGetArgs> for BackendBucket {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsGetArgs) -> String {
        format!(
            "gcp::compute::BackendBucket/{}/{}",
            input.project, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeBackendBucketsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendBucketsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendBucketList
// =============================================================================

/// ResourceIdentifier implementation for BackendBucketList with ComputeBackendBucketsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsListArgs> for BackendBucketList {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsListArgs) -> String {
        format!("gcp::compute::BackendBucketList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendBucketList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendBucketListUsable
// =============================================================================

/// ResourceIdentifier implementation for BackendBucketListUsable with ComputeBackendBucketsListUsableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsListUsableArgs> for BackendBucketListUsable {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsListUsableArgs) -> String {
        format!("gcp::compute::BackendBucketListUsable/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendBucketListUsable"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendBucketsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendBucketsSetEdgeSecurityPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsSetEdgeSecurityPolicyArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeBackendBucketsSetEdgeSecurityPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeBackendBucketsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeBackendBucketsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendBucketsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendBucketsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendBucketsUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendServicesAddSignedUrlKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesAddSignedUrlKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendServicesAddSignedUrlKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendServiceAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for BackendServiceAggregatedList with ComputeBackendServicesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesAggregatedListArgs> for BackendServiceAggregatedList {
    fn generate_resource_id(&self, input: &ComputeBackendServicesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::BackendServiceAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendServiceAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendServicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendServicesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendServicesDeleteSignedUrlKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesDeleteSignedUrlKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendServicesDeleteSignedUrlKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendService
// =============================================================================

/// ResourceIdentifier implementation for BackendService with ComputeBackendServicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesGetArgs> for BackendService {
    fn generate_resource_id(&self, input: &ComputeBackendServicesGetArgs) -> String {
        format!(
            "gcp::compute::BackendService/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendService"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendServiceGroupHealth
// =============================================================================

/// ResourceIdentifier implementation for BackendServiceGroupHealth with ComputeBackendServicesGetHealthArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesGetHealthArgs> for BackendServiceGroupHealth {
    fn generate_resource_id(&self, input: &ComputeBackendServicesGetHealthArgs) -> String {
        format!(
            "gcp::compute::BackendServiceGroupHealth/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendServiceGroupHealth"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeBackendServicesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeBackendServicesGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendServicesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendServicesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendServiceList
// =============================================================================

/// ResourceIdentifier implementation for BackendServiceList with ComputeBackendServicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesListArgs> for BackendServiceList {
    fn generate_resource_id(&self, input: &ComputeBackendServicesListArgs) -> String {
        format!("gcp::compute::BackendServiceList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendServiceList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendServiceListUsable
// =============================================================================

/// ResourceIdentifier implementation for BackendServiceListUsable with ComputeBackendServicesListUsableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesListUsableArgs> for BackendServiceListUsable {
    fn generate_resource_id(&self, input: &ComputeBackendServicesListUsableArgs) -> String {
        format!("gcp::compute::BackendServiceListUsable/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendServiceListUsable"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendServicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendServicesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendServicesSetEdgeSecurityPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesSetEdgeSecurityPolicyArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeBackendServicesSetEdgeSecurityPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeBackendServicesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeBackendServicesSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendServicesSetSecurityPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesSetSecurityPolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendServicesSetSecurityPolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeBackendServicesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeBackendServicesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeBackendServicesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeBackendServicesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeBackendServicesUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeCrossSiteNetworksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeCrossSiteNetworksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeCrossSiteNetworksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.crossSiteNetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CrossSiteNetwork
// =============================================================================

/// ResourceIdentifier implementation for CrossSiteNetwork with ComputeCrossSiteNetworksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeCrossSiteNetworksGetArgs> for CrossSiteNetwork {
    fn generate_resource_id(&self, input: &ComputeCrossSiteNetworksGetArgs) -> String {
        format!(
            "gcp::compute::CrossSiteNetwork/{}/{}",
            input.project, input.crossSiteNetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CrossSiteNetwork"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeCrossSiteNetworksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeCrossSiteNetworksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeCrossSiteNetworksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CrossSiteNetworkList
// =============================================================================

/// ResourceIdentifier implementation for CrossSiteNetworkList with ComputeCrossSiteNetworksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeCrossSiteNetworksListArgs> for CrossSiteNetworkList {
    fn generate_resource_id(&self, input: &ComputeCrossSiteNetworksListArgs) -> String {
        format!("gcp::compute::CrossSiteNetworkList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CrossSiteNetworkList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeCrossSiteNetworksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeCrossSiteNetworksPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeCrossSiteNetworksPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.crossSiteNetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskTypeAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for DiskTypeAggregatedList with ComputeDiskTypesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDiskTypesAggregatedListArgs> for DiskTypeAggregatedList {
    fn generate_resource_id(&self, input: &ComputeDiskTypesAggregatedListArgs) -> String {
        format!("gcp::compute::DiskTypeAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::DiskTypeAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskType
// =============================================================================

/// ResourceIdentifier implementation for DiskType with ComputeDiskTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDiskTypesGetArgs> for DiskType {
    fn generate_resource_id(&self, input: &ComputeDiskTypesGetArgs) -> String {
        format!(
            "gcp::compute::DiskType/{}/{}/{}",
            input.project, input.zone, input.diskType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::DiskType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskTypeList
// =============================================================================

/// ResourceIdentifier implementation for DiskTypeList with ComputeDiskTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDiskTypesListArgs> for DiskTypeList {
    fn generate_resource_id(&self, input: &ComputeDiskTypesListArgs) -> String {
        format!(
            "gcp::compute::DiskTypeList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::DiskTypeList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksAddResourcePoliciesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksAddResourcePoliciesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksAddResourcePoliciesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for DiskAggregatedList with ComputeDisksAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksAggregatedListArgs> for DiskAggregatedList {
    fn generate_resource_id(&self, input: &ComputeDisksAggregatedListArgs) -> String {
        format!("gcp::compute::DiskAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::DiskAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksBulkInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksBulkInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksBulkInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksBulkSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksBulkSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksBulkSetLabelsArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksCreateSnapshotArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksCreateSnapshotArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksCreateSnapshotArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Disk
// =============================================================================

/// ResourceIdentifier implementation for Disk with ComputeDisksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksGetArgs> for Disk {
    fn generate_resource_id(&self, input: &ComputeDisksGetArgs) -> String {
        format!(
            "gcp::compute::Disk/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Disk"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeDisksGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeDisksGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskList
// =============================================================================

/// ResourceIdentifier implementation for DiskList with ComputeDisksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksListArgs> for DiskList {
    fn generate_resource_id(&self, input: &ComputeDisksListArgs) -> String {
        format!("gcp::compute::DiskList/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::DiskList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksRemoveResourcePoliciesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksRemoveResourcePoliciesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksRemoveResourcePoliciesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksResizeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksResizeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksResizeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeDisksSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeDisksSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksStartAsyncReplicationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksStartAsyncReplicationArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksStartAsyncReplicationArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksStopAsyncReplicationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksStopAsyncReplicationArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksStopAsyncReplicationArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksStopGroupAsyncReplicationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksStopGroupAsyncReplicationArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksStopGroupAsyncReplicationArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeDisksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeDisksTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeDisksUpdateKmsKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeDisksUpdateKmsKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeDisksUpdateKmsKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeExternalVpnGatewaysDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeExternalVpnGatewaysDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeExternalVpnGatewaysDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.externalVpnGateway
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ExternalVpnGateway
// =============================================================================

/// ResourceIdentifier implementation for ExternalVpnGateway with ComputeExternalVpnGatewaysGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeExternalVpnGatewaysGetArgs> for ExternalVpnGateway {
    fn generate_resource_id(&self, input: &ComputeExternalVpnGatewaysGetArgs) -> String {
        format!(
            "gcp::compute::ExternalVpnGateway/{}/{}",
            input.project, input.externalVpnGateway
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ExternalVpnGateway"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeExternalVpnGatewaysInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeExternalVpnGatewaysInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeExternalVpnGatewaysInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ExternalVpnGatewayList
// =============================================================================

/// ResourceIdentifier implementation for ExternalVpnGatewayList with ComputeExternalVpnGatewaysListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeExternalVpnGatewaysListArgs> for ExternalVpnGatewayList {
    fn generate_resource_id(&self, input: &ComputeExternalVpnGatewaysListArgs) -> String {
        format!("gcp::compute::ExternalVpnGatewayList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ExternalVpnGatewayList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeExternalVpnGatewaysSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeExternalVpnGatewaysSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeExternalVpnGatewaysSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeExternalVpnGatewaysTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeExternalVpnGatewaysTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeExternalVpnGatewaysTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesAddAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesAddAssociationArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesAddAssociationArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesAddRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesAddRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesAddRuleArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesCloneRulesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesCloneRulesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesCloneRulesArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesDeleteArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicy
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicy with ComputeFirewallPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesGetArgs> for FirewallPolicy {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesGetArgs) -> String {
        format!("gcp::compute::FirewallPolicy/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyAssociation
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyAssociation with ComputeFirewallPoliciesGetAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesGetAssociationArgs> for FirewallPolicyAssociation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesGetAssociationArgs) -> String {
        format!(
            "gcp::compute::FirewallPolicyAssociation/{}",
            input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeFirewallPoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyRule
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyRule with ComputeFirewallPoliciesGetRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesGetRuleArgs> for FirewallPolicyRule {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesGetRuleArgs) -> String {
        format!("gcp::compute::FirewallPolicyRule/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesInsertArgs) -> String {
        "gcp::compute::Operation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyList
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyList with ComputeFirewallPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesListArgs> for FirewallPolicyList {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesListArgs) -> String {
        "gcp::compute::FirewallPolicyList".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPoliciesListAssociationsResponse
// =============================================================================

/// ResourceIdentifier implementation for FirewallPoliciesListAssociationsResponse with ComputeFirewallPoliciesListAssociationsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesListAssociationsArgs>
    for FirewallPoliciesListAssociationsResponse
{
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesListAssociationsArgs) -> String {
        "gcp::compute::FirewallPoliciesListAssociationsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPoliciesListAssociationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesMoveArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesMoveArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesPatchArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesPatchRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesPatchRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesPatchRuleArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesRemoveAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesRemoveAssociationArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesRemoveAssociationArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallPoliciesRemoveRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesRemoveRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesRemoveRuleArgs) -> String {
        format!("gcp::compute::Operation/{}", input.firewallPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeFirewallPoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeFirewallPoliciesSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeFirewallPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallPoliciesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(
        &self,
        input: &ComputeFirewallPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!("gcp::compute::TestPermissionsResponse/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewall
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Firewall
// =============================================================================

/// ResourceIdentifier implementation for Firewall with ComputeFirewallsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallsGetArgs> for Firewall {
    fn generate_resource_id(&self, input: &ComputeFirewallsGetArgs) -> String {
        format!(
            "gcp::compute::Firewall/{}/{}",
            input.project, input.firewall
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Firewall"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallList
// =============================================================================

/// ResourceIdentifier implementation for FirewallList with ComputeFirewallsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallsListArgs> for FirewallList {
    fn generate_resource_id(&self, input: &ComputeFirewallsListArgs) -> String {
        format!("gcp::compute::FirewallList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewall
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeFirewallsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeFirewallsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFirewallsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFirewallsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFirewallsUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewall
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ForwardingRuleAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for ForwardingRuleAggregatedList with ComputeForwardingRulesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeForwardingRulesAggregatedListArgs> for ForwardingRuleAggregatedList {
    fn generate_resource_id(&self, input: &ComputeForwardingRulesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::ForwardingRuleAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ForwardingRuleAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeForwardingRulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeForwardingRulesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeForwardingRulesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.forwardingRule
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ForwardingRule
// =============================================================================

/// ResourceIdentifier implementation for ForwardingRule with ComputeForwardingRulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeForwardingRulesGetArgs> for ForwardingRule {
    fn generate_resource_id(&self, input: &ComputeForwardingRulesGetArgs) -> String {
        format!(
            "gcp::compute::ForwardingRule/{}/{}/{}",
            input.project, input.region, input.forwardingRule
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ForwardingRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeForwardingRulesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeForwardingRulesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeForwardingRulesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ForwardingRuleList
// =============================================================================

/// ResourceIdentifier implementation for ForwardingRuleList with ComputeForwardingRulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeForwardingRulesListArgs> for ForwardingRuleList {
    fn generate_resource_id(&self, input: &ComputeForwardingRulesListArgs) -> String {
        format!(
            "gcp::compute::ForwardingRuleList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ForwardingRuleList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeForwardingRulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeForwardingRulesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeForwardingRulesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.forwardingRule
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeForwardingRulesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeForwardingRulesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeForwardingRulesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeForwardingRulesSetTargetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeForwardingRulesSetTargetArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeForwardingRulesSetTargetArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.forwardingRule
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FutureReservationsAggregatedListResponse
// =============================================================================

/// ResourceIdentifier implementation for FutureReservationsAggregatedListResponse with ComputeFutureReservationsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFutureReservationsAggregatedListArgs>
    for FutureReservationsAggregatedListResponse
{
    fn generate_resource_id(&self, input: &ComputeFutureReservationsAggregatedListArgs) -> String {
        format!(
            "gcp::compute::FutureReservationsAggregatedListResponse/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FutureReservationsAggregatedListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFutureReservationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFutureReservationsCancelArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFutureReservationsCancelArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.futureReservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFutureReservationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFutureReservationsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFutureReservationsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.futureReservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FutureReservation
// =============================================================================

/// ResourceIdentifier implementation for FutureReservation with ComputeFutureReservationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFutureReservationsGetArgs> for FutureReservation {
    fn generate_resource_id(&self, input: &ComputeFutureReservationsGetArgs) -> String {
        format!(
            "gcp::compute::FutureReservation/{}/{}/{}",
            input.project, input.zone, input.futureReservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FutureReservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFutureReservationsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFutureReservationsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFutureReservationsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FutureReservationsListResponse
// =============================================================================

/// ResourceIdentifier implementation for FutureReservationsListResponse with ComputeFutureReservationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFutureReservationsListArgs> for FutureReservationsListResponse {
    fn generate_resource_id(&self, input: &ComputeFutureReservationsListArgs) -> String {
        format!(
            "gcp::compute::FutureReservationsListResponse/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FutureReservationsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeFutureReservationsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeFutureReservationsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeFutureReservationsUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.futureReservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalAddressesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalAddressesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalAddressesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.address
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Address
// =============================================================================

/// ResourceIdentifier implementation for Address with ComputeGlobalAddressesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalAddressesGetArgs> for Address {
    fn generate_resource_id(&self, input: &ComputeGlobalAddressesGetArgs) -> String {
        format!("gcp::compute::Address/{}/{}", input.project, input.address)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Address"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalAddressesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalAddressesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalAddressesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AddressList
// =============================================================================

/// ResourceIdentifier implementation for AddressList with ComputeGlobalAddressesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalAddressesListArgs> for AddressList {
    fn generate_resource_id(&self, input: &ComputeGlobalAddressesListArgs) -> String {
        format!("gcp::compute::AddressList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::AddressList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalAddressesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalAddressesMoveArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalAddressesMoveArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.address
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalAddressesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalAddressesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalAddressesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeGlobalAddressesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalAddressesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeGlobalAddressesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalForwardingRulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalForwardingRulesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalForwardingRulesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.forwardingRule
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ForwardingRule
// =============================================================================

/// ResourceIdentifier implementation for ForwardingRule with ComputeGlobalForwardingRulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalForwardingRulesGetArgs> for ForwardingRule {
    fn generate_resource_id(&self, input: &ComputeGlobalForwardingRulesGetArgs) -> String {
        format!(
            "gcp::compute::ForwardingRule/{}/{}",
            input.project, input.forwardingRule
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ForwardingRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalForwardingRulesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalForwardingRulesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalForwardingRulesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ForwardingRuleList
// =============================================================================

/// ResourceIdentifier implementation for ForwardingRuleList with ComputeGlobalForwardingRulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalForwardingRulesListArgs> for ForwardingRuleList {
    fn generate_resource_id(&self, input: &ComputeGlobalForwardingRulesListArgs) -> String {
        format!("gcp::compute::ForwardingRuleList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ForwardingRuleList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalForwardingRulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalForwardingRulesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalForwardingRulesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.forwardingRule
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalForwardingRulesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalForwardingRulesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalForwardingRulesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalForwardingRulesSetTargetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalForwardingRulesSetTargetArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalForwardingRulesSetTargetArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.forwardingRule
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalNetworkEndpointGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalNetworkEndpointGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalNetworkEndpointGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroup
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroup with ComputeGlobalNetworkEndpointGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalNetworkEndpointGroupsGetArgs> for NetworkEndpointGroup {
    fn generate_resource_id(&self, input: &ComputeGlobalNetworkEndpointGroupsGetArgs) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroup/{}/{}",
            input.project, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalNetworkEndpointGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalNetworkEndpointGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalNetworkEndpointGroupsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroupList
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroupList with ComputeGlobalNetworkEndpointGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalNetworkEndpointGroupsListArgs> for NetworkEndpointGroupList {
    fn generate_resource_id(&self, input: &ComputeGlobalNetworkEndpointGroupsListArgs) -> String {
        format!("gcp::compute::NetworkEndpointGroupList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroupList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroupsListNetworkEndpoints
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroupsListNetworkEndpoints with ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs>
    for NetworkEndpointGroupsListNetworkEndpoints
{
    fn generate_resource_id(
        &self,
        input: &ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroupsListNetworkEndpoints/{}/{}",
            input.project, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroupsListNetworkEndpoints"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperationAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for OperationAggregatedList with ComputeGlobalOperationsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalOperationsAggregatedListArgs> for OperationAggregatedList {
    fn generate_resource_id(&self, input: &ComputeGlobalOperationsAggregatedListArgs) -> String {
        format!("gcp::compute::OperationAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::OperationAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalOperationsGetArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.operation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperationList
// =============================================================================

/// ResourceIdentifier implementation for OperationList with ComputeGlobalOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalOperationsListArgs> for OperationList {
    fn generate_resource_id(&self, input: &ComputeGlobalOperationsListArgs) -> String {
        format!("gcp::compute::OperationList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::OperationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalOperationsWaitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalOperationsWaitArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalOperationsWaitArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.operation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalOrganizationOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalOrganizationOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeGlobalOrganizationOperationsGetArgs) -> String {
        format!("gcp::compute::Operation/{}", input.operation)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperationList
// =============================================================================

/// ResourceIdentifier implementation for OperationList with ComputeGlobalOrganizationOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalOrganizationOperationsListArgs> for OperationList {
    fn generate_resource_id(&self, input: &ComputeGlobalOrganizationOperationsListArgs) -> String {
        "gcp::compute::OperationList".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::OperationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalPublicDelegatedPrefixesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalPublicDelegatedPrefixesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeGlobalPublicDelegatedPrefixesDeleteArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.publicDelegatedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublicDelegatedPrefix
// =============================================================================

/// ResourceIdentifier implementation for PublicDelegatedPrefix with ComputeGlobalPublicDelegatedPrefixesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalPublicDelegatedPrefixesGetArgs> for PublicDelegatedPrefix {
    fn generate_resource_id(&self, input: &ComputeGlobalPublicDelegatedPrefixesGetArgs) -> String {
        format!(
            "gcp::compute::PublicDelegatedPrefix/{}/{}",
            input.project, input.publicDelegatedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PublicDelegatedPrefix"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalPublicDelegatedPrefixesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalPublicDelegatedPrefixesInsertArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeGlobalPublicDelegatedPrefixesInsertArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublicDelegatedPrefixList
// =============================================================================

/// ResourceIdentifier implementation for PublicDelegatedPrefixList with ComputeGlobalPublicDelegatedPrefixesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalPublicDelegatedPrefixesListArgs>
    for PublicDelegatedPrefixList
{
    fn generate_resource_id(&self, input: &ComputeGlobalPublicDelegatedPrefixesListArgs) -> String {
        format!("gcp::compute::PublicDelegatedPrefixList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PublicDelegatedPrefixList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeGlobalPublicDelegatedPrefixesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeGlobalPublicDelegatedPrefixesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeGlobalPublicDelegatedPrefixesPatchArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.publicDelegatedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthChecksAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for HealthChecksAggregatedList with ComputeHealthChecksAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHealthChecksAggregatedListArgs> for HealthChecksAggregatedList {
    fn generate_resource_id(&self, input: &ComputeHealthChecksAggregatedListArgs) -> String {
        format!("gcp::compute::HealthChecksAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthChecksAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHealthChecksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHealthChecksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHealthChecksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.healthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthCheck
// =============================================================================

/// ResourceIdentifier implementation for HealthCheck with ComputeHealthChecksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHealthChecksGetArgs> for HealthCheck {
    fn generate_resource_id(&self, input: &ComputeHealthChecksGetArgs) -> String {
        format!(
            "gcp::compute::HealthCheck/{}/{}",
            input.project, input.healthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthCheck"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHealthChecksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHealthChecksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHealthChecksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthCheckList
// =============================================================================

/// ResourceIdentifier implementation for HealthCheckList with ComputeHealthChecksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHealthChecksListArgs> for HealthCheckList {
    fn generate_resource_id(&self, input: &ComputeHealthChecksListArgs) -> String {
        format!("gcp::compute::HealthCheckList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthCheckList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHealthChecksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHealthChecksPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHealthChecksPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.healthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeHealthChecksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHealthChecksTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeHealthChecksTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHealthChecksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHealthChecksUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHealthChecksUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.healthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHttpHealthChecksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpHealthChecksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHttpHealthChecksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.httpHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpHealthCheck
// =============================================================================

/// ResourceIdentifier implementation for HttpHealthCheck with ComputeHttpHealthChecksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpHealthChecksGetArgs> for HttpHealthCheck {
    fn generate_resource_id(&self, input: &ComputeHttpHealthChecksGetArgs) -> String {
        format!(
            "gcp::compute::HttpHealthCheck/{}/{}",
            input.project, input.httpHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HttpHealthCheck"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHttpHealthChecksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpHealthChecksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHttpHealthChecksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpHealthCheckList
// =============================================================================

/// ResourceIdentifier implementation for HttpHealthCheckList with ComputeHttpHealthChecksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpHealthChecksListArgs> for HttpHealthCheckList {
    fn generate_resource_id(&self, input: &ComputeHttpHealthChecksListArgs) -> String {
        format!("gcp::compute::HttpHealthCheckList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HttpHealthCheckList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHttpHealthChecksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpHealthChecksPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHttpHealthChecksPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.httpHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeHttpHealthChecksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpHealthChecksTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(
        &self,
        input: &ComputeHttpHealthChecksTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHttpHealthChecksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpHealthChecksUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHttpHealthChecksUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.httpHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHttpsHealthChecksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpsHealthChecksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHttpsHealthChecksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.httpsHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpsHealthCheck
// =============================================================================

/// ResourceIdentifier implementation for HttpsHealthCheck with ComputeHttpsHealthChecksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpsHealthChecksGetArgs> for HttpsHealthCheck {
    fn generate_resource_id(&self, input: &ComputeHttpsHealthChecksGetArgs) -> String {
        format!(
            "gcp::compute::HttpsHealthCheck/{}/{}",
            input.project, input.httpsHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HttpsHealthCheck"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHttpsHealthChecksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpsHealthChecksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHttpsHealthChecksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpsHealthCheckList
// =============================================================================

/// ResourceIdentifier implementation for HttpsHealthCheckList with ComputeHttpsHealthChecksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpsHealthChecksListArgs> for HttpsHealthCheckList {
    fn generate_resource_id(&self, input: &ComputeHttpsHealthChecksListArgs) -> String {
        format!("gcp::compute::HttpsHealthCheckList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HttpsHealthCheckList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHttpsHealthChecksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpsHealthChecksPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHttpsHealthChecksPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.httpsHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeHttpsHealthChecksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpsHealthChecksTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeHttpsHealthChecksTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeHttpsHealthChecksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeHttpsHealthChecksUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeHttpsHealthChecksUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.httpsHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ImageFamilyView
// =============================================================================

/// ResourceIdentifier implementation for ImageFamilyView with ComputeImageFamilyViewsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImageFamilyViewsGetArgs> for ImageFamilyView {
    fn generate_resource_id(&self, input: &ComputeImageFamilyViewsGetArgs) -> String {
        format!(
            "gcp::compute::ImageFamilyView/{}/{}/{}",
            input.project, input.zone, input.family
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ImageFamilyView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeImagesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeImagesDeleteArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.image)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeImagesDeprecateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesDeprecateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeImagesDeprecateArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.image)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Image
// =============================================================================

/// ResourceIdentifier implementation for Image with ComputeImagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesGetArgs> for Image {
    fn generate_resource_id(&self, input: &ComputeImagesGetArgs) -> String {
        format!("gcp::compute::Image/{}/{}", input.project, input.image)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Image"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Image
// =============================================================================

/// ResourceIdentifier implementation for Image with ComputeImagesGetFromFamilyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesGetFromFamilyArgs> for Image {
    fn generate_resource_id(&self, input: &ComputeImagesGetFromFamilyArgs) -> String {
        format!("gcp::compute::Image/{}/{}", input.project, input.family)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Image"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeImagesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeImagesGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeImagesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeImagesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ImageList
// =============================================================================

/// ResourceIdentifier implementation for ImageList with ComputeImagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesListArgs> for ImageList {
    fn generate_resource_id(&self, input: &ComputeImagesListArgs) -> String {
        format!("gcp::compute::ImageList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ImageList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeImagesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeImagesPatchArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.image)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeImagesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeImagesSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeImagesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeImagesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeImagesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeImagesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeImagesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagerResizeRequestsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagerResizeRequestsCancelArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagerResizeRequestsCancelArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager, input.resizeRequest
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagerResizeRequestsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagerResizeRequestsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagerResizeRequestsDeleteArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager, input.resizeRequest
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManagerResizeRequest
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManagerResizeRequest with ComputeInstanceGroupManagerResizeRequestsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagerResizeRequestsGetArgs>
    for InstanceGroupManagerResizeRequest
{
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagerResizeRequestsGetArgs,
    ) -> String {
        format!(
            "gcp::compute::InstanceGroupManagerResizeRequest/{}/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager, input.resizeRequest
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManagerResizeRequest"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagerResizeRequestsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagerResizeRequestsInsertArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagerResizeRequestsInsertArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManagerResizeRequestsListResponse
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManagerResizeRequestsListResponse with ComputeInstanceGroupManagerResizeRequestsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagerResizeRequestsListArgs>
    for InstanceGroupManagerResizeRequestsListResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagerResizeRequestsListArgs,
    ) -> String {
        format!(
            "gcp::compute::InstanceGroupManagerResizeRequestsListResponse/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManagerResizeRequestsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersAbandonInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersAbandonInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersAbandonInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManagerAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManagerAggregatedList with ComputeInstanceGroupManagersAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersAggregatedListArgs>
    for InstanceGroupManagerAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::InstanceGroupManagerAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManagerAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersCreateInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersCreateInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersCreateInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupManagersDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersDeleteInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersDeleteInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersDeleteInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManager
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManager with ComputeInstanceGroupManagersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersGetArgs> for InstanceGroupManager {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupManagersGetArgs) -> String {
        format!(
            "gcp::compute::InstanceGroupManager/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManager"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupManagersInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManagerList
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManagerList with ComputeInstanceGroupManagersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersListArgs> for InstanceGroupManagerList {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupManagersListArgs) -> String {
        format!(
            "gcp::compute::InstanceGroupManagerList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManagerList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManagersListErrorsResponse
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManagersListErrorsResponse with ComputeInstanceGroupManagersListErrorsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersListErrorsArgs>
    for InstanceGroupManagersListErrorsResponse
{
    fn generate_resource_id(&self, input: &ComputeInstanceGroupManagersListErrorsArgs) -> String {
        format!(
            "gcp::compute::InstanceGroupManagersListErrorsResponse/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManagersListErrorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManagersListManagedInstancesResponse
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManagersListManagedInstancesResponse with ComputeInstanceGroupManagersListManagedInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersListManagedInstancesArgs>
    for InstanceGroupManagersListManagedInstancesResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersListManagedInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::InstanceGroupManagersListManagedInstancesResponse/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManagersListManagedInstancesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManagersListPerInstanceConfigsResp
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManagersListPerInstanceConfigsResp with ComputeInstanceGroupManagersListPerInstanceConfigsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersListPerInstanceConfigsArgs>
    for InstanceGroupManagersListPerInstanceConfigsResp
{
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersListPerInstanceConfigsArgs,
    ) -> String {
        format!(
            "gcp::compute::InstanceGroupManagersListPerInstanceConfigsResp/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManagersListPerInstanceConfigsResp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupManagersPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersRecreateInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersRecreateInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersRecreateInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersResizeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersResizeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupManagersResizeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersResumeInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersResumeInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersResumeInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersSetInstanceTemplateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersSetInstanceTemplateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersSetInstanceTemplateArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersSetTargetPoolsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersSetTargetPoolsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersSetTargetPoolsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersStartInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersStartInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersStartInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersStopInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersStopInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersStopInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersSuspendInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersSuspendInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersSuspendInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupsAddInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsAddInstancesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsAddInstancesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupAggregatedList with ComputeInstanceGroupsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsAggregatedListArgs> for InstanceGroupAggregatedList {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsAggregatedListArgs) -> String {
        format!(
            "gcp::compute::InstanceGroupAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroup
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroup with ComputeInstanceGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsGetArgs> for InstanceGroup {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsGetArgs) -> String {
        format!(
            "gcp::compute::InstanceGroup/{}/{}/{}",
            input.project, input.zone, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupList
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupList with ComputeInstanceGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsListArgs> for InstanceGroupList {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsListArgs) -> String {
        format!(
            "gcp::compute::InstanceGroupList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupsListInstances
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupsListInstances with ComputeInstanceGroupsListInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsListInstancesArgs> for InstanceGroupsListInstances {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsListInstancesArgs) -> String {
        format!(
            "gcp::compute::InstanceGroupsListInstances/{}/{}/{}",
            input.project, input.zone, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupsListInstances"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupsRemoveInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsRemoveInstancesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsRemoveInstancesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceGroupsSetNamedPortsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsSetNamedPortsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsSetNamedPortsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeInstanceGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceGroupsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeInstanceGroupsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceSettings
// =============================================================================

/// ResourceIdentifier implementation for InstanceSettings with ComputeInstanceSettingsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceSettingsGetArgs> for InstanceSettings {
    fn generate_resource_id(&self, input: &ComputeInstanceSettingsGetArgs) -> String {
        format!(
            "gcp::compute::InstanceSettings/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceSettingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceSettingsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceSettingsPatchArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceTemplateAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for InstanceTemplateAggregatedList with ComputeInstanceTemplatesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceTemplatesAggregatedListArgs>
    for InstanceTemplateAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeInstanceTemplatesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::InstanceTemplateAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceTemplateAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceTemplatesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceTemplatesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.instanceTemplate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceTemplate
// =============================================================================

/// ResourceIdentifier implementation for InstanceTemplate with ComputeInstanceTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceTemplatesGetArgs> for InstanceTemplate {
    fn generate_resource_id(&self, input: &ComputeInstanceTemplatesGetArgs) -> String {
        format!(
            "gcp::compute::InstanceTemplate/{}/{}",
            input.project, input.instanceTemplate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInstanceTemplatesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceTemplatesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInstanceTemplatesGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstanceTemplatesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceTemplatesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstanceTemplatesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceTemplateList
// =============================================================================

/// ResourceIdentifier implementation for InstanceTemplateList with ComputeInstanceTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceTemplatesListArgs> for InstanceTemplateList {
    fn generate_resource_id(&self, input: &ComputeInstanceTemplatesListArgs) -> String {
        format!("gcp::compute::InstanceTemplateList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceTemplateList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInstanceTemplatesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceTemplatesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInstanceTemplatesSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeInstanceTemplatesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstanceTemplatesTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeInstanceTemplatesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesAddAccessConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesAddAccessConfigArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesAddAccessConfigArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesAddNetworkInterfaceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesAddNetworkInterfaceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesAddNetworkInterfaceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesAddResourcePoliciesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesAddResourcePoliciesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesAddResourcePoliciesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for InstanceAggregatedList with ComputeInstancesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesAggregatedListArgs> for InstanceAggregatedList {
    fn generate_resource_id(&self, input: &ComputeInstancesAggregatedListArgs) -> String {
        format!("gcp::compute::InstanceAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesAttachDiskArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesAttachDiskArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesAttachDiskArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesBulkInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesBulkInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesBulkInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesDeleteAccessConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesDeleteAccessConfigArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesDeleteAccessConfigArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesDeleteNetworkInterfaceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesDeleteNetworkInterfaceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesDeleteNetworkInterfaceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesDetachDiskArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesDetachDiskArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesDetachDiskArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Instance
// =============================================================================

/// ResourceIdentifier implementation for Instance with ComputeInstancesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesGetArgs> for Instance {
    fn generate_resource_id(&self, input: &ComputeInstancesGetArgs) -> String {
        format!(
            "gcp::compute::Instance/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Instance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstancesGetEffectiveFirewallsResponse
// =============================================================================

/// ResourceIdentifier implementation for InstancesGetEffectiveFirewallsResponse with ComputeInstancesGetEffectiveFirewallsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesGetEffectiveFirewallsArgs>
    for InstancesGetEffectiveFirewallsResponse
{
    fn generate_resource_id(&self, input: &ComputeInstancesGetEffectiveFirewallsArgs) -> String {
        format!(
            "gcp::compute::InstancesGetEffectiveFirewallsResponse/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstancesGetEffectiveFirewallsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GuestAttributes
// =============================================================================

/// ResourceIdentifier implementation for GuestAttributes with ComputeInstancesGetGuestAttributesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesGetGuestAttributesArgs> for GuestAttributes {
    fn generate_resource_id(&self, input: &ComputeInstancesGetGuestAttributesArgs) -> String {
        format!(
            "gcp::compute::GuestAttributes/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::GuestAttributes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInstancesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInstancesGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Screenshot
// =============================================================================

/// ResourceIdentifier implementation for Screenshot with ComputeInstancesGetScreenshotArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesGetScreenshotArgs> for Screenshot {
    fn generate_resource_id(&self, input: &ComputeInstancesGetScreenshotArgs) -> String {
        format!(
            "gcp::compute::Screenshot/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Screenshot"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SerialPortOutput
// =============================================================================

/// ResourceIdentifier implementation for SerialPortOutput with ComputeInstancesGetSerialPortOutputArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesGetSerialPortOutputArgs> for SerialPortOutput {
    fn generate_resource_id(&self, input: &ComputeInstancesGetSerialPortOutputArgs) -> String {
        format!(
            "gcp::compute::SerialPortOutput/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SerialPortOutput"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ShieldedInstanceIdentity
// =============================================================================

/// ResourceIdentifier implementation for ShieldedInstanceIdentity with ComputeInstancesGetShieldedInstanceIdentityArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesGetShieldedInstanceIdentityArgs>
    for ShieldedInstanceIdentity
{
    fn generate_resource_id(
        &self,
        input: &ComputeInstancesGetShieldedInstanceIdentityArgs,
    ) -> String {
        format!(
            "gcp::compute::ShieldedInstanceIdentity/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ShieldedInstanceIdentity"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceList
// =============================================================================

/// ResourceIdentifier implementation for InstanceList with ComputeInstancesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesListArgs> for InstanceList {
    fn generate_resource_id(&self, input: &ComputeInstancesListArgs) -> String {
        format!(
            "gcp::compute::InstanceList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceListReferrers
// =============================================================================

/// ResourceIdentifier implementation for InstanceListReferrers with ComputeInstancesListReferrersArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesListReferrersArgs> for InstanceListReferrers {
    fn generate_resource_id(&self, input: &ComputeInstancesListReferrersArgs) -> String {
        format!(
            "gcp::compute::InstanceListReferrers/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceListReferrers"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesPerformMaintenanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesPerformMaintenanceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesPerformMaintenanceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesRemoveResourcePoliciesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesRemoveResourcePoliciesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesRemoveResourcePoliciesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesReportHostAsFaultyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesReportHostAsFaultyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesReportHostAsFaultyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesResetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesResetArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesResetArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesResumeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesResumeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesResumeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetDeletionProtectionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetDeletionProtectionArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetDeletionProtectionArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetDiskAutoDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetDiskAutoDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetDiskAutoDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInstancesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInstancesSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetMachineResourcesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetMachineResourcesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetMachineResourcesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetMachineTypeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetMachineTypeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetMachineTypeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetMetadataArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetMetadataArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetMinCpuPlatformArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetMinCpuPlatformArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetMinCpuPlatformArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetNameArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetNameArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetNameArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetSchedulingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetSchedulingArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetSchedulingArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetSecurityPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetSecurityPolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetSecurityPolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetServiceAccountArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetServiceAccountArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetServiceAccountArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSetTagsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSetTagsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSetTagsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSimulateMaintenanceEventArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSimulateMaintenanceEventArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSimulateMaintenanceEventArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesStartArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesStartArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesStartArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesStartWithEncryptionKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesStartWithEncryptionKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesStartWithEncryptionKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesStopArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesStopArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesStopArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesSuspendArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesSuspendArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesSuspendArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeInstancesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeInstancesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesUpdateAccessConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesUpdateAccessConfigArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesUpdateAccessConfigArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesUpdateDisplayDeviceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesUpdateDisplayDeviceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesUpdateDisplayDeviceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesUpdateNetworkInterfaceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesUpdateNetworkInterfaceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstancesUpdateNetworkInterfaceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstancesUpdateShieldedInstanceConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstancesUpdateShieldedInstanceConfigArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInstancesUpdateShieldedInstanceConfigArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstantSnapshotGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instantSnapshotGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstantSnapshotGroup
// =============================================================================

/// ResourceIdentifier implementation for InstantSnapshotGroup with ComputeInstantSnapshotGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotGroupsGetArgs> for InstantSnapshotGroup {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotGroupsGetArgs) -> String {
        format!(
            "gcp::compute::InstantSnapshotGroup/{}/{}/{}",
            input.project, input.zone, input.instantSnapshotGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstantSnapshotGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInstantSnapshotGroupsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotGroupsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotGroupsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstantSnapshotGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotGroupsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInstantSnapshotGroups
// =============================================================================

/// ResourceIdentifier implementation for ListInstantSnapshotGroups with ComputeInstantSnapshotGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotGroupsListArgs> for ListInstantSnapshotGroups {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotGroupsListArgs) -> String {
        format!(
            "gcp::compute::ListInstantSnapshotGroups/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ListInstantSnapshotGroups"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInstantSnapshotGroupsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotGroupsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotGroupsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeInstantSnapshotGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotGroupsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeInstantSnapshotGroupsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstantSnapshotAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for InstantSnapshotAggregatedList with ComputeInstantSnapshotsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsAggregatedListArgs>
    for InstantSnapshotAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotsAggregatedListArgs) -> String {
        format!(
            "gcp::compute::InstantSnapshotAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstantSnapshotAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstantSnapshotsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.instantSnapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstantSnapshot
// =============================================================================

/// ResourceIdentifier implementation for InstantSnapshot with ComputeInstantSnapshotsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsGetArgs> for InstantSnapshot {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotsGetArgs) -> String {
        format!(
            "gcp::compute::InstantSnapshot/{}/{}/{}",
            input.project, input.zone, input.instantSnapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstantSnapshot"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInstantSnapshotsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstantSnapshotsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstantSnapshotList
// =============================================================================

/// ResourceIdentifier implementation for InstantSnapshotList with ComputeInstantSnapshotsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsListArgs> for InstantSnapshotList {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotsListArgs) -> String {
        format!(
            "gcp::compute::InstantSnapshotList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstantSnapshotList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInstantSnapshotsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInstantSnapshotsSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInstantSnapshotsSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeInstantSnapshotsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInstantSnapshotsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(
        &self,
        input: &ComputeInstantSnapshotsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectAttachmentGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectAttachmentGroupsDeleteArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.interconnectAttachmentGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectAttachmentGroup
// =============================================================================

/// ResourceIdentifier implementation for InterconnectAttachmentGroup with ComputeInterconnectAttachmentGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsGetArgs>
    for InterconnectAttachmentGroup
{
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentGroupsGetArgs) -> String {
        format!(
            "gcp::compute::InterconnectAttachmentGroup/{}/{}",
            input.project, input.interconnectAttachmentGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectAttachmentGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInterconnectAttachmentGroupsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectAttachmentGroupsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectAttachmentGroupsGetOperationalStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for InterconnectAttachmentGroupsGetOperationalStatusResponse with ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs>
    for InterconnectAttachmentGroupsGetOperationalStatusResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs,
    ) -> String {
        format!(
            "gcp::compute::InterconnectAttachmentGroupsGetOperationalStatusResponse/{}/{}",
            input.project, input.interconnectAttachmentGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectAttachmentGroupsGetOperationalStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectAttachmentGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsInsertArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectAttachmentGroupsInsertArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectAttachmentGroupsListResponse
// =============================================================================

/// ResourceIdentifier implementation for InterconnectAttachmentGroupsListResponse with ComputeInterconnectAttachmentGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsListArgs>
    for InterconnectAttachmentGroupsListResponse
{
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentGroupsListArgs) -> String {
        format!(
            "gcp::compute::InterconnectAttachmentGroupsListResponse/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectAttachmentGroupsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectAttachmentGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentGroupsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.interconnectAttachmentGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInterconnectAttachmentGroupsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectAttachmentGroupsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectAttachmentAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for InterconnectAttachmentAggregatedList with ComputeInterconnectAttachmentsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentsAggregatedListArgs>
    for InterconnectAttachmentAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectAttachmentsAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::InterconnectAttachmentAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectAttachmentAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectAttachmentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.interconnectAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectAttachment
// =============================================================================

/// ResourceIdentifier implementation for InterconnectAttachment with ComputeInterconnectAttachmentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentsGetArgs> for InterconnectAttachment {
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentsGetArgs) -> String {
        format!(
            "gcp::compute::InterconnectAttachment/{}/{}/{}",
            input.project, input.region, input.interconnectAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectAttachment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectAttachmentsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectAttachmentList
// =============================================================================

/// ResourceIdentifier implementation for InterconnectAttachmentList with ComputeInterconnectAttachmentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentsListArgs> for InterconnectAttachmentList {
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentsListArgs) -> String {
        format!(
            "gcp::compute::InterconnectAttachmentList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectAttachmentList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectAttachmentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.interconnectAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectAttachmentsSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectAttachmentsSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectAttachmentsSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectGroupsCreateMembersArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsCreateMembersArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectGroupsCreateMembersArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.interconnectGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.interconnectGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectGroup
// =============================================================================

/// ResourceIdentifier implementation for InterconnectGroup with ComputeInterconnectGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsGetArgs> for InterconnectGroup {
    fn generate_resource_id(&self, input: &ComputeInterconnectGroupsGetArgs) -> String {
        format!(
            "gcp::compute::InterconnectGroup/{}/{}",
            input.project, input.interconnectGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInterconnectGroupsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInterconnectGroupsGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectGroupsGetOperationalStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for InterconnectGroupsGetOperationalStatusResponse with ComputeInterconnectGroupsGetOperationalStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsGetOperationalStatusArgs>
    for InterconnectGroupsGetOperationalStatusResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectGroupsGetOperationalStatusArgs,
    ) -> String {
        format!(
            "gcp::compute::InterconnectGroupsGetOperationalStatusResponse/{}/{}",
            input.project, input.interconnectGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectGroupsGetOperationalStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectGroupsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectGroupsListResponse
// =============================================================================

/// ResourceIdentifier implementation for InterconnectGroupsListResponse with ComputeInterconnectGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsListArgs> for InterconnectGroupsListResponse {
    fn generate_resource_id(&self, input: &ComputeInterconnectGroupsListArgs) -> String {
        format!(
            "gcp::compute::InterconnectGroupsListResponse/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectGroupsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectGroupsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.interconnectGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeInterconnectGroupsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeInterconnectGroupsSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeInterconnectGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectGroupsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeInterconnectGroupsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectLocation
// =============================================================================

/// ResourceIdentifier implementation for InterconnectLocation with ComputeInterconnectLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectLocationsGetArgs> for InterconnectLocation {
    fn generate_resource_id(&self, input: &ComputeInterconnectLocationsGetArgs) -> String {
        format!(
            "gcp::compute::InterconnectLocation/{}/{}",
            input.project, input.interconnectLocation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectLocation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectLocationList
// =============================================================================

/// ResourceIdentifier implementation for InterconnectLocationList with ComputeInterconnectLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectLocationsListArgs> for InterconnectLocationList {
    fn generate_resource_id(&self, input: &ComputeInterconnectLocationsListArgs) -> String {
        format!("gcp::compute::InterconnectLocationList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectLocationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectRemoteLocation
// =============================================================================

/// ResourceIdentifier implementation for InterconnectRemoteLocation with ComputeInterconnectRemoteLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectRemoteLocationsGetArgs> for InterconnectRemoteLocation {
    fn generate_resource_id(&self, input: &ComputeInterconnectRemoteLocationsGetArgs) -> String {
        format!(
            "gcp::compute::InterconnectRemoteLocation/{}/{}",
            input.project, input.interconnectRemoteLocation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectRemoteLocation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectRemoteLocationList
// =============================================================================

/// ResourceIdentifier implementation for InterconnectRemoteLocationList with ComputeInterconnectRemoteLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectRemoteLocationsListArgs>
    for InterconnectRemoteLocationList
{
    fn generate_resource_id(&self, input: &ComputeInterconnectRemoteLocationsListArgs) -> String {
        format!(
            "gcp::compute::InterconnectRemoteLocationList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectRemoteLocationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.interconnect
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Interconnect
// =============================================================================

/// ResourceIdentifier implementation for Interconnect with ComputeInterconnectsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectsGetArgs> for Interconnect {
    fn generate_resource_id(&self, input: &ComputeInterconnectsGetArgs) -> String {
        format!(
            "gcp::compute::Interconnect/{}/{}",
            input.project, input.interconnect
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Interconnect"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectsGetDiagnosticsResponse
// =============================================================================

/// ResourceIdentifier implementation for InterconnectsGetDiagnosticsResponse with ComputeInterconnectsGetDiagnosticsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectsGetDiagnosticsArgs>
    for InterconnectsGetDiagnosticsResponse
{
    fn generate_resource_id(&self, input: &ComputeInterconnectsGetDiagnosticsArgs) -> String {
        format!(
            "gcp::compute::InterconnectsGetDiagnosticsResponse/{}/{}",
            input.project, input.interconnect
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectsGetDiagnosticsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectsGetMacsecConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for InterconnectsGetMacsecConfigResponse with ComputeInterconnectsGetMacsecConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectsGetMacsecConfigArgs>
    for InterconnectsGetMacsecConfigResponse
{
    fn generate_resource_id(&self, input: &ComputeInterconnectsGetMacsecConfigArgs) -> String {
        format!(
            "gcp::compute::InterconnectsGetMacsecConfigResponse/{}/{}",
            input.project, input.interconnect
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectsGetMacsecConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterconnectList
// =============================================================================

/// ResourceIdentifier implementation for InterconnectList with ComputeInterconnectsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectsListArgs> for InterconnectList {
    fn generate_resource_id(&self, input: &ComputeInterconnectsListArgs) -> String {
        format!("gcp::compute::InterconnectList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InterconnectList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.interconnect
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeInterconnectsSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeInterconnectsSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeInterconnectsSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LicenseCode
// =============================================================================

/// ResourceIdentifier implementation for LicenseCode with ComputeLicenseCodesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicenseCodesGetArgs> for LicenseCode {
    fn generate_resource_id(&self, input: &ComputeLicenseCodesGetArgs) -> String {
        format!(
            "gcp::compute::LicenseCode/{}/{}",
            input.project, input.licenseCode
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::LicenseCode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeLicenseCodesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicenseCodesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeLicenseCodesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeLicensesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicensesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeLicensesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.license
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for License
// =============================================================================

/// ResourceIdentifier implementation for License with ComputeLicensesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicensesGetArgs> for License {
    fn generate_resource_id(&self, input: &ComputeLicensesGetArgs) -> String {
        format!("gcp::compute::License/{}/{}", input.project, input.license)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::License"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeLicensesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicensesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeLicensesGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeLicensesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicensesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeLicensesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LicensesListResponse
// =============================================================================

/// ResourceIdentifier implementation for LicensesListResponse with ComputeLicensesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicensesListArgs> for LicensesListResponse {
    fn generate_resource_id(&self, input: &ComputeLicensesListArgs) -> String {
        format!("gcp::compute::LicensesListResponse/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::LicensesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeLicensesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicensesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeLicensesSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeLicensesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicensesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeLicensesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeLicensesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeLicensesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeLicensesUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.license
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeMachineImagesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineImagesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeMachineImagesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.machineImage
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MachineImage
// =============================================================================

/// ResourceIdentifier implementation for MachineImage with ComputeMachineImagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineImagesGetArgs> for MachineImage {
    fn generate_resource_id(&self, input: &ComputeMachineImagesGetArgs) -> String {
        format!(
            "gcp::compute::MachineImage/{}/{}",
            input.project, input.machineImage
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::MachineImage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeMachineImagesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineImagesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeMachineImagesGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeMachineImagesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineImagesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeMachineImagesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MachineImageList
// =============================================================================

/// ResourceIdentifier implementation for MachineImageList with ComputeMachineImagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineImagesListArgs> for MachineImageList {
    fn generate_resource_id(&self, input: &ComputeMachineImagesListArgs) -> String {
        format!("gcp::compute::MachineImageList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::MachineImageList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeMachineImagesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineImagesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeMachineImagesSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeMachineImagesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineImagesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeMachineImagesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeMachineImagesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineImagesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeMachineImagesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MachineTypeAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for MachineTypeAggregatedList with ComputeMachineTypesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineTypesAggregatedListArgs> for MachineTypeAggregatedList {
    fn generate_resource_id(&self, input: &ComputeMachineTypesAggregatedListArgs) -> String {
        format!("gcp::compute::MachineTypeAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::MachineTypeAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MachineType
// =============================================================================

/// ResourceIdentifier implementation for MachineType with ComputeMachineTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineTypesGetArgs> for MachineType {
    fn generate_resource_id(&self, input: &ComputeMachineTypesGetArgs) -> String {
        format!(
            "gcp::compute::MachineType/{}/{}/{}",
            input.project, input.zone, input.machineType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::MachineType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MachineTypeList
// =============================================================================

/// ResourceIdentifier implementation for MachineTypeList with ComputeMachineTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeMachineTypesListArgs> for MachineTypeList {
    fn generate_resource_id(&self, input: &ComputeMachineTypesListArgs) -> String {
        format!(
            "gcp::compute::MachineTypeList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::MachineTypeList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkAttachmentAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for NetworkAttachmentAggregatedList with ComputeNetworkAttachmentsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsAggregatedListArgs>
    for NetworkAttachmentAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeNetworkAttachmentsAggregatedListArgs) -> String {
        format!(
            "gcp::compute::NetworkAttachmentAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkAttachmentAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkAttachmentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkAttachmentsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.networkAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkAttachment
// =============================================================================

/// ResourceIdentifier implementation for NetworkAttachment with ComputeNetworkAttachmentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsGetArgs> for NetworkAttachment {
    fn generate_resource_id(&self, input: &ComputeNetworkAttachmentsGetArgs) -> String {
        format!(
            "gcp::compute::NetworkAttachment/{}/{}/{}",
            input.project, input.region, input.networkAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkAttachment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeNetworkAttachmentsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeNetworkAttachmentsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkAttachmentsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkAttachmentsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkAttachmentList
// =============================================================================

/// ResourceIdentifier implementation for NetworkAttachmentList with ComputeNetworkAttachmentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsListArgs> for NetworkAttachmentList {
    fn generate_resource_id(&self, input: &ComputeNetworkAttachmentsListArgs) -> String {
        format!(
            "gcp::compute::NetworkAttachmentList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkAttachmentList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkAttachmentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkAttachmentsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.networkAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeNetworkAttachmentsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeNetworkAttachmentsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeNetworkAttachmentsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkAttachmentsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkAttachmentsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEdgeSecurityServiceAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for NetworkEdgeSecurityServiceAggregatedList with ComputeNetworkEdgeSecurityServicesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEdgeSecurityServicesAggregatedListArgs>
    for NetworkEdgeSecurityServiceAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkEdgeSecurityServicesAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::NetworkEdgeSecurityServiceAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEdgeSecurityServiceAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkEdgeSecurityServicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEdgeSecurityServicesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkEdgeSecurityServicesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.networkEdgeSecurityService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEdgeSecurityService
// =============================================================================

/// ResourceIdentifier implementation for NetworkEdgeSecurityService with ComputeNetworkEdgeSecurityServicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEdgeSecurityServicesGetArgs> for NetworkEdgeSecurityService {
    fn generate_resource_id(&self, input: &ComputeNetworkEdgeSecurityServicesGetArgs) -> String {
        format!(
            "gcp::compute::NetworkEdgeSecurityService/{}/{}/{}",
            input.project, input.region, input.networkEdgeSecurityService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEdgeSecurityService"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkEdgeSecurityServicesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEdgeSecurityServicesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkEdgeSecurityServicesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkEdgeSecurityServicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEdgeSecurityServicesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkEdgeSecurityServicesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.networkEdgeSecurityService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroupAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroupAggregatedList with ComputeNetworkEndpointGroupsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsAggregatedListArgs>
    for NetworkEndpointGroupAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkEndpointGroupsAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroupAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroupAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkEndpointGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkEndpointGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroup
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroup with ComputeNetworkEndpointGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsGetArgs> for NetworkEndpointGroup {
    fn generate_resource_id(&self, input: &ComputeNetworkEndpointGroupsGetArgs) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroup/{}/{}/{}",
            input.project, input.zone, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkEndpointGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkEndpointGroupsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroupList
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroupList with ComputeNetworkEndpointGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsListArgs> for NetworkEndpointGroupList {
    fn generate_resource_id(&self, input: &ComputeNetworkEndpointGroupsListArgs) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroupList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroupList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroupsListNetworkEndpoints
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroupsListNetworkEndpoints with ComputeNetworkEndpointGroupsListNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsListNetworkEndpointsArgs>
    for NetworkEndpointGroupsListNetworkEndpoints
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkEndpointGroupsListNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroupsListNetworkEndpoints/{}/{}/{}",
            input.project, input.zone, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroupsListNetworkEndpoints"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeNetworkEndpointGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkEndpointGroupsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkEndpointGroupsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesAddAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesAddAssociationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesAddAssociationArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesAddRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesAddRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesAddRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkFirewallPolicyAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for NetworkFirewallPolicyAggregatedList with ComputeNetworkFirewallPoliciesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesAggregatedListArgs>
    for NetworkFirewallPolicyAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::NetworkFirewallPolicyAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkFirewallPolicyAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesCloneRulesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesCloneRulesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesCloneRulesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicy
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicy with ComputeNetworkFirewallPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesGetArgs> for FirewallPolicy {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesGetArgs) -> String {
        format!(
            "gcp::compute::FirewallPolicy/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyAssociation
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyAssociation with ComputeNetworkFirewallPoliciesGetAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesGetAssociationArgs>
    for FirewallPolicyAssociation
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesGetAssociationArgs,
    ) -> String {
        format!(
            "gcp::compute::FirewallPolicyAssociation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeNetworkFirewallPoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyRule
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyRule with ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs>
    for FirewallPolicyRule
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs,
    ) -> String {
        format!(
            "gcp::compute::FirewallPolicyRule/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyRule
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyRule with ComputeNetworkFirewallPoliciesGetRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesGetRuleArgs> for FirewallPolicyRule {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesGetRuleArgs) -> String {
        format!(
            "gcp::compute::FirewallPolicyRule/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyList
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyList with ComputeNetworkFirewallPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesListArgs> for FirewallPolicyList {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesListArgs) -> String {
        format!("gcp::compute::FirewallPolicyList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesPatchRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesPatchRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesPatchRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesRemoveAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesRemoveAssociationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesRemoveAssociationArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworkFirewallPoliciesRemoveRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesRemoveRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworkFirewallPoliciesRemoveRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeNetworkFirewallPoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeNetworkFirewallPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkFirewallPoliciesTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeNetworkFirewallPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkProfile
// =============================================================================

/// ResourceIdentifier implementation for NetworkProfile with ComputeNetworkProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkProfilesGetArgs> for NetworkProfile {
    fn generate_resource_id(&self, input: &ComputeNetworkProfilesGetArgs) -> String {
        format!(
            "gcp::compute::NetworkProfile/{}/{}",
            input.project, input.networkProfile
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkProfilesListResponse
// =============================================================================

/// ResourceIdentifier implementation for NetworkProfilesListResponse with ComputeNetworkProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworkProfilesListArgs> for NetworkProfilesListResponse {
    fn generate_resource_id(&self, input: &ComputeNetworkProfilesListArgs) -> String {
        format!(
            "gcp::compute::NetworkProfilesListResponse/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkProfilesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworksAddPeeringArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksAddPeeringArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworksAddPeeringArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Network
// =============================================================================

/// ResourceIdentifier implementation for Network with ComputeNetworksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksGetArgs> for Network {
    fn generate_resource_id(&self, input: &ComputeNetworksGetArgs) -> String {
        format!("gcp::compute::Network/{}/{}", input.project, input.network)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Network"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworksGetEffectiveFirewallsResponse
// =============================================================================

/// ResourceIdentifier implementation for NetworksGetEffectiveFirewallsResponse with ComputeNetworksGetEffectiveFirewallsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksGetEffectiveFirewallsArgs>
    for NetworksGetEffectiveFirewallsResponse
{
    fn generate_resource_id(&self, input: &ComputeNetworksGetEffectiveFirewallsArgs) -> String {
        format!(
            "gcp::compute::NetworksGetEffectiveFirewallsResponse/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworksGetEffectiveFirewallsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkList
// =============================================================================

/// ResourceIdentifier implementation for NetworkList with ComputeNetworksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksListArgs> for NetworkList {
    fn generate_resource_id(&self, input: &ComputeNetworksListArgs) -> String {
        format!("gcp::compute::NetworkList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ExchangedPeeringRoutesList
// =============================================================================

/// ResourceIdentifier implementation for ExchangedPeeringRoutesList with ComputeNetworksListPeeringRoutesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksListPeeringRoutesArgs> for ExchangedPeeringRoutesList {
    fn generate_resource_id(&self, input: &ComputeNetworksListPeeringRoutesArgs) -> String {
        format!(
            "gcp::compute::ExchangedPeeringRoutesList/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ExchangedPeeringRoutesList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworksPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworksRemovePeeringArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksRemovePeeringArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworksRemovePeeringArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworksRequestRemovePeeringArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksRequestRemovePeeringArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworksRequestRemovePeeringArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworksSwitchToCustomModeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksSwitchToCustomModeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworksSwitchToCustomModeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNetworksUpdatePeeringArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNetworksUpdatePeeringArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNetworksUpdatePeeringArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.network
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeGroupsAddNodesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsAddNodesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsAddNodesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeGroupAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for NodeGroupAggregatedList with ComputeNodeGroupsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsAggregatedListArgs> for NodeGroupAggregatedList {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsAggregatedListArgs) -> String {
        format!("gcp::compute::NodeGroupAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeGroupAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeGroupsDeleteNodesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsDeleteNodesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsDeleteNodesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeGroup
// =============================================================================

/// ResourceIdentifier implementation for NodeGroup with ComputeNodeGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsGetArgs> for NodeGroup {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsGetArgs) -> String {
        format!(
            "gcp::compute::NodeGroup/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeNodeGroupsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeGroupList
// =============================================================================

/// ResourceIdentifier implementation for NodeGroupList with ComputeNodeGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsListArgs> for NodeGroupList {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsListArgs) -> String {
        format!(
            "gcp::compute::NodeGroupList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeGroupList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeGroupsListNodes
// =============================================================================

/// ResourceIdentifier implementation for NodeGroupsListNodes with ComputeNodeGroupsListNodesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsListNodesArgs> for NodeGroupsListNodes {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsListNodesArgs) -> String {
        format!(
            "gcp::compute::NodeGroupsListNodes/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeGroupsListNodes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeGroupsPerformMaintenanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsPerformMaintenanceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsPerformMaintenanceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeNodeGroupsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeGroupsSetNodeTemplateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsSetNodeTemplateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsSetNodeTemplateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeGroupsSimulateMaintenanceEventArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsSimulateMaintenanceEventArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeNodeGroupsSimulateMaintenanceEventArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.nodeGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeNodeGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeGroupsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeNodeGroupsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeTemplateAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for NodeTemplateAggregatedList with ComputeNodeTemplatesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTemplatesAggregatedListArgs> for NodeTemplateAggregatedList {
    fn generate_resource_id(&self, input: &ComputeNodeTemplatesAggregatedListArgs) -> String {
        format!("gcp::compute::NodeTemplateAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeTemplateAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTemplatesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeTemplatesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.nodeTemplate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeTemplate
// =============================================================================

/// ResourceIdentifier implementation for NodeTemplate with ComputeNodeTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTemplatesGetArgs> for NodeTemplate {
    fn generate_resource_id(&self, input: &ComputeNodeTemplatesGetArgs) -> String {
        format!(
            "gcp::compute::NodeTemplate/{}/{}/{}",
            input.project, input.region, input.nodeTemplate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeNodeTemplatesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTemplatesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeNodeTemplatesGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeNodeTemplatesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTemplatesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeNodeTemplatesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeTemplateList
// =============================================================================

/// ResourceIdentifier implementation for NodeTemplateList with ComputeNodeTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTemplatesListArgs> for NodeTemplateList {
    fn generate_resource_id(&self, input: &ComputeNodeTemplatesListArgs) -> String {
        format!(
            "gcp::compute::NodeTemplateList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeTemplateList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeNodeTemplatesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTemplatesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeNodeTemplatesSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeNodeTemplatesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTemplatesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeNodeTemplatesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeTypeAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for NodeTypeAggregatedList with ComputeNodeTypesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTypesAggregatedListArgs> for NodeTypeAggregatedList {
    fn generate_resource_id(&self, input: &ComputeNodeTypesAggregatedListArgs) -> String {
        format!("gcp::compute::NodeTypeAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeTypeAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeType
// =============================================================================

/// ResourceIdentifier implementation for NodeType with ComputeNodeTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTypesGetArgs> for NodeType {
    fn generate_resource_id(&self, input: &ComputeNodeTypesGetArgs) -> String {
        format!(
            "gcp::compute::NodeType/{}/{}/{}",
            input.project, input.zone, input.nodeType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NodeTypeList
// =============================================================================

/// ResourceIdentifier implementation for NodeTypeList with ComputeNodeTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeNodeTypesListArgs> for NodeTypeList {
    fn generate_resource_id(&self, input: &ComputeNodeTypesListArgs) -> String {
        format!(
            "gcp::compute::NodeTypeList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NodeTypeList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesAddAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesAddAssociationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesAddAssociationArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesAddRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesAddRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesAddRuleArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesCopyRulesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesCopyRulesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesCopyRulesArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesDeleteArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicy
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicy with ComputeOrganizationSecurityPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesGetArgs> for SecurityPolicy {
    fn generate_resource_id(&self, input: &ComputeOrganizationSecurityPoliciesGetArgs) -> String {
        format!("gcp::compute::SecurityPolicy/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicyAssociation
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicyAssociation with ComputeOrganizationSecurityPoliciesGetAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesGetAssociationArgs>
    for SecurityPolicyAssociation
{
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesGetAssociationArgs,
    ) -> String {
        format!(
            "gcp::compute::SecurityPolicyAssociation/{}",
            input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicyAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicyRule
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicyRule with ComputeOrganizationSecurityPoliciesGetRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesGetRuleArgs> for SecurityPolicyRule {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesGetRuleArgs,
    ) -> String {
        format!("gcp::compute::SecurityPolicyRule/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicyRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesInsertArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesInsertArgs,
    ) -> String {
        "gcp::compute::Operation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicyList
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicyList with ComputeOrganizationSecurityPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesListArgs> for SecurityPolicyList {
    fn generate_resource_id(&self, input: &ComputeOrganizationSecurityPoliciesListArgs) -> String {
        "gcp::compute::SecurityPolicyList".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OrganizationSecurityPoliciesListAssociationsResponse
// =============================================================================

/// ResourceIdentifier implementation for OrganizationSecurityPoliciesListAssociationsResponse with ComputeOrganizationSecurityPoliciesListAssociationsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesListAssociationsArgs>
    for OrganizationSecurityPoliciesListAssociationsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesListAssociationsArgs,
    ) -> String {
        "gcp::compute::OrganizationSecurityPoliciesListAssociationsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::OrganizationSecurityPoliciesListAssociationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPoliciesListPreconfiguredExpressionSetsResponse
// =============================================================================

/// ResourceIdentifier implementation for SecurityPoliciesListPreconfiguredExpressionSetsResponse with ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs>
    for SecurityPoliciesListPreconfiguredExpressionSetsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs,
    ) -> String {
        "gcp::compute::SecurityPoliciesListPreconfiguredExpressionSetsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPoliciesListPreconfiguredExpressionSetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesMoveArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeOrganizationSecurityPoliciesMoveArgs) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeOrganizationSecurityPoliciesPatchArgs) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesPatchRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesPatchRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesPatchRuleArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesRemoveAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesRemoveAssociationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesRemoveAssociationArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeOrganizationSecurityPoliciesRemoveRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeOrganizationSecurityPoliciesRemoveRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeOrganizationSecurityPoliciesRemoveRuleArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}", input.securityPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PacketMirroringAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for PacketMirroringAggregatedList with ComputePacketMirroringsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePacketMirroringsAggregatedListArgs>
    for PacketMirroringAggregatedList
{
    fn generate_resource_id(&self, input: &ComputePacketMirroringsAggregatedListArgs) -> String {
        format!(
            "gcp::compute::PacketMirroringAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PacketMirroringAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePacketMirroringsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePacketMirroringsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePacketMirroringsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.packetMirroring
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PacketMirroring
// =============================================================================

/// ResourceIdentifier implementation for PacketMirroring with ComputePacketMirroringsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePacketMirroringsGetArgs> for PacketMirroring {
    fn generate_resource_id(&self, input: &ComputePacketMirroringsGetArgs) -> String {
        format!(
            "gcp::compute::PacketMirroring/{}/{}/{}",
            input.project, input.region, input.packetMirroring
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PacketMirroring"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePacketMirroringsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePacketMirroringsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePacketMirroringsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PacketMirroringList
// =============================================================================

/// ResourceIdentifier implementation for PacketMirroringList with ComputePacketMirroringsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePacketMirroringsListArgs> for PacketMirroringList {
    fn generate_resource_id(&self, input: &ComputePacketMirroringsListArgs) -> String {
        format!(
            "gcp::compute::PacketMirroringList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PacketMirroringList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePacketMirroringsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePacketMirroringsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePacketMirroringsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.packetMirroring
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputePacketMirroringsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePacketMirroringsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(
        &self,
        input: &ComputePacketMirroringsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PreviewFeature
// =============================================================================

/// ResourceIdentifier implementation for PreviewFeature with ComputePreviewFeaturesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePreviewFeaturesGetArgs> for PreviewFeature {
    fn generate_resource_id(&self, input: &ComputePreviewFeaturesGetArgs) -> String {
        format!(
            "gcp::compute::PreviewFeature/{}/{}",
            input.project, input.previewFeature
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PreviewFeature"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PreviewFeatureList
// =============================================================================

/// ResourceIdentifier implementation for PreviewFeatureList with ComputePreviewFeaturesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePreviewFeaturesListArgs> for PreviewFeatureList {
    fn generate_resource_id(&self, input: &ComputePreviewFeaturesListArgs) -> String {
        format!("gcp::compute::PreviewFeatureList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PreviewFeatureList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePreviewFeaturesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePreviewFeaturesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePreviewFeaturesUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.previewFeature
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsDisableXpnHostArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsDisableXpnHostArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsDisableXpnHostArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsDisableXpnResourceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsDisableXpnResourceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsDisableXpnResourceArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsEnableXpnHostArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsEnableXpnHostArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsEnableXpnHostArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsEnableXpnResourceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsEnableXpnResourceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsEnableXpnResourceArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Project
// =============================================================================

/// ResourceIdentifier implementation for Project with ComputeProjectsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsGetArgs> for Project {
    fn generate_resource_id(&self, input: &ComputeProjectsGetArgs) -> String {
        format!("gcp::compute::Project/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Project"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Project
// =============================================================================

/// ResourceIdentifier implementation for Project with ComputeProjectsGetXpnHostArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsGetXpnHostArgs> for Project {
    fn generate_resource_id(&self, input: &ComputeProjectsGetXpnHostArgs) -> String {
        format!("gcp::compute::Project/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Project"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProjectsGetXpnResources
// =============================================================================

/// ResourceIdentifier implementation for ProjectsGetXpnResources with ComputeProjectsGetXpnResourcesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsGetXpnResourcesArgs> for ProjectsGetXpnResources {
    fn generate_resource_id(&self, input: &ComputeProjectsGetXpnResourcesArgs) -> String {
        format!("gcp::compute::ProjectsGetXpnResources/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ProjectsGetXpnResources"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for XpnHostList
// =============================================================================

/// ResourceIdentifier implementation for XpnHostList with ComputeProjectsListXpnHostsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsListXpnHostsArgs> for XpnHostList {
    fn generate_resource_id(&self, input: &ComputeProjectsListXpnHostsArgs) -> String {
        format!("gcp::compute::XpnHostList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::XpnHostList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsMoveDiskArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsMoveDiskArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsMoveDiskArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsMoveInstanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsMoveInstanceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsMoveInstanceArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsSetCloudArmorTierArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsSetCloudArmorTierArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsSetCloudArmorTierArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsSetCommonInstanceMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsSetCommonInstanceMetadataArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsSetCommonInstanceMetadataArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsSetDefaultNetworkTierArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsSetDefaultNetworkTierArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsSetDefaultNetworkTierArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeProjectsSetUsageExportBucketArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeProjectsSetUsageExportBucketArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeProjectsSetUsageExportBucketArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicAdvertisedPrefixesAnnounceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicAdvertisedPrefixesAnnounceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicAdvertisedPrefixesAnnounceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.publicAdvertisedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicAdvertisedPrefixesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicAdvertisedPrefixesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicAdvertisedPrefixesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.publicAdvertisedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublicAdvertisedPrefix
// =============================================================================

/// ResourceIdentifier implementation for PublicAdvertisedPrefix with ComputePublicAdvertisedPrefixesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicAdvertisedPrefixesGetArgs> for PublicAdvertisedPrefix {
    fn generate_resource_id(&self, input: &ComputePublicAdvertisedPrefixesGetArgs) -> String {
        format!(
            "gcp::compute::PublicAdvertisedPrefix/{}/{}",
            input.project, input.publicAdvertisedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PublicAdvertisedPrefix"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicAdvertisedPrefixesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicAdvertisedPrefixesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicAdvertisedPrefixesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublicAdvertisedPrefixList
// =============================================================================

/// ResourceIdentifier implementation for PublicAdvertisedPrefixList with ComputePublicAdvertisedPrefixesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicAdvertisedPrefixesListArgs> for PublicAdvertisedPrefixList {
    fn generate_resource_id(&self, input: &ComputePublicAdvertisedPrefixesListArgs) -> String {
        format!("gcp::compute::PublicAdvertisedPrefixList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PublicAdvertisedPrefixList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicAdvertisedPrefixesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicAdvertisedPrefixesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicAdvertisedPrefixesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.publicAdvertisedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicAdvertisedPrefixesWithdrawArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicAdvertisedPrefixesWithdrawArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicAdvertisedPrefixesWithdrawArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.publicAdvertisedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublicDelegatedPrefixAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for PublicDelegatedPrefixAggregatedList with ComputePublicDelegatedPrefixesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicDelegatedPrefixesAggregatedListArgs>
    for PublicDelegatedPrefixAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputePublicDelegatedPrefixesAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::PublicDelegatedPrefixAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PublicDelegatedPrefixAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicDelegatedPrefixesAnnounceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicDelegatedPrefixesAnnounceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicDelegatedPrefixesAnnounceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.publicDelegatedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicDelegatedPrefixesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicDelegatedPrefixesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicDelegatedPrefixesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.publicDelegatedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublicDelegatedPrefix
// =============================================================================

/// ResourceIdentifier implementation for PublicDelegatedPrefix with ComputePublicDelegatedPrefixesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicDelegatedPrefixesGetArgs> for PublicDelegatedPrefix {
    fn generate_resource_id(&self, input: &ComputePublicDelegatedPrefixesGetArgs) -> String {
        format!(
            "gcp::compute::PublicDelegatedPrefix/{}/{}/{}",
            input.project, input.region, input.publicDelegatedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PublicDelegatedPrefix"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicDelegatedPrefixesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicDelegatedPrefixesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicDelegatedPrefixesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublicDelegatedPrefixList
// =============================================================================

/// ResourceIdentifier implementation for PublicDelegatedPrefixList with ComputePublicDelegatedPrefixesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicDelegatedPrefixesListArgs> for PublicDelegatedPrefixList {
    fn generate_resource_id(&self, input: &ComputePublicDelegatedPrefixesListArgs) -> String {
        format!(
            "gcp::compute::PublicDelegatedPrefixList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::PublicDelegatedPrefixList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicDelegatedPrefixesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicDelegatedPrefixesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicDelegatedPrefixesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.publicDelegatedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputePublicDelegatedPrefixesWithdrawArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputePublicDelegatedPrefixesWithdrawArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputePublicDelegatedPrefixesWithdrawArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.publicDelegatedPrefix
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionAutoscalersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionAutoscalersDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionAutoscalersDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.autoscaler
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Autoscaler
// =============================================================================

/// ResourceIdentifier implementation for Autoscaler with ComputeRegionAutoscalersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionAutoscalersGetArgs> for Autoscaler {
    fn generate_resource_id(&self, input: &ComputeRegionAutoscalersGetArgs) -> String {
        format!(
            "gcp::compute::Autoscaler/{}/{}/{}",
            input.project, input.region, input.autoscaler
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Autoscaler"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionAutoscalersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionAutoscalersInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionAutoscalersInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionAutoscalerList
// =============================================================================

/// ResourceIdentifier implementation for RegionAutoscalerList with ComputeRegionAutoscalersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionAutoscalersListArgs> for RegionAutoscalerList {
    fn generate_resource_id(&self, input: &ComputeRegionAutoscalersListArgs) -> String {
        format!(
            "gcp::compute::RegionAutoscalerList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionAutoscalerList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionAutoscalersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionAutoscalersPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionAutoscalersPatchArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionAutoscalersTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionAutoscalersTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionAutoscalersTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionAutoscalersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionAutoscalersUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionAutoscalersUpdateArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionBackendBucketsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionBackendBucketsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendBucket
// =============================================================================

/// ResourceIdentifier implementation for BackendBucket with ComputeRegionBackendBucketsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsGetArgs> for BackendBucket {
    fn generate_resource_id(&self, input: &ComputeRegionBackendBucketsGetArgs) -> String {
        format!(
            "gcp::compute::BackendBucket/{}/{}/{}",
            input.project, input.region, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionBackendBucketsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeRegionBackendBucketsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionBackendBucketsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionBackendBucketsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendBucketList
// =============================================================================

/// ResourceIdentifier implementation for BackendBucketList with ComputeRegionBackendBucketsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsListArgs> for BackendBucketList {
    fn generate_resource_id(&self, input: &ComputeRegionBackendBucketsListArgs) -> String {
        format!(
            "gcp::compute::BackendBucketList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendBucketList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendBucketListUsable
// =============================================================================

/// ResourceIdentifier implementation for BackendBucketListUsable with ComputeRegionBackendBucketsListUsableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsListUsableArgs> for BackendBucketListUsable {
    fn generate_resource_id(&self, input: &ComputeRegionBackendBucketsListUsableArgs) -> String {
        format!(
            "gcp::compute::BackendBucketListUsable/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendBucketListUsable"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionBackendBucketsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionBackendBucketsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.backendBucket
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionBackendBucketsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeRegionBackendBucketsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionBackendBucketsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendBucketsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionBackendBucketsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionBackendServicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendService
// =============================================================================

/// ResourceIdentifier implementation for BackendService with ComputeRegionBackendServicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesGetArgs> for BackendService {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesGetArgs) -> String {
        format!(
            "gcp::compute::BackendService/{}/{}/{}",
            input.project, input.region, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendService"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendServiceGroupHealth
// =============================================================================

/// ResourceIdentifier implementation for BackendServiceGroupHealth with ComputeRegionBackendServicesGetHealthArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesGetHealthArgs> for BackendServiceGroupHealth {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesGetHealthArgs) -> String {
        format!(
            "gcp::compute::BackendServiceGroupHealth/{}/{}/{}",
            input.project, input.region, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendServiceGroupHealth"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionBackendServicesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionBackendServicesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendServiceList
// =============================================================================

/// ResourceIdentifier implementation for BackendServiceList with ComputeRegionBackendServicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesListArgs> for BackendServiceList {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesListArgs) -> String {
        format!(
            "gcp::compute::BackendServiceList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendServiceList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendServiceListUsable
// =============================================================================

/// ResourceIdentifier implementation for BackendServiceListUsable with ComputeRegionBackendServicesListUsableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesListUsableArgs> for BackendServiceListUsable {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesListUsableArgs) -> String {
        format!(
            "gcp::compute::BackendServiceListUsable/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::BackendServiceListUsable"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionBackendServicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionBackendServicesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionBackendServicesSetSecurityPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesSetSecurityPolicyArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionBackendServicesSetSecurityPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionBackendServicesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionBackendServicesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionBackendServicesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionBackendServicesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionBackendServicesUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.backendService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CommitmentAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for CommitmentAggregatedList with ComputeRegionCommitmentsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCommitmentsAggregatedListArgs> for CommitmentAggregatedList {
    fn generate_resource_id(&self, input: &ComputeRegionCommitmentsAggregatedListArgs) -> String {
        format!("gcp::compute::CommitmentAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CommitmentAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Commitment
// =============================================================================

/// ResourceIdentifier implementation for Commitment with ComputeRegionCommitmentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCommitmentsGetArgs> for Commitment {
    fn generate_resource_id(&self, input: &ComputeRegionCommitmentsGetArgs) -> String {
        format!(
            "gcp::compute::Commitment/{}/{}/{}",
            input.project, input.region, input.commitment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Commitment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionCommitmentsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCommitmentsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionCommitmentsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CommitmentList
// =============================================================================

/// ResourceIdentifier implementation for CommitmentList with ComputeRegionCommitmentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCommitmentsListArgs> for CommitmentList {
    fn generate_resource_id(&self, input: &ComputeRegionCommitmentsListArgs) -> String {
        format!(
            "gcp::compute::CommitmentList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CommitmentList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionCommitmentsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCommitmentsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionCommitmentsUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.commitment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CompositeHealthCheckAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for CompositeHealthCheckAggregatedList with ComputeRegionCompositeHealthChecksAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCompositeHealthChecksAggregatedListArgs>
    for CompositeHealthCheckAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionCompositeHealthChecksAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::CompositeHealthCheckAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CompositeHealthCheckAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionCompositeHealthChecksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCompositeHealthChecksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionCompositeHealthChecksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.compositeHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CompositeHealthCheck
// =============================================================================

/// ResourceIdentifier implementation for CompositeHealthCheck with ComputeRegionCompositeHealthChecksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCompositeHealthChecksGetArgs> for CompositeHealthCheck {
    fn generate_resource_id(&self, input: &ComputeRegionCompositeHealthChecksGetArgs) -> String {
        format!(
            "gcp::compute::CompositeHealthCheck/{}/{}/{}",
            input.project, input.region, input.compositeHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CompositeHealthCheck"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CompositeHealthCheckHealth
// =============================================================================

/// ResourceIdentifier implementation for CompositeHealthCheckHealth with ComputeRegionCompositeHealthChecksGetHealthArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCompositeHealthChecksGetHealthArgs>
    for CompositeHealthCheckHealth
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionCompositeHealthChecksGetHealthArgs,
    ) -> String {
        format!(
            "gcp::compute::CompositeHealthCheckHealth/{}/{}/{}",
            input.project, input.region, input.compositeHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CompositeHealthCheckHealth"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionCompositeHealthChecksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCompositeHealthChecksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionCompositeHealthChecksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CompositeHealthCheckList
// =============================================================================

/// ResourceIdentifier implementation for CompositeHealthCheckList with ComputeRegionCompositeHealthChecksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCompositeHealthChecksListArgs> for CompositeHealthCheckList {
    fn generate_resource_id(&self, input: &ComputeRegionCompositeHealthChecksListArgs) -> String {
        format!(
            "gcp::compute::CompositeHealthCheckList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::CompositeHealthCheckList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionCompositeHealthChecksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCompositeHealthChecksPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionCompositeHealthChecksPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.compositeHealthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionCompositeHealthChecksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionCompositeHealthChecksTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionCompositeHealthChecksTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskType
// =============================================================================

/// ResourceIdentifier implementation for DiskType with ComputeRegionDiskTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDiskTypesGetArgs> for DiskType {
    fn generate_resource_id(&self, input: &ComputeRegionDiskTypesGetArgs) -> String {
        format!(
            "gcp::compute::DiskType/{}/{}/{}",
            input.project, input.region, input.diskType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::DiskType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionDiskTypeList
// =============================================================================

/// ResourceIdentifier implementation for RegionDiskTypeList with ComputeRegionDiskTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDiskTypesListArgs> for RegionDiskTypeList {
    fn generate_resource_id(&self, input: &ComputeRegionDiskTypesListArgs) -> String {
        format!(
            "gcp::compute::RegionDiskTypeList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionDiskTypeList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksAddResourcePoliciesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksAddResourcePoliciesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksAddResourcePoliciesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksBulkInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksBulkInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksBulkInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksCreateSnapshotArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksCreateSnapshotArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksCreateSnapshotArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Disk
// =============================================================================

/// ResourceIdentifier implementation for Disk with ComputeRegionDisksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksGetArgs> for Disk {
    fn generate_resource_id(&self, input: &ComputeRegionDisksGetArgs) -> String {
        format!(
            "gcp::compute::Disk/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Disk"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionDisksGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeRegionDisksGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskList
// =============================================================================

/// ResourceIdentifier implementation for DiskList with ComputeRegionDisksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksListArgs> for DiskList {
    fn generate_resource_id(&self, input: &ComputeRegionDisksListArgs) -> String {
        format!("gcp::compute::DiskList/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::DiskList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksRemoveResourcePoliciesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksRemoveResourcePoliciesArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksRemoveResourcePoliciesArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksResizeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksResizeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksResizeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionDisksSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeRegionDisksSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksStartAsyncReplicationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksStartAsyncReplicationArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksStartAsyncReplicationArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksStopAsyncReplicationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksStopAsyncReplicationArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksStopAsyncReplicationArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksStopGroupAsyncReplicationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksStopGroupAsyncReplicationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionDisksStopGroupAsyncReplicationArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionDisksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeRegionDisksTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionDisksUpdateKmsKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionDisksUpdateKmsKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionDisksUpdateKmsKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.disk
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthAggregationPolicyAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for HealthAggregationPolicyAggregatedList with ComputeRegionHealthAggregationPoliciesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthAggregationPoliciesAggregatedListArgs>
    for HealthAggregationPolicyAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthAggregationPoliciesAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::HealthAggregationPolicyAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthAggregationPolicyAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthAggregationPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthAggregationPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthAggregationPoliciesDeleteArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthAggregationPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthAggregationPolicy
// =============================================================================

/// ResourceIdentifier implementation for HealthAggregationPolicy with ComputeRegionHealthAggregationPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthAggregationPoliciesGetArgs> for HealthAggregationPolicy {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthAggregationPoliciesGetArgs,
    ) -> String {
        format!(
            "gcp::compute::HealthAggregationPolicy/{}/{}/{}",
            input.project, input.region, input.healthAggregationPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthAggregationPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthAggregationPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthAggregationPoliciesInsertArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthAggregationPoliciesInsertArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthAggregationPolicyList
// =============================================================================

/// ResourceIdentifier implementation for HealthAggregationPolicyList with ComputeRegionHealthAggregationPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthAggregationPoliciesListArgs>
    for HealthAggregationPolicyList
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthAggregationPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::compute::HealthAggregationPolicyList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthAggregationPolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthAggregationPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthAggregationPoliciesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthAggregationPoliciesPatchArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthAggregationPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthCheckServiceAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for HealthCheckServiceAggregatedList with ComputeRegionHealthCheckServicesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthCheckServicesAggregatedListArgs>
    for HealthCheckServiceAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthCheckServicesAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::HealthCheckServiceAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthCheckServiceAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthCheckServicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthCheckServicesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthCheckServicesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthCheckService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthCheckService
// =============================================================================

/// ResourceIdentifier implementation for HealthCheckService with ComputeRegionHealthCheckServicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthCheckServicesGetArgs> for HealthCheckService {
    fn generate_resource_id(&self, input: &ComputeRegionHealthCheckServicesGetArgs) -> String {
        format!(
            "gcp::compute::HealthCheckService/{}/{}/{}",
            input.project, input.region, input.healthCheckService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthCheckService"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthCheckServicesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthCheckServicesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthCheckServicesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthCheckServicesList
// =============================================================================

/// ResourceIdentifier implementation for HealthCheckServicesList with ComputeRegionHealthCheckServicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthCheckServicesListArgs> for HealthCheckServicesList {
    fn generate_resource_id(&self, input: &ComputeRegionHealthCheckServicesListArgs) -> String {
        format!(
            "gcp::compute::HealthCheckServicesList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthCheckServicesList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthCheckServicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthCheckServicesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthCheckServicesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthCheckService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionHealthCheckServicesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthCheckServicesTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthCheckServicesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthChecksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthChecksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthChecksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthCheck
// =============================================================================

/// ResourceIdentifier implementation for HealthCheck with ComputeRegionHealthChecksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthChecksGetArgs> for HealthCheck {
    fn generate_resource_id(&self, input: &ComputeRegionHealthChecksGetArgs) -> String {
        format!(
            "gcp::compute::HealthCheck/{}/{}/{}",
            input.project, input.region, input.healthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthCheck"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthChecksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthChecksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthChecksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthCheckList
// =============================================================================

/// ResourceIdentifier implementation for HealthCheckList with ComputeRegionHealthChecksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthChecksListArgs> for HealthCheckList {
    fn generate_resource_id(&self, input: &ComputeRegionHealthChecksListArgs) -> String {
        format!(
            "gcp::compute::HealthCheckList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthCheckList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthChecksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthChecksPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthChecksPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionHealthChecksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthChecksTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthChecksTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthChecksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthChecksUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthChecksUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthCheck
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthSourceAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for HealthSourceAggregatedList with ComputeRegionHealthSourcesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthSourcesAggregatedListArgs>
    for HealthSourceAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeRegionHealthSourcesAggregatedListArgs) -> String {
        format!("gcp::compute::HealthSourceAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthSourceAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthSourcesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthSourcesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthSourcesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthSource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthSource
// =============================================================================

/// ResourceIdentifier implementation for HealthSource with ComputeRegionHealthSourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthSourcesGetArgs> for HealthSource {
    fn generate_resource_id(&self, input: &ComputeRegionHealthSourcesGetArgs) -> String {
        format!(
            "gcp::compute::HealthSource/{}/{}/{}",
            input.project, input.region, input.healthSource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthSource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthSourceHealth
// =============================================================================

/// ResourceIdentifier implementation for HealthSourceHealth with ComputeRegionHealthSourcesGetHealthArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthSourcesGetHealthArgs> for HealthSourceHealth {
    fn generate_resource_id(&self, input: &ComputeRegionHealthSourcesGetHealthArgs) -> String {
        format!(
            "gcp::compute::HealthSourceHealth/{}/{}/{}",
            input.project, input.region, input.healthSource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthSourceHealth"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthSourcesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthSourcesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthSourcesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HealthSourceList
// =============================================================================

/// ResourceIdentifier implementation for HealthSourceList with ComputeRegionHealthSourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthSourcesListArgs> for HealthSourceList {
    fn generate_resource_id(&self, input: &ComputeRegionHealthSourcesListArgs) -> String {
        format!(
            "gcp::compute::HealthSourceList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::HealthSourceList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionHealthSourcesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthSourcesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionHealthSourcesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.healthSource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionHealthSourcesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionHealthSourcesTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionHealthSourcesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager, input.resizeRequest
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager, input.resizeRequest
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManagerResizeRequest
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManagerResizeRequest with ComputeRegionInstanceGroupManagerResizeRequestsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagerResizeRequestsGetArgs>
    for InstanceGroupManagerResizeRequest
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagerResizeRequestsGetArgs,
    ) -> String {
        format!(
            "gcp::compute::InstanceGroupManagerResizeRequest/{}/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager, input.resizeRequest
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManagerResizeRequest"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionInstanceGroupManagerResizeRequestsListResponse
// =============================================================================

/// ResourceIdentifier implementation for RegionInstanceGroupManagerResizeRequestsListResponse with ComputeRegionInstanceGroupManagerResizeRequestsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagerResizeRequestsListArgs>
    for RegionInstanceGroupManagerResizeRequestsListResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagerResizeRequestsListArgs,
    ) -> String {
        format!(
            "gcp::compute::RegionInstanceGroupManagerResizeRequestsListResponse/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionInstanceGroupManagerResizeRequestsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersAbandonInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersAbandonInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersAbandonInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersCreateInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersCreateInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersCreateInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupManagersDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersDeleteInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersDeleteInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersDeleteInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroupManager
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroupManager with ComputeRegionInstanceGroupManagersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersGetArgs> for InstanceGroupManager {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupManagersGetArgs) -> String {
        format!(
            "gcp::compute::InstanceGroupManager/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroupManager"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupManagersInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionInstanceGroupManagerList
// =============================================================================

/// ResourceIdentifier implementation for RegionInstanceGroupManagerList with ComputeRegionInstanceGroupManagersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersListArgs>
    for RegionInstanceGroupManagerList
{
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupManagersListArgs) -> String {
        format!(
            "gcp::compute::RegionInstanceGroupManagerList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionInstanceGroupManagerList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionInstanceGroupManagersListErrorsResponse
// =============================================================================

/// ResourceIdentifier implementation for RegionInstanceGroupManagersListErrorsResponse with ComputeRegionInstanceGroupManagersListErrorsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersListErrorsArgs>
    for RegionInstanceGroupManagersListErrorsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersListErrorsArgs,
    ) -> String {
        format!(
            "gcp::compute::RegionInstanceGroupManagersListErrorsResponse/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionInstanceGroupManagersListErrorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionInstanceGroupManagersListInstancesResponse
// =============================================================================

/// ResourceIdentifier implementation for RegionInstanceGroupManagersListInstancesResponse with ComputeRegionInstanceGroupManagersListManagedInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersListManagedInstancesArgs>
    for RegionInstanceGroupManagersListInstancesResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersListManagedInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::RegionInstanceGroupManagersListInstancesResponse/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionInstanceGroupManagersListInstancesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionInstanceGroupManagersListInstanceConfigsResp
// =============================================================================

/// ResourceIdentifier implementation for RegionInstanceGroupManagersListInstanceConfigsResp with ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs>
    for RegionInstanceGroupManagersListInstanceConfigsResp
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs,
    ) -> String {
        format!(
            "gcp::compute::RegionInstanceGroupManagersListInstanceConfigsResp/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionInstanceGroupManagersListInstanceConfigsResp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupManagersPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersRecreateInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersRecreateInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersRecreateInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersResizeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersResizeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupManagersResizeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersResumeInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersResumeInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersResumeInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersSetTargetPoolsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersSetTargetPoolsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersSetTargetPoolsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersStartInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersStartInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersStartInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersStopInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersStopInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersStopInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersSuspendInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersSuspendInstancesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersSuspendInstancesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroupManager
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceGroup
// =============================================================================

/// ResourceIdentifier implementation for InstanceGroup with ComputeRegionInstanceGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupsGetArgs> for InstanceGroup {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupsGetArgs) -> String {
        format!(
            "gcp::compute::InstanceGroup/{}/{}/{}",
            input.project, input.region, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionInstanceGroupList
// =============================================================================

/// ResourceIdentifier implementation for RegionInstanceGroupList with ComputeRegionInstanceGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupsListArgs> for RegionInstanceGroupList {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupsListArgs) -> String {
        format!(
            "gcp::compute::RegionInstanceGroupList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionInstanceGroupList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionInstanceGroupsListInstances
// =============================================================================

/// ResourceIdentifier implementation for RegionInstanceGroupsListInstances with ComputeRegionInstanceGroupsListInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupsListInstancesArgs>
    for RegionInstanceGroupsListInstances
{
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupsListInstancesArgs) -> String {
        format!(
            "gcp::compute::RegionInstanceGroupsListInstances/{}/{}/{}",
            input.project, input.region, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionInstanceGroupsListInstances"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceGroupsSetNamedPortsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupsSetNamedPortsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceGroupsSetNamedPortsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionInstanceGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceGroupsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstanceGroupsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceTemplatesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceTemplatesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instanceTemplate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceTemplate
// =============================================================================

/// ResourceIdentifier implementation for InstanceTemplate with ComputeRegionInstanceTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceTemplatesGetArgs> for InstanceTemplate {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceTemplatesGetArgs) -> String {
        format!(
            "gcp::compute::InstanceTemplate/{}/{}/{}",
            input.project, input.region, input.instanceTemplate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstanceTemplatesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceTemplatesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceTemplatesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstanceTemplateList
// =============================================================================

/// ResourceIdentifier implementation for InstanceTemplateList with ComputeRegionInstanceTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstanceTemplatesListArgs> for InstanceTemplateList {
    fn generate_resource_id(&self, input: &ComputeRegionInstanceTemplatesListArgs) -> String {
        format!(
            "gcp::compute::InstanceTemplateList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstanceTemplateList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstancesBulkInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstancesBulkInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstancesBulkInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstantSnapshotGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instantSnapshotGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstantSnapshotGroup
// =============================================================================

/// ResourceIdentifier implementation for InstantSnapshotGroup with ComputeRegionInstantSnapshotGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotGroupsGetArgs> for InstantSnapshotGroup {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotGroupsGetArgs) -> String {
        format!(
            "gcp::compute::InstantSnapshotGroup/{}/{}/{}",
            input.project, input.region, input.instantSnapshotGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstantSnapshotGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstantSnapshotGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotGroupsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInstantSnapshotGroups
// =============================================================================

/// ResourceIdentifier implementation for ListInstantSnapshotGroups with ComputeRegionInstantSnapshotGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotGroupsListArgs> for ListInstantSnapshotGroups {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotGroupsListArgs) -> String {
        format!(
            "gcp::compute::ListInstantSnapshotGroups/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ListInstantSnapshotGroups"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstantSnapshotsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.instantSnapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstantSnapshot
// =============================================================================

/// ResourceIdentifier implementation for InstantSnapshot with ComputeRegionInstantSnapshotsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotsGetArgs> for InstantSnapshot {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotsGetArgs) -> String {
        format!(
            "gcp::compute::InstantSnapshot/{}/{}/{}",
            input.project, input.region, input.instantSnapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstantSnapshot"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionInstantSnapshotsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstantSnapshotsGetIamPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstantSnapshotsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstantSnapshotList
// =============================================================================

/// ResourceIdentifier implementation for InstantSnapshotList with ComputeRegionInstantSnapshotsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotsListArgs> for InstantSnapshotList {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotsListArgs) -> String {
        format!(
            "gcp::compute::InstantSnapshotList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::InstantSnapshotList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionInstantSnapshotsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstantSnapshotsSetIamPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionInstantSnapshotsSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotsSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionInstantSnapshotsSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionInstantSnapshotsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionInstantSnapshotsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionInstantSnapshotsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkEndpointGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkEndpointGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionNetworkEndpointGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroup
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroup with ComputeRegionNetworkEndpointGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkEndpointGroupsGetArgs> for NetworkEndpointGroup {
    fn generate_resource_id(&self, input: &ComputeRegionNetworkEndpointGroupsGetArgs) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroup/{}/{}/{}",
            input.project, input.region, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkEndpointGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkEndpointGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionNetworkEndpointGroupsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroupList
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroupList with ComputeRegionNetworkEndpointGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkEndpointGroupsListArgs> for NetworkEndpointGroupList {
    fn generate_resource_id(&self, input: &ComputeRegionNetworkEndpointGroupsListArgs) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroupList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroupList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkEndpointGroupsListNetworkEndpoints
// =============================================================================

/// ResourceIdentifier implementation for NetworkEndpointGroupsListNetworkEndpoints with ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs>
    for NetworkEndpointGroupsListNetworkEndpoints
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs,
    ) -> String {
        format!(
            "gcp::compute::NetworkEndpointGroupsListNetworkEndpoints/{}/{}/{}",
            input.project, input.region, input.networkEndpointGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NetworkEndpointGroupsListNetworkEndpoints"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesAddAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesAddAssociationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesAddAssociationArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesAddRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesAddRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesAddRuleArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesCloneRulesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesCloneRulesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesCloneRulesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesDeleteArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicy
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicy with ComputeRegionNetworkFirewallPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesGetArgs> for FirewallPolicy {
    fn generate_resource_id(&self, input: &ComputeRegionNetworkFirewallPoliciesGetArgs) -> String {
        format!(
            "gcp::compute::FirewallPolicy/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyAssociation
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyAssociation with ComputeRegionNetworkFirewallPoliciesGetAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesGetAssociationArgs>
    for FirewallPolicyAssociation
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesGetAssociationArgs,
    ) -> String {
        format!(
            "gcp::compute::FirewallPolicyAssociation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse
// =============================================================================

/// ResourceIdentifier implementation for RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse with ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs>
    for RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs,
    ) -> String {
        format!(
            "gcp::compute::RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyRule
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyRule with ComputeRegionNetworkFirewallPoliciesGetRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesGetRuleArgs> for FirewallPolicyRule {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesGetRuleArgs,
    ) -> String {
        format!(
            "gcp::compute::FirewallPolicyRule/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesInsertArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesInsertArgs,
    ) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallPolicyList
// =============================================================================

/// ResourceIdentifier implementation for FirewallPolicyList with ComputeRegionNetworkFirewallPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesListArgs> for FirewallPolicyList {
    fn generate_resource_id(&self, input: &ComputeRegionNetworkFirewallPoliciesListArgs) -> String {
        format!(
            "gcp::compute::FirewallPolicyList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::FirewallPolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesPatchArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesPatchRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesPatchRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesPatchRuleArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.firewallPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs,
    ) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationEndpointAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for NotificationEndpointAggregatedList with ComputeRegionNotificationEndpointsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNotificationEndpointsAggregatedListArgs>
    for NotificationEndpointAggregatedList
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNotificationEndpointsAggregatedListArgs,
    ) -> String {
        format!(
            "gcp::compute::NotificationEndpointAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NotificationEndpointAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNotificationEndpointsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNotificationEndpointsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionNotificationEndpointsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.notificationEndpoint
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationEndpoint
// =============================================================================

/// ResourceIdentifier implementation for NotificationEndpoint with ComputeRegionNotificationEndpointsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNotificationEndpointsGetArgs> for NotificationEndpoint {
    fn generate_resource_id(&self, input: &ComputeRegionNotificationEndpointsGetArgs) -> String {
        format!(
            "gcp::compute::NotificationEndpoint/{}/{}/{}",
            input.project, input.region, input.notificationEndpoint
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NotificationEndpoint"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionNotificationEndpointsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNotificationEndpointsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionNotificationEndpointsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationEndpointList
// =============================================================================

/// ResourceIdentifier implementation for NotificationEndpointList with ComputeRegionNotificationEndpointsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNotificationEndpointsListArgs> for NotificationEndpointList {
    fn generate_resource_id(&self, input: &ComputeRegionNotificationEndpointsListArgs) -> String {
        format!(
            "gcp::compute::NotificationEndpointList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NotificationEndpointList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionNotificationEndpointsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionNotificationEndpointsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionNotificationEndpointsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionOperationsGetArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.operation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperationList
// =============================================================================

/// ResourceIdentifier implementation for OperationList with ComputeRegionOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionOperationsListArgs> for OperationList {
    fn generate_resource_id(&self, input: &ComputeRegionOperationsListArgs) -> String {
        format!(
            "gcp::compute::OperationList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::OperationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionOperationsWaitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionOperationsWaitArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionOperationsWaitArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.operation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSecurityPoliciesAddRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesAddRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesAddRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSecurityPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicy
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicy with ComputeRegionSecurityPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesGetArgs> for SecurityPolicy {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesGetArgs) -> String {
        format!(
            "gcp::compute::SecurityPolicy/{}/{}/{}",
            input.project, input.region, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicyRule
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicyRule with ComputeRegionSecurityPoliciesGetRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesGetRuleArgs> for SecurityPolicyRule {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesGetRuleArgs) -> String {
        format!(
            "gcp::compute::SecurityPolicyRule/{}/{}/{}",
            input.project, input.region, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicyRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSecurityPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicyList
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicyList with ComputeRegionSecurityPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesListArgs> for SecurityPolicyList {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesListArgs) -> String {
        format!(
            "gcp::compute::SecurityPolicyList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSecurityPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSecurityPoliciesPatchRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesPatchRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesPatchRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSecurityPoliciesRemoveRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesRemoveRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesRemoveRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSecurityPoliciesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSecurityPoliciesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSecurityPoliciesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SnapshotSettings
// =============================================================================

/// ResourceIdentifier implementation for SnapshotSettings with ComputeRegionSnapshotSettingsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotSettingsGetArgs> for SnapshotSettings {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotSettingsGetArgs) -> String {
        format!(
            "gcp::compute::SnapshotSettings/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SnapshotSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSnapshotSettingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotSettingsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotSettingsPatchArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSnapshotsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.snapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Snapshot
// =============================================================================

/// ResourceIdentifier implementation for Snapshot with ComputeRegionSnapshotsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsGetArgs> for Snapshot {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsGetArgs) -> String {
        format!(
            "gcp::compute::Snapshot/{}/{}/{}",
            input.project, input.region, input.snapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Snapshot"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionSnapshotsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSnapshotsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SnapshotList
// =============================================================================

/// ResourceIdentifier implementation for SnapshotList with ComputeRegionSnapshotsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsListArgs> for SnapshotList {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsListArgs) -> String {
        format!(
            "gcp::compute::SnapshotList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SnapshotList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeRegionSnapshotsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSnapshotsSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRegionSnapshotsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSnapshotsUpdateKmsKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSnapshotsUpdateKmsKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSnapshotsUpdateKmsKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.snapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSslCertificatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslCertificatesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSslCertificatesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.sslCertificate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCertificate
// =============================================================================

/// ResourceIdentifier implementation for SslCertificate with ComputeRegionSslCertificatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslCertificatesGetArgs> for SslCertificate {
    fn generate_resource_id(&self, input: &ComputeRegionSslCertificatesGetArgs) -> String {
        format!(
            "gcp::compute::SslCertificate/{}/{}/{}",
            input.project, input.region, input.sslCertificate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSslCertificatesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslCertificatesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSslCertificatesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCertificateList
// =============================================================================

/// ResourceIdentifier implementation for SslCertificateList with ComputeRegionSslCertificatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslCertificatesListArgs> for SslCertificateList {
    fn generate_resource_id(&self, input: &ComputeRegionSslCertificatesListArgs) -> String {
        format!(
            "gcp::compute::SslCertificateList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslCertificateList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSslPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSslPoliciesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.sslPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslPolicy
// =============================================================================

/// ResourceIdentifier implementation for SslPolicy with ComputeRegionSslPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslPoliciesGetArgs> for SslPolicy {
    fn generate_resource_id(&self, input: &ComputeRegionSslPoliciesGetArgs) -> String {
        format!(
            "gcp::compute::SslPolicy/{}/{}/{}",
            input.project, input.region, input.sslPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSslPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslPoliciesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSslPoliciesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslPoliciesList
// =============================================================================

/// ResourceIdentifier implementation for SslPoliciesList with ComputeRegionSslPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslPoliciesListArgs> for SslPoliciesList {
    fn generate_resource_id(&self, input: &ComputeRegionSslPoliciesListArgs) -> String {
        format!(
            "gcp::compute::SslPoliciesList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslPoliciesList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslPoliciesListAvailableFeaturesResponse
// =============================================================================

/// ResourceIdentifier implementation for SslPoliciesListAvailableFeaturesResponse with ComputeRegionSslPoliciesListAvailableFeaturesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslPoliciesListAvailableFeaturesArgs>
    for SslPoliciesListAvailableFeaturesResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeRegionSslPoliciesListAvailableFeaturesArgs,
    ) -> String {
        format!(
            "gcp::compute::SslPoliciesListAvailableFeaturesResponse/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslPoliciesListAvailableFeaturesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionSslPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionSslPoliciesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionSslPoliciesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.sslPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetHttpProxiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpProxiesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpProxiesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetHttpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpProxy
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpProxy with ComputeRegionTargetHttpProxiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpProxiesGetArgs> for TargetHttpProxy {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpProxiesGetArgs) -> String {
        format!(
            "gcp::compute::TargetHttpProxy/{}/{}/{}",
            input.project, input.region, input.targetHttpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpProxy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetHttpProxiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpProxiesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpProxiesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpProxyList
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpProxyList with ComputeRegionTargetHttpProxiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpProxiesListArgs> for TargetHttpProxyList {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpProxiesListArgs) -> String {
        format!(
            "gcp::compute::TargetHttpProxyList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpProxyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetHttpProxiesSetUrlMapArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpProxiesSetUrlMapArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpProxiesSetUrlMapArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetHttpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetHttpsProxiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpsProxiesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpsProxiesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpsProxy
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpsProxy with ComputeRegionTargetHttpsProxiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpsProxiesGetArgs> for TargetHttpsProxy {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpsProxiesGetArgs) -> String {
        format!(
            "gcp::compute::TargetHttpsProxy/{}/{}/{}",
            input.project, input.region, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpsProxy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetHttpsProxiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpsProxiesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpsProxiesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpsProxyList
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpsProxyList with ComputeRegionTargetHttpsProxiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpsProxiesListArgs> for TargetHttpsProxyList {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpsProxiesListArgs) -> String {
        format!(
            "gcp::compute::TargetHttpsProxyList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpsProxyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetHttpsProxiesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpsProxiesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpsProxiesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetHttpsProxiesSetUrlMapArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetHttpsProxiesSetUrlMapArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetHttpsProxiesSetUrlMapArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetTcpProxiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetTcpProxiesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetTcpProxiesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetTcpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetTcpProxy
// =============================================================================

/// ResourceIdentifier implementation for TargetTcpProxy with ComputeRegionTargetTcpProxiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetTcpProxiesGetArgs> for TargetTcpProxy {
    fn generate_resource_id(&self, input: &ComputeRegionTargetTcpProxiesGetArgs) -> String {
        format!(
            "gcp::compute::TargetTcpProxy/{}/{}/{}",
            input.project, input.region, input.targetTcpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetTcpProxy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionTargetTcpProxiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetTcpProxiesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionTargetTcpProxiesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetTcpProxyList
// =============================================================================

/// ResourceIdentifier implementation for TargetTcpProxyList with ComputeRegionTargetTcpProxiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionTargetTcpProxiesListArgs> for TargetTcpProxyList {
    fn generate_resource_id(&self, input: &ComputeRegionTargetTcpProxiesListArgs) -> String {
        format!(
            "gcp::compute::TargetTcpProxyList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetTcpProxyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionUrlMapsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionUrlMapsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionUrlMapsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.urlMap
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UrlMap
// =============================================================================

/// ResourceIdentifier implementation for UrlMap with ComputeRegionUrlMapsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionUrlMapsGetArgs> for UrlMap {
    fn generate_resource_id(&self, input: &ComputeRegionUrlMapsGetArgs) -> String {
        format!(
            "gcp::compute::UrlMap/{}/{}/{}",
            input.project, input.region, input.urlMap
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::UrlMap"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionUrlMapsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionUrlMapsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionUrlMapsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UrlMapList
// =============================================================================

/// ResourceIdentifier implementation for UrlMapList with ComputeRegionUrlMapsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionUrlMapsListArgs> for UrlMapList {
    fn generate_resource_id(&self, input: &ComputeRegionUrlMapsListArgs) -> String {
        format!(
            "gcp::compute::UrlMapList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::UrlMapList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionUrlMapsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionUrlMapsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionUrlMapsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.urlMap
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRegionUrlMapsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionUrlMapsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRegionUrlMapsUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.urlMap
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UrlMapsValidateResponse
// =============================================================================

/// ResourceIdentifier implementation for UrlMapsValidateResponse with ComputeRegionUrlMapsValidateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionUrlMapsValidateArgs> for UrlMapsValidateResponse {
    fn generate_resource_id(&self, input: &ComputeRegionUrlMapsValidateArgs) -> String {
        format!(
            "gcp::compute::UrlMapsValidateResponse/{}/{}/{}",
            input.project, input.region, input.urlMap
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::UrlMapsValidateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ZoneList
// =============================================================================

/// ResourceIdentifier implementation for ZoneList with ComputeRegionZonesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionZonesListArgs> for ZoneList {
    fn generate_resource_id(&self, input: &ComputeRegionZonesListArgs) -> String {
        format!("gcp::compute::ZoneList/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ZoneList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Region
// =============================================================================

/// ResourceIdentifier implementation for Region with ComputeRegionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionsGetArgs> for Region {
    fn generate_resource_id(&self, input: &ComputeRegionsGetArgs) -> String {
        format!("gcp::compute::Region/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Region"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionList
// =============================================================================

/// ResourceIdentifier implementation for RegionList with ComputeRegionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRegionsListArgs> for RegionList {
    fn generate_resource_id(&self, input: &ComputeRegionsListArgs) -> String {
        format!("gcp::compute::RegionList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RegionList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationBlocksGetResponse
// =============================================================================

/// ResourceIdentifier implementation for ReservationBlocksGetResponse with ComputeReservationBlocksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationBlocksGetArgs> for ReservationBlocksGetResponse {
    fn generate_resource_id(&self, input: &ComputeReservationBlocksGetArgs) -> String {
        format!(
            "gcp::compute::ReservationBlocksGetResponse/{}/{}/{}/{}",
            input.project, input.zone, input.reservation, input.reservationBlock
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ReservationBlocksGetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeReservationBlocksGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationBlocksGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeReservationBlocksGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}/{}",
            input.project, input.zone, input.parentResource, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationBlocksListResponse
// =============================================================================

/// ResourceIdentifier implementation for ReservationBlocksListResponse with ComputeReservationBlocksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationBlocksListArgs> for ReservationBlocksListResponse {
    fn generate_resource_id(&self, input: &ComputeReservationBlocksListArgs) -> String {
        format!(
            "gcp::compute::ReservationBlocksListResponse/{}/{}/{}",
            input.project, input.zone, input.reservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ReservationBlocksListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationBlocksPerformMaintenanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationBlocksPerformMaintenanceArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeReservationBlocksPerformMaintenanceArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.zone, input.reservation, input.reservationBlock
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeReservationBlocksSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationBlocksSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeReservationBlocksSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}/{}",
            input.project, input.zone, input.parentResource, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeReservationBlocksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationBlocksTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeReservationBlocksTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}/{}",
            input.project, input.zone, input.parentResource, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationSlotsGetResponse
// =============================================================================

/// ResourceIdentifier implementation for ReservationSlotsGetResponse with ComputeReservationSlotsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSlotsGetArgs> for ReservationSlotsGetResponse {
    fn generate_resource_id(&self, input: &ComputeReservationSlotsGetArgs) -> String {
        format!(
            "gcp::compute::ReservationSlotsGetResponse/{}/{}/{}/{}",
            input.project, input.zone, input.parentName, input.reservationSlot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ReservationSlotsGetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationSlotsGetVersionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSlotsGetVersionArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationSlotsGetVersionArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.zone, input.parentName, input.reservationSlot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationSlotsListResponse
// =============================================================================

/// ResourceIdentifier implementation for ReservationSlotsListResponse with ComputeReservationSlotsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSlotsListArgs> for ReservationSlotsListResponse {
    fn generate_resource_id(&self, input: &ComputeReservationSlotsListArgs) -> String {
        format!(
            "gcp::compute::ReservationSlotsListResponse/{}/{}/{}",
            input.project, input.zone, input.parentName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ReservationSlotsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationSlotsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSlotsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationSlotsUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.zone, input.parentName, input.reservationSlot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationSubBlocksGetResponse
// =============================================================================

/// ResourceIdentifier implementation for ReservationSubBlocksGetResponse with ComputeReservationSubBlocksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSubBlocksGetArgs> for ReservationSubBlocksGetResponse {
    fn generate_resource_id(&self, input: &ComputeReservationSubBlocksGetArgs) -> String {
        format!(
            "gcp::compute::ReservationSubBlocksGetResponse/{}/{}/{}/{}",
            input.project, input.zone, input.parentName, input.reservationSubBlock
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ReservationSubBlocksGetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeReservationSubBlocksGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSubBlocksGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeReservationSubBlocksGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}/{}",
            input.project, input.zone, input.parentResource, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationSubBlocksGetVersionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSubBlocksGetVersionArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationSubBlocksGetVersionArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.zone, input.parentName, input.reservationSubBlock
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationSubBlocksListResponse
// =============================================================================

/// ResourceIdentifier implementation for ReservationSubBlocksListResponse with ComputeReservationSubBlocksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSubBlocksListArgs> for ReservationSubBlocksListResponse {
    fn generate_resource_id(&self, input: &ComputeReservationSubBlocksListArgs) -> String {
        format!(
            "gcp::compute::ReservationSubBlocksListResponse/{}/{}/{}",
            input.project, input.zone, input.parentName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ReservationSubBlocksListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationSubBlocksPerformMaintenanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSubBlocksPerformMaintenanceArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeReservationSubBlocksPerformMaintenanceArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.zone, input.parentName, input.reservationSubBlock
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationSubBlocksReportFaultyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSubBlocksReportFaultyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationSubBlocksReportFaultyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}/{}",
            input.project, input.zone, input.parentName, input.reservationSubBlock
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeReservationSubBlocksSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSubBlocksSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeReservationSubBlocksSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}/{}",
            input.project, input.zone, input.parentResource, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeReservationSubBlocksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationSubBlocksTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeReservationSubBlocksTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}/{}",
            input.project, input.zone, input.parentResource, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for ReservationAggregatedList with ComputeReservationsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsAggregatedListArgs> for ReservationAggregatedList {
    fn generate_resource_id(&self, input: &ComputeReservationsAggregatedListArgs) -> String {
        format!("gcp::compute::ReservationAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ReservationAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.reservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reservation
// =============================================================================

/// ResourceIdentifier implementation for Reservation with ComputeReservationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsGetArgs> for Reservation {
    fn generate_resource_id(&self, input: &ComputeReservationsGetArgs) -> String {
        format!(
            "gcp::compute::Reservation/{}/{}/{}",
            input.project, input.zone, input.reservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Reservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeReservationsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeReservationsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReservationList
// =============================================================================

/// ResourceIdentifier implementation for ReservationList with ComputeReservationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsListArgs> for ReservationList {
    fn generate_resource_id(&self, input: &ComputeReservationsListArgs) -> String {
        format!(
            "gcp::compute::ReservationList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ReservationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationsPerformMaintenanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsPerformMaintenanceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationsPerformMaintenanceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.reservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationsResizeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsResizeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationsResizeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.reservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeReservationsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeReservationsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeReservationsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeReservationsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeReservationsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeReservationsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeReservationsUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.reservation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ResourcePolicyAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for ResourcePolicyAggregatedList with ComputeResourcePoliciesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesAggregatedListArgs>
    for ResourcePolicyAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeResourcePoliciesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::ResourcePolicyAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ResourcePolicyAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeResourcePoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeResourcePoliciesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resourcePolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ResourcePolicy
// =============================================================================

/// ResourceIdentifier implementation for ResourcePolicy with ComputeResourcePoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesGetArgs> for ResourcePolicy {
    fn generate_resource_id(&self, input: &ComputeResourcePoliciesGetArgs) -> String {
        format!(
            "gcp::compute::ResourcePolicy/{}/{}/{}",
            input.project, input.region, input.resourcePolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ResourcePolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeResourcePoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeResourcePoliciesGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeResourcePoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeResourcePoliciesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ResourcePolicyList
// =============================================================================

/// ResourceIdentifier implementation for ResourcePolicyList with ComputeResourcePoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesListArgs> for ResourcePolicyList {
    fn generate_resource_id(&self, input: &ComputeResourcePoliciesListArgs) -> String {
        format!(
            "gcp::compute::ResourcePolicyList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ResourcePolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeResourcePoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeResourcePoliciesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resourcePolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeResourcePoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeResourcePoliciesSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeResourcePoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeResourcePoliciesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(
        &self,
        input: &ComputeResourcePoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RouterAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for RouterAggregatedList with ComputeRoutersAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersAggregatedListArgs> for RouterAggregatedList {
    fn generate_resource_id(&self, input: &ComputeRoutersAggregatedListArgs) -> String {
        format!("gcp::compute::RouterAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RouterAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutersDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutersDeleteRoutePolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersDeleteRoutePolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutersDeleteRoutePolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Router
// =============================================================================

/// ResourceIdentifier implementation for Router with ComputeRoutersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersGetArgs> for Router {
    fn generate_resource_id(&self, input: &ComputeRoutersGetArgs) -> String {
        format!(
            "gcp::compute::Router/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Router"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NatIpInfoResponse
// =============================================================================

/// ResourceIdentifier implementation for NatIpInfoResponse with ComputeRoutersGetNatIpInfoArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersGetNatIpInfoArgs> for NatIpInfoResponse {
    fn generate_resource_id(&self, input: &ComputeRoutersGetNatIpInfoArgs) -> String {
        format!(
            "gcp::compute::NatIpInfoResponse/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::NatIpInfoResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VmEndpointNatMappingsList
// =============================================================================

/// ResourceIdentifier implementation for VmEndpointNatMappingsList with ComputeRoutersGetNatMappingInfoArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersGetNatMappingInfoArgs> for VmEndpointNatMappingsList {
    fn generate_resource_id(&self, input: &ComputeRoutersGetNatMappingInfoArgs) -> String {
        format!(
            "gcp::compute::VmEndpointNatMappingsList/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VmEndpointNatMappingsList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RoutersGetRoutePolicyResponse
// =============================================================================

/// ResourceIdentifier implementation for RoutersGetRoutePolicyResponse with ComputeRoutersGetRoutePolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersGetRoutePolicyArgs> for RoutersGetRoutePolicyResponse {
    fn generate_resource_id(&self, input: &ComputeRoutersGetRoutePolicyArgs) -> String {
        format!(
            "gcp::compute::RoutersGetRoutePolicyResponse/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RoutersGetRoutePolicyResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RouterStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for RouterStatusResponse with ComputeRoutersGetRouterStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersGetRouterStatusArgs> for RouterStatusResponse {
    fn generate_resource_id(&self, input: &ComputeRoutersGetRouterStatusArgs) -> String {
        format!(
            "gcp::compute::RouterStatusResponse/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RouterStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutersInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RouterList
// =============================================================================

/// ResourceIdentifier implementation for RouterList with ComputeRoutersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersListArgs> for RouterList {
    fn generate_resource_id(&self, input: &ComputeRoutersListArgs) -> String {
        format!(
            "gcp::compute::RouterList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RouterList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RoutersListBgpRoutes
// =============================================================================

/// ResourceIdentifier implementation for RoutersListBgpRoutes with ComputeRoutersListBgpRoutesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersListBgpRoutesArgs> for RoutersListBgpRoutes {
    fn generate_resource_id(&self, input: &ComputeRoutersListBgpRoutesArgs) -> String {
        format!(
            "gcp::compute::RoutersListBgpRoutes/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RoutersListBgpRoutes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RoutersListRoutePolicies
// =============================================================================

/// ResourceIdentifier implementation for RoutersListRoutePolicies with ComputeRoutersListRoutePoliciesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersListRoutePoliciesArgs> for RoutersListRoutePolicies {
    fn generate_resource_id(&self, input: &ComputeRoutersListRoutePoliciesArgs) -> String {
        format!(
            "gcp::compute::RoutersListRoutePolicies/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RoutersListRoutePolicies"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutersPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutersPatchRoutePolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersPatchRoutePolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutersPatchRoutePolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RoutersPreviewResponse
// =============================================================================

/// ResourceIdentifier implementation for RoutersPreviewResponse with ComputeRoutersPreviewArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersPreviewArgs> for RoutersPreviewResponse {
    fn generate_resource_id(&self, input: &ComputeRoutersPreviewArgs) -> String {
        format!(
            "gcp::compute::RoutersPreviewResponse/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RoutersPreviewResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutersUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutersUpdateRoutePolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutersUpdateRoutePolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutersUpdateRoutePolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.router
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutesDeleteArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.route)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Route
// =============================================================================

/// ResourceIdentifier implementation for Route with ComputeRoutesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutesGetArgs> for Route {
    fn generate_resource_id(&self, input: &ComputeRoutesGetArgs) -> String {
        format!("gcp::compute::Route/{}/{}", input.project, input.route)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Route"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeRoutesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeRoutesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RouteList
// =============================================================================

/// ResourceIdentifier implementation for RouteList with ComputeRoutesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutesListArgs> for RouteList {
    fn generate_resource_id(&self, input: &ComputeRoutesListArgs) -> String {
        format!("gcp::compute::RouteList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::RouteList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeRoutesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeRoutesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeRoutesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSecurityPoliciesAddRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesAddRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesAddRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPoliciesAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for SecurityPoliciesAggregatedList with ComputeSecurityPoliciesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesAggregatedListArgs>
    for SecurityPoliciesAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::SecurityPoliciesAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPoliciesAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSecurityPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicy
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicy with ComputeSecurityPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesGetArgs> for SecurityPolicy {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesGetArgs) -> String {
        format!(
            "gcp::compute::SecurityPolicy/{}/{}",
            input.project, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicyRule
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicyRule with ComputeSecurityPoliciesGetRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesGetRuleArgs> for SecurityPolicyRule {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesGetRuleArgs) -> String {
        format!(
            "gcp::compute::SecurityPolicyRule/{}/{}",
            input.project, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicyRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSecurityPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPolicyList
// =============================================================================

/// ResourceIdentifier implementation for SecurityPolicyList with ComputeSecurityPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesListArgs> for SecurityPolicyList {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesListArgs) -> String {
        format!("gcp::compute::SecurityPolicyList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityPoliciesListPreconfiguredExpressionSetsResponse
// =============================================================================

/// ResourceIdentifier implementation for SecurityPoliciesListPreconfiguredExpressionSetsResponse with ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs>
    for SecurityPoliciesListPreconfiguredExpressionSetsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs,
    ) -> String {
        format!(
            "gcp::compute::SecurityPoliciesListPreconfiguredExpressionSetsResponse/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SecurityPoliciesListPreconfiguredExpressionSetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSecurityPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSecurityPoliciesPatchRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesPatchRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesPatchRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSecurityPoliciesRemoveRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesRemoveRuleArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesRemoveRuleArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.securityPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSecurityPoliciesSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSecurityPoliciesSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSecurityPoliciesSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceAttachmentAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for ServiceAttachmentAggregatedList with ComputeServiceAttachmentsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsAggregatedListArgs>
    for ServiceAttachmentAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeServiceAttachmentsAggregatedListArgs) -> String {
        format!(
            "gcp::compute::ServiceAttachmentAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ServiceAttachmentAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeServiceAttachmentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeServiceAttachmentsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.serviceAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceAttachment
// =============================================================================

/// ResourceIdentifier implementation for ServiceAttachment with ComputeServiceAttachmentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsGetArgs> for ServiceAttachment {
    fn generate_resource_id(&self, input: &ComputeServiceAttachmentsGetArgs) -> String {
        format!(
            "gcp::compute::ServiceAttachment/{}/{}/{}",
            input.project, input.region, input.serviceAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ServiceAttachment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeServiceAttachmentsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeServiceAttachmentsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeServiceAttachmentsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeServiceAttachmentsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceAttachmentList
// =============================================================================

/// ResourceIdentifier implementation for ServiceAttachmentList with ComputeServiceAttachmentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsListArgs> for ServiceAttachmentList {
    fn generate_resource_id(&self, input: &ComputeServiceAttachmentsListArgs) -> String {
        format!(
            "gcp::compute::ServiceAttachmentList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ServiceAttachmentList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeServiceAttachmentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeServiceAttachmentsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.serviceAttachment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeServiceAttachmentsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeServiceAttachmentsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeServiceAttachmentsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeServiceAttachmentsTestIamPermissionsArgs>
    for TestPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ComputeServiceAttachmentsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SnapshotSettings
// =============================================================================

/// ResourceIdentifier implementation for SnapshotSettings with ComputeSnapshotSettingsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotSettingsGetArgs> for SnapshotSettings {
    fn generate_resource_id(&self, input: &ComputeSnapshotSettingsGetArgs) -> String {
        format!("gcp::compute::SnapshotSettings/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SnapshotSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSnapshotSettingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotSettingsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSnapshotSettingsPatchArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSnapshotsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSnapshotsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.snapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Snapshot
// =============================================================================

/// ResourceIdentifier implementation for Snapshot with ComputeSnapshotsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsGetArgs> for Snapshot {
    fn generate_resource_id(&self, input: &ComputeSnapshotsGetArgs) -> String {
        format!(
            "gcp::compute::Snapshot/{}/{}",
            input.project, input.snapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Snapshot"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeSnapshotsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeSnapshotsGetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSnapshotsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSnapshotsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SnapshotList
// =============================================================================

/// ResourceIdentifier implementation for SnapshotList with ComputeSnapshotsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsListArgs> for SnapshotList {
    fn generate_resource_id(&self, input: &ComputeSnapshotsListArgs) -> String {
        format!("gcp::compute::SnapshotList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SnapshotList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeSnapshotsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeSnapshotsSetIamPolicyArgs) -> String {
        format!("gcp::compute::Policy/{}/{}", input.project, input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSnapshotsSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSnapshotsSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeSnapshotsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeSnapshotsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSnapshotsUpdateKmsKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSnapshotsUpdateKmsKeyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSnapshotsUpdateKmsKeyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.snapshot
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCertificateAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for SslCertificateAggregatedList with ComputeSslCertificatesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslCertificatesAggregatedListArgs> for SslCertificateAggregatedList {
    fn generate_resource_id(&self, input: &ComputeSslCertificatesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::SslCertificateAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslCertificateAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSslCertificatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslCertificatesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSslCertificatesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.sslCertificate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCertificate
// =============================================================================

/// ResourceIdentifier implementation for SslCertificate with ComputeSslCertificatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslCertificatesGetArgs> for SslCertificate {
    fn generate_resource_id(&self, input: &ComputeSslCertificatesGetArgs) -> String {
        format!(
            "gcp::compute::SslCertificate/{}/{}",
            input.project, input.sslCertificate
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSslCertificatesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslCertificatesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSslCertificatesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCertificateList
// =============================================================================

/// ResourceIdentifier implementation for SslCertificateList with ComputeSslCertificatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslCertificatesListArgs> for SslCertificateList {
    fn generate_resource_id(&self, input: &ComputeSslCertificatesListArgs) -> String {
        format!("gcp::compute::SslCertificateList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslCertificateList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslPoliciesAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for SslPoliciesAggregatedList with ComputeSslPoliciesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslPoliciesAggregatedListArgs> for SslPoliciesAggregatedList {
    fn generate_resource_id(&self, input: &ComputeSslPoliciesAggregatedListArgs) -> String {
        format!("gcp::compute::SslPoliciesAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslPoliciesAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSslPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSslPoliciesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.sslPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslPolicy
// =============================================================================

/// ResourceIdentifier implementation for SslPolicy with ComputeSslPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslPoliciesGetArgs> for SslPolicy {
    fn generate_resource_id(&self, input: &ComputeSslPoliciesGetArgs) -> String {
        format!(
            "gcp::compute::SslPolicy/{}/{}",
            input.project, input.sslPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSslPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslPoliciesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSslPoliciesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslPoliciesList
// =============================================================================

/// ResourceIdentifier implementation for SslPoliciesList with ComputeSslPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslPoliciesListArgs> for SslPoliciesList {
    fn generate_resource_id(&self, input: &ComputeSslPoliciesListArgs) -> String {
        format!("gcp::compute::SslPoliciesList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslPoliciesList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslPoliciesListAvailableFeaturesResponse
// =============================================================================

/// ResourceIdentifier implementation for SslPoliciesListAvailableFeaturesResponse with ComputeSslPoliciesListAvailableFeaturesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslPoliciesListAvailableFeaturesArgs>
    for SslPoliciesListAvailableFeaturesResponse
{
    fn generate_resource_id(&self, input: &ComputeSslPoliciesListAvailableFeaturesArgs) -> String {
        format!(
            "gcp::compute::SslPoliciesListAvailableFeaturesResponse/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SslPoliciesListAvailableFeaturesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSslPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSslPoliciesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSslPoliciesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.sslPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoragePoolTypeAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for StoragePoolTypeAggregatedList with ComputeStoragePoolTypesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolTypesAggregatedListArgs>
    for StoragePoolTypeAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeStoragePoolTypesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::StoragePoolTypeAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::StoragePoolTypeAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoragePoolType
// =============================================================================

/// ResourceIdentifier implementation for StoragePoolType with ComputeStoragePoolTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolTypesGetArgs> for StoragePoolType {
    fn generate_resource_id(&self, input: &ComputeStoragePoolTypesGetArgs) -> String {
        format!(
            "gcp::compute::StoragePoolType/{}/{}/{}",
            input.project, input.zone, input.storagePoolType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::StoragePoolType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoragePoolTypeList
// =============================================================================

/// ResourceIdentifier implementation for StoragePoolTypeList with ComputeStoragePoolTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolTypesListArgs> for StoragePoolTypeList {
    fn generate_resource_id(&self, input: &ComputeStoragePoolTypesListArgs) -> String {
        format!(
            "gcp::compute::StoragePoolTypeList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::StoragePoolTypeList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoragePoolAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for StoragePoolAggregatedList with ComputeStoragePoolsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsAggregatedListArgs> for StoragePoolAggregatedList {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsAggregatedListArgs) -> String {
        format!("gcp::compute::StoragePoolAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::StoragePoolAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeStoragePoolsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.storagePool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoragePool
// =============================================================================

/// ResourceIdentifier implementation for StoragePool with ComputeStoragePoolsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsGetArgs> for StoragePool {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsGetArgs) -> String {
        format!(
            "gcp::compute::StoragePool/{}/{}/{}",
            input.project, input.zone, input.storagePool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::StoragePool"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeStoragePoolsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeStoragePoolsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoragePoolList
// =============================================================================

/// ResourceIdentifier implementation for StoragePoolList with ComputeStoragePoolsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsListArgs> for StoragePoolList {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsListArgs) -> String {
        format!(
            "gcp::compute::StoragePoolList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::StoragePoolList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StoragePoolListDisks
// =============================================================================

/// ResourceIdentifier implementation for StoragePoolListDisks with ComputeStoragePoolsListDisksArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsListDisksArgs> for StoragePoolListDisks {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsListDisksArgs) -> String {
        format!(
            "gcp::compute::StoragePoolListDisks/{}/{}/{}",
            input.project, input.zone, input.storagePool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::StoragePoolListDisks"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeStoragePoolsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeStoragePoolsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeStoragePoolsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeStoragePoolsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeStoragePoolsUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.storagePool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SubnetworkAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for SubnetworkAggregatedList with ComputeSubnetworksAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksAggregatedListArgs> for SubnetworkAggregatedList {
    fn generate_resource_id(&self, input: &ComputeSubnetworksAggregatedListArgs) -> String {
        format!("gcp::compute::SubnetworkAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SubnetworkAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSubnetworksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSubnetworksDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.subnetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSubnetworksExpandIpCidrRangeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksExpandIpCidrRangeArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSubnetworksExpandIpCidrRangeArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.subnetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subnetwork
// =============================================================================

/// ResourceIdentifier implementation for Subnetwork with ComputeSubnetworksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksGetArgs> for Subnetwork {
    fn generate_resource_id(&self, input: &ComputeSubnetworksGetArgs) -> String {
        format!(
            "gcp::compute::Subnetwork/{}/{}/{}",
            input.project, input.region, input.subnetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Subnetwork"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeSubnetworksGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeSubnetworksGetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSubnetworksInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSubnetworksInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SubnetworkList
// =============================================================================

/// ResourceIdentifier implementation for SubnetworkList with ComputeSubnetworksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksListArgs> for SubnetworkList {
    fn generate_resource_id(&self, input: &ComputeSubnetworksListArgs) -> String {
        format!(
            "gcp::compute::SubnetworkList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::SubnetworkList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UsableSubnetworksAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for UsableSubnetworksAggregatedList with ComputeSubnetworksListUsableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksListUsableArgs> for UsableSubnetworksAggregatedList {
    fn generate_resource_id(&self, input: &ComputeSubnetworksListUsableArgs) -> String {
        format!(
            "gcp::compute::UsableSubnetworksAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::UsableSubnetworksAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSubnetworksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeSubnetworksPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.subnetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ComputeSubnetworksSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &ComputeSubnetworksSetIamPolicyArgs) -> String {
        format!(
            "gcp::compute::Policy/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeSubnetworksSetPrivateIpGoogleAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksSetPrivateIpGoogleAccessArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeSubnetworksSetPrivateIpGoogleAccessArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.subnetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeSubnetworksTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeSubnetworksTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeSubnetworksTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetGrpcProxiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetGrpcProxiesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetGrpcProxiesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetGrpcProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetGrpcProxy
// =============================================================================

/// ResourceIdentifier implementation for TargetGrpcProxy with ComputeTargetGrpcProxiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetGrpcProxiesGetArgs> for TargetGrpcProxy {
    fn generate_resource_id(&self, input: &ComputeTargetGrpcProxiesGetArgs) -> String {
        format!(
            "gcp::compute::TargetGrpcProxy/{}/{}",
            input.project, input.targetGrpcProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetGrpcProxy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetGrpcProxiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetGrpcProxiesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetGrpcProxiesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetGrpcProxyList
// =============================================================================

/// ResourceIdentifier implementation for TargetGrpcProxyList with ComputeTargetGrpcProxiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetGrpcProxiesListArgs> for TargetGrpcProxyList {
    fn generate_resource_id(&self, input: &ComputeTargetGrpcProxiesListArgs) -> String {
        format!("gcp::compute::TargetGrpcProxyList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetGrpcProxyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetGrpcProxiesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetGrpcProxiesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetGrpcProxiesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetGrpcProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpProxyAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpProxyAggregatedList with ComputeTargetHttpProxiesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpProxiesAggregatedListArgs>
    for TargetHttpProxyAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeTargetHttpProxiesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::TargetHttpProxyAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpProxyAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpProxiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpProxiesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpProxiesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpProxy
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpProxy with ComputeTargetHttpProxiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpProxiesGetArgs> for TargetHttpProxy {
    fn generate_resource_id(&self, input: &ComputeTargetHttpProxiesGetArgs) -> String {
        format!(
            "gcp::compute::TargetHttpProxy/{}/{}",
            input.project, input.targetHttpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpProxy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpProxiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpProxiesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpProxiesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpProxyList
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpProxyList with ComputeTargetHttpProxiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpProxiesListArgs> for TargetHttpProxyList {
    fn generate_resource_id(&self, input: &ComputeTargetHttpProxiesListArgs) -> String {
        format!("gcp::compute::TargetHttpProxyList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpProxyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpProxiesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpProxiesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpProxiesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpProxiesSetUrlMapArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpProxiesSetUrlMapArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpProxiesSetUrlMapArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpsProxyAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpsProxyAggregatedList with ComputeTargetHttpsProxiesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesAggregatedListArgs>
    for TargetHttpsProxyAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::TargetHttpsProxyAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpsProxyAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpsProxiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpsProxy
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpsProxy with ComputeTargetHttpsProxiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesGetArgs> for TargetHttpsProxy {
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesGetArgs) -> String {
        format!(
            "gcp::compute::TargetHttpsProxy/{}/{}",
            input.project, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpsProxy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpsProxiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetHttpsProxyList
// =============================================================================

/// ResourceIdentifier implementation for TargetHttpsProxyList with ComputeTargetHttpsProxiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesListArgs> for TargetHttpsProxyList {
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesListArgs) -> String {
        format!("gcp::compute::TargetHttpsProxyList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetHttpsProxyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpsProxiesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpsProxiesSetCertificateMapArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesSetCertificateMapArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeTargetHttpsProxiesSetCertificateMapArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpsProxiesSetQuicOverrideArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesSetQuicOverrideArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesSetQuicOverrideArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpsProxiesSetSslCertificatesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesSetSslCertificatesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeTargetHttpsProxiesSetSslCertificatesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpsProxiesSetSslPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesSetSslPolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesSetSslPolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetHttpsProxiesSetUrlMapArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetHttpsProxiesSetUrlMapArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetHttpsProxiesSetUrlMapArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetHttpsProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetInstanceAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for TargetInstanceAggregatedList with ComputeTargetInstancesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetInstancesAggregatedListArgs> for TargetInstanceAggregatedList {
    fn generate_resource_id(&self, input: &ComputeTargetInstancesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::TargetInstanceAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetInstanceAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetInstancesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetInstancesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetInstancesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.targetInstance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetInstance
// =============================================================================

/// ResourceIdentifier implementation for TargetInstance with ComputeTargetInstancesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetInstancesGetArgs> for TargetInstance {
    fn generate_resource_id(&self, input: &ComputeTargetInstancesGetArgs) -> String {
        format!(
            "gcp::compute::TargetInstance/{}/{}/{}",
            input.project, input.zone, input.targetInstance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetInstance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetInstancesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetInstancesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetInstancesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetInstanceList
// =============================================================================

/// ResourceIdentifier implementation for TargetInstanceList with ComputeTargetInstancesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetInstancesListArgs> for TargetInstanceList {
    fn generate_resource_id(&self, input: &ComputeTargetInstancesListArgs) -> String {
        format!(
            "gcp::compute::TargetInstanceList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetInstanceList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetInstancesSetSecurityPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetInstancesSetSecurityPolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetInstancesSetSecurityPolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.targetInstance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeTargetInstancesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetInstancesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeTargetInstancesTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.zone, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetPoolsAddHealthCheckArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsAddHealthCheckArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsAddHealthCheckArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetPoolsAddInstanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsAddInstanceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsAddInstanceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetPoolAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for TargetPoolAggregatedList with ComputeTargetPoolsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsAggregatedListArgs> for TargetPoolAggregatedList {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsAggregatedListArgs) -> String {
        format!("gcp::compute::TargetPoolAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetPoolAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetPoolsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetPool
// =============================================================================

/// ResourceIdentifier implementation for TargetPool with ComputeTargetPoolsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsGetArgs> for TargetPool {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsGetArgs) -> String {
        format!(
            "gcp::compute::TargetPool/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetPool"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetPoolInstanceHealth
// =============================================================================

/// ResourceIdentifier implementation for TargetPoolInstanceHealth with ComputeTargetPoolsGetHealthArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsGetHealthArgs> for TargetPoolInstanceHealth {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsGetHealthArgs) -> String {
        format!(
            "gcp::compute::TargetPoolInstanceHealth/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetPoolInstanceHealth"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetPoolsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetPoolList
// =============================================================================

/// ResourceIdentifier implementation for TargetPoolList with ComputeTargetPoolsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsListArgs> for TargetPoolList {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsListArgs) -> String {
        format!(
            "gcp::compute::TargetPoolList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetPoolList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetPoolsRemoveHealthCheckArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsRemoveHealthCheckArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsRemoveHealthCheckArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetPoolsRemoveInstanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsRemoveInstanceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsRemoveInstanceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetPoolsSetBackupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsSetBackupArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsSetBackupArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetPoolsSetSecurityPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsSetSecurityPolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsSetSecurityPolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetPool
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeTargetPoolsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetPoolsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeTargetPoolsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetSslProxiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetSslProxiesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetSslProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetSslProxy
// =============================================================================

/// ResourceIdentifier implementation for TargetSslProxy with ComputeTargetSslProxiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesGetArgs> for TargetSslProxy {
    fn generate_resource_id(&self, input: &ComputeTargetSslProxiesGetArgs) -> String {
        format!(
            "gcp::compute::TargetSslProxy/{}/{}",
            input.project, input.targetSslProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetSslProxy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetSslProxiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetSslProxiesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetSslProxyList
// =============================================================================

/// ResourceIdentifier implementation for TargetSslProxyList with ComputeTargetSslProxiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesListArgs> for TargetSslProxyList {
    fn generate_resource_id(&self, input: &ComputeTargetSslProxiesListArgs) -> String {
        format!("gcp::compute::TargetSslProxyList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetSslProxyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetSslProxiesSetBackendServiceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesSetBackendServiceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetSslProxiesSetBackendServiceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetSslProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetSslProxiesSetCertificateMapArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesSetCertificateMapArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetSslProxiesSetCertificateMapArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetSslProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetSslProxiesSetProxyHeaderArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesSetProxyHeaderArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetSslProxiesSetProxyHeaderArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetSslProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetSslProxiesSetSslCertificatesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesSetSslCertificatesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ComputeTargetSslProxiesSetSslCertificatesArgs,
    ) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetSslProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetSslProxiesSetSslPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesSetSslPolicyArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetSslProxiesSetSslPolicyArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetSslProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeTargetSslProxiesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetSslProxiesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(
        &self,
        input: &ComputeTargetSslProxiesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetTcpProxyAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for TargetTcpProxyAggregatedList with ComputeTargetTcpProxiesAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetTcpProxiesAggregatedListArgs>
    for TargetTcpProxyAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeTargetTcpProxiesAggregatedListArgs) -> String {
        format!(
            "gcp::compute::TargetTcpProxyAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetTcpProxyAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetTcpProxiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetTcpProxiesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetTcpProxiesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetTcpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetTcpProxy
// =============================================================================

/// ResourceIdentifier implementation for TargetTcpProxy with ComputeTargetTcpProxiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetTcpProxiesGetArgs> for TargetTcpProxy {
    fn generate_resource_id(&self, input: &ComputeTargetTcpProxiesGetArgs) -> String {
        format!(
            "gcp::compute::TargetTcpProxy/{}/{}",
            input.project, input.targetTcpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetTcpProxy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetTcpProxiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetTcpProxiesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetTcpProxiesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetTcpProxyList
// =============================================================================

/// ResourceIdentifier implementation for TargetTcpProxyList with ComputeTargetTcpProxiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetTcpProxiesListArgs> for TargetTcpProxyList {
    fn generate_resource_id(&self, input: &ComputeTargetTcpProxiesListArgs) -> String {
        format!("gcp::compute::TargetTcpProxyList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetTcpProxyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetTcpProxiesSetBackendServiceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetTcpProxiesSetBackendServiceArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetTcpProxiesSetBackendServiceArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetTcpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetTcpProxiesSetProxyHeaderArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetTcpProxiesSetProxyHeaderArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetTcpProxiesSetProxyHeaderArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.targetTcpProxy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeTargetTcpProxiesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetTcpProxiesTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(
        &self,
        input: &ComputeTargetTcpProxiesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetVpnGatewayAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for TargetVpnGatewayAggregatedList with ComputeTargetVpnGatewaysAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetVpnGatewaysAggregatedListArgs>
    for TargetVpnGatewayAggregatedList
{
    fn generate_resource_id(&self, input: &ComputeTargetVpnGatewaysAggregatedListArgs) -> String {
        format!(
            "gcp::compute::TargetVpnGatewayAggregatedList/{}",
            input.project
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetVpnGatewayAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetVpnGatewaysDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetVpnGatewaysDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetVpnGatewaysDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.targetVpnGateway
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetVpnGateway
// =============================================================================

/// ResourceIdentifier implementation for TargetVpnGateway with ComputeTargetVpnGatewaysGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetVpnGatewaysGetArgs> for TargetVpnGateway {
    fn generate_resource_id(&self, input: &ComputeTargetVpnGatewaysGetArgs) -> String {
        format!(
            "gcp::compute::TargetVpnGateway/{}/{}/{}",
            input.project, input.region, input.targetVpnGateway
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetVpnGateway"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetVpnGatewaysInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetVpnGatewaysInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetVpnGatewaysInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetVpnGatewayList
// =============================================================================

/// ResourceIdentifier implementation for TargetVpnGatewayList with ComputeTargetVpnGatewaysListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetVpnGatewaysListArgs> for TargetVpnGatewayList {
    fn generate_resource_id(&self, input: &ComputeTargetVpnGatewaysListArgs) -> String {
        format!(
            "gcp::compute::TargetVpnGatewayList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TargetVpnGatewayList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeTargetVpnGatewaysSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeTargetVpnGatewaysSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeTargetVpnGatewaysSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UrlMapsAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for UrlMapsAggregatedList with ComputeUrlMapsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsAggregatedListArgs> for UrlMapsAggregatedList {
    fn generate_resource_id(&self, input: &ComputeUrlMapsAggregatedListArgs) -> String {
        format!("gcp::compute::UrlMapsAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::UrlMapsAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeUrlMapsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeUrlMapsDeleteArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.urlMap)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UrlMap
// =============================================================================

/// ResourceIdentifier implementation for UrlMap with ComputeUrlMapsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsGetArgs> for UrlMap {
    fn generate_resource_id(&self, input: &ComputeUrlMapsGetArgs) -> String {
        format!("gcp::compute::UrlMap/{}/{}", input.project, input.urlMap)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::UrlMap"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeUrlMapsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeUrlMapsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeUrlMapsInvalidateCacheArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsInvalidateCacheArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeUrlMapsInvalidateCacheArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.urlMap)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UrlMapList
// =============================================================================

/// ResourceIdentifier implementation for UrlMapList with ComputeUrlMapsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsListArgs> for UrlMapList {
    fn generate_resource_id(&self, input: &ComputeUrlMapsListArgs) -> String {
        format!("gcp::compute::UrlMapList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::UrlMapList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeUrlMapsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeUrlMapsPatchArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.urlMap)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeUrlMapsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeUrlMapsTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}",
            input.project, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeUrlMapsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeUrlMapsUpdateArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.urlMap)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UrlMapsValidateResponse
// =============================================================================

/// ResourceIdentifier implementation for UrlMapsValidateResponse with ComputeUrlMapsValidateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeUrlMapsValidateArgs> for UrlMapsValidateResponse {
    fn generate_resource_id(&self, input: &ComputeUrlMapsValidateArgs) -> String {
        format!(
            "gcp::compute::UrlMapsValidateResponse/{}/{}",
            input.project, input.urlMap
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::UrlMapsValidateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VpnGatewayAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for VpnGatewayAggregatedList with ComputeVpnGatewaysAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnGatewaysAggregatedListArgs> for VpnGatewayAggregatedList {
    fn generate_resource_id(&self, input: &ComputeVpnGatewaysAggregatedListArgs) -> String {
        format!("gcp::compute::VpnGatewayAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VpnGatewayAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeVpnGatewaysDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnGatewaysDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeVpnGatewaysDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.vpnGateway
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VpnGateway
// =============================================================================

/// ResourceIdentifier implementation for VpnGateway with ComputeVpnGatewaysGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnGatewaysGetArgs> for VpnGateway {
    fn generate_resource_id(&self, input: &ComputeVpnGatewaysGetArgs) -> String {
        format!(
            "gcp::compute::VpnGateway/{}/{}/{}",
            input.project, input.region, input.vpnGateway
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VpnGateway"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VpnGatewaysGetStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for VpnGatewaysGetStatusResponse with ComputeVpnGatewaysGetStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnGatewaysGetStatusArgs> for VpnGatewaysGetStatusResponse {
    fn generate_resource_id(&self, input: &ComputeVpnGatewaysGetStatusArgs) -> String {
        format!(
            "gcp::compute::VpnGatewaysGetStatusResponse/{}/{}/{}",
            input.project, input.region, input.vpnGateway
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VpnGatewaysGetStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeVpnGatewaysInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnGatewaysInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeVpnGatewaysInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VpnGatewayList
// =============================================================================

/// ResourceIdentifier implementation for VpnGatewayList with ComputeVpnGatewaysListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnGatewaysListArgs> for VpnGatewayList {
    fn generate_resource_id(&self, input: &ComputeVpnGatewaysListArgs) -> String {
        format!(
            "gcp::compute::VpnGatewayList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VpnGatewayList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeVpnGatewaysSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnGatewaysSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeVpnGatewaysSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestPermissionsResponse with ComputeVpnGatewaysTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnGatewaysTestIamPermissionsArgs> for TestPermissionsResponse {
    fn generate_resource_id(&self, input: &ComputeVpnGatewaysTestIamPermissionsArgs) -> String {
        format!(
            "gcp::compute::TestPermissionsResponse/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::TestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VpnTunnelAggregatedList
// =============================================================================

/// ResourceIdentifier implementation for VpnTunnelAggregatedList with ComputeVpnTunnelsAggregatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnTunnelsAggregatedListArgs> for VpnTunnelAggregatedList {
    fn generate_resource_id(&self, input: &ComputeVpnTunnelsAggregatedListArgs) -> String {
        format!("gcp::compute::VpnTunnelAggregatedList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VpnTunnelAggregatedList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeVpnTunnelsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnTunnelsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeVpnTunnelsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.vpnTunnel
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VpnTunnel
// =============================================================================

/// ResourceIdentifier implementation for VpnTunnel with ComputeVpnTunnelsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnTunnelsGetArgs> for VpnTunnel {
    fn generate_resource_id(&self, input: &ComputeVpnTunnelsGetArgs) -> String {
        format!(
            "gcp::compute::VpnTunnel/{}/{}/{}",
            input.project, input.region, input.vpnTunnel
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VpnTunnel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeVpnTunnelsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnTunnelsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeVpnTunnelsInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.region)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VpnTunnelList
// =============================================================================

/// ResourceIdentifier implementation for VpnTunnelList with ComputeVpnTunnelsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnTunnelsListArgs> for VpnTunnelList {
    fn generate_resource_id(&self, input: &ComputeVpnTunnelsListArgs) -> String {
        format!(
            "gcp::compute::VpnTunnelList/{}/{}",
            input.project, input.region
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VpnTunnelList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeVpnTunnelsSetLabelsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeVpnTunnelsSetLabelsArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeVpnTunnelsSetLabelsArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.region, input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeWireGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeWireGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeWireGroupsDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.crossSiteNetwork, input.wireGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WireGroup
// =============================================================================

/// ResourceIdentifier implementation for WireGroup with ComputeWireGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeWireGroupsGetArgs> for WireGroup {
    fn generate_resource_id(&self, input: &ComputeWireGroupsGetArgs) -> String {
        format!(
            "gcp::compute::WireGroup/{}/{}/{}",
            input.project, input.crossSiteNetwork, input.wireGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::WireGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeWireGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeWireGroupsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeWireGroupsInsertArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}",
            input.project, input.crossSiteNetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WireGroupList
// =============================================================================

/// ResourceIdentifier implementation for WireGroupList with ComputeWireGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeWireGroupsListArgs> for WireGroupList {
    fn generate_resource_id(&self, input: &ComputeWireGroupsListArgs) -> String {
        format!(
            "gcp::compute::WireGroupList/{}/{}",
            input.project, input.crossSiteNetwork
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::WireGroupList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeWireGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeWireGroupsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeWireGroupsPatchArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.crossSiteNetwork, input.wireGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeZoneOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZoneOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeZoneOperationsGetArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.operation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperationList
// =============================================================================

/// ResourceIdentifier implementation for OperationList with ComputeZoneOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZoneOperationsListArgs> for OperationList {
    fn generate_resource_id(&self, input: &ComputeZoneOperationsListArgs) -> String {
        format!(
            "gcp::compute::OperationList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::OperationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeZoneOperationsWaitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZoneOperationsWaitArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeZoneOperationsWaitArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.operation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeZoneVmExtensionPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZoneVmExtensionPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeZoneVmExtensionPoliciesDeleteArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.vmExtensionPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VmExtensionPolicy
// =============================================================================

/// ResourceIdentifier implementation for VmExtensionPolicy with ComputeZoneVmExtensionPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZoneVmExtensionPoliciesGetArgs> for VmExtensionPolicy {
    fn generate_resource_id(&self, input: &ComputeZoneVmExtensionPoliciesGetArgs) -> String {
        format!(
            "gcp::compute::VmExtensionPolicy/{}/{}/{}",
            input.project, input.zone, input.vmExtensionPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VmExtensionPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeZoneVmExtensionPoliciesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZoneVmExtensionPoliciesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeZoneVmExtensionPoliciesInsertArgs) -> String {
        format!("gcp::compute::Operation/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VmExtensionPolicyList
// =============================================================================

/// ResourceIdentifier implementation for VmExtensionPolicyList with ComputeZoneVmExtensionPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZoneVmExtensionPoliciesListArgs> for VmExtensionPolicyList {
    fn generate_resource_id(&self, input: &ComputeZoneVmExtensionPoliciesListArgs) -> String {
        format!(
            "gcp::compute::VmExtensionPolicyList/{}/{}",
            input.project, input.zone
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::VmExtensionPolicyList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ComputeZoneVmExtensionPoliciesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZoneVmExtensionPoliciesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &ComputeZoneVmExtensionPoliciesUpdateArgs) -> String {
        format!(
            "gcp::compute::Operation/{}/{}/{}",
            input.project, input.zone, input.vmExtensionPolicy
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Zone
// =============================================================================

/// ResourceIdentifier implementation for Zone with ComputeZonesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZonesGetArgs> for Zone {
    fn generate_resource_id(&self, input: &ComputeZonesGetArgs) -> String {
        format!("gcp::compute::Zone/{}/{}", input.project, input.zone)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::Zone"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ZoneList
// =============================================================================

/// ResourceIdentifier implementation for ZoneList with ComputeZonesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ComputeZonesListArgs> for ZoneList {
    fn generate_resource_id(&self, input: &ComputeZonesListArgs) -> String {
        format!("gcp::compute::ZoneList/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::compute::ZoneList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
